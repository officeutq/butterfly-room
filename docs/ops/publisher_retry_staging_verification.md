# 配信接続の有限再試行：ステージング反映記録（#1336 / #1340）

2026-09-17。利用者から「ステージングに反映してほしい。確認後にマージに進む」と指示を受け、未マージの#1341〜#1344を含む版を反映した。本番への反映・mainへのマージは行っていない。

## 反映した版と控え

- 対象：ステージングEC2 `i-00a52dfe98ba9a9a7`、DB `butterfly_room_staging`。
- 旧版：`6be2d15a6847c9e79866414b0323d26d47790fa2`。反映版：`5dadac488de4c2cd76aa4ffa3b283e21acb127dd`。反映版の[CI](https://github.com/officeutq/butterfly-room/actions/runs/35212375840)はquality・testとも成功。
- 新イメージ：`sha256:ba6ab2836d92c87d09b86dbd79555ebcc9a8dba9002984182f75ddd9d1ff219d`。旧イメージは`butterfly_room_staging:rollback-before-retry-1336-20260917`として保持。
- 控え：`/opt/butterfly-room/backups/publisher-retry-1336-20260917/`。DBの`before.dump`は553,741 bytes、SHA-256 `7e40e2a1f3bb89da4d43fd34ae5a44efff8f3e844534cf5edd4929977a2d98fc`。`pg_restore --list`に成功。復元は行っていない。
- ディレクトリ700・控え600で、旧版・旧イメージID・環境設定・既存未追跡ファイルのチェックサム・既存データ一覧を保存した。秘密値は本書やIssueに掲載していない。

## 反映と稼働確認

1. 配信中・離席中、直近視聴者、未解放配信接続がいずれも0件であることを確認した。
2. 依存定義・Dockerfile・公開ファイル・vendor・bin・Compose設定が旧版と同じことを確認し、稼働イメージを基に`app/config/db/lib/script`を更新した。CSSと静的資産を再生成し、ソースのSHA-256を照合した。
3. 旧Web・worker（定期処理）を停止し、DBがステージングであることと既存データを照合してから、`20260917000000`・`20260917010000`・`20260917020000`を適用した。追加3列と切断呼出枠テーブルを確認し、未適用migrationは0件。
4. Webは22:54:26 JST、workerは22:54:36 JSTに同じ新イメージで起動した。Webの200応答を確認してからworkerを起動し、22:54:55 JSTまでにHTTPS `/up`の200を確認した。
5. 反映前後で店舗・ブース・配信・接続・店舗所属・キャスト紐づけ・店舗台帳・ドリンク・ウォレット・入出金・コメント・キャスト招待の12種類の全列値が一致した。今回追加した接続3列は比較対象外。旧履歴の再補完やseed再投入は行っていない。環境設定と既存未追跡2ファイルも不変だった。
6. 22:56:39 JSTの確認で、通常切断の最大試行4回・待ち時間0.5秒／1秒／2秒、廃止した再確認経路の不在、状態取得・開始確認失敗記録の経路、運用用runnerの配置を確認した。
7. 現在のworkerコンテナに属するWorker・Dispatcher・Scheduler・Supervisorの生存通知と、失敗ジョブ0件を確認した。停止した旧Supervisorの記録が一時的に残っていたため、現在のコンテナの識別子で照合した。記録の手動削除は行っていない。
8. 22:56:51 JSTまでにHTTPSの`/up`・トップ・ログイン画面、新しい切断状態表示・強制終了通知用JavaScriptの200応答を確認した。

サーバー上の`verification.json`に適用DB変更、再試行設定、経路、新しい表示用JavaScriptのURL、定期処理の確認結果を保存した。今回のアプリコードに新しい環境変数・IAM権限は不要で、既存設定を維持した。

## 利用者の確認結果と残項目

2026-09-18、利用者からステージングで「配信開始・通常終了・強制終了は問題ない」「今回の操作によるエラーログは出ていない」と報告を受けた。追加回答の「PC・店舗管理者で配信・システム管理者で強制終了」を踏まえ、PCの店舗管理者による配信開始・通常終了、システム管理者による強制終了を確認済みとして記録する。

続いて、配信開始・通常終了・再配信後にPCのシステム管理者から強制終了する3操作を案内し、「iPhone15pro:キャスト、PCシステム管理者で正常でした。」との報告を受けた。iPhone 15 Proのキャストで、映像・音声、通常終了時のリザルト遷移、強制終了時の送信停止とリザルト遷移が正常だった結果として追記する。

さらに、スマートフォンの店舗管理者でも同じ3操作を確認するよう案内し、「スマホ・店舗管理者でも正常にできました。」との報告を受けた。配信開始・通常終了・再配信後のPCからの強制終了を確認済みとする。

店舗管理者のスマートフォン機種と、各ブラウザー名は今回未申告。PCのキャスト、システム管理者自身によるPC／スマートフォンでの配信操作は未確認のまま残す。ステージングへの異常応答の注入や、実利用者の配信・アカウントの変更は実施していない。

同日07:08:59 JSTの読み取り確認で、反映完了時刻の2026-09-17 22:56:51 JST以降のエラーログは0件。利用者が挙げた過去2件はともに`source=runner`で、当時の確認スクリプトの実行記録と一致した。

| 発生日時（JST） | エラーログ | 発生元・対応 |
| --- | --- | --- |
| 2026-09-17 22:50:18 | 10010 / `NameError` | 反映前データ確認用スクリプトで`CastInvitation`と誤記。実在する`StoreCastInvitation`へ修正し、データ一覧・控え取得をやり直して成功した |
| 2026-09-17 22:55:15 | 10011 / `SystemExit` | 起動確認スクリプトが停止済みの旧Supervisorの生存記録も判定し、`stale job process`で停止。現在のworkerコンテナに属する記録で判定するよう修正し、全4種類の稼働を確認した |

いずれも配信操作で発生したものではなく、確認スクリプト側の問題。既存エラーログは削除・アーカイブせず保持した。正常に成功した配信操作でエラーログが増えないことは想定どおりであり、意図的な失敗時のログ表示検証とは区別する。

役割・端末別の記録では、次の確認範囲を区別する。

- キャスト／店舗管理者／システム管理者の配信開始と通常終了。映像・音声が止まり、リザルトへ進むこと。
- 別ブラウザーからの強制終了。配信側が通知を受けて送信を止め、リザルトへ進むこと。
- 専用の再確認ボタンがなく、正常終了後に再度配信できること。PC・スマートフォンの差を記録する。

失敗・再試行の詳しい確認表は[検証記録](../design/publisher_retry_validation.md)、上限到達後の手順は[運用復旧](publisher_retry_recovery.md)を参照する。2026-09-18に利用者の指示で#1341〜#1344を順にマージし、main `be8da1c`のCIも成功した。ステージングの実行コードと差分はなく、本番は未反映。同日、専用ローカルDB・代替AWS応答による切断失敗からエラーログ画面・運用復旧・再配信までの5ケースも成功した。さらに開始確認の初回／各再試行／上限失敗と終了結果不明のブラウザー検証42ケースが成功し、こちらの残項目は解消した。実機の役割／端末別確認は前述の組合せが残るため、#1340・親EpicはIn Progressを維持する。

戻す必要が生じても、追加列や履歴を削除せず、旧workerの無期限再試行を再開しないよう[戻す場合の注意](publisher_retry_recovery.md#戻す場合)を確認する。
