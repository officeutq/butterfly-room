# ブース・店舗選択のステージング確認（#1277）

2026-09-16。ユーザーからステージングへの反映・検証まで実施する指示を受けた。本番への反映は対象外。[ローカル確認](current_selection_local_verification.md)と本書の確認を区別し、未実施の実機確認を完了扱いにしない。

## 反映した版・復旧用の控え

- 対象：ステージングEC2 `i-00a52dfe98ba9a9a7`、DB `butterfly_room_staging`。
- 旧版：`e92d55526b89e1fb905aca1388d3ad8b2f4d89f9`。反映版：`758f2c0a2cb495bb0c5b8ac4b86836dd193756f0`。反映版の[CI](https://github.com/officeutq/butterfly-room/actions/runs/35078922655)は成功。
- `Gemfile`・npm依存定義・DB・公開静的ファイル・配備設定に差分がないことを確認し、稼働イメージを基にアプリケーションの対象ディレクトリを置換した。削除済みの旧キャスト一覧もイメージから除去。CSS・静的資産を再生成し、ソースのSHA-256を照合した。
- 新イメージ：`sha256:095b665a7853e1199190389dfc8b7ec78b5d2f2bc8e1599391fbae037ff40634`。旧イメージは`butterfly_room_staging:rollback-before-selection-20260916`として保持。
- 控え：`/opt/butterfly-room/backups/selection-1277-20260916/`。DBの`before.dump`は535,096 bytes、SHA-256 `c70f343e89a0542f9625156831758747a2299eb80181a6ec7ce796e01f69bd64`。`pg_restore --list`成功。復元は実施していない。
- 環境設定、稼働版、旧イメージID、データ比較用一覧、既存未追跡ファイルのチェックサムも同じディレクトリに保存。ディレクトリ700、機密情報を含む控え600。認証情報をGitへ記載しない。

## 反映直後の確認

- 配信中・離席中・直近視聴者・未解放の配信接続が0件であることを反映直前に確認。
- Webは2026-09-16 18:37:32 JST、worker（定期処理）は18:37:45 JSTに同じ新イメージで起動。Webの正常応答を確認してからworkerを起動した。
- 起動中の一時的な接続エラーは解消し、18:38:01 JSTまでにHTTPS `/up`の200を確認。
- 両コンテナのソースSHA-256、旧一覧ファイルの削除、既存設定・未追跡ファイルの不変を確認。
- 反映前後の店舗・ブース・配信・接続・所属・台帳・ドリンク・ウォレット・コメントのデータ一覧が完全一致。DB構造変更、データ移行、旧履歴の再補完は行っていない。

## 操作の確認

検証専用の店舗19・20、現役ブース8・9、閉鎖済みブース10、専用利用者5人を使用。ブラウザーの合成映像・音声による実IVS接続と、物理端末・カメラ・マイクでの操作は区別する。検証用に利用者のポイントを変更せず、ドリンク購入・消化は実行しない。

ChromiumのPC幅1440px・スマートフォン幅390pxで、キャスト・店舗管理者・システム管理者の6組合せが成功した（18:49〜18:50 JST）。

- 情報・編集・配信履歴からヘッダーで選択を変更すると、対象とURL・ヘッダーが一致する。
- 未保存変更のキャンセルでは切替POSTを送らず入力を保持し、続行では変更先の編集画面へ進む。選択外の古い編集URLは409になる。
- 選択外の公開ブースは視聴へ進み、管理対象の選択は変わらない。選択中ではキャストが配信準備、管理者が視聴／配信の選択へ進む。
- 管理者は閉鎖済みブースを選択・維持でき、準備URLは情報へ戻る。履歴は閲覧できる。店舗の切替も確認した。閉鎖済み情報画面のボタン整理は後続#1292であり、本確認の完了対象へ含めない。

実行記録は作業端末の`tmp/selection-staging-result.json`。画面は`tmp/selection-staging-{cast,manager,system}-{1440,390}.png`。マニュアル用の画像をこれらの検証データで置き換えていない。

## 確認中に発見した修正（#1330）

配信準備A→Bで選択POSTがBを返しても、Bの準備URLで選択不一致となり、ヘッダーがAに戻る事象を確認した。視聴者数の定期取得が`Set-Cookie`を返しており、遅れた応答で切替前の選択を再保存できる。通信順序の記録は`tmp/selection-staging-race-result.json`。通常の再試行3回では再現せず、通信順序に依存する条件として扱う。

`StreamSessionsController#presence_summary`と`StreamSessions::PresencesController#ping`で選択の自動設定とログインsessionのCookie再発行を止める。認証・公開範囲・BAN・PresenceのDB処理は維持する。`presence_selection_cookie_test`では、AのCookieで開始した応答を保持したままBを選択し、遅れた応答がCookieを上書きできないことを確認する。修正前は視聴者数・生存通知の2件とも再発行を検出して失敗し、修正後は成功した。

- 修正版：`e69eaddd4994624a98bc6092ef4763001b95ac7f`、[PR #1331](https://github.com/officeutq/butterfly-room/pull/1331)。旧版からの実行コード差分は上記Controllerの2ファイルのみ。
- 修正後イメージ：`sha256:dd02c2a6e5411b98b404a4c49ba134a38b469c2be00b187a5122b9e8f1e17ecc`。Webは19:03:11 JST、workerは19:03:24 JSTに起動し、19:03:40 JSTまでにHTTPS 200を確認。
- ソースSHA-256、再反映前後のデータ一覧、設定・既存未追跡ファイルの不変を再確認。専用検証データの識別用ファイルを保護して引き継いだ。
- ローカルの関連Rails 33件・396項目成功。追加のBAN条件を含む新規テスト3件・17項目も成功。Ruby 3ファイルのRuboCop、文書リンク、差分形式の確認に成功。
- 修正版の[CI](https://github.com/officeutq/butterfly-room/actions/runs/35082693716)はquality・testとも成功。Rails 1,633件・14,425項目、JavaScript 188件が成功した。

## 修正版での実IVS確認

修正版でも3役割×2画面幅の切替確認6組合せを再実施し、すべて成功（19:04〜19:05 JST）。その後、合成映像・音声と実DeepAR／実IVSを使い、次の3組合せで一連の配信操作が成功した。

| 役割・画面幅 | 配信セッション | 準備作成者X | 記録された実配信者Y | 完了時刻（JST） |
| --- | --- | --- | --- | --- |
| キャスト・1440px | 19 | 35 | 36 | 19:07:24 |
| 店舗管理者・390px | 20 | 35 | 37 | 19:09:14 |
| システム管理者・1440px | 21 | 35 | 38 | 19:11:22 |

各組合せで、準備A→Bの映像トラック、Aの準備保持、視聴側への映像・音声到達、開始直後のヘッダー固定、古いヘッダーを持つ独立したログイン環境からの切替拒否、離席・復帰、終了後の選択維持と再切替を確認した。管理者では店舗変更の拒否も確認。Xは準備作成だけでは固定されず、Y配信中のBを選択できても準備には入れない。配信中の店舗非公開化は`Stores::UpdateService`で拒否され、各配信終了後の未解放接続は0件だった。

19:12:06 JST、準備画面を開いた後に店舗を非公開にし、開始操作の最初から「非公開店舗のブースでは配信準備・配信開始はできません」と表示されることを確認。準備は保持され、未解放接続は0件だった。検証後に専用店舗を公開へ戻し、19:12:20 JSTに一連の検証を完了した。

追加でA選択時のCookieを別のHTTP環境へ保存し、ブラウザー側だけBへ変更した後に古いCookieで視聴者数を取得した。応答は200で`Set-Cookie`なし、ブラウザーのB情報は200のまま。結果は`tmp/selection-staging-cookie-check-result.json`。本番には接続・反映していない。

## 検証データの整理・最終確認

- 専用ブース3件を`Booths::CloseAndArchiveService`で閉鎖し、店舗19・20を非公開、専用利用者5人を利用停止にした。検証用の準備18は正規の終了処理で終了。終了済み配信19・20・21、人物記録、接続3件を保持し、履歴を物理削除していない。検証店舗の売上は0pt。
- 専用2 Stageの接続中参加者0、DB全体の配信中・離席中0、未解放接続0を確認。
- 19:14:19 JSTまでの最終確認で、既存の店舗13件・ブース5件・配信17件・接続4件・所属19件・キャスト紐づけ3件・台帳2件・ドリンク4件・ウォレット5件・入出金9件・コメント8件が、検証開始前の全列値と一致した。
- Web・workerとも修正版で稼働し、実配信者制御フラグtrue、未適用migrationなし。定期処理の全種類で直近の生存通知を確認し、配信接続の切断再試行ジョブの失敗0。HTTPSの`/up`・トップ・ログイン画面は200。
- 結果はバックアップと同じサーバーディレクトリにも`browser-result.json`・`cookie-result.json`・`cleanup-result.json`として保存。ブラウザー結果のSHA-256は`fd78a50230c414dc94bdd9d6c25d0b406a3af8710bbedf23198f0ff828884693`。認証情報を含む検証データの識別用ファイルは権限600で保持する。

## 残る確認・対象外

- 利用者から報告されたキャスト・システム管理者／PCの実機確認は[ローカル記録](current_selection_local_verification.md)の範囲を維持する。
- 店舗管理者の物理カメラ・マイク、スマートフォン実機での主要操作と各役割の確認漏れは#1277に残す。今回のChromium・合成映像／音声・390px表示による確認で完了へ置き換えない。
- 同一アカウントの独立したブラウザー環境による検証は行うが、別の物理端末を追加で操作した結果とは扱わない。
- 本番反映、管理用店舗情報#1251〜#1253、ブース管理統合#1291〜#1294、配信開始途中のボタン整理は今回の対象外。#1255・#1277は開いたままとする。

## 追加の実機確認と開始確認の再試行（#1332／PR #1333）

- 利用者から、iPhone 15 Pro・Safari・システム管理者で、画面の追従、準備A→Bの映像・音声、配信中／離席中の切替禁止、終了後の再切替、モーダル・ボタン操作が問題ないと報告を受けた。
- 店舗管理者の「電波ブース」は、利用者の最終補足ではPCの開始は成功、同じiPhoneの1回目は確認エラー、2回目は成功。21:08:32 JSTにセッション24の開始確定が503となり接続12を取消、21:13:43 JSTには同じ準備で新しい接続13の開始が確定し、21:14:07 JSTに終了した。初回503の直接原因は既存ログだけでは断定しない。
- #1332で、開始確定の503 `publisher_state_unavailable`に限り、同じ要求ID・世代・SDK接続で0.5秒・1秒・2秒後に最大3回再確認する処理を追加。待機の取消、画面離脱、SDK退出、上限到達、権限・競合等の拒否を検証した。JavaScript全194件（配信処理45件）が成功し、[CI](https://github.com/officeutq/butterfly-room/actions/runs/35095551614)も成功。
- 21:28:31 JSTまでにステージングを`1880c700f99059cae67a982df209171fe9df73b1`へ更新。イメージは`sha256:4b98d1cbb3166cf9e253e36cadd833e49c1990810086c26f44424ce9ec9b14cf`。変更した実行コードは`publisher_connection.js`のみで静的資産を再生成。Web・workerのコードを照合し、環境設定・既存未追跡ファイル・反映前後の11種類のDBデータ一覧の不変とHTTPS 200を確認した。DB構造・IAMの変更、本番への反映はない。
- Chromiumの390px表示・店舗管理者・合成映像／音声・実DeepAR／実IVSで検証。ブラウザー側で最初の3回の開始確定応答を意図的に503とし、4回目は実サーバーへ送った。要求・世代・接続が1つのまま開始成功。要求間隔は850ms・1,365ms・2,328msで、取消は0回。これは一時失敗時の回復確認であり、最初に報告されたiPhoneのAWS応答を再現・特定したものではない。
- 別の開始では4回とも模擬503を返し、上限到達後に同じ要求の取消が1回実行されることを確認。実IVS接続は切断・解放され、準備は維持し、配信成功の記録を作らなかった。
- 21:31:04 JSTまでに専用ブース12を閉鎖、店舗21を非公開、利用者41を停止。配信26・27と接続2件を保持。専用Stageの接続中0、専用ブースの未解放接続0、売上0pt。利用者の「電波ブース」は変更していない。
- 実行結果は`tmp/publisher-retry-staging-result.json`。反映前のコード・イメージ・設定・データ一覧の控えは`/opt/butterfly-room/backups/publisher-retry-1332-20260916/`。修正版のiPhoneでの再確認、および#1277の残る役割・操作範囲は未完了として区別する。
