# キャスト招待後のチュートリアル修正のステージング確認（#1334）

2026-09-17。[PR #1335](https://github.com/officeutq/butterfly-room/pull/1335)。#1277のステージング確認中に、招待を承認したキャストへ店舗管理者向けの案内が表示される不具合を修正した。本番は未反映。

## 原因と確認範囲

`show_onboarding?` が「店舗管理者、または店舗の案内が未完了」で表示していた。また、`DashboardController#show` が管理権限を確認せず店舗の進捗を更新していた。店舗単位の進捗を保持する仕様は維持し、共通レイアウトの表示と `Stores::AdvanceOnboarding` の更新に同じ管理権限確認を追加した。

- 修正前に、新規キャスト承認後と、管理者ログアウト後のキャスト閲覧で誤表示する回帰テストの失敗を確認。
- 修正後の関連Railsテスト69件・649項目が成功。管理権限のない利用者・退会済み利用者による進捗更新拒否、管理者の継続、完了・スキップ済みの非表示、初回店舗設定・サンクスページも確認した。
- [実装版CI](https://github.com/officeutq/butterfly-room/actions/runs/35165723635)はquality・testとも成功。Rails 1,640件・14,467項目、JavaScript 194件が成功した。

## 反映した版

- ステージングEC2 `i-00a52dfe98ba9a9a7`、DB `butterfly_room_staging`。
- 旧版 `1880c700f99059cae67a982df209171fe9df73b1` → 修正版 `6be2d15a6847c9e79866414b0323d26d47790fa2`。
- 稼働イメージ `sha256:8d920360e0da5c054862c4e429264f6c1552089683e9ce71357155a55e4a0b57`。実行コードの差分はController 2件・Helper 1件・Service 1件だけ。依存定義・DB・静的資産・配備設定に差分なし。
- 09:16:52 JSTまでにWeb・workerの起動とHTTPS `/up`の200を確認。両コンテナのソースSHA-256を照合した。
- 配信中・離席中・未解放接続・直近視聴者が0件であることを反映前に確認。反映前後の店舗・ブース・配信・接続・所属・台帳・ドリンク・ウォレット・コメント・招待を含む12種類のデータ一覧が一致し、環境設定と既存未追跡ファイルも不変だった。
- 控えは `/opt/butterfly-room/backups/cast-onboarding-1334-20260917/`。旧イメージは `butterfly_room_staging:rollback-before-cast-onboarding-1334`。DB構造変更・データ移行・IAM変更は実施していない。

## ブラウザー確認

Chromium・390px幅のスマートフォン表示で、専用の非公開店舗22・管理者43・新規キャスト44・招待7・ブース14を使用した。iPhone実機の確認ではない。

1. 管理者でログインし、共有後の `go_dashboard_for_drinks` の案内が表示されることを確認。
2. 同じブラウザーで管理者をログアウトし、招待URLからキャストを新規登録・承認。プロフィール編集に進んでも案内が表示されない。
3. プロフィール保存→ブース編集・保存→ホーム→ダッシュボードで案内が表示されず、店舗の進捗が `go_dashboard_for_drinks` のまま。招待はキャストとして承認済みで、配信セッションは作成されない。
4. キャストからスキップを要求すると403で、進捗は変わらない。
5. 管理者へログインし直してダッシュボードへ進むと `setup_drinks` に進み、案内が再開する。スキップは200で成功し、再読込後も案内は非表示。

09:22:01 JSTのDB再読込で、専用ブース14の閉鎖、店舗22の非公開、利用者43・44の停止、専用配信セッション・未解放接続0を確認した。招待・所属等の履歴を物理削除していない。検証前から存在した12種類の店舗・配信・所属・金銭・招待等の全列値が検証前と一致し、Web・workerの修正版稼働とHTTPS 200も再確認した。

結果は作業端末の `tmp/cast-onboarding-staging-result.json`。検証スクリプトの整理直後のブース閉鎖判定は、古い関連オブジェクトを参照してfalseを記録したため、上記のDB再読込結果を追記した。アプリの閉鎖処理は成功している。

修正後のiPhone実機での再確認と、#1277の既存の未確認項目は残す。今回の修正だけで#1277を完了扱いにしない。
