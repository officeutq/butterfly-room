## 0) まず決める前提（ここではこう置く）

* **destroy は論理削除**：`enabled=false` に倒す（復活は update で enabled=true）
* `position` は **0開始のまま**（既存DBに合わせる）。表示は position 昇順 → id 昇順。

---

## 1) Model を追加

### `app/models/drink_item.rb`

```ruby
# frozen_string_literal: true

class DrinkItem < ApplicationRecord
  belongs_to :store

  validates :name, presence: true
  validates :price_points, numericality: { only_integer: true, greater_than: 0 }
  validates :position, numericality: { only_integer: true }

  scope :enabled_only, -> { where(enabled: true) }
  scope :ordered, -> { order(:position, :id) }
end
```

### `app/models/store.rb` に関連追加

```ruby
# frozen_string_literal: true

class Store < ApplicationRecord
  has_many :booths, dependent: :destroy
  has_many :store_memberships, dependent: :destroy
  has_many :drink_items, dependent: :destroy
end
```

### `app/models/store_membership.rb`
```ruby
# frozen_string_literal: true

class StoreMembership < ApplicationRecord
  belongs_to :store
  belongs_to :user

  enum :membership_role, { cast: 0, admin: 1 }

  scope :admin_only, -> { where(membership_role: membership_roles[:admin]) }
end
```
---

## 3) Admin::DrinkItemsController を実装

routes は既にあるので controller を追加するだけです。

### `app/controllers/admin/drink_items_controller.rb`

```ruby
# frozen_string_literal: true

module Admin
  class DrinkItemsController < BaseController
    before_action :require_current_store!
    before_action :set_drink_item, only: %i[update destroy]

    def index
      @drink_item = current_store.drink_items.new(enabled: true, position: default_position)
      @drink_items = current_store.drink_items.ordered
    end

    def create
      @drink_item = current_store.drink_items.new(drink_item_params)
      if @drink_item.save
        redirect_to admin_drink_items_path, notice: "作成しました"
      else
        @drink_items = current_store.drink_items.ordered
        render :index, status: :unprocessable_entity
      end
    end

    def update
      if @drink_item.update(drink_item_params)
        redirect_to admin_drink_items_path, notice: "更新しました"
      else
        @drink_items = current_store.drink_items.ordered
        @drink_item = current_store.drink_items.new(enabled: true, position: default_position)
        render :index, status: :unprocessable_entity
      end
    end

    def destroy
      @drink_item.update!(enabled: false) # 論理削除
      redirect_to admin_drink_items_path, notice: "無効にしました"
    end

    private

    def set_drink_item
      @drink_item = current_store.drink_items.find(params[:id])
    end

    def drink_item_params
      params.require(:drink_item).permit(:name, :price_points, :position, :enabled)
    end

    def default_position
      last = current_store.drink_items.maximum(:position)
      last.present? ? last + 1 : 0
    end
  end
end
```

### Admin::BaseController に current_store を追加（確定版）
`app/controllers/admin/base_controller.rb`
```ruby
module Admin
  class BaseController < ApplicationController
    before_action -> { require_role!(:store_admin, :system_admin) }

    private

    def current_store
      return @current_store if defined?(@current_store)

      membership =
        StoreMembership
          .includes(:store)
          .where(user_id: current_user.id, membership_role: StoreMembership.membership_roles[:admin])
          .order(:id)
          .first

      @current_store = membership&.store
    end
    helper_method :current_store

    def require_current_store!
      return if current_store.present?

      head :forbidden
    end
  end
end
```
---

## 4) View（最小）を追加

### `app/views/admin/drink_items/index.html.erb`

Bootstrap 5 前提の簡素版です。

```erb
<h1>ドリンクメニュー</h1>

<% if flash[:notice].present? %>
  <div class="alert alert-success"><%= flash[:notice] %></div>
<% end %>

<div class="card mb-4">
  <div class="card-header">新規作成</div>
  <div class="card-body">
    <%= render "form", drink_item: @drink_item, url: admin_drink_items_path, method: :post %>
  </div>
</div>

<table class="table align-middle">
  <thead>
    <tr>
      <th>ID</th>
      <th>表示順</th>
      <th>名前</th>
      <th>価格(pt)</th>
      <th>有効</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @drink_items.each do |item| %>
      <tr class="<%= "text-muted" unless item.enabled %>">
        <td><%= item.id %></td>
        <td><%= item.position %></td>
        <td><%= item.name %></td>
        <td><%= item.price_points %></td>
        <td>
          <%= form_with model: [:admin, item], method: :patch, class: "d-inline" do |f| %>
            <%= f.hidden_field :enabled, value: (!item.enabled).to_s %>
            <%= f.submit(item.enabled ? "有効" : "無効", class: "btn btn-sm #{item.enabled ? "btn-success" : "btn-outline-secondary"}") %>
          <% end %>
        </td>
        <td class="text-end">
          <button class="btn btn-sm btn-outline-primary" type="button" data-bs-toggle="collapse" data-bs-target="#edit-<%= item.id %>">
            編集
          </button>

          <%= button_to "無効化", admin_drink_item_path(item), method: :delete,
                class: "btn btn-sm btn-outline-danger",
                form: { data: { turbo_confirm: "無効にしますか？" } } %>
        </td>
      </tr>

      <tr class="collapse" id="edit-<%= item.id %>">
        <td colspan="6">
          <div class="card card-body">
            <%= render "form", drink_item: item, url: admin_drink_item_path(item), method: :patch %>
          </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
```

### `app/views/admin/drink_items/_form.html.erb`

```erb
<%= form_with model: drink_item, url: url, method: method do |f| %>
  <% if drink_item.errors.any? %>
    <div class="alert alert-danger">
      <ul class="mb-0">
        <% drink_item.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row g-3">
    <div class="col-md-5">
      <%= f.label :name, class: "form-label" %>
      <%= f.text_field :name, class: "form-control" %>
    </div>

    <div class="col-md-3">
      <%= f.label :price_points, class: "form-label" %>
      <%= f.number_field :price_points, min: 1, class: "form-control" %>
    </div>

    <div class="col-md-2">
      <%= f.label :position, class: "form-label" %>
      <%= f.number_field :position, class: "form-control" %>
    </div>

    <div class="col-md-2 d-flex align-items-end">
      <div class="form-check">
        <%= f.check_box :enabled, class: "form-check-input" %>
        <%= f.label :enabled, class: "form-check-label" %>
      </div>
    </div>

    <div class="col-12">
      <%= f.submit "保存", class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
```

---

## 5) 視聴画面（booths#show）で enabled のみ表示

### `app/controllers/booths_controller.rb` に追加

```ruby
class BoothsController < ApplicationController
  def show
    @booth = Booth.find(params[:id])
    @stream_session = @booth.current_stream_session

    @drink_items = @booth.store.drink_items.enabled_only.ordered

    @comments =
      if @stream_session.present?
        Comment.alive.where(stream_session: @stream_session)
              .order(created_at: :desc)
              .limit(50)
              .reverse
      else
        []
      end
  end
end
```

### `app/views/booths/show.html.erb` に表示（スタブでOK）

`<hr>` の下あたりに追加：

```erb
<hr>

<h2>ドリンクメニュー（enabledのみ）</h2>
<% if @drink_items.any? %>
  <ul>
    <% @drink_items.each do |item| %>
      <li><%= item.name %>（<%= item.price_points %>pt）</li>
    <% end %>
  </ul>
<% else %>
  <p>現在、表示できるドリンクがありません</p>
<% end %>
```

---

## 6) 動作確認用ビュー

### `app/views/home/show.html.erb`
```erb
<h1>home (stub)</h1>

<ul>
  <li><%= link_to "Public booth show (id=1)", booth_path(1) %></li>
  <li><%= link_to "Cast booths", cast_booths_path %></li>
  <li><%= link_to "Admin booths", admin_booths_path %></li>

  <% if user_signed_in? && (current_user.store_admin? || current_user.system_admin?) %>
    <li><%= link_to "Admin drink items", admin_drink_items_path %></li>
  <% end %>
</ul>

<hr>

<% if user_signed_in? %>
  <p>ログイン中: <%= current_user.email %>（role: <%= current_user.role %>）</p>
  <%= button_to "ログアウト", destroy_user_session_path, method: :delete %>
<% else %>
  <%= link_to "ログイン", new_user_session_path %>
<% end %>
```

---

## 6) “即反映”について（この構成で満たす）

* 視聴画面が毎回 `@booth.store.drink_items.enabled_only.ordered` を引くので、**更新→リロードで即反映**します。
* もし将来「視聴画面は常時開きっぱなしで、管理画面更新が自動反映される」が欲しければ、Turbo Stream 配信やポーリングが追加になりますが、Issue本文のニュアンス的には **まずは即時DB反映で十分**だと思います。

Closes #18 
Relates to #12 
