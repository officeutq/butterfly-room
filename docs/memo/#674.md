では、そのまま貼れる形でまとめます。
今回は **安定重視: `rails-i18n` + `config/locales/ja.yml` + `config/locales/devise.ja.yml`** で進める前提です。

---

## 1. Gemfile

`rails-i18n` を追加します。

```ruby
gem "rails-i18n"
```

追加後、インストールです。

```bash
docker compose run --rm app bundle install
```

---

## 2. config/application.rb

`default_locale` を追加します。
今の内容にこれを足してください。

```ruby
require_relative "boot"

require "rails/all"

Bundler.require(*Rails.groups)

module App
  class Application < Rails::Application
    config.load_defaults 8.1

    config.i18n.default_locale = :ja

    # Propshaft の assets load path を早い段階で確定（testでも効かせる）
    config.assets.paths << Rails.root.join("app/assets/builds")
    config.assets.paths << Rails.root.join("node_modules")
    config.assets.paths << Rails.root.join("vendor/assets")
    config.assets.paths << Rails.root.join("vendor/assets/stylesheets")

    config.autoload_lib(ignore: %w[assets tasks])
  end
end
```

---

## 3. config/locales/ja.yml

新規作成です。
まずは今回の Issue に必要な範囲をしっかり押さえた版にしています。

```yml
ja:
  activerecord:
    models:
      user: ユーザー
      store: 店舗
      booth: ブース
      drink_item: ドリンク
      store_admin_invitation: 店舗管理者招待
      store_cast_invitation: キャスト招待

    attributes:
      user:
        email: メールアドレス
        password: パスワード
        password_confirmation: パスワード（確認）
        bio: 自己紹介
        role: 権限

      store:
        name: 店舗名
        description: 店舗説明
        area: エリア
        business_type: 業種
        referral_code: 紹介コード

      booth:
        name: ブース名
        description: ブース説明
        status: 配信状態

      drink_item:
        name: ドリンク名
        price_points: ポイント
        position: 表示順
        icon_key: アイコン

      store_admin_invitation:
        expires_at: 有効期限
        token_digest: トークン

      store_cast_invitation:
        expires_at: 有効期限
        token_digest: トークン

  activemodel:
    models:
      customers/registration_form: 顧客登録
      casts/registration_form: キャスト登録
      store_admins/registration_form: 店舗管理者登録
      stores/registration_form: 店舗登録

    attributes:
      customers/registration_form:
        email: メールアドレス
        password: パスワード
        password_confirmation: パスワード（確認）

      casts/registration_form:
        email: メールアドレス
        password: パスワード
        password_confirmation: パスワード（確認）

      store_admins/registration_form:
        email: メールアドレス
        password: パスワード
        password_confirmation: パスワード（確認）

      stores/registration_form:
        store_name: 店舗名
        email: メールアドレス
        password: パスワード
        password_confirmation: パスワード（確認）
        referral_code: 紹介コード

  errors:
    format: "%{attribute}%{message}"
```

ポイントは `activemodel.attributes` に form object を入れていることです。
これで `@form.errors.full_messages` でも自然な日本語になります。

---

## 4. config/locales/devise.ja.yml

これも新規作成です。

```yml
ja:
  devise:
    failure:
      already_authenticated: すでにログインしています
      inactive: アカウントが有効化されていません
      invalid: "%{authentication_keys}またはパスワードが正しくありません"
      locked: アカウントはロックされています
      last_attempt: 残り1回でアカウントがロックされます
      not_found_in_database: "%{authentication_keys}またはパスワードが正しくありません"
      timeout: セッションの有効期限が切れました。再度ログインしてください
      unauthenticated: ログインまたは新規登録をしてください
      unconfirmed: メールアドレスの確認が必要です

    sessions:
      signed_in: ログインしました
      signed_out: ログアウトしました
      already_signed_out: すでにログアウトしています

    passwords:
      no_token: パスワード再設定メールのURLからアクセスしてください
      send_instructions: パスワード再設定方法を記載したメールを送信しました
      send_paranoid_instructions: 該当するメールアドレスが存在する場合、パスワード再設定方法を記載したメールを送信しました
      updated: パスワードを変更しました。現在ログイン中です
      updated_not_active: パスワードを変更しました

    confirmations:
      confirmed: メールアドレスを確認しました
      send_instructions: メールアドレス確認用のメールを送信しました
      send_paranoid_instructions: 該当するメールアドレスが存在する場合、メールアドレス確認用のメールを送信しました

    registrations:
      destroyed: アカウントを削除しました。ご利用ありがとうございました
      signed_up: アカウント登録が完了しました
      signed_up_but_inactive: アカウント登録は完了しましたが、まだ有効化されていないためログインできません
      signed_up_but_locked: アカウント登録は完了しましたが、アカウントがロックされているためログインできません
      signed_up_but_unconfirmed: 確認用メールを送信しました。メール内のリンクから有効化してください
      update_needs_confirmation: アカウント情報を更新しました。新しいメールアドレスの確認が必要です
      updated: アカウント情報を更新しました
      updated_but_not_signed_in: アカウント情報を更新しました。パスワード変更のため再ログインしてください

    unlocks:
      send_instructions: アカウントのロック解除方法を記載したメールを送信しました
      send_paranoid_instructions: 該当するアカウントが存在する場合、ロック解除方法を記載したメールを送信しました
      unlocked: アカウントのロックを解除しました。ログインしてください

  errors:
    messages:
      already_confirmed: はすでに確認済みです
      confirmation_period_expired: は%{period}以内に確認が必要です。再度リクエストしてください
      expired: の有効期限が切れています。再度リクエストしてください
      not_found: が見つかりません
      not_locked: はロックされていません
      not_saved:
        one: "%{resource}を保存できませんでした。1件のエラーがあります"
        other: "%{resource}を保存できませんでした。%{count}件のエラーがあります"
```

---

## 5. 各 form object の修正

手書き文言をやめて、I18n に寄せます。
`errors.add(:email, :taken)` に変えるのが重要です。

---

### app/forms/customers/registration_form.rb

```ruby
# frozen_string_literal: true

module Customers
  class RegistrationForm
    include ActiveModel::Model
    include ActiveModel::Attributes

    attribute :email, :string
    attribute :password, :string
    attribute :password_confirmation, :string

    attr_reader :user

    validates :email, presence: true
    validates :password, presence: true, confirmation: true
    validates :password_confirmation, presence: true
    validate  :email_must_be_unique

    def save
      return false unless valid?

      result = Customers::RegisterCustomer.call!(
        email: email,
        password: password
      )

      @user = result.user
      true
    rescue ActiveRecord::RecordInvalid => e
      e.record.errors.each do |error|
        errors.add(error.attribute, error.type, **error.options.except(:message))
      end
      false
    end

    private

    def email_must_be_unique
      return if email.blank?
      return unless User.exists?(email: email)

      errors.add(:email, :taken)
    end
  end
end
```

---

### app/forms/casts/registration_form.rb

```ruby
# frozen_string_literal: true

module Casts
  class RegistrationForm
    include ActiveModel::Model
    include ActiveModel::Attributes

    attribute :email, :string
    attribute :password, :string
    attribute :password_confirmation, :string

    attr_reader :user

    validates :email, presence: true
    validates :password, presence: true, confirmation: true
    validates :password_confirmation, presence: true
    validate  :email_must_be_unique

    def save
      return false unless valid?

      result = Casts::RegisterCast.call!(
        email: email,
        password: password
      )

      @user = result.user
      true
    rescue ActiveRecord::RecordInvalid => e
      e.record.errors.each do |error|
        errors.add(error.attribute, error.type, **error.options.except(:message))
      end
      false
    end

    private

    def email_must_be_unique
      return if email.blank?
      return unless User.exists?(email: email)

      errors.add(:email, :taken)
    end
  end
end
```

---

### app/forms/store_admins/registration_form.rb

```ruby
# frozen_string_literal: true

module StoreAdmins
  class RegistrationForm
    include ActiveModel::Model
    include ActiveModel::Attributes

    attribute :email, :string
    attribute :password, :string
    attribute :password_confirmation, :string

    attr_reader :user

    validates :email, presence: true
    validates :password, presence: true, confirmation: true
    validates :password_confirmation, presence: true
    validate  :email_must_be_unique

    def save
      return false unless valid?

      result = StoreAdmins::RegisterStoreAdmin.call!(
        email: email,
        password: password
      )

      @user = result.user
      true
    rescue ActiveRecord::RecordInvalid => e
      e.record.errors.each do |error|
        errors.add(error.attribute, error.type, **error.options.except(:message))
      end
      false
    end

    private

    def email_must_be_unique
      return if email.blank?
      return unless User.exists?(email: email)

      errors.add(:email, :taken)
    end
  end
end
```

---

### app/forms/stores/registration_form.rb

`referral_code` だけは独自文言が必要なので、ここは I18n キーを切るか、当面メッセージ指定でもよいです。
今回は I18n に寄せる前提で `:invalid` を使います。

```ruby
# frozen_string_literal: true

module Stores
  class RegistrationForm
    include ActiveModel::Model
    include ActiveModel::Attributes

    attribute :store_name, :string
    attribute :email, :string
    attribute :password, :string
    attribute :password_confirmation, :string
    attribute :referral_code, :string

    attr_reader :user, :store

    validates :store_name, presence: true
    validates :email, presence: true
    validates :password, presence: true, confirmation: true
    validates :password_confirmation, presence: true
    validates :referral_code, presence: true
    validate  :referral_code_must_be_usable
    validate  :email_must_be_unique

    def save
      return false unless valid?

      result = Stores::RegisterStoreAdmin.call!(
        store_name: store_name,
        email: email,
        password: password,
        referral_code: referral_code
      )

      @store = result.store
      @user  = result.user
      true
    rescue ActiveRecord::RecordInvalid => e
      e.record.errors.each do |error|
        errors.add(error.attribute, error.type, **error.options.except(:message))
      end
      false
    end

    private

    def referral_code_must_be_usable
      rc = ReferralCode.find_by(code: referral_code)
      return if rc.present? && rc.usable?

      errors.add(:referral_code, :invalid)
    end

    def email_must_be_unique
      return if email.blank?
      return unless User.exists?(email: email)

      errors.add(:email, :taken)
    end
  end
end
```

---

## 6. フォーム view は基本そのままでOK

今の

```erb
<% @form.errors.full_messages.each do |msg| %>
```

はそのままで大丈夫です。
今回の修正で、ここが日本語として自然に出るようになります。

---

## 7. 動作確認ポイント

最低限これを見れば大丈夫です。

### 顧客登録

* メール空欄
* パスワード空欄
* パスワード確認不一致
* 既存メールアドレス

### 店舗登録

* 店舗名空欄
* 紹介コード空欄
* 無効な紹介コード
* 既存メールアドレス

### Devise ログイン

* 存在しないメールアドレス
* パスワード間違い
* ログイン成功
* ログアウト成功

---

## 8. テスト実行

Rails タスクはいつもどおり `docker compose` 経由です。

全体を軽く見るなら:

```bash
docker compose run --rm app bin/rails test
```

認証・登録まわりを重点確認するなら、関連 test を個別に回してください。

---

## 9. 補足

`findstr` の結果で `cast/booths_controller.rb` に文字化けした flash が見えているので、
これは **今回の I18n とは別に、UTF-8 で開き直して日本語文言を修正** したほうがよいです。

これは locale で直る問題ではなく、ファイル自体の文字化けです。

---

必要なら次に、**この変更を前提にしたコミット単位の分け方** も出します。
