了解、GO ですね。
最終実装プランと、**そのままコピペできる差分コード**を出します。

---

# ✅ 実装方針（最小差分）

やることは4点です。

1. **フッター購入に `return_to` を付ける**
2. **CreateCheckoutService に `return_to` を通す**
3. **checkout/return で `return_to` を安全に検証して優先利用**
4. **booth/show の残高表示＆購入UI撤去 → ヘッダーへ wallet購読移設**

---

# ① フッター購入に return_to を付与

## `app/views/layouts/application.html.erb`

フッターの購入ボタンを修正：

```erb
<%= button_to "購入(1000pt)",
      wallet_purchases_path,
      method: :post,
      params: {
        points: 1000,
        return_to: request.fullpath
      },
      form: { data: { turbo: false } },
      class: "btn btn-sm btn-outline-primary" %>
```

---

# ② Wallet::PurchasesController に return_to を渡す

## `app/controllers/wallet/purchases_controller.rb`

```ruby
def create
  checkout_url = Wallets::CreateCheckoutService.new(
    customer_user: current_user,
    points: params.require(:points).to_i,
    booth_id: nil, # 戻り先は return_to で管理
    return_to: params[:return_to],
    base_url: request.base_url
  ).call!

  redirect_to checkout_url, allow_other_host: true
end
```

---

# ③ CreateCheckoutService を拡張

## `app/services/wallets/create_checkout_service.rb`

### initialize 変更

```ruby
def initialize(customer_user:, points:, booth_id:, return_to:, base_url:)
  @customer_user = customer_user
  @points = points
  @booth_id = booth_id
  @return_to = return_to
  @base_url = base_url
end
```

### success_url / cancel_url 変更

```ruby
success_url: build_return_url(
  status: "success",
  purchase_id: purchase.id,
  return_to: @return_to
),
cancel_url: build_return_url(
  status: "cancel",
  purchase_id: purchase.id,
  return_to: @return_to
),
```

### build_return_url 変更

```ruby
def build_return_url(status:, purchase_id:, return_to:)
  path = Rails.application.routes.url_helpers.checkout_return_path(
    status: status,
    purchase_id: purchase_id,
    return_to: return_to
  )
  "#{@base_url}#{path}"
end
```

---

# ④ CheckoutController で安全な return_to を優先

## `app/controllers/checkout_controller.rb`

```ruby
def return
  @status = params[:status].to_s
  @return_to = safe_return_path(params[:return_to])

  @wallet = Wallet.find_by(customer_user_id: current_user.id)

  render :return
end

private

def safe_return_path(path)
  return nil if path.blank?
  return nil unless path.start_with?("/")
  return nil if path.start_with?("//")

  path
end
```

---

# ⑤ checkout/return ビュー修正

## `app/views/checkout/return.html.erb`

戻りリンク部分を変更：

```erb
<% if @return_to.present? %>
  <%= link_to "元のページへ戻る", @return_to, class: "btn btn-primary" %>
<% else %>
  <%= link_to "トップへ戻る", root_path, class: "btn btn-outline-secondary" %>
<% end %>
```

---

# ⑥ booth/show から残高＆購入UI撤去

## `app/views/booths/show.html.erb`

以下ブロックを **削除**：

* turbo_stream_from [current_user, :wallet]
* wallet_balance の箱
* 購入ボタン一式

（残すのは stream_state / お気に入り / 退室のみ）

---

# ⑦ ヘッダーへ wallet購読を移設

## `app/views/layouts/application.html.erb`

ヘッダー内の points 表示を変更：

### 変更前

```erb
<span class="text-muted small">
  points: <strong><%= layout_wallet_points %>pt</strong>
</span>
```

### 変更後

```erb
<% if user_signed_in? %>
  <%= turbo_stream_from [current_user, :wallet] %>

  <span class="text-muted small" id="wallet_balance">
    <%= render "wallets/balance", wallet: current_user.wallet %>
  </span>
<% end %>
```

---

# ✅ これで達成されること

* フッター購入 → 元いたページへ戻る
* open redirect 対策あり
* booth/show 依存排除
* ヘッダー残高がリアルタイム更新
* webhook フロー無変更
* booth_id 依存除去（将来削除可能）

---

# 🔎 動作確認チェックリスト

1. boothページから購入 → return → boothへ戻る
2. rootから購入 → return → rootへ戻る
3. Stripe webhook後、ページ遷移なしでヘッダー残高更新
4. return_to に `https://evil.com` を入れても root にフォールバック

---

# 🚀 次のステップ（任意）

* `WalletPurchase` の `booth_id` を将来的に削除するか検討
* return 成功時は 3秒自動遷移を入れるとUX向上

---

必要なら、
✔ コミットメッセージ案
✔ PR本文（変更点/仕様/動作確認）

も出します。
