了解。
既存互換を維持しつつ、Issue #537 の要求を満たす実装案をそのまま貼れる形でまとめます。 

まずマイグレーションは空で作ってから書き換える前提で、コマンドはこれです。

```bash
docker compose exec app bin/rails generate migration AddKindAndMetadataToComments
```

以下、各ファイルの完成版です。

---

## 1. migration

`db/migrate/XXXXXXXXXXXXXX_add_kind_and_metadata_to_comments.rb`

```ruby
# frozen_string_literal: true

class AddKindAndMetadataToComments < ActiveRecord::Migration[8.0]
  def change
    add_column :comments, :kind, :string, null: false, default: "chat"
    add_column :comments, :metadata, :jsonb, null: false, default: {}

    change_column_null :comments, :body, true
  end
end
```

---

## 2. Comment モデル

`app/models/comment.rb`

```ruby
class Comment < ApplicationRecord
  KIND_CHAT = "chat"
  KIND_DRINK = "drink"
  KIND_ENTRY = "entry"
  KIND_EXIT = "exit"
  KIND_SYSTEM = "system"

  KINDS = [
    KIND_CHAT,
    KIND_DRINK,
    KIND_ENTRY,
    KIND_EXIT,
    KIND_SYSTEM
  ].freeze

  belongs_to :stream_session
  belongs_to :booth
  belongs_to :user

  scope :alive, -> { where(deleted_at: nil) }

  before_validation :normalize_kind
  before_validation :normalize_body
  before_validation :normalize_metadata

  validates :kind, presence: true, inclusion: { in: KINDS }
  validates :body, length: { maximum: 200 }, allow_nil: true
  validates :body, presence: true, if: :chat?

  def chat?
    kind == KIND_CHAT
  end

  def metadata_hash
    metadata.is_a?(Hash) ? metadata : {}
  end

  private

  def normalize_kind
    self.kind = kind.to_s.strip.presence || KIND_CHAT
  end

  def normalize_body
    self.body = body.to_s.strip.presence
  end

  def normalize_metadata
    self.metadata =
      case metadata
      when ActionController::Parameters
        metadata.to_unsafe_h
      when Hash
        metadata
      when nil
        {}
      else
        metadata.respond_to?(:to_h) ? metadata.to_h : {}
      end
  rescue TypeError, NoMethodError
    self.metadata = {}
  end
end
```

---

## 3. CreateService

`app/services/stream_sessions/comments/create_service.rb`

```ruby
# frozen_string_literal: true

module StreamSessions
  module Comments
    class CreateService
      class RateLimitedError < StandardError; end
      class BoothNotLiveError < StandardError; end

      def initialize(stream_session:, user:, body: nil, kind: Comment::KIND_CHAT, metadata: {})
        @stream_session = stream_session
        @user = user
        @body = body
        @kind = kind.to_s.strip.presence || Comment::KIND_CHAT
        @metadata = normalize_metadata(metadata)
      end

      def call
        raise BoothNotLiveError unless booth_allows_comments?
        raise RateLimitedError if chat? && rate_limited?

        comment = Comment.create!(
          stream_session: @stream_session,
          booth_id: @stream_session.booth_id,
          user: @user,
          body: @body,
          kind: @kind,
          metadata: @metadata
        )

        CommentNotifier.append(comment)
        comment
      end

      private

      def booth_allows_comments?
        booth = @stream_session.booth
        booth.live? || booth.away?
      end

      def rate_limited?
        Comment.where(user_id: @user.id, stream_session_id: @stream_session.id)
               .where("created_at >= ?", 1.second.ago)
               .exists?
      end

      def chat?
        @kind == Comment::KIND_CHAT
      end

      def normalize_metadata(metadata)
        case metadata
        when ActionController::Parameters
          metadata.to_unsafe_h
        when Hash
          metadata
        when nil
          {}
        else
          metadata.respond_to?(:to_h) ? metadata.to_h : {}
        end
      rescue TypeError, NoMethodError
        {}
      end
    end
  end
end
```

---

## 4. コメント partial

`app/views/comments/_comment.html.erb`

```erb
<div class="comment comment-item">
  <% case comment.kind %>
  <% when Comment::KIND_CHAT %>
    <div class="comment-avatar">
      <%= user_avatar_badge(comment.user, size: 32, klass: "comment-avatar-badge") %>
    </div>

    <div class="comment-text">
      <span class="comment-author"><%= display_name_or_anonymous(comment.user) %></span>
      <span class="comment-body"><%= comment.body %></span>
    </div>
  <% else %>
    <div class="comment-avatar">
      <%= user_avatar_badge(comment.user, size: 32, klass: "comment-avatar-badge") %>
    </div>

    <div class="comment-text">
      <span class="comment-author"><%= display_name_or_anonymous(comment.user) %></span>
      <span class="comment-body">
        <%= comment.body.presence || "イベントが発生しました" %>
      </span>
    </div>
  <% end %>
</div>
```

この形なら、今は `chat` をそのまま維持しつつ、後続 Issue で `when Comment::KIND_DRINK` などを安全に追加できます。

---

## 5. controller

`app/controllers/stream_sessions/comments_controller.rb`

```ruby
# frozen_string_literal: true

module StreamSessions
  class CommentsController < ApplicationController
    include StoreBanGuard

    before_action :authenticate_user!
    before_action :set_stream_session
    before_action :reject_banned_customer_for_stream_session!

    def create
      policy = Authorization::ViewerPolicy.new(current_user, @stream_session)
      head :forbidden and return unless policy.create_comment?

      StreamSessions::Comments::CreateService.new(
        stream_session: @stream_session,
        user: current_user,
        body: params.require(:comment).fetch(:body)
      ).call

      respond_to do |format|
        format.turbo_stream do
          render turbo_stream: turbo_stream.replace(
            "comment_form",
            partial: "stream_sessions/comments/form",
            formats: [ :html ],
            locals: { stream_session: @stream_session }
          )
        end
        format.html { redirect_back fallback_location: root_path }
      end
    rescue StreamSessions::Comments::CreateService::BoothNotLiveError
      render_comment_form_error("配信中（live/away）のときのみコメントできます", status: :conflict)
    rescue StreamSessions::Comments::CreateService::RateLimitedError
      render_comment_form_error("送信が速すぎます。少し待ってからもう一度送信してください", status: :unprocessable_entity)
    rescue ActiveRecord::RecordInvalid => e
      message = e.record.errors.full_messages.to_sentence.presence || "入力が不正です"
      render_comment_form_error(message, status: :unprocessable_entity)
    rescue ActionController::ParameterMissing, KeyError
      render_comment_form_error("入力が不正です", status: :unprocessable_entity)
    end

    private

    def set_stream_session
      @stream_session = StreamSession.find(params[:stream_session_id])
    end

    def reject_banned_customer_for_stream_session!
      reject_banned_customer!(store: @stream_session.store)
    end

    def render_comment_form_error(message, status:)
      respond_to do |format|
        format.turbo_stream do
          render(
            turbo_stream: turbo_stream.replace(
              "comment_form",
              partial: "stream_sessions/comments/form",
              formats: [ :html ],
              locals: { stream_session: @stream_session, error_message: message }
            ),
            status: status
          )
        end
        format.html { redirect_back fallback_location: root_path, alert: message }
      end
    end
  end
end
```

`ActiveRecord::RecordInvalid` を拾うようにしたのがポイントです。
これで、将来 `chat` 以外を使い始めても controller 側が 500 になりにくくなります。

---

## 6. 新規テスト: Comment モデル

`test/models/comment_test.rb`

```ruby
require "test_helper"

class CommentTest < ActiveSupport::TestCase
  setup do
    @store = Store.create!(name: "Test Store")
    @cast = User.create!(email: "cast_comment_model@example.com", password: "password", role: :cast)
    @customer = User.create!(email: "customer_comment_model@example.com", password: "password", role: :customer)

    @booth = Booth.create!(
      store: @store,
      name: "Test Booth",
      status: :offline,
      ivs_stage_arn: "arn:aws:ivs:ap-northeast-1:123456789012:stage/comment-model"
    )

    BoothCast.create!(booth: @booth, cast_user: @cast)

    @stream_session = StreamSessions::StartService.new(booth: @booth, actor: @cast).call
    @booth.update!(status: :live)
  end

  test "chat は body 必須" do
    comment = Comment.new(
      stream_session: @stream_session,
      booth: @booth,
      user: @customer,
      kind: Comment::KIND_CHAT,
      body: nil,
      metadata: {}
    )

    assert_not comment.valid?
    assert_includes comment.errors[:body], "can't be blank"
  end

  test "event 系 kind は body なしでも有効" do
    comment = Comment.new(
      stream_session: @stream_session,
      booth: @booth,
      user: @customer,
      kind: Comment::KIND_DRINK,
      body: nil,
      metadata: {}
    )

    assert comment.valid?
  end

  test "kind 未指定時は chat 扱いになる" do
    comment = Comment.new(
      stream_session: @stream_session,
      booth: @booth,
      user: @customer,
      body: "hello",
      metadata: {}
    )

    assert comment.valid?
    assert_equal Comment::KIND_CHAT, comment.kind
  end

  test "metadata 未指定時は空 hash になる" do
    comment = Comment.create!(
      stream_session: @stream_session,
      booth: @booth,
      user: @customer,
      kind: Comment::KIND_CHAT,
      body: "hello",
      metadata: nil
    )

    assert_equal({}, comment.metadata)
  end

  test "許可されていない kind は無効" do
    comment = Comment.new(
      stream_session: @stream_session,
      booth: @booth,
      user: @customer,
      kind: "unknown",
      body: "hello",
      metadata: {}
    )

    assert_not comment.valid?
    assert_includes comment.errors[:kind], "is not included in the list"
  end
end
```

---

## 7. 新規テスト: CreateService

`test/services/stream_sessions/comments/create_service_test.rb`

```ruby
require "test_helper"

class StreamSessions::Comments::CreateServiceTest < ActiveSupport::TestCase
  setup do
    @store = Store.create!(name: "Test Store")
    @cast = User.create!(email: "cast_comment_service@example.com", password: "password", role: :cast)
    @customer = User.create!(email: "customer_comment_service@example.com", password: "password", role: :customer)

    @booth = Booth.create!(
      store: @store,
      name: "Test Booth",
      status: :offline,
      ivs_stage_arn: "arn:aws:ivs:ap-northeast-1:123456789012:stage/comment-service"
    )

    BoothCast.create!(booth: @booth, cast_user: @cast)

    @stream_session = StreamSessions::StartService.new(booth: @booth, actor: @cast).call
    @booth.update!(status: :live)
  end

  test "従来どおり chat コメントを作成できる" do
    comment = nil

    without_comment_broadcast do
      assert_difference "Comment.count", 1 do
        comment = StreamSessions::Comments::CreateService.new(
          stream_session: @stream_session,
          user: @customer,
          body: " hello "
        ).call
      end
    end

    assert_equal Comment::KIND_CHAT, comment.kind
    assert_equal "hello", comment.body
    assert_equal({}, comment.metadata)
  end

  test "chat は body が空だと invalid" do
    without_comment_broadcast do
      assert_raises(ActiveRecord::RecordInvalid) do
        StreamSessions::Comments::CreateService.new(
          stream_session: @stream_session,
          user: @customer,
          body: "   "
        ).call
      end
    end
  end

  test "drink は body なしでも作成できる" do
    comment = nil

    without_comment_broadcast do
      assert_difference "Comment.count", 1 do
        comment = StreamSessions::Comments::CreateService.new(
          stream_session: @stream_session,
          user: @customer,
          kind: Comment::KIND_DRINK,
          metadata: { "drink_item_id" => 123 }
        ).call
      end
    end

    assert_equal Comment::KIND_DRINK, comment.kind
    assert_nil comment.body
    assert_equal({ "drink_item_id" => 123 }, comment.metadata)
  end

  test "metadata が nil でも空 hash で作成される" do
    comment = nil

    without_comment_broadcast do
      comment = StreamSessions::Comments::CreateService.new(
        stream_session: @stream_session,
        user: @customer,
        body: "hello",
        metadata: nil
      ).call
    end

    assert_equal({}, comment.metadata)
  end

  test "配信中でないと作成できない" do
    @booth.update!(status: :standby)

    without_comment_broadcast do
      assert_raises(StreamSessions::Comments::CreateService::BoothNotLiveError) do
        StreamSessions::Comments::CreateService.new(
          stream_session: @stream_session,
          user: @customer,
          body: "hello"
        ).call
      end
    end
  end

  test "chat は rate limit 対象" do
    without_comment_broadcast do
      StreamSessions::Comments::CreateService.new(
        stream_session: @stream_session,
        user: @customer,
        body: "first"
      ).call

      assert_raises(StreamSessions::Comments::CreateService::RateLimitedError) do
        StreamSessions::Comments::CreateService.new(
          stream_session: @stream_session,
          user: @customer,
          body: "second"
        ).call
      end
    end
  end

  private

  def without_comment_broadcast
    notifier_class = CommentNotifier.singleton_class
    original_method = CommentNotifier.method(:append) if CommentNotifier.respond_to?(:append)

    notifier_class.send(:define_method, :append) do |_comment|
      nil
    end

    yield
  ensure
    if original_method
      notifier_class.send(:define_method, :append, original_method)
    else
      notifier_class.send(:remove_method, :append)
    end
  end
end
```

---

## 8. 既存フォームはそのままでOK

`app/views/stream_sessions/comments/_form.html.erb` は今回は変更不要です。

今の Issue は「基盤改修」なので、投稿 UI は従来どおり `body` だけ送るままで問題ありません。
内部で `kind=chat` が既定値になるので既存互換を維持できます。

---

## 9. 実行順

おすすめはこの順です。

1. migration 作成して内容差し替え
2. `Comment` モデル変更
3. `CreateService` 変更
4. `_comment.html.erb` 変更
5. controller の rescue 追加
6. テスト2本追加
7. migrate
8. test 実行

コマンドはこんな感じです。

```bash
docker compose exec app bin/rails db:migrate
docker compose exec app bin/rails test test/models/comment_test.rb
docker compose exec app bin/rails test test/services/stream_sessions/comments/create_service_test.rb
docker compose exec app bin/rails test test/integration/cast/booths_two_screens_test.rb
```

---

## 10. この実装で満たしていること

* `comments.kind` 追加
* `comments.metadata(jsonb)` 追加
* `kind` デフォルト `chat`
* `body` は `chat` のときだけ必須
* Service が `kind / metadata` を扱える
* partial が `kind` 分岐可能
* 既存の通常コメント投稿フローを維持
* テスト追加

---

最後に1点だけ。
`metadata` は PostgreSQL の `jsonb` で問題ないですが、将来キーをシンボルで触りたくなると揺れやすいので、今後は **string key 前提** で統一すると事故が少ないです。

---
---

原因はこれです。
**テスト側で `@stream_session.booth` が `standby` のまま見えている** 可能性が高いです。

`StartService` 実行直後の booth は standby で、そのあと

```ruby
@booth.update!(status: :live)
```

していても、`CreateService` 内では

```ruby
booth = @stream_session.booth
```

を見ているので、**関連オブジェクトのキャッシュが残っている** と `live? || away?` が false のままになり、`BoothNotLiveError` になります。

---

## 修正方法

`test/services/stream_sessions/comments/create_service_test.rb` の `setup` の最後をこうしてください。

```ruby
@stream_session = StreamSessions::StartService.new(booth: @booth, actor: @cast).call
@booth.reload
@booth.update!(status: :live)
@stream_session.reload
```

ただ、これだけだと association キャッシュが残ることがあるので、より安全なのはこれです。

```ruby
@stream_session = StreamSessions::StartService.new(booth: @booth, actor: @cast).call
@booth.reload
@booth.update!(status: :live)
@stream_session = StreamSession.find(@stream_session.id)
```

これで `@stream_session.booth` も取り直されます。

---

## おすすめの setup 完成形

`test/services/stream_sessions/comments/create_service_test.rb` の `setup` をこの形に差し替えてください。

```ruby
setup do
  @store = Store.create!(name: "Test Store")
  @cast = User.create!(email: "cast_comment_service@example.com", password: "password", role: :cast)
  @customer = User.create!(email: "customer_comment_service@example.com", password: "password", role: :customer)

  @booth = Booth.create!(
    store: @store,
    name: "Test Booth",
    status: :offline,
    ivs_stage_arn: "arn:aws:ivs:ap-northeast-1:123456789012:stage/comment-service"
  )

  BoothCast.create!(booth: @booth, cast_user: @cast)

  @stream_session = StreamSessions::StartService.new(booth: @booth, actor: @cast).call
  @booth.reload
  @booth.update!(status: :live)
  @stream_session = StreamSession.find(@stream_session.id)
end
```

---

## なぜこれで直るか

`CreateService` は `@stream_session.booth` を見ています。
なのでテストでは `@booth` を live にするだけでなく、**`@stream_session` 側から見える booth も最新にしておく** 必要があります。

---

## 次に実行するコマンド

```bash
docker compose exec web bin/rails test test/services/stream_sessions/comments/create_service_test.rb
```

これでまだ落ちるなら、次は `CreateService` 側の `booth_allows_comments?` を少し堅くする修正を入れます。
その場合はそのままエラー全文を貼ってください。
