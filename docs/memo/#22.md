**増加反映は webhook を正**、return は「復帰＋再取得（未反映も考慮）」に寄せます。

---

# 0) Gemfile に stripe を追加

`Gemfile` に入れてください（なければ）：

```ruby
gem "stripe"
```

### 2) bundle install（docker compose 経由）

（あなたの方針どおり docker compose 経由で）

```powershell
docker compose run --rm app bundle install
```

### 3) 再起動

```powershell
docker compose up
```

---

# 1) マイグレーション（2本）

## 1-1. wallet_purchases

```powershell
docker compose run --rm -e DISABLE_SPRING=1 app bin/rails g migration CreateWalletPurchases
```

```ruby
class CreateWalletPurchases < ActiveRecord::Migration[8.1]
  def change
    create_table :wallet_purchases do |t|
      t.references :wallet, null: false, foreign_key: true
      t.integer :points, null: false

      t.string :stripe_checkout_session_id
      t.string :stripe_payment_intent_id
      t.string :stripe_customer_id

      t.bigint :booth_id
      t.integer :status, null: false, default: 0 # pending/paid/credited/canceled/failed

      t.datetime :paid_at
      t.datetime :credited_at

      t.timestamps
    end

    add_index :wallet_purchases, :stripe_checkout_session_id, unique: true
    add_index :wallet_purchases, :booth_id
  end
end
```

## 1-2. stripe_webhook_events（冪等管理：推奨）

Stripe は同じイベントを再送します。**冪等がないと二重加算**が起きます。

```powershell
docker compose run --rm -e DISABLE_SPRING=1 app bin/rails g migration CreateStripeWebhookEvents
```

```ruby
# db/migrate/XXXXXXXXXXXXXX_create_stripe_webhook_events.rb
class CreateStripeWebhookEvents < ActiveRecord::Migration[8.1]
  def change
    create_table :stripe_webhook_events do |t|
      t.string :event_id, null: false
      t.string :event_type, null: false
      t.datetime :received_at, null: false
      t.jsonb :payload
      t.string :livemode
      t.timestamps
    end

    add_index :stripe_webhook_events, :event_id, unique: true
  end
end
```

## 1-3. migrate

```powershell
docker compose run --rm -e DISABLE_SPRING=1 app bin/rails db:migrate
```

---

# 2) Model

## 2-1. WalletPurchase
`app/models/wallet_purchase.rb`
```ruby
class WalletPurchase < ApplicationRecord
  belongs_to :wallet

  enum :status, {
    pending: 0,
    paid: 1,
    credited: 2,
    canceled: 3,
    failed: 4
  }

  validates :points, numericality: { only_integer: true, greater_than: 0 }
end
```

## 2-2. StripeWebhookEvent
`app/models/stripe_webhook_event.rb`
```ruby
class StripeWebhookEvent < ApplicationRecord
  validates :event_id, presence: true, uniqueness: true
  validates :event_type, presence: true
  validates :received_at, presence: true
end
```

---

# 3) Controller

## 3-1. wallet/purchases#create（Checkout 生成して Stripe へリダイレクト）
`app/controllers/wallet/purchases_controller.rb`
```ruby
class Wallet::PurchasesController < ApplicationController
  before_action :authenticate_user!

  def create
    checkout_url = Wallets::CreateCheckoutService.new(
      customer_user: current_user,
      points: params.require(:points).to_i,
      booth_id: params[:booth_id],
      base_url: request.base_url
    ).call!

    redirect_to checkout_url, allow_other_host: true
  end
end
```

## 3-2. checkout#return（復帰表示 → booth に戻す）

**Phase1 の最小**として：return ページで状況を見せて、ボタンでブースへ戻す。
（成功でも未反映があり得るので「再取得」もここでやる）
`app/controllers/checkout_controller.rb`
```ruby
class CheckoutController < ApplicationController
  before_action :authenticate_user!

  def return
    @status = params[:status].to_s
    @booth_id = params[:booth_id]

    @wallet = Wallet.find_by(customer_user_id: current_user.id)

    render :return
  end
end
```
反映ラグ対策（自動再取得）は #41 で。

## 3-3. webhooks/stripe#create（署名検証＋冪等＋反映）
`app/controllers/webhooks/stripe_controller.rb`
```ruby
class Webhooks::StripeController < ActionController::Base
  protect_from_forgery with: :null_session

  def create
    payload = request.raw_post
    sig = request.env["HTTP_STRIPE_SIGNATURE"]

    secret =
      ENV["STRIPE_WEBHOOK_SECRET"].presence ||
      Rails.application.credentials.dig(:stripe, :webhook_secret)

    raise "Stripe webhook secret is missing" if secret.blank?

    event = Stripe::Webhook.construct_event(payload, sig, secret)

    # 冪等：同じ event_id は2回処理しない
    StripeWebhookEvent.create!(
      event_id: event.id,
      event_type: event.type,
      received_at: Time.current
    )

    case event.type
    when "checkout.session.completed"
      session = event.data.object
      purchase_id = session.metadata&.[]("wallet_purchase_id").presence

      if purchase_id.blank?
        Rails.logger.warn("[stripe] checkout.session.completed missing wallet_purchase_id. session_id=#{session.id}")
        return head :ok
      end

      Wallets::ApplyPurchaseFromStripeService.new(checkout_session: session).call!
    end
  rescue ActiveRecord::RecordNotUnique
    # 同一イベント再送：正常系として 200 を返す
    head :ok
  rescue Stripe::SignatureVerificationError
    head :bad_request
  end
end
```

---

# 4) Service

## 4-1. CreateCheckoutService（購入レコード作成 → Stripe Checkout）

ここは **return_url を絶対URL**にするため `base_url` を受け取り、`success_url/cancel_url` を生成します。
`app/services/wallets/create_checkout_service.rb`
```ruby
module Wallets
  class CreateCheckoutService
    PACK_POINTS = [1000, 3000, 5000].freeze

    def initialize(customer_user:, points:, booth_id:, base_url:)
      @customer_user = customer_user
      @points = points
      @booth_id = booth_id
      @base_url = base_url
    end

    def call!
      raise ArgumentError, "invalid points" unless PACK_POINTS.include?(@points)

      wallet = Wallet.find_or_create_by!(customer_user_id: @customer_user.id) do |w|
        w.available_points = 0
        w.reserved_points = 0
      end

      purchase = WalletPurchase.create!(
        wallet: wallet,
        points: @points,
        booth_id: @booth_id,
        status: :pending
      )

      session = Stripe::Checkout::Session.create(
        mode: "payment",
        line_items: [
          {
            price_data: {
              currency: "jpy",
              product_data: { name: "ポイント #{@points}pt" },
              unit_amount: @points
            },
            quantity: 1
          }
        ],
        success_url: build_return_url(status: "success", purchase_id: purchase.id, booth_id: @booth_id),
        cancel_url:  build_return_url(status: "cancel",  purchase_id: purchase.id, booth_id: @booth_id),
        metadata: {
          wallet_purchase_id: purchase.id,
          customer_user_id: @customer_user.id
        }
      )

      purchase.update!(stripe_checkout_session_id: session.id)

      session.url
    end

    private

    def build_return_url(status:, purchase_id:, booth_id:)
      path = Rails.application.routes.url_helpers.checkout_return_path(
        status: status,
        purchase_id: purchase_id,
        booth_id: booth_id
      )
      "#{@base_url}#{path}"
    end
  end
end
```

## 4-2. ApplyPurchaseFromStripeService（Webhookで wallet 反映）

**重要**：二重加算を防ぐため、`purchase.credited?` なら何もしない。
wallet 更新は transaction + lock で。
`app/services/wallets/apply_purchase_from_stripe_service.rb`
```ruby
module Wallets
  class ApplyPurchaseFromStripeService
    def initialize(checkout_session:)
      @session = checkout_session
    end

    def call!
      purchase_id = @session.metadata&.[]("wallet_purchase_id").presence
      raise "missing wallet_purchase_id" if purchase_id.blank?
      purchase_id = purchase_id.to_i

      WalletPurchase.transaction do
        purchase = WalletPurchase.lock.find(purchase_id)
        return if purchase.credited? # 冪等（最低限）

        if purchase.stripe_checkout_session_id.present? && purchase.stripe_checkout_session_id != @session.id
          raise "checkout session mismatch"
        end

        purchase.update!(
          status: :paid,
          stripe_payment_intent_id: @session.payment_intent,
          stripe_customer_id: @session.customer,
          paid_at: Time.current
        )

        wallet = Wallet.lock.find(purchase.wallet_id)
        wallet.update!(available_points: wallet.available_points + purchase.points)

        WalletTransaction.create!(
          wallet: wallet,
          kind: :purchase,
          points: purchase.points,
          ref: purchase,
          occurred_at: Time.current
        )

        purchase.update!(status: :credited, credited_at: Time.current)
      end
    end
  end
end
```

---

# 5) View：checkout/return.html.erb（復帰＋再取得）
`app/views/checkout/return.html.erb`
```erb
<h1>ポイント購入</h1>

<% if @status == "cancel" %>
  <div class="alert alert-warning">購入はキャンセルされました。</div>
<% elsif @status == "success" %>
  <div class="alert alert-info">
    決済は完了しました。反映まで少し時間がかかる場合があります。
  </div>
<% else %>
  <div class="alert alert-danger">購入に失敗した可能性があります。</div>
<% end %>

<% if @wallet.present? %>
  <p>現在の残高: <strong><%= @wallet.available_points %>pt</strong></p>
<% else %>
  <p class="text-muted">Wallet が見つかりません。</p>
<% end %>

<% if @booth_id.present? %>
  <%= link_to "ブースへ戻る", booth_path(@booth_id), class: "btn btn-primary" %>
<% else %>
  <%= link_to "トップへ戻る", root_path, class: "btn btn-outline-secondary" %>
<% end %>
```

> Phase1 的にはこれで「return で再取得」は満たせます。
> “success直後未反映”をもっと強くケアするなら、ここに「再読み込み」ボタンや JS で数回リロードを足すのが次の一手です。

---

# 6) Booth 画面に「残高表示＋購入導線」を最小追加

今 `booths#show` の controller 側のコードは出ていませんが、ビューだけで最小にやるなら：

* `@wallet`（現在ユーザーの wallet）が `booths#show` で取れている前提にする
  （無ければ controller で `find_or_create_by`）

`app/views/booths/show.html.erb` の適当な場所（例：ドリンクメニューの上）に追加：

```erb
<hr>
<h2>所持ポイント</h2>

<% if user_signed_in? %>
  <p>残高: <strong><%= @wallet&.available_points || 0 %>pt</strong></p>

  <div class="d-flex gap-2 mb-3">
    <%= button_to "1000pt 購入",
          wallet_purchases_path,
          method: :post,
          params: { points: 1000, booth_id: @booth.id },
          form: { data: { turbo: false } },
          class: "btn btn-sm btn-outline-primary" %>

    <%= button_to "3000pt 購入",
          wallet_purchases_path,
          method: :post,
          params: { points: 3000, booth_id: @booth.id },
          form: { data: { turbo: false } },
          class: "btn btn-sm btn-outline-primary" %>
  </div>
<% else %>
  <p class="text-muted">ログインすると購入できます</p>
<% end %>
```

> 外部決済へ飛ぶので `turbo: false` は今の `送信` と同じ思想でOKです。

---

# 7) BoothsController に @wallet を追加

購入導線と、復帰後に残高を表示（＝再取得）するために、@wallet を用意します。

`app/controllers/booths_controller.rb`

```ruby
# frozen_string_literal: true

class BoothsController < ApplicationController
  def show
    @booth = Booth.find(params[:id])
    @stream_session = @booth.current_stream_session
    @drink_items = @booth.store.drink_items.enabled_only.ordered

    @wallet =
      if user_signed_in?
        Wallet.find_or_create_by!(customer_user_id: current_user.id) do |w|
          w.available_points = 0
          w.reserved_points = 0
        end
      end

    @comments =
      if @stream_session.present?
        Comment.alive.where(stream_session: @stream_session)
              .order(created_at: :desc)
              .limit(50)
              .reverse
      else
        []
      end
  end
end
```

---
# 8) Stripe.api_keyの設定

`config/initializers/stripe.rb`
```ruby
# frozen_string_literal: true

# Stripe API Key 設定
#
# 優先順位:
# 1. ENV["STRIPE_SECRET_KEY"]
#    - 本番環境（EC2 / CI / 一時的な鍵差し替え）向け
#    - デプロイ時に環境変数で安全に切り替えられる
#
# 2. Rails credentials (:stripe, :secret_key)
#    - ローカル開発・ステージング向け
#    - リポジトリ外で安全に管理できる
#
# ※ どちらも未設定の場合は Stripe API 呼び出し時に例外が発生する
Stripe.api_key =
  ENV["STRIPE_SECRET_KEY"].presence ||
  Rails.application.credentials.dig(:stripe, :secret_key)

# ネットワークエラー時の自動リトライ回数
# Stripe 公式 SDK の推奨に従い、過度な再試行は避ける
Stripe.max_network_retries = 2
```

ローカル .env（例）

```env
# =========================================================
# Stripe（ローカル開発用）
# =========================================================

# Stripe Secret API Key
# ダッシュボード > Developers > API keys > Secret key
# ※ 必ず sk_test_ で始まるテストキーを使う
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxx

# Stripe Webhook Signing Secret
# stripe listen 実行後に表示される whsec_ で始まる値
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxx


# =========================================================
# Rails
# =========================================================

RAILS_ENV=development
```

---

# Stripe CLI セットアップ手順（Windows / ローカル開発）

## ゴール

* ✅ Stripe CLI が使える
* ✅ `STRIPE_SECRET_KEY`（APIキー）を取得
* ✅ `STRIPE_WEBHOOK_SECRET`（Webhook署名用）を取得
* ✅ `.env` に反映
* ✅ #22（Checkout → Webhook → Wallet反映）がローカルで動く

---

## STEP 1. Stripe CLI を Windows にインストール

（パッケージ名は環境で表記が違うことがあるので、まず検索）
```powershell
winget search stripe
```
検索結果に Stripe.StripeCLI（またはそれに近い名前）が出たら：
```powershell
winget install Stripe.StripeCLI
```

確認：

```powershell
stripe --version
```

バージョンが表示されればOK。

---

## STEP 2. Stripe CLI でログイン

```powershell
stripe login
```

* ブラウザが開く
* Stripe ダッシュボードにログイン
* 「Allow access」を押す

成功すると：

```
Done! You are now logged in as xxxx
```

---

## STEP 3. STRIPE_SECRET_KEY を取得（APIキー）

### 取得場所（ブラウザ）

1. Stripe ダッシュボード https://dashboard.stripe.com/
2. **設定 → 開発者 → API キーを管理**
3. **API 認証についてもっと知る**
4. **Secret key（sk_test_...）** をコピー

---

## STEP 4. Webhook 転送を開始 ー 決済フロー（Webhook反映まで）をローカルで確認するたびに必要

```powershell
stripe listen --forward-to localhost:3000/webhooks/stripe
```

起動すると、**必ずこの行が出ます**：

```
Your webhook signing secret is whsec_****************
```

👉 これが **STRIPE_WEBHOOK_SECRET** です
👉 **このターミナルは起動したまま**にしてください（止めるとWebhookが届きません）
👉 2 回目以降は、表示された `whsec_...` が .env の `STRIPE_WEBHOOK_SECRET` と**異なる場合のみ**更新してください（一致していれば更新不要です）

> 補足：stripe listen を起動し直すと whsec_... が変わることがあります。
> その場合、.env を更新しないとWebhook署名検証でエラー（400）になり、ポイント反映されません。
---

## STEP 5. `.env` を作成 / 編集

プロジェクトルートに `.env` を作成（なければ）。

```env
# =====================================================
# Stripe（ローカル開発用 / Test mode）
# =====================================================

# Stripe Secret API Key（sk_test_ で始まる）
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxx

# Stripe Webhook Signing Secret（stripe listen 起動時に表示）
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxx


# =====================================================
# Rails
# =====================================================

RAILS_ENV=development
```

⚠️ `.env` は **Git に含めない**

```gitignore
.env
```

---

## STEP 6. 動作確認（重要）

### ① Rails 起動

```powershell
docker compose up
```

### ② Webhook 疑似発火（決済なしでOK）

別ターミナルで：

```powershell
stripe trigger checkout.session.completed
```

### ③ Rails ログで確認

* `ApplyPurchaseFromStripeService` が呼ばれる
* エラーが出ない

👉 **Webhook がローカルに届いている証拠**

---

## STEP 8. 実決済フロー確認（任意）

1. ブース画面で「1000pt購入」
2. Stripe Checkout 表示
3. テストカード入力：

   ```
   4242 4242 4242 4242
   有効期限: 任意
   CVC: 任意
   ```
4. 支払い完了 → return → webhook → Wallet反映

---

## よくあるハマりポイント

| 症状                 | 原因                          |
| ------------------ | --------------------------- |
| webhook が来ない       | `stripe listen` を起動していない    |
| 署名エラー              | `STRIPE_WEBHOOK_SECRET` が違う |
| Wallet増えない         | Webhook側の service が呼ばれていない  |
| `sk_live_` を使ってしまう | Test mode OFF               |

---

## 最短チェックリスト（#22 用）

* [ ] `stripe --version` OK
* [ ] `stripe login` 済み
* [ ] `stripe listen` 起動中
* [ ] `.env` に `STRIPE_SECRET_KEY`
* [ ] `.env` に `STRIPE_WEBHOOK_SECRET`
* [ ] `stripe trigger checkout.session.completed` が Rails に届く
