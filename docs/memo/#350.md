了解です。
#350 は **「viewer の visible 領域を Stage のみに寄せ、Stage 高さを viewport - header - footer で固定する」** 実装で進めるのがよさそうです。

今回の実装方針は次です。

* `_viewer_main.html.erb`

  * viewer main 自体に `live-stage` controller を載せる
  * flash は visible のまま残す
  * `yield` 側を Stage 用ラッパで包み、**残り領域を viewer 本体**として使う

* `_stream_state.html.erb`

  * 既存の `viewer-stage-shell` / `viewer_stage` 構造は維持
  * live / away はその中に viewer を描画
  * offline は空 shell のまま維持

* `_ivs_viewer.html.erb`

  * visible 側は **video のみ**
  * `card / state / mute / error` はすべて offscreen へ退避
  * ただし `data-controller="ivs-viewer"` は visible 側に残す

* `live_stage_controller.js`

  * 既存ロジックをそのまま viewer main でも再利用
  * scroll lock は **viewer 画面表示中** に有効になる
    今回は controller を viewer_main に付けるので、viewer show では常時 lock になります。
  * これは #350 の Issue 文には合っています。未配信時だけ通常スクロールにする細分化は後続で必要なら分ける形です。

* `application.scss`

  * viewer main 用の flex / overflow / height 制御を追加
  * `viewer-stage-shell` を **実 height: var(--live-stage-h)** に寄せる
  * visible の viewer video は `#viewer_stage` 全面で contain
  * `_ivs_viewer.html.erb` のオフスクリーン化用クラスを使う

以下、そのまま貼れる形です。

---

### `app/views/layouts/_viewer_main.html.erb`

```erb
<main class="flex-grow-1 viewer-main" data-controller="live-stage">
  <div class="container mt-3">
    <%# ★常時存在する flash の箱（StoreBanGuard の turbo_stream target） %>
    <div id="flash_inner">
      <% if notice.present? %>
        <%= render "shared/flash_message", level: "success", message: notice %>
      <% end %>

      <% if alert.present? %>
        <%= render "shared/flash_message", level: "danger", message: alert %>
      <% end %>
    </div>
  </div>

  <div class="container my-4 viewer-main-body">
    <%= yield %>
  </div>
</main>

<%# --- Footer (viewer main) --- %>
<footer id="app_footer" class="border-top bg-body fixed-bottom">
  <div class="container py-2">
    <div class="d-flex justify-content-between align-items-center">
      <div class="d-flex gap-3">
        <%= link_to "ホーム", root_path, class: "btn btn-sm btn-outline-secondary" %>
        <%= link_to "お気に入り", favorites_booths_path, class: "btn btn-sm btn-outline-secondary" %>
      </div>

      <div>
      </div>

      <div class="d-flex gap-2 align-items-center">
        <%= link_to "ダッシュボード", dashboard_path_for(current_user), class: "btn btn-sm btn-outline-secondary" %>

        <% if user_signed_in? %>
          <%= button_to "ログアウト",
                destroy_user_session_path,
                method: :delete,
                class: "btn btn-sm btn-outline-secondary" %>
        <% end %>
      </div>
    </div>
  </div>
</footer>
```

---

### `app/views/stream_sessions/_ivs_viewer.html.erb`

```erb
<div
  class="ivs-viewer-root"
  data-controller="ivs-viewer"
  data-ivs-viewer-token-url-value="<%= stream_session_ivs_participant_tokens_path(stream_session) %>"
>
  <video
    class="ivs-viewer-video"
    playsinline
    autoplay
    data-ivs-viewer-target="video"
  ></video>

  <div class="viewer-legacy-offscreen" aria-hidden="true">
    <div class="card">
      <div class="card-header">
        視聴
        <span class="ms-2 text-muted small" data-ivs-viewer-target="state">idle</span>
      </div>

      <div class="card-body">
        <%# ★常時表示：ミュートトグル（🔇/🔊） %>
        <div class="position-absolute top-0 start-0 m-2">
          <button
            type="button"
            class="btn btn-dark btn-sm"
            data-action="ivs-viewer#toggleMute"
            data-ivs-viewer-target="muteButton"
            aria-label="ミュート切替"
          >
            🔇
          </button>

          <%# 音が出ない等のときだけ出す補助文 %>
          <div
            class="small mt-1 text-white-50"
            data-ivs-viewer-target="muteHint"
            style="display: none;"
          >
            タップで音を出せます
          </div>
        </div>

        <div class="mt-2 text-danger small" data-ivs-viewer-target="error"></div>
      </div>
    </div>
  </div>
</div>
```

---

### `app/assets/stylesheets/application.scss`

既存ファイルの該当部分を次の内容に置き換え / 追記してください。

#### 1. `#live_stage` まわりはそのままでOKです。

#### 2. `ivs viewer (#336)` と `viewer stage space prep (#377)` のブロックを、下記に差し替えてください。

```scss
/* ------------------------------------------------------------
   ivs viewer (#336, #350)
------------------------------------------------------------ */
.ivs-viewer-root {
  position: absolute;
  inset: 0;
  min-width: 0;
  min-height: 0;
  overflow: hidden;
}

.ivs-viewer-video {
  position: absolute;
  inset: 0;

  display: block;
  width: 100%;
  height: 100%;

  object-fit: contain;
  background: #000;
}

/* ------------------------------------------------------------
   viewer stage space prep (#377, #350)
------------------------------------------------------------ */
.viewer-main {
  overflow: hidden;
}

.viewer-main-body {
  min-width: 0;
  min-height: 0;
}

.viewer-page,
.viewer-stream-state {
  min-width: 0;
  min-height: 0;
}

.viewer-stage-shell {
  width: 100%;
  height: var(--live-stage-h);
  min-width: 0;
  min-height: 0;
}

.viewer-stage-shell-empty {
  border-radius: 12px;
  background: #000;
  overflow: hidden;
}

.viewer-stage-shell > #viewer_stage,
.viewer-stage-shell-empty > #viewer_stage {
  position: relative;
  width: 100%;
  height: 100%;
  min-width: 0;
  min-height: 0;
  overflow: hidden;
  border-radius: 12px;
  background: #000;
}
```

#### 3. 既存の `.viewer-page, .viewer-stream-state` ブロックの直後あたりに、次も追記してください。

```scss
.viewer-page {
  width: 100%;
}

.viewer-stream-state {
  width: 100%;
}
```

---

### `app/views/booths/_stream_state.html.erb`

これは構造自体はそのままで大丈夫です。
**変更不要**で進められます。

---

### `app/views/booths/show.html.erb`

これも **変更不要** です。

---

### `app/javascript/controllers/live_stage_controller.js`

これも **変更不要** で大丈夫です。
今回 `_viewer_main.html.erb` に `data-controller="live-stage"` を付けることで再利用します。

---

## 実装後の確認ポイント

1. viewer 画面で、header と fixed footer の間いっぱいに黒い Stage が出ること
2. live / away 中、映像が Stage 内に `contain` で収まること
3. offline 時も空の Stage shell が崩れないこと
4. viewer 画面でページ全体がスクロールしないこと
5. `card-header / mute / error` が visible 領域から消えていること
6. ただし join 自体は壊れず、映像は再生されること
7. Turbo で live / away / offline 切替しても `#stream_state` 置換が壊れないこと

## 補足

今回の切り方だと、mute UI も offscreen に入るので、**音声解除の visible UI は一時的になくなります。**
これは #350 の source of truth には反しませんが、後続のミュート UI Issue が入るまでは一時的に操作導線が減ります。
Epic 優先で Stage を先に完成させるなら妥当です。

必要なら次に、そのまま使える **コミットコメント 1行** と **PR文（Closes #350）** を出します。

---
---
了解です。
#350 は **viewer でも「Stage の中に flash layer を持つ」構成に寄せる** のが一番きれいです。

今回の最終形はこうです。

* `_viewer_main.html.erb`

  * flash を外へ置かない
  * `live-stage` controller をここに付けて高さ計算だけ使う
  * main は `yield` 用の最小骨格にする
  * footer はここに残す

* `_stream_state.html.erb`

  * live / away 時の `viewer-stage-shell` 内に

    * `#viewer_stage`
    * `#viewer_flash_layer`
      を置く
  * flash は cast/live 同様、Stage 上の独立レイヤーにする

* `_ivs_viewer.html.erb`

  * visible は video のみ
  * `card / card-header / card-body / mute / error / state` は offscreen へ退避
  * controller 本体は visible 側に残す

* `application.scss`

  * viewer 用 flash layer を追加
  * viewer main は余白で Stage を押しつぶさない
  * Stage は `var(--live-stage-h)` ベースで固定

以下、そのまま貼れる最終版です。

---

## `app/views/layouts/_viewer_main.html.erb`

```erb
<main class="flex-grow-1 viewer-main" data-controller="live-stage">
  <div class="container viewer-main-body">
    <%= yield %>
  </div>
</main>

<%# --- Footer (viewer main) --- %>
<footer id="app_footer" class="border-top bg-body fixed-bottom">
  <div class="container py-2">
    <div class="d-flex justify-content-between align-items-center">
      <div class="d-flex gap-3">
        <%= link_to "ホーム", root_path, class: "btn btn-sm btn-outline-secondary" %>
        <%= link_to "お気に入り", favorites_booths_path, class: "btn btn-sm btn-outline-secondary" %>
      </div>

      <div>
      </div>

      <div class="d-flex gap-2 align-items-center">
        <%= link_to "ダッシュボード", dashboard_path_for(current_user), class: "btn btn-sm btn-outline-secondary" %>

        <% if user_signed_in? %>
          <%= button_to "ログアウト",
                destroy_user_session_path,
                method: :delete,
                class: "btn btn-sm btn-outline-secondary" %>
        <% end %>
      </div>
    </div>
  </div>
</footer>
```

---

## `app/views/booths/_stream_state.html.erb`

```erb
<%# locals: booth:, stream_session:, comments:, drink_items: %>

<% live_like = booth.live? || booth.away? %>

<div class="viewer-stream-state">
  <% if stream_session.present? && live_like %>
    <%= turbo_stream_from [stream_session, :pending_drink_orders] %>
    <%= turbo_stream_from [stream_session, :comments] %>
    <%= turbo_stream_from [stream_session, :viewer_stage] %>

    <div class="viewer-stage-shell">
      <div id="viewer_stage">
        <%= render "stream_sessions/ivs_viewer", stream_session: stream_session %>

        <div id="viewer_flash_layer">
          <div id="flash_inner">
            <% if notice.present? %>
              <%= render "shared/flash_message", level: "success", message: notice %>
            <% end %>

            <% if alert.present? %>
              <%= render "shared/flash_message", level: "danger", message: alert %>
            <% end %>
          </div>
        </div>
      </div>
    </div>

    <div class="viewer-legacy-offscreen" aria-hidden="true">
      <div
        data-controller="presence-poll"
        data-presence-poll-ping-url-value="<%= ping_stream_session_presence_path(stream_session) %>"
        data-presence-poll-summary-url-value="<%= presence_summary_stream_session_path(stream_session) %>"
        data-presence-poll-interval-ms-value="20000"
      >
        <% title_display = stream_session.title.presence || booth.name %>

        <div class="mb-2">
          <div class="fw-semibold"><%= title_display %></div>
          <% if stream_session.description.present? %>
            <div class="text-muted small"><%= stream_session.description %></div>
          <% end %>
        </div>

        <hr>

        viewers: <span data-presence-poll-target="count">-</span>
      </div>

      <hr>

      <%= render "booths/drink_menu",
            booth: booth,
            stream_session: stream_session,
            drink_items: drink_items %>

      <hr>

      <%= render "stream_sessions/pending_drink_orders", stream_session: stream_session %>

      <hr>

      <div id="comments">
        <%= render partial: "comments/comment", collection: (comments || []), as: :comment %>
      </div>

      <%= render "stream_sessions/comments/form", stream_session: stream_session %>
    </div>

  <% else %>
    <div class="viewer-stage-shell viewer-stage-shell-empty">
      <div id="viewer_stage">
        <div id="viewer_flash_layer">
          <div id="flash_inner">
            <% if notice.present? %>
              <%= render "shared/flash_message", level: "success", message: notice %>
            <% end %>

            <% if alert.present? %>
              <%= render "shared/flash_message", level: "danger", message: alert %>
            <% end %>
          </div>
        </div>
      </div>
    </div>

    <div class="viewer-legacy-offscreen" aria-hidden="true">
      <div class="text-muted">現在配信していません</div>

      <hr>

      <%= render "booths/drink_menu",
            booth: booth,
            stream_session: stream_session,
            drink_items: drink_items %>

      <hr>

      <div class="text-muted">配信中のみ表示されます</div>

      <hr>

      <p>配信中のみコメントできます</p>
    </div>
  <% end %>
</div>
```

---

## `app/views/stream_sessions/_ivs_viewer.html.erb`

```erb
<div
  class="ivs-viewer-root"
  data-controller="ivs-viewer"
  data-ivs-viewer-token-url-value="<%= stream_session_ivs_participant_tokens_path(stream_session) %>"
>
  <video
    class="ivs-viewer-video"
    playsinline
    autoplay
    data-ivs-viewer-target="video"
  ></video>

  <div class="viewer-legacy-offscreen" aria-hidden="true">
    <div class="card">
      <div class="card-header">
        視聴
        <span class="ms-2 text-muted small" data-ivs-viewer-target="state">idle</span>
      </div>

      <div class="card-body">
        <div class="position-absolute top-0 start-0 m-2">
          <button
            type="button"
            class="btn btn-dark btn-sm"
            data-action="ivs-viewer#toggleMute"
            data-ivs-viewer-target="muteButton"
            aria-label="ミュート切替"
          >
            🔇
          </button>

          <div
            class="small mt-1 text-white-50"
            data-ivs-viewer-target="muteHint"
            style="display: none;"
          >
            タップで音を出せます
          </div>
        </div>

        <div class="mt-2 text-danger small" data-ivs-viewer-target="error"></div>
      </div>
    </div>
  </div>
</div>
```

---

## `app/assets/stylesheets/application.scss`

既存の `ivs viewer (#336)` ブロックと `viewer stage space prep (#377)` ブロックを、次の内容に置き換えてください。

```scss
/* ------------------------------------------------------------
   ivs viewer (#336, #350)
------------------------------------------------------------ */
.ivs-viewer-root {
  position: absolute;
  inset: 0;
  min-width: 0;
  min-height: 0;
  overflow: hidden;
}

.ivs-viewer-video {
  position: absolute;
  inset: 0;

  display: block;
  width: 100%;
  height: 100%;

  object-fit: contain;
  background: #000;
}

/* ------------------------------------------------------------
   viewer stage space prep (#377, #350)
------------------------------------------------------------ */
.viewer-main {
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.viewer-main-body {
  flex: 1 1 auto;
  min-width: 0;
  min-height: 0;
  overflow: hidden;
}

.viewer-page,
.viewer-stream-state {
  width: 100%;
  height: 100%;
  min-width: 0;
  min-height: 0;
  overflow: hidden;
}

.viewer-stage-shell {
  width: 100%;
  height: var(--live-stage-h);
  min-width: 0;
  min-height: 0;
}

.viewer-stage-shell-empty {
  border-radius: 12px;
  background: #000;
  overflow: hidden;
}

.viewer-stage-shell > #viewer_stage,
.viewer-stage-shell-empty > #viewer_stage {
  position: relative;
  width: 100%;
  height: 100%;
  min-width: 0;
  min-height: 0;
  overflow: hidden;
  border-radius: 12px;
  background: #000;
}

/* viewer flash layer（cast/live の live_flash_layer 相当） */
#viewer_flash_layer {
  position: absolute;
  top: 12px;
  left: 50%;
  transform: translateX(-50%);
  width: min(640px, calc(100vw - 24px));
  z-index: 20;
  pointer-events: none;
}

#viewer_flash_layer #flash_inner {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

#viewer_flash_layer .alert {
  margin-bottom: 0;
  pointer-events: auto;
  box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.35);
}
```

`.viewer-legacy-offscreen` はそのまま残してください。

---

## `app/javascript/controllers/live_stage_controller.js`

これは **変更不要** です。
`_viewer_main.html.erb` に `data-controller="live-stage"` を付けるだけで再利用します。

---

## この版の確認ポイント

1. viewer 画面で、**黒い Stage が header / footer の間に収まる**
2. flash が main 上ではなく、**Stage 上の独立レイヤー** で出る
3. live / away / offline 切替で DevTools エラーが出ない
4. 映像が Stage 内で `contain` される
5. `card / state / mute / error` が visible 領域に出ない
6. viewer 画面でページ全体スクロールが抑止される

必要なら次に、そのまま使える **コミットコメント1行** と **PR文（Closes #350）** を出します。

---
---

追加で必要なファイルはありません。
原因は、ほぼこれで確定です。

**viewer だけ `_viewer_main.html.erb` で `yield` が `.container` の中に入っている**のが差分です。

比較するとこうです。

* `cast/live`

  * `_cast_live_main.html.erb` は `<%= yield %>` だけ
  * `live.html.erb` の `#live_stage` は container に包まれていない
  * だから横いっぱいになる

* `viewer`

  * `_viewer_main.html.erb` で `yield` が `.container` の中
  * `viewer-stage-shell` が container 幅で止まる
  * だから横いっぱいにならない

* `default`

  * `_default_main.html.erb` も `.container` ベース
  * つまり今の viewer は default 寄りの骨格が残っている

なので、#350 で直すべきポイントは明確です。

## 今Issueで直すべきこと

**`_viewer_main.html.erb` から `yield` を包む `.container` を外す**
これで Stage の箱自体を cast/live と同じく full width に寄せます。

## 直す形

`app/views/layouts/_viewer_main.html.erb` はこうするのが自然です。

```erb
<main class="flex-grow-1 viewer-main" data-controller="live-stage">
  <%= yield %>
</main>

<%# --- Footer (viewer main) --- %>
<footer id="app_footer" class="border-top bg-body fixed-bottom">
  <div class="container py-2">
    <div class="d-flex justify-content-between align-items-center">
      <div class="d-flex gap-3">
        <%= link_to "ホーム", root_path, class: "btn btn-sm btn-outline-secondary" %>
        <%= link_to "お気に入り", favorites_booths_path, class: "btn btn-sm btn-outline-secondary" %>
      </div>

      <div>
      </div>

      <div class="d-flex gap-2 align-items-center">
        <%= link_to "ダッシュボード", dashboard_path_for(current_user), class: "btn btn-sm btn-outline-secondary" %>

        <% if user_signed_in? %>
          <%= button_to "ログアウト",
                destroy_user_session_path,
                method: :delete,
                class: "btn btn-sm btn-outline-secondary" %>
        <% end %>
      </div>
    </div>
  </div>
</footer>
```

## SCSS もこれに合わせる

`viewer-main-body` を消すので、`application.scss` 側の viewer ブロックはこう寄せるのがよいです。

```scss
/* ------------------------------------------------------------
   viewer stage space prep (#377, #350)
------------------------------------------------------------ */
.viewer-main {
  overflow: hidden;
}

.viewer-page,
.viewer-stream-state {
  width: 100%;
  height: 100%;
  min-width: 0;
  min-height: 0;
  overflow: hidden;
}

.viewer-stage-shell {
  width: 100%;
  height: var(--live-stage-h);
  min-width: 0;
  min-height: 0;
}

.viewer-stage-shell-empty {
  border-radius: 12px;
  background: #000;
  overflow: hidden;
}

.viewer-stage-shell > #viewer_stage,
.viewer-stage-shell-empty > #viewer_stage {
  position: relative;
  width: 100%;
  height: 100%;
  min-width: 0;
  min-height: 0;
  overflow: hidden;
  border-radius: 12px;
  background: #000;
}

/* viewer flash layer（cast/live の live_flash_layer 相当） */
#viewer_flash_layer {
  position: absolute;
  top: 12px;
  left: 50%;
  transform: translateX(-50%);
  width: min(640px, calc(100vw - 24px));
  z-index: 20;
  pointer-events: none;
}

#viewer_flash_layer #flash_inner {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

#viewer_flash_layer .alert {
  margin-bottom: 0;
  pointer-events: auto;
  box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.35);
}
```

## 判断

今の段階では、

* 映像を Stage にどうフィットさせるかは次 Issue
* でも **Stage の箱自体が横いっぱいかどうか** は #350 の範囲

です。
なので、ここは今直してよいです。

必要なら次に、**この修正版を前提にした最終確認チェック項目**か、**コミットコメント / PR文**を出します。
