では、今回のIssue向けにそのまま貼れる形で出します。
方針はこれです。

* ホーム画面追加訴求をパーシャル化
* standalone 起動時は Stimulus で自動非表示
* 「追加方法を見る」ボタンで Bootstrap モーダルを開く
* 既存の `home/show` の構造はできるだけ壊さない

---

## 1. 新規作成

`app/views/home/_install_prompt.html.erb`

```erb
<div
  class="mb-3"
  data-controller="install-prompt">
  <div class="card border-info-subtle bg-body-tertiary">
    <div class="card-body">
      <div class="d-flex flex-column flex-md-row align-items-md-center justify-content-between gap-3">
        <div class="d-flex align-items-start gap-3">
          <div class="fs-3 lh-1 text-info" aria-hidden="true">
            <i class="bi bi-house-heart"></i>
          </div>

          <div>
            <h2 class="h5 mb-2">ホーム画面に追加すると、アプリのように使えます</h2>
            <p class="mb-0 text-muted">
              iPhone の Safari からホームに追加すると、次回からホーム画面からすぐ開けます。
            </p>
          </div>
        </div>

        <div class="flex-shrink-0">
          <button
            type="button"
            class="btn btn-outline-info"
            data-action="install-prompt#openModal">
            追加方法を見る
          </button>
        </div>
      </div>
    </div>
  </div>

  <div
    class="modal fade"
    tabindex="-1"
    aria-labelledby="installPromptModalLabel"
    aria-hidden="true"
    data-install-prompt-target="modal">
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content" data-bs-theme="light">
        <div class="modal-header">
          <h2 class="modal-title fs-5" id="installPromptModalLabel">ホーム画面に追加する方法</h2>
          <button
            type="button"
            class="btn-close"
            aria-label="閉じる"
            data-action="install-prompt#closeModal"></button>
        </div>

        <div class="modal-body">
          <ol class="mb-3 ps-3">
            <li class="mb-2">Safari でこのページを開いた状態で、共有ボタンをタップします。</li>
            <li class="mb-2">メニューから「ホーム画面に追加」を選びます。</li>
            <li>確認画面で「追加」をタップします。</li>
          </ol>

          <p class="mb-0 text-muted">
            追加後は、ホーム画面のアイコンからアプリのように起動できます。
          </p>
        </div>

        <div class="modal-footer">
          <button
            type="button"
            class="btn btn-secondary"
            data-action="install-prompt#closeModal">
            閉じる
          </button>
        </div>
      </div>
    </div>
  </div>
</div>
```

---

## 2. 新規作成

`app/javascript/controllers/install_prompt_controller.js`

```js
import { Controller } from "@hotwired/stimulus"
import { Modal } from "bootstrap"

export default class extends Controller {
  static targets = ["modal"]

  connect() {
    if (this.isStandalone()) {
      this.element.remove()
      return
    }

    this.modal = this.hasModalTarget ? new Modal(this.modalTarget) : null
  }

  disconnect() {
    if (this.modal) {
      try {
        this.modal.dispose()
      } catch (_) {}
      this.modal = null
    }

    document.querySelectorAll(".modal-backdrop").forEach((el) => el.remove())
    document.body.classList.remove("modal-open")
    document.body.style.removeProperty("padding-right")
    document.body.style.removeProperty("overflow")
  }

  openModal() {
    this.modal?.show()
  }

  closeModal() {
    this.modal?.hide()
  }

  isStandalone() {
    return (
      window.matchMedia("(display-mode: standalone)").matches ||
      window.navigator.standalone === true
    )
  }
}
```

---

## 3. 修正

`app/views/home/show.html.erb`

```erb
<% if user_signed_in? %>
  <%= render "install_prompt" %>

  <%# --- Search (Input group / GET) --- %>
  <div class="mb-3">
  <%= form_with url: root_path, method: :get, scope: nil, class: "d-flex flex-grow-1" do |f| %>
    <div class="input-group">
      <%= link_to "ブース",
            root_path(mode: "booths", q: @q.presence),
            class: ["btn", (@mode == "stores" ? "btn-outline-secondary" : "btn-secondary")] %>

      <%= link_to "店舗",
            root_path(mode: "stores", q: @q.presence),
            class: ["btn", (@mode == "stores" ? "btn-secondary" : "btn-outline-secondary")] %>
      <%= f.hidden_field :mode, value: @mode %>

      <%= f.text_field :q,
            value: @q,
            class: "form-control",
            placeholder: "検索（部分一致）",
            autocomplete: "off" %>

      <button class="btn btn-primary" type="submit" aria-label="検索">
        <i class="bi bi-search"></i>
      </button>
    </div>
  </div>
  <% end %>

  <%# --- Results (cards) --- %>
  <% if @mode == "stores" %>
    <% if @stores.present? %>
      <div class="row row-cols-1 row-cols-md-3 g-3">
        <% @stores.each do |store| %>
          <% favorited = @favorite_store_ids.include?(store.id) %>

          <div class="col">
            <%= render "stores/card", store: store, favorited: favorited %>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="alert alert-secondary" role="alert">
        表示できる店舗がありません。
      </div>
    <% end %>
  <% else %>
    <% if @booths.present? %>
      <div class="row row-cols-1 row-cols-md-3 g-3">
        <% @booths.each do |booth| %>
          <div class="col">
            <%= render "booths/card",
                  booth: booth,
                  booth_favorited: @favorite_booth_ids.include?(booth.id),
                  store_favorited: @favorite_store_ids.include?(booth.store_id) %>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="alert alert-secondary" role="alert">
        表示できるブースがありません。
      </div>
    <% end %>
  <% end %>
<% else %>
  <%= render "install_prompt" %>

  <div style="max-width: 720px;">
    <h1 class="mb-3">ようこそ</h1>
    <p class="text-muted">視聴するにはログイン、または新規アカウント作成を行ってください。</p>

    <div class="d-flex gap-2 mt-3">
      <%= link_to "ログイン", new_user_session_path, class: "btn btn-primary" %>
      <%= link_to "新規アカウント作成", sign_up_path, class: "btn btn-outline-primary" %>
    </div>
  </div>
<% end %>
```

---

## 補足

今回の形なら、CSS追加なしでもそこそこ整います。
ただし、見た目をもう少しLP寄りにしたくなったら、あとで最小限のSCSSを足すのはありです。

一点だけ確認ポイントがあります。

`render "install_prompt"` は、`home/show.html.erb` から呼ぶので
Rails 的には `app/views/home/_install_prompt.html.erb` を見に行くため、この書き方で大丈夫です。

---

## 動作確認ポイント

* 通常のSafari表示では訴求カードが出る
* 「追加方法を見る」でモーダルが開く
* 閉じるボタン、右上の × で閉じられる
* ホーム画面追加後、standalone 起動時はカードごと出ない
* 既存の検索、一覧、未ログイン導線が壊れていない

---

必要なら次に、
**このままの実装で気をつけるテスト観点** も整理します。

---
---

では、**「表示制御は standalone + モバイル寄り判定」「文言だけ Safari 寄せ」** の形で、そのまま差し替えられるコードを出します。

---

## 1. `app/javascript/controllers/install_prompt_controller.js`

```js
import { Controller } from "@hotwired/stimulus"
import { Modal } from "bootstrap"

export default class extends Controller {
  static targets = ["modal", "safariOnly", "genericOnly"]

  connect() {
    if (!this.shouldShowPrompt()) {
      this.element.remove()
      return
    }

    this.updateBrowserSpecificContent()
    this.modal = this.hasModalTarget ? new Modal(this.modalTarget) : null
  }

  disconnect() {
    if (this.modal) {
      try {
        this.modal.dispose()
      } catch (_) {}
      this.modal = null
    }

    document.querySelectorAll(".modal-backdrop").forEach((el) => el.remove())
    document.body.classList.remove("modal-open")
    document.body.style.removeProperty("padding-right")
    document.body.style.removeProperty("overflow")
  }

  openModal() {
    this.modal?.show()
  }

  closeModal() {
    this.modal?.hide()
  }

  shouldShowPrompt() {
    if (this.isStandalone()) return false

    const coarse = window.matchMedia("(pointer: coarse)").matches
    const noHover = window.matchMedia("(hover: none)").matches
    const tabletOrSmaller = window.matchMedia("(max-width: 1024px)").matches

    return tabletOrSmaller && (coarse || noHover)
  }

  updateBrowserSpecificContent() {
    const safari = this.isSafari()

    this.safariOnlyTargets.forEach((element) => {
      element.hidden = !safari
    })

    this.genericOnlyTargets.forEach((element) => {
      element.hidden = safari
    })
  }

  isStandalone() {
    return (
      window.matchMedia("(display-mode: standalone)").matches ||
      window.navigator.standalone === true
    )
  }

  isSafari() {
    const ua = window.navigator.userAgent
    const isIos = /iPhone|iPad|iPod/.test(ua)
    const hasSafari = /Safari/.test(ua)
    const excluded = /CriOS|FxiOS|EdgiOS|OPiOS/.test(ua)

    return isIos && hasSafari && !excluded
  }
}
```

---

## 2. `app/views/home/_install_prompt.html.erb`

```erb
<div
  class="mb-3"
  data-controller="install-prompt">
  <div class="card border-info-subtle bg-body-tertiary">
    <div class="card-body">
      <div class="d-flex flex-column flex-md-row align-items-md-center justify-content-between gap-3">
        <div class="d-flex align-items-start gap-3">
          <div class="fs-3 lh-1 text-info" aria-hidden="true">
            <i class="bi bi-house-heart"></i>
          </div>

          <div>
            <h2 class="h5 mb-2">ホーム画面に追加すると、アプリのように使えます</h2>

            <p class="mb-0 text-muted" data-install-prompt-target="safariOnly">
              iPhone の Safari からホームに追加すると、次回からホーム画面からすぐ開けます。
            </p>

            <p class="mb-0 text-muted" data-install-prompt-target="genericOnly" hidden>
              ホーム画面に追加すると、次回からアプリのようにすぐ開けます。
            </p>
          </div>
        </div>

        <div class="flex-shrink-0">
          <button
            type="button"
            class="btn btn-outline-info"
            data-action="install-prompt#openModal">
            追加方法を見る
          </button>
        </div>
      </div>
    </div>
  </div>

  <div
    class="modal fade"
    tabindex="-1"
    aria-labelledby="installPromptModalLabel"
    aria-hidden="true"
    data-install-prompt-target="modal">
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content" data-bs-theme="light">
        <div class="modal-header">
          <h2 class="modal-title fs-5" id="installPromptModalLabel">ホーム画面に追加する方法</h2>
          <button
            type="button"
            class="btn-close"
            aria-label="閉じる"
            data-action="install-prompt#closeModal"></button>
        </div>

        <div class="modal-body">
          <div data-install-prompt-target="safariOnly">
            <ol class="mb-3 ps-3">
              <li class="mb-2">Safari でこのページを開いた状態で、共有ボタンをタップします。</li>
              <li class="mb-2">メニューから「ホーム画面に追加」を選びます。</li>
              <li>確認画面で「追加」をタップします。</li>
            </ol>

            <p class="mb-0 text-muted">
              追加後は、ホーム画面のアイコンからアプリのように起動できます。
            </p>
          </div>

          <div data-install-prompt-target="genericOnly" hidden>
            <p class="mb-3">
              お使いのブラウザによっては、共有メニューやブラウザメニューからホーム画面に追加できる場合があります。
            </p>

            <p class="mb-0 text-muted">
              追加後は、ホーム画面のアイコンからアプリのように起動できます。
            </p>
          </div>
        </div>

        <div class="modal-footer">
          <button
            type="button"
            class="btn btn-secondary"
            data-action="install-prompt#closeModal">
            閉じる
          </button>
        </div>
      </div>
    </div>
  </div>
</div>
```

---

## 3. この差分で変わること

* **standalone 起動時** は出ない
* **スマホ / タブレット寄り環境のみ** 出る
* **Safari のときだけ**

  * カード本文を Safari 専用文言にする
  * モーダルも Safari 専用手順を出す
* **Safari 以外** は

  * 汎用文言に切り替える

---

## 4. 補足

今回の `isSafari()` は、**説明文出し分け用** としては十分です。
機能の可否そのものを厳密に保証する判定ではなく、案内文を自然にするための判定として使う、という位置づけです。

---

## 5. 動作確認ポイント

* iPhone Safari 通常表示

  * カード表示あり
  * Safari 文言になる
  * モーダルで Safari 手順が出る

* iPhone ホーム画面追加後の standalone 起動

  * カード非表示

* PC Chrome

  * カード非表示

* iPad Safari

  * カード表示あり
  * Safari 文言になる

必要なら次に、**この実装に対する最小テスト観点** をまとめます。
