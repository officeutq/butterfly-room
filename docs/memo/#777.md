OK、これで完全に実装いけます 👍
（追加ファイルはもう不要です）

---

# 🎯 実装方針（確定版）

あなたのコード構造を見ると👇

* `set_booth` に認可ロジックあり → これを使う
* `Booth.active` で取得 → archivedは除外される
* indexでは archived 表示できてる

👉 **showは「archived含めて表示したい」ので少しだけ調整が必要**

---

# ⚠️ 重要な設計判断（今回）

## ❗ set_booth はそのまま使えない

```ruby
booth = Booth.active.find(params[:id])
```

👉 これだと **閉鎖済みブースが見れない**

---

## ✅ 解決方針

show専用で👇にする

```ruby
Booth.find(params[:id])
```

＋認可はそのまま流用

---

# 🚀 差分（コピペOK）

## ① routes

```diff
diff --git a/config/routes.rb b/config/routes.rb
@@
   namespace :cast do
     resource :current_booth, only: %i[create]

-    resources :booths, only: %i[index edit update] do
+    resources :booths, only: %i[index show edit update] do
```

---

## ② controller

```diff
diff --git a/app/controllers/cast/booths_controller.rb b/app/controllers/cast/booths_controller.rb
@@
-    before_action :set_booth, only: %i[live status edit update]
+    before_action :set_booth, only: %i[live status edit update]
+    before_action :set_booth_for_show, only: %i[show]

+    def show
+    end
```

---

### show用のset_booth追加

```diff
@@
   private
+
+    def set_booth_for_show
+      booth = Booth.find(params[:id])
+
+      allowed =
+        if current_user.system_admin?
+          true
+        elsif current_user.at_least?(:store_admin)
+          current_user.admin_of_store?(booth.store_id)
+        else
+          BoothCast.exists?(cast_user_id: current_user.id, booth_id: booth.id)
+        end
+
+      unless allowed
+        session.delete(:current_booth_id)
+        head :forbidden
+        return
+      end
+
+      @booth = booth
+    end
```

👉 **archivedも見れるようになる**

---

## ③ view

```diff
diff --git a/app/views/cast/booths/show.html.erb b/app/views/cast/booths/show.html.erb
new file mode 100644
```

```erb
<% content_for :title, @booth.name.presence || "ブース情報" %>

<div class="booth-show">
  <div class="booth-show-hero">
    <% if @booth.thumbnail_image.attached? %>
      <%= image_tag @booth.thumbnail_image,
            class: "booth-show-hero-image",
            alt: @booth.name %>
    <% else %>
      <div class="booth-show-hero-placeholder">
        <span class="text-muted">no thumbnail</span>
      </div>
    <% end %>
  </div>

  <div class="container booth-show-container">

    <div class="booth-show-header d-flex justify-content-between align-items-start gap-3">
      <div class="min-w-0">
        <div class="d-flex align-items-center gap-2 flex-wrap">
          <h1 class="booth-show-title mb-0">
            <%= @booth.name.presence || "(no name)" %>
          </h1>

          <%= booth_status_badge(@booth) %>

          <% if @booth.archived? %>
            <span class="badge text-bg-dark">閉鎖済み</span>
          <% end %>
        </div>
      </div>
    </div>

    <div class="booth-show-actions mt-4 d-flex gap-2 flex-wrap">

      <button
        class="btn btn-outline-primary"
        data-controller="clipboard"
        data-action="click->clipboard#copy"
        data-clipboard-text="<%= booth_url(@booth) %>">
        URLコピー
      </button>

      <% unless @booth.archived? %>
        <%= link_to "ブース編集",
              edit_cast_booth_path(@booth),
              class: "btn btn-outline-secondary" %>
      <% else %>
        <button class="btn btn-outline-secondary" disabled>ブース編集</button>
      <% end %>

      <button class="btn btn-outline-secondary" disabled>
        配信履歴（準備中）
      </button>

    </div>

    <div class="booth-show-info mt-4">

      <div class="booth-show-info-row">
        <div class="booth-show-info-label">所属店舗</div>
        <div class="booth-show-info-value">
          <%= link_to @booth.store.name, store_path(@booth.store) %>
        </div>
      </div>

    </div>

    <div class="booth-show-description mt-4">
      <%= simple_format(@booth.description.presence || "-") %>
    </div>

  </div>
</div>
```

---

# 🎯 ポイント

今回かなりいい設計になってます👇

### 👍 いい点

* 認可ロジック既存流用
* UIは store/user に統一
* clipboard既存活用
* archived対応済み

---

# 🚀 次

ここまでできたら👇

👉 「ダッシュボード導線 (#778)」いける
👉 もしくは「clipboard改善 (#779)」

---

やってみて詰まったら、そのまま貼ってくれれば即直します 👍
