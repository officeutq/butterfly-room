了解です。
では、**今の実ファイルに対する修正箇所のみ**で出します。

---

## `app/views/stream_sessions/_stream_meta_display.html.erb`

```diff
 <% user = stream_session.started_by_cast_user %>
 <% title_display = stream_session.title.presence || stream_session.booth.name %>

 <div class="live-meta-row viewer-meta-row">
   <!-- avatar -->
   <div class="meta-avatar">
     <%= user_avatar_badge(user, size: 28) %>
   </div>

   <!-- name -->
   <div class="meta-name">
     <%= display_name_or_anonymous(user) %>
   </div>

   <!-- title -->
-  <div class="meta-title text-truncate">
-    <%= title_display %>
+  <div class="meta-title">
+    <span class="meta-title-text text-truncate">
+      <%= title_display %>
+    </span>
+
+    <% if stream_session.booth.live? || stream_session.booth.away? %>
+      <%= booth_status_badge(stream_session.booth) %>
+    <% end %>
   </div>

   <!-- viewer count -->
   <div class="meta-count">
     <i class="bi bi-people-fill" aria-hidden="true"></i>
     <span data-presence-poll-target="count">-</span>
   </div>
 </div>
```

---

## `app/assets/stylesheets/components/_broadcast_meta.scss`

`/* title（可変） */` の `.meta-title` を差し替え、続けて `.meta-title-text` を追加してください。

```diff
 /* title（可変） */
 .meta-title {
   flex: 1 1 auto;
   min-width: 0;
+  display: flex;
+  align-items: center;
+  gap: 6px;

   font-size: 0.875rem;
   line-height: 1.2;
 }
+
+.meta-title-text {
+  flex: 1 1 auto;
+  min-width: 0;
+}
```

---

## 意図

* `meta-title` を横並びコンテナにする
* `meta-title-text` 側に `text-truncate` を効かせる
* バッジはタイトルの右隣に出す
* 視聴者数の位置はそのまま維持する

この差分なら、今の実ファイル構成にそのまま載ります。

---
---

できます。
今の構造なら、**`meta-row` の先頭にバッジを移す**のが一番素直です。

## 修正箇所のみ差分

### `app/views/stream_sessions/_stream_meta_display.html.erb`

```diff
 <% user = stream_session.started_by_cast_user %>
 <% title_display = stream_session.title.presence || stream_session.booth.name %>

 <div class="live-meta-row viewer-meta-row">
+  <% if stream_session.booth.live? || stream_session.booth.away? %>
+    <div class="meta-live-badge">
+      <%= booth_status_badge(stream_session.booth) %>
+    </div>
+  <% end %>
+
   <!-- avatar -->
   <div class="meta-avatar">
     <%= user_avatar_badge(user, size: 28) %>
   </div>

   <!-- name -->
   <div class="meta-name">
     <%= display_name_or_anonymous(user) %>
   </div>

   <!-- title -->
-  <div class="meta-title">
-    <span class="meta-title-text text-truncate">
-      <%= title_display %>
-    </span>
-
-    <% if stream_session.booth.live? || stream_session.booth.away? %>
-      <%= booth_status_badge(stream_session.booth) %>
-    <% end %>
-  </div>
+  <div class="meta-title text-truncate">
+    <%= title_display %>
+  </div>

   <!-- viewer count -->
   <div class="meta-count">
     <i class="bi bi-people-fill" aria-hidden="true"></i>
     <span data-presence-poll-target="count">-</span>
   </div>
 </div>
```

---

### `app/assets/stylesheets/components/_broadcast_meta.scss`

さっき追加した `meta-title` の flex 化を戻して、バッジ用クラスを追加します。

```diff
 /* title（可変） */
 .meta-title {
   flex: 1 1 auto;
   min-width: 0;
-  display: flex;
-  align-items: center;
-  gap: 6px;

   font-size: 0.875rem;
   line-height: 1.2;
 }
-
-.meta-title-text {
-  flex: 1 1 auto;
-  min-width: 0;
-}
+
+.meta-live-badge {
+  flex: 0 0 auto;
+}
```

## これでどうなるか

並びがこうなります。

* 配信中バッジ
* アバター
* 名前
* タイトル
* 視聴者数

タイトルは再び単独で `text-truncate` が効くので、左端バッジにしたいならこの形が一番安定します。
