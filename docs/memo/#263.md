了解。#263 を **最小差分・既存流儀に合わせて**実装する前提で、実装計画→貼り付けコードをまとめます。

---

## 実装計画（やる順）

1. **Route**：`/admin/stores/:store_id/payout_account/edit`（GET）と `/admin/stores/:store_id/payout_account`（PATCH）を追加
2. **Controller**：`Admin::StorePayoutAccountsController` を新規追加（edit/update）

   * store_admin：admin membership のある store のみ
   * system_admin：全 store
   * 更新は **新規作成 + 旧inactive**（transaction）
   * **新規が valid でない限り旧を触らない**（事故防止）
3. **Model**：`Store#payout_account_configured?` を追加（未設定バッジ判定を共通化）
4. **View**：口座設定 edit 画面を新規作成

   * 現在設定（下4桁のみ）
   * 入力フォーム（manual_bank）
   * 注意文言（金融誤認防止、残高/引出等の語は使わない）
   * 入力フォームは **既存口座の値をプリフィルしない**（フル表示回避）
5. **導線**：

   * dashboard（system_admin / store_admin）に「精算・振込設定」リンク + 未設定バッジ
   * #179（admin/stores/edit）に「精算・振込設定」セクション + 未設定バッジ（任意で下4桁表示）
6. **Test（integration）**：アクセス制御、更新で active1件、バッジ表示、フル表示されない

---

## ブランチ名案

おすすめ：`feature/263-store-payout-ui`

---

# 変更コード（貼り付け用）

## 1) config/routes.rb（追記）

`namespace :admin do` の中で `resources :stores` の定義を **ブロック付きに変更**して、nested resource を追加します（既存の `only:` は維持）。

```ruby
# --- Store Admin ---
namespace :admin do
  # current_store selection
  resources :stores, only: %i[index edit update] do
    resource :payout_account, only: %i[edit update], controller: "store_payout_accounts"
  end
  resource :current_store, only: %i[create], controller: "current_stores"

  resources :booths, only: %i[index show new edit create update] do
    member do
      get :watch
      patch :archive
      post :force_end
    end
  end

  resources :drink_items, only: %i[index create update destroy]
  resources :store_bans, only: %i[index create destroy]

  resources :casts, only: %i[index destroy] do
    collection do
      post :invite
    end
  end

  get "/cast_metrics", to: "metrics#cast"
end
```

---

## 2) app/controllers/admin/store_payout_accounts_controller.rb（新規）

**新規ファイル**です。

```ruby
# frozen_string_literal: true

module Admin
  class StorePayoutAccountsController < Admin::BaseController
    before_action :set_store
    before_action :authorize_store!

    def edit
      @active_payout_account = @store.active_payout_account

      # 事故防止：既存口座情報をフォームにプリフィルしない（フル表示回避）
      @payout_account = @store.store_payout_accounts.new(
        payout_method: :manual_bank,
        status: :active
      )
    end

    def update
      @active_payout_account = @store.active_payout_account

      @payout_account = @store.store_payout_accounts.new(payout_account_params)
      @payout_account.payout_method = :manual_bank
      @payout_account.status = :active
      @payout_account.updated_by_user = current_user

      # 新規が valid でない限り、既存 active を触らない（事故防止）
      unless @payout_account.valid?
        render :edit, status: :unprocessable_entity
        return
      end

      StorePayoutAccount.transaction do
        @active_payout_account&.update!(status: :inactive, updated_by_user: current_user)
        @payout_account.save!
      end

      redirect_to edit_admin_store_payout_account_path(@store), notice: "精算・振込設定を更新しました"
    end

    private

    def set_store
      @store = Store.find(params[:store_id])
    end

    def authorize_store!
      return if current_user.system_admin?

      ok = StoreMembership.admin_only.exists?(user_id: current_user.id, store_id: @store.id)
      head :forbidden unless ok
    end

    def payout_account_params
      params.require(:store_payout_account).permit(
        :bank_code,
        :branch_code,
        :account_type,
        :account_number,
        :account_holder_kana
      )
    end
  end
end
```

---

## 3) app/models/store.rb（追記）

`private` より上に、下記のメソッドを追加してください（置き場所は `self.business_type_select_options` の下あたりが自然）。

```ruby
  # --- Payout account (Issue #263) ---

  def payout_account_configured?
    pa = active_payout_account
    return false if pa.blank?

    if pa.manual_bank?
      pa.bank_code.present? &&
        pa.branch_code.present? &&
        pa.account_type.present? &&
        pa.account_number.present? &&
        pa.account_holder_kana.present?
    elsif pa.stripe_connect?
      pa.stripe_account_id.present?
    else
      false
    end
  end

  def payout_account_unconfigured?
    !payout_account_configured?
  end

  def payout_account_last4
    pa = active_payout_account
    return nil if pa.blank?
    return nil if pa.account_number.blank?

    pa.account_number.to_s.last(4)
  end
```

---

## 4) app/views/admin/store_payout_accounts/edit.html.erb（新規）

**新規ファイル**です。

```erb
<% content_for :title, "精算・振込設定" %>

<div style="max-width: 720px;">
  <h1 class="h4 mb-3">精算・振込設定</h1>

  <div class="alert alert-secondary" role="alert">
    <div>※振込先口座は精算処理に利用されます。誤りがあると入金できない場合があります。</div>
    <div class="mt-1">※この画面では口座の照会や任意の送金操作はできません（精算は月次で行われます）。</div>
  </div>

  <div class="card mb-4">
    <div class="card-header">現在の設定状況</div>
    <div class="card-body">
      <% if @active_payout_account.present? && @store.payout_account_configured? %>
        <dl class="row mb-0">
          <dt class="col-sm-4">銀行コード</dt>
          <dd class="col-sm-8"><%= @active_payout_account.bank_code %></dd>

          <dt class="col-sm-4">支店コード</dt>
          <dd class="col-sm-8"><%= @active_payout_account.branch_code %></dd>

          <dt class="col-sm-4">口座種別</dt>
          <dd class="col-sm-8">
            <%= @active_payout_account.ordinary? ? "普通" : "当座" %>
          </dd>

          <dt class="col-sm-4">口座番号</dt>
          <dd class="col-sm-8">
            <% last4 = @store.payout_account_last4 %>
            <%= last4.present? ? "****#{last4}" : "-" %>
          </dd>

          <dt class="col-sm-4">口座名義カナ</dt>
          <dd class="col-sm-8"><%= truncate(@active_payout_account.account_holder_kana.to_s, length: 32) %></dd>
        </dl>
      <% else %>
        <div class="text-muted">未設定</div>
      <% end %>
    </div>
  </div>

  <div class="card">
    <div class="card-header">口座情報（manual_bank）</div>
    <div class="card-body">
      <%= form_with model: @payout_account,
            url: admin_store_payout_account_path(@store),
            method: :patch do |f| %>

        <% if @payout_account.errors.any? %>
          <div class="alert alert-danger" role="alert">
            <ul class="mb-0">
              <% @payout_account.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="mb-3">
          <%= f.label :bank_code, "銀行コード（4桁）", class: "form-label" %>
          <%= f.text_field :bank_code, class: "form-control", inputmode: "numeric", autocomplete: "off" %>
        </div>

        <div class="mb-3">
          <%= f.label :branch_code, "支店コード（3桁）", class: "form-label" %>
          <%= f.text_field :branch_code, class: "form-control", inputmode: "numeric", autocomplete: "off" %>
        </div>

        <div class="mb-3">
          <%= f.label :account_type, "口座種別", class: "form-label" %>
          <%= f.select :account_type,
                options_for_select([["普通", "ordinary"], ["当座", "current"]], @payout_account.account_type),
                { include_blank: true },
                class: "form-select" %>
        </div>

        <div class="mb-3">
          <%= f.label :account_number, "口座番号（7桁）", class: "form-label" %>
          <%= f.text_field :account_number, class: "form-control", inputmode: "numeric", autocomplete: "off" %>
          <div class="form-text">※安全のため、現在の口座番号は表示しません。変更時は再入力してください。</div>
        </div>

        <div class="mb-3">
          <%= f.label :account_holder_kana, "口座名義カナ", class: "form-label" %>
          <%= f.text_field :account_holder_kana, class: "form-control", autocomplete: "off" %>
        </div>

        <div class="d-flex gap-2">
          <%= f.submit "更新する", class: "btn btn-primary" %>
          <%= link_to "戻る", dashboard_path, class: "btn btn-outline-secondary" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
```

---

## 5) app/views/dashboard/show.html.erb（追記）

system_admin / store_admin のリストに、既存の「店舗設定」と同じ要領で 1行追加します。

### system_admin セクション（`マニュアル精算（テスト）` の前後でOK）

```erb
      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to edit_admin_store_payout_account_path(store), class: "text-decoration-none" do %>
            精算・振込設定
            <% if store.payout_account_unconfigured? %>
              <span class="badge text-bg-warning ms-2">未設定</span>
            <% end %>
          <% end %>
        <% else %>
          <%= link_to "精算・振込設定（先に店舗を選択）", admin_stores_path, class: "text-decoration-none" %>
        <% end %>
      </li>
```

### store_admin セクション（`キャスト管理` の後あたりでOK）

```erb
      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to edit_admin_store_payout_account_path(store), class: "text-decoration-none" do %>
            精算・振込設定
            <% if store.payout_account_unconfigured? %>
              <span class="badge text-bg-warning ms-2">未設定</span>
            <% end %>
          <% end %>
        <% else %>
          <%= link_to "精算・振込設定（先に店舗を選択）", admin_stores_path, class: "text-decoration-none" %>
        <% end %>
      </li>
```

---

## 6) app/views/admin/stores/edit.html.erb（追記）

フォームの下（ボタン行の直前でも直後でもOK）にセクションを追加します。おすすめはフォームの最後、ボタンの前に `hr` を入れる。

```erb
  <hr class="my-4">

  <div class="card mb-4" style="max-width: 720px;">
    <div class="card-header d-flex justify-content-between align-items-center">
      <div>
        精算・振込設定
        <% if @store.payout_account_unconfigured? %>
          <span class="badge text-bg-warning ms-2">未設定</span>
        <% end %>
      </div>

      <%= link_to "設定する", edit_admin_store_payout_account_path(@store), class: "btn btn-sm btn-outline-primary" %>
    </div>

    <div class="card-body">
      <% if @store.payout_account_configured? %>
        <% last4 = @store.payout_account_last4 %>
        <div class="text-muted">
          口座番号（下4桁）：<strong><%= last4.presence || "-" %></strong>
        </div>
      <% else %>
        <div class="text-muted">振込先口座が未設定です。精算の前に設定してください。</div>
      <% end %>
    </div>
  </div>
```

---

# テスト追加（新規）

## test/integration/admin_store_payout_account_test.rb（新規）

```ruby
# frozen_string_literal: true

require "test_helper"

class AdminStorePayoutAccountTest < ActionDispatch::IntegrationTest
  setup do
    @store1 = Store.create!(name: "store1")
    @store2 = Store.create!(name: "store2")

    @store_admin  = User.create!(email: "admin_pa@example.com", password: "password", role: :store_admin)
    @system_admin = User.create!(email: "sys_pa@example.com", password: "password", role: :system_admin)

    StoreMembership.create!(store: @store1, user: @store_admin, membership_role: :admin)
  end

  test "store_admin can access own payout_account edit" do
    sign_in @store_admin, scope: :user

    get edit_admin_store_payout_account_path(@store1)
    assert_response :success
  end

  test "store_admin cannot access other store payout_account edit (403)" do
    sign_in @store_admin, scope: :user

    get edit_admin_store_payout_account_path(@store2)
    assert_response :forbidden
  end

  test "system_admin can access any store payout_account edit" do
    sign_in @system_admin, scope: :user

    get edit_admin_store_payout_account_path(@store2)
    assert_response :success
  end

  test "update creates new record and keeps active only one (old becomes inactive)" do
    sign_in @store_admin, scope: :user

    old = StorePayoutAccount.create!(
      store: @store1,
      payout_method: :manual_bank,
      status: :active,
      bank_code: "0001",
      branch_code: "001",
      account_type: :ordinary,
      account_number: "1234567",
      account_holder_kana: "テスト"
    )

    patch admin_store_payout_account_path(@store1), params: {
      store_payout_account: {
        bank_code: "0005",
        branch_code: "123",
        account_type: "current",
        account_number: "7654321",
        account_holder_kana: "テストカナ"
      }
    }

    assert_response :redirect
    assert_redirected_to edit_admin_store_payout_account_path(@store1)

    @store1.reload
    assert_equal 1, @store1.store_payout_accounts.active.count
    assert_equal 1, @store1.store_payout_accounts.inactive.count
    assert_equal "inactive", old.reload.status
  end

  test "dashboard shows unconfigured badge when payout account is missing" do
    sign_in @store_admin, scope: :user

    # current_store を store1 に
    post admin_current_store_path, params: { store_id: @store1.id }
    follow_redirect!
    assert_response :success

    get dashboard_path
    assert_response :success
    assert_select "span.badge", text: "未設定"
  end

  test "admin store edit shows unconfigured badge when payout account is missing" do
    sign_in @store_admin, scope: :user

    get edit_admin_store_path(@store1)
    assert_response :success
    assert_select "span.badge", text: "未設定"
  end

  test "account number is not fully displayed on edit screen (only last4)" do
    sign_in @store_admin, scope: :user

    StorePayoutAccount.create!(
      store: @store1,
      payout_method: :manual_bank,
      status: :active,
      bank_code: "0001",
      branch_code: "001",
      account_type: :ordinary,
      account_number: "1234567",
      account_holder_kana: "テスト"
    )

    get edit_admin_store_payout_account_path(@store1)
    assert_response :success

    # フル表示は禁止
    assert_no_match(/1234567/, response.body)
    # 下4桁は表示されてよい
    assert_match(/4567/, response.body)
  end
end
```

---

## 動作確認コマンド（PowerShell / Docker前提）

```bash
docker compose run --rm app rails test test/integration/admin_store_payout_account_test.rb
docker compose run --rm app rails test
```

---

## 期待される完成状態（Doneチェック）

* store_admin が自店舗の口座設定ページに入れる / 他店舗は 403
* system_admin は全店舗で edit/update できる
* 更新は履歴保持（旧inactive、新activeは常に1件）
* dashboard と admin/stores/edit の両方に導線 + 未設定バッジ
* 既存口座番号は **下4桁のみ**表示（フォームにプリフィルしない）
* 「残高/引出/出金」等の金融誤認ワードを UI に出さない

---

このまま実装してテストが通れば #263 は Done にできます。次に「実装したらテストで落ちた」系が出たら、そのログ貼ってくれれば差分で詰めます。
