## 0) ゴール（このIssueで“できた”と言える状態）

* `routes.rb` に **顧客 / キャスト / 店舗管理 / システム管理** の導線ができている 
* 各Controllerが **認証→認可→Service呼び出し→レスポンス** の形に揃っている 
* 認可は `Policy`（もしくは同等の仕組み）で **入口が統一**されている 
* “ダミーでもOK”で主要エンドポイントが 200/302/401/403 など **意図した応答**を返せる

---
## 1) ルーティングを骨組み化（名前空間を確定）

`config/routes.rb` を「public / cast / admin」に分けて固定します（中身のアクションはスタブでOK）。

```rb
Rails.application.routes.draw do
  get "up" => "rails/health#show", as: :rails_health_check

  devise_for :users, skip: %i[registrations]

  root "home#show"

  # --- Public (customer) ---
  resources :booths, only: %i[show]

  resources :stream_sessions, only: [] do
    resources :comments, only: %i[create], module: :stream_sessions
    resources :drink_orders, only: %i[create], module: :stream_sessions

    resource :presence, only: [] do
      post :ping
    end
  end

  namespace :wallet do
    resources :purchases, only: %i[create]
  end

  get  "/checkout/return", to: "checkout#return"
  post "/webhooks/stripe", to: "webhooks/stripe#create"

  # --- Cast ---
  namespace :cast do
    resources :booths, only: %i[index show] do
      patch :status, on: :member
      resources :stream_sessions, only: %i[create], module: :booths
    end

    resources :stream_sessions, only: [] do
      post :end, on: :member
      get  :pending_drink_orders, on: :member
    end

    resources :drink_orders, only: [] do
      post :consume, on: :member
    end
  end

  # --- Store Admin ---
  namespace :admin do
    resource :store, only: %i[show update]
    resources :booths, only: %i[index create update]
    resources :drink_items, only: %i[index create update destroy]
    resources :store_bans, only: %i[create destroy]
    get "/cast_metrics", to: "metrics#cast"
  end
end
```

この段階では controller が `render plain:` でもOK。

---

## 2) 認可の入口を統一する（require_role! を確定版で）

`User` に enum があるので、`current_user.role.to_sym` は確実に取れます。

`app/controllers/application_controller.rb` に追加：

```rb
class ApplicationController < ActionController::Base
  private

  def require_role!(*roles)
    authenticate_user! # Devise

    role = current_user.role.to_sym
    return if roles.include?(role)

    head :forbidden
  end

  def authorize!(policy_class, record, action)
    policy = policy_class.new(current_user, record)
    head(:forbidden) unless policy.public_send("#{action}?")
  end
end
```

ポイント：

* `authenticate_user!` を **require_role! 内で呼ぶ**と、BaseControllerがスッキリします
* 403 は `head :forbidden` で統一（HTML画面は後で整える）

---

## 3) 名前空間 BaseController を作る（cast/admin）

`app/controllers/cast/base_controller.rb`

```rb
module Cast
  class BaseController < ApplicationController
    before_action -> { require_role!(:cast, :system_admin) }
  end
end
```

`app/controllers/admin/base_controller.rb`

```rb
module Admin
  class BaseController < ApplicationController
    before_action -> { require_role!(:store_admin, :system_admin) }
  end
end
```

これで **cast配下/admin配下の入口が完全に統一**できます。

---

## 4) Policy の置き場を作る（services/authorization）

まず共通：

`app/services/authorization/application_policy.rb`

```rb
module Authorization
  class ApplicationPolicy
    def initialize(user, record)
      @user = user
      @record = record
    end

    private

    attr_reader :user, :record
  end
end
```

例：`app/services/authorization/booth_policy.rb`

```rb
module Authorization
  class BoothPolicy < ApplicationPolicy
    def show?
      true
    end

    def cast_operate?
      user.cast? || user.system_admin?
    end

    def admin_operate?
      user.store_admin? || user.system_admin?
    end
  end
end
```

※この段階は **ロジックが正しい必要はない**です。「置き場」と「呼び方」を固定するのが目的。

---

## 5) Controller は“薄型スタブ”で作って routes 到達確認

例：`app/controllers/cast/booths_controller.rb`

```rb
module Cast
  class BoothsController < BaseController
    def index
      render plain: "cast booths index (stub)"
    end

    def show
      render plain: "cast booths show (stub)"
    end
  end
end
```

例：`app/controllers/admin/booths_controller.rb`

```rb
module Admin
  class BoothsController < BaseController
    def index
      render plain: "admin booths index (stub)"
    end
  end
end
```

Public側：`app/controllers/booths_controller.rb`

```rb
class BoothsController < ApplicationController
  def show
    # booth = Booth.find(params[:id])   # 後で
    # authorize!(Authorization::BoothPolicy, booth, :show)
    render plain: "booth show (stub)"
  end
end
```
---

## 6) デバッグ用HomeController作成 

### 1) HomeController を作る

`app/controllers/home_controller.rb`

```rb
class HomeController < ApplicationController
  def show
  end
end
```

### 2) Home の view を作ってログアウトボタンを置く

`app/views/home/show.html.erb`

```erb
<h1>home (stub)</h1>

<p>
  <%= link_to "cast booths", cast_booths_path %> |
  <%= link_to "admin booths", admin_booths_path %>
</p>

<% if user_signed_in? %>
  <p>ログイン中: <%= current_user.email %>（role: <%= current_user.role %>）</p>

  <%= button_to "ログアウト", destroy_user_session_path, method: :delete %>
<% else %>
  <%= link_to "ログイン", new_user_session_path %>
<% end %>
```

これで：

* 未ログインなら「ログイン」リンクが出る
* ログイン中なら **メール/role表示 + ログアウトボタン**
* ついでに `/cast/booths` と `/admin/booths` へワンクリックで行ける

---

## 7) 動作確認

### 1) まず routes で Devise と対象URLが存在するか確認

```bash
docker compose run --rm app bin/rails routes | findstr /I "devise user session cast admin booths"
```

最低限、これが見えればOKです。

* `/users/sign_in`（またはカスタムしたログインURL）
* `cast_booths` の index
* `admin_booths` の index

---

### 2) 未ログイン時の確認（302になるか）

**未ログインで cast にアクセス**

* ブラウザで `http://localhost:3000/cast/booths`

* **302 → /users/sign_in** になる（Devise標準）

同様に `http://localhost:3000/admin/booths` も同じ。

---

### 3) ログイン済みで role別に 200/403 を確認する方法

#### 4.1 まず role 別ユーザーを用意（seedでもrails consoleでもOK）

```bash
docker compose run --rm app bin/rails runner "User.create!(email: 'cast@example.com', password: 'password', role: :cast)"
docker compose run --rm app bin/rails runner "User.create!(email: 'admin@example.com', password: 'password', role: :store_admin)"
docker compose run --rm app bin/rails runner "User.create!(email: 'cust@example.com', password: 'password', role: :customer)"
docker compose run --rm app bin/rails runner "User.create!(email: 'sys@example.com',  password: 'password', role: :system_admin)"
```

#### 4.2 ブラウザでログインして確認

* `http://localhost:3000/` → `/users/sign_in` で `cast@example.com` でログイン → `/cast/booths` が 200、`/admin/booths` は 403（想定）
* `admin@example.com` でログイン → `/admin/booths` が 200、`/cast/booths` は 403（想定）
* `cust@example.com` でログイン → 両方403（想定）
* `sys@example.com` でログイン → 両方200（想定）

**この確認は、UIがまだスタブでもできます（plain textが返れば200）。**

---

## 7) この構成で #14 が“完了”と言える Done 定義

* [x] `routes.rb` が public/cast/admin に整理されている
* [x] `Cast::BaseController` / `Admin::BaseController` が存在し role で弾ける
* [x] `require_role!` が `ApplicationController` にあり、Controllerに散らばってない
* [x] `Authorization::*Policy` の置き場ができ、`authorize!` で呼べる形になっている
* [x] 主要エンドポイントがスタブでも到達できる（200/302/403）

---

Closes #14
Relates to #12
