実装プランです。

1. `Stores::RegistrationsController#create` の成功時処理を変更する

   * `sign_in(@form.user)` はそのまま維持
   * `session[:current_store_id] = @form.store.id` を設定
   * `session.delete(:current_booth_id)` を実行
   * 遷移先を `dashboard_path` から `edit_admin_store_path(@form.store)` に変更
   * フラッシュ文言を更新

2. 変更範囲は最小限に留める

   * `RegistrationForm` / `RegisterStoreAdmin` / `Admin::CurrentStoresController` には手を入れない
   * 既存の current 切替ルールに合わせるだけに留める

3. Done 観点

   * 店舗登録直後に作成店舗が `current_store` になる
   * `current_booth_id` は安全側でクリアされる
   * そのまま店舗設定画面へ入れる
   * 既存の `admin_current_store_path` の挙動には影響しない

貼り付け用コードです。

`app/controllers/stores/registrations_controller.rb`

```ruby
# frozen_string_literal: true

module Stores
  class RegistrationsController < ApplicationController
    skip_before_action :authenticate_user!, raise: false

    def new
      @form = Stores::RegistrationForm.new(referral_code: params[:ref].to_s)
    end

    def create
      @form = Stores::RegistrationForm.new(registration_params)

      if @form.save
        sign_in(@form.user) # Devise
        session[:current_store_id] = @form.store.id
        session.delete(:current_booth_id)

        redirect_to edit_admin_store_path(@form.store), notice: "店舗登録が完了しました。続けて店舗情報を入力してください。"
      else
        render :new, status: :unprocessable_entity
      end
    end

    private

    def registration_params
      params.require(:store_registration).permit(
        :store_name,
        :email,
        :password,
        :password_confirmation,
        :referral_code
      )
    end
  end
end
```

補足です。

* `session[:current_booth_id]` は、現状の登録フローでは通常未設定のはずですが、既存の `Admin::CurrentStoresController#create` に合わせて常に消す実装で問題ありません。
* 今回は `store` 作成と `membership` 付与が `RegisterStoreAdmin` 側で完結しているため、controller 側はこの最小変更で十分です。
* `edit_admin_store_path(@form.store)` への遷移も、現在の認可条件で通る構成になっています。

必要なら次に、この Issue #440 用のコミットメッセージと PR 文もそのまま書きます。
