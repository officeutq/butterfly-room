### 0) migration

```bash
docker compose run --rm app bin/rails g migration AddArchivedAtToBooths
```

生成された `db/migrate/*_add_archived_at_to_booths.rb` を**全文置換**:

```ruby
# frozen_string_literal: true

class AddArchivedAtToBooths < ActiveRecord::Migration[8.1]
  def change
    add_column :booths, :archived_at, :datetime

    add_index :booths, :archived_at
    add_index :booths, %i[store_id archived_at]
  end
end
```

```bash
docker compose run --rm app rails db:migrate
```

---

### 1) `Booth` モデル

```ruby
# app/models/booth.rb

scope :active, -> { where(archived_at: nil) }
scope :archived, -> { where.not(archived_at: nil) }

def archived?
  archived_at.present?
end
```

---

### 2) Public：アーカイブ済みは 404

```ruby
# app/controllers/booths_controller.rb

def set_booth
  @booth = Booth.active.find(params[:id])
end
```

---

### 3) Admin routes：archive 追加

```ruby
# config/routes.rb

namespace :admin do
  resources :booths, only: %i[index show new edit create update] do
    member do
      get :watch
      patch :archive
    end
  end
end
```

---

### 4) Admin#index：現役のみデフォルト + 切替

`app/controllers/admin/booths_controller.rb`

```ruby
module Admin
  class BoothsController < Admin::BaseController
    ...
    before_action :set_booth, only: %i[show edit update watch]
    ...

    def index
      @include_archived = ActiveModel::Type::Boolean.new.cast(params[:archived])

      scope = current_store.booths
      scope = scope.active unless @include_archived

      @booths = scope.order(id: :desc)
    end
    ...
    # Admin#archive：offline のみ許可（強制終了は別Issue）
    def archive
      policy = Authorization::BoothPolicy.new(current_user, @booth)
      head :forbidden and return unless policy.update?

      booth = Booth.lock.find(@booth.id)

      if !booth.offline? || booth.current_stream_session_id.present?
        redirect_to admin_booths_path,
                    alert: "ブース##{booth.id}（#{booth.name}）は配信中の可能性があるためアーカイブできません。先に配信を終了してください。"
        return
      end

      booth.update!(archived_at: Time.current)

      redirect_to admin_booths_path, notice: "ブースをアーカイブしました"
    end
    ...
```

---

### 6) Admin一覧UI：切替リンク + archived 表示

`app/views/admin/booths/index.html.erb`

```erb
<%# 仮: flash表示（本来は layout に寄せたいが、まず index で見えるようにする） %>
<% if flash[:alert].present? %>
  <div class="alert alert-danger" role="alert">
    <%= flash[:alert] %>
  </div>
<% end %>

<% if flash[:notice].present? %>
  <div class="alert alert-success" role="alert">
    <%= flash[:notice] %>
  </div>
<% end %>

<h1>ブース管理</h1>

<div class="mb-3 d-flex gap-2 align-items-center">
  <%= link_to "新規作成", new_admin_booth_path, class: "btn btn-primary" %>

  <% if params[:archived].present? %>
    <%= link_to "現役のみ表示", admin_booths_path, class: "btn btn-outline-secondary" %>
  <% else %>
    <%= link_to "アーカイブも表示", admin_booths_path(archived: 1), class: "btn btn-outline-secondary" %>
  <% end %>
</div>

<table class="table table-sm align-middle">
  <thead>
    <tr>
      <th>ID</th>
      <th>名前</th>
      <th>Status</th>
      <th>IVS Stage ARN</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @booths.each do |booth| %>
      <tr class="<%= "text-muted" if booth.archived? %>">
        <td><%= booth.id %></td>

        <td>
          <%= booth.name %>
          <% if booth.archived? %>
            <span class="badge text-bg-secondary ms-2">archived</span>
          <% end %>
        </td>

        <td><%= booth.status %></td>

        <td class="text-muted small"><%= booth.ivs_stage_arn.presence || "-" %></td>

        <td class="d-flex justify-content-end gap-2">
          <%= link_to "詳細", admin_booth_path(booth), class: "btn btn-outline-secondary btn-sm" %>

          <% if booth.archived? %>
            <%# archived は操作なし %>
          <% elsif booth.offline? && booth.current_stream_session_id.blank? %>
            <%= button_to "アーカイブ",
                  archive_admin_booth_path(booth),
                  method: :patch,
                  class: "btn btn-outline-danger btn-sm",
                  data: { turbo_confirm: "このブースをアーカイブします。よろしいですか？" } %>
          <% else %>
            <span class="text-muted small">配信終了後にアーカイブ可</span>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
```

（一覧にアーカイブボタンも置くなら `button_to archive_admin_booth_path` を追加。ただし “offline限定” なので、押したら alert で止まるのもOK）

---

### 7) Cast：アーカイブ済み表示 + スタンバイ開始無効化

`app/views/cast/booths/show.html.erb`

```erb
<% if @booth.archived? %>
  <div class="alert alert-warning">
    このブースはアーカイブされています。配信は開始できません。
  </div>
<% end %>

<% if @booth.archived? %>
<% else %>
  <% policy = Authorization::BoothPolicy.new(current_user, @booth) %>
  <% if policy.update? %>
    <%= link_to "ブース編集", edit_cast_booth_path(@booth), class: "btn btn-outline-secondary btn-sm" %>
  <hr>
  <% end %>
<% end %>

<% if @booth.archived? %>
  <button class="btn btn-primary" disabled>スタンバイ開始</button>
<% else %>
  <%= button_to "スタンバイ開始",
        cast_booth_stream_sessions_path(@booth),
        method: :post,
        class: "btn btn-primary" %>
<% end %>
```

---

### 8) 配信開始の最終防衛線：StartService で弾く

```ruby
# app/services/stream_sessions/start_service.rb

class BoothArchived < Error; end
```

```ruby
booth = Booth.lock.find(@booth.id)

raise BoothArchived, "booth is archived" if booth.archived?

raise BoothNotOffline, "booth is #{booth.status}" unless booth.offline?
```

---

## このIssueでは “やらない” と決めるもの

* アーカイブ時の強制終了（EndService 実行）
* 管理画面の強制終了導線（store_admin でも可能にする）

→ これは新Issueに切ったものをそこで実装。

---

必要なら、#133 の PR 文（背景/原因/修正点/確認手順/Closes #133）もこの確定方針に合わせてすぐ書けます。
