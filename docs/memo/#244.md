了解、GOで進めます。#244 は **DBに `users.bio:text` を追加**＋**Userに最大500文字バリデーション**＋**モデルテスト追加**で完了です（Epic #243 の他要素には踏み込みません）。

---

## ブランチ名候補（最終）

* `feature/244-add-user-bio`
* `feature/244-users-bio`
* `improve/244-user-profile-bio`

---

## 実装手順（docker compose 経由）

### 1) 空のマイグレーション生成

```bash
docker compose exec app bash -lc "bin/rails g migration AddBioToUsers"
```

### 2) マイグレーションを手書きで編集

生成されたファイル（例：`db/migrate/2026xxxxxxxxxx_add_bio_to_users.rb`）を次に差し替え。

```ruby
# frozen_string_literal: true

class AddBioToUsers < ActiveRecord::Migration[8.1]
  def change
    add_column :users, :bio, :text, null: true
  end
end
```

### 3) User モデルに length バリデーション追加

`app/models/user.rb` に `validates` を追加（enum/associations の近くでOK。例として enum の下に置きます）

```ruby
class User < ApplicationRecord
  devise :database_authenticatable, :recoverable, :rememberable, :validatable

  enum :role, { customer: 0, cast: 1, store_admin: 2, system_admin: 3 }

  validates :bio, length: { maximum: 500 }, allow_nil: true

  ROLE_LEVELS = {
    customer: 0,
    cast: 1,
    store_admin: 2,
    system_admin: 3
  }.freeze

  has_one :wallet, foreign_key: :customer_user_id, dependent: :destroy, inverse_of: :customer_user
  has_many :store_memberships, dependent: :destroy
  has_many :stores, through: :store_memberships
  has_many :favorite_booths, dependent: :destroy
  has_many :favorite_stores, dependent: :destroy

  # --- Soft delete ---
  def deleted?
    deleted_at.present?
  end

  # Devise: 停止ユーザーはログイン不可
  def active_for_authentication?
    super && !deleted?
  end

  def role_level
    ROLE_LEVELS.fetch(role.to_sym)
  rescue KeyError, NoMethodError
    -1
  end

  def at_least?(required_role)
    required_level = ROLE_LEVELS.fetch(required_role.to_sym)
    role_level >= required_level
  rescue KeyError
    false
  end

  def admin_of_store?(store_id)
    return false if store_id.blank?
    store_memberships.admin_only.exists?(store_id: store_id)
  end
end
```

### 4) モデルテストを新規追加

`test/models/user_test.rb` を新規作成。

```ruby
# frozen_string_literal: true

require "test_helper"

class UserTest < ActiveSupport::TestCase
  test "bio is optional (nil is allowed)" do
    user = User.new(email: "bio_nil@example.com", password: "password", role: :customer, bio: nil)
    assert user.valid?
  end

  test "bio length must be <= 500" do
    user = User.new(email: "bio_len@example.com", password: "password", role: :customer)

    user.bio = "a" * 500
    assert user.valid?

    user.bio = "a" * 501
    assert_not user.valid?
    assert_includes user.errors[:bio], "is too long (maximum is 500 characters)"
  end
end
```

### 5) マイグレーション実行 & schema.rb 確認

```bash
docker compose exec app bash -lc "bin/rails db:migrate"
```

`db/schema.rb` の `users` に `t.text "bio"` が入っていることを確認。

### 6) テスト実行

```bash
docker compose exec app bash -lc "bin/rails test"
```

---

## Done条件チェック

* ✅ マイグレーションが通る
* ✅ `bio` 保存できる（null可 / 500文字までOK）
* ✅ 501文字でバリデーションエラー（テストで担保）

---

## コミットメッセージ案

* `feat(#244): add users.bio and validate max length`

## PR本文案（そのまま貼れる）

* 変更点

  * users に `bio:text`（null可）を追加
  * User に `bio` 最大500文字バリデーションを追加
  * User のモデルテストを追加（500/501文字境界）
* 仕様

  * `bio` は任意（nil可）
  * 500文字まで保存可、501文字以上はエラー
* 動作確認

  * `bin/rails db:migrate` が成功
  * `bin/rails test` が成功

このままコピペで進めればOKです。
