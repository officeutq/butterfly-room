了解です。以下が最終フル差分です。

---

## `config/routes.rb`

変更前：

```ruby
resources :stream_sessions, only: %i[create], module: :booths
```

変更後：

```ruby
resources :stream_sessions, only: %i[index create], module: :booths
```

---

## `app/controllers/cast/booths/stream_sessions_controller.rb`

全文置き換え：

```ruby
# frozen_string_literal: true

module Cast
  module Booths
    class StreamSessionsController < Cast::BaseController
      before_action :require_current_booth!, only: %i[index]

      def index
        @booth = current_booth

        @stream_sessions =
          @booth
            .stream_sessions
            .ended
            .includes(:started_by_cast_user)
            .order(started_at: :desc, id: :desc)
      end

      def create
        booth = current_booth
        if booth.blank?
          redirect_to cast_booths_path, alert: "選択できないブースです"
          return
        end

        result = ::Booths::EnterAsCastService.new(
          booth: booth,
          actor: current_user
        ).call

        case result.action
        when :redirect_live
          redirect_to live_cast_booth_path(result.booth), notice: "配信画面を開きました"
        when :occupied_by_other
          redirect_to cast_booths_path, alert: "このブースはすでに配信中です"
        when :already_live_elsewhere
          redirect_to cast_booths_path, alert: "他のブースで配信中のため開始できません"
        else
          redirect_to cast_booths_path, alert: "配信導線の開始に失敗しました"
        end
      rescue ::Booths::EnterAsCastService::NotAuthorized
        redirect_to cast_booths_path, alert: "選択できないブースです"
      end
    end
  end
end
```

---

## 新規作成

## `app/views/cast/booths/stream_sessions/index.html.erb`

```erb
<% content_for :title, "配信履歴" %>

<div class="mb-4">
  <div class="text-muted small"><%= @booth.store.name %></div>
  <h1 class="h4 fw-bold mb-0"><%= @booth.name.presence || "(no name)" %></h1>
</div>

<% if @stream_sessions.blank? %>
  <div class="alert alert-warning" role="alert">
    配信履歴はまだありません。
  </div>
<% else %>
  <div class="row g-4">
    <% @stream_sessions.each do |stream_session| %>
      <% duration_seconds = stream_session.broadcast_duration_seconds %>
      <% hours = duration_seconds / 3600 %>
      <% minutes = (duration_seconds % 3600) / 60 %>
      <% seconds = duration_seconds % 60 %>

      <div class="col-12 col-lg-6 col-xxl-4">
        <div class="card h-100" data-bs-theme="light">
          <div class="card-body d-flex flex-column">
            <div class="mb-3">
              <h2 class="h5 fw-bold mb-2">
                <%= stream_session.title.presence || @booth.name.presence || "(no title)" %>
              </h2>

              <div class="d-flex flex-column gap-2 small">
                <div>
                  <span class="text-body-secondary">配信者：</span>
                  <%= display_name_or_anonymous(stream_session.started_by_cast_user) %>
                </div>

                <div>
                  <span class="text-body-secondary">配信開始：</span>
                  <%= stream_session.broadcast_started_at.present? ? l(stream_session.broadcast_started_at, format: :long) : "-" %>
                </div>

                <div>
                  <span class="text-body-secondary">配信時間：</span>
                  <%= format("%02d:%02d:%02d", hours, minutes, seconds) %>
                </div>
              </div>
            </div>

            <div class="mt-auto">
              <%= link_to "詳細を見る",
                    cast_stream_session_path(stream_session),
                    class: "btn btn-outline-primary btn-sm" %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
<% end %>
```

---

## `app/controllers/cast/stream_sessions_controller.rb`

`show` 内のこの部分：

```ruby
@booth = booth

@comment_count = comments_scope.count
```

変更後：

```ruby
@booth = booth
@cast_user = @stream_session.started_by_cast_user

@comment_count = comments_scope.count
```

---

## `app/views/cast/stream_sessions/show.html.erb`

この行の下に追加：

```erb
<div><span class="text-body-secondary">配信名：</span><%= @stream_session.title.presence || @booth.name %></div>
```

追加するコード：

```erb
<div>
  <span class="text-body-secondary">配信者：</span>
  <%= display_name_or_anonymous(@cast_user) %>
</div>
```

---

## `app/views/dashboard/show.html.erb`

`ブース情報` と `ブース編集` のカードの間、または `ブース編集` の後に追加：

```erb
<div class="col-12 col-md-6 col-xl-4">
  <% if booth.present? %>
    <%= link_to cast_booth_stream_sessions_path(booth), class: "text-decoration-none" do %>
      <div class="card h-100 dashboard-role-card dashboard-role-card-cast" data-bs-theme="light">
        <div class="card-body">
          <h5 class="card-title mb-2">配信履歴</h5>
          <p class="card-text mb-0">現在のブースの過去配信を確認できます</p>
        </div>
      </div>
    <% end %>
  <% else %>
    <%= link_to select_modal_cast_booths_path(return_to_key: "booth_stream_sessions"),
                data: { turbo_frame: "modal" },
                class: "text-decoration-none" do %>
      <div class="card h-100 dashboard-role-card dashboard-role-card-cast" data-bs-theme="light">
        <div class="card-body">
          <h5 class="card-title mb-2">配信履歴</h5>
          <p class="card-text mb-0">現在のブースの過去配信を確認できます</p>
        </div>
      </div>
    <% end %>
  <% end %>
</div>
```

---

## `app/controllers/cast/booths_controller.rb`

`resolve_return_to_key` を変更。

変更前：

```ruby
case key.to_s
when "booth_show"
  cast_booth_path(booth)
when "booth_edit"
  edit_cast_booth_path(booth)
when "booth_live"
  live_cast_booth_path(booth)
else
  nil
end
```

変更後：

```ruby
case key.to_s
when "booth_show"
  cast_booth_path(booth)
when "booth_edit"
  edit_cast_booth_path(booth)
when "booth_live"
  live_cast_booth_path(booth)
when "booth_stream_sessions"
  cast_booth_stream_sessions_path(booth)
else
  nil
end
```

---

## `app/controllers/cast/current_booths_controller.rb`

こちらも同じく `resolve_return_to_key` を変更。

変更前：

```ruby
case key.to_s
when "booth_show"
  cast_booth_path(booth)
when "booth_edit"
  edit_cast_booth_path(booth)
when "booth_live"
  live_cast_booth_path(booth)
else
  nil
end
```

変更後：

```ruby
case key.to_s
when "booth_show"
  cast_booth_path(booth)
when "booth_edit"
  edit_cast_booth_path(booth)
when "booth_live"
  live_cast_booth_path(booth)
when "booth_stream_sessions"
  cast_booth_stream_sessions_path(booth)
else
  nil
end
```

---

最後に確認コマンド：

```bash
docker compose exec web bin/rails routes | findstr stream_sessions
docker compose exec web bin/rails test
```
