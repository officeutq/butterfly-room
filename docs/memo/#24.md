## 具体的な追加・変更コード

## routes

`config/routes.rb` の admin 部分をこれに変更：

```ruby
namespace :admin do
  root "dashboard#show"

  resource :store, only: %i[show update]
  resources :booths, only: %i[index create update]
  resources :drink_items, only: %i[index create update destroy]

  resources :store_bans, only: %i[index create destroy] # ★ index 追加

  resources :casts, only: %i[index create destroy]
  get "/cast_metrics", to: "metrics#cast"
end
```

---

## A. `app/services/authorization/store_ban_checker.rb`（新規）

```ruby
# frozen_string_literal: true

module Authorization
  class StoreBanChecker
    def initialize(store:, user:)
      @store = store
      @user = user
    end

    def banned?
      return false if @store.blank? || @user.blank?
      return false unless @user.customer?

      StoreBan.exists?(store_id: @store.id, customer_user_id: @user.id)
    end
  end
end
```

---

## B. `app/controllers/concerns/store_ban_guard.rb`（新規）

```ruby
# frozen_string_literal: true

module StoreBanGuard
  extend ActiveSupport::Concern

  private

  def reject_banned_customer!(store:)
    return if store.blank?
    return unless user_signed_in?
    return unless current_user.customer?

    if Authorization::StoreBanChecker.new(store: store, user: current_user).banned?
      render_banned!
    end
  end

  def render_banned!
    respond_to do |format|
      format.json do
        render json: { error: "banned" }, status: :forbidden
      end

      format.turbo_stream do
        html = <<~HTML
          <turbo-stream action="append" target="body">
            <template>
              <script>
                window.location.href = #{root_path.to_json};
              </script>
            </template>
          </turbo-stream>
        HTML

        render plain: html, content_type: "text/vnd.turbo-stream.html", status: :ok
      end

      format.html do
        redirect_to root_path, status: :see_other
      end

      format.any { head :forbidden }
    end
  end
end
```

> turbo_stream でフォーム差し替えしたい場合は、各 controller 側で `render ... status: :forbidden` にしてOK（後述）。

---

## C. 入室（booths#show）にBANチェック

`app/controllers/booths_controller.rb` に Concern を include して、show 冒頭で弾きます。
※「未ログイン閲覧はOK」にしておく（BANは user が特定できないので）。

```ruby
# frozen_string_literal: true

class BoothsController < ApplicationController
  include StoreBanGuard

  before_action :set_booth, only: %i[show]
  before_action :reject_banned_customer_for_booth!, only: %i[show]

  def show
    @stream_session = @booth.current_stream_session
    @drink_items = @booth.store.drink_items.enabled_only.ordered

    @wallet =
      if user_signed_in?
        Wallet.find_or_create_by!(customer_user_id: current_user.id) do |w|
          w.available_points = 0
          w.reserved_points = 0
        end
      end

    @comments =
      if @stream_session.present?
        Comment.alive.where(stream_session: @stream_session)
               .order(created_at: :desc)
               .limit(50)
               .reverse
      else
        []
      end
  end

  private

  def set_booth
    @booth = Booth.find(params[:id])
  end

  def reject_banned_customer_for_booth!
    reject_banned_customer!(store: @booth.store)
  end
end
```

---

## D. コメント：BAN時に 403 にする（Done条件対応）

`app/controllers/stream_sessions/comments_controller.rb`

```ruby
module StreamSessions
  class CommentsController < ApplicationController
    include StoreBanGuard

    before_action :authenticate_user!
    before_action :set_stream_session
    before_action :reject_banned_customer_for_stream_session!

    def create
      # 既存の処理...
    end

    private

    def set_stream_session
      @stream_session = StreamSession.find(params[:stream_session_id])
    end

    def reject_banned_customer_for_stream_session!
      reject_banned_customer!(store: @stream_session.store)
    end
  end
end
```

before_action でBANを弾くなら、ここは削除でOKです：

```ruby
rescue StreamSessions::Comments::CreateService::BannedError
  render_comment_form_error("BANされています")
```

---

## E. ドリンク送信：コントローラ入口でBANチェック＋ rescueの返し統一

今の `rescue DrinkOrders::CreateService::Forbidden` は **JSON固定**なので、HTMLのときに崩れます。
また、BANとそれ以外のforbiddenが混ざるので、**BANはコントローラ入口で確定させる**のがよいです。

`app/controllers/stream_sessions/drink_orders_controller.rb` をこうします：

```ruby
module StreamSessions
  class DrinkOrdersController < ApplicationController
    include StoreBanGuard

    before_action :authenticate_user!
    before_action :require_customer!
    before_action :set_stream_session
    before_action :reject_banned_customer_for_stream_session!

    def create
      # 既存の処理...
    end

    private

    def set_stream_session
      @stream_session = StreamSession.find(params[:stream_session_id])
    end

    def reject_banned_customer_for_stream_session!
      reject_banned_customer!(store: @stream_session.store)
    end

    def require_customer!
      head :forbidden unless current_user.customer?
    end
  end
end
```

---

## F. presence ping：BANユーザーの同接を増やさない（推奨）

`StreamSessions::PresencesController` にも入れます。

```ruby
class StreamSessions::PresencesController < ApplicationController
  include StoreBanGuard

  before_action :authenticate_user!
  before_action :set_stream_session
  before_action :reject_banned_customer_for_stream_session!

  def ping
    # 既存の処理...
  end

  private

  def set_stream_session
    @stream_session = StreamSession.find(params[:stream_session_id])
  end

  def reject_banned_customer_for_stream_session!
    reject_banned_customer!(store: @stream_session.store)
  end
end

```

---

# 管理側（Admin）実装

## G. Store 側に関連が無ければ追加（store.rb）

```ruby
class Store < ApplicationRecord
  has_many :store_bans, dependent: :destroy
end
```

## H. `app/controllers/admin/store_bans_controller.rb`（新規）

```ruby
# frozen_string_literal: true

module Admin
  class StoreBansController < Admin::BaseController
    before_action :require_current_store!

    def index
      @store_bans =
        current_store.store_bans
          .includes(:customer_user)
          .order(id: :desc)

      @store_ban = current_store.store_bans.new
    end

    def create
      ban = current_store.store_bans.new(store_ban_params)
      ban.created_by_store_admin_user = current_user

      if ban.save
        redirect_back fallback_location: admin_store_bans_path, notice: "BANしました"
      else
        redirect_back fallback_location: admin_store_bans_path, alert: ban.errors.full_messages.to_sentence
      end
    end

    def destroy
      ban = current_store.store_bans.find(params[:id])
      ban.destroy!
      redirect_back fallback_location: admin_store_bans_path, notice: "BAN解除しました"
    end

    private

    def store_ban_params
      params.require(:store_ban).permit(:customer_user_id, :reason)
    end
  end
end
```

## I. View（最短）

`app/views/admin/dashboard/show.html.erb` に追記：

```erb
<li><%= link_to "店舗BAN", admin_store_bans_path %></li>
```

`app/views/admin/store_bans/index.html.erb`（新規）

```erb
<h1>店舗BAN</h1>

<%= form_with model: @store_ban, url: admin_store_bans_path, scope: :store_ban, method: :post do |f| %>
  <div>
    <%= f.label :customer_user_id, "customer_user_id" %>
    <%= f.number_field :customer_user_id, required: true %>
  </div>
  <div>
    <%= f.label :reason, "理由" %>
    <%= f.text_field :reason %>
  </div>
  <%= f.submit "BANする" %>
<% end %>

<hr>

<ul>
  <% @store_bans.each do |ban| %>
    <li>
      user_id=<%= ban.customer_user_id %>
      (<%= ban.customer_user&.email %>)
      - <%= ban.reason %>
      <%= button_to "解除", admin_store_ban_path(ban), method: :delete %>
    </li>
  <% end %>
</ul>
```

---

## J.BANチェック削除（Service側）
DrinkOrders::CreateService から削除

これを 丸ごと削除です。

```ruby
# BAN
if StoreBan.exists?(store_id: booth.store_id, customer_user_id: @customer_user.id)
  raise Forbidden
end
```

→ controller の before_action で弾くので、service では見ません。


## K. 「BANなら presence_summary も 403」にする

`app\controllers\stream_sessions_controller.rb`

```ruby
# frozen_string_literal: true

class StreamSessionsController < ApplicationController
  include StoreBanGuard

  before_action :authenticate_user!
  before_action :set_stream_session
  before_action :reject_banned_customer_for_stream_session!

  def presence_summary
    viewer_count = Presences::SummaryService.new(
      stream_session: @stream_session,
      threshold_seconds: 45
    ).call!

    render json: { viewer_count: viewer_count }
  end

  private

  def set_stream_session
    @stream_session = StreamSession.find(params[:id])
  end

  def reject_banned_customer_for_stream_session!
    reject_banned_customer!(store: @stream_session.store)
  end
end
```
これで、BAN中ユーザーは

* `ping` も 403

* `presence_summary` も 403
になり、**同接カウントの不整合（BANなのに数だけ増える/表示される）** も防げます。

---

## L. 403 を検知したらトップへ戻す
ポイントは2つ：

* **ping が 403**（BAN）になったら即 `Turbo.visit("/")`
* summary も 403 になりうるので、そっちでも同様に戻す
* さらに「タイミングによってはコメント送信時だけBANが発動」みたいなケースもあるので、**どっちでも検知できる**ようにする

`app\javascript\controllers\presence_poll_controller.js`

```js
import { Controller } from "@hotwired/stimulus"
import { Turbo } from "@hotwired/turbo-rails"

export default class extends Controller {
  static values = {
    pingUrl: String,
    summaryUrl: String,
    intervalMs: Number
  }
  static targets = ["count"]

  connect() {
    this._tick = this._tick.bind(this)
    this._timer = setInterval(this._tick, this.intervalMsValue || 20000)
    this._tick()
  }

  disconnect() {
    if (this._timer) clearInterval(this._timer)
  }

  async _tick() {
    // customer画面だけ pingUrl を渡す（cast画面は渡さない＝書き込み無し）
    if (this.hasPingUrlValue) {
      const res = await fetch(this.pingUrlValue, {
        method: "POST",
        headers: {
          "X-CSRF-Token": document.querySelector("meta[name=csrf-token]").content,
          "Accept": "application/json"
        }
      }).catch(() => null)

      // ★BANなどで403ならトップへ退避（購読も切れる）
      if (res && res.status === 403) {
        Turbo.visit("/")
        return
      }
    }

    const res2 = await fetch(this.summaryUrlValue, {
      headers: { "Accept": "application/json" }
    }).catch(() => null)

    // ★summary側でも403ならトップへ退避
    if (res2 && res2.status === 403) {
      Turbo.visit("/")
      return
    }

    if (!res2 || !res2.ok) return

    const data = await res2.json()
    this.countTarget.textContent = String(data.viewer_count ?? "-")
  }
}
```

### これでどうなる？

BANされたままブースで何もしなくても、

* 次の ping（最大20秒後）で 403 を受けて
* 自動でトップへ戻る
  → 結果として **Turbo Streams の購読も切れる**ので、コメント/ドリンク更新も見えなくなります。

今は `Turbo.visit("/")` にしています。`root_path` が `/` ならこれでOK。

この対応は「ActionCableの購読をサーバ側でrejectする」より軽くて、フェーズ1にはかなりちょうどいいです。

---

## M. 「一瞬ブースが見えてトップに戻る」問題を解消

`app/views/booths/show.html.erb` の先頭をこうする
```erb
<% content_for :head do %>
  <meta name="turbo-cache-control" content="no-cache">
<% end %>
```

これで ブースページだけ Turbo snapshot が効きにくくなります。

---

Closes #24 
Relates to #12 