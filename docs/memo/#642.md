了解です。
まず **profile だけ** の最小差分でいきましょう。

今回の方針はこれです。

* バリデーションエラー時も `display_name` / `bio` は入力値を保持
* avatar は **保存済みの既存画像だけ** 初期表示
* 今回新しく選んだ未保存画像は再表示しない
* 500 を防ぐ
* `remove_avatar` の未許可もついでに解消

---

## 修正内容

### `app/controllers/profiles_controller.rb`

```ruby
# frozen_string_literal: true

class ProfilesController < ApplicationController
  include RemovableImageAttachment
  include AttachmentPersistenceChecker

  before_action :authenticate_user!

  def edit
    @user = current_user
  end

  def update
    @user = current_user

    success = @user.update(profile_params)

    if success
      unless ensure_attachment_persisted!(record: @user, attachment_name: :avatar)
        return render :edit, status: :unprocessable_entity
      end

      purge_attachment_if_requested(
        record: @user,
        attachment_name: :avatar,
        remove_param_name: :remove_avatar
      )

      redirect_to root_path, notice: "プロフィールを更新しました"
    else
      render :edit, status: :unprocessable_entity
    end
  end

  private

  def profile_params
    params.require(:user).permit(:display_name, :bio, :avatar, :remove_avatar)
  end
end
```

---

### `app/views/profiles/edit.html.erb`

```erb
<% content_for :head do %>
  <%= render "shared/filepond_head" %>
<% end %>

<h1>プロフィール編集</h1>

<%= form_with model: @user, url: profile_path do |f| %>
  <% if @user.errors.any? %>
    <div class="alert alert-danger" role="alert">
      <ul class="mb-0">
        <% @user.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= f.label :display_name, "表示名", class: "form-label" %>
    <%= f.text_field :display_name, class: "form-control" %>
    <div class="form-text">未入力の場合は email が表示されます（将来変更の可能性あり）</div>
  </div>

  <div class="mb-3">
    <%= f.label :bio, "自己紹介（bio）", class: "form-label" %>
    <%= f.text_area :bio, class: "form-control", rows: 6 %>
    <div class="form-text">最大500文字</div>
  </div>

  <div
    class="mb-3 image-upload-field"
    data-controller="image-upload"
    data-image-upload-width-value="1024"
    data-image-upload-height-value="1024"
    <% if @user.avatar.attached? && @user.avatar.blob&.persisted? %>
      data-image-upload-initial-url-value="<%= url_for(@user.avatar) %>"
    <% end %>>
    <%= f.label :avatar, "プロフィール画像", class: "form-label" %>

    <%= f.hidden_field :remove_avatar,
          value: "0",
          data: { "image-upload-target": "removeFlag" } %>

    <%= f.file_field :avatar,
          accept: ".jpg,.jpeg,.png,.webp,.heic,.heif",
          data: { "image-upload-target": "input" } %>
  </div>

  <div class="d-flex gap-2">
    <%= f.submit "更新する", class: "btn btn-primary" %>
    <%= link_to "戻る", dashboard_path, class: "btn btn-outline-secondary" %>
  </div>
<% end %>
```

---

## この修正でどうなるか

`bio` を 501文字以上にして更新失敗したとき、

* 500 にならない
* `edit` が `unprocessable_entity` で再描画される
* `display_name` / `bio` は入力値が残る
* avatar は「既存の保存済み画像」が表示される
* 今回新しく選んだ未保存画像は表示されない

という挙動になります。

---

## 確認ポイント

これで次を試せばOKです。

1. `profile/edit` を開く
2. 既存 avatar がある状態で、新しい画像を選ぶ
3. `bio` を 501文字以上にする
4. 更新する

期待結果は、

* 500 にならない
* バリデーションエラーが出る
* `bio` は入力値のまま
* avatar は既存画像のまま

です。

通ったら次に `cast/booths/edit` も同じ考え方で横展開できます。
