了解、GOで最小差分でいきます。

---

## ブランチ名候補

* `improve/247-dashboard-profile-link`

---

## 変更内容（貼り付け用）

### `app/views/dashboard/show.html.erb`

各ロールブロックにあるこの行：

* ` <li class="list-group-item text-muted"> ユーザープロフィール編集（未実装） </li>`

を、下記に **置き換え**ます（4箇所すべて同じ）：

```erb
<li class="list-group-item">
  <%= link_to "プロフィール編集", edit_profile_path, class: "text-decoration-none" %>
</li>
```

それ以外は変更不要です。

---

## 動作確認（手動）

1. customer / cast / store_admin / system_admin でログインしてダッシュボード表示
2. 「プロフィール編集」リンクが表示される
3. クリックで `/profile/edit` に遷移できる
4. レイアウト崩れがない（list-group の並びが維持される）

---

## コマンド（任意）

ルート確認：

```bash
docker compose exec app bash -lc "bin/rails routes" | findstr /I profile
```

テスト：

```bash
docker compose exec app bash -lc "bin/rails test"
```

---

## Done条件

* ✅ ダッシュボードから `/profile/edit` へ遷移できる
* ✅ どのロールでも動作する

---

## コミットメッセージ案

* `improve(#247): add profile edit link to dashboard`

## PR本文案

* 変更点

  * ダッシュボード（全ロール）に「プロフィール編集」リンクを追加（/profile/edit）
* 動作確認

  * 4ロールで表示・遷移確認
  * レイアウト崩れなし
  * `bin/rails test` 実行
