了解、GOで進めます（案B：admin/system_admin の dashboard ファイルも削除）。

以下、**実装計画 → 貼り付けコード一式**です（差分が大きいので、基本は「ファイル全文」＋「削除手順」）。

---

## 実装の全体像（高レベル）

1. **共通 `GET /dashboard` を新設**（ログイン必須：`authenticate_user!`）
2. フッター「ダッシュボード」導線を **常に `dashboard_path`** に統一

   * `dashboard_path_for(nil)` の挙動も `/dashboard` へ
3. **admin_root / system_admin_root を routes から削除**（root そのものが無くなる）
4. 既存の戻りリンク／リダイレクトを **すべて `/dashboard` に置換**
5. 既存テストで `admin_root_path` を使っていた箇所を置換し、必要な挙動（Current補正など）を別URLで担保
6. **admin/system_admin の dashboard controller/view は削除**（案B）

---

## ブランチ名候補

* `improve/235-dashboard-entrypoint`
* `improve/235-dashboard-unify-footer-link`
* `improve/235-dashboard-remove-admin-systemadmin-root`
* `improve/235-dashboard-common-screen`

---

## 変更コード（貼り付け用）

### 1) `config/routes.rb`（全文置換）

* `/dashboard` を追加
* `namespace :admin do root ... end` と `namespace :system_admin do root ... end` を削除

```ruby
Rails.application.routes.draw do
  get "up" => "rails/health#show", as: :rails_health_check

  devise_for :users, skip: %i[registrations]

  root "home#show"

  # --- Common dashboard (login required) ---
  get "/dashboard", to: "dashboard#show", as: :dashboard

  # --- Public (customer registration) ---
  get  "/sign_up", to: "customers/registrations#new",    as: :sign_up
  post "/sign_up", to: "customers/registrations#create"

  # --- Public (store registration) ---
  namespace :stores do
    get  :new_registration, to: "registrations#new"
    post :registrations,    to: "registrations#create"
  end

  # --- Favorites (login required) ---
  namespace :favorites do
    resources :booths, only: %i[index]
    resources :stores, only: %i[index]
  end

  # --- Customer (login required) ---
  resources :stores, only: %i[show] do
    resource :favorite, only: %i[create destroy], controller: "favorites/stores"
  end

  # --- Public (customer) ---
  resources :booths, only: %i[show] do
    member do
      get :enter
      post :enter_as_cast
    end

    resource :favorite, only: %i[create destroy], controller: "favorites/booths"
  end

  resources :stream_sessions, only: [] do
    resources :comments, only: %i[create], module: :stream_sessions
    resources :drink_orders, only: %i[create], module: :stream_sessions
    resources :ivs_participant_tokens, only: %i[create], module: :stream_sessions

    resource :presence, only: [], module: :stream_sessions do
      post :ping
    end

    get :presence_summary, on: :member
  end

  namespace :wallet do
    resources :purchases, only: %i[create]
  end

  get  "/checkout/return", to: "checkout#return"
  post "/webhooks/stripe", to: "webhooks/stripe#create"

  # --- Cast ---
  namespace :cast do
    # current_booth selection
    resource :current_booth, only: %i[create]

    resources :booths, only: %i[index show edit update] do
      get :live, on: :member
      patch :status, on: :member
      resources :stream_sessions, only: %i[create], module: :booths
    end

    resources :stream_sessions, only: [] do
      post :finish, on: :member
      get  :pending_drink_orders, on: :member
    end

    resources :drink_orders, only: [] do
      post :consume, on: :member
    end
  end

  # --- Store Admin ---
  namespace :admin do
    # current_store selection
    resources :stores, only: %i[index edit update]
    resource :current_store, only: %i[create]

    resources :booths, only: %i[index show new edit create update] do
      member do
        get :watch
        patch :archive
        post :force_end
      end
    end

    resources :drink_items, only: %i[index create update destroy]
    resources :store_bans, only: %i[index create destroy]
    resources :casts, only: %i[index create destroy]
    get "/cast_metrics", to: "metrics#cast"
  end

  # --- System Admin ---
  namespace :system_admin do
    resources :referral_codes, only: %i[index new create edit update]
    resources :users, only: %i[index new create edit update destroy]
  end
end
```

---

### 2) `app/controllers/dashboard_controller.rb`（新規作成）

```ruby
# frozen_string_literal: true

class DashboardController < ApplicationController
  before_action :authenticate_user!

  def show
  end
end
```

---

### 3) `app/views/dashboard/show.html.erb`（新規作成）

※あなたの指定リンクをそのまま反映。
※admin系の「店舗設定」「ブース一覧」は **store未選択なら店舗一覧へ**に寄せています（最小差分＆破綻防止）。

```erb
<div style="max-width: 960px;">
  <div class="d-flex justify-content-between align-items-start gap-3">
    <div>
      <h1 class="mb-1">ダッシュボード</h1>
      <p class="text-muted mb-0">ロール別リンク</p>
    </div>
  </div>

  <hr class="my-4">

  <% store = layout_current_store %>

  <% if current_user.system_admin? %>
    <h2 class="h5 mb-3">system_admin</h2>

    <ul class="list-group mb-4">
      <li class="list-group-item">
        <%= link_to "ユーザー管理", system_admin_users_path, class: "text-decoration-none" %>
      </li>
      <li class="list-group-item">
        <%= link_to "紹介コード管理", system_admin_referral_codes_path, class: "text-decoration-none" %>
      </li>

      <li class="list-group-item">
        <%= link_to "店舗一覧", admin_stores_path, class: "text-decoration-none" %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "店舗設定", edit_admin_store_path(store), class: "text-decoration-none" %>
        <% else %>
          <%= link_to "店舗設定（先に店舗を選択）", admin_stores_path, class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "ブース一覧", admin_booths_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "ブース一覧（先に店舗を選択）", admin_stores_path, class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item text-muted">
        ユーザープロフィール編集（未実装）
      </li>
    </ul>

  <% elsif current_user.store_admin? %>
    <h2 class="h5 mb-3">store_admin</h2>

    <ul class="list-group mb-4">
      <li class="list-group-item">
        <%= link_to "店舗一覧", admin_stores_path, class: "text-decoration-none" %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "店舗設定", edit_admin_store_path(store), class: "text-decoration-none" %>
        <% else %>
          <%= link_to "店舗設定（先に店舗を選択）", admin_stores_path, class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "ブース一覧", admin_booths_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "ブース一覧（先に店舗を選択）", admin_stores_path, class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item text-muted">
        ユーザープロフィール編集（未実装）
      </li>
    </ul>

  <% elsif current_user.cast? %>
    <h2 class="h5 mb-3">cast</h2>

    <ul class="list-group mb-4">
      <li class="list-group-item">
        <%= link_to "ブース一覧", cast_booths_path, class: "text-decoration-none" %>
      </li>

      <li class="list-group-item text-muted">
        ユーザープロフィール編集（未実装）
      </li>
    </ul>

  <% else %>
    <h2 class="h5 mb-3">customer</h2>

    <ul class="list-group mb-4">
      <li class="list-group-item text-muted">
        ユーザープロフィール編集（未実装）
      </li>
    </ul>
  <% end %>
</div>
```

---

### 4) `app/helpers/application_helper.rb`（`dashboard_path_for` を統一）

* user が nil でも **必ず `dashboard_path`** を返す（Done条件）

```ruby
  def dashboard_path_for(_user)
    dashboard_path
  end
```

※上のメソッドだけ差し替えてOKです（他はそのまま）。

---

### 5) `app/views/layouts/application.html.erb`（フッターの「ダッシュボード」リンク先）

現状 `dashboard_path_for(current_user)` なので、そのままでも 4) の変更で統一されます。
（直書きにしたいなら `dashboard_path` に変えてもOK。最小差分なら現状維持でOK）

---

### 6) `app/controllers/admin/current_stores_controller.rb`（選択後の戻り先を `/dashboard` に）

`redirect_to root_path` を `redirect_to dashboard_path` に変更。

```ruby
# frozen_string_literal: true

module Admin
  class CurrentStoresController < Admin::BaseController
    def create
      store_id = params.require(:store_id)

      if current_user.system_admin?
        unless Store.exists?(id: store_id)
          session.delete(:current_store_id)
          redirect_to admin_stores_path, alert: "選択できない店舗です"
          return
        end
      else
        # session 改ざん対策：必ず membership チェック
        unless StoreMembership.exists?(
          user_id: current_user.id,
          store_id: store_id,
          membership_role: StoreMembership.membership_roles[:admin]
        )
          session.delete(:current_store_id)
          redirect_to admin_stores_path, alert: "選択できない店舗です"
          return
        end
      end

      session[:current_store_id] = store_id
      session.delete(:current_booth_id)
      redirect_to dashboard_path, notice: "店舗を切り替えました"
    rescue ActionController::ParameterMissing
      redirect_to admin_stores_path, alert: "店舗を選択してください"
    end
  end
end
```

---

### 7) `app/controllers/stores/registrations_controller.rb`（店舗登録後の戻り先）

`redirect_to admin_root_path` → `redirect_to dashboard_path`

```ruby
# frozen_string_literal: true

module Stores
  class RegistrationsController < ApplicationController
    skip_before_action :authenticate_user!, raise: false

    def new
      @form = Stores::RegistrationForm.new(referral_code: params[:ref].to_s)
    end

    def create
      @form = Stores::RegistrationForm.new(registration_params)

      if @form.save
        sign_in(@form.user) # Devise
        redirect_to dashboard_path, notice: "店舗登録が完了しました"
      else
        render :new, status: :unprocessable_entity
      end
    end

    private

    def registration_params
      params.require(:store_registration).permit(
        :store_name,
        :email,
        :password,
        :password_confirmation,
        :referral_code
      )
    end
  end
end
```

---

### 8) admin / system_admin の「戻る」リンク置換

#### `app/views/admin/stores/edit.html.erb`

`admin_root_path` → `dashboard_path`

```erb
<%= link_to "戻る", dashboard_path, class: "btn btn-outline-secondary" %>
```

#### `app/views/admin/stores/index.html.erb`

`admin_root_path` → `dashboard_path`

```erb
<%= link_to "管理者トップへ", dashboard_path, class: "btn btn-outline-secondary" %>
```

#### `app/views/system_admin/referral_codes/index.html.erb`

`system_admin_root_path` → `dashboard_path`

```erb
<%= link_to "ダッシュボードへ戻る", dashboard_path, class: "btn btn-outline-secondary" %>
```

#### `app/views/system_admin/users/index.html.erb`

同上

```erb
<%= link_to "ダッシュボードへ戻る", dashboard_path, class: "btn btn-outline-secondary" %>
```

---

## admin/system_admin dashboard ファイル削除（案B）

以下を **削除**（`git rm` 推奨）：

* `app/controllers/admin/dashboard_controller.rb`
* `app/views/admin/dashboard/show.html.erb`
* `app/controllers/system_admin/dashboard_controller.rb`
* `app/views/system_admin/dashboard/show.html.erb`

（routes から root を消しているので、消してOK）

---

## テスト更新（admin_root_path の置換）

あなたが以前貼ってくれた 3ファイルは、こう置き換えるのが筋が良いです。

### 1) `test/integration/admin_current_store_required_for_system_admin_test.rb`

* `get admin_root_path` → `get admin_booths_path`（current_store 必須の動線で検証）
* 選択後 redirect 期待（`root_path` → `dashboard_path`）

**全文（置換案）**：

```ruby
# frozen_string_literal: true

require "test_helper"

class AdminCurrentStoreRequiredForSystemAdminTest < ActionDispatch::IntegrationTest
  setup do
    @store = Store.create!(name: "store1")
    @system_admin = User.create!(email: "sys_guard@example.com", password: "password", role: :system_admin)

    @store_admin = User.create!(email: "admin_guard@example.com", password: "password", role: :store_admin)
    StoreMembership.create!(store: @store, user: @store_admin, membership_role: :admin)
  end

  test "system_admin without current_store is redirected to /admin/stores on admin booths" do
    sign_in @system_admin, scope: :user

    get admin_booths_path
    assert_response :redirect
    assert_redirected_to admin_stores_path

    follow_redirect!
    assert_response :success
  end

  test "system_admin with invalid current_store_id is corrected and redirected to /admin/stores" do
    sign_in @system_admin, scope: :user

    # いったん選択して session に入れる
    post admin_current_store_path, params: { store_id: @store.id }
    assert_response :redirect
    assert_redirected_to dashboard_path

    # store を削除して session を不正化する
    @store.destroy!

    get admin_booths_path
    assert_response :redirect
    assert_redirected_to admin_stores_path

    follow_redirect!
    assert_response :success
  end

  test "store_admin behavior is not broken (can access admin booths without explicit selection)" do
    sign_in @store_admin, scope: :user

    get admin_booths_path
    assert_response :success
  end
end
```

---

### 2) `test/integration/current_consistency_admin_test.rb`

* `get admin_root_path` → `get admin_booths_path`
  （Current補正ロジックは `Admin::BaseController#current_store` で動くので、`require_current_store!` が走る URL の方が適切）

**置換ポイントだけ**：

* 3箇所の `get admin_root_path` を `get admin_booths_path` に変更

---

### 3) `test/integration/role_hierarchy_access_test.rb`

* cast/customer が forbidden を確認する admin 側URLは、`admin_stores_path` が安全（store未選択でも 403 が返る）
* system_admin の admin access は：

  * `get admin_stores_path` は success
  * `get admin_booths_path` は未選択なら admin_stores に redirect（従来と同じ考え方）

**全文（置換案）**：

```ruby
# frozen_string_literal: true

require "test_helper"

class RoleHierarchyAccessTest < ActionDispatch::IntegrationTest
  setup do
    @store1 = Store.create!(name: "store1")
    @store2 = Store.create!(name: "store2")

    @booth1 = Booth.create!(store: @store1, name: "booth1", status: :offline)
    @booth2 = Booth.create!(store: @store2, name: "booth2", status: :offline)

    @customer     = User.create!(email: "customer_h@example.com", password: "password", role: :customer)
    @cast         = User.create!(email: "cast_h@example.com",     password: "password", role: :cast)
    @store_admin  = User.create!(email: "admin_h@example.com",    password: "password", role: :store_admin)
    @system_admin = User.create!(email: "sys_h@example.com",      password: "password", role: :system_admin)

    # cast は booth1 に所属
    BoothCast.create!(booth: @booth1, cast_user: @cast)

    # store_admin は store1 の admin
    StoreMembership.create!(store: @store1, user: @store_admin, membership_role: :admin)
  end

  test "store_admin can access cast namespace (index) and can open own store booth" do
    sign_in @store_admin, scope: :user

    get cast_booths_path
    assert_response :success

    get cast_booth_path(@booth1)
    assert_response :success

    get cast_booth_path(@booth2)
    assert_response :forbidden
  end

  test "cast cannot access admin/system_admin (403)" do
    sign_in @cast, scope: :user

    # admin namespace 入口
    get admin_stores_path
    assert_response :forbidden

    get system_admin_referral_codes_path
    assert_response :forbidden
  end

  test "system_admin can access cast/admin (not forbidden)" do
    sign_in @system_admin, scope: :user

    get cast_booths_path
    assert_response :success

    # 未選択の admin_booths は /admin/stores に誘導される
    get admin_booths_path
    assert_response :redirect
    assert_redirected_to admin_stores_path

    # 選択後は dashboard に戻る
    post admin_current_store_path, params: { store_id: @store1.id }
    assert_response :redirect
    assert_redirected_to dashboard_path

    # 選択済みなら admin_booths に入れる
    get admin_booths_path
    assert_response :success
  end

  test "customer cannot access cast/admin/system_admin (403)" do
    sign_in @customer, scope: :user

    get cast_booths_path
    assert_response :forbidden

    get admin_stores_path
    assert_response :forbidden

    get system_admin_referral_codes_path
    assert_response :forbidden
  end
end
```

* `get admin_root_path` を

  * forbidden検証 → `get admin_stores_path`
  * system_admin の “未選択誘導” 検証 → `get admin_booths_path`
* `post admin_current_store_path` の redirect 期待は `dashboard_path` に


---

## 実行・確認コマンド（Railsは docker compose 経由）

```bash
docker compose exec app bash -lc "bin/rails routes | grep dashboard"
docker compose exec app bash -lc "bin/rails test"
```

※Windows PowerShell なら `grep` が無いので、routes は `findstr` でOKです：

```powershell
docker compose exec app bash -lc "bin/rails routes" | findstr /I "dashboard"
```

---

## 次のあなたの手順（そのまま進めてOK）

1. routes 更新（`/dashboard` 追加＋admin/system_admin root削除）
2. DashboardController / view を新規追加
3. helper の `dashboard_path_for` を統一
4. 既存の戻りリンクと redirect を `/dashboard` に張替え
5. dashboard ファイル4つを削除（案B）
6. テスト置換（admin_root_path 依存を除去）
7. `docker compose exec app bash -lc "bin/rails test"`

---

必要なら、この変更に合わせた **コミットメッセージ案 / PR本文**もすぐ出せます。

---
---

原因はシンプルで、**`Admin::CurrentStoresController#create` のリダイレクト先を `/dashboard` に統一した**のに、テスト側がまだ **`root_path` を期待**しているためです（失敗ログも全部それ）。

なので方針どおり **テストの期待値を `dashboard_path` に更新**すればOKです。

---

## 直すべきルール（今回の変更に合わせて統一）

* `post admin_current_store_path ...` の直後は
  ✅ `assert_redirected_to dashboard_path`
  （旧：`root_path`）

これを、該当テスト全部に反映します。

---

# 1) `test/integration/admin_stores_index_test.rb`

2箇所。

### store_admin の selection flow

```ruby
post admin_current_store_path, params: { store_id: @store1.id }
assert_response :redirect
assert_redirected_to dashboard_path
```

### system_admin の selection flow

```ruby
post admin_current_store_path, params: { store_id: @store2.id }
assert_response :redirect
assert_redirected_to dashboard_path
```

---

# 2) `test/integration/booth_edit_authorization_test.rb`

system_admin の current_store 選択部分だけ。

```ruby
post admin_current_store_path, params: { store_id: @store1.id }
assert_response :redirect
assert_redirected_to dashboard_path
```

---

# 3) `test/integration/admin_booth_force_end_test.rb`

system_admin の current_store 選択部分だけ。

```ruby
post admin_current_store_path, params: { store_id: @store1.id }
assert_response :redirect
assert_redirected_to dashboard_path
```

---

# 4) `test/integration/admin_booths_index_test.rb`

2箇所。

### system_admin: index requires current_store...

```ruby
post admin_current_store_path, params: { store_id: @store1.id }
assert_response :redirect
assert_redirected_to dashboard_path
```

### system_admin: archived=1...

```ruby
post admin_current_store_path, params: { store_id: @store1.id }
assert_response :redirect
assert_redirected_to dashboard_path
```

---

## 追加で落ちる可能性がある箇所（念のため一括検索）

PowerShell ならこれで `root_path` 期待を拾えます：

* `admin_current_store_path` の直後に `assert_redirected_to root_path` が残ってないか
* `Store登録後` のリダイレクトも `dashboard_path` に寄せるならテストも合わせる

```powershell
findstr /S /N /I "admin_current_store_path assert_redirected_to root_path" test\integration\*.rb
```

---

この修正を入れたら、今回出ている **6 failures は全部解消**するはずです。
もしまだ落ちるのが出たら、その失敗ログと該当テストだけ貼ってください（原因は同系統の期待値ズレの可能性が高いです）。
