了解です。では **一覧から即時切替あり + CRUD 完備** で、既存実装（users / referral_codes / drink_items）に完全に寄せた形でいきます。

---

# 最終実装方針（#487）

## 全体構成

* `system_admin/effects` を新設
* 機能

  * index（一覧 + enabled 即時切替）
  * new / create
  * edit / update
* UI方針

  * users / referral_codes と同じ「別ページ型」
  * drink_items の「即時トグル」を部分採用
* 並び順

  * `Effect.ordered`
* 認可

  * `SystemAdmin::BaseController` 継承のみ

---

# 1. routes

```ruby
namespace :system_admin do
  resources :referral_codes, only: %i[index new create edit update]
  resources :users, only: %i[index new create edit update destroy]

  resources :effects, only: %i[index new create edit update] # ← 追加

  resources :settlements, only: %i[index show] do
```

---

# 2. controller

`app/controllers/system_admin/effects_controller.rb`

```ruby
# frozen_string_literal: true

module SystemAdmin
  class EffectsController < SystemAdmin::BaseController
    before_action :set_effect, only: %i[edit update]

    def index
      @effects = Effect.order(:position, :id)
    end

    def new
      @effect = Effect.new(enabled: true, position: default_position)
    end

    def create
      @effect = Effect.new(effect_params)

      if @effect.save
        redirect_to system_admin_effects_path, notice: "Effectを作成しました"
      else
        render :new, status: :unprocessable_entity
      end
    end

    def edit
    end

    def update
      if @effect.update(effect_params)
        redirect_to system_admin_effects_path, notice: "更新しました"
      else
        render :edit, status: :unprocessable_entity
      end
    end

    private

    def set_effect
      @effect = Effect.find(params[:id])
    end

    def effect_params
      params.require(:effect).permit(
        :name,
        :key,
        :zip_filename,
        :icon_path,
        :enabled,
        :position
      )
    end

    def default_position
      last = Effect.maximum(:position)
      last.present? ? last + 1 : 0
    end
  end
end
```

---

# 3. index

`app/views/system_admin/effects/index.html.erb`

```erb
<h1>Effect管理</h1>

<div class="mb-3 d-flex gap-2">
  <%= link_to "新規作成", new_system_admin_effect_path, class: "btn btn-primary" %>
  <%= link_to "ダッシュボードへ戻る", dashboard_path, class: "btn btn-outline-secondary" %>
</div>

<table class="table table-sm align-middle">
  <thead>
    <tr>
      <th>ID</th>
      <th>表示名</th>
      <th>key</th>
      <th>zip</th>
      <th>icon</th>
      <th>position</th>
      <th>enabled</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @effects.each do |e| %>
      <tr class="<%= "text-muted" unless e.enabled %>">
        <td><%= e.id %></td>
        <td class="fw-semibold"><%= e.name %></td>
        <td><%= e.key %></td>
        <td><%= e.zip_filename %></td>
        <td><%= e.icon_path.presence || "-" %></td>
        <td><%= e.position %></td>

        <td>
          <%= form_with model: [:system_admin, e], method: :patch, class: "d-inline" do |f| %>
            <%= f.hidden_field :enabled, value: (!e.enabled).to_s %>
            <%= f.submit(e.enabled ? "有効" : "無効",
                  class: "btn btn-sm #{e.enabled ? "btn-success" : "btn-outline-secondary"}") %>
          <% end %>
        </td>

        <td class="d-flex justify-content-end gap-2">
          <%= link_to "編集", edit_system_admin_effect_path(e), class: "btn btn-outline-secondary btn-sm" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
```

---

# 4. form

`app/views/system_admin/effects/_form.html.erb`

```erb
<%= form_with model: [:system_admin, effect] do |f| %>
  <% if effect.errors.any? %>
    <div class="alert alert-danger">
      <ul class="mb-0">
        <% effect.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= f.label :name, "表示名", class: "form-label" %>
    <%= f.text_field :name, class: "form-control", required: true %>
  </div>

  <div class="mb-3">
    <%= f.label :key, "key（ユニーク）", class: "form-label" %>
    <%= f.text_field :key, class: "form-control", required: true %>
  </div>

  <div class="mb-3">
    <%= f.label :zip_filename, "zip_filename", class: "form-label" %>
    <%= f.text_field :zip_filename, class: "form-control", required: true %>
    <div class="form-text">public/banuba/effects に配置されたファイル名</div>
  </div>

  <div class="mb-3">
    <%= f.label :icon_path, "icon_path（任意）", class: "form-label" %>
    <%= f.text_field :icon_path, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= f.label :position, "position", class: "form-label" %>
    <%= f.number_field :position, class: "form-control" %>
  </div>

  <div class="form-check mb-3">
    <%= f.check_box :enabled, class: "form-check-input" %>
    <%= f.label :enabled, "enabled（有効）", class: "form-check-label" %>
  </div>

  <div class="d-flex gap-2">
    <%= f.submit effect.persisted? ? "更新する" : "作成する", class: "btn btn-primary" %>
    <%= link_to "キャンセル", system_admin_effects_path, class: "btn btn-outline-secondary" %>
  </div>
<% end %>
```

---

# 5. new

```erb
<h1>Effect作成</h1>

<%= render "form", effect: @effect %>

<div class="mt-3">
  <%= link_to "戻る", system_admin_effects_path, class: "btn btn-outline-secondary" %>
</div>
```

---

# 6. edit

```erb
<h1>Effect編集</h1>

<%= render "form", effect: @effect %>

<div class="mt-3">
  <%= link_to "戻る", system_admin_effects_path, class: "btn btn-outline-secondary" %>
</div>
```

---

# 7. ダッシュボード導線

`dashboard/show.html.erb` の system_admin セクションに追加：

```erb
<li class="list-group-item">
  <%= link_to "Effect管理", system_admin_effects_path, class: "text-decoration-none" %>
</li>
```

（ユーザー管理の近くに置くのが自然）

---

# 完了条件チェック

* 一覧表示 → OK
* 新規作成 → OK
* 編集 → OK
* enabled 切替

  * フォーム → OK
  * 一覧トグル → OK
* position 編集 → OK
* system_admin 限定 → BaseControllerでOK
* ダッシュボード導線 → OK

---

# 補足（設計としてかなり良い状態）

今回の構成で、

* Effect追加 = **DB登録だけ**
* zip差し替え = **public配置だけ**
* UI反映 = **DB参照に寄せればOK**

になっているので、Epic #483 の

> ノーコードに近い運用

にかなり近づいてます 👍

---

必要なら次は
👉「配信画面を DB の Effect 一覧に差し替える (#487後続)」
も一気に設計いけます。
