了解です。
まずは **home のみ** を対象にした、`fixed` 前提の最小差分です。

ポイントはこの3つです。

* 検索バーを `position: fixed`
* 本文側にその高さぶんの余白を追加
* `install_prompt` は fixedバーの下に置く

```diff
--- a/app/views/home/show.html.erb
+++ b/app/views/home/show.html.erb
@@ -1,37 +1,39 @@
 <% if user_signed_in? %>
-  <%= render "install_prompt" %>
-
   <%# --- Search --- %>
-  <div class="d-flex flex-column gap-2 mb-3">
-    <div class="btn-group" role="group" aria-label="home表示切り替え">
-      <%= link_to "ブース",
-            root_path(mode: "booths", q: @q.presence),
-            class: ["btn", (@mode == "booths" ? "btn-secondary active" : "btn-outline-secondary")],
-            "aria-current": (@mode == "booths" ? "page" : nil) %>
+  <div class="fixed-subnav fixed-subnav--with-form">
+    <div class="container py-2">
+      <div class="d-flex flex-column gap-2">
+        <div class="btn-group" role="group" aria-label="home表示切り替え">
+          <%= link_to "ブース",
+                root_path(mode: "booths", q: @q.presence),
+                class: ["btn", (@mode == "booths" ? "btn-secondary active" : "btn-outline-secondary")],
+                "aria-current": (@mode == "booths" ? "page" : nil) %>
 
-      <%= link_to "店舗",
-            root_path(mode: "stores", q: @q.presence),
-            class: ["btn", (@mode == "stores" ? "btn-secondary active" : "btn-outline-secondary")],
-            "aria-current": (@mode == "stores" ? "page" : nil) %>
+          <%= link_to "店舗",
+                root_path(mode: "stores", q: @q.presence),
+                class: ["btn", (@mode == "stores" ? "btn-secondary active" : "btn-outline-secondary")],
+                "aria-current": (@mode == "stores" ? "page" : nil) %>
 
-      <%= link_to "ユーザー",
-            root_path(mode: "users", q: @q.presence),
-            class: ["btn", (@mode == "users" ? "btn-secondary active" : "btn-outline-secondary")],
-            "aria-current": (@mode == "users" ? "page" : nil) %>
-    </div>
+          <%= link_to "ユーザー",
+                root_path(mode: "users", q: @q.presence),
+                class: ["btn", (@mode == "users" ? "btn-secondary active" : "btn-outline-secondary")],
+                "aria-current": (@mode == "users" ? "page" : nil) %>
+        </div>
 
-    <%= form_with url: root_path, method: :get, scope: nil do |f| %>
-      <div class="input-group">
-        <%= f.hidden_field :mode, value: @mode %>
+        <%= form_with url: root_path, method: :get, scope: nil do |f| %>
+          <div class="input-group">
+            <%= f.hidden_field :mode, value: @mode %>
 
-        <%= f.text_field :q,
-              value: @q,
-              class: "form-control",
-              placeholder: "キーワード",
-              autocomplete: "off" %>
+            <%= f.text_field :q,
+                  value: @q,
+                  class: "form-control",
+                  placeholder: "キーワード",
+                  autocomplete: "off" %>
 
-        <button class="btn btn-primary" type="submit" aria-label="検索">
-          <i class="bi bi-search"></i>
-        </button>
+            <button class="btn btn-primary" type="submit" aria-label="検索">
+              <i class="bi bi-search"></i>
+            </button>
+          </div>
+        <% end %>
       </div>
-    <% end %>
+    </div>
+  </div>
+
+  <div class="home-with-fixed-subnav">
+    <%= render "install_prompt" %>
 
-  </div>
-
-  <%# --- Results (cards) --- %>
-  <% if @mode == "users" %>
-    <% if @users.present? %>
-      <div class="row row-cols-1 row-cols-md-3 g-3">
-        <% @users.each do |user| %>
-          <div class="col">
-            <%= render "users/card", user: user %>
-          </div>
-        <% end %>
-      </div>
-    <% else %>
-      <div class="alert alert-secondary" role="alert">
-        表示できるユーザーがありません。
-      </div>
-    <% end %>
-  <% elsif @mode == "stores" %>
-    <% if @stores.present? %>
-      <div class="row row-cols-1 row-cols-md-3 g-3">
-        <% @stores.each do |store| %>
-          <% favorited = @favorite_store_ids.include?(store.id) %>
+    <%# --- Results (cards) --- %>
+    <% if @mode == "users" %>
+      <% if @users.present? %>
+        <div class="row row-cols-1 row-cols-md-3 g-3">
+          <% @users.each do |user| %>
+            <div class="col">
+              <%= render "users/card", user: user %>
+            </div>
+          <% end %>
+        </div>
+      <% else %>
+        <div class="alert alert-secondary" role="alert">
+          表示できるユーザーがありません。
+        </div>
+      <% end %>
+    <% elsif @mode == "stores" %>
+      <% if @stores.present? %>
+        <div class="row row-cols-1 row-cols-md-3 g-3">
+          <% @stores.each do |store| %>
+            <% favorited = @favorite_store_ids.include?(store.id) %>
 
-          <div class="col">
-            <%= render "stores/card", store: store, favorited: favorited %>
-          </div>
-        <% end %>
-      </div>
-    <% else %>
-      <div class="alert alert-secondary" role="alert">
-        表示できる店舗がありません。
-      </div>
-    <% end %>
-  <% else %>
-    <% if @booths.present? %>
-      <div class="row row-cols-1 row-cols-md-3 g-3">
-        <% @booths.each do |booth| %>
-          <div class="col">
-            <%= render "booths/card",
-                  booth: booth,
-                  booth_favorited: @favorite_booth_ids.include?(booth.id),
-                  store_favorited: @favorite_store_ids.include?(booth.store_id) %>
-          </div>
-        <% end %>
-      </div>
+            <div class="col">
+              <%= render "stores/card", store: store, favorited: favorited %>
+            </div>
+          <% end %>
+        </div>
+      <% else %>
+        <div class="alert alert-secondary" role="alert">
+          表示できる店舗がありません。
+        </div>
+      <% end %>
     <% else %>
-      <div class="alert alert-secondary" role="alert">
-        表示できるブースがありません。
-      </div>
+      <% if @booths.present? %>
+        <div class="row row-cols-1 row-cols-md-3 g-3">
+          <% @booths.each do |booth| %>
+            <div class="col">
+              <%= render "booths/card",
+                    booth: booth,
+                    booth_favorited: @favorite_booth_ids.include?(booth.id),
+                    store_favorited: @favorite_store_ids.include?(booth.store_id) %>
+            </div>
+          <% end %>
+        </div>
+      <% else %>
+        <div class="alert alert-secondary" role="alert">
+          表示できるブースがありません。
+        </div>
+      <% end %>
     <% end %>
-  <% end %>
+  </div>
 <% else %>
   <%= render "install_prompt" %>
 
--- a/app/assets/stylesheets/components/_layout.scss
+++ b/app/assets/stylesheets/components/_layout.scss
@@ -13,6 +13,31 @@
   );
 }
 
+.fixed-subnav {
+  position: fixed;
+  top: var(--app-header-h, 64px);
+  left: 0;
+  right: 0;
+  z-index: 1020;
+  background: var(--bs-body-bg);
+  border-bottom: 1px solid var(--bs-border-color);
+}
+
+.fixed-subnav--with-form {
+  .btn-group {
+    width: 100%;
+  }
+}
+
+.home-with-fixed-subnav {
+  padding-top: 96px;
+}
+
+@media (max-width: 767.98px) {
+  .fixed-subnav {
+    top: var(--app-header-h, 56px);
+  }
+
+  .home-with-fixed-subnav {
+    padding-top: 92px;
+  }
+}
+
 .app-footer-shell {
   width: 100%;
 }
```

補足だけです。
`padding-top: 96px` はまずの仮値なので、実機で見て

* バーの下に少し隙間があるなら小さく
* install_prompt が少し潜るなら大きく

で微調整するのがよいです。

次に崩れたら、`96px` を固定調整するのではなく、検索バー側の `py-2` を少し減らす方向で整えるのがおすすめです。
