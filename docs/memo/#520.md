了解です。**そのままコピペで置き換え可能な最終実装**を出します。

---

# ① `app/views/stream_sessions/_stream_meta_display.html.erb`

```erb
<%# locals: stream_session: %>
<% user = stream_session.cast_user %>
<% title_display = stream_session.title.presence || stream_session.booth.name %>

<div class="live-meta-row viewer-meta-row">
  <!-- avatar -->
  <div class="meta-avatar">
    <% if user&.avatar.present? %>
      <%= image_tag user.avatar, class: "meta-avatar-img" %>
    <% else %>
      <div class="meta-avatar-placeholder"></div>
    <% end %>
  </div>

  <!-- name -->
  <div class="meta-name">
    <%= display_name_or_anonymous(user) %>
  </div>

  <!-- title -->
  <div class="meta-title text-truncate">
    <%= title_display %>
  </div>

  <!-- viewer count -->
  <div class="meta-count">
    <i class="bi bi-people-fill" aria-hidden="true"></i>
    <span data-presence-poll-target="count">-</span>
  </div>
</div>
```

---

# ② `app/views/cast/booths/live.html.erb`（meta部分のみ置換）

## 🔥 ここを丸ごと削除

```erb
<div class="live-meta-layout">
  <div class="live-meta-main">
    <turbo-frame id="stream_meta_display">
      <%= render "stream_sessions/stream_meta_display",
                stream_session: @stream_session %>
    </turbo-frame>
  </div>

  <div class="live-meta-side">
    <div class="live-meta-viewer-count">
      <i class="bi bi-people-fill" aria-hidden="true"></i>
      <span data-presence-poll-target="count">-</span>
    </div>
  </div>
</div>
```

---

## ✅ これに置き換え

```erb
<turbo-frame id="stream_meta_display">
  <%= render "stream_sessions/stream_meta_display",
            stream_session: @stream_session %>
</turbo-frame>
```

---

# ③ CSS修正（超重要）

## `app/assets/stylesheets/components/_broadcast_meta.scss`

### 🔥 既存のこれ全部削除してOK

* `.live-meta-layout`
* `.live-meta-main`
* `.live-meta-side`
* `.live-meta-description`

---

## ✅ 追加コード

```scss
.live-meta-row,
.viewer-meta-row {
  display: flex;
  align-items: center;
  gap: 8px;

  min-width: 0;
  width: 100%;

  color: rgba(255, 255, 255, 0.96);
}

/* avatar */
.meta-avatar {
  flex: 0 0 auto;
}

.meta-avatar-img {
  width: 28px;
  height: 28px;
  border-radius: 50%;
  object-fit: cover;
}

.meta-avatar-placeholder {
  width: 28px;
  height: 28px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.3);
}

/* name */
.meta-name {
  flex: 0 0 auto;
  font-size: 0.875rem;
  font-weight: 600;
  white-space: nowrap;
}

/* title（可変） */
.meta-title {
  flex: 1 1 auto;
  min-width: 0;

  font-size: 0.875rem;
  line-height: 1.2;
}

/* count */
.meta-count {
  flex: 0 0 auto;
  display: inline-flex;
  align-items: center;
  gap: 4px;

  font-size: 0.75rem;
  white-space: nowrap;
}
```

---

# ④ 削除される機能（仕様通り）

* description 表示 → 完全削除
* sideブロック → 完全削除

---

# ⑤ 実装結果（完成状態）

## UI構造

```
[avatar] [name] [title.............] [👥 123]
```

## 挙動

* titleだけ縮む（ellipsis）
* avatar / name / count は絶対表示
* 1行固定（折り返しなし）
* cast / viewer 共通

---

# ⑥ 動作確認チェックリスト

* [ ] 配信画面で1行になっている
* [ ] 視聴画面でも同じ表示
* [ ] タイトル長いと省略される
* [ ] 名前と人数が消えない
* [ ] 人数が更新される（presence-poll）

---

# ⑦ コミットメッセージ

```
Improve meta display to single-line cast-centric layout (#520)
```

---

# ⑧ PR文

```
## 概要
配信・視聴画面の meta 欄を 1 行化し、配信者中心の情報構造へ刷新しました。

## 変更内容
- avatar / name / title / viewer count を 1 行に統合
- live-meta-main / side 構造を廃止
- description 表示を削除
- title のみ ellipsis 対応
- presence-poll の count 表示を meta 行へ移動

## 確認ポイント
- meta が 1 行で表示されること
- title が省略されること
- viewer count が更新されること

Closes #520
```

---

ここ、かなり完成度高いです。
👉 次は「avatarのデザイン詰める or タイトル編集UIとの干渉調整」あたりが来ると思う 👍
