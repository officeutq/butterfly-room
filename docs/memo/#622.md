完璧です 👍
この状態なら **そのまま実装に入れます**。

しかも今回のファイル構成を見る限り、やることはかなり明確です。

---

# ✅ 実装前の結論（重要）

今回の #622 はこの3つがコアです👇

### ① View置き換え（メイン）

* `file_field` → FilePond

### ② Userモデル修正（超重要）

* `normalizes_image_attachment :avatar` 削除

### ③ remove処理変更（仕様変更）

* checkbox → FilePond + hidden flag

---

# ⚠️ 事前に把握しておくべきポイント

## ❗ いまの構造（重要）

### Userモデル

```ruby
include NormalizedImageAttachment
normalizes_image_attachment :avatar
```

👉 **ここが mini_magick の入口**

---

### Controller

```ruby
rescue NormalizedImageAttachment::InvalidImageAttachment
```

👉 これも不要になる

---

### View

```erb
file_field + checkbox(remove_avatar)
```

👉 これ全部置き換え

---

# 🚀 実装ステップ（Docker前提・ブランチ付き）

---

## ■ Step0 ブランチ作成

```bash
git checkout -b feature/622_profile_avatar_filepond
```

---

## ■ Step1 Userモデル修正（mini_magick排除）

### 修正ファイル

* `app/models/user.rb`

### 修正内容

```diff
- include NormalizedImageAttachment
```

```diff
- normalizes_image_attachment :avatar
```

👉 **avatarだけ外すのではなく、includeごと削除でOK**
（他で使ってないため）

---

## ■ Step2 Controller修正

### 修正ファイル

* `app/controllers/profiles_controller.rb`

### 修正内容

```diff
- rescue NormalizedImageAttachment::InvalidImageAttachment => e
-   @user.assign_attributes(profile_params.except(:avatar))
-   @user.errors.add(:avatar, e.message)
-   success = false
```

👉 **この rescue 全削除**

---

## ■ Step3 View置き換え（最重要）

### 修正ファイル

* `app/views/profiles/edit.html.erb`

---

## 🔥 完成コード（そのまま貼れる）

```erb
<div class="mb-3" data-controller="profile-avatar">
  <%= f.label :avatar, "プロフィール画像", class: "form-label" %>

  <%= f.hidden_field :remove_avatar, value: "0", data: { profile_avatar_target: "removeFlag" } %>

  <%= f.file_field :avatar,
        accept: ".jpg,.jpeg,.png,.webp,.heic,.heif",
        data: { profile_avatar_target: "input" } %>
</div>
```

---

## ■ Step4 Stimulus作成

### 新規ファイル

```bash
touch app/javascript/controllers/profile_avatar_controller.js
```

---

## 🔥 コード（そのまま）

```javascript
import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  static targets = ["input", "removeFlag"]

  connect() {
    this.initialize()
  }

  disconnect() {
    if (this.pond) this.pond.destroy()
  }

  initialize() {
    if (!window.FilePond) return

    this.registerPlugins()

    this.pond = window.FilePond.create(this.inputTarget, {
      storeAsFile: true,
      allowMultiple: false,
      allowImagePreview: true,
      allowImageResize: true,
      allowImageTransform: true,

      imageResizeTargetWidth: 1024,
      imageResizeTargetHeight: 1024,
      imageResizeMode: "contain",
      imageResizeUpscale: false,

      imageTransformOutputMimeType: "image/jpeg",
      imageTransformOutputQuality: 94,

      acceptedFileTypes: [
        "image/jpeg",
        "image/png",
        "image/webp",
        "image/heic",
      ],
    })

    this.bindEvents()
  }

  bindEvents() {
    this.pond.on("addfile", () => {
      this.removeFlagTarget.value = "0"
    })

    this.pond.on("removefile", () => {
      this.removeFlagTarget.value = "1"
    })
  }

  registerPlugins() {
    if (window.__filepondRegistered) return

    const plugins = [
      window.FilePondPluginImagePreview,
      window.FilePondPluginImageResize,
      window.FilePondPluginImageTransform,
    ].filter(Boolean)

    plugins.forEach((p) => window.FilePond.registerPlugin(p))
    window.__filepondRegistered = true
  }
}
```

---

## ■ Step5 FilePond読み込み（重要）

### profiles/editに追加

```erb
<script src="/vendor/filepond/filepond.min.js" defer></script>
<script src="/vendor/filepond/filepond-plugin-image-preview.min.js" defer></script>
<script src="/vendor/filepond/filepond-plugin-image-resize.min.js" defer></script>
<script src="/vendor/filepond/filepond-plugin-image-transform.min.js" defer></script>
```

---

## ■ Step6 CSS適用

### application.scss に追加済みなのでOK

```scss
@import "components/filepond_verification";
```

👉 そのまま使える

---

## ■ Step7 Dockerで確認

```bash
docker compose up
```

ブラウザ：

```
http://app.localhost:3000/profile/edit
```

---

# ✅ 確認ポイント

* プレビュー出る
* 1024以内に縮小される
* JPEGになる
* 既存画像削除できる
* 新規画像で上書きできる
* mini_magickエラー出ない

---

# ⚠️ 今回あえてやってないこと

* 既存画像をFilePond初期表示（あとでやる）
* 共通controller化
* SVGサーバー拒否（後でOK）

---

# 🎯 次のステップ

これ終わったら👇

👉 Booth版ほぼコピペでいける
👉 Storeも同様

---

必要なら次👇

* 「既存画像をFilePond内に初期表示」実装
* mini_magick完全削除Issue
* Booth版のテンプレ

全部出せます 👍
