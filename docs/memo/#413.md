では、そのまま実装できるように、**変更差分ベース**でまとめます。

---

## 1. マイグレーション作成

まず空マイグレーションを作成します。

```bat
docker compose exec app bin/rails generate migration AddBroadcastStartedAtToStreamSessions
```

生成されたファイルをこうします。

```ruby
class AddBroadcastStartedAtToStreamSessions < ActiveRecord::Migration[8.0]
  def change
    add_column :stream_sessions, :broadcast_started_at, :datetime
    add_index :stream_sessions, :broadcast_started_at
  end
end
```

実行:

```bat
docker compose exec app bin/rails db:migrate
```

---

## 2. `config/routes.rb`

`cast` 名前空間の `resources :stream_sessions` に member action を追加します。

```ruby
resources :stream_sessions, only: %i[show] do
  post :finish, on: :member
  get  :pending_drink_orders, on: :member
  get  :meta_display, on: :member
  patch :metadata, on: :member
  patch :start_broadcast, on: :member
end
```

---

## 3. `app/views/cast/booths/live.html.erb`

`data-ivs-publisher-start-broadcast-url-value` を追加します。

```erb
<div
  class="cast-live-screen"
  data-controller="ivs-publisher live-stage"
  data-ivs-publisher-token-url-value="<%= stream_session_ivs_participant_tokens_path(@stream_session) %>"
  data-ivs-publisher-finish-url-value="<%= finish_cast_stream_session_path(@stream_session) %>"
  data-ivs-publisher-status-url-value="<%= status_cast_booth_path(@booth) %>"
  data-ivs-publisher-meta-display-url-value="<%= meta_display_cast_stream_session_path(@stream_session) %>"
  data-ivs-publisher-start-broadcast-url-value="<%= start_broadcast_cast_stream_session_path(@stream_session) %>"
  data-ivs-publisher-mirror-value="true"
```

この view は既存の data 属性で URL を渡す流儀なので、ここに足すのが自然です。 

---

## 4. `app/javascript/controllers/ivs_publisher/api_client.js`

関数を追加します。

```js
function csrfToken() {
  return document.querySelector('meta[name="csrf-token"]')?.content
}

export async function fetchParticipantToken(ctx, role) {
  const resp = await fetch(ctx.tokenUrlValue, {
    method: "POST",
    credentials: "same-origin",
    headers: {
      "Content-Type": "application/json",
      "Accept": "application/json",
      "X-CSRF-Token": csrfToken(),
    },
    body: JSON.stringify({ role }),
  })

  let body = null
  try {
    body = await resp.json()
  } catch (_) {}

  if (!resp.ok) {
    throw new Error(`token_api_failed(${resp.status}) ${body?.error || ""}`.trim())
  }

  return body.participant_token
}

export async function patchBoothStatus(ctx, to) {
  console.log("[ivs-publisher] statusUrlValue=", ctx.statusUrlValue, "to=", to)
  if (!ctx.statusUrlValue) return

  const url = new URL(ctx.statusUrlValue, window.location.origin)
  url.searchParams.set("to", to)

  const resp = await fetch(url.toString(), {
    method: "PATCH",
    redirect: "manual",
    credentials: "same-origin",
    headers: {
      "Accept": "text/vnd.turbo-stream.html",
      "X-CSRF-Token": csrfToken(),
    },
  })

  if (resp.status >= 300 && resp.status < 400) {
    return
  }

  if (!resp.ok) throw new Error(`booth_status_failed(${resp.status})`)

  const html = await resp.text()
  if (html && window.Turbo?.renderStreamMessage) {
    window.Turbo.renderStreamMessage(html)
  }
}

export async function patchBroadcastStartedAt(ctx) {
  if (!ctx.hasStartBroadcastUrlValue) return

  const resp = await fetch(ctx.startBroadcastUrlValue, {
    method: "PATCH",
    credentials: "same-origin",
    headers: {
      "Accept": "application/json",
      "X-CSRF-Token": csrfToken(),
    },
  })

  if (!resp.ok) {
    throw new Error(`start_broadcast_failed(${resp.status})`)
  }
}

export async function reloadMetaDisplay(ctx) {
  if (!ctx.hasMetaDisplayUrlValue) return

  const frame = document.getElementById("stream_meta_display")
  if (!frame) return

  const currentSrc = frame.getAttribute("src")
  if (currentSrc === ctx.metaDisplayUrlValue) {
    if (typeof frame.reload === "function") {
      await frame.reload()
    } else {
      frame.removeAttribute("src")
      frame.setAttribute("src", ctx.metaDisplayUrlValue)
    }
    return
  }

  frame.setAttribute("src", ctx.metaDisplayUrlValue)
}

export async function postFinish(ctx) {
  const resp = await fetch(ctx.finishUrlValue, {
    method: "POST",
    credentials: "same-origin",
    headers: {
      "Accept": "text/html, application/xhtml+xml",
      "X-CSRF-Token": csrfToken(),
    },
  })

  if (!resp.ok) throw new Error(`finish_failed(${resp.status})`)

  return resp.url
}
```

---

## 5. `app/javascript/controllers/ivs_publisher_controller.js`

import と values とメソッド呼び出しを追加します。

### import 修正

```js
import { fetchParticipantToken, patchBoothStatus, patchBroadcastStartedAt, postFinish, reloadMetaDisplay } from "controllers/ivs_publisher/api_client"
```

### static values に追加

```js
static values = {
  tokenUrl: String,
  finishUrl: String,
  statusUrl: String,
  metaDisplayUrl: String,
  startBroadcastUrl: String,
  mirror: { type: Boolean, default: true },
```

### `startBroadcast()` の join 成功直後に追加

ここが今回の肝です。`await this._stage.join()` 成功後に一度だけ送ります。

```js
      this._setState("joining")
      await this._stage.join()
      this._setState("live")

      await this._patchBroadcastStartedAt()

      this._broadcasting = true
      this._previewOnly = false
      this._syncUI()

      await this._patchBoothStatus("live")
```

### ラッパー追加

```js
  _patchBroadcastStartedAt() {
    return patchBroadcastStartedAt(this)
  }
```

今の controller は `join` 成功後に booth status を `live` にしているので、その直前で `broadcast_started_at` を確定するのが自然です。 

---

## 6. `app/controllers/cast/stream_sessions_controller.rb`

`only:` に action を追加して、保存用 action を追加します。

### before_action 修正

```ruby
before_action :set_stream_session, only: %i[show finish pending_drink_orders meta_display metadata start_broadcast]
before_action :authorize_stream_session_access!, only: %i[show finish pending_drink_orders meta_display metadata start_broadcast]
```

### action 追加

`metadata` の下あたりで良いです。

```ruby
    def start_broadcast
      booth = @stream_session.booth

      unless booth.current_stream_session_id == @stream_session.id
        return head :forbidden
      end

      if @stream_session.broadcast_started_at.blank?
        @stream_session.update!(broadcast_started_at: Time.current)
      end

      render json: { ok: true }, status: :ok
    rescue ActiveRecord::RecordInvalid => e
      message = e.record.errors.full_messages.join(", ").presence || "配信開始時刻の保存に失敗しました"
      render json: { error: message }, status: :unprocessable_entity
    end
```

### `show` の計算修正

```ruby
      @started_at = @stream_session.broadcast_started_at
      @ended_at = @stream_session.ended_at
      @duration_seconds =
        if @started_at.present? && @ended_at.present? && @ended_at >= @started_at
          (@ended_at - @started_at).to_i
        else
          0
        end
```

今は `started_at` を使っているので、ここを `broadcast_started_at` に差し替えます。そうしないと standby 時間が含まれたままです。 

---

## 7. `app/views/cast/stream_sessions/show.html.erb`

未開始時に `-` を出すようにします。

```erb
<h1>配信リザルト</h1>

<div class="row g-4">
  <div class="col-12 col-lg-6">
    <div class="card h-100" data-bs-theme="light">
      <div class="card-header">
        配信情報
      </div>
      <div class="card-body">
        <div class="d-flex flex-column gap-2">
          <div><span class="text-body-secondary">配信名：</span><%= @stream_session.title.presence || @booth.name %></div>
          <div>
            <span class="text-body-secondary">配信開始：</span>
            <%= @started_at.present? ? l(@started_at, format: :long) : "-" %>
          </div>
          <div><span class="text-body-secondary">配信終了：</span><%= l(@ended_at, format: :long) if @ended_at.present? %></div>
          <div>
            <span class="text-body-secondary">配信時間：</span>
            <% hours = @duration_seconds / 3600 %>
            <% minutes = (@duration_seconds % 3600) / 60 %>
            <% seconds = @duration_seconds % 60 %>
            <%= format("%02d:%02d:%02d", hours, minutes, seconds) %>
          </div>
          <div><span class="text-body-secondary">コメント数：</span><%= @comment_count %></div>
          <div><span class="text-body-secondary">ユニーク視聴者：</span><%= @viewer_count %></div>
        </div>
      </div>
    </div>
  </div>
```

これで `broadcast_started_at = NULL` のとき、Issue通り

* 配信開始: `-`
* 配信時間: `00:00:00`

になります。 

---

## 8. `app/models/stream_session.rb`

これは必須ではないですが、寄せておくと今後楽です。

```ruby
class StreamSession < ApplicationRecord
  belongs_to :booth
  belongs_to :store
  belongs_to :started_by_cast_user, class_name: "User"
  has_many :presences, dependent: :destroy
  has_many :comments, dependent: :destroy

  enum :status, { live: 0, ended: 1 }

  validates :title, length: { maximum: 64 }, allow_nil: true

  delegate :current_stream_session_id, :status, to: :booth, prefix: true

  def broadcast_duration_seconds
    return 0 if broadcast_started_at.blank?
    return 0 if ended_at.blank?
    return 0 if ended_at < broadcast_started_at

    (ended_at - broadcast_started_at).to_i
  end
end
```

今回は controller 側だけでも十分ですが、履歴一覧や時間上限判定に広がるなら model に持たせる価値があります。

---

## 9. 動作確認ポイント

1. standby 開始
2. しばらく待つ
3. 配信開始
4. 少しして配信終了
5. リザルトで

   * 配信開始が standby 開始時刻ではなく、実際の配信開始時刻になっている
   * 配信時間が standby を含まない
6. standby のまま終了した場合

   * 配信開始が `-`
   * 配信時間が `00:00:00`

---

## 10. あるとよいテスト

最低でも integration で1本あると安心です。

* `broadcast_started_at` が nil の終了済み session は `-` / `00:00:00`
* `broadcast_started_at` がある session は `ended_at - broadcast_started_at`

必要なら次で、**テスト差分だけ** そのまま書きます。
