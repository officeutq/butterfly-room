めちゃくちゃ良いところまで来てます。
ただしこの状態、**そのままでは DeepAR 動きません**。理由は明確です👇

---

# ❌ 今の状態の問題（重要）

## ① provider factory が未接続

```js
if (provider === "deepar") {
  throw new Error("DeepARProvider is not implemented yet")
}
```

👉 これが残ってるので絶対動かない


---

## ② DeepAR の data 属性が無い

`DeepARProvider` はこれを参照しています👇

```js
this.ctx.deeparLicenseKeyValue
this.ctx.deeparRootPathValue
this.ctx.deeparDefaultEffectUrlValue
```

👉 でも ERB に存在しない

---

## ③ publish 側が banuba 固定

```js
this._banubaVideoTrack
this._banubaStageStream
this._publishedVideoSource = "banuba"
```

👉 DeepAR に切り替えてもここは未対応

---

# 🔥 結論

👉 #800 は **あと3箇所だけ修正すれば完成**

---

# 必要な差分（コピペOK）

---

## ① provider factory 修正（最重要）

```diff
+ import { DeepARProvider } from "controllers/ivs_publisher/beauty_providers/deepar_provider"

_createBeautyProvider() {
  const provider = this.providerValue || "banuba"

  if (provider === "banuba") {
    return new BanubaProvider(this)
  }

- if (provider === "deepar") {
-   throw new Error("DeepARProvider is not implemented yet")
- }

+ if (provider === "deepar") {
+   return new DeepARProvider(this)
+ }

  throw new Error(`Unknown beauty provider: ${provider}`)
}
```

---

## ② ERB に DeepAR 用 data を追加

```erb
data-ivs-publisher-provider-value="<%= @beauty_provider %>"
```

の下に追加👇

```erb
data-ivs-publisher-deepar-license-key-value="<%= ENV['DEEPAR_LICENSE_KEY'] %>"
data-ivs-publisher-deepar-root-path-value="/deepar"
data-ivs-publisher-deepar-default-effect-url-value="/deepar/effects/aviators"
```

---

## ③ controller に values 追加

```diff
static values = {
  provider: { type: String, default: "banuba" },

+ deeparLicenseKey: String,
+ deeparRootPath: String,
+ deeparDefaultEffectUrl: String,
```

---

# 💡 ここまでで何が起きるか

```text
BEAUTY_PROVIDER=deepar
↓
DeepARProvider 起動
↓
banubaSurface に DeepAR 描画
↓
preview 表示される
```

👉 これで #800 の Done 条件クリア

---

# ⚠️ まだ未対応（でもOK）

```text
- IVS publish はまだ banuba固定
- video source = "banuba"
```

👉 これは次Issue

---

# 🎯 動作確認

これだけでOK👇

```text
1. ENV変更
BEAUTY_PROVIDER=deepar

2. cast/live開く

期待：
- カメラ許可ダイアログ出る
- DeepARの映像が出る
- サングラス（aviators）が出る
```

---

# 🚀 次の次

#800終わったら👇

👉 **DeepAR を IVS publish に載せる（ラスボス）**

---

# まとめ

今の状態：

```text
90%完成
```

あと：

```text
factory + data + values
```

だけです。

---

もしよければ次👇

👉 IVS publish を DeepAR に切り替える差分も出せます
