これで十分です。追加確認は不要です。
#708 はこのまま実装に入れます。

## 今回書くのがおすすめなテスト

3本立てがきれいです。

### 1. `test/integration/store_show_test.rb` を拡張

店舗詳細ページの表示確認を増やします。

追加したい観点:

* 住所
* Google Map リンク
* 電話番号
* 営業時間
* HP リンク
* SNS アイコン導線
* description 表示
* safe_external_url により危険URLがリンク化されないこと

### 2. `test/integration/admin_store_edit_authorization_test.rb` を拡張

管理画面で基礎情報が保存できることを確認します。

追加したい観点:

* address
* phone_number
* business_hours
* website_url
* x_url / instagram_url / tiktok_url / youtube_url
* address 更新時に latitude / longitude が保存されること

### 3. `test/models/store_test.rb` を新規作成

最低限でOKです。

追加したい観点:

* name 必須
* description 最大1000
* area 最大50
* `should_geocode?` 相当の挙動はモデル内部 private なので直接より、`valid?` や `save!` 経由で見る

---

# そのまま貼れるコード

## 1. `test/integration/store_show_test.rb` 全文差し替え

```ruby
# frozen_string_literal: true

require "test_helper"

class StoreShowTest < ActionDispatch::IntegrationTest
  test "guest is redirected to login" do
    store = Store.create!(name: "store")

    get store_path(store)
    assert_response :redirect
    assert_redirected_to new_user_session_path
  end

  test "customer can view store show and see active booths only" do
    store = Store.create!(
      name: "store",
      description: "Store description",
      area: "渋谷",
      business_type: :girls_bar,
      address: "熊本県熊本市中央区本丸1-1",
      phone_number: "090-1111-2222",
      business_hours: "平日 19:00〜1:00",
      website_url: "https://officeutq.co.jp",
      x_url: "https://x.com/Butterflyve_jp",
      instagram_url: "https://www.instagram.com/butterflyve_0315/",
      tiktok_url: "https://www.tiktok.com/@aespa_official",
      youtube_url: "https://www.youtube.com/@SleepRelaxingHealingMusic"
    )

    store.thumbnail.attach(
      io: File.open(Rails.root.join("test/fixtures/files/thumb.png")),
      filename: "thumb.png",
      content_type: "image/png"
    )

    active_booth = Booth.create!(store: store, name: "active", status: :offline)
    Booth.create!(store: store, name: "archived", status: :offline, archived_at: Time.current)

    cast = User.create!(email: "cast@example.com", password: "password", role: :cast, display_name: "Cast A")
    BoothCast.create!(booth: active_booth, cast_user: cast)

    customer = User.create!(email: "customer@example.com", password: "password", role: :customer)
    sign_in customer, scope: :user

    get store_path(store)
    assert_response :success

    assert_includes @response.body, "store"
    assert_includes @response.body, "Store description"
    assert_includes @response.body, "渋谷"
    assert_includes @response.body, "ガールズバー"
    assert_includes @response.body, "熊本県熊本市中央区本丸1-1"
    assert_includes @response.body, "090-1111-2222"
    assert_includes @response.body, "平日 19:00〜1:00"
    assert_includes @response.body, "https://officeutq.co.jp"
    assert_includes @response.body, "<img"

    assert_includes @response.body, "https://www.google.com/maps/search/?api=1&query=#{ERB::Util.url_encode(store.address)}"
    assert_includes @response.body, "https://officeutq.co.jp"
    assert_includes @response.body, "https://x.com/Butterflyve_jp"
    assert_includes @response.body, "https://www.instagram.com/butterflyve_0315/"
    assert_includes @response.body, "https://www.tiktok.com/@aespa_official"
    assert_includes @response.body, "https://www.youtube.com/@SleepRelaxingHealingMusic"

    assert_includes @response.body, "active"
    assert_includes @response.body, "Cast A"
    assert_includes @response.body, enter_booth_path(active_booth)

    refute_includes @response.body, "archived"
  end

  test "unsafe website url is not linkified" do
    store = Store.create!(
      name: "store",
      website_url: "javascript:alert(1)"
    )

    customer = User.create!(email: "customer2@example.com", password: "password", role: :customer)
    sign_in customer, scope: :user

    get store_path(store)
    assert_response :success

    assert_includes @response.body, "javascript:alert(1)"
    refute_includes @response.body, 'href="javascript:alert(1)"'
  end
end
```

---

## 2. `test/integration/admin_store_edit_authorization_test.rb` に追加

既存ファイルの末尾にこのテストを追加してください。

```ruby
test "store_admin can update basic info fields" do
  sign_in @store_admin, scope: :user

  patch admin_store_path(@store1), params: {
    store: {
      name: "store1 updated",
      description: "desc",
      area: "渋谷",
      business_type: "girls_bar",
      address: "熊本県熊本市中央区本丸1-1",
      phone_number: "090-1111-2222",
      business_hours: "平日 19:00〜1:00",
      website_url: "https://officeutq.co.jp",
      x_url: "https://x.com/Butterflyve_jp",
      instagram_url: "https://www.instagram.com/butterflyve_0315/",
      tiktok_url: "https://www.tiktok.com/@aespa_official",
      youtube_url: "https://www.youtube.com/@SleepRelaxingHealingMusic"
    }
  }

  assert_response :redirect
  assert_redirected_to dashboard_path

  @store1.reload
  assert_equal "store1 updated", @store1.name
  assert_equal "desc", @store1.description
  assert_equal "渋谷", @store1.area
  assert_equal "girls_bar", @store1.business_type
  assert_equal "熊本県熊本市中央区本丸1-1", @store1.address
  assert_equal "090-1111-2222", @store1.phone_number
  assert_equal "平日 19:00〜1:00", @store1.business_hours
  assert_equal "https://officeutq.co.jp", @store1.website_url
  assert_equal "https://x.com/Butterflyve_jp", @store1.x_url
  assert_equal "https://www.instagram.com/butterflyve_0315/", @store1.instagram_url
  assert_equal "https://www.tiktok.com/@aespa_official", @store1.tiktok_url
  assert_equal "https://www.youtube.com/@SleepRelaxingHealingMusic", @store1.youtube_url
  assert_not_nil @store1.latitude
  assert_not_nil @store1.longitude
end
```

---

## 3. `test/models/store_test.rb` 新規作成

```ruby
# frozen_string_literal: true

require "test_helper"

class StoreTest < ActiveSupport::TestCase
  test "name is required" do
    store = Store.new(name: nil)

    assert_not store.valid?
    assert_includes store.errors.details[:name], { error: :blank }
  end

  test "description has maximum length 1000" do
    store = Store.new(name: "store", description: "a" * 1001)

    assert_not store.valid?
    assert store.errors.details[:description].any? { |detail| detail[:error] == :too_long }
  end

  test "area has maximum length 50" do
    store = Store.new(name: "store", area: "a" * 51)

    assert_not store.valid?
    assert store.errors.details[:area].any? { |detail| detail[:error] == :too_long }
  end
end
```

---

## 補足

本当は geocoding をモデルテストでスタブして見る手もありますが、今回は **integration 側で address 更新 → lat/lng 保存** を見ているので、まずは十分です。
このほうがテストのメンテも軽いです。

## 実行コマンド

あなたの流儀だとこれですね。

```bash
docker compose exec app bin/rails test test/models/store_test.rb test/integration/store_show_test.rb test/integration/admin_store_edit_authorization_test.rb
```

必要なら、通らなかったときの修正もその場で詰めます。
