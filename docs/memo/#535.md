以下で進めればOKです。

### 変更ファイル

* `app/helpers/drink_items_helper.rb`
* `app/assets/stylesheets/components/_drink_item.scss`
* `app/views/admin/drink_items/index.html.erb`
* `test/helpers/drink_items_helper_test.rb`（新規）

---

```rb
# app/helpers/drink_items_helper.rb
# frozen_string_literal: true

module DrinkItemsHelper
  def drink_icon(drink_item, size: 40, klass: "")
    image_tag(
      drink_item_icon_path(drink_item),
      alt: drink_item_icon_label(drink_item),
      title: drink_item_icon_label(drink_item),
      class: class_names("rounded-circle", klass),
      style: "width: #{size}px; height: #{size}px; object-fit: cover;"
    )
  end

  def drink_item_display(drink_item, icon_size: 40, klass: "")
    content_tag(
      :div,
      class: class_names(
        "drink-item-display",
        drink_item_display_tone_class(drink_item&.price_points),
        klass
      )
    ) do
      safe_join(
        [
          content_tag(:div, drink_icon(drink_item, size: icon_size, klass: "drink-item-display-icon")),
          content_tag(:div, class: "drink-item-display-body") do
            safe_join(
              [
                content_tag(:div, drink_item_display_name(drink_item), class: "drink-item-display-name"),
                content_tag(:div, drink_item_display_points(drink_item), class: "drink-item-display-points")
              ]
            )
          end
        ]
      )
    end
  end

  private

  def drink_item_icon_path(drink_item)
    drink_item_icon_option(drink_item)[:path]
  end

  def drink_item_icon_label(drink_item)
    drink_item_icon_option(drink_item)[:label]
  end

  def drink_item_icon_option(drink_item)
    drink_icon_option_by_key(drink_item&.icon_key)
  end

  def drink_icon_option_by_key(icon_key)
    return { label: "未設定", path: "drink_icons/drink_unset.jpg" } if icon_key.blank?

    DrinkItem::ICON_OPTIONS.fetch(icon_key, { label: "未設定", path: "drink_icons/drink_unset.jpg" })
  end

  def drink_item_display_name(drink_item)
    name = drink_item&.name.to_s.strip
    name.presence || "未設定"
  end

  def drink_item_display_points(drink_item)
    points = normalize_drink_item_points(drink_item&.price_points)
    return "" if points.nil?

    "#{points} pt"
  end

  def drink_item_display_tone_class(price_points)
    points = normalize_drink_item_points(price_points)
    return "drink-item-display-tone-default" if points.nil?

    case points
    when 100_000..
      "drink-item-display-tone-red"
    when 50_000..99_999
      "drink-item-display-tone-magenta"
    when 20_000..49_999
      "drink-item-display-tone-orange"
    when 10_000..19_999
      "drink-item-display-tone-yellow"
    when 5_000..9_999
      "drink-item-display-tone-green"
    when 2_000..4_999
      "drink-item-display-tone-cyan"
    else
      "drink-item-display-tone-default"
    end
  end

  def normalize_drink_item_points(value)
    points = Integer(value, exception: false)
    return nil if points.nil? || points <= 0

    points
  end
end
```

```scss
/* app/assets/stylesheets/components/_drink_item.scss */
.drink-item-card {
  width: 100%;
}

.drink-item-card-disabled {
  opacity: 0.7;
}

.drink-item-card-icon-image {
  display: inline-block;
  width: 72px;
  height: 72px;
  border-radius: 50%;
  object-fit: cover;
}

.drink-icon-toggle-list {
  display: flex;
  gap: 0.75rem;
  overflow-x: auto;
  padding-bottom: 0.25rem;
}

.drink-icon-toggle-btn {
  flex: 0 0 auto;
  padding: 0.25rem;
  border: 2px solid var(--bs-border-color);
  border-radius: 9999px;
  background-color: var(--bs-body-bg);
}

.drink-icon-toggle-image {
  display: block;
  width: 64px;
  height: 64px;
  border-radius: 50%;
  object-fit: cover;
}

.btn-check:checked + .drink-icon-toggle-btn {
  border-color: var(--bs-primary);
  background-color: rgba(var(--bs-primary-rgb), 0.12);
}

.btn-check:focus-visible + .drink-icon-toggle-btn {
  outline: 0;
  box-shadow: 0 0 0 0.25rem rgba(var(--bs-primary-rgb), 0.25);
}

.drink-item-display {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  min-width: 0;
  padding: 0.625rem 0.875rem;
  border: 1px solid transparent;
  border-radius: 9999px;
}

.drink-item-display-icon {
  display: block;
  flex: 0 0 auto;
}

.drink-item-display-body {
  display: flex;
  flex-direction: column;
  min-width: 0;
}

.drink-item-display-name {
  font-weight: 600;
  line-height: 1.2;
  word-break: break-word;
}

.drink-item-display-points {
  font-size: 0.875rem;
  line-height: 1.2;
  opacity: 0.9;
}

.drink-item-display-tone-default {
  color: #fff;
  background: linear-gradient(135deg, #1976d2 0%, #1565c0 100%);
  border-color: #1565c0;
}

.drink-item-display-tone-cyan {
  color: #073042;
  background: linear-gradient(135deg, #80deea 0%, #4dd0e1 100%);
  border-color: #26c6da;
}

.drink-item-display-tone-green {
  color: #103a1f;
  background: linear-gradient(135deg, #c5e1a5 0%, #9ccc65 100%);
  border-color: #8bc34a;
}

.drink-item-display-tone-yellow {
  color: #4a3b00;
  background: linear-gradient(135deg, #fff59d 0%, #ffd54f 100%);
  border-color: #ffca28;
}

.drink-item-display-tone-orange {
  color: #4a2100;
  background: linear-gradient(135deg, #ffcc80 0%, #ff9800 100%);
  border-color: #fb8c00;
}

.drink-item-display-tone-magenta {
  color: #fff;
  background: linear-gradient(135deg, #ec407a 0%, #d81b60 100%);
  border-color: #c2185b;
}

.drink-item-display-tone-red {
  color: #fff;
  background: linear-gradient(135deg, #ef5350 0%, #d32f2f 100%);
  border-color: #c62828;
}

@media (max-width: 575.98px) {
  .drink-item-card-icon-image {
    width: 64px;
    height: 64px;
  }

  .drink-icon-toggle-image {
    width: 56px;
    height: 56px;
  }

  .drink-item-display {
    gap: 0.625rem;
    padding: 0.5rem 0.75rem;
  }
}
```

```erb
<!-- app/views/admin/drink_items/index.html.erb -->
<h1>ドリンクメニュー</h1>

<div class="row g-3">
  <% @drink_items.each do |item| %>
    <% editing = @editing_drink_item_id == item.id %>

    <div class="col-12 col-xl-6">
      <% if editing %>
        <%= render "form",
                   drink_item: item,
                   url: admin_drink_item_path(item),
                   method: :patch,
                   cancel_url: admin_drink_items_path,
                   submit_label: "編集して保存" %>
      <% else %>
        <%= render "card", drink_item: item %>

        <div class="card mt-2" data-bs-theme="light">
          <div class="card-body">
            <div class="small text-secondary mb-2">表示プレビュー（debug）</div>
            <%= drink_item_display(item) %>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>

  <div class="col-12 col-xl-6">
    <%= render "form",
               drink_item: @drink_item,
               url: admin_drink_items_path,
               method: :post,
               cancel_url: nil,
               submit_label: "新規作成" %>
  </div>
</div>
```

```rb
# test/helpers/drink_items_helper_test.rb
require "test_helper"

class DrinkItemsHelperTest < ActionView::TestCase
  test "drink_item_display renders name and points" do
    drink_item = DrinkItem.new(name: "シャンパン", price_points: 12_000, icon_key: "champagne")

    html = drink_item_display(drink_item)

    assert_includes html, "シャンパン"
    assert_includes html, "12000 pt"
    assert_includes html, "drink-item-display"
    assert_includes html, "drink-item-display-tone-yellow"
  end

  test "drink_item_display falls back safely for nil" do
    html = drink_item_display(nil)

    assert_includes html, "未設定"
    assert_includes html, "drink-item-display"
    assert_includes html, "drink-item-display-tone-default"
  end

  test "drink_item_display uses default tone for invalid points" do
    drink_item = DrinkItem.new(name: "未設定ドリンク", price_points: nil)

    html = drink_item_display(drink_item)

    assert_includes html, "drink-item-display-tone-default"
    refute_includes html, " pt"
  end

  test "drink_item_display merges custom class into wrapper" do
    drink_item = DrinkItem.new(name: "マイク", price_points: 2_500, icon_key: "microphone")

    html = drink_item_display(drink_item, klass: "custom-preview-class")

    assert_includes html, "custom-preview-class"
    assert_includes html, "drink-item-display-tone-cyan"
  end
end
```

テスト実行はこれでOKです。

```bash
docker compose exec web bin/rails test test/helpers/drink_items_helper_test.rb
```

コミットメッセージ案:
`Add drink_item_display helper (#535)`

---
---

原因は `case` の `when` で使っている range の書き方です。
環境側の Ruby 解釈で `100_000..` まわりがうまく扱われず、`bad value for range` になっています。

ここは素直に **if / elsif の大小比較** に変えるのが安全です。
`drink_item_display_tone_class` だけ差し替えれば直ります。

```rb
def drink_item_display_tone_class(price_points)
  points = normalize_drink_item_points(price_points)
  return "drink-item-display-tone-default" if points.nil?

  if points >= 100_000
    "drink-item-display-tone-red"
  elsif points >= 50_000
    "drink-item-display-tone-magenta"
  elsif points >= 20_000
    "drink-item-display-tone-orange"
  elsif points >= 10_000
    "drink-item-display-tone-yellow"
  elsif points >= 5_000
    "drink-item-display-tone-green"
  elsif points >= 2_000
    "drink-item-display-tone-cyan"
  else
    "drink-item-display-tone-default"
  end
end
```

この形なら range 解釈に依存しないので安定します。

そのあと再実行はこれでOKです。

```bash
docker compose exec web bin/rails test test/helpers/drink_items_helper_test.rb
```

必要なら、通った前提で次にコミットメッセージとPR文もそのまま出します。
