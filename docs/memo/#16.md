## コード

### 1. Model（最低限）

`app/models/presence.rb`

```ruby
# frozen_string_literal: true

class Presence < ApplicationRecord
  belongs_to :stream_session
  belongs_to :customer_user, class_name: "User"
end
```

`app/models/stream_session.rb`（追記）

```ruby
has_many :presences, dependent: :destroy
```

---

### 2. Services（履歴方式 + DISTINCT 集計）

#### 2.1 PingService（最新 active 行を更新、なければ作成）
`app/services/presences/ping_service.rb`
```ruby
# frozen_string_literal: true

module Presences
  class PingService
    def initialize(stream_session:, customer_user:)
      @stream_session = stream_session
      @customer_user = customer_user
    end

    def call!
      now = Time.current

      ::Presence.transaction do
        latest_active = ::Presence
          .where(stream_session_id: @stream_session.id, customer_user_id: @customer_user.id, left_at: nil)
          .order(joined_at: :desc, id: :desc)
          .lock("FOR UPDATE")
          .first

        if latest_active
          latest_active.update!(last_seen_at: now)
        else
          ::Presence.create!(
            stream_session_id: @stream_session.id,
            customer_user_id: @customer_user.id,
            joined_at: now,
            last_seen_at: now,
            left_at: nil
          )
        end
      end
    end
  end
end
```

#### 2.2 SummaryService（壊れにくい DISTINCT）
`app/services/presences/summary_service.rb`
```ruby
# frozen_string_literal: true

module Presences
  class SummaryService
    def initialize(stream_session:, threshold_seconds: 45)
      @stream_session = stream_session
      @threshold_seconds = threshold_seconds
    end

    def call!
      ::Presence
        .where(stream_session_id: @stream_session.id, left_at: nil)
        .where("last_seen_at >= ?", Time.current - @threshold_seconds)
        .distinct
        .count(:customer_user_id)
    end
  end
end
```
**threshold は ping 間隔の 2倍以上**にしておくと安定します。

* ping 20秒 → threshold **40秒以上**（おすすめは 45〜60）

---

### 3. Routes

`config/routes.rb`

```ruby
resources :stream_sessions, only: [] do
  resources :comments, only: %i[create], module: :stream_sessions
  resources :drink_orders, only: %i[create], module: :stream_sessions

  resource :presence, only: [], module: :stream_sessions do
    post :ping
  end

  get :presence_summary, on: :member
end
```

**生成されるURL/ヘルパ**

* ping

  * `POST /stream_sessions/:stream_session_id/presence/ping`
  * helper: `ping_stream_session_presence_path(@stream_session)`
* summary

  * `GET  /stream_sessions/:id/presence_summary`
  * helper: `presence_summary_stream_session_path(@stream_session)`

---

### 4. Controller
#### 4.1 ping（StreamSessions::PresencesController）
`app/controllers/stream_sessions/presences_controller.rb`
```ruby
# frozen_string_literal: true

class StreamSessions::PresencesController < ApplicationController
  before_action :authenticate_user!
  before_action :set_stream_session
  # before_action :require_customer! # 必要なら有効化

  def ping
    Presences::PingService.new(
      stream_session: @stream_session,
      customer_user: current_user
    ).call!

    head :no_content
  end

  private

  def set_stream_session
    @stream_session = StreamSession.find(params[:stream_session_id])
  end

  # def require_customer!
  #   return if current_user.customer?
  #   render json: { error: "forbidden" }, status: :forbidden
  # end
end
```

#### 4.2 summary（StreamSessionsController の member action）
`app/controllers/stream_sessions_controller.rb`
```ruby
# frozen_string_literal: true

class StreamSessionsController < ApplicationController
  before_action :authenticate_user!
  before_action :set_stream_session

  def presence_summary
    viewer_count = Presences::SummaryService.new(
      stream_session: @stream_session,
      threshold_seconds: 60
    ).call!

    render json: { viewer_count: viewer_count }
  end

  private

  def set_stream_session
    @stream_session = StreamSession.find(params[:id])
  end
end
```

#### 4.3 booths
`app/controllers/booths_controller.rb`
```ruby
# frozen_string_literal: true

class BoothsController < ApplicationController
  def show
    @booth = Booth.find(params[:id])
    @stream_session = @booth.current_stream_session
  end
end
```
`app\controllers\cast\booths_controller.rb`
```ruby
module Cast
  class BoothsController < Cast::BaseController
    def index
      @booths = Booth.order(:id)
    end

    def show
      @booth = Booth.find(params[:id])
      @stream_session = @booth.current_stream_session
    end

    def status
      booth = Booth.find(params[:id])

      StreamSessions::StatusService.new(
        booth: booth,
        actor: current_user,
        to_status: params[:to]
      ).call

      redirect_to cast_booth_path(booth), notice: "状態更新: #{params[:to]}"
    rescue => e
      redirect_to cast_booth_path(booth), alert: e.message
    end
  end
end
```

#### 4.4 呼び出し置き換え
`app\controllers\stream_sessions_controller.rb`
```ruby
    viewer_count = Presences::SummaryService.new(
      stream_session: @stream_session,
      threshold_seconds: 45
    ).call!
```
---

### 5. Stimulus（customer: ping→summary / cast: summaryのみ）
#### 5.1 controller
`app/javascript/controllers/presence_poll_controller.js`
```js
import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  static values = {
    pingUrl: String,
    summaryUrl: String,
    intervalMs: Number
  }
  static targets = ["count"]

  connect() {
    this._tick = this._tick.bind(this)
    this._timer = setInterval(this._tick, this.intervalMsValue || 20000)
    this._tick()
  }

  disconnect() {
    if (this._timer) clearInterval(this._timer)
  }

  async _tick() {
    // customer画面だけ pingUrl を渡す（cast画面は渡さない＝書き込み無し）
    if (this.hasPingUrlValue) {
      await fetch(this.pingUrlValue, {
        method: "POST",
        headers: {
          "X-CSRF-Token": document.querySelector("meta[name=csrf-token]").content,
          "Accept": "application/json"
        }
      }).catch(() => {})
    }

    const res = await fetch(this.summaryUrlValue, { headers: { "Accept": "application/json" } }).catch(() => null)
    if (!res || !res.ok) return

    const data = await res.json()
    this.countTarget.textContent = String(data.viewer_count ?? "-")
  }
}
```

#### 5.2 view 設置例

**customer（ping あり）**

```erb
<% if @stream_session.present? %>
  <div
    data-controller="presence-poll"
    data-presence-poll-ping-url-value="<%= ping_stream_session_presence_path(@stream_session) %>"
    data-presence-poll-summary-url-value="<%= presence_summary_stream_session_path(@stream_session) %>"
    data-presence-poll-interval-ms-value="20000"
  >
    viewers: <span data-presence-poll-target="count">-</span>
  </div>
<% end %>

<div class="d-flex justify-content-end mb-3">
  <%= link_to "退室", root_path, class: "btn btn-outline-secondary" %>
</div>
```

**cast（summary のみ）**

```erb
<% if @stream_session.present? %>
  <div
    data-controller="presence-poll"
    data-presence-poll-summary-url-value="<%= presence_summary_stream_session_path(@stream_session) %>"
    data-presence-poll-interval-ms-value="10000"
  >
    viewers: <span data-presence-poll-target="count">-</span>
  </div>
<% end %>
```

---

### 6 よくあるハマりどころ（これだけ注意）

* `ping` の params は `:stream_session_id`（ネスト）
  `presence_summary` は `:id`（member）
  → controller の `set_stream_session` がそれぞれ違うのは正しいです。
* viewer_count は **必ず DISTINCT**（履歴方式で壊れにくくする肝）

Closes #16 
Relates to #12 