## 基本実装

### Model + validation（文字数制限の土台）
`app/models/comment.rb`
```ruby
class Comment < ApplicationRecord
  belongs_to :stream_session
  belongs_to :booth
  belongs_to :user

  scope :alive, -> { where(deleted_at: nil) }

  validates :body, presence: true, length: { maximum: 200 }
end
```

---

### Notifier（append を 1箇所に集約）
`app/notifiers/comment_notifier.rb`
```ruby
class CommentNotifier
  def self.append(comment)
    Turbo::StreamsChannel.broadcast_append_to(
      [comment.stream_session, :comments],
      target: "comments",
      partial: "comments/comment",
      locals: { comment: comment }
    )
  end
end
```

---

### Controller
`app/controllers/stream_sessions/comments_controller.rb`
```ruby
module StreamSessions
  class CommentsController < ApplicationController
    before_action :authenticate_user!
    before_action :set_stream_session

    def create
      body = params.require(:comment).fetch(:body).to_s

      raise ActiveRecord::RecordInvalid.new(Comment.new) if body.blank?

      # MVP: BANは後で入れるならここで raise でもOK（後述でservice化）
      comment = Comment.create!(
        stream_session: @stream_session,
        booth: @stream_session.booth,
        user: current_user,
        body: body
      )

      CommentNotifier.append(comment)

      respond_to do |format|
        format.turbo_stream do
          render turbo_stream: turbo_stream.replace(
            "comment_form",
            partial: "stream_sessions/comments/form",
            formats: [:html],
            locals: { stream_session: @stream_session }
          )
        end

        format.html { redirect_back fallback_location: root_path }
      end
    end

    private

    def set_stream_session
      @stream_session = StreamSession.find(params[:stream_session_id])
    end
  end
end
```
`app\controllers\booths_controller.rb`・`app\controllers\cast\booths_controller.rb`  
の `show` に追加
```rb
@comments =
  if @stream_session.present?
    Comment.alive.where(stream_session: @stream_session)
           .order(created_at: :desc)
           .limit(50)
           .reverse
  else
    []
  end
```
* DBで「新しい順に50件だけ」取る

* 表示は「古→新」にしたいので reverse で並び替える

### View（購読 + 受け皿 + partial）

**超重要**：視聴側・キャスト側の両方で「同じ stream_session」を購読すること。

```erb
<% if @stream_session.present? %>
  <%= turbo_stream_from @stream_session, :comments %>

  <div id="comments">
    <%= render partial: "comments/comment", collection: @comments, as: :comment %>
  </div>

  <%= render "stream_sessions/comments/form", stream_session: @stream_session %>
<% else %>
  <p>配信中のみコメントできます</p>
<% end %>
```

`app/views/comments/_comment.html.erb`
```erb
<div class="comment">
  <span class="comment-body"><%= comment.body %></span>
</div>
```

`app/views/stream_sessions/comments/_form.html.erb`
```erb
<turbo-frame id="comment_form">
  <%= form_with url: stream_session_comments_path(stream_session), scope: :comment do |f| %>
    <%= f.text_field :body, maxlength: 200, required: true, autocomplete: "off", autofocus: true %>
    <%= f.submit "送信" %>
  <% end %>
</turbo-frame>
```

---

## 最低限制限（BAN / 連投）を “崩さず” 足す

controller に散らばりそうな条件を **Service に閉じ込める**のが一番安全です。UI・Turbo Stream・broadcast の流れはそのままにして、**投稿可否だけ追加**します。

### 0) 方針（壊さないためのルール）

* controller は **薄くする**（params取り出し → service 呼び出し → 成功時のレスポンス）
* broadcast（`CommentNotifier.append`）は **成功時だけ**
* 失敗時は Turbo Stream でフォーム枠に **エラーメッセージ付きフォームを差し戻す**
  （`_form` を replace してるので相性が良い）

### 1) Service を作る（BAN/連投/セッション状態をここで判定）

`app/services/stream_sessions/comments/create_service.rb`

```ruby
# frozen_string_literal: true

module StreamSessions
  module Comments
    class CreateService
      class BannedError < StandardError; end
      class RateLimitedError < StandardError; end

      def initialize(stream_session:, user:, body:)
        @stream_session = stream_session
        @user = user
        @body = body.to_s.strip
      end

      def call
        raise ActiveRecord::RecordInvalid.new(Comment.new) if @body.blank?
        raise BannedError if banned?
        raise RateLimitedError if rate_limited?

        comment = Comment.create!(
          stream_session: @stream_session,
          booth_id: @stream_session.booth_id,
          user: @user,
          body: @body
        )

        CommentNotifier.append(comment)
        comment
      end

      private

      def banned?
        return false unless @user.customer?
        StoreBan.exists?(store_id: @stream_session.store_id, customer_user_id: @user.id)
      end

      def rate_limited?
        Comment.where(user_id: @user.id, stream_session_id: @stream_session.id)
               .where("created_at >= ?", 1.second.ago)
               .exists?
      end
    end
  end
end
```

* `@user.customer?` で BAN 判定を customer のみに限定

* `store_id + customer_user_id` は `store_bans` に完全一致

---

### 2) Controller を service 化して、失敗時はフォームにエラー表示

まず `_form.html.erb` を少しだけ拡張して、エラーを出せるようにします。

`app/views/stream_sessions/comments/_form.html.erb`（エラー表示を追加）

```erb
<turbo-frame id="comment_form">
  <% if local_assigns[:error_message].present? %>
    <div class="alert alert-danger py-1 mb-2"><%= error_message %></div>
  <% end %>

  <%= form_with url: stream_session_comments_path(stream_session), scope: :comment do |f| %>
    <%= f.text_field :body, maxlength: 200, required: true, autocomplete: "off", autofocus: true %>
    <%= f.submit "送信" %>
  <% end %>
</turbo-frame>
```

`app\controllers\stream_sessions\comments_controller.rb`

「フォーム partial 側に `<turbo-frame id="comment_form">` を持たせる」方式で安定してる前提で、成功時は空フォームに置換、失敗時はエラー付きフォームに置換にします。

```ruby
module StreamSessions
  class CommentsController < ApplicationController
    before_action :authenticate_user!
    before_action :set_stream_session

    def create
      StreamSessions::Comments::CreateService.new(
        stream_session: @stream_session,
        user: current_user,
        body: params.require(:comment).fetch(:body)
      ).call

      respond_to do |format|
        format.turbo_stream do
          render turbo_stream: turbo_stream.replace(
            "comment_form",
            partial: "stream_sessions/comments/form",
            formats: [:html],
            locals: { stream_session: @stream_session }
          )
        end
        format.html { redirect_back fallback_location: root_path }
      end
    rescue StreamSessions::Comments::CreateService::BannedError
      render_comment_form_error("BANされています")
    rescue StreamSessions::Comments::CreateService::RateLimitedError
      render_comment_form_error("送信が速すぎます。少し待ってからもう一度送信してください")
    rescue ActionController::ParameterMissing, KeyError
      render_comment_form_error("入力が不正です")
    end

    private

    def set_stream_session
      @stream_session = StreamSession.find(params[:stream_session_id])
    end

    def render_comment_form_error(message)
      respond_to do |format|
        format.turbo_stream do
          render turbo_stream: turbo_stream.replace(
            "comment_form",
            partial: "stream_sessions/comments/form",
            formats: [:html],
            locals: { stream_session: @stream_session, error_message: message }
          )
        end
        format.html { redirect_back fallback_location: root_path, alert: message }
      end
    end
  end
end
```

### 3) StoreBan モデル
`app/models/store_ban.rb`
```ruby
class StoreBan < ApplicationRecord
  belongs_to :store
  belongs_to :customer_user, class_name: "User"
  belongs_to :created_by_store_admin_user, class_name: "User"
end
```

---

Closes #17 
Relates to #12 


