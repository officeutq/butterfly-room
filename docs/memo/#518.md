了解です。
確認できた範囲では、**Turbo差し替えは `cast/stream_sessions/meta_display.html.erb` → `_meta_display_frame` → partial** の流れなので、今回は view の整理だけで #518 を完了できます。viewer 側は直書き meta を共通partial化し、cast 側は Turbo Frame を維持したまま参照先だけを差し替える形です。

## 実装プラン

1. `stream_sessions/_stream_meta_display.html.erb` を新規作成

   * `stream_session` を唯一の必須 local にする
   * タイトルのフォールバックは `stream_session.booth.name`

2. `cast/stream_sessions/_meta_display_frame.html.erb` を更新

   * Turbo Frame はそのまま
   * 中身だけ新partial参照へ変更

3. `cast/booths/live.html.erb` を更新

   * 直 render を新partial参照へ変更
   * Turbo Frame id はそのまま維持

4. `stream_sessions/_ivs_viewer.html.erb` を更新

   * `title_display` のローカル計算を削除
   * 直書き meta 表示を共通partial render に置換

5. `cast/booths/_stream_meta_display.html.erb` は削除

   * 参照が新partialへ寄るため不要

---

## 貼り付け用コード

### 1. 新規作成

`app/views/stream_sessions/_stream_meta_display.html.erb`

```erb
<%# locals: stream_session: %>
<% title_display = stream_session.title.presence || stream_session.booth.name %>

<div class="live-meta-main-row">
  <div class="live-meta-title"><%= title_display %></div>
</div>

<% if stream_session.description.present? %>
  <div class="live-meta-description">
    <%= simple_format(stream_session.description) %>
  </div>
<% end %>
```

---

### 2. 更新

`app/views/cast/stream_sessions/_meta_display_frame.html.erb`

```erb
<%# locals: booth:, stream_session: %>
<turbo-frame id="stream_meta_display">
  <%= render "stream_sessions/stream_meta_display",
            stream_session: stream_session %>
</turbo-frame>
```

---

### 3. 更新

`app/views/cast/booths/live.html.erb`

```erb
<div
  class="cast-live-screen"
  data-controller="ivs-publisher live-stage"
  data-ivs-publisher-token-url-value="<%= stream_session_ivs_participant_tokens_path(@stream_session) %>"
  data-ivs-publisher-finish-url-value="<%= finish_cast_stream_session_path(@stream_session) %>"
  data-ivs-publisher-status-url-value="<%= status_cast_booth_path(@booth) %>"
  data-ivs-publisher-meta-display-url-value="<%= meta_display_cast_stream_session_path(@stream_session) %>"
  data-ivs-publisher-mirror-value="true"
  data-ivs-publisher-initial-mode-value="<%= @booth.away? ? "away" : "normal" %>"
  data-ivs-publisher-initial-booth-status-value="<%= @booth.status %>"
  data-ivs-publisher-banuba-client-token-value="<%= @banuba_client_token %>"
  data-ivs-publisher-banuba-sdk-base-url-value="<%= @banuba_sdk_base_url %>"
  data-ivs-publisher-banuba-face-tracker-url-value="<%= @banuba_face_tracker_url %>"
  data-ivs-publisher-banuba-eyes-url-value="<%= @banuba_eyes_url %>"
  data-ivs-publisher-banuba-lips-url-value="<%= @banuba_lips_url %>"
  data-ivs-publisher-banuba-skin-url-value="<%= @banuba_skin_url %>"
  data-ivs-publisher-banuba-background-url-value="<%= @banuba_background_url %>"
  data-ivs-publisher-banuba-hair-url-value="<%= @banuba_hair_url %>"
  data-ivs-publisher-banuba-effect-url-value="<%= @banuba_effect_url %>"
  data-ivs-publisher-banuba-effect-name-value="<%= @banuba_effect_name %>"
  data-ivs-publisher-banuba-beauty-config-url-value="/banuba/effects/beauty_base/config.json"
>
  <div id="live_stage">
    <div id="live_media">
      <div
        class="live-banuba-surface"
        data-ivs-publisher-target="banubaSurface"></div>

      <canvas
        class="d-none"
        data-ivs-publisher-target="canvas"></canvas>
    </div>

    <div id="live_flash_layer">
      <div id="flash_inner">
        <% if notice.present? %>
          <%= render "shared/flash_message", level: "success", message: notice %>
        <% end %>

        <% if alert.present? %>
          <%= render "shared/flash_message", level: "danger", message: alert %>
        <% end %>
      </div>
    </div>

    <div id="live_overlay">
      <div class="live-overlay-ui" data-live-grid-debug="<%= Rails.env.development? ? "1" : "0" %>">
        <div class="live-overlay-meta-slot">
          <div
            class="live-meta-panel d-none"
            data-controller="presence-poll"
            data-ivs-publisher-target="metaPanel"
            data-presence-poll-summary-url-value="<%= presence_summary_stream_session_path(@stream_session) %>"
            data-presence-poll-interval-ms-value="20000"
          >
            <div class="live-meta-layout">
              <div class="live-meta-main">
                <turbo-frame id="stream_meta_display">
                  <%= render "stream_sessions/stream_meta_display",
                            stream_session: @stream_session %>
                </turbo-frame>
              </div>

              <div class="live-meta-side">
                <div class="live-meta-viewer-count">
                  <i class="bi bi-people-fill" aria-hidden="true"></i>
                  <span data-presence-poll-target="count">-</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        <div class="live-overlay-main-row">
          <div class="live-overlay-main-spacer">
            <div class="live-standby-actions-panel d-none" data-ivs-publisher-target="summaryPanel">
              <div class="live-standby-actions-buttons">
                <%= link_to "配信設定",
                      meta_modal_cast_stream_session_path(@stream_session),
                      class: "live-standby-action-btn live-standby-action-link",
                      data: { turbo_frame: "modal" } %>
              </div>
            </div>
          </div>

          <div class="live-overlay-drink-slot">
            <%= turbo_stream_from [@stream_session, :cast_pending_drink_orders] %>

            <div class="live-drink-panel d-none" data-ivs-publisher-target="drinkPanel">
              <div class="live-drink-panel-header">ドリンク</div>

              <div class="live-drink-panel-body">
                <div id="cast-pending-drink-orders-container">
                  <%= render "cast/stream_sessions/pending_drink_orders", stream_session: @stream_session %>
                </div>
              </div>
            </div>
          </div>
        </div>

        <div class="live-overlay-bottom-row">
          <div class="live-overlay-comment-slot">
            <%= turbo_stream_from [@stream_session, :comments] %>

            <div id="cast_comment_section">
              <%= render "cast/booths/comment_section",
                        booth: @booth,
                        stream_session: @stream_session,
                        comments: @comments %>
            </div>
          </div>

          <div class="live-overlay-ops-slot">
            <div class="live-ops-panel d-none" data-ivs-publisher-target="opsPanel">
              <div class="live-ops-buttons">
                <%= form_with url: status_cast_booth_path(@booth, to: :away),
                              method: :patch,
                              class: "live-ops-form",
                              data: { action: "turbo:submit-end->ivs-publisher#onBoothStatusPatched" } do %>
                  <button type="submit"
                          class="live-ops-btn"
                          data-ivs-publisher-target="cameraOffBtn"
                          aria-label="カメラをオフにする"
                          title="カメラON">
                    <i class="bi bi-camera-video-fill" aria-hidden="true"></i>
                  </button>
                <% end %>

                <%= form_with url: status_cast_booth_path(@booth, to: :live),
                              method: :patch,
                              class: "live-ops-form",
                              data: { action: "turbo:submit-end->ivs-publisher#onBoothStatusPatched" } do %>
                  <button type="submit"
                          class="live-ops-btn"
                          data-ivs-publisher-target="cameraOnBtn"
                          aria-label="カメラをオンにする"
                          title="カメラOFF">
                    <i class="bi bi-camera-video-off-fill" aria-hidden="true"></i>
                  </button>
                <% end %>

                <button type="button"
                        class="live-ops-btn"
                        data-action="ivs-publisher#toggleMic"
                        data-ivs-publisher-target="micBtn"
                        aria-label="マイクを切り替える"
                        title="マイク切替">
                  <i class="bi bi-mic-fill" data-ivs-publisher-target="micIcon" aria-hidden="true"></i>
                </button>

                <button type="button"
                        class="live-ops-btn"
                        data-action="ivs-publisher#openEffectPanel"
                        data-ivs-publisher-target="effectOpenBtn"
                        aria-label="加工モードを選択する"
                        title="加工モード選択">
                  <i class="bi bi-stars" aria-hidden="true"></i>
                </button>

                <button type="button"
                        class="live-ops-btn d-none"
                        data-action="ivs-publisher#openBeautyPanel"
                        data-ivs-publisher-target="beautyAdjustBtn"
                        aria-label="Beauty 設定を開く"
                        title="Beauty 調整">
                  <i class="bi bi-sliders" aria-hidden="true"></i>
                </button>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <%= render "cast/booths/effect_panel", effects: @effects %>
    <%= render "cast/booths/beauty_panel" %>
  </div>

  <footer id="app_footer" class="cast-live-footer border-top bg-white fixed-bottom">
    <div class="container py-2">
      <div class="d-flex justify-content-between align-items-center">
        <div class="d-flex gap-3">
          <%= link_to "ホーム", root_path, class: "btn btn-sm btn-outline-secondary" %>
          <%= link_to "お気に入り", favorites_booths_path, class: "btn btn-sm btn-outline-secondary" %>
        </div>

        <div class="d-flex gap-2">
          <button type="button"
                  class="btn btn-sm btn-primary"
                  data-action="ivs-publisher#startBroadcast"
                  data-ivs-publisher-target="startBtn"
                  data-label-normal="配信開始"
                  data-label-resume="配信に戻る">
            配信開始
          </button>

          <button type="button"
                  class="btn btn-sm btn-danger d-none"
                  data-action="ivs-publisher#endBroadcast"
                  data-ivs-publisher-target="endBtn">
            配信終了
          </button>
        </div>

        <div class="d-flex gap-2 align-items-center">
          <%= link_to "ダッシュボード",
                dashboard_path_for(current_user),
                class: "btn btn-sm btn-outline-secondary" %>

          <% if user_signed_in? %>
            <%= button_to "ログアウト",
                  destroy_user_session_path,
                  method: :delete,
                  class: "btn btn-sm btn-outline-secondary" %>
          <% end %>
        </div>
      </div>
    </div>
  </footer>
</div>
```

---

### 4. 更新

`app/views/stream_sessions/_ivs_viewer.html.erb`

```erb
<%# locals: stream_session:, booth:, comments:, drink_items:, can_create_drink_order: %>

<% live_like = booth.live? || booth.away? %>
<% drink_menu_button_visible = live_like && Array(drink_items).any? %>

<div
  class="ivs-viewer-root"
  data-controller="ivs-viewer"
  data-ivs-viewer-token-url-value="<%= stream_session_ivs_participant_tokens_path(stream_session) %>"
>
  <video
    class="ivs-viewer-video"
    playsinline
    autoplay
    data-ivs-viewer-target="video"
  ></video>

  <div id="viewer_overlay">
    <div
      class="viewer-overlay-ui"
      data-controller="viewer-drink-panel"
      data-viewer-drink-panel-menu-url-value="<%= viewer_drink_menu_booth_path(booth) %>"
      data-viewer-grid-debug="<%= Rails.env.development? ? "1" : "0" %>">
      <div class="viewer-overlay-meta-slot">
        <div
          class="viewer-meta-panel"
          data-controller="presence-poll"
          data-presence-poll-ping-url-value="<%= ping_stream_session_presence_path(stream_session) %>"
          data-presence-poll-summary-url-value="<%= presence_summary_stream_session_path(stream_session) %>"
          data-presence-poll-interval-ms-value="20000"
        >
          <div class="viewer-meta-layout">
            <div class="viewer-meta-main">
              <%= render "stream_sessions/stream_meta_display",
                        stream_session: stream_session %>
            </div>

            <div class="viewer-meta-side">
              <div class="viewer-meta-viewer-count">
                <i class="bi bi-people-fill" aria-hidden="true"></i>
                <span data-presence-poll-target="count">-</span>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="viewer-overlay-main-row">
        <div class="viewer-overlay-main-spacer">
          <span class="viewer-slot-label">main spacer</span>
        </div>

        <div class="viewer-overlay-drink-slot">
          <div class="viewer-drink-panel">
            <div class="viewer-drink-panel-header">ドリンク</div>

            <div class="viewer-drink-panel-body">
              <%= render "stream_sessions/pending_drink_orders", stream_session: stream_session %>
            </div>
          </div>
        </div>
      </div>

      <div class="viewer-overlay-bottom-row">
        <div class="viewer-overlay-comment-slot">
          <%= render "booths/comment_section",
                booth: booth,
                stream_session: stream_session,
                comments: comments %>
        </div>

        <div class="viewer-overlay-ops-slot">
          <div class="viewer-ops-panel">
            <div class="viewer-ops-buttons">
              <% if drink_menu_button_visible %>
                <button
                  type="button"
                  class="viewer-ops-btn"
                  data-action="viewer-drink-panel#toggle"
                  data-viewer-drink-panel-target="openButton"
                  aria-label="ドリンクメニューを開く"
                  aria-expanded="false"
                  aria-controls="viewer_drink_menu_panel"
                  title="ドリンクメニュー">
                  <i class="bi bi-cup-straw" aria-hidden="true"></i>
                </button>
              <% end %>

              <button
                type="button"
                class="viewer-ops-btn"
                data-action="ivs-viewer#toggleMute"
                data-ivs-viewer-target="muteButton"
                aria-label="ミュートを切り替える"
                title="ミュート切替"
              >
                <i
                  class="bi bi-volume-mute-fill"
                  data-ivs-viewer-target="muteIcon"
                  aria-hidden="true"
                ></i>
              </button>

              <div
                class="viewer-mute-hint"
                data-ivs-viewer-target="muteHint"
                style="display: none;"
              >
                タップで音を出せます
              </div>

              <div class="viewer-error-text" data-ivs-viewer-target="error"></div>
            </div>
          </div>
        </div>
      </div>

      <% if drink_menu_button_visible %>
        <%= render "booths/drink_menu_panel" %>
      <% end %>
    </div>
  </div>
</div>
```

---

### 5. 削除対象

`app/views/cast/booths/_stream_meta_display.html.erb`

```text
削除
```

## 確認ポイント

* cast live で meta 表示が従来どおり出る
* standby で metadata 更新後に Turbo 差し替えされる
* viewer で title / description が従来どおり出る
* missing partial / undefined local variable が出ない

一点だけ補足すると、今回の共通partialは `live-meta-*` クラス名のままなので、viewer 側が `viewer-meta-*` 専用CSSに依存していたら見た目差分が出る可能性があります。Issue本文どおり「表示仕様は変えない」を最優先にするなら、まずこのまま当てて画面確認し、必要なら次で **クラス名だけを共通化する別Refactor** に分けるのが安全です。
