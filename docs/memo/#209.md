了解。#209 を **最小差分**で実装する前提で、**貼り付け用コード**をまとめます（URL直書きゼロ／enter導線統一／session更新／暫定HTMLで視聴/配信選択）。

---

## 実装方針（最終理解）

* `GET /booths/:id/enter` でロール/権限に応じて分岐

  * guest/customer：`booth_path`（視聴）
  * cast（BoothCastあり＝本人）：**session更新 → `live_cast_booth_path`**
  * cast（他人）：`booth_path`
  * store_admin（自店）/ system_admin：**暫定HTML（視聴/配信 選択）**
* `POST /booths/:id/enter_as_cast`

  * 認可OK（cast本人 / store_admin自店 / system_admin）なら **session更新 → `live_cast_booth_path`**
  * NGなら `403`
* `admin` の店舗切替（current_store）時に `current_booth` をクリア（Current整合ルール）

---

## ブランチ名候補

* `improve/209-enter-flow`
* `improve/209-booths-enter`
* `improve/209-enter-routing`

---

## 変更対象ファイル一覧

* `config/routes.rb`
* `app/controllers/booths_controller.rb`
* `app/views/booths/enter.html.erb`（新規）
* `app/views/home/show.html.erb`
* `app/views/favorites/booths/index.html.erb`
* `app/views/stores/show.html.erb`
* `app/controllers/admin/current_stores_controller.rb`
* `test/integration/store_show_test.rb`
* `test/integration/booth_enter_test.rb`（新規）

---

# Paste-ready code

## 1) `config/routes.rb`（public booths に member追加）

`resources :booths, only: %i[show] do` を次のように更新：

```ruby
  # --- Public (customer) ---
  resources :booths, only: %i[show] do
    member do
      get :enter
      post :enter_as_cast
    end

    resource :favorite, only: %i[create destroy], controller: "favorites/booths"
  end
```

---

## 2) `app/controllers/booths_controller.rb`（enter / enter_as_cast 追加）

全文置き換えでOK：

```ruby
# frozen_string_literal: true

class BoothsController < ApplicationController
  include StoreBanGuard

  before_action :set_booth, only: %i[show enter enter_as_cast]
  before_action :reject_banned_customer_for_booth!, only: %i[show]

  def show
    @stream_session = @booth.current_stream_session
    @drink_items = @booth.store.drink_items.enabled_only.ordered

    @wallet =
      if user_signed_in?
        Wallet.find_or_create_by!(customer_user_id: current_user.id) do |w|
          w.available_points = 0
          w.reserved_points = 0
        end
      end

    @comments =
      if @stream_session.present?
        Comment.alive.where(stream_session: @stream_session)
              .order(created_at: :desc)
              .limit(50)
              .reverse
      else
        []
      end

    @booth_favorited =
      user_signed_in? && current_user.favorite_booths.exists?(booth_id: @booth.id)
  end

  # ブースカードクリック導線（ロール/権限で分岐）
  def enter
    # guest は customer と同じ（視聴）
    unless user_signed_in?
      redirect_to booth_path(@booth)
      return
    end

    # customer は常に視聴
    if current_user.customer?
      redirect_to booth_path(@booth)
      return
    end

    # cast（本人ブースなら即配信 / 他人ブースは視聴）
    if current_user.cast?
      if BoothCast.exists?(cast_user_id: current_user.id, booth_id: @booth.id)
        set_current_context_for_booth!
        redirect_to live_cast_booth_path(@booth)
      else
        redirect_to booth_path(@booth)
      end
      return
    end

    # store_admin（自店なら選択UI / 他店なら視聴）
    if current_user.store_admin?
      if current_user.admin_of_store?(@booth.store_id)
        render :enter, status: :ok
      else
        redirect_to booth_path(@booth)
      end
      return
    end

    # system_admin（常に選択UI）
    if current_user.system_admin?
      render :enter, status: :ok
      return
    end

    # 想定外は安全側（視聴）
    redirect_to booth_path(@booth)
  end

  # 「配信する」選択 → session更新 → cast live へ
  def enter_as_cast
    require_at_least!(:cast)

    allowed =
      if current_user.system_admin?
        true
      elsif current_user.store_admin?
        current_user.admin_of_store?(@booth.store_id)
      else
        BoothCast.exists?(cast_user_id: current_user.id, booth_id: @booth.id)
      end

    head :forbidden unless allowed
    return unless allowed

    set_current_context_for_booth!
    redirect_to live_cast_booth_path(@booth)
  end

  private

  def set_booth
    @booth = Booth.active.find(params[:id])
  end

  def reject_banned_customer_for_booth!
    reject_banned_customer!(store: @booth.store)
  end

  def set_current_context_for_booth!
    session[:current_booth_id] = @booth.id
    session[:current_store_id] = @booth.store_id
  end
end
```

---

## 3) `app/views/booths/enter.html.erb`（新規作成）

新規ファイル作成：

```erb
<div class="container mt-4" style="max-width: 720px;">
  <h1 class="h4 mb-3"><%= @booth.name.presence || "ブース" %></h1>

  <div class="alert alert-secondary" role="alert">
    視聴 / 配信 を選択してください。
  </div>

  <div class="d-flex gap-2">
    <%= link_to "視聴する", booth_path(@booth), class: "btn btn-outline-primary" %>

    <%= button_to "配信する",
          enter_as_cast_booth_path(@booth),
          method: :post,
          class: "btn btn-primary" %>
  </div>

  <div class="mt-3">
    <%= link_to "戻る", root_path, class: "btn btn-outline-secondary btn-sm" %>
  </div>
</div>
```

---

## 4) Home / Favorites / Store show のリンクを enter に統一

### `app/views/home/show.html.erb`

2箇所を置換：

* `link_to booth_path(booth)` → `link_to enter_booth_path(booth)`
* `link_to booth.name, booth_path(booth)` → `link_to booth.name, enter_booth_path(booth)`

該当部分だけ貼るなら：

```erb
              <%= link_to enter_booth_path(booth), class: "text-decoration-none text-reset" do %>
                ...
              <% end %>
...
                    <%= link_to booth.name, enter_booth_path(booth), class: "text-decoration-none text-reset" %>
```

### `app/views/favorites/booths/index.html.erb`

2箇所を置換：

* `link_to booth.name, booth_path(booth)` → `enter_booth_path(booth)`
* `"開く"` の `booth_path(booth)` → `enter_booth_path(booth)`

```erb
                <%= link_to booth.name, enter_booth_path(booth), class: "text-decoration-none" %>
...
              <%= link_to "開く", enter_booth_path(booth), class: "btn btn-outline-primary btn-sm" %>
```

### `app/views/stores/show.html.erb`

1箇所を置換：

* `"視聴ページへ"` の `booth_path(booth)` → `enter_booth_path(booth)`

```erb
              <%= link_to "視聴ページへ", enter_booth_path(booth), class: "btn btn-outline-primary btn-sm" %>
```

---

## 5) `app/controllers/admin/current_stores_controller.rb`（store切替時に current_booth をクリア）

`session[:current_store_id] = store_id` の直後に `session.delete(:current_booth_id)` を追加：

```ruby
      session[:current_store_id] = store_id
      session.delete(:current_booth_id)
      redirect_to admin_root_path, notice: "店舗を切り替えました"
```

---

# テスト更新

## 6) `test/integration/store_show_test.rb`（enter 統一に追従）

`assert_includes @response.body, booth_path(active_booth)` を `enter_booth_path(active_booth)` に変更：

```ruby
    assert_includes @response.body, enter_booth_path(active_booth)
```

---

## 7) `test/integration/booth_enter_test.rb`（新規）

新規ファイル作成：

```ruby
# frozen_string_literal: true

require "test_helper"

class BoothEnterTest < ActionDispatch::IntegrationTest
  def create_store!(name: "store")
    Store.create!(name: name)
  end

  def create_booth!(store:, name: "booth", status: :offline)
    Booth.create!(store: store, name: name, status: status)
  end

  test "guest: enter redirects to booth show" do
    store = create_store!(name: "s")
    booth = create_booth!(store: store, name: "b", status: :offline)

    get enter_booth_path(booth)
    assert_response :redirect
    assert_redirected_to booth_path(booth)
  end

  test "customer: enter redirects to booth show" do
    store = create_store!(name: "s")
    booth = create_booth!(store: store, name: "b", status: :offline)

    customer = User.create!(email: "customer_enter@example.com", password: "password", role: :customer)
    sign_in customer, scope: :user

    get enter_booth_path(booth)
    assert_response :redirect
    assert_redirected_to booth_path(booth)
  end

  test "cast (own booth): enter redirects to cast live and sets session" do
    store = create_store!(name: "s")
    booth = create_booth!(store: store, name: "b", status: :standby)

    cast = User.create!(email: "cast_enter@example.com", password: "password", role: :cast)
    BoothCast.create!(booth: booth, cast_user: cast)

    sign_in cast, scope: :user

    get enter_booth_path(booth)
    assert_response :redirect
    assert_redirected_to live_cast_booth_path(booth)

    assert_equal booth.id, @request.session[:current_booth_id]
    assert_equal store.id, @request.session[:current_store_id]
  end

  test "cast (other booth): enter redirects to booth show" do
    store = create_store!(name: "s")
    booth = create_booth!(store: store, name: "b", status: :offline)

    cast = User.create!(email: "cast_other@example.com", password: "password", role: :cast)
    sign_in cast, scope: :user

    get enter_booth_path(booth)
    assert_response :redirect
    assert_redirected_to booth_path(booth)
  end

  test "store_admin (own store): enter renders selection page" do
    store = create_store!(name: "s")
    booth = create_booth!(store: store, name: "b", status: :offline)

    admin = User.create!(email: "admin_enter@example.com", password: "password", role: :store_admin)
    StoreMembership.create!(store: store, user: admin, membership_role: :admin)

    sign_in admin, scope: :user

    get enter_booth_path(booth)
    assert_response :success
    assert_includes response.body, "視聴する"
    assert_includes response.body, "配信する"
  end

  test "system_admin: enter renders selection page" do
    store = create_store!(name: "s")
    booth = create_booth!(store: store, name: "b", status: :offline)

    sys = User.create!(email: "sys_enter@example.com", password: "password", role: :system_admin)
    sign_in sys, scope: :user

    get enter_booth_path(booth)
    assert_response :success
    assert_includes response.body, "視聴する"
    assert_includes response.body, "配信する"
  end

  test "enter_as_cast: store_admin can start (sets session and redirects)" do
    store = create_store!(name: "s")
    booth = create_booth!(store: store, name: "b", status: :standby)

    admin = User.create!(email: "admin_enter_as_cast@example.com", password: "password", role: :store_admin)
    StoreMembership.create!(store: store, user: admin, membership_role: :admin)

    sign_in admin, scope: :user

    post enter_as_cast_booth_path(booth)
    assert_response :redirect
    assert_redirected_to live_cast_booth_path(booth)

    assert_equal booth.id, @request.session[:current_booth_id]
    assert_equal store.id, @request.session[:current_store_id]
  end
end
```

---

## 最後の確認コマンド（Windows/Docker前提）

* ルート確認
  `docker compose exec app bash -lc "bin/rails routes | grep -E 'enter|enter_as_cast'"`
* テスト
  `docker compose exec app bash -lc "RAILS_ENV=test bin/rails test"`

---

## 注意点（意図どおり）

* `checkout/return.html.erb` の `booth_path(@booth_id)` は **変更しません**（ブース内購入→ブースへ戻る、の方針どおり）。
* StoreBan は `booths#show` が最終防衛なので、`enter` は最小（showに委譲）でOK。

このまま実装していけば、#209 の Done（分岐・session更新・helper統一・URL直書きゼロ）は満たせます。必要なら、このあと **コミットメッセージ案 / PR本文** も #209 向けに作ります。
