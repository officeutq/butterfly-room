## 0) 追加するもの一覧（最小）

* `stream_sessions.ivs_stage_arn` を追加（NULL可、後から埋まる）
* `app/services/ivs/client.rb` を追加
* `app/services/stream_sessions/ensure_ivs_stage_service.rb` を追加
* `StreamSessions::StartService` に “ensure stage” を1行差し込む
* （任意）`aws-sdk-ivsrealtime` gem を追加

---

## 1) Migration（空生成→全文置換でOK）

### 1-1. 空マイグレーション生成

（あなたの方針どおり、まず空を作る）

```bash
docker compose exec app bash -lc "bin/rails g migration AddIvsStageArnToStreamSessions"
```

### 1-2. 生成されたファイルを全文置換

```rb
# frozen_string_literal: true

class AddIvsStageArnToStreamSessions < ActiveRecord::Migration[8.1]
  def change
    add_column :stream_sessions, :ivs_stage_arn, :string

    # Stage ARN は重複させない（1 stream_session = 1 stage の土台）
    add_index :stream_sessions,
              :ivs_stage_arn,
              unique: true,
              where: "ivs_stage_arn IS NOT NULL"
  end
end
```

### 1-3. migrate

```bash
docker compose exec app bash -lc "bin/rails db:migrate"
```

---

## 2) AWS SDK（IVS Real-Time）を使う gem を追加

Gemfile に追加（もし未追加なら）

```rb
gem "aws-sdk-ivsrealtime"
```

インストール

```bash
docker compose exec app bash -lc "bundle install"
```

> 既存で aws-sdk 系を入れていても、サービス別 gem が無ければ `Aws::IVSRealTime::Client` は使えないので、ここは入れるのが安全です。

---

## 3) `app/services/ivs/client.rb` を追加

`app/services/ivs/client.rb`

```rb
# frozen_string_literal: true

module Ivs
  class Client
    def initialize(region: ENV.fetch("AWS_REGION", "ap-northeast-1"))
      @client = Aws::IVSRealTime::Client.new(region: region)
    end

    # returns stage arn
    def create_stage!(name:)
      resp = @client.create_stage(name: name)
      resp.stage.arn
    end
  end
end
```

---

## 4) `EnsureIvsStageService` を追加（1:1 を守るコア）

`app/services/stream_sessions/ensure_ivs_stage_service.rb`

```rb
# frozen_string_literal: true

module StreamSessions
  class EnsureIvsStageService
    class Error < StandardError; end

    def initialize(stream_session:, ivs_client: Ivs::Client.new)
      @stream_session = stream_session
      @ivs_client = ivs_client
    end

    # returns stage arn
    def call
      @stream_session.with_lock do
        return @stream_session.ivs_stage_arn if @stream_session.ivs_stage_arn.present?

        # 追跡しやすい命名（AWS側の name 制約に引っかかりにくい）
        stage_name = "stream_session-#{@stream_session.id}"

        arn = @ivs_client.create_stage!(name: stage_name)

        @stream_session.update!(ivs_stage_arn: arn)
        arn
      end
    end
  end
end
```

ポイント：

* `with_lock` により **同一 stream_session で二重作成されにくい**
* DB 側 unique index が最後の砦

---

## 5) `StartService` に組み込み（配信開始時に Stage を確定）

`app/services/stream_sessions/start_service.rb` に、`session` 生成後に1行足します。

```rb
# frozen_string_literal: true

module StreamSessions
  class StartService
    class Error < StandardError; end
    class BoothNotOffline < Error; end
    class NotAuthorized < Error; end

    def initialize(booth:, actor:)
      @booth = booth
      @actor = actor
    end

    def call
      authorize!

      StreamSession.transaction do
        booth = Booth.lock.find(@booth.id)

        raise BoothNotOffline, "booth is #{booth.status}" unless booth.offline?

        session = StreamSession.create!(
          booth: booth,
          store: booth.store,
          status: :live,
          started_at: Time.current,
          started_by_cast_user: @actor
        )

        # ★追加：ここで Stage を確定（同一sessionなら固定）
        StreamSessions::EnsureIvsStageService.new(stream_session: session).call

        booth.update!(
          status: :live,
          current_stream_session_id: session.id
        )

        session
      end
    end

    private

    def authorize!
      allowed = @actor.cast? || @actor.system_admin?
      raise NotAuthorized unless allowed
    end
  end
end
```

> これで Issue #53 の Done
>
> * `stream_session.id` → `ivs_stage_arn` を取得可能（DBに保存）
> * “配信中にStageが切り替わらない”（同一sessionは常に同じ arn）
>   を満たせます。

Closes #53 
Relates to #50 