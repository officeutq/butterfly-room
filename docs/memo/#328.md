OK、**一気にフル実装いきます（そのままコピペで進められる粒度）** 👍

---

# 🌿 ブランチ

```bash
git checkout -b feature/home-sort-by-last-online-at
```

---

# ① migration

```bash
docker compose exec app bin/rails g migration AddLastOnlineAtToBooths
```

```ruby
# db/migrate/xxxx_add_last_online_at_to_booths.rb
class AddLastOnlineAtToBooths < ActiveRecord::Migration[8.0]
  def change
    add_column :booths, :last_online_at, :datetime
    add_index :booths, :last_online_at
  end
end
```

```bash
docker compose exec app bin/rails db:migrate
```

---

## 修正版 `app/services/stream_sessions/status_service.rb`

```ruby
# frozen_string_literal: true

module StreamSessions
  class StatusService
    class Error < StandardError; end
    class NotAuthorized < Error; end
    class NoCurrentSession < Error; end
    class InvalidTransition < Error; end
    class AnotherBoothAlreadyLive < Error; end

    def initialize(booth:, actor:, to_status:)
      @booth = booth
      @actor = actor
      @to_status = to_status.to_sym
    end

    def call
      authorize!

      Booth.transaction do
        booth = Booth.lock.find(@booth.id)

        raise NoCurrentSession if booth.current_stream_session_id.nil?
        raise InvalidTransition, "to_status must be live or away" unless %i[live away].include?(@to_status)

        from = booth.status.to_sym

        if @to_status == :live && another_live_booth_exists?(booth)
          raise AnotherBoothAlreadyLive, "他のブースで配信中のため開始できません"
        end

        now = Time.current

        # Issue #78 遷移
        # standby -> live（配信開始後にサーバ側でliveへ）
        # live <-> away
        # 同一はno-op
        case [ from, @to_status ]
        when %i[standby live]
          booth.update!(status: :live, last_online_at: now)
        when %i[live away]
          booth.update!(status: :away, last_online_at: now)
        when %i[away live]
          booth.update!(status: :live, last_online_at: now)
        when [ @to_status, @to_status ]
          # no-op
        else
          raise InvalidTransition, "from #{from} to #{@to_status}"
        end

        booth
      end
    end

    private

    def authorize!
      raise NotAuthorized unless @actor.at_least?(:cast)
    end

    def another_live_booth_exists?(booth)
      Booth.active
           .joins(:current_stream_session)
           .where(stream_sessions: { started_by_cast_user_id: @actor.id })
           .where(status: %i[live away])
           .where.not(id: booth.id)
           .exists?
    end
  end
end
```

---

# ③ HomeController 修正（コア）

## 🧩 stores 側（完全置き換え）

```ruby
if @mode == "stores"
  @booths = Booth.none

  stores = Store.all

  stores = stores.where("stores.name ILIKE ?", q_like) if q_like.present?

  # --- online: MAX(started_at) ---
  online_started_at =
    Booth.active
         .where(status: %i[live away])
         .joins("INNER JOIN stream_sessions ss ON ss.id = booths.current_stream_session_id")
         .select("booths.store_id AS store_id, MAX(ss.started_at) AS max_started_at")
         .group("booths.store_id")

  # --- offline/standby: MAX(last_online_at) ---
  last_online =
    Booth.active
         .select("booths.store_id AS store_id, MAX(booths.last_online_at) AS max_last_online_at")
         .group("booths.store_id")

  stores =
    stores
      .joins("LEFT JOIN (#{online_started_at.to_sql}) online ON online.store_id = stores.id")
      .joins("LEFT JOIN (#{last_online.to_sql}) last_online ON last_online.store_id = stores.id")
      .order(
        Arel.sql("CASE WHEN online.max_started_at IS NOT NULL THEN 1 ELSE 0 END DESC"),
        Arel.sql("online.max_started_at DESC NULLS LAST"),
        Arel.sql("last_online.max_last_online_at DESC NULLS LAST"),
        id: :desc
      )
      .limit(30)

  if user_signed_in? && current_user.customer?
    banned_store_ids = StoreBan.where(customer_user_id: current_user.id).select(:store_id)
    stores = stores.where.not(id: banned_store_ids)
  end

  @stores = stores

  if user_signed_in?
    @favorite_store_ids =
      current_user.favorite_stores.where(store_id: @stores.select(:id)).pluck(:store_id).to_set
  else
    @favorite_store_ids = Set.new
  end

  @favorite_booth_ids = Set.new
  return
end
```

---

## 🧩 booths 側（置き換え）

```ruby
booths = Booth.active

booths =
  booths.joins(<<~SQL)
    LEFT JOIN stream_sessions current_ss
      ON current_ss.id = booths.current_stream_session_id
  SQL

booths = booths.where("booths.name ILIKE ?", q_like) if q_like.present?

online_order =
  "CASE WHEN booths.status IN (#{Booth.statuses[:live]}, #{Booth.statuses[:away]}) THEN 1 ELSE 0 END"

booths =
  booths
    .includes(
      :store,
      :current_stream_session,
      { booth_casts: :cast_user },
      thumbnail_image_attachment: :blob
    )
    .order(
      Arel.sql("#{online_order} DESC"),
      Arel.sql("current_ss.started_at DESC NULLS LAST"),
      Arel.sql("booths.last_online_at DESC NULLS LAST"),
      id: :desc
    )
    .limit(60)
```

---

# ④ テスト追加（最小セット）

## status_service_test に追加

```ruby
test "go_live updates last_online_at" do
  freeze_time do
    now = Time.current
    StreamSessions::StatusService.call(stream_session: @ss, transition: "go_live")
    assert_equal now.to_i, @booth.reload.last_online_at.to_i
  end
end

test "go_away updates last_online_at" do
  freeze_time do
    now = Time.current
    StreamSessions::StatusService.call(stream_session: @ss, transition: "go_away")
    assert_equal now.to_i, @booth.reload.last_online_at.to_i
  end
end

test "back updates last_online_at" do
  freeze_time do
    now = Time.current
    StreamSessions::StatusService.call(stream_session: @ss, transition: "back")
    assert_equal now.to_i, @booth.reload.last_online_at.to_i
  end
end
```

---

# ⑤ 動作チェック観点

これだけ見ればOK👇

* [ ] 配信中が最上位に来る
* [ ] 配信中同士は「開始が新しい順」
* [ ] offlineは「最後に配信した順」
* [ ] 一度も配信してないものは最後

---

# 🎯 完成形（設計の美しさ）

今回の変更で👇

* **リアルタイム性 → current_ss.started_at**
* **履歴 → last_online_at**

👉 完全に責務分離される

---

# 💬 コミットメッセージ（おまけ）

```text
Add last_online_at and improve home ranking logic (#xxx)
```

---

# 🚀 次にやるといい

もし次やるなら👇

👉 **インデックス最適化（store_id + last_online_at）**
👉 **ランキングキャッシュ化（将来的に）**

---

必要なら
👉 「差分形式（git diff）」も出せます 👍
