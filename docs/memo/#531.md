以下の方針で実装します。Issueの仕様と、貼ってもらった `DrinkItem` / schema / migration 状況に合わせています。  

変更対象は4点です。

1. `drink_items` に `icon_key` を追加する migration
2. `DrinkItem` に `ICON_OPTIONS` と validation 追加
3. モデルテスト追加
4. `app/assets/images/drink_icons/` に画像配置

---

## ブランチ名候補

* `feature/531-drink-item-icon-key`
* `feature/531-drink-icons-foundation`
* `feature/531-add-icon-key-to-drink-items`

---

## 実装手順

まず空の migration を作成します。

```bash
docker compose exec app bin/rails generate migration AddIconKeyToDrinkItems
```

その後、生成された migration ファイルを以下に置き換えてください。

### `db/migrate/XXXXXXXXXXXXXX_add_icon_key_to_drink_items.rb`

```rb
# frozen_string_literal: true

class AddIconKeyToDrinkItems < ActiveRecord::Migration[8.1]
  def change
    add_column :drink_items, :icon_key, :string, null: true
  end
end
```

---

## 1. `app/models/drink_item.rb`

全文差し替えです。

```rb
# frozen_string_literal: true

class DrinkItem < ApplicationRecord
  ICON_OPTIONS = {
    "special" => {
      label: "スペシャル",
      path: "drink_icons/drink_special.jpg"
    },
    "mug" => {
      label: "マグ",
      path: "drink_icons/drink_mug.jpg"
    },
    "cocktail" => {
      label: "カクテル",
      path: "drink_icons/drink_cocktail.jpg"
    },
    "microphone" => {
      label: "マイク",
      path: "drink_icons/drink_microphone.jpg"
    },
    "camera" => {
      label: "カメラ",
      path: "drink_icons/drink_camera.jpg"
    },
    "angel" => {
      label: "エンジェル",
      path: "drink_icons/drink_angel.jpg"
    },
    "champagne" => {
      label: "シャンパン",
      path: "drink_icons/drink_champagne.jpg"
    }
  }.freeze

  belongs_to :store

  validates :name, presence: true
  validates :price_points, numericality: { only_integer: true, greater_than: 0 }
  validates :position, numericality: { only_integer: true }
  validates :icon_key, inclusion: { in: ICON_OPTIONS.keys }, allow_blank: true

  scope :enabled_only, -> { where(enabled: true) }
  scope :ordered, -> { order(:position, :id) }
end
```

---

## 2. モデルテスト追加

`test/models/drink_item_test.rb` は貼られていなかったので、新規追加でよいです。

### `test/models/drink_item_test.rb`

```rb
# frozen_string_literal: true

require "test_helper"

class DrinkItemTest < ActiveSupport::TestCase
  setup do
    @store = Store.create!(name: "drink-item-test-store")
  end

  test "icon_key allows blank" do
    drink_item = DrinkItem.new(
      store: @store,
      name: "テストドリンク",
      price_points: 100,
      position: 0,
      enabled: true,
      icon_key: nil
    )

    assert drink_item.valid?
  end

  test "icon_key allows values defined in ICON_OPTIONS" do
    DrinkItem::ICON_OPTIONS.each_key do |icon_key|
      drink_item = DrinkItem.new(
        store: @store,
        name: "テストドリンク",
        price_points: 100,
        position: 0,
        enabled: true,
        icon_key: icon_key
      )

      assert drink_item.valid?, "#{icon_key} should be valid"
    end
  end

  test "icon_key rejects values not defined in ICON_OPTIONS" do
    drink_item = DrinkItem.new(
      store: @store,
      name: "テストドリンク",
      price_points: 100,
      position: 0,
      enabled: true,
      icon_key: "invalid_icon"
    )

    assert_not drink_item.valid?
    assert_includes drink_item.errors[:icon_key], "is not included in the list"
  end
end
```

---

## 3. 画像配置

以下を配置してください。

* `app/assets/images/drink_icons/drink_special.jpg`
* `app/assets/images/drink_icons/drink_mug.jpg`
* `app/assets/images/drink_icons/drink_cocktail.jpg`
* `app/assets/images/drink_icons/drink_microphone.jpg`
* `app/assets/images/drink_icons/drink_camera.jpg`
* `app/assets/images/drink_icons/drink_angel.jpg`
* `app/assets/images/drink_icons/drink_champagne.jpg`

今回は基盤だけなので、表示側はまだ触りません。

---

## 4. migration 実行

```bash
docker compose exec app bin/rails db:migrate
```

---

## 5. テスト実行

まずモデルテストだけ確認します。

```bash
docker compose exec web bin/rails test test/models/drink_item_test.rb
```

必要なら関連も軽く確認します。

```bash
docker compose exec web bin/rails test
```

---

## 補足

今回の実装は Issue の非対象に合わせて、あえて以下は触れていません。

* admin の選択UI
* strong params
* 表示 helper / presenter
* 一覧や配信画面での表示

次の段階では、この `ICON_OPTIONS` を使って選択UIや表示 helper を載せていく形で自然につながります。
