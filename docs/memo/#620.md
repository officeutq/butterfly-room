了解です。
この方針でそのまま進めて大丈夫です。

## ブランチ名

```bash
git checkout -b feature/620-filepond-verification
```

---

## 使うコマンド

### 1. FilePond 関連を npm 追加

```bash
docker compose run --rm app bash -lc "npm install filepond filepond-plugin-image-preview filepond-plugin-image-resize filepond-plugin-image-transform filepond-plugin-file-validate-type"
```

### 2. public 配置

```bash
docker compose run --rm app bash -lc "mkdir -p public/vendor/filepond && \
cp node_modules/filepond/dist/filepond.min.css public/vendor/filepond/ && \
cp node_modules/filepond/dist/filepond.min.js public/vendor/filepond/ && \
cp node_modules/filepond-plugin-image-preview/dist/filepond-plugin-image-preview.min.css public/vendor/filepond/ && \
cp node_modules/filepond-plugin-image-preview/dist/filepond-plugin-image-preview.min.js public/vendor/filepond/ && \
cp node_modules/filepond-plugin-image-resize/dist/filepond-plugin-image-resize.min.js public/vendor/filepond/ && \
cp node_modules/filepond-plugin-image-transform/dist/filepond-plugin-image-transform.min.js public/vendor/filepond/ && \
cp node_modules/filepond-plugin-file-validate-type/dist/filepond-plugin-file-validate-type.min.js public/vendor/filepond/"
```

### 3. マイグレーション生成

```bash
docker compose run --rm app bin/rails generate migration CreateFilepondTestUploads
```

### 4. マイグレーション実行

```bash
docker compose run --rm app bin/rails db:migrate
```

---

# Step 1. モデル追加

## `app/models/filepond_test_upload.rb`

```ruby
# frozen_string_literal: true

class FilepondTestUpload < ApplicationRecord
  ALLOWED_CONTENT_TYPES = %w[
    image/jpeg
    image/png
    image/webp
    image/heic
    image/heif
  ].freeze

  has_one_attached :image

  validates :title, length: { maximum: 100 }, allow_blank: true
  validate :image_presence
  validate :image_content_type

  private

  def image_presence
    errors.add(:image, "を選択してください") unless image.attached?
  end

  def image_content_type
    return unless image.attached?

    content_type = image.blob.content_type.to_s
    return if ALLOWED_CONTENT_TYPES.include?(content_type)

    errors.add(:image, "は jpg / png / webp / heic のみアップロードできます")
  end
end
```

---

# Step 2. マイグレーション

## `db/migrate/XXXXXXXXXXXXXX_create_filepond_test_uploads.rb`

生成されたファイルを全文置き換え

```ruby
# frozen_string_literal: true

class CreateFilepondTestUploads < ActiveRecord::Migration[8.0]
  def change
    create_table :filepond_test_uploads do |t|
      t.string :title

      t.timestamps
    end
  end
end
```

---

# Step 3. route 追加

## `config/routes.rb`

`if Rails.env.development?` の中をこうします。

```ruby
if Rails.env.development?
  namespace :dev do
    resource :banuba_verification, only: %i[show], controller: "banuba_verifications"
    resource :deepar_verification, only: %i[show], controller: "deepar_verifications"
    resource :filepond_verification, only: %i[show create], controller: "filepond_verifications"
  end
end
```

---

# Step 4. dev controller 追加

## `app/controllers/dev/filepond_verifications_controller.rb`

```ruby
# frozen_string_literal: true

module Dev
  class FilepondVerificationsController < ApplicationController
    before_action :ensure_development!
    before_action -> { require_at_least!(:system_admin) }

    def show
      @form_upload = FilepondTestUpload.new
      @upload = requested_upload || latest_upload
    end

    def create
      @form_upload = FilepondTestUpload.new(filepond_test_upload_params)

      if @form_upload.save
        redirect_to dev_filepond_verification_path(upload_id: @form_upload.id),
                    notice: "FilePond 検証画像を保存しました"
      else
        @upload = latest_upload
        render :show, status: :unprocessable_entity
      end
    end

    private

    def ensure_development!
      head :not_found unless Rails.env.development?
    end

    def filepond_test_upload_params
      params.require(:filepond_test_upload).permit(:title, :image)
    end

    def requested_upload
      return if params[:upload_id].blank?

      FilepondTestUpload.with_attached_image.find_by(id: params[:upload_id])
    end

    def latest_upload
      FilepondTestUpload.with_attached_image.order(created_at: :desc, id: :desc).first
    end
  end
end
```

---

# Step 5. view 追加

## `app/views/dev/filepond_verifications/show.html.erb`

```erb
<% content_for :title, "FilePond 検証" %>

<script src="/vendor/filepond/filepond.min.js" defer></script>
<script src="/vendor/filepond/filepond-plugin-file-validate-type.min.js" defer></script>
<script src="/vendor/filepond/filepond-plugin-image-preview.min.js" defer></script>
<script src="/vendor/filepond/filepond-plugin-image-resize.min.js" defer></script>
<script src="/vendor/filepond/filepond-plugin-image-transform.min.js" defer></script>

<div class="container py-4 filepond-verification-page">
  <div class="row g-4">
    <div class="col-12">
      <div class="card filepond-verification-card" data-bs-theme="light">
        <div class="card-body">
          <h1 class="h4 mb-3">FilePond 検証</h1>

          <p class="text-body-secondary mb-3">
            development 限定の検証ページです。FilePond による画像プレビュー、
            クライアント側縮小、JPEG 変換、Active Storage 保存を確認します。
          </p>

          <div class="small text-body-secondary">
            <div>URL: <code><%= dev_filepond_verification_path %></code></div>
            <div>最大サイズ: <code>1920x1080</code></div>
            <div>出力形式: <code>image/jpeg</code></div>
            <div>品質: <code>0.8</code></div>
          </div>
        </div>
      </div>
    </div>

    <div class="col-12 col-xl-7">
      <div
        class="card filepond-verification-card h-100"
        data-bs-theme="light"
        data-controller="filepond-verification">
        <div class="card-header">アップロード検証</div>

        <div class="card-body">
          <%= form_with model: @form_upload,
                        url: dev_filepond_verification_path,
                        local: true,
                        html: { multipart: true } do |f| %>
            <% if @form_upload.errors.any? %>
              <div class="alert alert-danger" role="alert">
                <ul class="mb-0">
                  <% @form_upload.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                  <% end %>
                </ul>
              </div>
            <% end %>

            <div class="mb-3">
              <%= f.label :title, "タイトル", class: "form-label" %>
              <%= f.text_field :title, class: "form-control", maxlength: 100 %>
            </div>

            <div class="mb-3">
              <%= f.label :image, "画像", class: "form-label" %>
              <%= f.file_field :image,
                               class: "form-control",
                               accept: ".jpg,.jpeg,.png,.webp,.heic,.heif",
                               data: { filepond_verification_target: "input" } %>
              <div class="form-text">
                jpg / png / webp / heic を選択できます。クライアント側で最大 1920x1080 に縮小し、
                JPEG に変換して送信します。
              </div>
            </div>

            <div class="d-flex gap-2">
              <%= f.submit "送信する", class: "btn btn-primary" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>

    <div class="col-12 col-xl-5">
      <div class="card filepond-verification-card h-100" data-bs-theme="light">
        <div class="card-header">保存結果</div>

        <div class="card-body">
          <% if @upload&.persisted? && @upload.image.attached? %>
            <div class="mb-3">
              <div class="fw-semibold mb-2">タイトル</div>
              <div><%= @upload.title.presence || "-" %></div>
            </div>

            <div class="mb-3">
              <div class="fw-semibold mb-2">サムネイル</div>
              <div class="filepond-verification-preview">
                <%= image_tag @upload.image, alt: "保存画像", class: "img-fluid rounded border" %>
              </div>
            </div>

            <div class="mb-0">
              <div class="fw-semibold mb-2">メタ情報</div>
              <dl class="row mb-0">
                <dt class="col-sm-4">content_type</dt>
                <dd class="col-sm-8"><code><%= @upload.image.blob.content_type %></code></dd>

                <dt class="col-sm-4">byte_size</dt>
                <dd class="col-sm-8">
                  <code><%= @upload.image.blob.byte_size %></code>
                  <span class="text-body-secondary">(<%= number_to_human_size(@upload.image.blob.byte_size) %>)</span>
                </dd>
              </dl>
            </div>
          <% else %>
            <p class="text-body-secondary mb-0">
              まだ保存された画像はありません。
            </p>
          <% end %>
        </div>
      </div>
    </div>

    <div class="col-12">
      <div class="card filepond-verification-card" data-bs-theme="light">
        <div class="card-header">確認項目</div>
        <div class="card-body">
          <ul class="mb-0">
            <li>画像選択時にプレビューが表示される</li>
            <li>選び直しができる</li>
            <li>縮小後にフォーム送信できる</li>
            <li>保存後画像が表示される</li>
            <li>content_type / byte_size を確認できる</li>
            <li>mini_magick を使わずに成立する</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>
```

---

# Step 6. Stimulus controller 追加

## `app/javascript/controllers/filepond_verification_controller.js`

```javascript
import { Controller } from "@hotwired/stimulus"

const MIME_TYPE_BY_EXTENSION = {
  jpg: "image/jpeg",
  jpeg: "image/jpeg",
  png: "image/png",
  webp: "image/webp",
  heic: "image/heic",
  heif: "image/heif",
}

export default class extends Controller {
  static targets = ["input"]

  connect() {
    this.initializeFilePond()
  }

  disconnect() {
    this.destroyFilePond()
  }

  initializeFilePond() {
    if (!this.hasInputTarget) return
    if (!window.FilePond) {
      console.error("[filepond-verification] FilePond is not loaded")
      return
    }

    this.registerPlugins()

    this.pond = window.FilePond.create(this.inputTarget, {
      storeAsFile: true,
      allowMultiple: false,
      allowReorder: false,
      allowProcess: false,
      allowRevert: false,
      allowImagePreview: true,
      allowImageResize: true,
      allowImageTransform: true,
      imageResizeTargetWidth: 1920,
      imageResizeTargetHeight: 1080,
      imageResizeMode: "contain",
      imageResizeUpscale: false,
      imageTransformOutputMimeType: "image/jpeg",
      imageTransformOutputQuality: 0.8,
      imageTransformCanvasBackgroundColor: "#ffffff",
      acceptedFileTypes: Object.values(MIME_TYPE_BY_EXTENSION),
      fileValidateTypeDetectType: (source, type) =>
        new Promise((resolve) => {
          if (type && type.length > 0) {
            resolve(type)
            return
          }

          const filename = source?.name?.toString() || ""
          const ext = filename.split(".").pop()?.toLowerCase()
          resolve(MIME_TYPE_BY_EXTENSION[ext] || type)
        }),
      labelIdle:
        '画像をドラッグ＆ドロップするか <span class="filepond--label-action">参照</span>',
      labelFileTypeNotAllowed: "この形式の画像はアップロードできません",
      fileValidateTypeLabelExpectedTypes:
        "jpg / png / webp / heic の画像を選択してください",
    })
  }

  destroyFilePond() {
    if (!this.pond) return

    this.pond.destroy()
    this.pond = null
  }

  registerPlugins() {
    if (window.__filepondVerificationPluginsRegistered) return

    const plugins = [
      window.FilePondPluginFileValidateType,
      window.FilePondPluginImagePreview,
      window.FilePondPluginImageResize,
      window.FilePondPluginImageTransform,
    ].filter(Boolean)

    plugins.forEach((plugin) => {
      window.FilePond.registerPlugin(plugin)
    })

    window.__filepondVerificationPluginsRegistered = true
  }
}
```

---

# Step 7. CSS 追加

## `app/assets/stylesheets/components/_filepond_verification.scss`

```scss
@import url("/vendor/filepond/filepond.min.css");
@import url("/vendor/filepond/filepond-plugin-image-preview.min.css");

.filepond-verification-page {
  .filepond-verification-card {
    border: 1px solid rgba(0, 0, 0, 0.08);
  }

  .filepond-verification-preview {
    max-width: 360px;
  }

  .filepond--root {
    margin-bottom: 0;
  }

  .filepond--panel-root {
    background-color: #f8f9fa;
  }
}
```

---

# Step 8. `application.scss` に読み込み追加

`app/assets/stylesheets/application.scss` の components 読み込み群に、これを追加してください。

```scss
@use "components/filepond_verification";
```

もしこのファイルが `@import` 形式なら、そちらに合わせて

```scss
@import "components/filepond_verification";
```

で追加してください。

---

# Step 9. 動作確認

## 起動

```bash
docker compose up
```

## 開くURL

```text
/dev/filepond_verification
```

---

# まず確認すること

1. system_admin でログインして開ける
2. 画像選択で FilePond プレビューが出る
3. 送信できる
4. 保存後画像が表示される
5. `content_type` が `image/jpeg` になっている
6. `byte_size` が確認できる

---

# 補足

今回の実装では、**既存の `NormalizedImageAttachment` を一切通しません**。
なので、この検証ページでは「mini_magick を使わずに Active Storage 保存が成立するか」を純粋に確認できます。既存の `User#avatar` などが concern 経由で mini_magick 前提になっている現状とは切り分けて検証できます。

必要なら次に、
**テストを書く場合の最小セット** までそのまま出します。
