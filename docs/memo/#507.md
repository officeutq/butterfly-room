いいですね、ここまで来たら **完全に壊れない設計にしておくのが正解** です 👍
そのまま貼り替えられる「完全版」を出します。

---

# 🎯 方針（最終形）

## 変更点

### ① MiniMagick失敗は例外にする（握りつぶさない）

→ setter 内で例外発生 → update 失敗へ

### ② controller で rescue して validation error に変換

→ ユーザーには edit 画面

### ③ S3 実在チェックも継続

---

# ① Concern 修正（完全版）

## ✅ `app/models/concerns/normalized_image_attachment.rb`

### 🔥 差し替え（重要）

```ruby
def normalize_image_attachment_attachable(attachable, max_width:, max_height:)
  content_type = extract_attachable_content_type(attachable)
  return attachable unless ALLOWED_CONTENT_TYPES.include?(content_type)

  image = MiniMagick::Image.open(extract_attachable_path(attachable))

  image.auto_orient
  image.resize("#{max_width}x#{max_height}>")

  tempfile = Tempfile.new(["normalized", ".jpg"], binmode: true)
  image.format("jpg")
  image.write(tempfile.path)
  tempfile.rewind

  {
    io: tempfile,
    filename: extract_attachable_filename(attachable),
    content_type: "image/jpeg"
  }

rescue MiniMagick::Error, MiniMagick::Invalid => e
  Rails.logger.error(
    "[NormalizedImageAttachment] normalize failed: " \
    "class=#{e.class} message=#{e.message} " \
    "filename=#{extract_attachable_filename(attachable)} " \
    "content_type=#{extract_attachable_content_type(attachable)}"
  )

  # 🔥 ここがポイント：握りつぶさず例外化
  raise StandardError, "画像の処理に失敗しました"
end
```

---

# ② Controller 修正（完全版）

## ✅ `app/controllers/admin/booths_controller.rb`

```ruby
def update
  begin
    if @booth.update(booth_params)
      # --- S3実在チェック ---
      if @booth.thumbnail_image.attached?
        blob = @booth.thumbnail_image.blob

        unless blob.service.exist?(blob.key)
          Rails.logger.error("[BoothThumbnail] upload missing blob_id=#{blob.id} key=#{blob.key}")

          @booth.thumbnail_image.purge
          @booth.errors.add(:thumbnail_image, "の保存に失敗しました。再度アップロードしてください")

          return render :edit, status: :unprocessable_entity
        end
      end
      # --- ここまで ---

      purge_attachment_if_requested(
        record: @booth,
        attachment_name: :thumbnail_image,
        remove_param_name: :remove_thumbnail_image
      )

      redirect_to admin_booths_path, notice: "ブースを更新しました"
    else
      render :edit, status: :unprocessable_entity
    end

  rescue => e
    Rails.logger.error("[BoothUpdate] #{e.class}: #{e.message}")

    @booth.errors.add(:thumbnail_image, "の処理に失敗しました。png / jpg / webp の画像で再度お試しください")
    render :edit, status: :unprocessable_entity
  end
end
```

---

## ✅ `app/controllers/cast/booths_controller.rb`

```ruby
def update
  begin
    if @booth.update(booth_params)
      # --- S3実在チェック ---
      if @booth.thumbnail_image.attached?
        blob = @booth.thumbnail_image.blob

        unless blob.service.exist?(blob.key)
          Rails.logger.error("[BoothThumbnail] upload missing blob_id=#{blob.id} key=#{blob.key}")

          @booth.thumbnail_image.purge
          @booth.errors.add(:thumbnail_image, "の保存に失敗しました。再度アップロードしてください")

          return render :edit, status: :unprocessable_entity
        end
      end
      # --- ここまで ---

      purge_attachment_if_requested(
        record: @booth,
        attachment_name: :thumbnail_image,
        remove_param_name: :remove_thumbnail_image
      )

      redirect_to cast_booths_path, notice: "ブースを更新しました"
    else
      render :edit, status: :unprocessable_entity
    end

  rescue => e
    Rails.logger.error("[BoothUpdate] #{e.class}: #{e.message}")

    @booth.errors.add(:thumbnail_image, "の処理に失敗しました。png / jpg / webp の画像で再度お試しください")
    render :edit, status: :unprocessable_entity
  end
end
```

---

# ③ accept（重要）

```erb
<%= f.file_field :thumbnail_image,
                 class: "form-control",
                 accept: "image/png,image/jpeg,image/webp" %>
```

---

# 🎯 この実装でどうなるか

## ✔ MiniMagick失敗

→ 例外発生
→ controller rescue
→ edit に戻る
→ ユーザー再アップロード

## ✔ S3アップロード失敗

→ exist? false
→ purge
→ edit に戻る

## ✔ 正常画像

→ 問題なく保存

---

# 💡 この設計の本質

👉 **「怪しい画像は絶対に通さない」**

今までは：

* 失敗しても通す（危険）

これから：

* 失敗したら止める（安全）

---

# 🚀 これで解決できるもの

* NoSuchKey
* 壊れたサムネ
* スマホ依存の謎挙動
* 再現しないバグ

---

# 次の一手（必要なら）

ここまでやると、次はこれができる👇

* HEIC → 自動JPEG変換
* フロントで事前チェック
* プレビュー表示

---

実装としてはかなりいいところまで来てます。
このレベルなら本番でも安心して回せます 👍
