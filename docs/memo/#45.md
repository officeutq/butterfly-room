# 実装方針（Phase1最小）

* `/admin/casts`：店舗に紐づく **cast の所属一覧**（`store_memberships.membership_role = cast`）
* 追加：`users.role = cast` から選んで `store_memberships` を作成
* 削除：その `store_memberships` を削除（**admin membership を消さない**ように cast のみ対象）

> URL は “casts” だけど、実体は **StoreMembership 管理**です（Phase1として最短）。

---

# 1) ルーティング追加

`config/routes.rb` の `namespace :admin do` に追加：

```ruby
namespace :admin do
  root "dashboard#show"

  resource :store, only: %i[show update]
  resources :booths, only: %i[index create update]
  resources :drink_items, only: %i[index create update destroy]
  resources :store_bans, only: %i[create destroy]
  get :cast_metrics, to: "metrics#cast"

  resources :casts, only: %i[index create destroy]
end
```

これで：

* `GET    /admin/casts`  → index
* `POST   /admin/casts`  → create
* `DELETE /admin/casts/:id` → destroy（※ :id は StoreMembership の id にします）

---

# 2) Controller 追加

`app/controllers/admin/casts_controller.rb`（新規）

```ruby
# frozen_string_literal: true

module Admin
  class CastsController < Admin::BaseController
    before_action :require_current_store!

    def index
      @cast_memberships =
        StoreMembership
          .includes(:user)
          .where(store_id: current_store.id, membership_role: :cast)
          .order(:id)

      existing_user_ids = @cast_memberships.map(&:user_id)

      @candidate_cast_users =
        User
          .where(role: User.roles[:cast])
          .where.not(id: existing_user_ids)
          .order(:id)

      @store_membership = StoreMembership.new
    end

    def create
      user_id = params.require(:store_membership).permit(:user_id)[:user_id]
      user = User.find(user_id)

      unless user.cast?
        redirect_to admin_casts_path, alert: "castユーザーのみ追加できます"
        return
      end

      StoreMembership.create!(
        store_id: current_store.id,
        user_id: user.id,
        membership_role: :cast
      )

      redirect_to admin_casts_path, notice: "キャストを登録しました"
    rescue ActiveRecord::RecordNotFound
      redirect_to admin_casts_path, alert: "ユーザーが見つかりません"
    rescue ActiveRecord::RecordInvalid => e
      redirect_to admin_casts_path, alert: e.record.errors.full_messages.join(", ")
    rescue ActiveRecord::RecordNotUnique
      redirect_to admin_casts_path, alert: "すでに登録されています"
    end

    def destroy
      membership =
        StoreMembership
          .where(store_id: current_store.id, membership_role: :cast)
          .find(params[:id])

      membership.destroy!

      redirect_to admin_casts_path, notice: "キャスト登録を解除しました"
    rescue ActiveRecord::RecordNotFound
      head :not_found
    end
  end
end
```

---

# 3) View 追加

`app/views/admin/casts/index.html.erb`（新規）

```erb
<h1>キャスト管理</h1>

<h2 class="h5 mt-4">登録済みキャスト</h2>

<table class="table table-sm">
  <thead>
    <tr>
      <th>キャスト</th>
      <th>Email</th>
      <th class="text-end">操作</th>
    </tr>
  </thead>
  <tbody>
    <% @cast_memberships.each do |m| %>
      <tr>
        <td><%= m.user.display_name.presence || "-" %></td>
        <td><%= m.user.email %></td>
        <td class="text-end">
          <%= button_to "削除",
                        admin_cast_path(m),
                        method: :delete,
                        data: { turbo_confirm: "このキャスト登録を解除しますか？" },
                        class: "btn btn-sm btn-outline-danger" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<h2 class="h5 mt-4">キャストを追加</h2>

<% if @candidate_cast_users.any? %>
  <%= form_with model: @store_membership, url: admin_casts_path, scope: :store_membership do |f| %>
    <div class="row g-2 align-items-end">
      <div class="col-auto">
        <%= f.label :user_id, "追加するキャスト", class: "form-label" %>
        <%= f.select :user_id,
                     options_from_collection_for_select(@candidate_cast_users, :id, :email),
                     {},
                     class: "form-select" %>
      </div>
      <div class="col-auto">
        <%= f.submit "追加", class: "btn btn-primary" %>
      </div>
    </div>
  <% end %>
<% else %>
  <p class="text-muted">追加できる cast ユーザーがいません。</p>
<% end %>
```

> select の表示はまず `email` でOK（後で `display_name || email` 表示にしたければ helper 化）。

---

# 4) 管理者トップ（Dashboard）に導線追加

`app/views/admin/dashboard/show.html.erb` に1行追加：

```erb
<li><%= link_to "キャスト管理", admin_casts_path %></li>
```

（「キャスト別数値一覧」の近くに置くのがおすすめ）

Closes #45 
Relates to #34

---

# 5) 動作確認（最短）

1. store_admin（または system_admin でも membership が admin で存在するユーザー）でログイン
2. `http://localhost:3000/admin`
3. 「キャスト管理」へ
4. castユーザーを追加 → `store_memberships` に `membership_role=cast` が増える
5. `/admin/cast_metrics` へ → 追加したキャストが **0pt/0時間でも表示**される

---

# よくあるハマりポイント（先に潰す）

* **system_admin でも current_store が取れないと 403**
  → 今の `current_store` 実装は “admin membership から store を決める” 方式なので、system_admin でも store_membership(admin) が無いと入れません（仕様としてOKならそのまま）。
* destroy の :id は **User.id ではなく StoreMembership.id**
  → これを間違えると消せなくなります（今回のコードは membership id 前提で統一してます）。
* 追加は unique index があるので、二重追加は `RecordNotUnique` になります（ハンドリング済み）。

