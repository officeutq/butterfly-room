ALL FILES PROVIDED とみなして進めます（デザインは後回し、不要なものは削除でOKの方針で実装を固めます）。

## (1) High-level implementation understanding（最終）

#158 は「store_admin が admin 画面の操作対象 Store を選択し、その選択をセッションで保持する」機能です。

* `session[:current_store_id]` を導入し、`current_store` は **session優先**で解決
* sessionが無い場合は **最初の admin membership の store** を返す（従来互換）
* sessionが改ざん/削除/脱退で不正になった場合は **毎回クリアして `/admin/stores` に誘導**
* `current_store` 未選択（nil）の場合、admin画面は 403 ではなく **`/admin/stores` に誘導**
* `/admin/stores`（一覧）と、「この店舗を開く」（セッション更新）を追加
* system_admin は引き続き全 store を扱える（今回は store_admin の UX を主対象）

※ #138 未実装のため、「招待承認後〜」は #158 の Done から削除（Issue側で文言更新）。

---

## (2) Branch name candidates

* `feature/158-admin-current-store-select`
* `feature/158-admin-stores-context`
* `feature/158-current-store-session`
* `feature/158-admin-store-switcher`

---

## (3) 実装タスク（順序付き）

1. **routes 追加**

* `GET /admin/stores`（一覧）
* `POST /admin/current_store`（選択確定：session更新）※名前は後述コードで統一

2. **Admin::BaseController を中核に改修**

* `current_store` を「session優先 + membership fallback」に
* sessionが不正ならクリア（存在しない / admin membership 無し）
* `require_current_store!` を「403ではなく /admin/stores 誘導」に変更
* `helper_method :current_store` は維持
* `before_action` の role guard は維持（store_admin / system_admin）

3. **/admin/stores を実装**

* controller: `Admin::StoresController#index`（一覧）
* controller: `Admin::CurrentStoresController#create`（選択 → session更新 → /admin へ）

  * セッション改ざん対策として、ここでも membership 所有チェック
* view: `app/views/admin/stores/index.html.erb`（最低限表示 + ボタン）

4. **既存 admin controllers の整合**

* `require_current_store!` を使っている controllers は、Base側の誘導型により UX要件を満たす
* `Admin::BoothsController` の独自 403 ガード（`require_current_store_for_booths!`）は廃止し、`before_action :require_current_store!` に統一
* `before_action :set_booth` の重複行を削除（ついでの安全修正）

5. **admin レイアウト表示**

* layoutに「現在の店舗名」表示（雑でOK）
* current_store nil のときは「店舗を選択」リンクを出す
* flash表示は既に layout にあるので、booths/index / drink_items/index の重複 flash は削除（不要なものは削除OK方針）

6. **Done文言更新案（Issue本文側）**

* 「招待承認後…」を削除し、代わりに「不正session時にクリアして一覧へ誘導」を明記

7. **テスト（最小）**

* 今のリポジトリにテストがどの程度あるか未提示なので、まずは request/integration で最低限：

  * store_admin が `/admin/stores` を見れる
  * membership のある store のみ選択できる
  * session改ざん（他store_id）で `/admin/stores` に誘導される
  * current_store 未選択で `/admin` などが `/admin/stores` に誘導される
    （テスト文化が薄ければ、最低限 1〜2本でも可）

---

## (4) そのままコピペで実装できるコード一式

以下は **差し替え/新規追加ファイル**です。

### 1) `config/routes.rb`（admin 部分のみ差し替え）

※あなたの routes 全体は長いので、`namespace :admin do ... end` ブロック内をこの形に更新してください（他は触らない）。

```ruby
# --- Store Admin ---
namespace :admin do
  root "dashboard#show"

  # current_store selection
  resources :stores, only: %i[index]
  resource :current_store, only: %i[create]

  resource :store, only: %i[show update]
  resources :booths, only: %i[index show new edit create update] do
    member do
      get :watch
      patch :archive
    end
  end
  resources :drink_items, only: %i[index create update destroy]
  resources :store_bans, only: %i[index create destroy]
  resources :casts, only: %i[index create destroy]
  get "/cast_metrics", to: "metrics#cast"
end
```

---

### 2) `app/controllers/admin/base_controller.rb`（全文差し替え）

```ruby
module Admin
  class BaseController < ApplicationController
    before_action -> { require_role!(:store_admin, :system_admin) }

    private

    def current_store
      return Store.first if current_user.system_admin?

      # 1) session 優先
      if session[:current_store_id].present?
        store = Store.find_by(id: session[:current_store_id])
        if store.present? && admin_membership_exists_for_store?(store.id)
          return store
        end

        # 改ざん/脱退/削除 への耐性：不正ならクリア
        session.delete(:current_store_id)
      end

      # 2) fallback: 従来通り「最初の admin membership」
      membership =
        StoreMembership
          .includes(:store)
          .where(user_id: current_user.id, membership_role: :admin)
          .order(:id)
          .first

      membership&.store
    end

    helper_method :current_store

    def require_current_store!
      return if current_user.system_admin?
      return if current_store.present?

      redirect_to admin_stores_path, alert: "操作対象の店舗を選択してください"
    end

    def admin_membership_exists_for_store?(store_id)
      StoreMembership.exists?(
        user_id: current_user.id,
        store_id: store_id,
        membership_role: StoreMembership.membership_roles[:admin]
      )
    end
  end
end
```

---

### 3) 新規: `app/controllers/admin/stores_controller.rb`

```ruby
# frozen_string_literal: true

module Admin
  class StoresController < Admin::BaseController
    def index
      @stores =
        Store
          .joins(:store_memberships)
          .where(store_memberships: { user_id: current_user.id, membership_role: StoreMembership.membership_roles[:admin] })
          .distinct
          .order(:id)

      # 現在選択中があれば view で判定できるように
      @current_store_id = session[:current_store_id]
    end
  end
end
```

---

### 4) 新規: `app/controllers/admin/current_stores_controller.rb`

```ruby
# frozen_string_literal: true

module Admin
  class CurrentStoresController < Admin::BaseController
    def create
      store_id = params.require(:store_id)

      # session 改ざん対策：必ず membership チェック
      unless StoreMembership.exists?(
        user_id: current_user.id,
        store_id: store_id,
        membership_role: StoreMembership.membership_roles[:admin]
      )
        session.delete(:current_store_id)
        redirect_to admin_stores_path, alert: "選択できない店舗です"
        return
      end

      session[:current_store_id] = store_id
      redirect_to admin_root_path, notice: "店舗を切り替えました"
    rescue ActionController::ParameterMissing
      redirect_to admin_stores_path, alert: "店舗を選択してください"
    end
  end
end
```

---

### 5) 新規: `app/views/admin/stores/index.html.erb`

（デザインは最低限。存在すればOK方針）

```erb
<h1>店舗を選択</h1>

<% if @stores.blank? %>
  <div class="alert alert-warning" role="alert">
    管理可能な店舗がありません。
  </div>
<% else %>
  <table class="table align-middle">
    <thead>
      <tr>
        <th>ID</th>
        <th>店舗名</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @stores.each do |store| %>
        <tr class="<%= "table-active" if store.id.to_s == @current_store_id.to_s %>">
          <td><%= store.id %></td>
          <td><%= store.name.presence || "(no name)" %></td>
          <td class="text-end">
            <%= button_to "この店舗を開く",
                  admin_current_store_path(store_id: store.id),
                  method: :post,
                  class: "btn btn-primary btn-sm" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>

<p class="mt-3">
  <%= link_to "管理者トップへ", admin_root_path, class: "btn btn-outline-secondary" %>
</p>
```

---

### 6) `app/controllers/admin/booths_controller.rb`（必要箇所の整理）

* `before_action :require_current_store_for_booths!` を廃止して `before_action :require_current_store!` に統一
* `before_action :set_booth` の重複を削除
* `require_current_store_for_booths!` メソッドを削除

**全文差し替え版**（そのまま置き換え可）：

```ruby
# frozen_string_literal: true

module Admin
  class BoothsController < Admin::BaseController
    before_action :require_current_store!
    before_action :set_booth, only: %i[show edit update watch archive]
    before_action :authorize_update!, only: %i[edit update]

    def index
      @include_archived = ActiveModel::Type::Boolean.new.cast(params[:archived])

      scope = current_store.booths
      scope = scope.active unless @include_archived

      @booths = scope.order(id: :desc)
    end

    def show
    end

    def new
      @booth = current_store.booths.new
      authorize_create!
    end

    def create
      @booth = current_store.booths.new(booth_create_params)
      authorize_create!

      Booth.transaction do
        @booth.save!
        Booths::ProvisionIvsStageService.new(booth: @booth).call!
      end

      redirect_to admin_booth_path(@booth), notice: "ブースを作成しました"
    rescue ActiveRecord::RecordInvalid
      render :new, status: :unprocessable_entity
    rescue Booths::ProvisionIvsStageService::StageProvisionFailed => e
      @booth.errors.add(:base, "IVS Stage の作成に失敗しました: #{e.message}")
      render :new, status: :unprocessable_entity
    end

    def edit
    end

    def update
      if remove_thumbnail_image?
        @booth.thumbnail_image.purge_later if @booth.thumbnail_image.attached?
      end

      if @booth.update(booth_params)
        redirect_to admin_booth_path(@booth), notice: "更新しました"
      else
        render :edit, status: :unprocessable_entity
      end
    end

    def watch
      policy = Authorization::BoothPolicy.new(current_user, @booth)
      head :forbidden and return unless policy.admin_operate?

      stream_session = StreamSession.find_by(id: @booth.current_stream_session_id)

      if stream_session.blank? || !stream_session.live?
        redirect_to admin_booth_path(@booth), alert: "配信中ではありません"
        return
      end

      redirect_to booth_path(@booth)
    end

    def archive
      policy = Authorization::BoothPolicy.new(current_user, @booth)
      head :forbidden and return unless policy.update?

      booth = Booth.lock.find(@booth.id)

      if !booth.offline? || booth.current_stream_session_id.present?
        redirect_to admin_booths_path,
                    alert: "ブース##{booth.id}（#{booth.name}）は配信中の可能性があるためアーカイブできません。先に配信を終了してください。"
        return
      end

      booth.update!(archived_at: Time.current)

      redirect_to admin_booths_path, notice: "ブースをアーカイブしました"
    end

    private

    def set_booth
      scope =
        if current_user.system_admin?
          Booth.all
        else
          current_store.booths
        end

      @booth = scope.find(params[:id])
    end

    def authorize_update!
      policy = Authorization::BoothPolicy.new(current_user, @booth)
      head :forbidden unless policy.update?
    end

    def authorize_create!
      policy = Authorization::BoothPolicy.new(current_user, @booth)
      head :forbidden unless policy.create?
    end

    def booth_create_params
      params.require(:booth).permit(:name, :description, :thumbnail_image)
    end

    def booth_params
      params.require(:booth).permit(:description, :thumbnail_image)
    end

    def remove_thumbnail_image?
      params.dig(:booth, :remove_thumbnail_image) == "1"
    end
  end
end
```

---

### 7) `app/views/layouts/application.html.erb`（店舗名表示を追加）

`<body>` 内、flash箱の直後あたりに「現在の店舗」を出すのが手軽です。system_admin は現状 `Store.first` なので表示も出ます。

差し込み（flash箱の下に追加）：

```erb
    <div class="container mt-3">
      <div id="flash_inner">
        <% if notice.present? %>
          <div class="alert alert-success" role="alert"><%= notice %></div>
        <% end %>

        <% if alert.present? %>
          <div class="alert alert-danger" role="alert"><%= alert %></div>
        <% end %>
      </div>
    </div>

    <%# admin: current_store 表示（最低限） %>
    <% if current_user.present? && (current_user.store_admin? || current_user.system_admin?) %>
      <div class="container mb-3">
        <div class="d-flex justify-content-between align-items-center">
          <div class="text-muted small">
            現在の店舗:
            <% if defined?(current_store) && current_store.present? %>
              <strong><%= current_store.name.presence || "(no name)" %></strong>
            <% else %>
              <span class="text-danger">未選択</span>
            <% end %>
          </div>

          <% if current_user.store_admin? %>
            <%= link_to "店舗を切り替える", admin_stores_path, class: "btn btn-sm btn-outline-secondary" %>
          <% end %>
        </div>
      </div>
    <% end %>

    <%= yield %>
```

---

### 8) 不要 flash の削除（任意だが推奨）

* `app/views/admin/booths/index.html.erb` の先頭にある flash 表示は、layout に箱があるので削除
* `app/views/admin/drink_items/index.html.erb` の `flash[:notice]` も削除

（デザイン後回し＆不要削除OK方針なので、ここは今回で消してOK）

---

## #158 の Done 文言（差し替え案：コピペ可）

「招待承認後…」を外して、代わりに改ざん耐性と誘導を明確にします。

```md
### Done

* [ ] `/admin/stores` が存在する
* [ ] 「この店舗を開く」で `session[:current_store_id]` がセットされ、以後の admin 画面で current_store が切り替わる
* [ ] `current_store` はセッション優先で解決され、未設定時は従来通り「最初の admin membership」の store にフォールバックする
* [ ] `session[:current_store_id]` が「存在しない store」または「admin権限のない store」を指す場合、セッション値がクリアされ `/admin/stores` に誘導される
* [ ] current_store 未解決（nil）の場合、admin各画面は 403 ではなく `/admin/stores` に誘導して選択を促す
* [ ] 自分が admin でない store は選択できない
```

---

## 実装時の注意（今回のコードに含めた/含めない）

* `resource :store` の routes は残しました（既存のダッシュボードリンクがあるため）。ただし controller が無いので、これは別Issueで整理が必要。
* system_admin の `current_store` は現状維持（`Store.first`）。必要なら system_admin 側にも store選択を導入するのは Phase2 的に別Issueが妥当。

---

必要なら、コミットメッセージ案・PR文（変更点/仕様変更/動作確認）もこのまま作れます。
