了解です。
最小差分で、Issue #584 の要件に沿う形で進める実装案をまとめます。

今回は **追加CSSなし** でいけます。
カード本体に `data-bs-theme="light"` を付け、Bootstrap utility と最小限の inline style で収めます。

---

## 実装方針

* `app/views/booths/_card.html.erb` を新規作成
* home / favorites/booths の一覧は、その partial を呼ぶだけにする
* `Booth` に `primary_cast_user` を追加して、View から `booth_casts.max_by` を消す
* `home_controller` の booths モードで `@favorite_store_ids` も持つ
* `favorites/booths_controller#index` でも `@favorite_store_ids` を持つ
* カードは `data-bs-theme="light"` にする
* `enter` 導線は、**現状 home の条件分岐をそのまま踏襲**する
* `button_home` partial の置換IDは既存維持

---

## 1. `app/views/booths/_card.html.erb` を新規作成

```erb
<% open_in_modal =
     user_signed_in? && (
       current_user.system_admin? ||
       (current_user.store_admin? && current_user.admin_of_store?(booth.store_id))
     ) %>
<% primary_cast = booth.primary_cast_user %>

<div class="card h-100 shadow-sm" data-bs-theme="light">
  <div class="position-relative">
    <%= button_to enter_booth_path(booth),
          method: :get,
          class: "w-100 p-0 border-0 bg-transparent text-start",
          form: {
            class: "d-block w-100 m-0",
            data: (
              (open_in_modal ? { turbo_frame: "modal" } : {})
                .merge(turbo_confirm: enter_booth_switch_confirm_message(booth))
            )
          } do %>
      <% if booth.thumbnail_image.attached? %>
        <%= image_tag booth.thumbnail_image,
              class: "card-img-top",
              style: "aspect-ratio: 16 / 9; object-fit: cover;" %>
      <% else %>
        <div class="bg-light d-flex align-items-center justify-content-center"
             style="aspect-ratio: 16 / 9;">
          <span class="text-muted small">no thumbnail</span>
        </div>
      <% end %>
    <% end %>

    <% if booth.live? || booth.away? %>
      <% stream_title = booth.current_stream_session&.title.to_s.strip %>
      <% if stream_title.present? %>
        <div class="position-absolute top-0 start-0 p-2 pe-5" style="max-width: 100%;">
          <span class="badge text-bg-dark d-inline-block text-truncate align-middle"
                style="max-width: 100%;">
            <%= stream_title %>
          </span>
        </div>
      <% end %>
    <% end %>

    <div class="position-absolute top-0 end-0 p-2">
      <%= booth_status_badge(booth) %>
    </div>
  </div>

  <div class="card-body d-flex flex-column gap-2">
    <div class="d-flex align-items-start justify-content-between gap-2">
      <div class="min-w-0 flex-grow-1">
        <div class="text-muted small">ブース名</div>
        <div class="fw-semibold text-break">
          <%= button_to booth.name,
                enter_booth_path(booth),
                method: :get,
                class: "btn btn-link p-0 text-decoration-none text-reset align-baseline text-start fw-semibold",
                form: {
                  class: "d-inline",
                  data: (
                    (open_in_modal ? { turbo_frame: "modal" } : {})
                      .merge(turbo_confirm: enter_booth_switch_confirm_message(booth))
                  )
                } %>
        </div>
      </div>

      <div class="flex-shrink-0">
        <%= render "favorites/booths/button_home", booth: booth, favorited: booth_favorited %>
      </div>
    </div>

    <div class="d-flex align-items-start justify-content-between gap-2">
      <div class="min-w-0 flex-grow-1">
        <div class="text-muted small">所属キャスト名</div>
        <div class="text-break">
          <% if primary_cast.present? %>
            <%= display_name_or_anonymous(primary_cast) %>
          <% else %>
            <span class="text-muted">（未設定）</span>
          <% end %>
        </div>
      </div>

      <div class="flex-shrink-0">
        <button type="button"
                class="viewer-favorite-btn"
                title="未実装"
                aria-label="所属キャストお気に入り（未実装）"
                disabled>
          <i class="bi bi-star" aria-hidden="true"></i>
        </button>
      </div>
    </div>

    <div class="d-flex align-items-start justify-content-between gap-2">
      <div class="min-w-0 flex-grow-1">
        <div class="text-muted small">所属店舗名</div>
        <div class="text-break">
          <%= booth.store&.name %>
        </div>
      </div>

      <div class="flex-shrink-0">
        <%= render "favorites/stores/button_home", store: booth.store, favorited: store_favorited %>
      </div>
    </div>
  </div>
</div>
```

---

## 2. `app/models/booth.rb` を修正

```ruby
class Booth < ApplicationRecord
  include NormalizedImageAttachment

  belongs_to :store
  belongs_to :current_stream_session, class_name: "StreamSession", optional: true

  has_many :stream_sessions, dependent: :restrict_with_error
  has_many :booth_casts, dependent: :restrict_with_error
  has_many :cast_users, through: :booth_casts, source: :cast_user
  has_many :favorite_booths, dependent: :destroy
  has_one_attached :thumbnail_image

  normalizes_image_attachment :thumbnail_image

  enum :status, { offline: 0, live: 1, away: 2, standby: 3 }

  scope :active, -> { where(archived_at: nil) }
  scope :archived, -> { where.not(archived_at: nil) }

  def archived?
    archived_at.present?
  end

  def primary_cast_user_id
    booth_casts.order(created_at: :desc, id: :desc).pick(:cast_user_id)
  end

  def primary_cast_user
    if booth_casts.loaded?
      booth_casts.max_by { |booth_cast| [ booth_cast.created_at, booth_cast.id ] }&.cast_user
    else
      booth_casts.includes(:cast_user).order(created_at: :desc, id: :desc).first&.cast_user
    end
  end
end
```

---

## 3. `app/controllers/home_controller.rb` を修正

```ruby
# frozen_string_literal: true

class HomeController < ApplicationController
  def show
    @mode = params[:mode].to_s
    @mode = "booths" unless %w[booths stores].include?(@mode)

    @q = params[:q].to_s.strip
    q_like =
      if @q.present?
        escaped = ActiveRecord::Base.sanitize_sql_like(@q)
        "%#{escaped}%"
      end

    if @mode == "stores"
      @booths = Booth.none

      stores = Store.all

      # store name で絞り込み
      stores = stores.where("stores.name ILIKE ?", q_like) if q_like.present?

      active_booth_counts =
        Booth.active
             .select("booths.store_id AS store_id, COUNT(*) AS active_booths_count")
             .group("booths.store_id")

      online_flags =
        Booth.active
             .where(status: %i[live away])
             .select("booths.store_id AS store_id, 1 AS online_priority")
             .group("booths.store_id")

      stores =
        stores
          .joins("LEFT JOIN (#{active_booth_counts.to_sql}) active_booth_counts ON active_booth_counts.store_id = stores.id")
          .joins("LEFT JOIN (#{online_flags.to_sql}) online_flags ON online_flags.store_id = stores.id")
          .order(
            Arel.sql("COALESCE(online_flags.online_priority, 0) DESC"),
            Arel.sql("COALESCE(active_booth_counts.active_booths_count, 0) DESC"),
            id: :desc
          )
          .limit(30)

      # customer はBAN店舗を除外（現状踏襲）
      if user_signed_in? && current_user.customer?
        banned_store_ids = StoreBan.where(customer_user_id: current_user.id).select(:store_id)
        stores = stores.where.not(id: banned_store_ids)
      end

      @stores = stores

      # favorites（表示対象だけ）
      if user_signed_in?
        @favorite_store_ids =
          current_user.favorite_stores.where(store_id: @stores.select(:id)).pluck(:store_id).to_set
      else
        @favorite_store_ids = Set.new
      end
      @favorite_booth_ids = Set.new

      return
    end

    # --- mode=booths ---
    @stores = Store.none

    booths = Booth.active

    # current_stream_session.started_at を order で使うため JOIN
    booths =
      booths.joins(<<~SQL)
        LEFT JOIN stream_sessions current_ss
          ON current_ss.id = booths.current_stream_session_id
      SQL

    # booth name で絞り込み（仕様どおり stores.name は対象外）
    booths = booths.where("booths.name ILIKE ?", q_like) if q_like.present?

    # online（live/away）優先
    online_order =
      "CASE WHEN booths.status IN (#{Booth.statuses[:live]}, #{Booth.statuses[:away]}) THEN 1 ELSE 0 END"

    booths =
      booths
        .includes(
          :store,
          :current_stream_session,
          { booth_casts: :cast_user },
          thumbnail_image_attachment: :blob
        )
        .order(
          Arel.sql("#{online_order} DESC"),
          Arel.sql("current_ss.started_at DESC NULLS LAST"),
          id: :desc
        )
        .limit(60)

    # customer はBAN店舗を除外（現状踏襲）
    if user_signed_in? && current_user.customer?
      banned_store_ids = StoreBan.where(customer_user_id: current_user.id).select(:store_id)
      booths = booths.where.not(store_id: banned_store_ids)
    end

    @booths = booths

    # favorites（表示対象だけ）
    if user_signed_in?
      @favorite_booth_ids =
        current_user.favorite_booths.where(booth_id: @booths.select(:id)).pluck(:booth_id).to_set

      @favorite_store_ids =
        current_user.favorite_stores.where(store_id: @booths.select(:store_id)).pluck(:store_id).to_set
    else
      @favorite_booth_ids = Set.new
      @favorite_store_ids = Set.new
    end
  end
end
```

---

## 4. `app/controllers/favorites/booths_controller.rb` を修正

```ruby
# frozen_string_literal: true

module Favorites
  class BoothsController < ApplicationController
    before_action -> { require_at_least!(:customer) }
    before_action :set_booth, only: %i[create destroy]

    def index
      scope =
        current_user
          .favorite_booths
          .joins(:booth)
          .merge(Booth.active)
          .includes(
            booth: [
              :store,
              :current_stream_session,
              { booth_casts: :cast_user },
              { thumbnail_image_attachment: :blob }
            ]
          )
          .order(created_at: :desc, id: :desc)

      # Home と同じ「可能な範囲で予防」：customer のみ BAN store を除外
      if current_user.customer?
        banned_store_ids = StoreBan.where(customer_user_id: current_user.id).select(:store_id)
        scope = scope.where.not(booths: { store_id: banned_store_ids })
      end

      @favorite_booths = scope

      @favorite_store_ids =
        current_user
          .favorite_stores
          .where(store_id: @favorite_booths.select("booths.store_id"))
          .pluck(:store_id)
          .to_set
    end

    def create
      current_user.favorite_booths.find_or_create_by!(booth: @booth)

      render_favorite_button(favorited: true)
    rescue ActiveRecord::RecordNotUnique
      # UNIQUE index で競合しても成功扱いに寄せる
      render_favorite_button(favorited: true)
    end

    def destroy
      current_user.favorite_booths.where(booth: @booth).destroy_all

      render_favorite_button(favorited: false)
    end

    private

    def set_booth
      @booth = Booth.active.find(params[:booth_id])
    end

    def render_favorite_button(favorited:)
      respond_to do |format|
        format.turbo_stream do
          streams = [
            turbo_stream.replace(
              "booth_favorite_button",
              partial: "favorites/booths/button_booth_show",
              formats: [ :html ],
              locals: { booth: @booth, favorited: favorited }
            ),
            turbo_stream.replace(
              "booth_#{@booth.id}_favorite_button",
              partial: "favorites/booths/button_home",
              formats: [ :html ],
              locals: { booth: @booth, favorited: favorited }
            )
          ]
          render turbo_stream: streams
        end

        format.html { redirect_back fallback_location: booth_path(@booth) }
      end
    end
  end
end
```

---

## 5. `app/views/home/show.html.erb` を修正

```erb
<% if user_signed_in? %>
  <%# --- Search (Input group / GET) --- %>
  <div class="mb-3">
    <%= form_with url: root_path, method: :get, scope: nil do |f| %>
      <%= f.hidden_field :mode, value: @mode %>

      <div class="input-group">
        <div class="btn-group">
          <button
            class="btn btn-outline-secondary dropdown-toggle"
            type="button"
            data-bs-toggle="dropdown"
            aria-expanded="false">
            <%= @mode == "stores" ? "店舗" : "ブース" %>
          </button>

          <ul class="dropdown-menu">
            <li>
              <%= link_to "ブース",
                    root_path(mode: "booths", q: @q.presence),
                    class: "dropdown-item" %>
            </li>
            <li>
              <%= link_to "店舗",
                    root_path(mode: "stores", q: @q.presence),
                    class: "dropdown-item" %>
            </li>
          </ul>
        </div>

        <%= f.text_field :q,
              value: @q,
              class: "form-control",
              placeholder: "ブース名 / ストア名で検索（部分一致）",
              autocomplete: "off" %>

        <button class="btn btn-primary" type="submit" aria-label="検索">
          <i class="bi bi-search"></i>
        </button>
      </div>
    <% end %>
  </div>

  <%# --- Results (cards) --- %>
  <% if @mode == "stores" %>
    <% if @stores.present? %>
      <div class="row row-cols-1 row-cols-md-3 g-3">
        <% @stores.each do |store| %>
          <% favorited = @favorite_store_ids.include?(store.id) %>

          <div class="col">
            <div class="card h-100">
              <div class="card-body">
                <div class="fw-semibold">
                  <%= link_to store.name, store_path(store), class: "text-decoration-none text-reset" %>
                </div>
              </div>

              <div class="card-footer bg-white border-0 pt-0">
                <%= render "favorites/stores/button_home", store: store, favorited: favorited %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="alert alert-secondary" role="alert">
        表示できる店舗がありません。
      </div>
    <% end %>
  <% else %>
    <% if @booths.present? %>
      <div class="row row-cols-1 row-cols-md-3 g-3">
        <% @booths.each do |booth| %>
          <div class="col">
            <%= render "booths/card",
                  booth: booth,
                  booth_favorited: @favorite_booth_ids.include?(booth.id),
                  store_favorited: @favorite_store_ids.include?(booth.store_id) %>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="alert alert-secondary" role="alert">
        表示できるブースがありません。
      </div>
    <% end %>
  <% end %>
<% else %>
  <div style="max-width: 720px;">
    <h1 class="mb-3">ようこそ</h1>
    <p class="text-muted">視聴するにはログイン、または新規アカウント作成を行ってください。</p>

    <div class="d-flex gap-2 mt-3">
      <%= link_to "ログイン", new_user_session_path, class: "btn btn-primary" %>
      <%= link_to "新規アカウント作成", sign_up_path, class: "btn btn-outline-primary" %>
    </div>
  </div>
<% end %>
```

---

## 6. `app/views/favorites/booths/index.html.erb` を修正

```erb
<div style="max-width: 960px;">
  <div class="d-flex justify-content-between align-items-start gap-3">
    <div>
      <h1 class="mb-1">お気に入りブース</h1>
      <p class="text-muted mb-0">新しい順</p>
    </div>

    <div class="d-flex gap-2">
      <%= link_to "店舗のお気に入り", favorites_stores_path, class: "btn btn-outline-secondary btn-sm" %>
    </div>
  </div>

  <hr class="my-4">

  <% if @favorite_booths.blank? %>
    <div class="alert alert-secondary" role="alert">
      お気に入りブースはありません。
    </div>
  <% else %>
    <div class="row row-cols-1 row-cols-md-3 g-3">
      <% @favorite_booths.each do |favorite| %>
        <% booth = favorite.booth %>

        <div class="col">
          <%= render "booths/card",
                booth: booth,
                booth_favorited: true,
                store_favorited: @favorite_store_ids.include?(booth.store_id) %>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
```

---

## 補足

今回の実装では、カード内の店舗お気に入りボタンも `favorites/stores/button_home` をそのまま使っているので、
**Turbo Stream 置換IDは既存のまま**です。

また、所属キャストのプレースホルダは **未実装の見た目だけ** にしてあり、クリック動作は持たせていません。

---

## 確認ポイント

実装後はこの順で確認すると早いです。

1. home の booth 一覧が新カードで出る
2. favorites/booths も同じカード見た目になる
3. サムネ右上に配信状態バッジが出る
4. live / away かつタイトルありのときだけ左上タイトルが出る
5. ブース名横のお気に入りが動く
6. 店舗行のお気に入りが動く
7. お気に入り解除後も Turbo Stream の置換が崩れない
8. admin / system_admin の enter モーダル導線が壊れていない

必要なら次に、**この実装に合わせた最小テスト観点**もまとめます。

---
---
了解です。
`_booths_card.scss` を新設する前提で、**修正版一式**を貼ります。Issueの「ラベル＋値を同じ行」に合わせてあります。

---

## 1. `app/views/booths/_card.html.erb`

```erb
<%# locals: booth:, booth_favorited:, store_favorited: %>
<% open_in_modal =
     user_signed_in? && (
       current_user.system_admin? ||
       (current_user.store_admin? && current_user.admin_of_store?(booth.store_id))
     ) %>
<% primary_cast = booth.primary_cast_user %>
<% stream_title = booth.current_stream_session&.title.to_s.strip %>

<div class="card h-100 shadow-sm booths-card" data-bs-theme="light">
  <div class="booths-card-thumbnail-wrap">
    <%= button_to enter_booth_path(booth),
          method: :get,
          class: "w-100 p-0 border-0 bg-transparent text-start booths-card-thumbnail-button",
          form: {
            class: "d-block w-100 m-0",
            data: (
              (open_in_modal ? { turbo_frame: "modal" } : {})
                .merge(turbo_confirm: enter_booth_switch_confirm_message(booth))
            )
          } do %>
      <% if booth.thumbnail_image.attached? %>
        <%= image_tag booth.thumbnail_image, class: "card-img-top booths-card-thumbnail" %>
      <% else %>
        <div class="booths-card-thumbnail booths-card-thumbnail-placeholder">
          <span class="text-muted small">no thumbnail</span>
        </div>
      <% end %>
    <% end %>

    <% if (booth.live? || booth.away?) && stream_title.present? %>
      <div class="booths-card-overlay-title-wrap">
        <span class="badge text-bg-dark booths-card-overlay-title"><%= stream_title %></span>
      </div>
    <% end %>

    <div class="booths-card-overlay-status">
      <%= booth_status_badge(booth) %>
    </div>
  </div>

  <div class="card-body d-flex flex-column gap-2">
    <div class="booths-card-row">
      <div class="booths-card-row-main">
        <div class="booths-card-label">ブース名</div>
        <div class="booths-card-value booths-card-value-emphasis">
          <%= button_to booth.name,
                enter_booth_path(booth),
                method: :get,
                class: "btn btn-link p-0 text-decoration-none text-reset align-baseline text-start fw-semibold booths-card-name-button",
                form: {
                  class: "d-inline",
                  data: (
                    (open_in_modal ? { turbo_frame: "modal" } : {})
                      .merge(turbo_confirm: enter_booth_switch_confirm_message(booth))
                  )
                } %>
        </div>
      </div>

      <div class="booths-card-action">
        <%= render "favorites/booths/button_home", booth: booth, favorited: booth_favorited %>
      </div>
    </div>

    <div class="booths-card-row">
      <div class="booths-card-row-main">
        <div class="booths-card-label">所属キャスト名</div>
        <div class="booths-card-value">
          <% if primary_cast.present? %>
            <%= display_name_or_anonymous(primary_cast) %>
          <% else %>
            <span class="text-muted">（未設定）</span>
          <% end %>
        </div>
      </div>

      <div class="booths-card-action">
        <button type="button"
                class="viewer-favorite-btn booths-card-placeholder-btn"
                title="未実装"
                aria-label="所属キャストお気に入り（未実装）"
                disabled>
          <i class="bi bi-star" aria-hidden="true"></i>
        </button>
      </div>
    </div>

    <div class="booths-card-row">
      <div class="booths-card-row-main">
        <div class="booths-card-label">所属店舗名</div>
        <div class="booths-card-value">
          <%= booth.store&.name %>
        </div>
      </div>

      <div class="booths-card-action">
        <%= render "favorites/stores/button_home", store: booth.store, favorited: store_favorited %>
      </div>
    </div>
  </div>
</div>
```

---

## 2. `app/assets/stylesheets/components/_booths_card.scss` を新規作成

```scss
.booths-card {
  overflow: hidden;
}

.booths-card[data-bs-theme="light"] {
  color: var(--bs-body-color);
}

.booths-card-thumbnail-wrap {
  position: relative;
}

.booths-card-thumbnail-button {
  display: block;
}

.booths-card-thumbnail {
  display: block;
  aspect-ratio: 16 / 9;
  object-fit: cover;
}

.booths-card-thumbnail-placeholder {
  background: var(--bs-light);
  display: flex;
  align-items: center;
  justify-content: center;
}

.booths-card-overlay-title-wrap {
  position: absolute;
  top: 0;
  left: 0;
  padding: 0.5rem;
  max-width: 100%;
}

.booths-card-overlay-title {
  display: inline-block;
  max-width: 100%;
  vertical-align: middle;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.booths-card-overlay-status {
  position: absolute;
  top: 0;
  right: 0;
  padding: 0.5rem;
}

.booths-card-row {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 0.5rem;
}

.booths-card-row-main {
  min-width: 0;
  flex: 1 1 auto;
  display: flex;
  align-items: flex-start;
  gap: 0.5rem;
}

.booths-card-label {
  flex: 0 0 auto;
  color: var(--bs-secondary-color);
  font-size: 0.875rem;
  line-height: 1.5;
  white-space: nowrap;
}

.booths-card-value {
  min-width: 0;
  flex: 1 1 auto;
  line-height: 1.5;
  word-break: break-word;
}

.booths-card-value-emphasis {
  font-weight: 600;
}

.booths-card-action {
  flex: 0 0 auto;
}

.booths-card-name-button {
  word-break: break-word;
}

.booths-card-placeholder-btn[disabled] {
  opacity: 0.65;
  cursor: default;
}
```

---

## 3. `app/assets/stylesheets/application.scss`

`components` の読み込み群に、これを追加してください。

```scss
@use "components/booths_card";
```

すでに `@use "components/..."` 形式で並んでいるなら、その並びに1行追加で大丈夫です。

---

## 4. 補足

この形だと、

* ラベルと値は同じ行
* 値が長ければ自然に折り返し
* お気に入りボタンは右固定
* サムネイル上のタイトルと状態バッジは SCSS 管理

になります。

もし次に詰めるなら、
**配信タイトルを1行省略ではなく2行まで表示したい** かどうか、そこだけ調整余地があります。
