進め方はこれでよさそうです。

# 実装方針

#423 は **Banuba Studio 側で作った `beauty_base.zip` を Rails 検証画面で読める状態にし、その使い方とパラメータ定義を repo に残す** Issue として切るのが自然です。

今回の変更は大きく3つです。

1. dev 検証画面の既定 effect を `glasses_RayBan4165_Dark.zip` から `beauty_base.zip` に差し替える
2. `public/banuba/effects/beauty_base.zip` を配置する
3. `docs/08_画面加工設計.md` を追加して、export 手順・パラメータ一覧・運用メモを残す

現在の実装は、`Player.create(...)` で SDK 本体を初期化し、`Module(face_tracker.zip)` を追加し、`Effect(zip)` を適用する構成です。これは Banuba の Web SDK ドキュメントにある基本的な使い方と整合しています。([docs.banuba.com][1])
また、顔追従系の effect では `face_tracker` 系 module を含める前提が自然で、Banuba のパッケージ説明でも face tracking は別モジュール扱いです。([docs.banuba.com][2])

# beauty_base.zip の作成方針

Studio 側では、**1本の Beauty base effect に必要項目をまとめる** 方針でよいです。
Issue 本文どおり、対象は次です。

* 美肌補正
* 輪郭
* 目
* 鼻
* 口

初期値は弱めにして、後続 UI で ON/OFF とスライダーを当てる前提にします。
つまり Studio 側では「見た目を完成させる」より、**後から調整できる土台 effect を作る** のが主目的です。

# Banuba Studio 側の実務手順

ここは Banuba Studio の画面差分がバージョンで多少変わる可能性があるので、**UI ラベルは環境差がありうる前提**で、実務上の流れを整理します。

1. Banuba Studio で新規 effect を作成する
2. Beauty / face reshape 系の機能を追加する
3. 下記の調整項目を持つように構成する

   * `skin_smoothing`
   * `face_contour`
   * `eye_enlarge`
   * `nose_reshape`
   * `lip_reshape`
4. 初期値を弱めに設定する
5. Web 向けに export する
6. 出力された zip を `beauty_base.zip` に統一して repo へ置く

Banuba の公開ドキュメントでは、Web 側では effect を zip としてロードする運用が前提です。([docs.banuba.com][1])

初期値のおすすめは、まず次くらいの「弱めスタート」です。

* `skin_smoothing`: 0.20
* `face_contour`: 0.10
* `eye_enlarge`: 0.08
* `nose_reshape`: 0.06
* `lip_reshape`: 0.05

これは **Banuba 公式推奨値ではなく、Butterfly Room 用の初期仮値** です。
大事なのは「やりすぎ感を避けること」なので、最初はかなり弱めで十分です。

想定レンジはこんな形でよいです。

* `skin_smoothing`: 0.0 - 0.6
* `face_contour`: 0.0 - 0.3
* `eye_enlarge`: 0.0 - 0.25
* `nose_reshape`: 0.0 - 0.2
* `lip_reshape`: 0.0 - 0.2

OFF 相当はすべて `0.0` で統一する方針にしておくと、後続 UI 実装が楽です。

# 変更対象

今回の変更対象はこれで十分です。

* `app/controllers/dev/banuba_verifications_controller.rb`
* `app/views/dev/banuba_verifications/show.html.erb`
* `docs/08_画面加工設計.md`
* `public/banuba/effects/beauty_base.zip` ← 実ファイル追加

`banuba_verification_controller.js` は、今の構成なら **必須変更ではありません**。
単一 effect zip を適用するだけなので、そのまま使えます。

---

# 貼り付け用コード

## 1. `app/controllers/dev/banuba_verifications_controller.rb`

```ruby
# frozen_string_literal: true

module Dev
  class BanubaVerificationsController < ApplicationController
    before_action :ensure_development!
    before_action -> { require_at_least!(:system_admin) }

    def show
      @banuba_client_token = ENV["BANUBA_CLIENT_TOKEN"].to_s
      @banuba_sdk_base_url = "/banuba/sdk"
      @banuba_face_tracker_url = "/banuba/modules/face_tracker.zip"
      @banuba_effect_url = "/banuba/effects/beauty_base.zip"
      @banuba_effect_name = "beauty_base.zip"
    end

    private

    def ensure_development!
      head :not_found unless Rails.env.development?
    end
  end
end
```

## 2. `app/views/dev/banuba_verifications/show.html.erb`

```erb
<% content_for :title, "Banuba 検証" %>

<div class="container py-4 banuba-page">
  <div class="row g-4">
    <div class="col-12">
      <div class="card banuba-card" data-bs-theme="light">
        <div class="card-body">
          <h1 class="h4 mb-3">Banuba Web SDK 検証</h1>

          <p class="text-body-secondary mb-3">
            development 限定の検証ページです。Rails 上で Banuba が起動し、
            カメラ映像と Butterfly Room 向け Beauty effect が適用できるかを確認します。
          </p>

          <div class="small text-body-secondary">
            <div>SDK: <code><%= @banuba_sdk_base_url %></code></div>
            <div>Face tracker: <code><%= @banuba_face_tracker_url %></code></div>
            <div>Effect: <code><%= @banuba_effect_url %></code></div>
          </div>

          <% if @banuba_client_token.blank? %>
            <div class="alert alert-danger mt-3 mb-0" role="alert">
              BANUBA_CLIENT_TOKEN が未設定です。.env を確認してください。
            </div>
          <% end %>
        </div>
      </div>
    </div>

    <div class="col-12 col-xl-8">
      <div
        class="card banuba-card h-100"
        data-bs-theme="light"
        data-controller="banuba-verification"
        data-banuba-verification-client-token-value="<%= @banuba_client_token %>"
        data-banuba-verification-sdk-base-url-value="<%= @banuba_sdk_base_url %>"
        data-banuba-verification-face-tracker-url-value="<%= @banuba_face_tracker_url %>"
        data-banuba-verification-effect-url-value="<%= @banuba_effect_url %>"
        data-banuba-verification-effect-name-value="<%= @banuba_effect_name %>"
      >
        <div class="card-header d-flex justify-content-between align-items-center">
          <span>プレビュー</span>
          <span class="badge text-bg-secondary" data-banuba-verification-target="state">idle</span>
        </div>

        <div class="card-body">
          <div class="banuba-preview-wrap">
            <div class="banuba-preview-surface" data-banuba-verification-target="surface"></div>
          </div>

          <div class="banuba-error mt-3" data-banuba-verification-target="error"></div>

          <div class="d-flex flex-wrap gap-2 mt-3">
            <button
              type="button"
              class="btn btn-primary"
              data-action="banuba-verification#start"
              data-banuba-verification-target="startBtn">
              起動
            </button>

            <button
              type="button"
              class="btn btn-outline-secondary d-none"
              data-action="banuba-verification#stop"
              data-banuba-verification-target="stopBtn">
              停止
            </button>

            <button
              type="button"
              class="btn btn-outline-primary d-none"
              data-action="banuba-verification#reapplyEffect"
              data-banuba-verification-target="effectBtn">
              effect 再適用
            </button>
          </div>
        </div>
      </div>
    </div>

    <div class="col-12 col-xl-4">
      <div class="card banuba-card h-100" data-bs-theme="light">
        <div class="card-header">確認メモ</div>
        <div class="card-body">
          <div class="mb-3">
            <div class="fw-semibold">起動時に確認すること</div>
            <ul class="mb-0">
              <li>カメラ権限が取れる</li>
              <li>映像が表示される</li>
              <li>Beauty effect 適用で見た目が変わる</li>
              <li>顔移動に追従する</li>
            </ul>
          </div>

          <div class="mb-3">
            <div class="fw-semibold">Beauty 確認項目</div>
            <ul class="mb-0">
              <li>美肌が過度に強すぎない</li>
              <li>輪郭補正が不自然に崩れない</li>
              <li>目の拡大が違和感なく追従する</li>
              <li>鼻・口補正が極端に破綻しない</li>
            </ul>
          </div>

          <div class="mb-3">
            <div class="fw-semibold">安定性</div>
            <ul class="mb-0">
              <li>数分放置で console error が出ない</li>
              <li>停止 → 起動 を数回繰り返して壊れない</li>
              <li>Turbo 遷移後にカメラが残らない</li>
            </ul>
          </div>

          <div class="mb-0">
            <div class="fw-semibold">URL</div>
            <code><%= dev_banuba_verification_path %></code>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
```

## 3. `docs/08_画面加工設計.md`

```md
# 08_画面加工設計

## 概要

Butterfly Room の cast/live における配信映像加工のため、
Banuba Web SDK を用いた Beauty effect 資材と、その運用方針を整理する。

本ドキュメントは以下を目的とする。

- Butterfly Room 用 Beauty effect の管理
- Web 用 export 資材の配置ルール整理
- 後続 UI 制御 Issue へ引き渡すパラメータ定義の明文化
- dev 検証画面での確認観点の記録

---

## スコープ

本ドキュメントで扱う対象は以下。

- Banuba Studio で作成する Butterfly Room 用 Beauty effect
- Web export した effect zip
- Rails 検証画面での読込確認
- 後続 UI 実装用のパラメータ一覧

以下は本ドキュメントの直接対象外とする。

- cast/live 画面への本組み込み
- ON/OFF トグル UI 実装
- スライダー UI 実装
- 加工値の永続化
- IVS publish への反映
- 最終 UX 調整

---

## 構成方針

### 1. effect は 1 本の Beauty base にまとめる

Phase1 では複数の effect zip を切り替える方式ではなく、
1 本の `beauty_base.zip` に必要な Beauty / face reshape 系機能をまとめる。

理由:

- UI 側で ON/OFF / スライダー制御しやすい
- effect 資材の管理が単純
- cast/live 組み込み時の扱いが楽
- 後続実装で effect 切り替えロジックを避けられる

### 2. 初期値は弱めにする

初期状態は「やりすぎ感」を避けるため、弱め〜中弱程度を基本とする。

### 3. OFF 相当は 0.0 に統一する

後続 UI 制御を簡単にするため、
各パラメータの無効状態は 0.0 で統一する。

---

## 対象加工項目

Butterfly Room で扱う Beauty 項目は以下。

- 美肌補正
- 顔形状補正
  - 輪郭
  - 目
  - 鼻
  - 口

---

## パラメータ定義

### 方針

パラメータ名は、後続 UI 実装時に人間が意味を理解しやすい名前で管理する。

### 一覧

- `skin_smoothing`
  - 意味: 美肌補正
  - デフォルト値: `0.20`
  - 推奨レンジ: `0.0..0.6`
  - OFF 相当: `0.0`

- `face_contour`
  - 意味: 輪郭補正
  - デフォルト値: `0.10`
  - 推奨レンジ: `0.0..0.3`
  - OFF 相当: `0.0`

- `eye_enlarge`
  - 意味: 目の補正
  - デフォルト値: `0.08`
  - 推奨レンジ: `0.0..0.25`
  - OFF 相当: `0.0`

- `nose_reshape`
  - 意味: 鼻の補正
  - デフォルト値: `0.06`
  - 推奨レンジ: `0.0..0.2`
  - OFF 相当: `0.0`

- `lip_reshape`
  - 意味: 口の補正
  - デフォルト値: `0.05`
  - 推奨レンジ: `0.0..0.2`
  - OFF 相当: `0.0`

---

## 成果物

### Web 用 effect zip

想定成果物:

- `public/banuba/effects/beauty_base.zip`

### 既存 module / sdk

以下は既存資材をそのまま利用する。

- `public/banuba/modules/face_tracker.zip`
- `public/banuba/sdk/BanubaSDK.browser.esm.js`
- `public/banuba/sdk/BanubaSDK.data`
- `public/banuba/sdk/BanubaSDK.wasm`
- `public/banuba/sdk/BanubaSDK.simd.wasm`

---

## 配置ルール

### effect zip

Web から読み込む effect zip は以下に配置する。

- `public/banuba/effects/`

Butterfly Room 用 Beauty base の標準ファイル名は以下とする。

- `beauty_base.zip`

---

## Banuba Studio での作成手順

### 1. 新規 Beauty base を作成する

Banuba Studio で Butterfly Room 用の Beauty effect を新規作成する。

### 2. 加工項目を追加する

最低限、以下を含める。

- 美肌
- 輪郭
- 目
- 鼻
- 口

### 3. 初期値を設定する

初期値は弱めに設定する。

- skin smoothing: 0.20
- face contour: 0.10
- eye enlarge: 0.08
- nose reshape: 0.06
- lip reshape: 0.05

### 4. 極端値を確認する

少なくとも以下を確認する。

- デフォルト値で違和感が強すぎない
- 最小値 0.0 で補正が外れる
- 最大寄りで顔形状が破綻しない
- 顔移動時に追従が外れない

### 5. Web 用に export する

Banuba Studio から Web 用 effect として export し、
出力 zip を `beauty_base.zip` として扱う。

### 6. Rails へ配置する

以下へ配置する。

- `public/banuba/effects/beauty_base.zip`

---

## dev 検証画面での確認観点

### 起動確認

- カメラ権限が取得できる
- 映像が表示される
- `beauty_base.zip` が読み込まれる
- 顔追従が継続して動作する

### Beauty 確認

- 美肌が強すぎない
- 輪郭補正が不自然でない
- 目の補正が違和感なく追従する
- 鼻補正が破綻しない
- 口補正が破綻しない

### 安定性確認

- 数分放置でも console error が出ない
- 停止 → 起動 を繰り返しても壊れない
- Turbo 遷移後にカメラが残らない

---

## 注意点

- 本ドキュメントのデフォルト値は暫定値である
- 最終 UX 調整は別 Issue で行う
- cast/live 本組み込み時は、Banuba -> canvas -> MediaStream -> IVS publish の流れで接続する
- ON/OFF トグル、スライダー UI、値の永続化は別 Issue で扱う
```

---

# beauty_base.zip 配置

次のファイルを repo に追加してください。

* `public/banuba/effects/beauty_base.zip`

Studio 側で export した zip のファイル名が別名なら、repo に置くときに `beauty_base.zip` に統一でよいです。

# 実装後の確認

確認ポイントはこれです。

* `/dev/banuba_verification` を開く
* 起動を押す
* `beauty_base.zip` が適用される
* 顔追従が動く
* 数分放置でエラーが出ない
* 停止 → 起動 を数回繰り返しても壊れない

# 補足

もし次に一歩だけ進めるなら、`show.html.erb` の右カラムに **現在の暫定パラメータ値** を表示しておくのもありです。
ただ、Issue #423 の Done には必須ではないので、今回は入れなくて十分です。

必要なら次に、
**コミットメッセージ 1行** と **PR文（Closes #423入り）** をそのまま出します。

[1]: https://docs.banuba.com/far-sdk/generated/typedoc/index.html?utm_source=chatgpt.com "Banuba WebAR JavaScript API Reference"
[2]: https://docs.banuba.com/far-sdk/tutorials/development/installation?utm_source=chatgpt.com "Adding Banuba SDK to your project"

---
---

こうやります。
今の `banuba_verification_controller.js` はこれになっていますよね。

```javascript
await player.addModule(new Module(this.faceTrackerUrlValue))
```

これを、**複数 module をまとめて追加**する形に変えます。

まず controller に `eyesUrl` と `lipsUrl` を持たせます。
`skin` も入れるなら一緒に足しておくのがおすすめです。

## 変更後 `app/javascript/controllers/banuba_verification_controller.js`

```javascript
import { Controller } from "@hotwired/stimulus"
import { Dom, Effect, Module, Player, Webcam } from "banuba-web-sdk"

export default class extends Controller {
  static targets = ["surface", "error", "state", "startBtn", "stopBtn", "effectBtn"]

  static values = {
    clientToken: String,
    sdkBaseUrl: String,
    faceTrackerUrl: String,
    eyesUrl: String,
    lipsUrl: String,
    skinUrl: String,
    effectUrl: String,
    effectName: String,
  }

  connect() {
    this._player = null
    this._started = false

    this._beforeCache = () => this.stop()
    document.addEventListener("turbo:before-cache", this._beforeCache)

    this._setState("idle")
    this._syncUI()
  }

  disconnect() {
    document.removeEventListener("turbo:before-cache", this._beforeCache)
    this.stop()
  }

  async start() {
    if (this._started) return

    if (!this.clientTokenValue) {
      this._setError("BANUBA_CLIENT_TOKEN が未設定です。")
      return
    }

    this._clearError()
    this._setState("starting")

    try {
      const sdkBase = this._normalizedSdkBaseUrl()

      const player = await Player.create({
        clientToken: this.clientTokenValue,
        locateFile: {
          "BanubaSDK.data": `${sdkBase}/BanubaSDK.data`,
          "BanubaSDK.wasm": `${sdkBase}/BanubaSDK.wasm`,
          "BanubaSDK.simd.wasm": `${sdkBase}/BanubaSDK.simd.wasm`,
        },
      })

      const modules = [new Module(this.faceTrackerUrlValue)]

      if (this.eyesUrlValue) {
        modules.push(new Module(this.eyesUrlValue))
      }

      if (this.lipsUrlValue) {
        modules.push(new Module(this.lipsUrlValue))
      }

      if (this.skinUrlValue) {
        modules.push(new Module(this.skinUrlValue))
      }

      await player.addModule(...modules)
      await player.use(new Webcam())

      Dom.render(player, this.surfaceTarget)

      if (this.effectUrlValue) {
        player.applyEffect(new Effect(this.effectUrlValue))
      }

      this._player = player
      this._started = true
      this._setState("running")
    } catch (error) {
      console.error("[banuba-verification] start failed", error)
      this._setError(this._humanizeError(error))
      await this._destroyPlayer()
      this._setState("error")
    } finally {
      this._syncUI()
    }
  }

  async stop() {
    if (!this._player && !this._started) {
      this._setState("idle")
      this._syncUI()
      return
    }

    this._setState("stopping")

    try {
      await this._destroyPlayer()
      this._clearError()
      this._setState("idle")
    } catch (error) {
      console.error("[banuba-verification] stop failed", error)
      this._setError("停止処理でエラーが発生しました。再読込してください。")
      this._setState("error")
    } finally {
      this._syncUI()
    }
  }

  reapplyEffect() {
    if (!this._player) return
    if (!this.effectUrlValue) return

    try {
      this._player.applyEffect(new Effect(this.effectUrlValue))
      this._clearError()
      this._setState(`effect:${this.effectNameValue || "applied"}`)
    } catch (error) {
      console.error("[banuba-verification] effect apply failed", error)
      this._setError("effect の再適用に失敗しました。")
      this._setState("error")
    } finally {
      this._syncUI()
    }
  }

  async _destroyPlayer() {
    if (this._player) {
      try {
        if (typeof this._player.destroy === "function") {
          await this._player.destroy()
        }
      } catch (_) {
      }
    }

    this._player = null
    this._started = false

    if (this.hasSurfaceTarget) {
      this.surfaceTarget.innerHTML = ""
    }
  }

  _normalizedSdkBaseUrl() {
    return (this.sdkBaseUrlValue || "").replace(/\/+$/, "")
  }

  _syncUI() {
    if (this.hasStartBtnTarget) {
      this.startBtnTarget.classList.toggle("d-none", this._started)
      this.startBtnTarget.disabled = this._started
    }

    if (this.hasStopBtnTarget) {
      this.stopBtnTarget.classList.toggle("d-none", !this._started)
      this.stopBtnTarget.disabled = !this._started
    }

    if (this.hasEffectBtnTarget) {
      const visible = this._started && !!this.effectUrlValue
      this.effectBtnTarget.classList.toggle("d-none", !visible)
      this.effectBtnTarget.disabled = !visible
    }
  }

  _setState(state) {
    if (this.hasStateTarget) {
      this.stateTarget.textContent = state
    }
  }

  _clearError() {
    if (this.hasErrorTarget) {
      this.errorTarget.textContent = ""
    }
  }

  _setError(message) {
    if (this.hasErrorTarget) {
      this.errorTarget.textContent = message
    }
  }

  _humanizeError(error) {
    const message = `${error?.message || error}`

    if (error?.name === "NotAllowedError" || error?.name === "SecurityError") {
      return "カメラ権限が拒否されました。ブラウザ設定で許可してください。"
    }

    if (error?.name === "NotFoundError" || error?.name === "OverconstrainedError") {
      return "利用できるカメラが見つかりません。接続やOS設定を確認してください。"
    }

    if (error?.name === "NotReadableError") {
      return "カメラを使用できません。他アプリが使用中の可能性があります。"
    }

    return `Banuba の起動に失敗しました: ${message}`
  }
}
```

次に、view から URL を渡します。

## `app/views/dev/banuba_verifications/show.html.erb` の data 属性追加

この部分に追加してください。

```erb
<div
  class="card banuba-card h-100"
  data-bs-theme="light"
  data-controller="banuba-verification"
  data-banuba-verification-client-token-value="<%= @banuba_client_token %>"
  data-banuba-verification-sdk-base-url-value="<%= @banuba_sdk_base_url %>"
  data-banuba-verification-face-tracker-url-value="<%= @banuba_face_tracker_url %>"
  data-banuba-verification-eyes-url-value="<%= @banuba_eyes_url %>"
  data-banuba-verification-lips-url-value="<%= @banuba_lips_url %>"
  data-banuba-verification-skin-url-value="<%= @banuba_skin_url %>"
  data-banuba-verification-effect-url-value="<%= @banuba_effect_url %>"
  data-banuba-verification-effect-name-value="<%= @banuba_effect_name %>"
>
```

最後に controller 側で instance variable を足します。

## `app/controllers/dev/banuba_verifications_controller.rb`

```ruby
# frozen_string_literal: true

module Dev
  class BanubaVerificationsController < ApplicationController
    before_action :ensure_development!
    before_action -> { require_at_least!(:system_admin) }

    def show
      @banuba_client_token = ENV["BANUBA_CLIENT_TOKEN"].to_s
      @banuba_sdk_base_url = "/banuba/sdk"
      @banuba_face_tracker_url = "/banuba/modules/face_tracker.zip"
      @banuba_eyes_url = "/banuba/modules/eyes.zip"
      @banuba_lips_url = "/banuba/modules/lips.zip"
      @banuba_skin_url = "/banuba/modules/skin.zip"
      @banuba_effect_url = "/banuba/effects/beauty_base.zip"
      @banuba_effect_name = "beauty_base.zip"
    end

    private

    def ensure_development!
      head :not_found unless Rails.env.development?
    end
  end
end
```

ポイントはここです。

```javascript
const modules = [new Module(this.faceTrackerUrlValue)]

if (this.eyesUrlValue) {
  modules.push(new Module(this.eyesUrlValue))
}

if (this.lipsUrlValue) {
  modules.push(new Module(this.lipsUrlValue))
}

if (this.skinUrlValue) {
  modules.push(new Module(this.skinUrlValue))
}

await player.addModule(...modules)
```

これで `face_tracker` だけでなく、`eyes` `lips` `skin` も読み込まれます。

確認はこの順でやるのが安全です。

1. `public/banuba/modules/` に `eyes.zip` `lips.zip` `skin.zip` がある
2. `/dev/banuba_verification` を開く
3. 起動する
4. まだ落ちるなら、今度は missing resource 名が変わるか確認する

必要なら次に、**現状ファイルに対する全文差し替え版**をそのまままとめて出します。
