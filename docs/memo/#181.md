以下、#181 の **最終実装プラン**と **コピペ用コード一式**です（#152/#180 のパターンに合わせて、通常GETで実装 / BAN・archived維持 / N+1回避）。

---

## 実装プラン（最小・安全）

1. `HomeController#show`

* `params[:q]` を受け取り、空白トリムした検索語 `@q` を用意
* `@q` があれば `booths.name OR stores.name` の **部分一致**で絞り込み（`ILIKE`）
* `params[:online] == "1"` の時だけ `status: :live` に絞り込み（= online）
* 既存の `Booth.active` / customer BAN除外 / `includes(...)` / `order` / `limit(60)` は維持

2. `app/views/home/show.html.erb`

* ブース一覧の上に検索フォーム（`form_with method: :get, scope: nil`）
* `online` は hidden を出さない（`include_hidden: false`）＝未チェック時にパラメータが出ないようにする
* 検索中表示：`“xxx”` と online チェック、件数を最小表示

3. テスト（fixtures無し前提）

* `test/integration/home_search_test.rb` を新設
* `sign_in` を使って customer で Home を叩く
* qなし / qあり（booth名・store名） / archived除外 / online=1 / BAN予防 + booths#show最終拒否 を確認

---

## ブランチ名案（再掲）

* `feature/181-home-booths-search`
* `feature/181-home-search-q`
* `feature/181-home-booth-store-search`

---

## 変更コード（コピペ）

### 1) `app/controllers/home_controller.rb`（全文置き換え）

```ruby
# frozen_string_literal: true

class HomeController < ApplicationController
  def show
    @q = params[:q].to_s.strip
    @online_only = (params[:online] == "1")

    booths = Booth.active

    if @q.present?
      escaped = ActiveRecord::Base.sanitize_sql_like(@q)
      pattern = "%#{escaped}%"

      booths =
        booths
          .joins(:store)
          .where("booths.name ILIKE :q OR stores.name ILIKE :q", q: pattern)
    end

    booths = booths.where(status: %i[live away]) if @online_only

    booths =
      booths
        .includes(
          :store,
          { booth_casts: :cast_user },
          thumbnail_image_attachment: :blob
        )
        .order(updated_at: :desc, id: :desc)
        .limit(60)

    # 可能な範囲で予防（ただし最終防衛は BoothsController#show の StoreBanGuard が担保）
    if user_signed_in? && current_user.customer?
      banned_store_ids = StoreBan.where(customer_user_id: current_user.id).select(:store_id)
      booths = booths.where.not(store_id: banned_store_ids)
    end

    @booths = booths

    # --- store list (minimal) ---
    stores =
      Store
        .where(id: Booth.active.select(:store_id)) # active booth を持つ store だけ
        .order(updated_at: :desc, id: :desc)
        .limit(30)

    if user_signed_in? && current_user.customer?
      banned_store_ids = StoreBan.where(customer_user_id: current_user.id).select(:store_id)
      stores = stores.where.not(id: banned_store_ids)
    end

    @stores = stores

    # --- favorites preloading for home (avoid N+1) ---
    if user_signed_in?
      @favorite_booth_ids =
        current_user.favorite_booths.where(booth_id: @booths.select(:id)).pluck(:booth_id).to_set

      @favorite_store_ids =
        current_user.favorite_stores.where(store_id: @stores.select(:id)).pluck(:store_id).to_set
    else
      @favorite_booth_ids = Set.new
      @favorite_store_ids = Set.new
    end
  end
end
```

---

### 2) `app/views/home/show.html.erb`（ログイン時ブース一覧の上に検索UI追加）

※全文置き換えでもいいですが、ここでは「ログイン時」部分を差し替えやすいように、`<h1>ブース一覧</h1>` の直下に入れる想定で、ログイン時の全文を提示します（guest側は変更なし）。

```erb
<% if user_signed_in? %>
  <div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
      <h1 class="h4 mb-0">ブース一覧</h1>
      <div class="text-muted small">
        ログイン中: <%= current_user.email %>（role: <%= current_user.role %>）
      </div>
    </div>

    <%# --- Search (minimal / GET) --- %>
    <div class="card mb-3">
      <div class="card-body">
        <%= form_with url: root_path, method: :get, scope: nil do |f| %>
          <div class="row g-2 align-items-center">
            <div class="col-12 col-md-7">
              <%= f.text_field :q,
                    value: @q,
                    class: "form-control",
                    placeholder: "ブース名 / ストア名で検索（部分一致）",
                    autocomplete: "off" %>
            </div>

            <div class="col-12 col-md-auto">
              <div class="form-check">
                <%= f.check_box :online,
                      { class: "form-check-input", checked: @online_only, include_hidden: false },
                      "1" %>
                <%= f.label :online, "配信中のみ", class: "form-check-label" %>
              </div>
            </div>

            <div class="col-12 col-md-auto">
              <%= f.submit "検索", class: "btn btn-primary w-100" %>
            </div>

            <div class="col-12 col-md-auto">
              <%= link_to "クリア", root_path, class: "btn btn-outline-secondary w-100" %>
            </div>
          </div>
        <% end %>

        <% if @q.present? || @online_only %>
          <div class="mt-2 text-muted small">
            <% labels = [] %>
            <% labels << "“#{@q}”" if @q.present? %>
            <% labels << "配信中のみ" if @online_only %>
            <span><%= labels.join(" / ") %> の検索結果</span>
            <span class="ms-2">（<%= @booths.size %> 件）</span>
          </div>
        <% end %>
      </div>
    </div>

    <% if @booths.present? %>
      <div class="row row-cols-1 row-cols-md-3 g-3">
        <% @booths.each do |booth| %>
          <% primary_cast = booth.booth_casts.max_by { |bc| [bc.created_at, bc.id] }&.cast_user %>
          <% favorited = @favorite_booth_ids.include?(booth.id) %>

          <div class="col">
            <div class="card h-100">
              <%= link_to booth_path(booth), class: "text-decoration-none text-reset" do %>
                <% if booth.thumbnail_image.attached? %>
                  <%= image_tag booth.thumbnail_image,
                        class: "card-img-top",
                        style: "aspect-ratio: 16 / 9; object-fit: cover;" %>
                <% else %>
                  <div class="bg-light d-flex align-items-center justify-content-center"
                       style="aspect-ratio: 16 / 9;">
                    <span class="text-muted small">no thumbnail</span>
                  </div>
                <% end %>
              <% end %>

              <div class="card-body">
                <div class="d-flex justify-content-between align-items-start gap-2">
                  <div class="fw-semibold">
                    <%= link_to booth.name, booth_path(booth), class: "text-decoration-none text-reset" %>
                  </div>
                  <div><%= booth_status_badge(booth) %></div>
                </div>

                <div class="mt-2 text-muted small">
                  <% if primary_cast.present? %>
                    cast: <%= display_name_or_email(primary_cast) %>
                  <% else %>
                    cast: <span class="text-muted">（未設定）</span>
                  <% end %>
                </div>

                <div class="text-muted small">
                  store: <%= booth.store&.name %>
                </div>
              </div>

              <div class="card-footer bg-white border-0 pt-0">
                <div id="booth_<%= booth.id %>_favorite_button">
                  <%= render "favorites/booths/button", booth: booth, favorited: favorited %>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="alert alert-secondary" role="alert">
        表示できるブースがありません。
      </div>
    <% end %>

    <hr class="my-4">

    <h2 class="h5 mb-3">店舗一覧</h2>

    <% if @stores.present? %>
      <div class="list-group mb-4">
        <% @stores.each do |store| %>
          <% favorited = @favorite_store_ids.include?(store.id) %>

          <div class="list-group-item">
            <div class="d-flex justify-content-between align-items-center gap-3">
              <div class="flex-grow-1">
                <%= link_to store.name, store_path(store), class: "text-decoration-none" %>
              </div>

              <div id="store_<%= store.id %>_favorite_button">
                <%= render "favorites/stores/button", store: store, favorited: favorited %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="alert alert-secondary" role="alert">
        表示できる店舗がありません。
      </div>
    <% end %>

    <hr class="my-4">

    <%# --- Dev / role entry links (暫定) --- %>
    <h2 class="h6">導線（暫定）</h2>
    <ul class="mb-3">
      <li><%= link_to "お気に入り（ブース）", favorites_booths_path %></li>
      <li><%= link_to "お気に入り（店舗）", favorites_stores_path %></li>
      <li><%= link_to "Cast booths", cast_booths_path %></li>
      <li><%= link_to "Store Admin Dashboard", admin_root_path %></li>
      <li><%= link_to "System Admin Dashboard", system_admin_root_path %></li>
      <li><%= link_to "店舗として登録（ref=TEST）", stores_new_registration_path(ref: "TEST") %></li>
    </ul>

    <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-outline-secondary" %>
  </div>
<% else %>
  <div class="container mt-4" style="max-width: 720px;">
    <h1 class="mb-3">ようこそ</h1>
    <p class="text-muted">視聴するにはログイン、または新規アカウント作成を行ってください。</p>

    <div class="d-flex gap-2 mt-3">
      <%= link_to "ログイン", new_user_session_path, class: "btn btn-primary" %>
      <%= link_to "新規アカウント作成", sign_up_path, class: "btn btn-outline-primary" %>
    </div>
  </div>
<% end %>
```

---

### 3) 新規テスト `test/integration/home_search_test.rb`

```ruby
# frozen_string_literal: true

require "test_helper"

class HomeSearchTest < ActionDispatch::IntegrationTest
  def create_user!(email:, role:)
    User.create!(
      email: email,
      password: "password",
      password_confirmation: "password",
      role: role
    )
  end

  def create_store!(name:)
    Store.create!(name: name)
  end

  def create_booth!(store:, name:, status:, archived_at: nil)
    Booth.create!(
      store: store,
      name: name,
      status: status,
      archived_at: archived_at
    )
  end

  test "qなし: 通常一覧（archivedは出ない）" do
    store1 = create_store!(name: "Alpha Store")
    store2 = create_store!(name: "Beta Store")

    booth_live = create_booth!(store: store1, name: "Rose Booth", status: :live)
    booth_off  = create_booth!(store: store2, name: "Tulip Booth", status: :offline)
    create_booth!(store: store1, name: "Archived Booth", status: :live, archived_at: Time.current)

    customer = create_user!(email: "customer@example.com", role: :customer)
    login_as(customer, scope: :user)

    get root_path
    assert_response :success

    assert_includes @response.body, booth_live.name
    assert_includes @response.body, booth_off.name
    refute_includes @response.body, "Archived Booth"
  end

  test "qあり: booth名の部分一致で絞られる" do
    store = create_store!(name: "Alpha Store")
    booth1 = create_booth!(store: store, name: "Rose Booth", status: :live)
    booth2 = create_booth!(store: store, name: "Tulip Booth", status: :offline)

    customer = create_user!(email: "customer2@example.com", role: :customer)
    login_as(customer, scope: :user)

    get root_path, params: { q: "Ros" }
    assert_response :success

    assert_includes @response.body, booth1.name
    refute_includes @response.body, booth2.name
  end

  test "qあり: store名の部分一致で絞られる" do
    store1 = create_store!(name: "Alpha Store")
    store2 = create_store!(name: "Beta Store")
    booth1 = create_booth!(store: store1, name: "Rose Booth", status: :live)
    booth2 = create_booth!(store: store2, name: "Tulip Booth", status: :offline)

    customer = create_user!(email: "customer3@example.com", role: :customer)
    login_as(customer, scope: :user)

    get root_path, params: { q: "Alpha" }
    assert_response :success

    assert_includes @response.body, booth1.name
    refute_includes @response.body, booth2.name
  end

  test "archived は検索しても出ない" do
    store = create_store!(name: "Alpha Store")
    create_booth!(store: store, name: "Archived Booth", status: :live, archived_at: Time.current)

    customer = create_user!(email: "customer4@example.com", role: :customer)
    login_as(customer, scope: :user)

    get root_path, params: { q: "Archived" }
    assert_response :success

    refute_includes @response.body, "Archived Booth"
  end

  test "online=1: live のみが出る" do
    store = create_store!(name: "Alpha Store")
    booth_live = create_booth!(store: store, name: "Live Booth", status: :live)
    booth_off  = create_booth!(store: store, name: "Offline Booth", status: :offline)

    customer = create_user!(email: "customer5@example.com", role: :customer)
    login_as(customer, scope: :user)

    get root_path, params: { online: "1" }
    assert_response :success

    assert_includes @response.body, booth_live.name
    refute_includes @response.body, booth_off.name
  end

  test "customer のBAN: Homeで予防され、booths#show でも最終拒否される" do
    store = create_store!(name: "Alpha Store")
    booth = create_booth!(store: store, name: "Banned Booth", status: :live)

    customer = create_user!(email: "banned_customer@example.com", role: :customer)
    store_admin = create_user!(email: "store_admin@example.com", role: :store_admin)

    StoreBan.create!(
      store: store,
      customer_user: customer,
      created_by_store_admin_user: store_admin
    )

    login_as(customer, scope: :user)

    get root_path
    assert_response :success
    refute_includes @response.body, booth.name

    get booth_path(booth)
    assert_response :see_other
    assert_redirected_to root_path
  end
end
```

---

## 実行コマンド（docker compose 経由）

```bash
docker compose exec app bash -lc "RAILS_ENV=test bin/rails test"
```

---

必要なら、このまま **コミットメッセージ案**と **PR本文テンプレ（#152/#180に寄せた形式）**も出します。
