## 実装タスク（#256 / Done に直結する順）

1. **PostgreSQL 拡張 `btree_gist` を有効化**（EXCLUDE制約の前提）
2. **`settlements` テーブル作成**（カラム一式、FK）
3. **DB制約を追加**

   * (store_id, period_from, period_to) **ユニーク**
   * `period_from < period_to` **CHECK**
   * 同一store内の期間オーバーラップ禁止 **EXCLUDE**
4. **モデル `Settlement` 追加**（enum・関連・最小バリデーション）
5. **`Store` に関連追加**（`has_many :settlements, dependent: :restrict_with_error`）
6. **テスト追加**

   * 完全一致ユニークが効く
   * オーバーラップが弾かれる
   * period整合性（モデル/DB）

---

## 変更ファイル一覧

* `db/migrate/XXXXXXXXXXXXXX_enable_btree_gist_extension.rb`（新規）
* `db/migrate/XXXXXXXXXXXXXX_create_settlements.rb`（新規）
* `app/models/settlement.rb`（新規）
* `app/models/store.rb`（変更）
* `test/models/settlement_test.rb`（新規）

---

## まず実行するコマンド（空migration生成 → 全文置換の流儀）

> すべて **docker compose 経由**で書いてます。

```bash
docker compose exec app bash -lc "bin/rails g migration EnableBtreeGistExtension"
docker compose exec app bash -lc "bin/rails g migration CreateSettlements"
```

生成された2ファイルを、以下の内容で **全文置換**してください（タイムスタンプ部分 `XXXXXXXXXXXXXX` は生成されたものに合わせる）。

---

## 1) migration: btree_gist 有効化

**db/migrate/XXXXXXXXXXXXXX_enable_btree_gist_extension.rb**

```ruby
class EnableBtreeGistExtension < ActiveRecord::Migration[8.1]
  def change
    enable_extension "btree_gist" unless extension_enabled?("btree_gist")
  end
end
```

---

## 2) migration: settlements 作成 + 制約

**db/migrate/XXXXXXXXXXXXXX_create_settlements.rb**

```ruby
class CreateSettlements < ActiveRecord::Migration[8.1]
  def change
    create_table :settlements do |t|
      t.references :store, null: false, foreign_key: true

      # --- Period (JST handled at app level) ---
      t.datetime :period_from, null: false
      t.datetime :period_to, null: false

      # --- Kind / Status ---
      t.integer :kind, null: false
      t.integer :status, null: false, default: 0

      # --- Amounts (yen) ---
      t.bigint :gross_yen, null: false, default: 0
      t.bigint :store_share_yen, null: false, default: 0
      t.bigint :platform_fee_yen, null: false, default: 0

      # --- Audit times ---
      t.datetime :confirmed_at
      t.datetime :exported_at
      t.bigint :exported_by_user_id

      # --- Export info ---
      t.string :export_format
      t.string :export_file_key

      # --- Payout snapshot (fixed at export time) ---
      t.string  :payout_bank_code, limit: 4
      t.string  :payout_branch_code, limit: 3
      t.integer :payout_account_type
      t.string  :payout_account_number, limit: 7
      t.string  :payout_account_holder_kana

      t.timestamps
    end

    add_foreign_key :settlements, :users, column: :exported_by_user_id

    # --- Constraints ---
    add_check_constraint :settlements, "period_from < period_to", name: "settlements_period_from_before_period_to"

    add_index :settlements, [:store_id, :period_from, :period_to],
              unique: true,
              name: "uniq_settlements_store_period_exact"

    # Reference indexes
    add_index :settlements, :status
    add_index :settlements, :kind
    add_index :settlements, [:period_from, :period_to]

    # Overlap guard: same store cannot have overlapping periods ([from, to))
    # Requires btree_gist extension for "store_id WITH =" in gist
    add_exclusion_constraint :settlements,
      "store_id WITH =, tsrange(period_from, period_to) WITH &&",
      using: :gist,
      name: "excl_settlements_store_period_no_overlap"
  end
end
```

> `tsrange(period_from, period_to)` は PostgreSQL のデフォルトで **[from, to)** になります（今回のルールに合致）。

---

## 3) モデル: Settlement

**app/models/settlement.rb（新規）**

```ruby
# frozen_string_literal: true

class Settlement < ApplicationRecord
  belongs_to :store
  belongs_to :exported_by_user, class_name: "User", optional: true

  enum :kind, { monthly: 0, manual: 1 }
  enum :status, { draft: 0, confirmed: 1, exported: 2, paid: 3 }
  enum :payout_account_type, { ordinary: 0, current: 1 }

  validates :store, presence: true
  validates :period_from, presence: true
  validates :period_to, presence: true
  validate :period_range_validation

  validates :gross_yen, :store_share_yen, :platform_fee_yen,
            numericality: { greater_than_or_equal_to: 0 }

  # --- Phase1 minimal guardrails (app-side) ---
  with_options if: :confirmed_or_later? do
    validates :confirmed_at, presence: true
  end

  with_options if: :exported_or_later? do
    validates :exported_at, presence: true
    validates :exported_by_user, presence: true
    validates :export_format, presence: true

    validates :payout_bank_code, presence: true
    validates :payout_branch_code, presence: true
    validates :payout_account_type, presence: true
    validates :payout_account_number, presence: true
    validates :payout_account_holder_kana, presence: true
  end

  private

  def period_range_validation
    return if period_from.blank? || period_to.blank?

    errors.add(:period_to, "must be after period_from") unless period_from < period_to
  end

  def confirmed_or_later?
    confirmed? || exported? || paid?
  end

  def exported_or_later?
    exported? || paid?
  end
end
```

---

## 4) Store に関連を追加

**app/models/store.rb** に1行追加（場所は `has_many` 群の近く）

```ruby
has_many :settlements, dependent: :restrict_with_error
```

（例：`has_many :store_payout_accounts...` の近くが自然）

---

## 5) テスト追加（制約テストの“既存流儀”に寄せる）

**test/models/settlement_test.rb（新規）**

```ruby
# frozen_string_literal: true

require "test_helper"

class SettlementTest < ActiveSupport::TestCase
  test "exact same period is unique per store at DB level" do
    store = Store.create!(name: "store")

    from = Time.zone.parse("2026-02-01 00:00:00")
    to   = Time.zone.parse("2026-03-01 00:00:00")

    Settlement.create!(
      store: store,
      kind: :monthly,
      status: :draft,
      period_from: from,
      period_to: to
    )

    assert_raises(ActiveRecord::ExclusionViolation) do
      Settlement.create!(
        store: store,
        kind: :monthly,
        status: :draft,
        period_from: from,
        period_to: to
      )
    end
  end

  test "overlapping period is rejected at DB level (exclude constraint)" do
    store = Store.create!(name: "store")

    Settlement.create!(
      store: store,
      kind: :monthly,
      status: :draft,
      period_from: Time.zone.parse("2026-02-01 00:00:00"),
      period_to:   Time.zone.parse("2026-03-01 00:00:00")
    )

    assert_raises(ActiveRecord::StatementInvalid) do
      Settlement.create!(
        store: store,
        kind: :monthly,
        status: :draft,
        period_from: Time.zone.parse("2026-02-15 00:00:00"),
        period_to:   Time.zone.parse("2026-03-15 00:00:00")
      )
    end
  end

  test "period_from must be before period_to (model validation)" do
    store = Store.create!(name: "store")

    s = Settlement.new(
      store: store,
      kind: :monthly,
      status: :draft,
      period_from: Time.zone.parse("2026-03-01 00:00:00"),
      period_to:   Time.zone.parse("2026-03-01 00:00:00")
    )

    assert_not s.valid?
    assert_includes s.errors[:period_to], "must be after period_from"
  end

  test "period_from must be before period_to (DB check constraint)" do
    store = Store.create!(name: "store")

    s = Settlement.new(
      store: store,
      kind: :monthly,
      status: :draft,
      period_from: Time.zone.parse("2026-03-01 00:00:00"),
      period_to:   Time.zone.parse("2026-03-01 00:00:00")
    )

    assert_raises(ActiveRecord::StatementInvalid) do
      s.save!(validate: false)
    end
  end
end
```

---

## マイグレーション実行・テスト

```bash
docker compose exec app bash -lc "bin/rails db:migrate"
docker compose exec app bash -lc "bin/rails test test/models/settlement_test.rb"
```

---

## Done チェック（#256 の Done に対応）

* ✅ 同一期間の二重精算ができない
  → `uniq_settlements_store_period_exact`（DBユニーク）で担保

* ✅ 同一storeで期間が被る settlement を作れない
  → `excl_settlements_store_period_no_overlap`（EXCLUDE）で担保

* ✅ CSV生成時に口座情報を `payout_*` にスナップショット保存できる構造
  → カラム一式 + exported以降で埋める前提のモデルバリデーションまで追加済み

---

### もしここだけハマったら（よくある1点）

`add_exclusion_constraint` が環境で未定義エラーになった場合、Rails側のDSLが想定より古い可能性があります。
その場合は **SQLで EXCLUDE を貼る版**に差し替えます（すぐ代替案出します）。
