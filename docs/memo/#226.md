了解、**「名前テキストだけを link_to」**で行きます。ここからは **貼り付け即反映できるコード**を出します。

---

## 実装プラン（最小差分）

1. `app/views/admin/booths/index.html.erb`

* 「名前」列の `booth.name` を **enter へのリンク**に変更

  * `enter_booth_path(booth)`
  * `data: { turbo_frame: "modal" }`（admin は store_admin 以上なので常にモーダルでOK）
* 「詳細」ボタンを削除
* archived バッジ・アーカイブボタンの条件分岐は現状維持

2. テスト追加（新規）

* `test/integration/admin_booths_index_test.rb` を追加

  * store_admin：current_store の booths のみ出る
  * system_admin：current_store 選択後に current_store の booths のみ出る
  * ブース名リンクの遷移先が `enter_booth_path`
  * `params[:archived]` で archived が出る/出ないが切り替わる
  * 「詳細」ボタンが存在しない

---

## 修正ファイル1: `app/views/admin/booths/index.html.erb`（全文置き換え）

```erb
<h1>ブース管理</h1>

<div class="mb-3 d-flex gap-2 align-items-center">
  <%= link_to "新規作成", new_admin_booth_path, class: "btn btn-primary" %>

  <% if params[:archived].present? %>
    <%= link_to "現役のみ表示", admin_booths_path, class: "btn btn-outline-secondary" %>
  <% else %>
    <%= link_to "アーカイブも表示", admin_booths_path(archived: 1), class: "btn btn-outline-secondary" %>
  <% end %>
</div>

<table class="table table-sm align-middle">
  <thead>
    <tr>
      <th>ID</th>
      <th>名前</th>
      <th>Status</th>
      <th>IVS Stage ARN</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @booths.each do |booth| %>
      <tr class="<%= "text-muted" if booth.archived? %>">
        <td><%= booth.id %></td>

        <td>
          <%= link_to booth.name,
                enter_booth_path(booth),
                data: { turbo_frame: "modal" },
                class: "text-decoration-none text-reset" %>
        </td>

        <td><%= booth.status %></td>

        <td class="text-muted small"><%= booth.ivs_stage_arn.presence || "-" %></td>

        <td>
          <div class="d-flex justify-content-end gap-2 align-items-center">
            <% if booth.archived? %>
              <span class="badge text-bg-secondary">archived</span>
            <% elsif booth.offline? && booth.current_stream_session_id.blank? %>
              <%= button_to "アーカイブ",
                    archive_admin_booth_path(booth),
                    method: :patch,
                    class: "btn btn-outline-danger btn-sm",
                    data: { turbo_confirm: "このブースをアーカイブします。よろしいですか？" } %>
            <% else %>
              <span class="text-muted small">配信終了後にアーカイブ可</span>
            <% end %>
          </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
```

---

## 追加ファイル: `test/integration/admin_booths_index_test.rb`（新規）

```ruby
# frozen_string_literal: true

require "test_helper"

class AdminBoothsIndexTest < ActionDispatch::IntegrationTest
  setup do
    @store1 = Store.create!(name: "store1")
    @store2 = Store.create!(name: "store2")

    @store_admin  = User.create!(email: "admin@example.com", password: "password", role: :store_admin)
    @system_admin = User.create!(email: "sys@example.com", password: "password", role: :system_admin)

    StoreMembership.create!(store: @store1, user: @store_admin, membership_role: :admin)

    @booth1_active = Booth.create!(store: @store1, name: "booth1", status: :offline)
    @booth1_arch   = Booth.create!(store: @store1, name: "booth1_arch", status: :offline, archived_at: Time.current)

    @booth2_active = Booth.create!(store: @store2, name: "booth2", status: :offline)
    @booth2_arch   = Booth.create!(store: @store2, name: "booth2_arch", status: :offline, archived_at: Time.current)
  end

  test "store_admin: index shows only current_store booths (active only by default) and booth name links to enter in modal" do
    sign_in @store_admin, scope: :user

    # store_admin は BaseController#current_store が「最初の admin membership」をfallbackする仕様
    get admin_booths_path
    assert_response :success

    # current_store(store1) の active のみ
    assert_select "tbody tr", minimum: 1
    assert_select "a[href=?][data-turbo-frame=?]", enter_booth_path(@booth1_active), "modal", text: @booth1_active.name
    assert_select "a", text: @booth1_arch.name, count: 0

    # 他storeは出ない
    assert_select "a", text: @booth2_active.name, count: 0
    assert_select "a", text: @booth2_arch.name, count: 0

    # 「詳細」ボタンが無い
    assert_select "a", text: "詳細", count: 0
    assert_select "a[href=?]", admin_booth_path(@booth1_active), count: 0
  end

  test "store_admin: archived=1 includes archived booths in current_store scope" do
    sign_in @store_admin, scope: :user

    get admin_booths_path(archived: 1)
    assert_response :success

    # current_store(store1) の active + archived が出る
    assert_select "a", text: @booth1_active.name, count: 1
    assert_select "a", text: @booth1_arch.name, count: 1

    # 他storeは出ない
    assert_select "a", text: @booth2_active.name, count: 0
    assert_select "a", text: @booth2_arch.name, count: 0
  end

  test "system_admin: index requires current_store and shows only that store booths" do
    sign_in @system_admin, scope: :user

    # current_store 未選択は /admin/stores へ誘導
    get admin_booths_path
    assert_response :redirect
    assert_redirected_to admin_stores_path

    # 選択（選択後は root にリダイレクト）
    post admin_current_store_path, params: { store_id: @store1.id }
    assert_response :redirect
    assert_redirected_to root_path

    # 選択後に index
    get admin_booths_path
    assert_response :success

    # store1 の active のみ
    assert_select "a", text: @booth1_active.name, count: 1
    assert_select "a", text: @booth1_arch.name, count: 0

    # 他storeは出ない
    assert_select "a", text: @booth2_active.name, count: 0
    assert_select "a", text: @booth2_arch.name, count: 0

    # ブース名は enter（modal）
    assert_select "a[href=?][data-turbo-frame=?]", enter_booth_path(@booth1_active), "modal", text: @booth1_active.name

    # 「詳細」ボタンが無い
    assert_select "a", text: "詳細", count: 0
  end

  test "system_admin: archived=1 includes archived booths in current_store scope" do
    sign_in @system_admin, scope: :user

    post admin_current_store_path, params: { store_id: @store1.id }
    assert_response :redirect
    assert_redirected_to root_path

    get admin_booths_path(archived: 1)
    assert_response :success

    assert_select "a", text: @booth1_active.name, count: 1
    assert_select "a", text: @booth1_arch.name, count: 1
    assert_select "a", text: @booth2_active.name, count: 0
    assert_select "a", text: @booth2_arch.name, count: 0
  end
end
```

> NOTE: このテストは「一覧の導線＝名前リンク」「詳細が消えた」「current_store スコープ」「archived 切替」をまとめて担保します。

---

## 手元での確認コマンド（PowerShell / Docker Compose 前提）

```powershell
docker compose exec app bash -lc "bin/rails test test/integration/admin_booths_index_test.rb"
docker compose exec app bash -lc "bin/rails test"
```

---

## コミットメッセージ案

* `Improve admin booths index: booth name opens enter modal and remove show link (#226)`

---

## PR本文案（コピペ用）

* 目的

  * admin/booths を「視聴/配信の入口」として整備し、ブース名クリックを enter（モーダル）に統一（#226 / #207）
* 変更点

  * admin/booths 一覧：ブース名を `enter_booth_path`（`data-turbo-frame="modal"`）へ
  * 一覧の「詳細」ボタンを削除（show は非対象で残置）
  * archived トグル/挙動は維持
* 仕様

  * current_store 必須（#206）：system_admin は未選択時 `/admin/stores` にリダイレクト
  * 一覧表示は current_store の booths のみ（store_admin / system_admin 共通）
* 動作確認

  * store_admin / system_admin で `/admin/booths` を確認
  * ブース名クリックで enter がモーダル表示される
  * `archived=1` で archived が含まれる
  * テスト通過（`bin/rails test`）

必要なら、このまま **差分が出た時の調整（assert_select のセレクタ等）**も、ログ貼ってくれれば即合わせます。
