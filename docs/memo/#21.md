# 実装

## 1 `app/services/wallets/release_service.rb`

```ruby
# frozen_string_literal: true

module Wallets
  class ReleaseService
    class InsufficientReserved < StandardError; end

    def initialize(wallet:, points:)
      @wallet = wallet
      @points = points.to_i
    end

    def call!
      raise ArgumentError, "points must be positive" unless @points.positive?

      wallet = Wallet.lock.find(@wallet.id)

      if wallet.reserved_points < @points
        raise InsufficientReserved, "reserved_points不足 (reserved=#{wallet.reserved_points}, release=#{@points})"
      end

      wallet.update!(
        reserved_points:  wallet.reserved_points - @points,
        available_points: wallet.available_points + @points
      )

      wallet
    end
  end
end
```

---

## 2 `app/services/drink_orders/refund_service.rb`

**価格スナップショットは wallet_transactions(kind: :hold, ref: drink_order, points: -price)** を使います。

要件：「pending→refunded」「顧客ごと集計して reserved→available を返す」「トランザクションで」

```ruby
# frozen_string_literal: true

module DrinkOrders
  class RefundService
    class Error < StandardError; end
    class MissingHold < Error; end
    class DuplicateHold < Error; end
    class InsufficientReserved < Error; end

    Result = Struct.new(:refunded_count, :refunded_points_sum, keyword_init: true)

    def initialize(stream_session:)
      @stream_session = stream_session
    end

    def call!
      now = Time.current

      pending_orders = DrinkOrder
        .where(stream_session_id: @stream_session.id, status: :pending)
        .lock
        .to_a

      return Result.new(refunded_count: 0, refunded_points_sum: 0) if pending_orders.empty?

      holds = WalletTransaction
        .where(kind: :hold, ref: pending_orders)
        .lock
        .to_a

      holds_by_order_id = holds.group_by(&:ref_id)

      pending_orders.each do |order|
        list = holds_by_order_id[order.id] || []
        raise MissingHold,   "hold not found drink_order_id=#{order.id}" if list.empty?
        raise DuplicateHold, "hold duplicated drink_order_id=#{order.id}" if list.size > 1
      end

      refunded_points_sum = 0

      # wallet_id ごとに集計して reserved→available を返す（顧客ごと集計の要件を満たす）
      refund_points_by_wallet_id =
        holds.group_by(&:wallet_id).transform_values { |txs| txs.sum { |tx| tx.points.abs } }

      refund_points_by_wallet_id.each do |wallet_id, points|
        refunded_points_sum += points
        wallet = Wallet.find(wallet_id)
        Wallets::ReleaseService.new(wallet: wallet, points: points).call!
      end

      # 返却ログ（注文単位で release を積む）
      pending_orders.each do |order|
        hold = holds_by_order_id.fetch(order.id).first
        price = hold.points.abs

        WalletTransaction.create!(
          wallet_id: hold.wallet_id,
          kind: :release,
          points: price,
          ref: order,
          occurred_at: now
        )
      end

      # 注文を refunded に
      DrinkOrder.where(id: pending_orders.map(&:id)).update_all(
        status: DrinkOrder.statuses.fetch("refunded"),
        refunded_at: now,
        updated_at: now
      )

      Result.new(refunded_count: pending_orders.size, refunded_points_sum: refunded_points_sum)
    end
  end
end
```

* `DrinkOrders::CreateService` が **hold tx を必ず作り**、`ref: drink_order` で紐づけてる
  → Refund は `drink_item` を見ずに正しい価格を返せます。
* `WalletTransaction` をロックしてるので、返却と並走する処理があっても崩れにくいです。
---

## 3 `app/services/stream_sessions/end_service.rb`

ポイント：

やることは実質2点だけです：

1. `DrinkOrders::RefundService` を実装して、EndService から確実に呼ばれるようにする
2. 「終了UI差し替え」を Turbo で入れる（最小は pending欄を ended表示に replace）

以下、`EndService` に合わせた “確定版”

### `StreamSessions::EndService` の修正（差分最小）

今のままだと `defined?(DrinkOrders::RefundService)` に依存していて、実装しても呼ばれますが、**Phase1の必須要件**なので、ここは **常に呼ぶ**に寄せた方が安全です（未実装時に silently skip すると事故ります）。

```diff
-        # 返却Service呼び出し（Issue15では「接続だけ」）
-        # 実体が未実装なら TODO のままでもOK（ただしPhase1の最終要件では必須）
-        if defined?(DrinkOrders::RefundService)
-          DrinkOrders::RefundService.new(stream_session: session).call
-        end
+        DrinkOrders::RefundService.new(stream_session: session).call!
```

※ メソッド名は `call!` に統一するのがあなたのサービス設計と相性いいです（CreateServiceも `call!`）。

**EndService の commit 後に呼ぶ（誤通知防止）**

transaction 内で broadcast するとロールバック時に嘘通知が出るので、`call` の最後（transactionの外）で呼ぶのが安全です。

```diff
-      StreamSession.transaction do
+      ended_session = nil
+      StreamSession.transaction do
         session = StreamSession.lock.find(@stream_session.id)
         raise AlreadyEnded if session.ended_at.present?
         ...
-        session
+        ended_session = session
       end
+      StreamSessionNotifier.broadcast_ended(ended_session)
+      ended_session
```

> `StreamSessionNotifier` は今回新規作成するので `defined?` なしで呼んでもOKですが、既存思想に合わせる。

---

## 4 `app/notifiers/stream_session_notifier.rb`

「終了UI通知（視聴画面の状態差し替え）」を最小で満たします。

* public: `[stream_session, :pending_drink_orders]` の target `pending_drink_orders` を ended partial へ差し替え
* cast: `[stream_session, :cast_pending_drink_orders]` の target `cast_pending_drink_orders` を ended partial へ差し替え

```ruby
# frozen_string_literal: true

class StreamSessionNotifier
  def self.broadcast_ended(stream_session)
    Turbo::StreamsChannel.broadcast_replace_to(
      [stream_session, :pending_drink_orders],
      target: "pending_drink_orders",
      partial: "stream_sessions/ended",
      locals: { stream_session: stream_session }
    )

    Turbo::StreamsChannel.broadcast_replace_to(
      [stream_session, :cast_pending_drink_orders],
      target: "cast_pending_drink_orders",
      partial: "cast/stream_sessions/ended",
      locals: { stream_session: stream_session }
    )
  end
end
```

---

## 5 partial（差し替え先）

### `app/views/stream_sessions/_ended.html.erb`

```erb
<div id="pending_drink_orders" class="alert alert-secondary mb-0" role="alert">
  配信は終了しました。未消化ドリンクは返却されました。
</div>
```

### `app/views/cast/stream_sessions/_ended.html.erb`

```erb
<div id="cast_pending_drink_orders" class="alert alert-secondary mb-0" role="alert">
  配信は終了しました（未消化は返却済み）。
</div>
```

---

# 3) 注文の価格（points）の根拠

* **注文の価格（points）の根拠は常に `WalletTransaction(kind: :hold, ref: drink_order).points.abs`**

  * CreateService がすでに hold tx を作っているので、ここを “真実” にします
* Consume / Refund どちらもこの根拠で points を確定する

## 3.1 ConsumeService の修正（最小差分）

### 変更点

```diff
-        points = drink_order.drink_item.price_points
+        points = hold_points_for!(drink_order)
```

### 追加（private メソッド）

`app/services/drink_orders/consume_service.rb` に以下を足してください（末尾の private 配下でOK）。

```ruby
    private

    def hold_points_for!(drink_order)
      txs = WalletTransaction.where(kind: :hold, ref: drink_order).lock.to_a
      raise MissingWalletError, "hold tx missing drink_order_id=#{drink_order.id}" if txs.empty?
      raise MissingWalletError, "hold tx duplicated drink_order_id=#{drink_order.id}" if txs.size > 1
      txs.first.points.abs
    end
```

> 例外クラスを増やしたい場合は `MissingHoldError/DuplicateHoldError` を新設してもいいですが、今回は **差分最小**で `MissingWalletError` を流用しています（メッセージで識別できます）。

## 3.2 ReleaseService のロック方式は with_lock に寄せる

`app\services\wallets\consume_service.rb`

```ruby
def call!
  raise ArgumentError, "points must be positive" unless @points.positive?

  @wallet.with_lock do
    if @wallet.reserved_points < @points
      raise InsufficientReservedPoints
    end

    @wallet.update!(
      reserved_points: @wallet.reserved_points - @points
    )

    WalletTransaction.create!(
      wallet: @wallet,
      kind: :consume,
      points: @points,
      ref: @ref,
      occurred_at: @occurred_at
    )
  end

  @wallet
end
```

---

# 4) 動作確認（最短）

（あなたのルール通り docker compose 経由で）

1. castで配信開始（live）
2. customerでドリンク送信 → `DrinkOrder pending` ができ、wallet の reserved が増える
3. castで `finish`

   * `drink_orders.pending` が 0
   * `drink_orders.refunded_at` が入る
   * wallet が reserved→available に戻る
   * public画面の pending リストが「配信は終了しました…」へ差し替わる
   * castの pending リストも同様に差し替わる

Closes #21 
Relates to #12 