貼り替え用全文です。

```erb
<% pending = DrinkOrder
  .includes(:drink_item)
  .where(stream_session_id: stream_session.id, status: :pending)
  .order(:created_at, :id) %>

<div id="pending_drink_orders">
  <div class="vstack gap-2">
    <% if pending.empty? %>
      <div class="text-muted">未消化のドリンクはありません</div>
    <% else %>
      <% pending.each do |order| %>
        <%= drink_item_display(order.drink_item) %>
      <% end %>
    <% end %>
  </div>
</div>
```

今回の変更点は、Issue文どおりに `card/card-body` と時刻表示を削除して、各 item を `drink_item_display(order.drink_item)` ベースへ統一した形です。元の対象仕様は 、現状ファイルは  に対応しています。
