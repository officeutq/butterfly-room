了解、ここから実装案＋貼り付けコードを出します（#148）。

ポイントは **`viewer_stage` を絶対に差し替えない**ことなので、`_stream_state` の「ドリンクメニュー部分だけ」を `turbo-frame id="drink_menu"` で囲い、`DrinkOrdersController#create` は **そのフレームだけ `replace`**します。pending / wallet は既存 notifier の broadcast に委譲（確認済み）。

---

## 実装方針（最小・安全）

* `app/views/booths/_stream_state.html.erb`

  * 既存の「ドリンクメニュー」ブロックを **専用 partial** に切り出し
  * その partial 内で `<turbo-frame id="drink_menu">` を提供
  * **`viewer_stage` や pending / comments は触らない**
* `StreamSessions::DrinkOrdersController#create`

  * `format.turbo_stream` を追加
  * 成功：`drink_menu` を置換（必要なら flash に成功メッセージを append）
  * 失敗：`drink_menu` 内にエラー表示（コメントと同型）
  * 既存の `format.html`（redirect_back）と `format.json` は維持

---

## ブランチ名候補

* `improve/148-drink-order-turbo-frame`
* `improve/148-drink-send-turbo-stream`
* `improve/148-drink-menu-turbo-frame`

---

## 変更ファイル一覧

* 変更：`app/views/booths/_stream_state.html.erb`
* 追加：`app/views/booths/_drink_menu.html.erb`
* 変更：`app/controllers/stream_sessions/drink_orders_controller.rb`

---

## 貼り付けコード

### 1) 追加 `app/views/booths/_drink_menu.html.erb`

```erb
<%# locals: booth:, stream_session:, drink_items:, error_message: (optional) %>

<turbo-frame id="drink_menu">
  <% if local_assigns[:error_message].present? %>
    <div class="alert alert-danger py-1 mb-2"><%= error_message %></div>
  <% end %>

  <h2>ドリンクメニュー（enabledのみ）</h2>

  <% live_like = booth.live? || booth.away? %>

  <% if stream_session.present? && live_like %>
    <% if (drink_items || []).any? %>
      <ul class="mb-0">
        <% (drink_items || []).each do |item| %>
          <li class="mb-2">
            <%= item.name %>（<%= item.price_points %>pt）

            <%= form_with url: stream_session_drink_orders_path(stream_session),
                          scope: :drink_order,
                          method: :post,
                          class: "d-inline" do |f| %>
              <%= f.hidden_field :drink_item_id, value: item.id %>
              <%= f.submit "送信", class: "btn btn-sm btn-primary ms-2" %>
            <% end %>
          </li>
        <% end %>
      </ul>
    <% else %>
      <p class="mb-0">現在、表示できるドリンクがありません</p>
    <% end %>
  <% else %>
    <p class="mb-0">配信中のみドリンク送信できます</p>
  <% end %>
</turbo-frame>
```

> `button_to turbo:false` を廃止し、`form_with`（Turbo有効）に寄せています。
> frame 置換の単位をここに固定します。

---

### 2) 変更 `app/views/booths/_stream_state.html.erb`

**「ドリンクメニュー」部分を差し替え**します（それ以外は維持）。

```erb
<%# locals: booth:, stream_session:, comments:, drink_items: %>

<% live_like = booth.live? || booth.away? %>

<% if stream_session.present? && live_like %>
  <div
    data-controller="presence-poll"
    data-presence-poll-ping-url-value="<%= ping_stream_session_presence_path(stream_session) %>"
    data-presence-poll-summary-url-value="<%= presence_summary_stream_session_path(stream_session) %>"
    data-presence-poll-interval-ms-value="20000"
  >
    viewers: <span data-presence-poll-target="count">-</span>
  </div>

  <hr>

  <%= render "booths/drink_menu",
        booth: booth,
        stream_session: stream_session,
        drink_items: drink_items %>

  <hr>

  <%= turbo_stream_from [stream_session, :pending_drink_orders] %>
  <%= render "stream_sessions/pending_drink_orders", stream_session: stream_session %>

  <hr>

  <%= turbo_stream_from [stream_session, :comments] %>

  <div id="comments">
    <%= render partial: "comments/comment", collection: (comments || []), as: :comment %>
  </div>

  <%= render "stream_sessions/comments/form", stream_session: stream_session %>

  <hr>

  <%= turbo_stream_from [stream_session, :viewer_stage] %>

  <div id="viewer_stage">
    <%= render "stream_sessions/ivs_viewer", stream_session: stream_session %>
  </div>

<% else %>
  <div class="text-muted">現在配信していません</div>

  <hr>

  <%= render "booths/drink_menu",
        booth: booth,
        stream_session: stream_session,
        drink_items: drink_items %>

  <hr>

  <div class="text-muted">配信中のみ表示されます</div>

  <hr>

  <p>配信中のみコメントできます</p>

  <hr>

  <div id="viewer_stage"></div>
<% end %>
```

> オフライン側にも `drink_menu` フレームを出しておくことで、表示状態が切り替わっても「ターゲットが存在しない」事故を避けられます。

---

### 3) 変更 `app/controllers/stream_sessions/drink_orders_controller.rb`

```ruby
# frozen_string_literal: true

module StreamSessions
  class DrinkOrdersController < ApplicationController
    include StoreBanGuard

    before_action :authenticate_user!
    before_action :require_customer!
    before_action :set_stream_session
    before_action :reject_banned_customer_for_stream_session!

    def create
      drink_item_id = params[:drink_item_id] || params.dig(:drink_order, :drink_item_id)
      drink_item = DrinkItem.find(drink_item_id)

      result = DrinkOrders::CreateService.new(
        stream_session: @stream_session,
        customer_user: current_user,
        drink_item: drink_item
      ).call!

      respond_to do |format|
        format.turbo_stream do
          render turbo_stream: [
            turbo_stream.replace(
              "drink_menu",
              partial: "booths/drink_menu",
              formats: [:html],
              locals: {
                booth: @stream_session.booth,
                stream_session: @stream_session,
                drink_items: DrinkItem.where(store_id: @stream_session.store_id, enabled: true).order(:id)
              }
            ),
            turbo_stream.append(
              "flash_inner",
              partial: "shared/flash_message",
              locals: { level: "success", message: "ドリンクを送信しました" }
            )
          ]
        end

        format.json do
          render json: {
            drink_order_id: result.drink_order.id,
            wallet: {
              available_points: result.wallet.available_points,
              reserved_points:  result.wallet.reserved_points
            }
          }, status: :created
        end

        format.html do
          redirect_back fallback_location: booth_path(@stream_session.booth_id),
                        notice: "ドリンクを送信しました"
        end
      end
    rescue Wallets::HoldService::InsufficientPoints
      render_drink_menu_error("ポイント不足です")
    rescue DrinkOrders::CreateService::Conflict
      render_drink_menu_error("配信中のみドリンク送信できます")
    rescue DrinkOrders::CreateService::InvalidItem
      render_drink_menu_error("このドリンクは送信できません")
    rescue DrinkOrders::CreateService::Forbidden
      render_drink_menu_error("送信できません（forbidden）", status: :forbidden)
    end

    private

    def set_stream_session
      @stream_session = StreamSession.find(params[:stream_session_id])
    end

    def reject_banned_customer_for_stream_session!
      reject_banned_customer!(store: @stream_session.store)
    end

    def require_customer!
      head :forbidden unless current_user.customer?
    end

    def render_drink_menu_error(message, status: :ok)
      respond_to do |format|
        format.turbo_stream do
          render turbo_stream: turbo_stream.replace(
            "drink_menu",
            partial: "booths/drink_menu",
            formats: [:html],
            locals: {
              booth: @stream_session.booth,
              stream_session: @stream_session,
              drink_items: DrinkItem.where(store_id: @stream_session.store_id, enabled: true).order(:id),
              error_message: message
            }
          ), status: status
        end

        format.json do
          # 既存互換を維持（必要なら error code を増やしてOK）
          code =
            case message
            when /ポイント不足/ then "insufficient_points"
            when /配信中/       then "booth_not_live"
            when /送信できません（forbidden）/ then "forbidden"
            else "invalid"
            end
          http_status =
            case code
            when "insufficient_points" then :payment_required
            when "booth_not_live"      then :conflict
            when "forbidden"           then :forbidden
            else :unprocessable_entity
            end
          render json: { error: code }, status: http_status
        end

        format.html do
          redirect_back fallback_location: booth_path(@stream_session.booth_id), alert: message
        end
      end
    end
  end
end
```

**ポイント**

* `drink_item_id` は **Turbo送信（`drink_order[drink_item_id]`）**と、既存の **json/html（`drink_item_id` 直）**の両対応にしてあります。
* Turbo成功時は `drink_menu` だけ replace ＋ flash に成功メッセージ append
* 失敗時はコメントと同型で `drink_menu` 内にエラー表示（遷移なし）
* `viewer_stage` は一切触らないので WebRTC の DOM は維持されます

---

### 3) 変更 `app/notifiers/wallet_notifier.rb`

```ruby
Turbo::StreamsChannel.broadcast_update_to(
  [ user, :wallet ],
  target: "wallet_balance",
  partial: "wallets/balance",
  locals: { wallet: wallet }
)
```

---

## 確認手順（Issue #148 の Done に対応）

1. customerで視聴ページを開き、配信を live/away にする
2. ドリンク「送信」

   * **ページ遷移が発生しない**（URL/スクロール/映像が維持）
   * `pending_drink_orders` が broadcast で増える
   * `wallet_balance` が broadcast で減る
3. ポイント不足で送信

   * **遷移なし**で `drink_menu` 内にエラー表示
4. BANユーザーで送信

   * flash にBAN表示が出て root へ遷移（既存仕様どおり）


