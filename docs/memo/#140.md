了解、**GO** なので「実装計画 → コピペ用コード一式」を出します。

---

## 実装計画（順序つき）

1. **DB追加**

* `referral_codes` テーブル追加（`code` unique / `label` / `expires_at` / `enabled`）

2. **Model追加**

* `ReferralCode` を新規追加
* `code` presence + uniqueness
* 「登録で使えるか？」判定用に `usable?`（enabled + not expired）を用意（#137 側で使える）

3. **Routes**

* `namespace :system_admin do resources :referral_codes, only: %i[index new create edit update] end`

4. **SystemAdmin CRUD**

* `SystemAdmin::ReferralCodesController`（薄く）
* `create` 失敗は `render :new, status: :unprocessable_entity`
* `update` 失敗は `render :edit, status: :unprocessable_entity`
* 一覧で enabled トグル（`PATCH update` を button_to で叩く）

5. **Views**

* index/new/edit を最低限Bootstrapで作る
* index で「enabled」「expired」を視覚化

6. **Dashboard導線**

* `system_admin/dashboard#show` に「紹介コード管理」リンクを足す

7. **テスト**

* fixtures無しの流儀で `IntegrationTest`
* system_admin 以外は 403
* system_admin は index 200 / create redirect / toggle redirect を最低限確認

---

## ブランチ名候補

* `feature/140-system-admin-referral-codes`
* `feature/140-referral-codes-admin`
* `feature/140-system-admin-manage-referral-codes`

---

## 変更ファイル一覧（コピペ用）

### 1) Migration（空を作ってから手で書き換え）

作成コマンド（いつも通り docker compose 経由）：

```bash
docker compose run --rm app bin/rails g migration CreateReferralCodes
```

生成された migration を **全文置き換え**：

`db/migrate/XXXXXXXXXXXXXX_create_referral_codes.rb`

```ruby
# frozen_string_literal: true

class CreateReferralCodes < ActiveRecord::Migration[8.1]
  def change
    create_table :referral_codes do |t|
      t.string :code, null: false
      t.string :label
      t.datetime :expires_at
      t.boolean :enabled, null: false, default: true

      t.timestamps
    end

    add_index :referral_codes, :code, unique: true
    add_index :referral_codes, :enabled
    add_index :referral_codes, :expires_at
  end
end
```

反映：

```bash
docker compose run --rm app bin/rails db:migrate
```

---

### 2) Model

`app/models/referral_code.rb`

```ruby
# frozen_string_literal: true

class ReferralCode < ApplicationRecord
  validates :code, presence: true, uniqueness: true

  def expired?
    expires_at.present? && expires_at < Time.current
  end

  # 店舗登録で「使用可能」かどうか（#137側で利用する想定）
  def usable?
    enabled? && !expired?
  end
end
```

---

### 3) Routes

`config/routes.rb` の System Admin セクションをこう変更：

```ruby
  # --- System Admin ---
  namespace :system_admin do
    root "dashboard#show"

    resources :referral_codes, only: %i[index new create edit update]
  end
```

---

### 4) Controller

`app/controllers/system_admin/referral_codes_controller.rb`

```ruby
# frozen_string_literal: true

module SystemAdmin
  class ReferralCodesController < SystemAdmin::BaseController
    before_action :set_referral_code, only: %i[edit update]

    def index
      @referral_codes = ReferralCode.order(id: :desc)
    end

    def new
      @referral_code = ReferralCode.new(enabled: true)
    end

    def create
      @referral_code = ReferralCode.new(referral_code_params)

      if @referral_code.save
        redirect_to system_admin_referral_codes_path, notice: "紹介コードを作成しました"
      else
        render :new, status: :unprocessable_entity
      end
    end

    def edit
    end

    def update
      if @referral_code.update(referral_code_params)
        redirect_to system_admin_referral_codes_path, notice: "更新しました"
      else
        render :edit, status: :unprocessable_entity
      end
    end

    private

    def set_referral_code
      @referral_code = ReferralCode.find(params[:id])
    end

    def referral_code_params
      params.require(:referral_code).permit(:code, :label, :expires_at, :enabled)
    end
  end
end
```

---

### 5) Views

`app/views/system_admin/referral_codes/index.html.erb`

```erb
<h1>紹介コード管理</h1>

<div class="mb-3 d-flex gap-2">
  <%= link_to "新規作成", new_system_admin_referral_code_path, class: "btn btn-primary" %>
  <%= link_to "ダッシュボードへ戻る", system_admin_root_path, class: "btn btn-outline-secondary" %>
</div>

<table class="table table-sm align-middle">
  <thead>
    <tr>
      <th>Code</th>
      <th>Label</th>
      <th>Expires</th>
      <th>Status</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @referral_codes.each do |rc| %>
      <tr class="<%= "text-muted" if !rc.enabled? || rc.expired? %>">
        <td class="fw-semibold"><%= rc.code %></td>
        <td><%= rc.label.presence || "-" %></td>
        <td class="text-muted small">
          <%= rc.expires_at.present? ? l(rc.expires_at) : "-" %>
        </td>

        <td>
          <% if rc.expired? %>
            <span class="badge text-bg-warning">expired</span>
          <% end %>

          <% if rc.enabled? %>
            <span class="badge text-bg-success">enabled</span>
          <% else %>
            <span class="badge text-bg-secondary">disabled</span>
          <% end %>

          <% unless rc.usable? %>
            <span class="badge text-bg-danger ms-1">登録不可</span>
          <% end %>
        </td>

        <td class="d-flex justify-content-end gap-2">
          <%= link_to "編集", edit_system_admin_referral_code_path(rc), class: "btn btn-outline-secondary btn-sm" %>

          <% if rc.enabled? %>
            <%= button_to "無効化",
                  system_admin_referral_code_path(rc),
                  method: :patch,
                  params: { referral_code: { enabled: false } },
                  class: "btn btn-outline-danger btn-sm",
                  data: { turbo_confirm: "この紹介コードを無効化します。よろしいですか？" } %>
          <% else %>
            <%= button_to "有効化",
                  system_admin_referral_code_path(rc),
                  method: :patch,
                  params: { referral_code: { enabled: true } },
                  class: "btn btn-outline-success btn-sm" %>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
```

`app/views/system_admin/referral_codes/new.html.erb`

```erb
<h1>紹介コード作成</h1>

<%= render "form", referral_code: @referral_code %>

<div class="mt-3">
  <%= link_to "戻る", system_admin_referral_codes_path, class: "btn btn-outline-secondary" %>
</div>
```

`app/views/system_admin/referral_codes/edit.html.erb`

```erb
<h1>紹介コード編集</h1>

<%= render "form", referral_code: @referral_code %>

<div class="mt-3">
  <%= link_to "戻る", system_admin_referral_codes_path, class: "btn btn-outline-secondary" %>
</div>
```

`app/views/system_admin/referral_codes/_form.html.erb`

```erb
<%= form_with model: [:system_admin, referral_code] do |f| %>
  <% if referral_code.errors.any? %>
    <div class="alert alert-danger">
      <ul class="mb-0">
        <% referral_code.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= f.label :code, "code（ユニーク）", class: "form-label" %>
    <%= f.text_field :code, class: "form-control", required: true, autocomplete: "off" %>
    <div class="form-text">店舗登録時に入力される共通コード（許可リスト）</div>
  </div>

  <div class="mb-3">
    <%= f.label :label, "label（識別用）", class: "form-label" %>
    <%= f.text_field :label, class: "form-control" %>
    <div class="form-text">例：営業A / 媒体 / 施策名</div>
  </div>

  <div class="mb-3">
    <%= f.label :expires_at, "expires_at（任意）", class: "form-label" %>
    <%= f.datetime_field :expires_at, class: "form-control" %>
    <div class="form-text">期限切れの場合、店舗登録では使用不可になります</div>
  </div>

  <div class="form-check mb-3">
    <%= f.check_box :enabled, class: "form-check-input" %>
    <%= f.label :enabled, "enabled（有効）", class: "form-check-label" %>
  </div>

  <div class="d-flex gap-2">
    <%= f.submit referral_code.persisted? ? "更新する" : "作成する", class: "btn btn-primary" %>
    <%= link_to "キャンセル", system_admin_referral_codes_path, class: "btn btn-outline-secondary" %>
  </div>
<% end %>
```

---

### 6) Dashboard導線

`app/views/system_admin/dashboard/show.html.erb` を一部更新（リンク追加）：

```erb
<h1>System Admin Dashboard</h1>

<ul>
  <li><strong>店舗管理</strong>（今後リンク追加）</li>
  <li><strong>ユーザー管理</strong>（今後リンク追加）</li>
  <li><strong>配信管理</strong>（今後リンク追加）</li>
  <li><strong>売上監査</strong>（今後リンク追加）</li>
  <li><strong>紹介コード管理</strong>（店舗登録の許可リスト）</li>
</ul>

<hr>

<ul>
  <li><%= link_to "店舗管理（placeholder）", "#" %></li>
  <li><%= link_to "ユーザー管理（placeholder）", "#" %></li>
  <li><%= link_to "配信管理（placeholder）", "#" %></li>
  <li><%= link_to "売上監査（placeholder）", "#" %></li>
  <li><%= link_to "紹介コード管理", system_admin_referral_codes_path %></li>
</ul>
```

---

### 7) Tests（最低限の認可＋CRUD）

`test/integration/system_admin_referral_codes_test.rb`

```ruby
# frozen_string_literal: true

require "test_helper"

class SystemAdminReferralCodesTest < ActionDispatch::IntegrationTest
  setup do
    @customer     = User.create!(email: "customer_rc@example.com", password: "password", role: :customer)
    @store_admin  = User.create!(email: "admin_rc@example.com", password: "password", role: :store_admin)
    @system_admin = User.create!(email: "sys_rc@example.com", password: "password", role: :system_admin)
  end

  test "non system_admin cannot access (403)" do
    sign_in @customer, scope: :user
    get system_admin_referral_codes_path
    assert_response :forbidden

    sign_in @store_admin, scope: :user
    get system_admin_referral_codes_path
    assert_response :forbidden
  end

  test "system_admin can list/create/update(toggle)" do
    sign_in @system_admin, scope: :user

    get system_admin_referral_codes_path
    assert_response :success

    # create
    post system_admin_referral_codes_path, params: {
      referral_code: {
        code: "TESTCODE",
        label: "label1",
        enabled: true
      }
    }
    assert_response :redirect
    assert_redirected_to system_admin_referral_codes_path

    rc = ReferralCode.find_by!(code: "TESTCODE")
    assert_equal true, rc.enabled?

    # toggle disable
    patch system_admin_referral_code_path(rc), params: { referral_code: { enabled: false } }
    assert_response :redirect
    assert_redirected_to system_admin_referral_codes_path
    assert_equal false, rc.reload.enabled?
  end
end
```

---

## Doneチェック（Issue #140）

* [x] system_admin が紹介コードを作成できる（new/create）
* [x] 有効/無効を切り替えられる（indexのボタン → update）
* [x] 期限切れコードは登録不可（`ReferralCode#usable?` と `expired?` を用意。#137側で `usable?` を使えばOK）
* [x] system_admin以外はアクセス不可（BaseController + テスト）
* [x] 最低限の認可テストがある（integration test）

---

## 実行・確認コマンド（docker compose）

```bash
docker compose run --rm app bin/rails test
```

ブラウザ確認：

* `http://localhost:3000/system_admin` → 「紹介コード管理」へ
* `http://localhost:3000/system_admin/referral_codes`

---

必要なら、この次に **#137（店舗登録側）で `ReferralCode.find_by(code:).usable?` を必須チェック**する差分も、そのまま続けて出せます。
