いけます。
このまま実装に進めて大丈夫です。

おすすめブランチは **`fix/524-keyboard-role-separation`** です。

以下、最終実装方針と、そのまま貼れる完成版です。
今回の方針は Issue の定義どおり、**cast 側だけで入力種別を分離し、title 入力時は `.live-overlay-bottom-row` のみ非表示**にします。viewer 側の挙動は変えません。

---

## 実装方針

* `live_stage_controller.js`

  * `focusin` 時に `document.activeElement.dataset.keyboardRole` を見て、`comment` / `title` を分岐
  * `keyboard-open` は共通で付与
  * cast では

    * comment: `cast-live-keyboard-open`
    * title: `cast-keyboard-open-for-title`
  * `focusin` ごとに cast 用 class をいったん全解除してから再付与
  * `focusout` では、別 input へ移動した場合は閉じない
  * スクロール復元は **comment モードだったときだけ** 維持

* view

  * title input に `data-keyboard-role="title"`
  * comment input に `data-keyboard-role="comment"`

* SCSS

  * 既存の `cast-live-keyboard-open` はそのまま
  * 新規 `cast-keyboard-open-for-title` では

    * `.live-overlay-bottom-row` のみ `display: none`
    * header / footer / meta / main row は維持

---

## 1. `app/javascript/controllers/live_stage_controller.js`

```javascript
import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  connect() {
    this._vv = window.visualViewport || null

    this._onFocusIn = this._onFocusIn.bind(this)
    this._onFocusOut = this._onFocusOut.bind(this)
    this._onResize = this._onResize.bind(this)

    this._keyboardCloseTimers = []

    this._applyStaticLayoutVars()

    document.addEventListener("focusin", this._onFocusIn)
    document.addEventListener("focusout", this._onFocusOut)
    window.addEventListener("orientationchange", this._onResize)
    window.addEventListener("resize", this._onResize)
  }

  disconnect() {
    document.removeEventListener("focusin", this._onFocusIn)
    document.removeEventListener("focusout", this._onFocusOut)
    window.removeEventListener("orientationchange", this._onResize)
    window.removeEventListener("resize", this._onResize)

    this._clearKeyboardCloseTimers()
    this._clearKeyboardModeClasses()

    document.documentElement.style.removeProperty("--live-stage-h")
    document.documentElement.style.removeProperty("--viewer-stage-h")
    document.documentElement.style.removeProperty("--app-header-h")
    document.documentElement.style.removeProperty("--app-footer-h")
  }

  _onResize() {
    // 端末回転や通常リサイズ時だけ静的値を取り直す
    // キーボード開閉に追随する再計算はしない
    this._applyStaticLayoutVars()
  }

  _onFocusIn(event) {
    if (!this._isMobileLike()) return

    const target = event.target
    if (!this._isTextInputElement(target)) return

    this._clearKeyboardCloseTimers()

    if (this._isCastLiveLayout()) {
      const role = this._keyboardRoleOf(target)

      if (!["comment", "title"].includes(role)) return

      this._clearKeyboardModeClasses()
      document.body.classList.add("keyboard-open")

      if (role === "comment") {
        document.body.classList.add("cast-live-keyboard-open")
      } else if (role === "title") {
        document.body.classList.add("cast-keyboard-open-for-title")
      }

      return
    }

    if (this._isViewerLayout()) {
      if (!this._hasTextInputFocus()) return

      this._clearKeyboardCloseTimers()
      document.body.classList.add("keyboard-open", "viewer-keyboard-open")

      requestAnimationFrame(() => {
        requestAnimationFrame(() => {
          const panel = document.querySelector('[data-controller~="comment-panel"]')
          if (!panel) return

          const controller = this.application.getControllerForElementAndIdentifier(panel, "comment-panel")
          controller?.scrollToBottomNow?.()
        })
      })
    }
  }

  _onFocusOut() {
    requestAnimationFrame(() => {
      if (this._hasTextInputFocus()) return

      const wasCastCommentOpen = document.body.classList.contains("cast-live-keyboard-open")

      this._clearKeyboardModeClasses()

      if (wasCastCommentOpen) {
        this._restoreRootScrollAfterKeyboardClose()
      }
    })
  }

  _restoreRootScrollAfterKeyboardClose() {
    const run = () => {
      const offsetTop = Math.max(0, Math.round(this._vv?.offsetTop || 0))
      if (offsetTop !== 0) return

      const root = document.scrollingElement || document.documentElement

      if (root.scrollTop > 0) {
        root.scrollTop = 0
      }

      if (document.documentElement.scrollTop > 0) {
        document.documentElement.scrollTop = 0
      }

      if (document.body.scrollTop > 0) {
        document.body.scrollTop = 0
      }

      if ((window.scrollY || 0) > 0) {
        window.scrollTo(0, 0)
      }
    }

    requestAnimationFrame(run)
    this._keyboardCloseTimers.push(window.setTimeout(run, 100))
    this._keyboardCloseTimers.push(window.setTimeout(run, 250))
    this._keyboardCloseTimers.push(window.setTimeout(run, 400))
  }

  _clearKeyboardCloseTimers() {
    this._keyboardCloseTimers.forEach((id) => clearTimeout(id))
    this._keyboardCloseTimers = []
  }

  _clearKeyboardModeClasses() {
    document.body.classList.remove(
      "keyboard-open",
      "cast-live-keyboard-open",
      "cast-keyboard-open-for-title",
      "viewer-keyboard-open"
    )
  }

  _applyStaticLayoutVars() {
    const header = document.getElementById("app_header")
    const footer = document.getElementById("app_footer")

    const headerH = header ? header.getBoundingClientRect().height : 0
    const footerH = footer ? footer.getBoundingClientRect().height : 0
    const viewportH = window.innerHeight || document.documentElement.clientHeight || 0

    document.documentElement.style.setProperty("--live-stage-h", `${Math.max(0, Math.round(viewportH))}px`)
    document.documentElement.style.setProperty(
      "--viewer-stage-h",
      `${Math.max(0, Math.round(viewportH - headerH - footerH))}px`
    )
    document.documentElement.style.setProperty("--app-header-h", `${Math.max(0, Math.round(headerH))}px`)
    document.documentElement.style.setProperty("--app-footer-h", `${Math.max(0, Math.round(footerH))}px`)
  }

  _isMobileLike() {
    return window.matchMedia("(pointer: coarse)").matches || window.innerWidth < 992
  }

  _isCastLiveLayout() {
    return document.body.classList.contains("cast-live-layout")
  }

  _isViewerLayout() {
    return document.body.classList.contains("viewer-layout")
  }

  _keyboardRoleOf(element) {
    return element?.dataset?.keyboardRole || ""
  }

  _isTextInputElement(element) {
    if (!element) return false

    if (element.matches("textarea")) return true
    if (element.matches("[contenteditable='true']")) return true

    if (element.matches("input")) {
      const type = (element.getAttribute("type") || "text").toLowerCase()
      return ![
        "button",
        "checkbox",
        "color",
        "file",
        "hidden",
        "image",
        "radio",
        "range",
        "reset",
        "submit",
      ].includes(type)
    }

    return false
  }

  _hasTextInputFocus() {
    return this._isTextInputElement(document.activeElement)
  }
}
```

---

## 2. `app/views/cast/booths/live.html.erb`

title input に `data-keyboard-role="title"` を追加します。

```erb
<div
  class="cast-live-screen"
  data-controller="ivs-publisher live-stage"
  data-ivs-publisher-token-url-value="<%= stream_session_ivs_participant_tokens_path(@stream_session) %>"
  data-ivs-publisher-finish-url-value="<%= finish_cast_stream_session_path(@stream_session) %>"
  data-ivs-publisher-status-url-value="<%= status_cast_booth_path(@booth) %>"
  data-ivs-publisher-meta-display-url-value="<%= meta_display_cast_stream_session_path(@stream_session) %>"
  data-ivs-publisher-mirror-value="true"
  data-ivs-publisher-initial-mode-value="<%= @booth.away? ? "away" : "normal" %>"
  data-ivs-publisher-initial-booth-status-value="<%= @booth.status %>"
  data-ivs-publisher-banuba-client-token-value="<%= @banuba_client_token %>"
  data-ivs-publisher-banuba-sdk-base-url-value="<%= @banuba_sdk_base_url %>"
  data-ivs-publisher-banuba-face-tracker-url-value="<%= @banuba_face_tracker_url %>"
  data-ivs-publisher-banuba-eyes-url-value="<%= @banuba_eyes_url %>"
  data-ivs-publisher-banuba-lips-url-value="<%= @banuba_lips_url %>"
  data-ivs-publisher-banuba-skin-url-value="<%= @banuba_skin_url %>"
  data-ivs-publisher-banuba-background-url-value="<%= @banuba_background_url %>"
  data-ivs-publisher-banuba-hair-url-value="<%= @banuba_hair_url %>"
  data-ivs-publisher-banuba-effect-url-value="<%= @banuba_effect_url %>"
  data-ivs-publisher-banuba-effect-name-value="<%= @banuba_effect_name %>"
  data-ivs-publisher-banuba-beauty-config-url-value="/banuba/effects/beauty_base/config.json"
>
  <div id="live_stage">
    <div id="live_media">
      <div
        class="live-banuba-surface"
        data-ivs-publisher-target="banubaSurface"></div>

      <canvas
        class="d-none"
        data-ivs-publisher-target="canvas"></canvas>
    </div>

    <div id="live_flash_layer">
      <div id="flash_inner">
        <% if notice.present? %>
          <%= render "shared/flash_message", level: "success", message: notice %>
        <% end %>

        <% if alert.present? %>
          <%= render "shared/flash_message", level: "danger", message: alert %>
        <% end %>
      </div>
    </div>

    <div id="live_overlay">
      <div class="live-overlay-ui" data-live-grid-debug="<%= Rails.env.development? ? "1" : "0" %>">
        <div class="live-overlay-meta-slot">
          <div
            class="live-meta-panel d-none"
            data-controller="presence-poll"
            data-ivs-publisher-target="metaPanel"
            data-presence-poll-summary-url-value="<%= presence_summary_stream_session_path(@stream_session) %>"
            data-presence-poll-interval-ms-value="20000"
          >
            <div class="live-meta-layout">
              <div class="live-meta-main">
                <turbo-frame id="stream_meta_display">
                  <%= render "stream_sessions/stream_meta_display",
                            stream_session: @stream_session %>
                </turbo-frame>
              </div>

              <div class="live-meta-side">
                <div class="live-meta-viewer-count">
                  <i class="bi bi-people-fill" aria-hidden="true"></i>
                  <span data-presence-poll-target="count">-</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        <div class="live-overlay-main-row">
          <div class="live-overlay-main-spacer">
            <div class="live-standby-actions-panel d-none" data-ivs-publisher-target="summaryPanel">
              <% if @booth.standby? && @booth.current_stream_session_id == @stream_session.id %>
                <%= form_with model: @stream_session,
                              url: metadata_cast_stream_session_path(@stream_session),
                              method: :patch,
                              class: "live-standby-actions-form" do |f| %>
                  <div class="input-group">
                    <%= f.text_field :title,
                                     class: "form-control",
                                     maxlength: 64,
                                     placeholder: "未入力ならブース名が表示されます",
                                     aria: { label: "タイトル" },
                                     data: { keyboard_role: "title" } %>
                    <%= f.submit "設定する", class: "btn btn-primary" %>
                  </div>
                <% end %>
              <% end %>
            </div>
          </div>

          <div class="live-overlay-drink-slot">
            <%= turbo_stream_from [@stream_session, :cast_pending_drink_orders] %>

            <div class="live-drink-panel d-none" data-ivs-publisher-target="drinkPanel">
              <div class="live-drink-panel-header">ドリンク</div>

              <div class="live-drink-panel-body">
                <div id="cast-pending-drink-orders-container">
                  <%= render "cast/stream_sessions/pending_drink_orders", stream_session: @stream_session %>
                </div>
              </div>
            </div>
          </div>
        </div>

        <div class="live-overlay-bottom-row">
          <div class="live-overlay-comment-slot">
            <%= turbo_stream_from [@stream_session, :comments] %>

            <div id="cast_comment_section">
              <%= render "cast/booths/comment_section",
                        booth: @booth,
                        stream_session: @stream_session,
                        comments: @comments %>
            </div>
          </div>

          <div class="live-overlay-ops-slot">
            <div class="live-ops-panel d-none" data-ivs-publisher-target="opsPanel">
              <div class="live-ops-buttons">
                <%= form_with url: status_cast_booth_path(@booth, to: :away),
                              method: :patch,
                              class: "live-ops-form",
                              data: { action: "turbo:submit-end->ivs-publisher#onBoothStatusPatched" } do %>
                  <button type="submit"
                          class="live-ops-btn"
                          data-ivs-publisher-target="cameraOffBtn"
                          aria-label="カメラをオフにする"
                          title="カメラON">
                    <i class="bi bi-camera-video-fill" aria-hidden="true"></i>
                  </button>
                <% end %>

                <%= form_with url: status_cast_booth_path(@booth, to: :live),
                              method: :patch,
                              class: "live-ops-form",
                              data: { action: "turbo:submit-end->ivs-publisher#onBoothStatusPatched" } do %>
                  <button type="submit"
                          class="live-ops-btn"
                          data-ivs-publisher-target="cameraOnBtn"
                          aria-label="カメラをオンにする"
                          title="カメラOFF">
                    <i class="bi bi-camera-video-off-fill" aria-hidden="true"></i>
                  </button>
                <% end %>

                <button type="button"
                        class="live-ops-btn"
                        data-action="ivs-publisher#toggleMic"
                        data-ivs-publisher-target="micBtn"
                        aria-label="マイクを切り替える"
                        title="マイク切替">
                  <i class="bi bi-mic-fill" data-ivs-publisher-target="micIcon" aria-hidden="true"></i>
                </button>

                <button type="button"
                        class="live-ops-btn"
                        data-action="ivs-publisher#openEffectPanel"
                        data-ivs-publisher-target="effectOpenBtn"
                        aria-label="加工モードを選択する"
                        title="加工モード選択">
                  <i class="bi bi-stars" aria-hidden="true"></i>
                </button>

                <button type="button"
                        class="live-ops-btn d-none"
                        data-action="ivs-publisher#openBeautyPanel"
                        data-ivs-publisher-target="beautyAdjustBtn"
                        aria-label="Beauty 設定を開く"
                        title="Beauty 調整">
                  <i class="bi bi-sliders" aria-hidden="true"></i>
                </button>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <%= render "cast/booths/effect_panel", effects: @effects %>
    <%= render "cast/booths/beauty_panel" %>
  </div>

  <footer id="app_footer" class="cast-live-footer border-top bg-white fixed-bottom">
    <div class="container py-2">
      <div class="d-flex justify-content-between align-items-center">
        <div class="d-flex gap-3">
          <%= link_to "ホーム", root_path, class: "btn btn-sm btn-outline-secondary" %>
          <%= link_to "お気に入り", favorites_booths_path, class: "btn btn-sm btn-outline-secondary" %>
        </div>

        <div class="d-flex gap-2">
          <button type="button"
                  class="btn btn-sm btn-primary"
                  data-action="ivs-publisher#startBroadcast"
                  data-ivs-publisher-target="startBtn"
                  data-label-normal="配信開始"
                  data-label-resume="配信に戻る">
            配信開始
          </button>

          <button type="button"
                  class="btn btn-sm btn-danger d-none"
                  data-action="ivs-publisher#endBroadcast"
                  data-ivs-publisher-target="endBtn">
            配信終了
          </button>
        </div>

        <div class="d-flex gap-2 align-items-center">
          <%= link_to "ダッシュボード",
                dashboard_path_for(current_user),
                class: "btn btn-sm btn-outline-secondary" %>

          <% if user_signed_in? %>
            <%= button_to "ログアウト",
                  destroy_user_session_path,
                  method: :delete,
                  class: "btn btn-sm btn-outline-secondary" %>
          <% end %>
        </div>
      </div>
    </div>
  </footer>
</div>
```

---

## 3. `app/views/stream_sessions/comments/_form.html.erb`

comment input に `data-keyboard-role="comment"` を追加します。

```erb
<turbo-frame id="comment_form">
  <% if local_assigns[:error_message].present? %>
    <div class="alert alert-danger py-1 mb-2"><%= error_message %></div>
  <% end %>

  <%= form_with url: stream_session_comments_path(stream_session), scope: :comment do |f| %>
    <%= f.text_field :body,
          maxlength: 200,
          required: true,
          autocomplete: "off",
          class: "form-control form-control-sm",
          placeholder: "コメント…",
          data: { keyboard_role: "comment" } %>

    <%= f.submit "送信", class: "btn btn-primary btn-sm" %>
  <% end %>
</turbo-frame>
```

---

## 4. `app/assets/stylesheets/components/_broadcast_layout.scss`

既存の comment 用 keyboard レイアウトはそのままにして、title 用の分岐だけ追加します。

```scss
/* ------------------------------------------------------------
   レイアウト調整メモ（live overlay）

   このファイルの clamp() を変更すると、Overlay UI のサイズを調整できる。

   ■ bottom row の高さ
     grid-template-rows の clamp(...) を変更する
     MIN   : 小画面でも最低限確保する高さ
     IDEAL : 画面高に応じた目標（vh）
     MAX   : PCなどで大きくなりすぎない上限

   ■ drink slot の幅（右カラム / main row）
     .live-overlay-main-row の
     grid-template-columns: minmax(0,1fr) clamp(...)
     の clamp を変更する

     MIN   : 小画面で潰れない最低幅
     IDEAL : 画面幅に応じた目標（vw）
     MAX   : PCで太りすぎない上限

   ■ ops slot の幅（右カラム / bottom row）
     .live-overlay-bottom-row の
     grid-template-columns: minmax(0,1fr) clamp(...)
     の clamp を変更する

     MIN   : ボタンが折り返さない最低幅
     IDEAL : 画面幅に応じた目標（vw）
     MAX   : PCで太りすぎない上限

   調整の目安
     ・iPhone縦で右カラムが細い → MIN / IDEAL を上げる
     ・PCで右カラムが太い     → MAX を下げる
------------------------------------------------------------ */

#live_flash_layer,
#viewer_flash_layer {
  position: absolute;
  top: 12px;
  left: 50%;
  transform: translateX(-50%);
  width: min(640px, calc(100vw - 24px));
  z-index: 40;
  pointer-events: none;
}

#live_flash_layer #flash_inner,
#viewer_flash_layer #flash_inner {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

#live_flash_layer .alert,
#viewer_flash_layer .alert {
  margin-bottom: 0;
  pointer-events: auto;
  box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.35);
}

#live_overlay,
#viewer_overlay {
  position: absolute;
  inset: 0;
  z-index: 10;
  pointer-events: none;
  overflow: hidden;
}

#live_overlay .live-overlay-ui,
#viewer_overlay .viewer-overlay-ui {
  pointer-events: auto;
  width: 100%;
  height: 100%;
  padding-top: calc(var(--app-header-h) + 0px);
  padding-right: 0px;
  padding-bottom: calc(var(--app-footer-h) + 0px);
  padding-left: 0px;

  display: grid;
  grid-template-rows:
    minmax(40px, 40px)
    minmax(0px, 1fr)
    minmax(0px, 1fr);

  overflow: hidden;
  min-width: 0;
  min-height: 0;
}

.live-overlay-main-row,
.live-overlay-bottom-row,
.viewer-overlay-main-row,
.viewer-overlay-bottom-row {
  min-width: 0;
  min-height: 0;
}

.live-overlay-main-row,
.viewer-overlay-main-row {
  display: grid;
  grid-template-columns: minmax(0, 1fr) clamp(120px, 20vw, 280px);
}

.live-overlay-bottom-row,
.viewer-overlay-bottom-row {
  display: grid;
  grid-template-columns: minmax(0, 1fr) clamp(40px, 10vw, 100px);
}

.live-overlay-meta-slot,
.live-overlay-main-spacer,
.live-overlay-drink-slot,
.live-overlay-comment-slot,
.live-overlay-ops-slot,
.viewer-overlay-meta-slot,
.viewer-overlay-main-spacer,
.viewer-overlay-drink-slot,
.viewer-overlay-comment-slot,
.viewer-overlay-ops-slot {
  min-width: 0;
  min-height: 0;
  overflow: hidden;
  border-radius: 12px;
}

.live-overlay-main-spacer,
.viewer-overlay-main-spacer {
  display: flex;
  align-items: flex-start;
}

.live-overlay-meta-slot,
.viewer-overlay-meta-slot {
  display: flex;
  align-items: flex-start;
  max-height: clamp(120px, 24vh, 240px);
  min-height: 42px;

  overflow-y: auto;
  overflow-x: hidden;

  overscroll-behavior: contain;
  -webkit-overflow-scrolling: touch;
}

.live-overlay-drink-slot,
.live-overlay-comment-slot,
.live-overlay-ops-slot,
.viewer-overlay-drink-slot,
.viewer-overlay-comment-slot,
.viewer-overlay-ops-slot {
  min-height: 48px;
}

.live-slot-label,
.viewer-slot-label {
  display: none;
  font-size: 0.875rem;
  font-weight: 600;
  line-height: 1.2;
  padding: 8px 10px;
  color: #fff;
}

.live-overlay-drink-slot,
.viewer-overlay-drink-slot {
  display: flex;
  align-items: flex-start;
}

.live-overlay-comment-slot,
.viewer-overlay-comment-slot {
  display: flex;
  align-items: flex-end;
}

.live-overlay-ops-slot,
.viewer-overlay-ops-slot {
  display: flex;
  align-items: flex-end;
}

.live-overlay-ui[data-live-grid-debug="1"] .live-overlay-meta-slot,
.live-overlay-ui[data-live-grid-debug="1"] .live-overlay-main-spacer,
.live-overlay-ui[data-live-grid-debug="1"] .live-overlay-drink-slot,
.live-overlay-ui[data-live-grid-debug="1"] .live-overlay-comment-slot,
.live-overlay-ui[data-live-grid-debug="1"] .live-overlay-ops-slo,
.viewer-overlay-ui[data-viewer-grid-debug="1"] .viewer-overlay-meta-slot,
.viewer-overlay-ui[data-viewer-grid-debug="1"] .viewer-overlay-main-spacer,
.viewer-overlay-ui[data-viewer-grid-debug="1"] .viewer-overlay-drink-slot,
.viewer-overlay-ui[data-viewer-grid-debug="1"] .viewer-overlay-comment-slot,
.viewer-overlay-ui[data-viewer-grid-debug="1"] .viewer-overlay-ops-slot {
  outline: 1px solid rgba(255, 255, 255, 0.7);
  outline-offset: -1px;
}

.live-overlay-ui[data-live-grid-debug="1"] .live-overlay-meta-slot,
.viewer-overlay-ui[data-viewer-grid-debug="1"] .viewer-overlay-meta-slot {
  background: rgba(13, 110, 253, 0.22);
}

.live-overlay-ui[data-live-grid-debug="1"] .live-overlay-main-spacer,
.viewer-overlay-ui[data-viewer-grid-debug="1"] .viewer-overlay-main-spacer {
  background: rgba(255, 255, 255, 0.06);
}

.live-overlay-ui[data-live-grid-debug="1"] .live-overlay-drink-slot,
.viewer-overlay-ui[data-viewer-grid-debug="1"] .viewer-overlay-drink-slot {
  background: rgba(255, 193, 7, 0.22);
}

.live-overlay-ui[data-live-grid-debug="1"] .live-overlay-comment-slot,
.viewer-overlay-ui[data-viewer-grid-debug="1"] .viewer-overlay-comment-slot {
  background: rgba(25, 135, 84, 0.22);
}

.live-overlay-ui[data-live-grid-debug="1"] .live-overlay-ops-slot,
.viewer-overlay-ui[data-viewer-grid-debug="1"] .viewer-overlay-ops-slot {
  background: rgba(220, 53, 69, 0.22);
}

.live-overlay-ui[data-live-grid-debug="1"] .live-slot-label,
.viewer-overlay-ui[data-viewer-grid-debug="1"] .viewer-slot-label {
  display: inline-block;
}

.live-overlay-ui[data-live-grid-debug="0"] .live-overlay-meta-slot,
.live-overlay-ui[data-live-grid-debug="0"] .live-overlay-main-spacer,
.live-overlay-ui[data-live-grid-debug="0"] .live-overlay-drink-slot,
.live-overlay-ui[data-live-grid-debug="0"] .live-overlay-comment-slot,
.live-overlay-ui[data-live-grid-debug="0"] .live-overlay-ops-slot,
.viewer-overlay-ui[data-viewer-grid-debug="0"] .viewer-overlay-meta-slot,
.viewer-overlay-ui[data-viewer-grid-debug="0"] .viewer-overlay-main-spacer,
.viewer-overlay-ui[data-viewer-grid-debug="0"] .viewer-overlay-drink-slot,
.viewer-overlay-ui[data-viewer-grid-debug="0"] .viewer-overlay-comment-slot,
.viewer-overlay-ui[data-viewer-grid-debug="0"] .viewer-overlay-ops-slot {
  background: transparent;
  outline: none;
}

body.cast-live-layout.cast-live-keyboard-open #app_header,
body.cast-live-layout.cast-live-keyboard-open #app_footer.cast-live-footer,
body.viewer-layout.viewer-keyboard-open #app_header,
body.viewer-layout.viewer-keyboard-open #app_footer {
  opacity: 0;
  pointer-events: none;
  visibility: hidden;
}

body.cast-live-layout.cast-live-keyboard-open #live_overlay,
body.viewer-layout.viewer-keyboard-open #viewer_overlay {
  overflow: hidden;
}

body.cast-live-layout.cast-live-keyboard-open #live_overlay .live-overlay-ui,
body.viewer-layout.viewer-keyboard-open #viewer_overlay .viewer-overlay-ui {
  display: grid;
  width: 100%;
  height: 100%;
  padding-top: calc(env(safe-area-inset-top) + 0px);
  padding-right: 0px;
  padding-bottom: calc(env(safe-area-inset-bottom) + 0px);
  padding-left: 0px;
  grid-template-rows: 1fr minmax(0, auto);
  overflow: hidden;
}

body.cast-live-layout.cast-live-keyboard-open .live-overlay-meta-slot,
body.cast-live-layout.cast-live-keyboard-open .live-overlay-main-row,
body.cast-live-layout.cast-live-keyboard-open .live-overlay-ops-slot,
body.viewer-layout.viewer-keyboard-open .viewer-overlay-meta-slot,
body.viewer-layout.viewer-keyboard-open .viewer-overlay-main-row,
body.viewer-layout.viewer-keyboard-open .viewer-overlay-ops-slot {
  display: none !important;
}

body.cast-live-layout.cast-live-keyboard-open .live-overlay-bottom-row,
body.viewer-layout.viewer-keyboard-open .viewer-overlay-bottom-row {
  grid-template-columns: minmax(0, 1fr);
  gap: 0;
  min-height: 0;
  align-self: end;
}

body.cast-live-layout.cast-live-keyboard-open .live-overlay-comment-slot,
body.viewer-layout.viewer-keyboard-open .viewer-overlay-comment-slot {
  position: static;
  min-height: 0;
  overflow: hidden;
  border-radius: 12px;
  pointer-events: auto;
  display: flex;
  align-items: flex-end;
}

body.cast-live-layout.cast-live-keyboard-open .live-overlay-comment-slot > #cast_comment_section,
body.viewer-layout.viewer-keyboard-open .viewer-overlay-comment-slot > #viewer_comment_section {
  width: 100%;
  max-width: 100%;
}

body.cast-live-layout.cast-live-keyboard-open #live_flash_layer,
body.viewer-layout.viewer-keyboard-open #viewer_flash_layer {
  top: 12px;
  z-index: 40;
}

/* ------------------------------------------------------------
   title input keyboard mode (cast only)
------------------------------------------------------------ */
body.cast-live-layout.cast-keyboard-open-for-title .live-overlay-bottom-row {
  display: none !important;
}
```

---

## 確認観点

* title input フォーカス時

  * `keyboard-open` と `cast-keyboard-open-for-title` が付く
  * `cast-live-keyboard-open` は付かない
  * `.live-overlay-bottom-row` だけ消える
  * header / footer / meta / main row は残る

* comment input フォーカス時

  * 今までどおり `keyboard-open` と `cast-live-keyboard-open`
  * header / footer 非表示
  * comment 用圧縮レイアウト

* title → comment、comment → title の連続切り替えで class が混線しない

* viewer は従来どおり

必要なら次に、**コミットメッセージ**と**PR文**までそのまま出します。

---
---

出せます。
この方針なら、**title は standby 中だけ `live-overlay-comment-slot` に出し、スマホキーボード時は comment と同じ `cast-live-keyboard-open` に統一**で進めるのが自然です。現状の `summaryPanel` は standby 用、comment は live/away 用なので、その役割分離を comment slot 側に寄せ直す形です。

今回の変更点は3つです。

* `live.html.erb`

  * title フォームを `live-overlay-main-row` から削除
  * `live-overlay-comment-slot` 内に standby 時だけ title フォームを表示
  * `input-group-text` で「配信タイトル」を表示
* `live_stage_controller.js`

  * `title` 専用分岐を削除
  * title / comment のどちらも cast では `cast-live-keyboard-open`
* `_broadcast_layout.scss`

  * `cast-keyboard-open-for-title` 用 CSS を削除

---

## 1. `app/javascript/controllers/live_stage_controller.js`

```javascript
import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  connect() {
    this._vv = window.visualViewport || null

    this._onFocusIn = this._onFocusIn.bind(this)
    this._onFocusOut = this._onFocusOut.bind(this)
    this._onResize = this._onResize.bind(this)

    this._keyboardCloseTimers = []

    this._applyStaticLayoutVars()

    document.addEventListener("focusin", this._onFocusIn)
    document.addEventListener("focusout", this._onFocusOut)
    window.addEventListener("orientationchange", this._onResize)
    window.addEventListener("resize", this._onResize)
  }

  disconnect() {
    document.removeEventListener("focusin", this._onFocusIn)
    document.removeEventListener("focusout", this._onFocusOut)
    window.removeEventListener("orientationchange", this._onResize)
    window.removeEventListener("resize", this._onResize)

    this._clearKeyboardCloseTimers()
    this._clearKeyboardModeClasses()

    document.documentElement.style.removeProperty("--live-stage-h")
    document.documentElement.style.removeProperty("--viewer-stage-h")
    document.documentElement.style.removeProperty("--app-header-h")
    document.documentElement.style.removeProperty("--app-footer-h")
  }

  _onResize() {
    this._applyStaticLayoutVars()
  }

  _onFocusIn(event) {
    if (!this._isMobileLike()) return

    const target = event.target
    if (!this._isTextInputElement(target)) return

    this._clearKeyboardCloseTimers()

    if (this._isCastLiveLayout()) {
      const role = this._keyboardRoleOf(target)

      if (!["comment", "title"].includes(role)) return

      this._clearKeyboardModeClasses()
      document.body.classList.add("keyboard-open", "cast-live-keyboard-open")
      return
    }

    if (this._isViewerLayout()) {
      if (!this._hasTextInputFocus()) return

      this._clearKeyboardCloseTimers()
      document.body.classList.add("keyboard-open", "viewer-keyboard-open")

      requestAnimationFrame(() => {
        requestAnimationFrame(() => {
          const panel = document.querySelector('[data-controller~="comment-panel"]')
          if (!panel) return

          const controller = this.application.getControllerForElementAndIdentifier(panel, "comment-panel")
          controller?.scrollToBottomNow?.()
        })
      })
    }
  }

  _onFocusOut() {
    requestAnimationFrame(() => {
      if (this._hasTextInputFocus()) return

      const wasCastCommentOpen = document.body.classList.contains("cast-live-keyboard-open")

      this._clearKeyboardModeClasses()

      if (wasCastCommentOpen) {
        this._restoreRootScrollAfterKeyboardClose()
      }
    })
  }

  _restoreRootScrollAfterKeyboardClose() {
    const run = () => {
      const offsetTop = Math.max(0, Math.round(this._vv?.offsetTop || 0))
      if (offsetTop !== 0) return

      const root = document.scrollingElement || document.documentElement

      if (root.scrollTop > 0) {
        root.scrollTop = 0
      }

      if (document.documentElement.scrollTop > 0) {
        document.documentElement.scrollTop = 0
      }

      if (document.body.scrollTop > 0) {
        document.body.scrollTop = 0
      }

      if ((window.scrollY || 0) > 0) {
        window.scrollTo(0, 0)
      }
    }

    requestAnimationFrame(run)
    this._keyboardCloseTimers.push(window.setTimeout(run, 100))
    this._keyboardCloseTimers.push(window.setTimeout(run, 250))
    this._keyboardCloseTimers.push(window.setTimeout(run, 400))
  }

  _clearKeyboardCloseTimers() {
    this._keyboardCloseTimers.forEach((id) => clearTimeout(id))
    this._keyboardCloseTimers = []
  }

  _clearKeyboardModeClasses() {
    document.body.classList.remove(
      "keyboard-open",
      "cast-live-keyboard-open",
      "viewer-keyboard-open"
    )
  }

  _applyStaticLayoutVars() {
    const header = document.getElementById("app_header")
    const footer = document.getElementById("app_footer")

    const headerH = header ? header.getBoundingClientRect().height : 0
    const footerH = footer ? footer.getBoundingClientRect().height : 0
    const viewportH = window.innerHeight || document.documentElement.clientHeight || 0

    document.documentElement.style.setProperty("--live-stage-h", `${Math.max(0, Math.round(viewportH))}px`)
    document.documentElement.style.setProperty(
      "--viewer-stage-h",
      `${Math.max(0, Math.round(viewportH - headerH - footerH))}px`
    )
    document.documentElement.style.setProperty("--app-header-h", `${Math.max(0, Math.round(headerH))}px`)
    document.documentElement.style.setProperty("--app-footer-h", `${Math.max(0, Math.round(footerH))}px`)
  }

  _isMobileLike() {
    return window.matchMedia("(pointer: coarse)").matches || window.innerWidth < 992
  }

  _isCastLiveLayout() {
    return document.body.classList.contains("cast-live-layout")
  }

  _isViewerLayout() {
    return document.body.classList.contains("viewer-layout")
  }

  _keyboardRoleOf(element) {
    return element?.dataset?.keyboardRole || ""
  }

  _isTextInputElement(element) {
    if (!element) return false

    if (element.matches("textarea")) return true
    if (element.matches("[contenteditable='true']")) return true

    if (element.matches("input")) {
      const type = (element.getAttribute("type") || "text").toLowerCase()
      return ![
        "button",
        "checkbox",
        "color",
        "file",
        "hidden",
        "image",
        "radio",
        "range",
        "reset",
        "submit",
      ].includes(type)
    }

    return false
  }

  _hasTextInputFocus() {
    return this._isTextInputElement(document.activeElement)
  }
}
```

---

## 2. `app/views/cast/booths/live.html.erb`

standby 用 title フォームを main row から外し、comment slot に移します。standby 中は title のみ、live/away 中は従来どおり comment section のみ表示です。現状でも title は standby 条件、comment は live/away 条件で出し分けているので、その置き場所だけを変更する形です。

```erb
<div
  class="cast-live-screen"
  data-controller="ivs-publisher live-stage"
  data-ivs-publisher-token-url-value="<%= stream_session_ivs_participant_tokens_path(@stream_session) %>"
  data-ivs-publisher-finish-url-value="<%= finish_cast_stream_session_path(@stream_session) %>"
  data-ivs-publisher-status-url-value="<%= status_cast_booth_path(@booth) %>"
  data-ivs-publisher-meta-display-url-value="<%= meta_display_cast_stream_session_path(@stream_session) %>"
  data-ivs-publisher-mirror-value="true"
  data-ivs-publisher-initial-mode-value="<%= @booth.away? ? "away" : "normal" %>"
  data-ivs-publisher-initial-booth-status-value="<%= @booth.status %>"
  data-ivs-publisher-banuba-client-token-value="<%= @banuba_client_token %>"
  data-ivs-publisher-banuba-sdk-base-url-value="<%= @banuba_sdk_base_url %>"
  data-ivs-publisher-banuba-face-tracker-url-value="<%= @banuba_face_tracker_url %>"
  data-ivs-publisher-banuba-eyes-url-value="<%= @banuba_eyes_url %>"
  data-ivs-publisher-banuba-lips-url-value="<%= @banuba_lips_url %>"
  data-ivs-publisher-banuba-skin-url-value="<%= @banuba_skin_url %>"
  data-ivs-publisher-banuba-background-url-value="<%= @banuba_background_url %>"
  data-ivs-publisher-banuba-hair-url-value="<%= @banuba_hair_url %>"
  data-ivs-publisher-banuba-effect-url-value="<%= @banuba_effect_url %>"
  data-ivs-publisher-banuba-effect-name-value="<%= @banuba_effect_name %>"
  data-ivs-publisher-banuba-beauty-config-url-value="/banuba/effects/beauty_base/config.json"
>
  <div id="live_stage">
    <div id="live_media">
      <div
        class="live-banuba-surface"
        data-ivs-publisher-target="banubaSurface"></div>

      <canvas
        class="d-none"
        data-ivs-publisher-target="canvas"></canvas>
    </div>

    <div id="live_flash_layer">
      <div id="flash_inner">
        <% if notice.present? %>
          <%= render "shared/flash_message", level: "success", message: notice %>
        <% end %>

        <% if alert.present? %>
          <%= render "shared/flash_message", level: "danger", message: alert %>
        <% end %>
      </div>
    </div>

    <div id="live_overlay">
      <div class="live-overlay-ui" data-live-grid-debug="<%= Rails.env.development? ? "1" : "0" %>">
        <div class="live-overlay-meta-slot">
          <div
            class="live-meta-panel d-none"
            data-controller="presence-poll"
            data-ivs-publisher-target="metaPanel"
            data-presence-poll-summary-url-value="<%= presence_summary_stream_session_path(@stream_session) %>"
            data-presence-poll-interval-ms-value="20000"
          >
            <div class="live-meta-layout">
              <div class="live-meta-main">
                <turbo-frame id="stream_meta_display">
                  <%= render "stream_sessions/stream_meta_display",
                            stream_session: @stream_session %>
                </turbo-frame>
              </div>

              <div class="live-meta-side">
                <div class="live-meta-viewer-count">
                  <i class="bi bi-people-fill" aria-hidden="true"></i>
                  <span data-presence-poll-target="count">-</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        <div class="live-overlay-main-row">
          <div class="live-overlay-main-spacer">
            <div class="live-standby-actions-panel d-none" data-ivs-publisher-target="summaryPanel"></div>
          </div>

          <div class="live-overlay-drink-slot">
            <%= turbo_stream_from [@stream_session, :cast_pending_drink_orders] %>

            <div class="live-drink-panel d-none" data-ivs-publisher-target="drinkPanel">
              <div class="live-drink-panel-header">ドリンク</div>

              <div class="live-drink-panel-body">
                <div id="cast-pending-drink-orders-container">
                  <%= render "cast/stream_sessions/pending_drink_orders", stream_session: @stream_session %>
                </div>
              </div>
            </div>
          </div>
        </div>

        <div class="live-overlay-bottom-row">
          <div class="live-overlay-comment-slot">
            <% if @booth.standby? && @booth.current_stream_session_id == @stream_session.id %>
              <div id="cast_title_section">
                <div class="live-comment-panel" data-ivs-publisher-target="summaryPanel">
                  <div class="live-comment-form">
                    <%= form_with model: @stream_session,
                                  url: metadata_cast_stream_session_path(@stream_session),
                                  method: :patch,
                                  class: "live-standby-actions-form" do |f| %>
                      <div class="input-group">
                        <span class="input-group-text">配信タイトル</span>
                        <%= f.text_field :title,
                                         class: "form-control",
                                         maxlength: 64,
                                         placeholder: "未入力ならブース名が表示されます",
                                         aria: { label: "タイトル" },
                                         data: { keyboard_role: "title" } %>
                        <%= f.submit "設定する", class: "btn btn-primary" %>
                      </div>
                    <% end %>
                  </div>
                </div>
              </div>
            <% end %>

            <% if @booth.live? || @booth.away? %>
              <%= turbo_stream_from [@stream_session, :comments] %>

              <div id="cast_comment_section">
                <%= render "cast/booths/comment_section",
                          booth: @booth,
                          stream_session: @stream_session,
                          comments: @comments %>
              </div>
            <% end %>
          </div>

          <div class="live-overlay-ops-slot">
            <div class="live-ops-panel d-none" data-ivs-publisher-target="opsPanel">
              <div class="live-ops-buttons">
                <%= form_with url: status_cast_booth_path(@booth, to: :away),
                              method: :patch,
                              class: "live-ops-form",
                              data: { action: "turbo:submit-end->ivs-publisher#onBoothStatusPatched" } do %>
                  <button type="submit"
                          class="live-ops-btn"
                          data-ivs-publisher-target="cameraOffBtn"
                          aria-label="カメラをオフにする"
                          title="カメラON">
                    <i class="bi bi-camera-video-fill" aria-hidden="true"></i>
                  </button>
                <% end %>

                <%= form_with url: status_cast_booth_path(@booth, to: :live),
                              method: :patch,
                              class: "live-ops-form",
                              data: { action: "turbo:submit-end->ivs-publisher#onBoothStatusPatched" } do %>
                  <button type="submit"
                          class="live-ops-btn"
                          data-ivs-publisher-target="cameraOnBtn"
                          aria-label="カメラをオンにする"
                          title="カメラOFF">
                    <i class="bi bi-camera-video-off-fill" aria-hidden="true"></i>
                  </button>
                <% end %>

                <button type="button"
                        class="live-ops-btn"
                        data-action="ivs-publisher#toggleMic"
                        data-ivs-publisher-target="micBtn"
                        aria-label="マイクを切り替える"
                        title="マイク切替">
                  <i class="bi bi-mic-fill" data-ivs-publisher-target="micIcon" aria-hidden="true"></i>
                </button>

                <button type="button"
                        class="live-ops-btn"
                        data-action="ivs-publisher#openEffectPanel"
                        data-ivs-publisher-target="effectOpenBtn"
                        aria-label="加工モードを選択する"
                        title="加工モード選択">
                  <i class="bi bi-stars" aria-hidden="true"></i>
                </button>

                <button type="button"
                        class="live-ops-btn d-none"
                        data-action="ivs-publisher#openBeautyPanel"
                        data-ivs-publisher-target="beautyAdjustBtn"
                        aria-label="Beauty 設定を開く"
                        title="Beauty 調整">
                  <i class="bi bi-sliders" aria-hidden="true"></i>
                </button>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <%= render "cast/booths/effect_panel", effects: @effects %>
    <%= render "cast/booths/beauty_panel" %>
  </div>

  <footer id="app_footer" class="cast-live-footer border-top bg-white fixed-bottom">
    <div class="container py-2">
      <div class="d-flex justify-content-between align-items-center">
        <div class="d-flex gap-3">
          <%= link_to "ホーム", root_path, class: "btn btn-sm btn-outline-secondary" %>
          <%= link_to "お気に入り", favorites_booths_path, class: "btn btn-sm btn-outline-secondary" %>
        </div>

        <div class="d-flex gap-2">
          <button type="button"
                  class="btn btn-sm btn-primary"
                  data-action="ivs-publisher#startBroadcast"
                  data-ivs-publisher-target="startBtn"
                  data-label-normal="配信開始"
                  data-label-resume="配信に戻る">
            配信開始
          </button>

          <button type="button"
                  class="btn btn-sm btn-danger d-none"
                  data-action="ivs-publisher#endBroadcast"
                  data-ivs-publisher-target="endBtn">
            配信終了
          </button>
        </div>

        <div class="d-flex gap-2 align-items-center">
          <%= link_to "ダッシュボード",
                dashboard_path_for(current_user),
                class: "btn btn-sm btn-outline-secondary" %>

          <% if user_signed_in? %>
            <%= button_to "ログアウト",
                  destroy_user_session_path,
                  method: :delete,
                  class: "btn btn-sm btn-outline-secondary" %>
          <% end %>
        </div>
      </div>
    </div>
  </footer>
</div>
```

---

## 3. `app/assets/stylesheets/components/_broadcast_layout.scss`

title 専用モードを削除します。comment 用 keyboard モードだけ残します。現状の keyboard レイアウトは `cast-live-keyboard-open` で bottom row に寄せる実装なので、title を comment slot に移した今回の方針と整合します。

削除対象はこれです。

```scss
/* ------------------------------------------------------------
   title input keyboard mode (cast only)
------------------------------------------------------------ */
body.cast-live-layout.cast-keyboard-open-for-title .live-overlay-bottom-row {
  display: none !important;
}
```

---

## 4. `app/views/stream_sessions/comments/_form.html.erb`

これはそのままでOKです。
すでに `data-keyboard-role="comment"` を入れている前提なら変更不要です。

```erb
<turbo-frame id="comment_form">
  <% if local_assigns[:error_message].present? %>
    <div class="alert alert-danger py-1 mb-2"><%= error_message %></div>
  <% end %>

  <%= form_with url: stream_session_comments_path(stream_session), scope: :comment do |f| %>
    <%= f.text_field :body,
          maxlength: 200,
          required: true,
          autocomplete: "off",
          class: "form-control form-control-sm",
          placeholder: "コメント…",
          data: { keyboard_role: "comment" } %>

    <%= f.submit "送信", class: "btn btn-primary btn-sm" %>
  <% end %>
</turbo-frame>
```

---

## 確認ポイント

* standby 中

  * title フォームだけ見える
  * comment フォームは出ない
  * title input フォーカスで `cast-live-keyboard-open`
* live / away 中

  * title フォームは消える
  * comment セクションだけ見える
* スマホで title input が comment と同じ下寄せ領域で操作できる

1点だけ注意として、`summaryPanel` を同じ画面内で複数要素に付けると、`ivs_publisher_controller.js` 側の target 利用方法によっては単数 target 参照でコケる可能性があります。今回の変更では main row 側に空の `summaryPanel` を残して、bottom row 側に実体を置いているので、もし controller が `summaryPanelTarget` を単数前提で触っているなら、そこだけ確認してください。必要なら次に `ivs_publisher_controller.js` 前提で安全版に整えます。

コミットメッセージとPR文も続けて出せます。
