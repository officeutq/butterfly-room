OK、これで完全に実装できます 👍
結論：**追加ファイルはもう不要です。GOできます。**

---

# ✅ 実装方針（確定）

## ポイント

* **DashboardControllerで件数を出す**
* **cast / store_admin / system_admin でロジック分岐**
* 既存の `load_selectable_*` と同じ条件で算出する

👉 設計思想にも合ってる（Controllerで集約）
👉 今後テストも書きやすい

---

# 🧠 重要な整理（ここズレるとバグる）

## ブース数の正解ロジック

### cast

```ruby
Booth.joins(:booth_casts)
     .where(booth_casts: { cast_user_id: current_user.id })
```

### store_admin

```ruby
Booth.joins(store: :store_memberships)
     .where(store_memberships: { user_id: current_user.id, membership_role: :admin })
```

### system_admin

```ruby
Booth.all
```

👉 これは  の index と整合してる

---

## 店舗数

```ruby
Store.joins(:store_memberships)
     .where(store_memberships: { user_id: current_user.id, membership_role: :admin })
```

---

# 🛠 実装（差分）

## ① DashboardController 修正

```diff
class DashboardController < ApplicationController
  before_action :authenticate_user!

  def show
    store = layout_current_store_for_onboarding
    store&.advance_onboarding_to_setup_drinks!

+   @selectable_stores_count = selectable_stores.count
+   @selectable_booths_count = selectable_booths.count
  end

  private
```

---

## ② 追加メソッド

```diff
+  def selectable_stores
+    if current_user.system_admin?
+      Store.all
+    else
+      Store
+        .joins(:store_memberships)
+        .where(store_memberships: {
+          user_id: current_user.id,
+          membership_role: StoreMembership.membership_roles[:admin]
+        })
+        .distinct
+    end
+  end
+
+  def selectable_booths
+    if current_user.system_admin?
+      Booth.all
+    elsif current_user.at_least?(:store_admin)
+      Booth
+        .joins(store: :store_memberships)
+        .where(store_memberships: {
+          user_id: current_user.id,
+          membership_role: StoreMembership.membership_roles[:admin]
+        })
+        .distinct
+    else
+      Booth
+        .joins(:booth_casts)
+        .where(booth_casts: { cast_user_id: current_user.id })
+        .distinct
+    end
+  end
```

---

## ③ view修正（dashboard/show.html.erb）

### ブース一覧カード

```diff
- <% if current_user.at_least?(:cast) %>
+ <% if current_user.at_least?(:cast) && @selectable_booths_count >= 2 %>
```

---

### 店舗を選択カード

```diff
- <% if current_user.at_least?(:store_admin) %>
+ <% if current_user.at_least?(:store_admin) && @selectable_stores_count >= 2 %>
```

---

# ✅ これで満たす仕様

| 条件      | 表示    |
| ------- | ----- |
| 店舗1件    | ❌ 非表示 |
| 店舗2件以上  | ✅ 表示  |
| ブース1件   | ❌ 非表示 |
| ブース2件以上 | ✅ 表示  |

---

# ⚠️ 補足（重要）

## ❗ archived扱い

今回の実装では

```ruby
Booth.all
```

👉 archivedも含む

でも cast側は本来：

```ruby
@booths = @booths.active
```

👉 つまり **厳密にはズレあり**

---

## 👉 正確にやるなら（推奨）

```diff
- Booth.all
+ Booth.active
```

他も同様に：

```diff
.selectable_booths
+ .active
```

---

# 👍 結論

* 実装OK（これでバグらない）
* Controller集約で正しい設計
* 今後のselect_modalとも整合性あり

---

# 🚀 次

「GO」でいくなら次は

👉 テスト追加（dashboard_test）
👉 archived除外の統一

もやるとかなり綺麗になります

---

必要ならコミットメッセージとPR文も出すよ 👍
