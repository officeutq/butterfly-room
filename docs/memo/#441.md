了解です。
今回は「新規実装」よりも、**既存の精算・振込設定機能を current_store 前提の独立画面へ整理し直す**対応です。

実装方針はこうです。

* `admin/stores/:store_id/payout_account` のネストルートをやめる
* `admin/payout_account` の singular resource に変更する
* `Admin::StorePayoutAccountsController` は `params[:store_id]` ではなく `current_store` を使う
* `require_current_store!` を使って、店舗未選択時は既存どおり店舗選択画面へ誘導する
* ダッシュボードの「精算・振込設定」リンクは新URLへ向ける
* 店舗選択後の `return_to_key: "payout_account_edit"` も新URLへ向ける
* `admin/stores/edit` から精算・振込設定カードを削除する
* 未設定判定や保存ロジックは既存のまま活かす

これで、Issue #441 の完了条件は素直に満たせます。

以下、差し替え後全文です。

---

### `config/routes.rb`

```ruby
Rails.application.routes.draw do
  get "up" => "rails/health#show", as: :rails_health_check

  devise_for :users, skip: %i[registrations]

  root "home#show"

  if Rails.env.development?
    namespace :dev do
      resource :banuba_verification, only: %i[show], controller: "banuba_verifications"
    end
  end

  # --- Common dashboard (login required) ---
  get "/dashboard", to: "dashboard#show", as: :dashboard

  # --- Common profile (login required) ---
  resource :profile, only: %i[edit update]

  # --- Public user profiles (login required) ---
  resources :users, only: %i[show]

  # --- Public (customer registration) ---
  get  "/sign_up", to: "customers/registrations#new",    as: :sign_up
  post "/sign_up", to: "customers/registrations#create"

  # --- Cast registration (invite only) ---
  get  "/cast/sign_up", to: "casts/registrations#new",    as: :cast_sign_up
  post "/cast/sign_up", to: "casts/registrations#create"

  # --- StoreAdmin registration (invite only) ---
  get  "/store_admin/sign_up", to: "store_admins/registrations#new",    as: :store_admin_sign_up
  post "/store_admin/sign_up", to: "store_admins/registrations#create"

  # --- Public (store registration) ---
  namespace :stores do
    get  :new_registration, to: "registrations#new"
    post :registrations,    to: "registrations#create"
  end

  # --- Public (cast invitation) ---
  get  "/cast_invitations/:token", to: "cast_invitations#show",   as: :cast_invitation
  post "/cast_invitations/:token/accept", to: "cast_invitations#accept", as: :accept_cast_invitation

  # --- Public (store_admin invitation) ---
  get  "/store_admin_invitations/:token", to: "store_admin_invitations#show",   as: :store_admin_invitation
  post "/store_admin_invitations/:token/accept", to: "store_admin_invitations#accept", as: :accept_store_admin_invitation

  # --- Favorites (login required) ---
  namespace :favorites do
    resources :booths, only: %i[index]
    resources :stores, only: %i[index]
  end

  # --- Customer (login required) ---
  resources :stores, only: %i[show] do
    resource :favorite, only: %i[create destroy], controller: "favorites/stores"
  end

  # --- Public (customer) ---
  resources :booths, only: %i[show] do
    member do
      get :enter
      post :enter_as_cast
    end

    resource :favorite, only: %i[create destroy], controller: "favorites/booths"
  end

  resources :stream_sessions, only: [] do
    resources :comments, only: %i[create], module: :stream_sessions
    resources :drink_orders, only: %i[create], module: :stream_sessions
    resources :ivs_participant_tokens, only: %i[create], module: :stream_sessions

    resource :presence, only: [], module: :stream_sessions do
      post :ping
    end

    get :presence_summary, on: :member
  end

  namespace :wallet do
    resources :purchases, only: %i[new create]
  end

  get  "/checkout/return", to: "checkout#return"
  post "/webhooks/stripe", to: "webhooks/stripe#create"

  # --- Cast ---
  namespace :cast do
    resource :current_booth, only: %i[create]

    resources :booths, only: %i[index edit update] do
      get :live, on: :member
      patch :status, on: :member
      resources :stream_sessions, only: %i[create], module: :booths
    end

    resources :stream_sessions, only: %i[show] do
      post :finish, on: :member
      get  :pending_drink_orders, on: :member
      get  :meta_modal, on: :member
      get  :meta_display, on: :member

      patch :metadata, on: :member
    end

    resources :drink_orders, only: [] do
      post :consume, on: :member
    end
  end

  # --- Store Admin ---
  namespace :admin do
    resources :stores, only: %i[index edit update]
    resource :payout_account, only: %i[edit update], controller: "store_payout_accounts"
    resource :current_store, only: %i[create], controller: "current_stores"

    resources :booths, only: %i[index show new edit create update] do
      member do
        get :watch
        patch :archive
        post :force_end
      end
    end

    resources :booth_casts, only: %i[index create]

    resources :drink_items, only: %i[index create update destroy]
    resources :store_bans, only: %i[index create destroy]

    resources :casts, only: %i[index destroy] do
      collection do
        post :invite
        post :invite_store_admin
      end
    end

    get "/cast_metrics", to: "metrics#cast"

    resources :settlements, only: %i[index show]
  end

  # --- System Admin ---
  namespace :system_admin do
    resources :referral_codes, only: %i[index new create edit update]
    resources :users, only: %i[index new create edit update destroy]

    resources :settlements, only: %i[index show] do
      collection do
        get  "manual/new",     to: "settlements#new_manual",     as: :new_manual
        post "manual/preview", to: "settlements#preview_manual", as: :preview_manual
        post "manual",         to: "settlements#create_manual",  as: :create_manual

        post :export_csv
      end

      member do
        post :confirm
        post :mark_paid
      end
    end

    resources :settlement_exports, only: %i[index show create]
  end
end
```

---

### `app/controllers/admin/store_payout_accounts_controller.rb`

```ruby
# frozen_string_literal: true

module Admin
  class StorePayoutAccountsController < Admin::BaseController
    before_action :require_current_store!
    before_action :set_store
    before_action :authorize_store!

    def edit
      @active_payout_account = @store.active_payout_account

      # 事故防止：既存口座情報をフォームにプリフィルしない（フル表示回避）
      @payout_account = @store.store_payout_accounts.new(
        payout_method: :manual_bank,
        status: :active
      )
    end

    def update
      @active_payout_account = @store.active_payout_account

      @payout_account = @store.store_payout_accounts.new(payout_account_params)
      @payout_account.payout_method = :manual_bank
      @payout_account.status = :active
      @payout_account.updated_by_user = current_user

      # 新規が valid でない限り、既存 active を触らない（事故防止）
      unless @payout_account.valid?
        render :edit, status: :unprocessable_entity
        return
      end

      StorePayoutAccount.transaction do
        @active_payout_account&.update!(status: :inactive, updated_by_user: current_user)
        @payout_account.save!
      end

      redirect_to edit_admin_payout_account_path, notice: "精算・振込設定を更新しました"
    end

    private

    def set_store
      @store = current_store
    end

    def authorize_store!
      return if current_user.system_admin?

      ok = StoreMembership.admin_only.exists?(user_id: current_user.id, store_id: @store.id)
      head :forbidden unless ok
    end

    def payout_account_params
      params.require(:store_payout_account).permit(
        :bank_code,
        :branch_code,
        :account_type,
        :account_number,
        :account_holder_kana
      )
    end
  end
end
```

---

### `app/views/admin/store_payout_accounts/edit.html.erb`

```erb
<% content_for :title, "精算・振込設定" %>

<div style="max-width: 720px;">
  <h1 class="h4 mb-3">精算・振込設定</h1>

  <div class="alert alert-secondary" role="alert">
    <div>※振込先口座は精算処理に利用されます。誤りがあると入金できない場合があります。</div>
    <div class="mt-1">※この画面では口座の照会や任意の送金操作はできません（精算は月次で行われます）。</div>
  </div>

  <div class="card mb-4">
    <div class="card-header">現在の設定状況</div>
    <div class="card-body">
      <% if @active_payout_account.present? && @store.payout_account_configured? %>
        <dl class="row mb-0">
          <dt class="col-sm-4">銀行コード</dt>
          <dd class="col-sm-8"><%= @active_payout_account.bank_code %></dd>

          <dt class="col-sm-4">支店コード</dt>
          <dd class="col-sm-8"><%= @active_payout_account.branch_code %></dd>

          <dt class="col-sm-4">口座種別</dt>
          <dd class="col-sm-8">
            <%= @active_payout_account.ordinary? ? "普通" : "当座" %>
          </dd>

          <dt class="col-sm-4">口座番号</dt>
          <dd class="col-sm-8">
            <% last4 = @store.payout_account_last4 %>
            <%= last4.present? ? "****#{last4}" : "-" %>
          </dd>

          <dt class="col-sm-4">口座名義カナ</dt>
          <dd class="col-sm-8"><%= truncate(@active_payout_account.account_holder_kana.to_s, length: 32) %></dd>
        </dl>
      <% else %>
        <div class="text-muted">未設定</div>
      <% end %>
    </div>
  </div>

  <div class="card">
    <div class="card-header">口座情報（manual_bank）</div>
    <div class="card-body">
      <%= form_with model: @payout_account,
            url: admin_payout_account_path,
            method: :patch do |f| %>

        <% if @payout_account.errors.any? %>
          <div class="alert alert-danger" role="alert">
            <ul class="mb-0">
              <% @payout_account.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="mb-3">
          <%= f.label :bank_code, "銀行コード（4桁）", class: "form-label" %>
          <%= f.text_field :bank_code, class: "form-control", inputmode: "numeric", autocomplete: "off" %>
        </div>

        <div class="mb-3">
          <%= f.label :branch_code, "支店コード（3桁）", class: "form-label" %>
          <%= f.text_field :branch_code, class: "form-control", inputmode: "numeric", autocomplete: "off" %>
        </div>

        <div class="mb-3">
          <%= f.label :account_type, "口座種別", class: "form-label" %>
          <%= f.select :account_type,
                options_for_select([["普通", "ordinary"], ["当座", "current"]], @payout_account.account_type),
                { include_blank: true },
                class: "form-select" %>
        </div>

        <div class="mb-3">
          <%= f.label :account_number, "口座番号（7桁）", class: "form-label" %>
          <%= f.text_field :account_number, class: "form-control", inputmode: "numeric", autocomplete: "off" %>
          <div class="form-text">※安全のため、現在の口座番号は表示しません。変更時は再入力してください。</div>
        </div>

        <div class="mb-3">
          <%= f.label :account_holder_kana, "口座名義カナ", class: "form-label" %>
          <%= f.text_field :account_holder_kana, class: "form-control", autocomplete: "off" %>
        </div>

        <div class="d-flex gap-2">
          <%= f.submit "更新する", class: "btn btn-primary" %>
          <%= link_to "戻る", dashboard_path, class: "btn btn-outline-secondary" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
```

---

### `app/controllers/admin/current_stores_controller.rb`

```ruby
# frozen_string_literal: true

module Admin
  class CurrentStoresController < Admin::BaseController
    def create
      store_id = params.require(:store_id)

      if current_user.system_admin?
        unless Store.exists?(id: store_id)
          session.delete(:current_store_id)
          redirect_to admin_stores_path, alert: "選択できない店舗です"
          return
        end
      else
        # session 改ざん対策：必ず membership チェック
        unless StoreMembership.exists?(
          user_id: current_user.id,
          store_id: store_id,
          membership_role: StoreMembership.membership_roles[:admin]
        )
          session.delete(:current_store_id)
          redirect_to admin_stores_path, alert: "選択できない店舗です"
          return
        end
      end

      session[:current_store_id] = store_id
      session.delete(:current_booth_id)

      redirect_to resolve_redirect_path(store_id), notice: "店舗を切り替えました"
    rescue ActionController::ParameterMissing
      redirect_to admin_stores_path, alert: "店舗を選択してください"
    end

    private

    # Issue #270: 店舗選択後の遷移先決定
    #
    # 1) return_to_key（allowlist）
    # 2) return_to（/admin/ の安全な相対パスのみ）
    # 3) session[:admin_return_to]（同上）
    # 4) dashboard_path（既存UX/既存テストと整合）
    def resolve_redirect_path(store_id)
      store = Store.find_by(id: store_id)

      # 1) return_to_key
      key = params[:return_to_key].presence
      if key.present?
        path = resolve_return_to_key(key, store)
        return path if path.present?
      end

      # 2) return_to
      rt = safe_return_to(params[:return_to])
      return rt if rt.present?

      # ★A: 選択画面（/admin/stores）からのPOSTは session 戻りを使わない
      if request.referer.to_s.start_with?(admin_stores_url)
        return dashboard_path
      end

      # 3) session（直前ページ）
      session_rt = safe_return_to(session[:admin_return_to])
      return session_rt if session_rt.present?

      # 4) fallback
      dashboard_path
    end

    # allowlist方式（推奨）
    def resolve_return_to_key(key, store)
      return nil if store.blank?

      case key.to_s
      when "payout_account_edit"
        edit_admin_payout_account_path
      when "store_edit"
        edit_admin_store_path(store)
      else
        nil
      end
    end

    # open redirect対策：/admin/ で始まる相対パスのみ許可
    def safe_return_to(value)
      s = value.to_s
      return nil if s.blank?

      # 相対パスのみ許可（open redirect対策）
      return nil unless s.start_with?("/")
      return nil if s.start_with?("//")
      return nil if s.include?("\n") || s.include?("\r")
      return nil if s.include?("\0")

      # ループ/ノイズ防止：選択画面と選択POSTには戻さない
      return nil if s == "/admin/stores"
      return nil if s == "/admin/current_store"

      s
    end
  end
end
```

---

### `app/views/dashboard/show.html.erb`

```erb
<div style="max-width: 960px;">
  <div class="d-flex justify-content-between align-items-start gap-3">
    <div>
      <h1 class="mb-1">ダッシュボード</h1>
      <p class="text-muted mb-0">ロール別リンク</p>
    </div>
  </div>

  <hr class="my-4">

  <% store = layout_current_store %>
  <% booth = layout_current_booth %>

  <%#=== system_admin ===%>
  <% if current_user.system_admin? %>
    <h2 class="h5 mb-3">system_admin</h2>

    <ul class="list-group mb-4">
      <li class="list-group-item">
        <%= link_to "ユーザー管理", system_admin_users_path, class: "text-decoration-none" %>
      </li>
      <li class="list-group-item">
        <%= link_to "紹介コード管理", system_admin_referral_codes_path, class: "text-decoration-none" %>
      </li>

      <li class="list-group-item">
        <%= link_to "マニュアル精算（テスト）", "/system_admin/settlements/manual/new", class: "text-decoration-none" %>
      </li>

      <li class="list-group-item">
        <%= link_to "振込CSV（住信SBI）", system_admin_settlement_exports_path, class: "text-decoration-none" %>
      </li>

      <li class="list-group-item">
        <%= link_to "精算一覧", system_admin_settlements_path, class: "text-decoration-none" %>
      </li>
    </ul>

    <ul class="list-group mb-4">
      <li class="list-group-item">
        <%= link_to "店舗一覧", admin_stores_path, class: "text-decoration-none" %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "店舗設定", edit_admin_store_path(store), class: "text-decoration-none" %>
        <% else %>
          <%= link_to "店舗設定（先に店舗を選択）",
                admin_stores_path(return_to_key: "store_edit"),
                class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "ブース一覧", admin_booths_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "ブース一覧（先に店舗を選択）",
                admin_stores_path(return_to: admin_booths_path),
                class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if booth.present? %>
          <%= link_to "ブースを編集", edit_cast_booth_path(booth), class: "text-decoration-none" %>
        <% else %>
          <%= link_to "ブースを編集（先にブースを選択）",
                cast_booths_path,
                class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "キャスト管理", admin_casts_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "キャスト管理（先に店舗を選択）",
                admin_stores_path(return_to: admin_casts_path),
                class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "ブースにキャストを紐づける", admin_booth_casts_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "ブースにキャストを紐づける（先に店舗を選択）",
                admin_stores_path(return_to: admin_booth_casts_path),
                class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "キャスト別数値一覧（直近30日）", admin_cast_metrics_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "キャスト別数値一覧（直近30日）（先に店舗を選択）",
                admin_stores_path(return_to: admin_cast_metrics_path),
                class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "ドリンクメニュー", admin_drink_items_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "ドリンクメニュー（先に店舗を選択）",
                admin_stores_path(return_to: admin_drink_items_path),
                class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "店舗BAN", admin_store_bans_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "店舗BAN（先に店舗を選択）",
                admin_stores_path(return_to: admin_store_bans_path),
                class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to edit_admin_payout_account_path, class: "text-decoration-none" do %>
            精算・振込設定
            <% if store.payout_account_unconfigured? %>
              <span class="badge text-bg-warning ms-2">未設定</span>
            <% end %>
          <% end %>
        <% else %>
          <%= link_to "精算・振込設定（先に店舗を選択）",
                admin_stores_path(return_to_key: "payout_account_edit"),
                class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "精算（予定・履歴）", admin_settlements_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "精算（予定・履歴）（先に店舗を選択）",
                admin_stores_path(return_to: admin_settlements_path),
                class: "text-decoration-none" %>
        <% end %>
      </li>
    </ul>

    <ul class="list-group mb-4">
      <li class="list-group-item">
        <%= link_to "プロフィール編集", edit_profile_path, class: "text-decoration-none" %>
      </li>
    </ul>

  <%#=== store_admin ===%>
  <% elsif current_user.store_admin? %>
    <h2 class="h5 mb-3">store_admin</h2>

    <ul class="list-group mb-4">
      <li class="list-group-item">
        <%= link_to "店舗一覧", admin_stores_path, class: "text-decoration-none" %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "店舗設定", edit_admin_store_path(store), class: "text-decoration-none" %>
        <% else %>
          <%= link_to "店舗設定（先に店舗を選択）",
                admin_stores_path(return_to_key: "store_edit"),
                class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "ブース一覧", admin_booths_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "ブース一覧（先に店舗を選択）",
                admin_stores_path(return_to: admin_booths_path),
                class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if booth.present? %>
          <%= link_to "ブースを編集", edit_cast_booth_path(booth), class: "text-decoration-none" %>
        <% else %>
          <%= link_to "ブースを編集（先にブースを選択）",
                cast_booths_path,
                class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "キャスト管理", admin_casts_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "キャスト管理（先に店舗を選択）",
                admin_stores_path(return_to: admin_casts_path),
                class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "ブースにキャストを紐づける", admin_booth_casts_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "ブースにキャストを紐づける（先に店舗を選択）",
                admin_stores_path(return_to: admin_booth_casts_path),
                class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "キャスト別数値一覧（直近30日）", admin_cast_metrics_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "キャスト別数値一覧（直近30日）（先に店舗を選択）",
                admin_stores_path(return_to: admin_cast_metrics_path),
                class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "ドリンクメニュー", admin_drink_items_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "ドリンクメニュー（先に店舗を選択）",
                admin_stores_path(return_to: admin_drink_items_path),
                class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "店舗BAN", admin_store_bans_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "店舗BAN（先に店舗を選択）",
                admin_stores_path(return_to: admin_store_bans_path),
                class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to edit_admin_payout_account_path, class: "text-decoration-none" do %>
            精算・振込設定
            <% if store.payout_account_unconfigured? %>
              <span class="badge text-bg-warning ms-2">未設定</span>
            <% end %>
          <% end %>
        <% else %>
          <%= link_to "精算・振込設定（先に店舗を選択）",
                admin_stores_path(return_to_key: "payout_account_edit"),
                class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "精算（予定・履歴）", admin_settlements_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "精算（予定・履歴）（先に店舗を選択）",
                admin_stores_path(return_to: admin_settlements_path),
                class: "text-decoration-none" %>
        <% end %>
      </li>
    </ul>

    <ul class="list-group mb-4">
      <li class="list-group-item">
        <%= link_to "プロフィール編集", edit_profile_path, class: "text-decoration-none" %>
      </li>
    </ul>

  <%#=== cast ===%>
  <% elsif current_user.cast? %>
    <h2 class="h5 mb-3">cast</h2>

    <ul class="list-group mb-4">
      <li class="list-group-item">
        <%= link_to "ブース一覧", cast_booths_path, class: "text-decoration-none" %>
      </li>

      <li class="list-group-item">
        <% if booth.present? %>
          <%= link_to "ブースを編集", edit_cast_booth_path(booth), class: "text-decoration-none" %>
        <% else %>
          <%= link_to "ブースを編集（先にブースを選択）",
                cast_booths_path,
                class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <%= link_to "プロフィール編集", edit_profile_path, class: "text-decoration-none" %>
      </li>
    </ul>

  <%#=== customer ===%>
  <% else %>
    <h2 class="h5 mb-3">customer</h2>

    <ul class="list-group mb-4">
      <li class="list-group-item">
        <%= link_to "プロフィール編集", edit_profile_path, class: "text-decoration-none" %>
      </li>
    </ul>
  <% end %>
</div>
```

---

### `app/views/admin/stores/edit.html.erb`

```erb
<h1>店舗設定</h1>

<%= form_with model: [:admin, @store] do |f| %>
  <% if @store.errors.any? %>
    <div class="alert alert-danger" role="alert">
      <ul class="mb-0">
        <% @store.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= f.label :name, "店舗名（必須）", class: "form-label" %>
    <%= f.text_field :name, class: "form-control", required: true %>
  </div>

  <div class="mb-3">
    <%= f.label :description, "概要", class: "form-label" %>
    <%= f.text_area :description, class: "form-control", rows: 6 %>
    <div class="form-text">最大1000文字</div>
  </div>

  <div class="mb-3">
    <%= f.label :area, "地域", class: "form-label" %>
    <%= f.text_field :area, class: "form-control" %>
    <div class="form-text">最大50文字（例：渋谷 / 福岡市中央区 など）</div>
  </div>

  <div class="mb-3">
    <%= f.label :business_type, "業態", class: "form-label" %>
    <%= f.select :business_type,
          options_for_select(Store.business_type_select_options, @store.business_type),
          { include_blank: true },
          class: "form-select" %>
  </div>

  <div class="mb-3">
    <%= f.label :thumbnail, "サムネイル画像", class: "form-label" %>

    <% if @store.thumbnail.attached? %>
      <div class="mb-2">
        <%= image_tag @store.thumbnail, style: "max-width: 240px; height: auto;" %>
      </div>

      <div class="form-check mb-2">
        <%= check_box_tag "store[remove_thumbnail]", "1", false,
                          class: "form-check-input", id: "remove_thumbnail" %>
        <%= label_tag "remove_thumbnail", "画像なし", class: "form-check-label" %>
      </div>

      <div class="text-muted small mb-2">
        画像を差し替える場合は新しいファイルを選択してください
      </div>
    <% end %>

    <%= f.file_field :thumbnail, class: "form-control" %>
    <div class="form-text">png / jpg / webp、最大 1024x1024 に縮小保存</div>
  </div>

  <div class="d-flex gap-2">
    <%= f.submit "更新する", class: "btn btn-primary" %>
    <%= link_to "戻る", dashboard_path, class: "btn btn-outline-secondary" %>
  </div>
<% end %>
```

---

## 補足

この変更で、URL は次の形になります。

* 編集画面: `/admin/payout_account/edit`
* 更新: `/admin/payout_account`

つまり、**選択中の店舗に対する精算・振込設定画面** という意味が URL でも明確になります。

## 動作確認ポイント

最低限ここを見れば十分です。

* ダッシュボードで store 選択済みなら「精算・振込設定」が開ける
* store 未選択なら店舗選択へ飛び、選択後に精算・振込設定へ戻る
* 店舗設定画面から精算・振込設定カードが消えている
* 精算・振込設定の保存が従来どおり成功する
* ダッシュボードの「未設定」バッジが、設定前後で期待どおり切り替わる

必要なら次に、Issue #441 用のコミットメッセージと PR 文もそのまま出せます。
