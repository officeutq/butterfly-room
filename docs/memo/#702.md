完璧、これでいけます 👍
追加で見る必要はありません。このまま設計確定できます。

---

## ① 追加カラム（確定版）

今回入れるのはこれ👇

```ruby
# 基本情報
address :string
phone_number :string
business_hours :string
website_url :string

# SNS
x_url :string
instagram_url :string
tiktok_url :string
youtube_url :string

# Geocoding
latitude :float
longitude :float
```

---

## ② マイグレーション生成（あなたの流儀）

空で作る👇

```bash
docker compose exec app bin/rails g migration AddBasicInfoToStores
```

---

## ③ migration全文（コピペOK）

```ruby
class AddBasicInfoToStores < ActiveRecord::Migration[8.0]
  def change
    change_table :stores, bulk: true do |t|
      # 基本情報
      t.string :address
      t.string :phone_number
      t.string :business_hours
      t.string :website_url

      # SNS
      t.string :x_url
      t.string :instagram_url
      t.string :tiktok_url
      t.string :youtube_url

      # Geocoding
      t.float :latitude
      t.float :longitude
    end
  end
end
```

---

## ④ 補足（重要だけど今はやらない）

### indexつける？

👉 今は不要

理由：

* 距離検索してない
* WHEREで使わない

👉 将来やるなら

```ruby
add_index :stores, [:latitude, :longitude]
```

---

### NOT NULLつける？

👉 今は全部 nullableでOK

理由：

* 段階的リリース
* 既存データある（#704で埋める）

---

### Storeモデル変更いる？

👉 今は不要

```ruby
# 何も書かないでOK
```

Geocoderは #703 でやる

---

### Strong params（重要）

今はまだ触らない 👇

```ruby
def store_params
```

👉 #705（フォーム）で一緒にやる

---

## ⑤ このIssueのゴール

* migration作成
* migrate成功
* schema.rbに反映

👉 ここまででOK

---

## ⑥ 次の流れ

このあと👇

1. #702 → 今やる
2. #703 → geocoder
3. #704 → バックフィル

---

## まとめ

👉 今やることはこれだけ

* migration作る
* migrateする

---

やったら
👉 `schema.rb` の差分だけ貼ってくれればチェックします 👍
