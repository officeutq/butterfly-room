了解、GOで進めます。ここからは **実装プラン → コピペ用コード → 動作確認 → コミット/PR文** の順で出します。

---

## 実装プラン（最小・表示層のみ）

### Tasks（順序つき）

1. `ApplicationHelper#booth_status_badge` を **2分類**に変更

   * `live / away` → 「配信中」(danger)
   * `standby / offline` → 「オフライン」(secondary)
   * 既存の `return "" if booth.blank?` は維持
2. 既存テストを実行して通す（Home検索テストは影響なしの想定）

### Done条件への対応表

* Home一覧のバッジが2種類のみ → ✅ helperで2分類
* live/away → 配信中 → ✅
* standby/offline → オフライン → ✅
* 視聴ページ挙動に影響しない → ✅ show側で helper未使用を確認済み
* 既存テストが通る → ✅ 文言アサートがない限り通る（最終は実行で確認）

---

## コピペ用コード（変更箇所：1ファイルのみ）

### `app/helpers/application_helper.rb`（`booth_status_badge` を差し替え）

```ruby
# frozen_string_literal: true

module ApplicationHelper
  def booth_status_badge(booth)
    return "" if booth.blank?

    live_like = booth.live? || booth.away?

    label = live_like ? "配信中" : "オフライン"
    klass = live_like ? "text-bg-danger" : "text-bg-secondary"

    content_tag(:span, label, class: "badge #{klass}")
  end

  def display_name_or_email(user)
    return "" if user.blank?

    user.display_name.presence || user.email
  end
end
```

ポイント：

* `enum` の状態増減に強い（`live? / away?` を基準）
* 仕様どおり Home では2種類だけ
* `StreamSession` や show 側ロジックには一切触れない

---

## 動作確認（Issue本文の確認手順をそのまま）

1. `booth.status = live` のカード → **配信中**
2. `booth.status = away` のカード → **配信中**
3. `booth.status = standby` のカード → **オフライン**
4. `booth.status = offline` のカード → **オフライン**
5. `booths#show` のUIは従来どおり（そもそも helper 使ってない）

テスト（docker compose 経由）

* `docker compose exec app bash -lc "RAILS_ENV=test bin/rails test"`

---

## コミットメッセージ案

* `Improve: Homeの配信状態バッジを2種類に統一 (#193)`
* `Improve: booth_status_badge を配信中/オフラインの2分類に変更 (#193)`
* `Improve: Home一覧の状態ラベルを簡略化 (#193)`

---

## PR本文（コピペ用）

**Title**

* `[Improve] Home一覧の配信状態バッジを2種類に統一（配信中 / オフライン） #193`

**変更点**

* Homeのブース一覧で表示している配信状態バッジを、以下の2種類に統一

  * `live / away` → 「配信中」
  * `standby / offline` → 「オフライン」
* 変更は表示層のみ（`ApplicationHelper#booth_status_badge`）で、StreamSessionや状態遷移ロジックは変更なし

**仕様/意図**

* Homeは「視聴入口」であり、`standby / away` のような内部状態を区別する責務は持たないため、UX観点で2分類に丸めた

**影響範囲**

* `ApplicationHelper#booth_status_badge`
* Home一覧の状態表示（呼び出し側は変更なし）

**影響なし（明示）**

* `booths#show` の状態制御/UI（helper未使用のため）
* `booth.status enum` / StreamSession / StoreBanGuard / Turbo Stream 挙動

**確認手順**

* Home一覧で各statusが以下になること

  * live/away → 配信中
  * standby/offline → オフライン
* 視聴ページ（booths#show）の挙動が従来どおりであること
* テスト実行：`RAILS_ENV=test bin/rails test`

---

必要なら、このIssue #193 を **03_基本設計_Phase1.md** に反映する文章（Homeの表示仕様の補足）も用意できます。
