## 実装方針（#320）

* **Turbo Streams の append 先は `target: "comments"`**（=`id="comments"` の要素）なので、Overlay 側に **`#comments` を常設**します（standby でも target は必要、の認識でOK）。
* コメントUIは `.live-overlay-comment-slot` に **可視で配置**し、`.live-offscreen` 側のコメントUI（`turbo_stream_from` と `#cast_comment_section`）は **重複IDを避けるため撤去**します。
* 自動追従は Stimulus（`comment-panel`）で実装：

  * `#comments`（スクロール領域）に `MutationObserver` を付ける
  * ユーザーが最下部付近にいるときだけ `scrollToBottom()`
  * ユーザーが上にスクロールしている間は追従しない

---

## 変更するファイル一覧

* `app/views/cast/booths/live.html.erb`
* `app/views/cast/booths/_comment_section.html.erb`
* `app/javascript/controllers/comment_panel_controller.js`（新規）
* `app/assets/stylesheets/application.scss`
* `app/views/stream_sessions/comments/_form.html.erb`（見た目と操作性の最低限改善）

---

## 実装コード（コピペ用）

### 1) `app/views/cast/booths/live.html.erb`（コメントをOverlayへ移動＆offscreenから撤去）

```erb
<div
  data-controller="ivs-publisher live-stage"
  data-ivs-publisher-token-url-value="<%= stream_session_ivs_participant_tokens_path(@stream_session) %>"
  data-ivs-publisher-finish-url-value="<%= finish_cast_stream_session_path(@stream_session) %>"
  data-ivs-publisher-status-url-value="<%= status_cast_booth_path(@booth) %>"
  data-ivs-publisher-mirror-value="true"
  data-ivs-publisher-initial-mode-value="<%= @booth.away? ? "away" : "normal" %>"
  data-ivs-publisher-initial-booth-status-value="<%= @booth.status %>"
>
  <%# ------------------------------------------------------------
      Stage（映像 + Overlay）
      ------------------------------------------------------------ %>
  <div id="live_stage">
    <div id="live_media">
      <video
        playsinline
        autoplay
        muted
        data-ivs-publisher-target="preview"></video>

      <canvas
        class="d-none"
        width="1280" height="720"
        data-ivs-publisher-target="canvas"></canvas>
    </div>

    <%# ------------------------------------------------------------
        Overlay（映像上にDOMを重ねる）
        - Overlay自体は pointer-events:none
        - 実UIは .live-overlay-ui で pointer-events:auto
        ------------------------------------------------------------ %>
    <div id="live_overlay">
      <div class="live-overlay-ui" data-live-grid-debug="1">
        <div class="live-overlay-meta-slot">
          <div class="live-slot-label">meta slot</div>
        </div>

        <div class="live-overlay-main-row">
          <div class="live-overlay-main-spacer"></div>

          <div class="live-overlay-drink-slot">
            <div class="live-slot-label">drink slot</div>
          </div>
        </div>

        <div class="live-overlay-bottom-row">
          <div class="live-overlay-comment-slot">
            <%# コメントは Overlay に表示する（target: #comments は常設） %>
            <%= turbo_stream_from [@stream_session, :comments] %>
            <%= render "cast/booths/comment_section",
                       booth: @booth,
                       stream_session: @stream_session,
                       comments: @comments %>
          </div>

          <div class="live-overlay-ops-slot">
            <div class="live-slot-label">ops slot</div>
          </div>
        </div>

        <%# --------------------------------------------------------
            既存UIは DOM を残したまま画面外へ退避
            -------------------------------------------------------- %>
        <div class="live-offscreen">
          <%# live は layout の Main が無いので、flash の箱だけはページ側に常設しておく %>
          <div id="flash_inner">
            <% if notice.present? %>
              <div class="alert alert-success" role="alert"><%= notice %></div>
            <% end %>
            <% if alert.present? %>
              <div class="alert alert-danger" role="alert"><%= alert %></div>
            <% end %>
          </div>

          <%# ---- 開発用テキスト（#317では残す） ---- %>
          <h1>Cast Booth（Live）</h1>

          <p>booth: <strong>#<%= @booth.id %> <%= @booth.name %></strong></p>
          <p>status: <strong><%= @booth.status %></strong></p>

          <%= render "cast/booths/stream_meta",
                     booth: @booth,
                     stream_session: @stream_session %>

          <div class="d-flex align-items-center gap-2 flex-wrap">
            <div class="fw-semibold">配信（Real-Time Stage）</div>
            <span class="text-muted small" data-ivs-publisher-target="state">idle</span>

            <% if @booth.standby? %>
              <%= button_to "サマリーへ戻る",
                    finish_cast_stream_session_path(@stream_session),
                    method: :post,
                    class: "btn btn-outline-secondary btn-sm",
                    data: { ivs_publisher_target: "summaryBtn" } %>
            <% end %>
          </div>

          <div class="mt-2 text-danger small"
               data-ivs-publisher-target="error"></div>

          <div class="mt-2 d-flex gap-2 flex-wrap">
            <!-- 離席 -->
            <%= form_with url: status_cast_booth_path(@booth, to: :away),
                          method: :patch,
                          data: { action: "turbo:submit-end->ivs-publisher#onBoothStatusPatched" } do %>
              <button type="submit"
                      class="btn btn-warning btn-sm"
                      data-ivs-publisher-target="awayBtn">
                離席
              </button>
            <% end %>

            <!-- 復帰 -->
            <%= form_with url: status_cast_booth_path(@booth, to: :live),
                          method: :patch,
                          data: { action: "turbo:submit-end->ivs-publisher#onBoothStatusPatched" } do %>
              <button type="submit"
                      class="btn btn-success btn-sm"
                      data-ivs-publisher-target="backBtn">
                復帰
              </button>
            <% end %>

            <!-- 離席中音声 -->
            <div class="form-check form-switch mt-3">
              <input class="form-check-input"
                     type="checkbox"
                     role="switch"
                     id="away-audio"
                     data-ivs-publisher-target="awayAudio"
                     data-action="change->ivs-publisher#toggleAwayAudio"
                     disabled>
              <label class="form-check-label" for="away-audio">
                離席中も音声を送る
              </label>
            </div>
          </div>

          <%= turbo_stream_from [@stream_session, :cast_pending_drink_orders] %>
          <div id="cast-pending-drink-orders-container">
            <%= render "cast/stream_sessions/pending_drink_orders", stream_session: @stream_session %>
          </div>

          <hr>

          <%# comments は Overlay 側へ移動したので、offscreen からは撤去（重複IDを避ける） %>
        </div>
      </div>
    </div>
  </div>

  <footer id="app_footer" class="border-top bg-white fixed-bottom">
    <div class="container py-2">
      <div class="d-flex justify-content-between align-items-center">
        <div class="d-flex gap-3">
          <%= link_to "ホーム", root_path, class: "btn btn-sm btn-outline-secondary" %>
          <%= link_to "お気に入り", favorites_booths_path, class: "btn btn-sm btn-outline-secondary" %>
        </div>

        <div class="d-flex gap-2">
          <button type="button"
                  class="btn btn-sm btn-primary"
                  data-action="ivs-publisher#startBroadcast"
                  data-ivs-publisher-target="startBtn"
                  data-label-normal="配信開始"
                  data-label-resume="配信に戻る">
            配信開始
          </button>

          <button type="button"
                  class="btn btn-sm btn-danger d-none"
                  data-action="ivs-publisher#endBroadcast"
                  data-ivs-publisher-target="endBtn">
            配信終了
          </button>
        </div>

        <div class="d-flex gap-2 align-items-center">
          <%= link_to "ダッシュボード",
                dashboard_path_for(current_user),
                class: "btn btn-sm btn-outline-secondary" %>

          <% if user_signed_in? %>
            <%= button_to "ログアウト",
                  destroy_user_session_path,
                  method: :delete,
                  class: "btn btn-sm btn-outline-secondary" %>
          <% end %>
        </div>
      </div>
    </div>
  </footer>
</div>
```

---

### 2) `app/views/cast/booths/_comment_section.html.erb`（一覧固定高＋フォーム下＋Stimulus）

```erb
<div class="live-comment-panel" data-controller="comment-panel">
  <%# standbyでも Turbo append の target が必要なので、#comments は常に置く %>
  <div id="comments" class="live-comment-list" data-comment-panel-target="list">
    <% if booth.live? || booth.away? %>
      <%= render partial: "comments/comment", collection: comments, as: :comment %>
    <% end %>
  </div>

  <% if booth.live? || booth.away? %>
    <div class="live-comment-form">
      <%= render "stream_sessions/comments/form", stream_session: stream_session %>
    </div>
  <% end %>
</div>
```

---

### 3) `app/javascript/controllers/comment_panel_controller.js`（新規）

```js
import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  static targets = ["list"]

  connect() {
    this._thresholdPx = 24
    this._nearBottom = true

    this._onScroll = () => {
      this._nearBottom = this._isNearBottom()
    }

    this.listTarget.addEventListener("scroll", this._onScroll)

    // 初期表示：最新に寄せる（コメント0でもOK）
    this._scrollToBottom()

    this._observer = new MutationObserver(() => {
      // 直前の状態（ユーザーが下付近にいるか）に基づいて追従
      if (this._nearBottom) this._scrollToBottom()
    })

    this._observer.observe(this.listTarget, { childList: true })
  }

  disconnect() {
    if (this._observer) {
      this._observer.disconnect()
      this._observer = null
    }

    if (this._onScroll) {
      this.listTarget.removeEventListener("scroll", this._onScroll)
      this._onScroll = null
    }
  }

  _isNearBottom() {
    const el = this.listTarget
    const distance = el.scrollHeight - (el.scrollTop + el.clientHeight)
    return distance < this._thresholdPx
  }

  _scrollToBottom() {
    const el = this.listTarget
    el.scrollTop = el.scrollHeight
  }
}
```

---

### 4) `app/assets/stylesheets/application.scss`（コメントパネル用CSSを追記）

`/* debug OFF では完全に枠表示を消す */` の後あたりに追記でOKです。

```scss
/* ------------------------------------------------------------
   live comment panel (#320)
------------------------------------------------------------ */
.live-overlay-comment-slot {
  display: flex;
  align-items: flex-end;
}

.live-comment-panel {
  width: 100%;
  height: 100%;
  display: flex;
  flex-direction: column;

  gap: 8px;
  padding: 10px;

  border-radius: 12px;
  background: rgba(0, 0, 0, 0.45);

  overflow: hidden;
  min-width: 0;
  min-height: 0;
}

/* コメント一覧：固定高さ＋内部スクロール（7行相当） */
.live-comment-list {
  flex: 0 0 auto;
  height: clamp(140px, 18vh, 220px);

  overflow-y: auto;
  overflow-x: hidden;

  overscroll-behavior: contain;
  -webkit-overflow-scrolling: touch;

  min-width: 0;
  min-height: 0;

  padding-right: 4px;
}

/* コメント行：最低限の見た目 */
.live-comment-list .comment {
  font-size: 0.875rem;
  line-height: 1.3;
  padding: 4px 0;
  color: rgba(255, 255, 255, 0.92);
}

.live-comment-list .comment-body {
  word-break: break-word;
}

/* フォーム（常に見える） */
.live-comment-form {
  flex: 0 0 auto;
}

/* フォーム内部の最低限のレイアウト */
.live-comment-panel #comment_form form {
  display: flex;
  gap: 8px;
  align-items: center;
}

.live-comment-panel #comment_form input[type="text"] {
  min-width: 0;
}
```

---

### 5) `app/views/stream_sessions/comments/_form.html.erb`（最低限のUI整形）

```erb
<turbo-frame id="comment_form">
  <% if local_assigns[:error_message].present? %>
    <div class="alert alert-danger py-1 mb-2"><%= error_message %></div>
  <% end %>

  <%= form_with url: stream_session_comments_path(stream_session), scope: :comment do |f| %>
    <%= f.text_field :body,
          maxlength: 200,
          required: true,
          autocomplete: "off",
          autofocus: true,
          class: "form-control form-control-sm",
          placeholder: "コメント…" %>

    <%= f.submit "送信", class: "btn btn-primary btn-sm" %>
  <% end %>
</turbo-frame>
```

---

## Done条件チェック（セルフ確認ポイント）

* live画面でコメントが **Overlay（左下）**に見える
* 一覧は **固定高**で、スクロールは **一覧だけ**
* `CommentNotifier.append` により、新規コメントは `#comments` の末尾に追加される
* 最下部付近にいるときだけ自動追従（上にスクロールしてる間は止まる）
* フォームから送信でき、送信後 `comment_form` が replace されて入力がクリアされる
* 送出ストリームに干渉していない（DOMだけ）

---

必要なら、このあと **コミットメッセージ案 / PR本文（テンプレ＋チェックリスト付き）**も #320 用に出します。
