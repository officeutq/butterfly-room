以下、#152 の **最終実装プラン** と **コピペ用コード一式**です（ルールどおり diff ではなく “貼り替え前提” で出します）。

---

## 実装プラン（最小・安全）

1. **HomeController#show** で `@booths` を用意

* `Booth.active`（アーカイブ除外）
* `includes(:store, { booth_casts: :cast_user }, thumbnail_image_attachment: :blob)`（N+1 回避）
* **customer のみ**：`StoreBan` を使って banned store の booth を **Home から予防除外**（可能な範囲）
* 最終防衛は既存どおり `BoothsController#show + StoreBanGuard` に任せる（迂回不可を維持）

2. `home/show.html.erb` に **ブース一覧セクション**を追加

* Bootstrap の grid + card
* サムネ無しでも崩れない placeholder
* `booth_path(booth)` に遷移できる

3. 表示の共通パーツとして `ApplicationHelper` に **状態badge helper** を追加

* `offline/live/away/standby` を Bootstrap badge に変換（最小）

---

## 変更ファイル一覧

* `app/controllers/home_controller.rb`（更新）
* `app/helpers/application_helper.rb`（更新）
* `app/views/home/show.html.erb`（更新）

---

## コピペ用コード

### 1) `app/controllers/home_controller.rb`（全文置き換え）

```ruby
# frozen_string_literal: true

class HomeController < ApplicationController
  def show
    booths = Booth.active
                 .includes(
                   :store,
                   { booth_casts: :cast_user },
                   thumbnail_image_attachment: :blob
                 )
                 .order(updated_at: :desc, id: :desc)
                 .limit(60)

    # 可能な範囲で予防（ただし最終防衛は BoothsController#show の StoreBanGuard が担保）
    if user_signed_in? && current_user.customer?
      banned_store_ids = StoreBan.where(customer_user_id: current_user.id).select(:store_id)
      booths = booths.where.not(store_id: banned_store_ids)
    end

    @booths = booths
  end
end
```

---

### 2) `app/helpers/application_helper.rb`（末尾に追記 / 無ければ新規作成）

> 既に `ApplicationHelper` があるなら「追記」、無ければこの内容で新規作成してください。

```ruby
# frozen_string_literal: true

module ApplicationHelper
  def booth_status_badge(booth)
    return "" if booth.blank?

    label, klass =
      case booth.status&.to_sym
      when :live
        ["配信中", "text-bg-danger"]
      when :standby
        ["スタンバイ", "text-bg-info"]
      when :away
        ["離席中", "text-bg-warning"]
      else
        ["オフライン", "text-bg-secondary"]
      end

    content_tag(:span, label, class: "badge #{klass}")
  end

  def display_name_or_email(user)
    return "" if user.blank?

    user.display_name.presence || user.email
  end
end
```

---

### 3) `app/views/home/show.html.erb`（全文置き換え）

```erb
<% if user_signed_in? %>
  <div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
      <h1 class="h4 mb-0">ブース一覧</h1>
      <div class="text-muted small">
        ログイン中: <%= current_user.email %>（role: <%= current_user.role %>）
      </div>
    </div>

    <%# --- Booth grid (watch entry) --- %>
    <% if @booths.present? %>
      <div class="row row-cols-1 row-cols-md-3 g-3">
        <% @booths.each do |booth| %>
          <% primary_cast = booth.booth_casts.max_by { |bc| [bc.created_at, bc.id] }&.cast_user %>

          <div class="col">
            <%= link_to booth_path(booth), class: "text-decoration-none text-reset" do %>
              <div class="card h-100">
                <% if booth.thumbnail_image.attached? %>
                  <%= image_tag booth.thumbnail_image,
                        class: "card-img-top",
                        style: "aspect-ratio: 16 / 9; object-fit: cover;" %>
                <% else %>
                  <div class="bg-light d-flex align-items-center justify-content-center"
                       style="aspect-ratio: 16 / 9;">
                    <span class="text-muted small">no thumbnail</span>
                  </div>
                <% end %>

                <div class="card-body">
                  <div class="d-flex justify-content-between align-items-start gap-2">
                    <div class="fw-semibold"><%= booth.name %></div>
                    <div><%= booth_status_badge(booth) %></div>
                  </div>

                  <div class="mt-2 text-muted small">
                    <% if primary_cast.present? %>
                      cast: <%= display_name_or_email(primary_cast) %>
                    <% else %>
                      cast: <span class="text-muted">（未設定）</span>
                    <% end %>
                  </div>

                  <div class="text-muted small">
                    store: <%= booth.store&.name %>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="alert alert-secondary" role="alert">
        表示できるブースがありません。
      </div>
    <% end %>

    <hr class="my-4">

    <%# --- Dev / role entry links (暫定) --- %>
    <h2 class="h6">導線（暫定）</h2>
    <ul class="mb-3">
      <li><%= link_to "Cast booths", cast_booths_path %></li>
      <li><%= link_to "Store Admin Dashboard", admin_root_path %></li>
      <li><%= link_to "System Admin Dashboard", system_admin_root_path %></li>
      <li><%= link_to "店舗として登録（ref=TEST）", stores_new_registration_path(ref: "TEST") %></li>
    </ul>

    <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-outline-secondary" %>
  </div>
<% else %>
  <div class="container mt-4" style="max-width: 720px;">
    <h1 class="mb-3">ようこそ</h1>
    <p class="text-muted">視聴するにはログイン、または新規アカウント作成を行ってください。</p>

    <div class="d-flex gap-2 mt-3">
      <%= link_to "ログイン", new_user_session_path, class: "btn btn-primary" %>
      <%= link_to "新規アカウント作成", sign_up_path, class: "btn btn-outline-primary" %>
    </div>
  </div>
<% end %>
```

---

## 最低限の確認手順（Issue の Done に対応）

1. 通常 booth：Home にカード表示 → クリックで `booths#show` に遷移できる
2. archived booth：`archived_at` を入れた booth が Home に出ない
3. customer BAN：

   * BAN 対象 store の booth が Home に（基本）出ない
   * もし何らかで show に行けても、従来どおり `flash_inner` に警告 + root へ移動（turbo_stream でも挙動維持）

---

必要ならこの次に、**コミットメッセージ案 / PR本文（変更点・仕様・動作確認）**も #152 向けに出します。
