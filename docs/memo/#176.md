了解、GOありがとう。#176 を **最小で安全（TurboのDOM事故回避）**に実装する方針で、実装計画 → コピペ用コード一式を出します。

---

## 実装計画（高レベル）

### 1) ルーティング

Issue案どおりに寄せて、以下を追加します（**POST/DELETE**）：

* Booth: `POST /booths/:booth_id/favorite` / `DELETE /booths/:booth_id/favorite`
* Store: `POST /stores/:store_id/favorite` / `DELETE /stores/:store_id/favorite`

`resource :favorite` を nested で追加し、controller は `Favorites::BoothsController` / `Favorites::StoresController`。

### 2) 認可

* controller 側は `require_at_least!(:customer)`（=ログイン必須）
* 未ログイン時のUXは #170 に合わせて **ボタンを表示しない**（導線上は到達しない、controllerは保険として403）

### 3) Turbo Stream（DOM事故防止）

* **ボタン領域だけ** replace
* replace 対象は、ページごとに固定DOM idを用意しておく

**Booth**

* booth/show: `booth_favorite_button`
* home: `booth_#{booth.id}_favorite_button`

**Store**

* store/show: `store_favorite_button`
* home: `store_#{store.id}_favorite_button`

Turboレスポンスは **両方をreplace** して返す（存在しないターゲットは無視されるので、同じcontrollerで流用可）。

### 4) 表示

* booth/show に wrapper を新設してボタン設置（stream_stateとは分離）
* store/show は既に wrapper があるので差し替え
* home は「カード全体リンク」をやめて、**リンクとボタンを分離**（クリック事故回避）
* home に store 一覧セクションも最小で追加（Issue本文の “homeでbooth/storeそれぞれ” を満たす）

### 5) N+1回避

home は最大60件なので、

* `@favorite_booth_ids` / `@favorite_store_ids` を `pluck` でまとめて取得して set 化して判定

### 6) テスト

* 追加/解除/二重追加（UNIQUE前提で壊れない）
* Turbo Stream（Acceptヘッダ）で 200 を確認、HTMLでも動く（必要なら）

---

## コピペ用コード一式

### 1) `config/routes.rb`（favorites 追加）

```rb
Rails.application.routes.draw do
  get "up" => "rails/health#show", as: :rails_health_check

  devise_for :users, skip: %i[registrations]

  root "home#show"

  # --- Public (customer registration) ---
  get  "/sign_up", to: "customers/registrations#new",    as: :sign_up
  post "/sign_up", to: "customers/registrations#create"

  # --- Public (store registration) ---
  namespace :stores do
    get  :new_registration, to: "registrations#new"
    post :registrations,    to: "registrations#create"
  end

  # --- Customer (login required) ---
  resources :stores, only: %i[show] do
    resource :favorite, only: %i[create destroy], controller: "favorites/stores"
  end

  # --- Public (customer) ---
  resources :booths, only: %i[show] do
    resource :favorite, only: %i[create destroy], controller: "favorites/booths"
  end

  resources :stream_sessions, only: [] do
    resources :comments, only: %i[create], module: :stream_sessions
    resources :drink_orders, only: %i[create], module: :stream_sessions
    resources :ivs_participant_tokens, only: %i[create], module: :stream_sessions

    resource :presence, only: [], module: :stream_sessions do
      post :ping
    end

    get :presence_summary, on: :member
  end

  namespace :wallet do
    resources :purchases, only: %i[create]
  end

  get  "/checkout/return", to: "checkout#return"
  post "/webhooks/stripe", to: "webhooks/stripe#create"

  # --- Cast ---
  namespace :cast do
    # current_booth selection
    resource :current_booth, only: %i[create]

    resources :booths, only: %i[index show edit update] do
      get :live, on: :member
      patch :status, on: :member
      resources :stream_sessions, only: %i[create], module: :booths
    end

    resources :stream_sessions, only: [] do
      post :finish, on: :member
      get  :pending_drink_orders, on: :member
    end

    resources :drink_orders, only: [] do
      post :consume, on: :member
    end
  end

  # --- Store Admin ---
  namespace :admin do
    root "dashboard#show"

    # current_store selection
    resources :stores, only: %i[index]
    resource :current_store, only: %i[create]

    resource :store, only: %i[show update]
    resources :booths, only: %i[index show new edit create update] do
      member do
        get :watch
        patch :archive
      end
    end
    resources :drink_items, only: %i[index create update destroy]
    resources :store_bans, only: %i[index create destroy]
    resources :casts, only: %i[index create destroy]
    get "/cast_metrics", to: "metrics#cast"
  end

  # --- System Admin ---
  namespace :system_admin do
    root "dashboard#show"

    resources :referral_codes, only: %i[index new create edit update]
  end
end
```

---

### 2) controllers 追加

#### `app/controllers/favorites/booths_controller.rb`

```rb
# frozen_string_literal: true

module Favorites
  class BoothsController < ApplicationController
    before_action -> { require_at_least!(:customer) }
    before_action :set_booth

    def create
      current_user.favorite_booths.find_or_create_by!(booth: @booth)

      render_favorite_button(favorited: true)
    rescue ActiveRecord::RecordNotUnique
      # UNIQUE index で競合しても成功扱いに寄せる
      render_favorite_button(favorited: true)
    end

    def destroy
      current_user.favorite_booths.where(booth: @booth).destroy_all

      render_favorite_button(favorited: false)
    end

    private

    def set_booth
      @booth = Booth.active.find(params[:booth_id])
    end

    def render_favorite_button(favorited:)
      respond_to do |format|
        format.turbo_stream do
          streams = [
            turbo_stream.replace(
              "booth_favorite_button",
              partial: "favorites/booths/button",
              formats: [:html],
              locals: { booth: @booth, favorited: favorited }
            ),
            turbo_stream.replace(
              "booth_#{@booth.id}_favorite_button",
              partial: "favorites/booths/button",
              formats: [:html],
              locals: { booth: @booth, favorited: favorited }
            )
          ]
          render turbo_stream: streams
        end

        format.html { redirect_back fallback_location: booth_path(@booth) }
      end
    end
  end
end
```

#### `app/controllers/favorites/stores_controller.rb`

```rb
# frozen_string_literal: true

module Favorites
  class StoresController < ApplicationController
    before_action -> { require_at_least!(:customer) }
    before_action :set_store

    def create
      current_user.favorite_stores.find_or_create_by!(store: @store)

      render_favorite_button(favorited: true)
    rescue ActiveRecord::RecordNotUnique
      render_favorite_button(favorited: true)
    end

    def destroy
      current_user.favorite_stores.where(store: @store).destroy_all

      render_favorite_button(favorited: false)
    end

    private

    def set_store
      @store = Store.find(params[:store_id])
    end

    def render_favorite_button(favorited:)
      respond_to do |format|
        format.turbo_stream do
          streams = [
            turbo_stream.replace(
              "store_favorite_button",
              partial: "favorites/stores/button",
              formats: [:html],
              locals: { store: @store, favorited: favorited }
            ),
            turbo_stream.replace(
              "store_#{@store.id}_favorite_button",
              partial: "favorites/stores/button",
              formats: [:html],
              locals: { store: @store, favorited: favorited }
            )
          ]
          render turbo_stream: streams
        end

        format.html { redirect_back fallback_location: store_path(@store) }
      end
    end
  end
end
```

---

### 3) partial 追加（favoriteボタン）

#### `app/views/favorites/booths/_button.html.erb`

```erb
<%# locals: booth:, favorited: %>

<% if favorited %>
  <%= button_to "お気に入り解除",
        booth_favorite_path(booth),
        method: :delete,
        class: "btn btn-sm btn-outline-secondary" %>
<% else %>
  <%= button_to "お気に入り",
        booth_favorite_path(booth),
        method: :post,
        class: "btn btn-sm btn-outline-primary" %>
<% end %>
```

#### `app/views/favorites/stores/_button.html.erb`

```erb
<%# locals: store:, favorited: %>

<% if favorited %>
  <%= button_to "お気に入り解除",
        store_favorite_path(store),
        method: :delete,
        class: "btn btn-sm btn-outline-secondary" %>
<% else %>
  <%= button_to "お気に入り",
        store_favorite_path(store),
        method: :post,
        class: "btn btn-sm btn-outline-primary" %>
<% end %>
```

---

### 4) controller 変更（home / booth / store）

#### `app/controllers/home_controller.rb`（store一覧 + favorite ids 追加）

```rb
# frozen_string_literal: true

class HomeController < ApplicationController
  def show
    booths = Booth.active
                 .includes(
                   :store,
                   { booth_casts: :cast_user },
                   thumbnail_image_attachment: :blob
                 )
                 .order(updated_at: :desc, id: :desc)
                 .limit(60)

    # 可能な範囲で予防（ただし最終防衛は BoothsController#show の StoreBanGuard が担保）
    if user_signed_in? && current_user.customer?
      banned_store_ids = StoreBan.where(customer_user_id: current_user.id).select(:store_id)
      booths = booths.where.not(store_id: banned_store_ids)
    end

    @booths = booths

    # --- store list (minimal) ---
    stores =
      Store
        .where(id: Booth.active.select(:store_id)) # active booth を持つ store だけ
        .order(updated_at: :desc, id: :desc)
        .limit(30)

    if user_signed_in? && current_user.customer?
      banned_store_ids = StoreBan.where(customer_user_id: current_user.id).select(:store_id)
      stores = stores.where.not(id: banned_store_ids)
    end

    @stores = stores

    # --- favorites preloading for home (avoid N+1) ---
    if user_signed_in?
      @favorite_booth_ids =
        current_user.favorite_booths.where(booth_id: @booths.select(:id)).pluck(:booth_id).to_set

      @favorite_store_ids =
        current_user.favorite_stores.where(store_id: @stores.select(:id)).pluck(:store_id).to_set
    else
      @favorite_booth_ids = Set.new
      @favorite_store_ids = Set.new
    end
  end
end
```

※ `Set` を使うので、もし `set` が読み込まれていない環境なら `require "set"` が必要になることがあります（通常はRuby標準で使えますが、もしエラーが出たら HomeController の先頭に `require "set"` を追加してください）。

#### `app/controllers/booths_controller.rb`（favorited 判定を追加）

```rb
# frozen_string_literal: true

class BoothsController < ApplicationController
  include StoreBanGuard

  before_action :set_booth, only: %i[show]
  before_action :reject_banned_customer_for_booth!, only: %i[show]

  def show
    @stream_session = @booth.current_stream_session
    @drink_items = @booth.store.drink_items.enabled_only.ordered

    @wallet =
      if user_signed_in?
        Wallet.find_or_create_by!(customer_user_id: current_user.id) do |w|
          w.available_points = 0
          w.reserved_points = 0
        end
      end

    @comments =
      if @stream_session.present?
        Comment.alive.where(stream_session: @stream_session)
              .order(created_at: :desc)
              .limit(50)
              .reverse
      else
        []
      end

    @booth_favorited =
      user_signed_in? && current_user.favorite_booths.exists?(booth_id: @booth.id)
  end

  private

  def set_booth
    @booth = Booth.active.find(params[:id])
  end

  def reject_banned_customer_for_booth!
    reject_banned_customer!(store: @booth.store)
  end
end
```

#### `app/controllers/stores_controller.rb`（favorited 判定を追加）

```rb
# frozen_string_literal: true

class StoresController < ApplicationController
  include StoreBanGuard

  before_action -> { require_at_least!(:customer) }
  before_action :set_store, only: %i[show]
  before_action :reject_banned_customer_for_store!, only: %i[show]

  def show
    @booths =
      @store
        .booths
        .active
        .includes(:cast_users)
        .order(:id)

    @store_favorited = current_user.favorite_stores.exists?(store_id: @store.id)
  end

  private

  def set_store
    @store = Store.find(params[:id])
  end

  def reject_banned_customer_for_store!
    reject_banned_customer!(store: @store)
  end
end
```

---

### 5) view 変更（booth/show, store/show, home/show）

#### `app/views/booths/show.html.erb`（favorite置き場追加：stream_stateとは別）

```erb
<% content_for :head do %>
  <meta name="turbo-cache-control" content="no-cache">
<% end %>

<h1>Public Booth#show (stub)</h1>

<p>booth_id: <%= @booth.id %></p>

<%# ★お気に入りボタン（stream_state とは分離して固定DOM） %>
<% if user_signed_in? %>
  <div id="booth_favorite_button" class="mb-3">
    <%= render "favorites/booths/button", booth: @booth, favorited: @booth_favorited %>
  </div>
<% end %>

<%# ★booth単位で常時購読（開始/終了/状態変化を自動反映） %>
<%= turbo_stream_from [@booth, :stream_state] %>

<%# ★箱（wrapper）は show 側に固定。idを絶対に消さない %>
<div id="stream_state">
  <%= render "booths/stream_state",
        booth: @booth,
        stream_session: @stream_session,
        comments: @comments,
        drink_items: @drink_items %>
</div>

<hr>

<h2>所持ポイント</h2>

<% if user_signed_in? %>
  <%# ★wallet（個人UI）は booth に混ぜず user(wallet) チャンネルで購読 %>
  <%= turbo_stream_from [current_user, :wallet] %>

  <%# ★箱（wrapper）は show 側に固定。idを絶対に消さない %>
  <div id="wallet_balance">
    <%= render "wallets/balance", wallet: @wallet %>
  </div>

  <div class="d-flex gap-2 mb-3">
    <%= button_to "1000pt 購入",
          wallet_purchases_path,
          method: :post,
          params: { points: 1000, booth_id: @booth.id },
          form: { data: { turbo: false } },
          class: "btn btn-sm btn-outline-primary" %>

    <%= button_to "3000pt 購入",
          wallet_purchases_path,
          method: :post,
          params: { points: 3000, booth_id: @booth.id },
          form: { data: { turbo: false } },
          class: "btn btn-sm btn-outline-primary" %>
  </div>
<% else %>
  <p class="text-muted">ログインすると購入できます</p>
<% end %>

<hr>

<div class="d-flex justify-content mb-3">
  <%= link_to "退室", root_path, class: "btn btn-outline-secondary" %>
</div>
```

#### `app/views/stores/show.html.erb`（準備中→実ボタン）

```erb
<div class="container mt-4" style="max-width: 960px;">
  <div class="d-flex justify-content-between align-items-start gap-3">
    <div>
      <h1 class="mb-1"><%= @store.name %></h1>
      <p class="text-muted mb-0">店舗詳細</p>
    </div>

    <div id="store_favorite_button">
      <%= render "favorites/stores/button", store: @store, favorited: @store_favorited %>
    </div>
  </div>

  <hr class="my-4">

  <h2 class="h5 mb-3">ブース一覧</h2>

  <% if @booths.blank? %>
    <div class="alert alert-warning" role="alert">
      公開中のブースがありません。
    </div>
  <% else %>
    <div class="list-group">
      <% @booths.each do |booth| %>
        <div class="list-group-item">
          <div class="d-flex justify-content-between align-items-center gap-3">
            <div class="flex-grow-1">
              <div class="d-flex align-items-center gap-2">
                <strong><%= booth.name %></strong>

                <%# 任意：状態バッジ（最小） %>
                <% if booth.live? %>
                  <span class="badge text-bg-danger">LIVE</span>
                <% elsif booth.standby? %>
                  <span class="badge text-bg-success">STANDBY</span>
                <% elsif booth.away? %>
                  <span class="badge text-bg-warning">AWAY</span>
                <% else %>
                  <span class="badge text-bg-secondary">OFFLINE</span>
                <% end %>
              </div>

              <% casts = booth.cast_users.to_a %>
              <% if casts.present? %>
                <div class="text-muted small mt-1">
                  cast:
                  <%= casts.map { |u| u.display_name.presence || u.email }.join(" / ") %>
                </div>
              <% else %>
                <div class="text-muted small mt-1">cast: (未設定)</div>
              <% end %>
            </div>

            <div class="text-end">
              <%= link_to "視聴ページへ", booth_path(booth), class: "btn btn-outline-primary btn-sm" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>

  <div class="mt-4">
    <%= link_to "戻る", root_path, class: "btn btn-outline-secondary" %>
  </div>
</div>
```

#### `app/views/home/show.html.erb`（カード全体リンクをやめ、booth/store両方に設置）

```erb
<% if user_signed_in? %>
  <div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
      <h1 class="h4 mb-0">ブース一覧</h1>
      <div class="text-muted small">
        ログイン中: <%= current_user.email %>（role: <%= current_user.role %>）
      </div>
    </div>

    <% if @booths.present? %>
      <div class="row row-cols-1 row-cols-md-3 g-3">
        <% @booths.each do |booth| %>
          <% primary_cast = booth.booth_casts.max_by { |bc| [bc.created_at, bc.id] }&.cast_user %>
          <% favorited = @favorite_booth_ids.include?(booth.id) %>

          <div class="col">
            <div class="card h-100">
              <%= link_to booth_path(booth), class: "text-decoration-none text-reset" do %>
                <% if booth.thumbnail_image.attached? %>
                  <%= image_tag booth.thumbnail_image,
                        class: "card-img-top",
                        style: "aspect-ratio: 16 / 9; object-fit: cover;" %>
                <% else %>
                  <div class="bg-light d-flex align-items-center justify-content-center"
                       style="aspect-ratio: 16 / 9;">
                    <span class="text-muted small">no thumbnail</span>
                  </div>
                <% end %>
              <% end %>

              <div class="card-body">
                <div class="d-flex justify-content-between align-items-start gap-2">
                  <div class="fw-semibold">
                    <%= link_to booth.name, booth_path(booth), class: "text-decoration-none text-reset" %>
                  </div>
                  <div><%= booth_status_badge(booth) %></div>
                </div>

                <div class="mt-2 text-muted small">
                  <% if primary_cast.present? %>
                    cast: <%= display_name_or_email(primary_cast) %>
                  <% else %>
                    cast: <span class="text-muted">（未設定）</span>
                  <% end %>
                </div>

                <div class="text-muted small">
                  store: <%= booth.store&.name %>
                </div>
              </div>

              <div class="card-footer bg-white border-0 pt-0">
                <div id="booth_<%= booth.id %>_favorite_button">
                  <%= render "favorites/booths/button", booth: booth, favorited: favorited %>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="alert alert-secondary" role="alert">
        表示できるブースがありません。
      </div>
    <% end %>

    <hr class="my-4">

    <h2 class="h5 mb-3">店舗一覧</h2>

    <% if @stores.present? %>
      <div class="list-group mb-4">
        <% @stores.each do |store| %>
          <% favorited = @favorite_store_ids.include?(store.id) %>

          <div class="list-group-item">
            <div class="d-flex justify-content-between align-items-center gap-3">
              <div class="flex-grow-1">
                <%= link_to store.name, store_path(store), class: "text-decoration-none" %>
              </div>

              <div id="store_<%= store.id %>_favorite_button">
                <%= render "favorites/stores/button", store: store, favorited: favorited %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="alert alert-secondary" role="alert">
        表示できる店舗がありません。
      </div>
    <% end %>

    <hr class="my-4">

    <%# --- Dev / role entry links (暫定) --- %>
    <h2 class="h6">導線（暫定）</h2>
    <ul class="mb-3">
      <li><%= link_to "Cast booths", cast_booths_path %></li>
      <li><%= link_to "Store Admin Dashboard", admin_root_path %></li>
      <li><%= link_to "System Admin Dashboard", system_admin_root_path %></li>
      <li><%= link_to "店舗として登録（ref=TEST）", stores_new_registration_path(ref: "TEST") %></li>
    </ul>

    <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-outline-secondary" %>
  </div>
<% else %>
  <div class="container mt-4" style="max-width: 720px;">
    <h1 class="mb-3">ようこそ</h1>
    <p class="text-muted">視聴するにはログイン、または新規アカウント作成を行ってください。</p>

    <div class="d-flex gap-2 mt-3">
      <%= link_to "ログイン", new_user_session_path, class: "btn btn-primary" %>
      <%= link_to "新規アカウント作成", sign_up_path, class: "btn btn-outline-primary" %>
    </div>
  </div>
<% end %>
```

---

### 6) テスト追加

#### `test/integration/favorites_toggle_test.rb`

```rb
# frozen_string_literal: true

require "test_helper"

class FavoritesToggleTest < ActionDispatch::IntegrationTest
  setup do
    @store = Store.create!(name: "store1")
    @booth = Booth.create!(store: @store, name: "booth1", status: :offline)
    @user  = User.create!(email: "customer@example.com", password: "password", role: :customer)
  end

  test "booth favorite create/destroy (turbo_stream)" do
    sign_in @user, scope: :user

    assert_difference("FavoriteBooth.count", +1) do
      post booth_favorite_path(@booth),
           headers: { "Accept" => "text/vnd.turbo-stream.html" }
    end
    assert_response :success

    assert_difference("FavoriteBooth.count", -1) do
      delete booth_favorite_path(@booth),
             headers: { "Accept" => "text/vnd.turbo-stream.html" }
    end
    assert_response :success
  end

  test "booth favorite is idempotent (double create does not break)" do
    sign_in @user, scope: :user

    post booth_favorite_path(@booth),
         headers: { "Accept" => "text/vnd.turbo-stream.html" }
    assert_response :success

    assert_no_difference("FavoriteBooth.count") do
      post booth_favorite_path(@booth),
           headers: { "Accept" => "text/vnd.turbo-stream.html" }
    end
    assert_response :success
  end

  test "store favorite create/destroy (turbo_stream)" do
    sign_in @user, scope: :user

    assert_difference("FavoriteStore.count", +1) do
      post store_favorite_path(@store),
           headers: { "Accept" => "text/vnd.turbo-stream.html" }
    end
    assert_response :success

    assert_difference("FavoriteStore.count", -1) do
      delete store_favorite_path(@store),
             headers: { "Accept" => "text/vnd.turbo-stream.html" }
    end
    assert_response :success
  end

  test "store favorite is idempotent (double create does not break)" do
    sign_in @user, scope: :user

    post store_favorite_path(@store),
         headers: { "Accept" => "text/vnd.turbo-stream.html" }
    assert_response :success

    assert_no_difference("FavoriteStore.count") do
      post store_favorite_path(@store),
           headers: { "Accept" => "text/vnd.turbo-stream.html" }
    end
    assert_response :success
  end
end
```

---

## 実行コマンド（このプロジェクト流）

```bash
docker compose exec app bash -lc "RAILS_ENV=test bin/rails test"
```

---

## Done条件との対応（チェック）

* booth/store 両方で追加/解除：✅（POST/DELETE）
* ボタンが即時切替（Turbo）：✅（replaceでボタン領域のみ）
* 二重追加で壊れない：✅（find_or_create_by + UNIQUE、競合は成功扱い）
* テストが通る：✅（統合テスト追加）

---

必要なら、このまま **コミットメッセージ案 / PR本文**（変更点・仕様・動作確認）も #176 用に出します。
