## #119 のブランチ名（この構成なら）

* **`feature/119-admin-booths-create-ivs-stage`**（おすすめ）

---

## #119 の実装方針（最小変更で同期プロビジョニング）

「ブース作成と Stage 作成を同期」を満たしつつ、運用を壊さない形はこれです。

### A) DB：`booths.ivs_stage_arn` を追加

（stream_session 側は #103 続きで寄せる）

### B) Service：`Booths::ProvisionIvsStageService` を新規

* Booth を受け取って `ivs_stage_arn` が無ければ `create_stage!` して保存
* 既にあるなら何もしない（再実行安全）

### C) Admin::BoothsController#create

* `Booth.transaction` で booth 作成 → provision
* provision 失敗なら **作成も失敗**（同期の要件）

---

## 具体コード（コピペベース）

### 0) routes に `new` を追加（必須）

いま `create` はあるのに `new` がないので、普通のフォーム導線が作れません。

```rb
# config/routes.rb（adminのboothsだけ）
resources :booths, only: %i[index show new create edit update] do
  member { get :watch }
end
```

---

### 1) migration（空生成→全文置換の流儀で）

例：`AddIvsStageArnToBooths`

```bash
docker compose exec app bash -lc "bin/rails g migration AddIvsStageArnToBooths"
```

```rb
# db/migrate/XXXXXXXXXX_add_ivs_stage_arn_to_booths.rb
class AddIvsStageArnToBooths < ActiveRecord::Migration[8.0]
  def change
    change_column_default :booths, :status, from: nil, to: 0
    add_column :booths, :ivs_stage_arn, :string
    add_index  :booths, :ivs_stage_arn
  end
end
```

```bash
docker compose run --rm app rails db:migrate
```

> unique は一旦不要（将来「同じStageを複数boothで共有しない」保証をDBに入れるなら unique もあり）

### 2) service 新規

`app/services/booths/provision_ivs_stage_service.rb`

```rb
# frozen_string_literal: true

module Booths
  class ProvisionIvsStageService
    class Error < StandardError; end
    class StageProvisionFailed < Error; end

    DEFAULT_PREFIX = "br".freeze

    def initialize(booth:, ivs_client: Ivs::Client.new)
      @booth = booth
      @ivs_client = ivs_client
    end

    def call!
      @booth.with_lock do
        return @booth.ivs_stage_arn if @booth.ivs_stage_arn.present?

        arn = @ivs_client.create_stage!(
          name: stage_name,
          tags: stage_tags
        )

        raise StageProvisionFailed, "create_stage returned blank arn" if arn.blank?

        @booth.update!(ivs_stage_arn: arn)
        arn
      end
    rescue Aws::Errors::ServiceError => e
      raise StageProvisionFailed, "IVS create_stage failed: #{e.class} #{e.message}"
    end

    private

    def stage_name
      # 例: br-prod-store-1-booth-3
      "#{name_prefix}-store-#{@booth.store_id}-booth-#{@booth.id}"
    end

    def name_prefix
      # 環境差は設定のみ（未設定時だけフォールバック）
      # 例: br-dev / br-prod
      ENV.fetch("IVS_STAGE_NAME_PREFIX", DEFAULT_PREFIX)
    end

    def stage_tags
      # タグは後の検索/ガベコレ/運用が超ラクになる
      {
        "app"      => "butterfly-room",
        "env"      => ENV.fetch("IVS_STAGE_ENV", name_prefix), # 任意。未設定ならprefixを流用
        "store_id" => @booth.store_id.to_s,
        "booth_id" => @booth.id.to_s
      }
    end
  end
end
```

ProvisionService が `tags:` を渡すので、`Ivs::Client` はこうしておいてください。

`app/services/ivs/client.rb`

```rb
def create_stage!(name:, tags: {})
  resp = @client.create_stage(name: name, tags: tags)
  resp.stage.arn
end
```

#### 環境変数（おすすめ値）

* ローカル：`IVS_STAGE_NAME_PREFIX=br-dev`
* 本番：`IVS_STAGE_NAME_PREFIX=br-prod`
* （任意）`IVS_STAGE_ENV=development` / `production`

  * 省略可。省略したら `env` タグは prefix を流用します

### 3) controller：index/new/create を実装

`app/controllers/admin/base_controller.rb`

```ruby
module Admin
  class BaseController < ApplicationController
    before_action -> { require_role!(:store_admin, :system_admin) }

    private

    def current_store
      return Store.first if current_user.system_admin?

      membership =
        StoreMembership
          .includes(:store)
          .where(user_id: current_user.id, membership_role: :admin)
          .order(:id)
          .first

      membership&.store
    end

    helper_method :current_store

    def require_current_store!
      return if current_user.system_admin?
      return if current_store.present?

      head :forbidden
    end
  end
end
```



`app/controllers/admin/booths_controller.rb` を拡張します。

ポイント：

* `authorize_update!` しか無いので、create 用の authorize が必要（既存ポリシーの `create?` が無いなら暫定で `admin_operate?` などに寄せる）
* `booth_params` は create 用に `:name` を許可する必要がある（今は `:description` しかない）

例（最低限の形）：

```rb
# frozen_string_literal: true

module Admin
  class BoothsController < Admin::BaseController
    before_action :require_current_store_for_booths!
    before_action :set_booth, only: %i[show edit update watch]
    before_action :authorize_update!, only: %i[edit update]

    def index
      # current_store 配下だけ出す（店舗管理なので）
      @booths = current_store.booths.order(id: :desc)
    end

    def show
    end

    def new
      @booth = current_store.booths.new
      authorize_create!
    end

    def create
      @booth = current_store.booths.new(booth_create_params)
      authorize_create!

      Booth.transaction do
        @booth.save!
        Booths::ProvisionIvsStageService.new(booth: @booth).call!
      end

      redirect_to admin_booth_path(@booth), notice: "ブースを作成しました"
    rescue ActiveRecord::RecordInvalid
      render :new, status: :unprocessable_entity
    rescue Booths::ProvisionIvsStageService::StageProvisionFailed => e
      @booth.errors.add(:base, "IVS Stage の作成に失敗しました: #{e.message}")
      render :new, status: :unprocessable_entity
    end

    def edit
    end

    def update
      if remove_thumbnail_image?
        @booth.thumbnail_image.purge_later if @booth.thumbnail_image.attached?
      end

      if @booth.update(booth_params)
        redirect_to admin_booth_path(@booth), notice: "更新しました"
      else
        render :edit, status: :unprocessable_entity
      end
    end

    def watch
      policy = Authorization::BoothPolicy.new(current_user, @booth)
      head :forbidden and return unless policy.admin_operate?

      stream_session = StreamSession.find_by(id: @booth.current_stream_session_id)

      if stream_session.blank? || !stream_session.live?
        redirect_to admin_booth_path(@booth), alert: "配信中ではありません"
        return
      end

      redirect_to booth_path(@booth)
    end

    private

    def set_booth
      scope =
        if current_user.system_admin?
          Booth.all
        else
          # require_current_store! があるので current_store は必ずいる前提
          current_store.booths
        end

      @booth = scope.find(params[:id])
    end

    def authorize_update!
      policy = Authorization::BoothPolicy.new(current_user, @booth)
      head :forbidden unless policy.update?
    end

    def authorize_create!
      policy = Authorization::BoothPolicy.new(current_user, @booth)
      head :forbidden unless policy.create?
    end

    # create 用（name など許可）
    def booth_create_params
      params.require(:booth).permit(:name, :description, :thumbnail_image)
    end

    # update 用（現状維持）
    def booth_params
      params.require(:booth).permit(:description, :thumbnail_image)
    end

    def remove_thumbnail_image?
      params.dig(:booth, :remove_thumbnail_image) == "1"
    end

    def require_current_store_for_booths!
      return if current_store.present?
      head :forbidden
    end
  end
end
```

1) `Authorization::BoothPolicy#create?` を追加

`app/services/authorization/booth_policy.rb` に追記：

```rb
def create?
  return false if user.blank?

  return true if user.system_admin?
  return false unless user.store_admin?

  # store_admin：自分の store の booth のみ作成可
  user.store_memberships.admin_only.exists?(store_id: record.store_id)
end
```

> `record.store_id` が nil だと落ちるので、controller 側で必ず `current_store` を入れてから呼びます。

### 4) view：index/new を追加

`app/views/admin/booths/index.html.erb`

```erb
<h1>ブース管理</h1>

<div class="mb-3">
  <%= link_to "新規作成", new_admin_booth_path, class: "btn btn-primary" %>
</div>

<table class="table table-sm align-middle">
  <thead>
    <tr>
      <th>ID</th>
      <th>名前</th>
      <th>Status</th>
      <th>IVS Stage ARN</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @booths.each do |booth| %>
      <tr>
        <td><%= booth.id %></td>
        <td><%= booth.name %></td>
        <td><%= booth.status %></td>
        <td class="text-muted small"><%= booth.ivs_stage_arn.presence || "-" %></td>
        <td class="text-end">
          <%= link_to "詳細", admin_booth_path(booth), class: "btn btn-outline-secondary btn-sm" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
```

`app/views/admin/booths/new.html.erb`

```erb
<h1>ブース作成</h1>

<%= form_with model: [:admin, @booth] do |f| %>
  <% if @booth.errors.any? %>
    <div class="alert alert-danger">
      <ul class="mb-0">
        <% @booth.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= f.label :name, "ブース名", class: "form-label" %>
    <%= f.text_field :name, class: "form-control", required: true %>
  </div>

  <div class="mb-3">
    <%= f.label :description, "説明文", class: "form-label" %>
    <%= f.text_area :description, class: "form-control", rows: 6 %>
  </div>

  <div class="mb-3">
    <%= f.label :thumbnail_image, "サムネ画像", class: "form-label" %>
    <%= f.file_field :thumbnail_image, class: "form-control" %>
  </div>

  <div class="d-flex gap-2">
    <%= f.submit "作成する", class: "btn btn-primary" %>
    <%= link_to "戻る", admin_booths_path, class: "btn btn-outline-secondary" %>
  </div>
<% end %>
```

`app/views/admin/booths/show.html.erb` に追記（確認が速い）

```erb
<p>ivs_stage_arn:</p>
<pre class="bg-light p-2"><%= @booth.ivs_stage_arn.presence || "-" %></pre>
```

### 5) edit の戻るリンク修正（ついでに）

`app/views/admin/booths/edit.html.erb`：

```diff
- <%= link_to "戻る", cast_booth_path(@booth), class: "btn btn-outline-secondary" %>
+ <%= link_to "戻る", admin_booth_path(@booth), class: "btn btn-outline-secondary" %>
```
---
## Test を修正
`test\integration\booth_edit_authorization_test.rb`
```ruby
  test "store_admin can update only own store booth" do
    sign_in @store_admin, scope: :user

    patch admin_booth_path(@booth1), params: { booth: { name: "x" } }
    assert_response :redirect
    assert_redirected_to admin_booth_path(@booth1)

    patch admin_booth_path(@booth2), params: { booth: { name: "x" } }
    assert_response :not_found
  end
```
---

## IAM ポリシーに ivs:TagResource を追加

### 1) `ButterflyRoomIvsRealTimeDev` に `ivs:TagResource` を追加

（Stage にタグを付けたい＝TagResource が必須）

ポリシー例（最小）：

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "IvsRealtimeStageOps",
      "Effect": "Allow",
      "Action": [
        "ivs:CreateStage",
        "ivs:GetStage",
        "ivs:DeleteStage",
        "ivs:CreateParticipantToken",
        "ivs:TagResource"
      ],
      "Resource": "*"
    }
  ]
}
```

※ `Resource` は最初は `*` でOK（運用が固まってから絞る）。
※ `CreateParticipantToken` は stage ARN を resource に取る都合で、絞り込みは少し工夫が要るので後回しでOK。

### 2) `ButterflyRoomIvsRealTimeStagePolicy` にも `ivs:TagResource` を追加する

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "IvsCreateStage",
      "Effect": "Allow",
      "Action": [
        "ivs:CreateStage",
        "ivs:GetStage",
        "ivs:TagResource"
      ],
      "Resource": "*"
    },
    {
      "Sid": "IvsCreateParticipantToken",
      "Effect": "Allow",
      "Action": [
        "ivs:CreateParticipantToken"
      ],
      "Resource": "*"
    }
  ]
}
```


---

## 確認方法

---

## #119 Done 条件（この構成で確認できる）

* 管理画面（admin）からブース作成できる
* 作成直後に `booths.ivs_stage_arn` が入っている
* AWS 側に stage が増えている（name が `booth-<id>` で追跡できる）
* 失敗時は作成がロールバックされ、画面にエラーが出る


