了解です。
#629 用に、**そのまま貼れるコード + Docker前提コマンド** でまとめます。

---

## ブランチ名

```bash
git checkout -b feature/629-booth-thumbnail-filepond
```

---

## 今回やること

* `booths/_form.html.erb` を FilePond 化
* `cast/edit` / `admin/edit` / `admin/new` で共通 partial をそのまま使う
* `remove_thumbnail_checkbox_id` を廃止
* `Booth#thumbnail_image` を `NormalizedImageAttachment` 対象から外す
* `cast/admin booths_controller` の mini_magick 前提 rescue を外す
* `profile_avatar_controller.js` をそのまま流用する
  いまの booth フォームは checkbox と通常 `file_field` 構成で、共通 partial を `cast/edit` と `admin/edit/new` が使っています。

---

# Step 1. `Booth` モデル修正

## `app/models/booth.rb`

`normalizes_image_attachment :thumbnail_image` を削除します。
`include NormalizedImageAttachment` は **いったん残してOK** です。`Store` など他用途があるためです。現状 `Booth` は `thumbnail_image` に対して `normalizes_image_attachment` が入っています。

### 修正後全文

```ruby
class Booth < ApplicationRecord
  include NormalizedImageAttachment

  belongs_to :store
  belongs_to :current_stream_session, class_name: "StreamSession", optional: true

  has_many :stream_sessions, dependent: :restrict_with_error
  has_many :booth_casts, dependent: :restrict_with_error
  has_many :cast_users, through: :booth_casts, source: :cast_user
  has_many :favorite_booths, dependent: :destroy
  has_one_attached :thumbnail_image

  enum :status, { offline: 0, live: 1, away: 2, standby: 3 }

  scope :active, -> { where(archived_at: nil) }
  scope :archived, -> { where.not(archived_at: nil) }

  def archived?
    archived_at.present?
  end

  def primary_cast_user_id
    booth_casts.order(created_at: :desc, id: :desc).pick(:cast_user_id)
  end

  def primary_cast_user
    if booth_casts.loaded?
      booth_casts.max_by { |booth_cast| [ booth_cast.created_at, booth_cast.id ] }&.cast_user
    else
      booth_casts.includes(:cast_user).order(created_at: :desc, id: :desc).first&.cast_user
    end
  end
end
```

---

# Step 2. 共通 partial を FilePond 化

## `app/views/booths/_form.html.erb`

今の partial は checkbox と既存画像表示つきです。これをプロフィールと同じ hidden flag 方式へ寄せます。

### 修正後全文

```erb
<%# locals: f:, booth:, submit_label:, back_path: %>

<% if booth.errors.any? %>
  <div class="alert alert-danger">
    <ul class="mb-0">
      <% booth.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<div class="mb-3">
  <%= f.label :name, "ブース名", class: "form-label" %>
  <%= f.text_field :name, class: "form-control", required: true %>
</div>

<div class="mb-3">
  <%= f.label :description, "説明文", class: "form-label" %>
  <%= f.text_area :description, class: "form-control", rows: 6 %>
</div>

<div class="mb-3 profile-avatar-field" data-controller="profile-avatar">
  <%= f.label :thumbnail_image, "サムネ画像", class: "form-label" %>

  <%= f.hidden_field :remove_thumbnail_image,
        value: "0",
        data: { profile_avatar_target: "removeFlag" } %>

  <%= f.file_field :thumbnail_image,
        accept: ".jpg,.jpeg,.png,.webp,.heic,.heif",
        data: { profile_avatar_target: "input" } %>
</div>

<div class="d-flex gap-2">
  <%= f.submit submit_label, class: "btn btn-primary" %>
  <%= link_to "戻る", back_path, class: "btn btn-outline-secondary" %>
</div>
```

---

# Step 3. `cast/edit` 側の render 引数整理

## `app/views/cast/booths/edit.html.erb`

### 修正後全文

```erb
<script src="/vendor/filepond/filepond.min.js" defer></script>
<script src="/vendor/filepond/filepond-plugin-image-preview.min.js" defer></script>
<script src="/vendor/filepond/filepond-plugin-image-resize.min.js" defer></script>
<script src="/vendor/filepond/filepond-plugin-image-transform.min.js" defer></script>

<h1>ブース編集</h1>

<%= form_with model: [:cast, @booth] do |f| %>
  <%= render "booths/form",
            f: f,
            booth: @booth,
            submit_label: "更新する",
            back_path: dashboard_path_for(current_user) %>
<% end %>
```

---

# Step 4. `admin/edit` 側の render 引数整理

## `app/views/admin/booths/edit.html.erb`

### 修正後全文

```erb
<script src="/vendor/filepond/filepond.min.js" defer></script>
<script src="/vendor/filepond/filepond-plugin-image-preview.min.js" defer></script>
<script src="/vendor/filepond/filepond-plugin-image-resize.min.js" defer></script>
<script src="/vendor/filepond/filepond-plugin-image-transform.min.js" defer></script>

<h1>ブース編集</h1>

<%= form_with model: [:admin, @booth] do |f| %>
  <%= render "booths/form",
            f: f,
            booth: @booth,
            submit_label: "更新する",
            back_path: dashboard_path_for(current_user) %>
<% end %>
```

---

# Step 5. `admin/new` 側の render 引数整理

## `app/views/admin/booths/new.html.erb`

### 修正後全文

```erb
<script src="/vendor/filepond/filepond.min.js" defer></script>
<script src="/vendor/filepond/filepond-plugin-image-preview.min.js" defer></script>
<script src="/vendor/filepond/filepond-plugin-image-resize.min.js" defer></script>
<script src="/vendor/filepond/filepond-plugin-image-transform.min.js" defer></script>

<h1>ブース作成</h1>

<%= form_with model: [:admin, @booth] do |f| %>
  <%= render "booths/form",
            f: f,
            booth: @booth,
            submit_label: "作成する",
            back_path: dashboard_path_for(current_user) %>
<% end %>
```

---

# Step 6. `cast` controller の rescue 見直し

## `app/controllers/cast/booths_controller.rb`

`update` の `rescue => e` は、今回の方針では mini_magick 前提の文言になっているので外します。
現状は失敗時に `"png / jpg / webp の画像で再度お試しください"` を出していますが、今回は HEIC も受け付け、クライアントで JPEG に変換して送る前提です。

### `update` メソッドだけ置き換え

```ruby
def update
  if @booth.update(booth_params)
    unless ensure_attachment_persisted!(record: @booth, attachment_name: :thumbnail_image)
      return render :edit, status: :unprocessable_entity
    end

    purge_attachment_if_requested(
      record: @booth,
      attachment_name: :thumbnail_image,
      remove_param_name: :remove_thumbnail_image
    )

    redirect_to helpers.dashboard_path_for(current_user), notice: "ブースを更新しました"
  else
    render :edit, status: :unprocessable_entity
  end
end
```

---

# Step 7. `admin` controller の rescue 見直し

## `app/controllers/admin/booths_controller.rb`

### 7-1 `create` の `rescue NormalizedImageAttachment::InvalidImageAttachment` を削除

現状 `create` には `NormalizedImageAttachment::InvalidImageAttachment` rescue があります。`thumbnail_image` について `normalizes_image_attachment` を外すので、これも不要です。

### 7-2 `update` の包括 rescue を削除

### `create` メソッド全文置き換え

```ruby
def create
  @booth = current_store.booths.new
  authorize_create!

  @booth.assign_attributes(booth_create_params)

  Booth.transaction do
    @booth.save!
    Booths::ProvisionIvsStageService.new(booth: @booth).call!
  end

  unless ensure_attachment_persisted!(record: @booth, attachment_name: :thumbnail_image)
    return render :new, status: :unprocessable_entity
  end

  purge_attachment_if_requested(
    record: @booth,
    attachment_name: :thumbnail_image,
    remove_param_name: :remove_thumbnail_image
  )

  redirect_to helpers.dashboard_path_for(current_user), notice: "ブースを作成しました"

rescue ActiveRecord::RecordInvalid
  render :new, status: :unprocessable_entity

rescue Booths::ProvisionIvsStageService::StageProvisionFailed => e
  @booth.errors.add(:base, "IVS Stage の作成に失敗しました: #{e.message}")
  render :new, status: :unprocessable_entity

rescue => e
  Rails.logger.error("[BoothCreate] #{e.class}: #{e.message}")

  @booth ||= current_store.booths.new
  @booth.errors.add(:base, "ブースの作成に失敗しました")

  render :new, status: :unprocessable_entity
end
```

### `update` メソッド全文置き換え

```ruby
def update
  if @booth.update(booth_params)
    unless ensure_attachment_persisted!(record: @booth, attachment_name: :thumbnail_image)
      return render :edit, status: :unprocessable_entity
    end

    purge_attachment_if_requested(
      record: @booth,
      attachment_name: :thumbnail_image,
      remove_param_name: :remove_thumbnail_image
    )

    redirect_to helpers.dashboard_path_for(current_user), notice: "更新しました"
  else
    render :edit, status: :unprocessable_entity
  end
end
```

---

# Step 8. FilePond 資材は追加不要

今回の booth 実装では、#622 のプロフィール実装で使っている

* `/vendor/filepond/filepond.min.js`
* `/vendor/filepond/filepond-plugin-image-preview.min.js`
* `/vendor/filepond/filepond-plugin-image-resize.min.js`
* `/vendor/filepond/filepond-plugin-image-transform.min.js`
* `profile_avatar_controller.js`
* `_profile_avatar.scss`

をそのまま流用します。
いま `application.scss` には `@import "components/profile_avatar";` がすでに入っています。

なので **npm install や public コピーは不要** です。

---

# Step 9. 動作確認

## 起動

```bash
docker compose up
```

## 確認画面

* cast 側編集
* admin 側編集
* admin 側新規作成

## 確認ポイント

* カメラアイコンUIが出る
* 画像選択でプレビューが出る
* JPEG に変換される
* 1024x1024 以内に縮小される
* 作成できる
* 更新できる
* 削除状態で送信すると `purge_attachment_if_requested` が効く
* 既存の `name` / `description` 更新が壊れていない

---

# Step 10. テストとチェック

最低限これを回すのが安心です。

```bash
docker compose run --rm app bin/rails test
```

必要なら booth まわりだけ先に見るなら、該当テストファイル単位でもOKです。

---

## 補足

今回の変更では、プロフィールと同じく**サーバー側縮小はやめて、クライアント側に寄せる**方針です。
ただし `NormalizedImageAttachment` concern 自体はまだ `Store` などに残るので、完全削除は後続の掃除Issueでやるのが安全です。現状 `Booth` は `thumbnail_image` に `normalizes_image_attachment :thumbnail_image` が入っているため、今回そこだけ外す形になります。

必要なら次に、**この実装用のコミットメッセージとPR文**も出します。
