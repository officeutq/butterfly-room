実装方針はこれで進めるのが最小差分です。

* `cast/stream_sessions/:id` に `show` を追加
* `Cast::StreamSessionsController#show` で終了済み `stream_session` のみ表示
* 認可は `finish` と同系統
* 集計は controller でまとめて作成
* 画面は `default_main` 上に Bootstrap カードでシンプルに表示
* #400 では `finish` のリダイレクト先をこの画面へ差し替えやすい構成にしておく

変更ファイルは 3 つです。

---

## 1. `config/routes.rb`

`cast` 名前空間内の `resources :stream_sessions` を次の形に差し替えてください。

```ruby
    resources :stream_sessions, only: %i[show] do
      post :finish, on: :member
      get  :pending_drink_orders, on: :member
      get  :meta_modal, on: :member
      get  :meta_display, on: :member

      patch :metadata, on: :member
    end
```

---

## 2. `app/controllers/cast/stream_sessions_controller.rb`

全文差し替えです。

```ruby
# frozen_string_literal: true

module Cast
  class StreamSessionsController < Cast::BaseController
    before_action :set_stream_session, only: %i[show finish pending_drink_orders meta_modal meta_display metadata]
    before_action :authorize_stream_session_access!, only: %i[show finish pending_drink_orders meta_modal meta_display metadata]

    def show
      booth = @stream_session.booth

      unless @stream_session.ended?
        redirect_to live_cast_booth_path(booth), alert: "終了済み配信のリザルトのみ表示できます"
        return
      end

      comments_scope = Comment.alive.where(stream_session_id: @stream_session.id)
      drink_orders_scope = DrinkOrder.where(stream_session_id: @stream_session.id)
      consumed_orders_scope = drink_orders_scope.consumed
      refunded_orders_scope = drink_orders_scope.refunded
      presences_scope = Presence.where(stream_session_id: @stream_session.id)
      ledger_scope = StoreLedgerEntry.where(stream_session_id: @stream_session.id)

      @booth = booth
      @comment_count = comments_scope.count
      @viewer_count = presences_scope.distinct.count(:customer_user_id)

      @drink_order_count = drink_orders_scope.count
      @consumed_drink_count = consumed_orders_scope.count
      @refunded_drink_count = refunded_orders_scope.count

      @earned_points = ledger_scope.sum(:points)

      @drink_order_avg_price_points = average_price_points(drink_orders_scope)
      @consumed_drink_avg_price_points = average_price_points(consumed_orders_scope)
      @refunded_drink_avg_price_points = average_price_points(refunded_orders_scope)

      @started_at = @stream_session.created_at
      @ended_at = @stream_session.ended_at
      @duration_seconds =
        if @started_at.present? && @ended_at.present? && @ended_at >= @started_at
          (@ended_at - @started_at).to_i
        else
          0
        end
    end

    def finish
      ended_session = StreamSessions::EndService.new(
        stream_session: @stream_session,
        actor: current_user
      ).call

      redirect_to cast_booth_path(ended_session.booth_id), notice: "スタンバイを終了しました"
    rescue => e
      redirect_to cast_booth_path(@stream_session.booth_id), alert: e.message
    end

    def pending_drink_orders
      render partial: "cast/stream_sessions/pending_drink_orders",
             locals: { stream_session: @stream_session }
    end

    def meta_modal
      booth = @stream_session.booth

      unless booth.current_stream_session_id == @stream_session.id
        return head :forbidden
      end

      unless booth.standby?
        return head :conflict
      end

      render partial: "cast/stream_sessions/meta_modal",
             locals: { booth: booth, stream_session: @stream_session }
    rescue ActiveRecord::RecordNotFound
      head :not_found
    end

    def meta_display
      booth = @stream_session.booth

      unless booth.current_stream_session_id == @stream_session.id
        return head :forbidden
      end

      @booth = booth
    end

    # PATCH /cast/stream_sessions/:id/metadata
    def metadata
      booth = @stream_session.booth

      # 必須: current session 一致
      unless booth.current_stream_session_id == @stream_session.id
        return head :forbidden
      end

      # 状態ガード: standby のみ許可
      unless booth.standby?
        return respond_conflict(booth, "スタンバイ中のみ編集できます")
      end

      @stream_session.update!(metadata_params)

      # viewer 側に即反映（booth:stream_state 購読に乗せる）
      StreamSessionNotifier.broadcast_stream_state(booth: booth)

      respond_to do |format|
        format.html do
          redirect_to live_cast_booth_path(booth), notice: "設定しました"
        end

        format.turbo_stream do
          render turbo_stream: [
            turbo_stream.replace(
              "stream_meta_display",
              partial: "cast/stream_sessions/meta_display_frame",
              locals: { booth: booth, stream_session: @stream_session }
            ),
            turbo_stream.append(
              "flash_inner",
              partial: "shared/flash_message",
              locals: { level: "success", message: "設定しました" }
            )
          ]
        end

        format.json { render json: { ok: true }, status: :ok }
        format.any { head :ok }
      end
    rescue ActiveRecord::RecordInvalid => e
      message = e.record.errors.full_messages.join(", ").presence || "入力に誤りがあります"

      respond_to do |format|
        format.html { redirect_to live_cast_booth_path(booth), alert: message }
        format.turbo_stream do
          render turbo_stream: [
            turbo_stream.replace(
              "modal",
              partial: "cast/stream_sessions/meta_modal",
              locals: { booth: booth, stream_session: e.record }
            ),
            turbo_stream.append(
              "flash_inner",
              partial: "shared/flash_message",
              locals: { level: "danger", message: message }
            )
          ], status: :unprocessable_entity
        end
        format.json { render json: { error: message }, status: :unprocessable_entity }
        format.any { render plain: message, status: :unprocessable_entity }
      end
    rescue ActiveRecord::RecordNotFound
      head :not_found
    end

    private

    def set_stream_session
      @stream_session = StreamSession.find(params[:id])
    end

    def authorize_stream_session_access!
      return if operable_booth_for_stream_session?(@stream_session.booth)

      session.delete(:current_booth_id)
      head :forbidden
    end

    def metadata_params
      params.require(:stream_session).permit(:title, :description)
    end

    def operable_booth_for_stream_session?(booth)
      actor = current_user

      return false if actor.blank?
      return true if actor.system_admin?

      if actor.at_least?(:store_admin)
        return actor.admin_of_store?(booth.store_id)
      end

      BoothCast.exists?(cast_user_id: actor.id, booth_id: booth.id)
    end

    def average_price_points(scope)
      value = scope.joins(:drink_item).average(:price_points)
      value.present? ? value.to_f.round(1) : 0
    end

    def respond_conflict(booth, message)
      respond_to do |format|
        format.html { redirect_to live_cast_booth_path(booth), alert: message }
        format.turbo_stream do
          render turbo_stream: turbo_stream.append(
            "flash_inner",
            partial: "shared/flash_message",
            locals: { level: "danger", message: message }
          ), status: :conflict
        end
        format.json { render json: { error: message }, status: :conflict }
        format.any { render plain: message, status: :conflict }
      end
    end
  end
end
```

---

## 3. `app/views/cast/stream_sessions/show.html.erb`

新規作成です。

```erb
<% content_for :title, "配信リザルト" %>

<div class="row g-4">
  <div class="col-12">
    <div class="card border-0 shadow-sm">
      <div class="card-body">
        <div class="d-flex flex-wrap justify-content-between align-items-start gap-3">
          <div>
            <h1 class="h3 mb-2">今回の配信が終了しました</h1>
            <p class="text-body-secondary mb-0">
              stream_session #<%= @stream_session.id %>
              ／ booth #<%= @booth.id %> <%= @booth.name %>
            </p>
          </div>

          <div class="text-md-end">
            <div class="badge text-bg-secondary">終了済み</div>
            <% if @stream_session.title.present? %>
              <div class="mt-2 fw-semibold"><%= @stream_session.title %></div>
            <% end %>
          </div>
        </div>

        <% if @stream_session.description.present? %>
          <hr>
          <p class="mb-0"><%= @stream_session.description %></p>
        <% end %>
      </div>
    </div>
  </div>

  <div class="col-12">
    <div class="card border-0 shadow-sm">
      <div class="card-body">
        <h2 class="h5 mb-3">配信情報</h2>

        <div class="row g-3">
          <div class="col-12 col-md-4">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">配信開始</div>
              <div class="fw-semibold">
                <%= l(@started_at, format: :long) if @started_at.present? %>
              </div>
            </div>
          </div>

          <div class="col-12 col-md-4">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">配信終了</div>
              <div class="fw-semibold">
                <%= l(@ended_at, format: :long) if @ended_at.present? %>
              </div>
            </div>
          </div>

          <div class="col-12 col-md-4">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">配信時間</div>
              <div class="fw-semibold">
                <% hours = @duration_seconds / 3600 %>
                <% minutes = (@duration_seconds % 3600) / 60 %>
                <% seconds = @duration_seconds % 60 %>
                <%= format("%02d:%02d:%02d", hours, minutes, seconds) %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="col-12">
    <div class="card border-0 shadow-sm">
      <div class="card-body">
        <h2 class="h5 mb-3">配信結果サマリ</h2>

        <div class="row g-3">
          <div class="col-6 col-lg-3">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">コメント数</div>
              <div class="fs-4 fw-bold"><%= @comment_count %></div>
            </div>
          </div>

          <div class="col-6 col-lg-3">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">視聴者数</div>
              <div class="fs-4 fw-bold"><%= @viewer_count %></div>
              <div class="small text-body-secondary mt-1">累計ユニーク視聴者</div>
            </div>
          </div>

          <div class="col-6 col-lg-3">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">獲得ポイント</div>
              <div class="fs-4 fw-bold"><%= number_with_delimiter(@earned_points) %></div>
              <div class="small text-body-secondary mt-1">consumed 分の合計</div>
            </div>
          </div>

          <div class="col-6 col-lg-3">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">ドリンク総数</div>
              <div class="fs-4 fw-bold"><%= @drink_order_count %></div>
            </div>
          </div>
        </div>

        <hr>

        <div class="row g-3">
          <div class="col-12 col-md-4">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">消化ドリンク</div>
              <div class="fs-5 fw-bold"><%= @consumed_drink_count %></div>
              <div class="small text-body-secondary mt-2">
                平均単価: <%= number_with_precision(@consumed_drink_avg_price_points, precision: 1, strip_insignificant_zeros: true) %> pt
              </div>
            </div>
          </div>

          <div class="col-12 col-md-4">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">返却ドリンク</div>
              <div class="fs-5 fw-bold"><%= @refunded_drink_count %></div>
              <div class="small text-body-secondary mt-2">
                平均単価: <%= number_with_precision(@refunded_drink_avg_price_points, precision: 1, strip_insignificant_zeros: true) %> pt
              </div>
            </div>
          </div>

          <div class="col-12 col-md-4">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">ドリンク総平均単価</div>
              <div class="fs-5 fw-bold">
                <%= number_with_precision(@drink_order_avg_price_points, precision: 1, strip_insignificant_zeros: true) %> pt
              </div>
              <div class="small text-body-secondary mt-2">
                現在の drink_item.price_points ベース
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="col-12">
    <div class="d-flex flex-wrap gap-2">
      <%= link_to "ブースへ戻る", cast_booth_path(@booth), class: "btn btn-outline-secondary" %>
      <%= link_to "配信画面へ", live_cast_booth_path(@booth), class: "btn btn-outline-secondary" %>
      <%= link_to "キャスト管理へ", cast_booths_path, class: "btn btn-primary" %>
    </div>
  </div>
</div>
```

---

## 補足

今回の実装での数値定義はこうなります。

* 視聴者数: `presences` の累計ユニーク視聴者数
* 獲得ポイント: `store_ledger_entries.points` 合計
* 返却ドリンク数: 配信終了時に `pending` だった注文が `refunded` になった件数
* 平均単価: `drink_item.price_points` ベース
  価格スナップショットは未保持なので、将来 `drink_item` 価格変更が入ると過去結果とズレる可能性あり

このあと続けて必要なら、#399 用のコミットコメント1行と PR 文もそのまま出せます。

---
---

了解です。
**「消化ポイント / 平均消化ポイント / 返却ポイント / 平均返却ポイント」定義** に合わせた、`controller` と `view` の修正版全文です。

---

## `app/controllers/cast/stream_sessions_controller.rb`

全文差し替えしてください。

```ruby
# frozen_string_literal: true

module Cast
  class StreamSessionsController < Cast::BaseController
    before_action :set_stream_session, only: %i[show finish pending_drink_orders meta_modal meta_display metadata]
    before_action :authorize_stream_session_access!, only: %i[show finish pending_drink_orders meta_modal meta_display metadata]

    def show
      booth = @stream_session.booth

      unless @stream_session.ended?
        redirect_to live_cast_booth_path(booth), alert: "終了済み配信のリザルトのみ表示できます"
        return
      end

      comments_scope = Comment.alive.where(stream_session_id: @stream_session.id)
      drink_orders_scope = DrinkOrder.where(stream_session_id: @stream_session.id)
      consumed_orders_scope = drink_orders_scope.consumed
      refunded_orders_scope = drink_orders_scope.refunded
      presences_scope = Presence.where(stream_session_id: @stream_session.id)
      ledger_scope = StoreLedgerEntry.where(stream_session_id: @stream_session.id)

      refunded_order_ids = refunded_orders_scope.pluck(:id)
      refund_points_scope =
        WalletTransaction.where(
          kind: :release,
          ref_type: "DrinkOrder",
          ref_id: refunded_order_ids
        )

      @booth = booth

      @comment_count = comments_scope.count
      @viewer_count = presences_scope.distinct.count(:customer_user_id)

      @drink_order_count = drink_orders_scope.count
      @consumed_drink_count = consumed_orders_scope.count
      @refunded_drink_count = refunded_orders_scope.count

      @consumed_points = ledger_scope.sum(:points)
      @refunded_points = refund_points_scope.sum(:points)

      @avg_consumed_points =
        if @consumed_drink_count.positive?
          (@consumed_points.to_f / @consumed_drink_count).round(1)
        else
          0
        end

      @avg_refunded_points =
        if @refunded_drink_count.positive?
          (@refunded_points.to_f / @refunded_drink_count).round(1)
        else
          0
        end

      @started_at = @stream_session.started_at
      @ended_at = @stream_session.ended_at
      @duration_seconds =
        if @started_at.present? && @ended_at.present? && @ended_at >= @started_at
          (@ended_at - @started_at).to_i
        else
          0
        end
    end

    def finish
      ended_session = StreamSessions::EndService.new(
        stream_session: @stream_session,
        actor: current_user
      ).call

      redirect_to cast_booth_path(ended_session.booth_id), notice: "スタンバイを終了しました"
    rescue => e
      redirect_to cast_booth_path(@stream_session.booth_id), alert: e.message
    end

    def pending_drink_orders
      render partial: "cast/stream_sessions/pending_drink_orders",
             locals: { stream_session: @stream_session }
    end

    def meta_modal
      booth = @stream_session.booth

      unless booth.current_stream_session_id == @stream_session.id
        return head :forbidden
      end

      unless booth.standby?
        return head :conflict
      end

      render partial: "cast/stream_sessions/meta_modal",
             locals: { booth: booth, stream_session: @stream_session }
    rescue ActiveRecord::RecordNotFound
      head :not_found
    end

    def meta_display
      booth = @stream_session.booth

      unless booth.current_stream_session_id == @stream_session.id
        return head :forbidden
      end

      @booth = booth
    end

    # PATCH /cast/stream_sessions/:id/metadata
    def metadata
      booth = @stream_session.booth

      unless booth.current_stream_session_id == @stream_session.id
        return head :forbidden
      end

      unless booth.standby?
        return respond_conflict(booth, "スタンバイ中のみ編集できます")
      end

      @stream_session.update!(metadata_params)

      StreamSessionNotifier.broadcast_stream_state(booth: booth)

      respond_to do |format|
        format.html do
          redirect_to live_cast_booth_path(booth), notice: "設定しました"
        end

        format.turbo_stream do
          render turbo_stream: [
            turbo_stream.replace(
              "stream_meta_display",
              partial: "cast/stream_sessions/meta_display_frame",
              locals: { booth: booth, stream_session: @stream_session }
            ),
            turbo_stream.append(
              "flash_inner",
              partial: "shared/flash_message",
              locals: { level: "success", message: "設定しました" }
            )
          ]
        end

        format.json { render json: { ok: true }, status: :ok }
        format.any { head :ok }
      end
    rescue ActiveRecord::RecordInvalid => e
      message = e.record.errors.full_messages.join(", ").presence || "入力に誤りがあります"

      respond_to do |format|
        format.html { redirect_to live_cast_booth_path(booth), alert: message }
        format.turbo_stream do
          render turbo_stream: [
            turbo_stream.replace(
              "modal",
              partial: "cast/stream_sessions/meta_modal",
              locals: { booth: booth, stream_session: e.record }
            ),
            turbo_stream.append(
              "flash_inner",
              partial: "shared/flash_message",
              locals: { level: "danger", message: message }
            )
          ], status: :unprocessable_entity
        end
        format.json { render json: { error: message }, status: :unprocessable_entity }
        format.any { render plain: message, status: :unprocessable_entity }
      end
    rescue ActiveRecord::RecordNotFound
      head :not_found
    end

    private

    def set_stream_session
      @stream_session = StreamSession.find(params[:id])
    end

    def authorize_stream_session_access!
      return if operable_booth_for_stream_session?(@stream_session.booth)

      session.delete(:current_booth_id)
      head :forbidden
    end

    def metadata_params
      params.require(:stream_session).permit(:title, :description)
    end

    def operable_booth_for_stream_session?(booth)
      actor = current_user

      return false if actor.blank?
      return true if actor.system_admin?

      if actor.at_least?(:store_admin)
        return actor.admin_of_store?(booth.store_id)
      end

      BoothCast.exists?(cast_user_id: actor.id, booth_id: booth.id)
    end

    def respond_conflict(booth, message)
      respond_to do |format|
        format.html { redirect_to live_cast_booth_path(booth), alert: message }
        format.turbo_stream do
          render turbo_stream: turbo_stream.append(
            "flash_inner",
            partial: "shared/flash_message",
            locals: { level: "danger", message: message }
          ), status: :conflict
        end
        format.json { render json: { error: message }, status: :conflict }
        format.any { render plain: message, status: :conflict }
      end
    end
  end
end
```

---

## `app/views/cast/stream_sessions/show.html.erb`

新規作成してください。

```erb
<% content_for :title, "配信リザルト" %>

<div class="row g-4">
  <div class="col-12">
    <div class="card border-0 shadow-sm">
      <div class="card-body">
        <div class="d-flex flex-wrap justify-content-between align-items-start gap-3">
          <div>
            <h1 class="h3 mb-2">今回の配信が終了しました</h1>
            <p class="text-body-secondary mb-0">
              stream_session #<%= @stream_session.id %>
              ／ booth #<%= @booth.id %> <%= @booth.name %>
            </p>
          </div>

          <div class="text-md-end">
            <div class="badge text-bg-secondary">終了済み</div>
            <% if @stream_session.title.present? %>
              <div class="mt-2 fw-semibold"><%= @stream_session.title %></div>
            <% end %>
          </div>
        </div>

        <% if @stream_session.description.present? %>
          <hr>
          <p class="mb-0"><%= @stream_session.description %></p>
        <% end %>
      </div>
    </div>
  </div>

  <div class="col-12">
    <div class="card border-0 shadow-sm">
      <div class="card-body">
        <h2 class="h5 mb-3">配信情報</h2>

        <div class="row g-3">
          <div class="col-12 col-md-4">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">配信開始</div>
              <div class="fw-semibold">
                <%= l(@started_at, format: :long) if @started_at.present? %>
              </div>
            </div>
          </div>

          <div class="col-12 col-md-4">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">配信終了</div>
              <div class="fw-semibold">
                <%= l(@ended_at, format: :long) if @ended_at.present? %>
              </div>
            </div>
          </div>

          <div class="col-12 col-md-4">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">配信時間</div>
              <div class="fw-semibold">
                <% hours = @duration_seconds / 3600 %>
                <% minutes = (@duration_seconds % 3600) / 60 %>
                <% seconds = @duration_seconds % 60 %>
                <%= format("%02d:%02d:%02d", hours, minutes, seconds) %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="col-12">
    <div class="card border-0 shadow-sm">
      <div class="card-body">
        <h2 class="h5 mb-3">配信結果サマリ</h2>

        <div class="row g-3">
          <div class="col-6 col-lg-3">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">コメント数</div>
              <div class="fs-4 fw-bold"><%= @comment_count %></div>
            </div>
          </div>

          <div class="col-6 col-lg-3">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">視聴者数</div>
              <div class="fs-4 fw-bold"><%= @viewer_count %></div>
              <div class="small text-body-secondary mt-1">累計ユニーク視聴者</div>
            </div>
          </div>

          <div class="col-6 col-lg-3">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">ドリンク総数</div>
              <div class="fs-4 fw-bold"><%= @drink_order_count %></div>
            </div>
          </div>

          <div class="col-6 col-lg-3">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">消化ドリンク数</div>
              <div class="fs-4 fw-bold"><%= @consumed_drink_count %></div>
            </div>
          </div>
        </div>

        <hr>

        <div class="row g-3">
          <div class="col-12 col-md-6">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">消化ポイント</div>
              <div class="fs-4 fw-bold"><%= number_with_delimiter(@consumed_points) %> pt</div>
              <div class="small text-body-secondary mt-2">
                平均 <%= number_with_precision(@avg_consumed_points, precision: 1, strip_insignificant_zeros: true) %> pt / 消化ドリンク
              </div>
            </div>
          </div>

          <div class="col-12 col-md-6">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">返却ドリンク数</div>
              <div class="fs-4 fw-bold"><%= @refunded_drink_count %></div>
            </div>
          </div>
        </div>

        <div class="row g-3 mt-0">
          <div class="col-12 col-md-6">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">返却ポイント</div>
              <div class="fs-4 fw-bold"><%= number_with_delimiter(@refunded_points) %> pt</div>
              <div class="small text-body-secondary mt-2">
                平均 <%= number_with_precision(@avg_refunded_points, precision: 1, strip_insignificant_zeros: true) %> pt / 返却ドリンク
              </div>
            </div>
          </div>

          <div class="col-12 col-md-6">
            <div class="border rounded p-3 h-100">
              <div class="text-body-secondary small mb-1">ポイント差分</div>
              <div class="fs-4 fw-bold"><%= number_with_delimiter(@consumed_points - @refunded_points) %> pt</div>
              <div class="small text-body-secondary mt-2">
                消化ポイント − 返却ポイント
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="col-12">
    <div class="d-flex flex-wrap gap-2">
      <%= link_to "ブースへ戻る", cast_booth_path(@booth), class: "btn btn-outline-secondary" %>
      <%= link_to "配信画面へ", live_cast_booth_path(@booth), class: "btn btn-outline-secondary" %>
      <%= link_to "キャスト管理へ", cast_booths_path, class: "btn btn-primary" %>
    </div>
  </div>
</div>
```

---

補足だけ1点です。
この版は **返却ポイント = `WalletTransaction(kind: :release, ref=DrinkOrder)` 合計** で出しています。
今の実装には合っていますが、将来 `release` の用途が増えたら、その時点で定義を見直すのがよさそうです。

必要なら次に、**#399 のコミットコメント1行** と **PR文** をそのまま出します。

---
---

はい。現時点では #400 をまだ入れていないので、**終了後に自動遷移はしない**はずです。
そのため #399 単体の動作確認は、まず **result 画面へ直接アクセスできること** の確認になります。

確認用URLはこれです。

`/cast/stream_sessions/:id`

例:
`/cast/stream_sessions/123`

確認の流れはこうです。

1. cast で配信を開始して、対象の `stream_session.id` を控える
2. そのセッションを終了する
3. ブラウザで
   `/cast/stream_sessions/控えたid`
   を直接開く

この時点で期待する動作は次です。

* 終了済みセッションなら result 画面が表示される
* まだ終了していないセッションなら `live_cast_booth_path(@booth)` に戻される
* 権限がないユーザーなら 403 になる

なお、今の `finish` はまだこのままです。

* 配信終了後 → `cast_booth_path(session.booth_id)` に戻る

なので、**「終了ボタンを押したら自動で result へ行く」確認はまだできません**。
それは #400 の確認項目です。

もしすぐ確認しやすくしたいなら、Rails console かDB上で `stream_sessions` の最新 id を見て、その id を URL に入れるのが早いです。
必要なら次に、**Windows + Docker 前提での `stream_session.id` 確認コマンド** も出します。
