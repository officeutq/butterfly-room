## 1) ルーティングを「管理者トップ」に寄せる

いま `namespace :admin do ... end` の中に **root が無い**ので足します。
あわせて `get "/cast_metrics"` は resource で揃えておくと見通しがよいです（ただし今のままでもOK）。

### `config/routes.rb`（admin 部分だけ差し替え）

```ruby
  # --- Store Admin ---
  namespace :admin do
    root "dashboard#show"

    resource :store, only: %i[show update]
    resources :booths, only: %i[index create update]
    resources :drink_items, only: %i[index create update destroy]
    resources :store_bans, only: %i[create destroy]

    get :cast_metrics, to: "metrics#cast"
  end
```

> これで `admin_root_path` が生えます。

---

## 2) Query：`CastMetricsQuery`

`app/queries/cast_metrics_query.rb`

```ruby
# frozen_string_literal: true

class CastMetricsQuery
  Row = Struct.new(
    :cast_user_id,
    :cast_display_name,
    :stream_sales_points,
    :stream_seconds,
    :sales_per_hour,
    :real_store_sales_yen,
    keyword_init: true
  )

  DEFAULT_RANGE_DAYS = 30

  def initialize(store:, from: nil, to: nil, now: Time.zone.now)
    @store = store
    @now  = now

    @to   = (to || @now).to_time
    @from = (from || (@to - DEFAULT_RANGE_DAYS.days)).to_time
  end

  def call
    casts = store_cast_users

    sales_by_cast   = stream_sales_points_by_cast_user_id
    seconds_by_cast = stream_seconds_by_cast_user_id

    casts.map do |u|
      sales = sales_by_cast[u.id] || 0
      secs  = seconds_by_cast[u.id] || 0

      Row.new(
        cast_user_id: u.id,
        cast_display_name: u.display_name.presence || u.email,
        stream_sales_points: sales,
        stream_seconds: secs,
        sales_per_hour: calc_sales_per_hour(sales, secs),
        real_store_sales_yen: nil # Phase1: 参照は未実装でOK
      )
    end
  end

  private

  attr_reader :store, :from, :to, :now

  def store_cast_users
    User
      .joins(:store_memberships)
      .where(store_memberships: { store_id: store.id, membership_role: StoreMembership.membership_roles[:cast] })
      .distinct
      .order(:id)
  end

  def stream_sales_points_by_cast_user_id
    StoreLedgerEntry
      .joins(:stream_session)
      .where(store_id: store.id)
      .where(occurred_at: from...to)
      .group("stream_sessions.started_by_cast_user_id")
      .sum(:points)
  end

  def stream_seconds_by_cast_user_id
    sessions =
      StreamSession
        .where(store_id: store.id)
        .where("started_at < ? AND COALESCE(ended_at, ?) > ?", to, now, from)

    seconds_by_cast = Hash.new(0)

    sessions.find_each do |s|
      start_t = [s.started_at, from].max
      end_t   = [s.ended_at || now, to].min
      dur = end_t - start_t
      dur = 0 if dur.negative?

      seconds_by_cast[s.started_by_cast_user_id] += dur.to_i
    end

    seconds_by_cast
  end

  def calc_sales_per_hour(points, seconds)
    return nil if seconds.to_i <= 0
    (points.to_f / (seconds.to_f / 3600.0)).round(2)
  end
end
```

> ⚠️ `User.joins(:store_memberships)` を使うので、**User に関連が必要**です（後述のモデル修正）。

---

## 3) User モデルに関連を追加（必須）

`app/models/user.rb` に追記：

```ruby
  has_many :store_memberships, dependent: :destroy
  has_many :stores, through: :store_memberships
```

（wallet の下でも上でもOK）

---

## 4) Controller：`Admin::MetricsController#cast`

`app/controllers/admin/metrics_controller.rb`（新規）

```ruby
# frozen_string_literal: true

module Admin
  class MetricsController < Admin::BaseController
    before_action :require_current_store!

    def cast
      # Phase1: 直近30日固定（Query側デフォルト）
      @rows = CastMetricsQuery.new(store: current_store).call
    end
  end
end
```

---

## 5) View

### 1) キャスト別数値一覧

`app/views/admin/metrics/cast.html.erb`

```erb
<h1>キャスト別数値一覧</h1>

<p class="text-muted">
  期間：直近30日（Phase1の暫定）
</p>

<table class="table table-sm">
  <thead>
    <tr>
      <th>キャスト</th>
      <th class="text-end">配信売上(pt)</th>
      <th class="text-end">配信時間</th>
      <th class="text-end">売上/時間</th>
      <th class="text-end">実店舗売上(参照)</th>
    </tr>
  </thead>
  <tbody>
    <% @rows.each do |r| %>
      <tr>
        <td><%= r.cast_display_name %></td>
        <td class="text-end"><%= r.stream_sales_points %></td>
        <td class="text-end"><%= seconds_to_hhmm(r.stream_seconds) %></td>
        <td class="text-end"><%= r.sales_per_hour || "-" %></td>
        <td class="text-end"><%= r.real_store_sales_yen || "-" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
```

Helper（表示用）を足す：

`app/helpers/admin/metrics_helper.rb`

```ruby
# frozen_string_literal: true

module Admin::MetricsHelper
  def seconds_to_hhmm(seconds)
    s = seconds.to_i
    h = s / 3600
    m = (s % 3600) / 60
    format("%d:%02d", h, m)
  end
end
```

### 2) 管理者トップ画面を作る

`app/controllers/admin/dashboard_controller.rb`（新規）

```ruby
# frozen_string_literal: true

module Admin
  class DashboardController < Admin::BaseController
    before_action :require_current_store!

    def show
    end
  end
end
```

`app/views/admin/dashboard/show.html.erb`

```erb
<h1>管理者ページ</h1>

<ul>
  <li><%= link_to "店舗設定", admin_store_path %></li>
  <li><%= link_to "ブース管理", admin_booths_path %></li>
  <li><%= link_to "ドリンクメニュー", admin_drink_items_path %></li>
  <li><%= link_to "キャスト別数値一覧", admin_cast_metrics_path %></li>
</ul>
```

### 3) home のリンクを「Admin booths」→「Admin」へ

`app/views/home/show.html.erb` の該当行を差し替え。

```erb
<li><%= link_to "Admin", admin_root_path %></li>
```

そして、`Admin drink items` の直リンクは **ポータルに集約**できるので削ってOKです（残しても動く）。

Closes #23 
Relates to #12 
---

## 6) 動作確認手順（最短）

1. ファイル追加・修正
2. ルーティング確認
   `/admin/cast_metrics` は既にあるのでそのまま
3. store_admin でログインして
   `http://localhost:3000/admin/cast_metrics` を開く

   * store_memberships に **admin** があるユーザーでないと `403`（BaseController仕様）
   * `StoreMembership(membership_role: cast)` のキャストがいないと “空の表” になる（正常）

---

## 7) 落とし穴（今回の構成でハマりやすい所）

* **User に `has_many :store_memberships` が無いと joins で落ちる**（今回必須）
* **売上の帰属は「主担当キャスト（started_by_cast_user_id）」**
  台帳側に cast が無いので、ここに join するしかない（Phase1仕様として自然）
* **ended_at nil（配信中）**は Query で now を使って計算しているので、一覧が“伸びる”のは仕様
* **期間固定**でも Query は `from/to` を受け取れるので、後から params 化しやすい
