## #86 ブランチ名・コミット名

* **ブランチ名**：`feature/86-booth-edit-ui`
* **コミット名（1発）**：`feat(booths): add edit UI for description and thumbnail`

分割なら：

1. `feat(booths): implement booth edit/update for cast and admin`
2. `test(booths): add integration tests for booth profile update`

---

## 1) ルーティング追加

`config/routes.rb` の該当箇所を変更：

### Admin

```ruby
namespace :admin do
  ...
  resources :booths, only: %i[index show edit create update] do
    member do
      get :watch
    end
  end
  ...
end
```

---

## 2) Cast::BoothsController に edit/update を追加（認可付き）

`app/controllers/cast/booths_controller.rb`

ポイント：

* `show` で `@booth` を取ってるので、共通の `before_action :set_booth` に寄せる
* #85 の `Authorization::BoothPolicy#update?` を使って **403** にする

```ruby
module Cast
  class BoothsController < Cast::BaseController
    before_action :set_booth, only: %i[show status edit update]
    before_action :authorize_update!, only: %i[edit update]

    def index
      @booths = Booth.order(:id)
    end

    def show
      @stream_session = @booth.current_stream_session
      @comments =
        if @stream_session.present?
          Comment.alive.where(stream_session: @stream_session)
                .order(created_at: :desc)
                .limit(50)
                .reverse
        else
          []
        end
    end

    def edit
    end

    def update
      if remove_thumbnail_image?
        @booth.thumbnail_image.purge_later if @booth.thumbnail_image.attached?
      end

      if @booth.update(booth_params)
        redirect_to cast_booth_path(@booth), notice: "更新しました"
      else
        render :edit, status: :unprocessable_entity
      end
    end

    def status
      booth = Booth.find(params[:id])
      ...
    end

    private

    def set_booth
      @booth = Booth.find(params[:id])
    end

    def authorize_update!
      policy = Authorization::BoothPolicy.new(current_user, @booth)
      head :forbidden unless policy.update?
    end

    def booth_params
      params.require(:booth).permit(:description, :thumbnail_image)
    end

    def remove_thumbnail_image?
      params.dig(:booth, :remove_thumbnail_image) == "1"
    end
  end
end
```

---

## 3) Admin::BoothsController に edit/update を実装（認可付き）

現状 `show` が stub、`update` も実体なしなので、まず edit/update を作る。

`app/controllers/admin/booths_controller.rb`

```ruby
module Admin
  class BoothsController < Admin::BaseController
    before_action :require_current_store!, only: %i[watch]
    before_action :set_booth, only: %i[show edit update watch]
    before_action :authorize_update!, only: %i[edit update]

    def index
      render plain: "admin booths index (stub)"
    end

    def show
      # 最短で導線を置くため、一旦 plain じゃなく view を用意する方が楽
      # ここは後述の show.html.erb を作る
    end

    def edit
    end

    def update
      if remove_thumbnail_image?
        @booth.thumbnail_image.purge_later if @booth.thumbnail_image.attached?
      end

      if @booth.update(booth_params)
        redirect_to admin_booth_path(@booth), notice: "更新しました"
      else
        render :edit, status: :unprocessable_entity
      end
    end

    def watch
      policy = Authorization::BoothPolicy.new(current_user, @booth)
      head :forbidden and return unless policy.admin_operate?

      stream_session = StreamSession.find_by(id: @booth.current_stream_session_id)

      if stream_session.blank? || !stream_session.live?
        redirect_to admin_booth_path(@booth), alert: "配信中ではありません"
        return
      end

      redirect_to booth_path(@booth)
    end

    private

    def set_booth
      @booth = Booth.find(params[:id])
    end

    def authorize_update!
      policy = Authorization::BoothPolicy.new(current_user, @booth)
      head :forbidden unless policy.update?
    end

    def booth_params
      params.require(:booth).permit(:description, :thumbnail_image)
    end

    def remove_thumbnail_image?
      params.dig(:booth, :remove_thumbnail_image) == "1"
    end
  end
end
```

---

## 4) View を作る

### 4.1 Cast show に「編集」導線を追加

`app/views/cast/booths/show.html.erb` の最後の戻るリンク付近などに追加（分かりやすい位置でOK）：

```erb
<% policy = Authorization::BoothPolicy.new(current_user, @booth) %>
<% if policy.update? %>
  <%= link_to "ブース編集", edit_cast_booth_path(@booth), class: "btn btn-outline-secondary btn-sm" %>
<% end %>
```

※ cast show は castユーザーが見る画面なので customer は来ないですが、要件「導線は編集可能ロールのみ」に沿って policy で制御しておくのが安全です。

---

### 4.2 Cast edit 画面

`app/views/cast/booths/edit.html.erb`（新規）

```erb
<h1>ブース編集</h1>

<%= form_with model: [:cast, @booth] do |f| %>
  <div class="mb-3">
    <%= f.label :description, "説明文", class: "form-label" %>
    <%= f.text_area :description, class: "form-control", rows: 6 %>
  </div>

  <div class="mb-3">
    <%= f.label :thumbnail_image, "サムネ画像", class: "form-label" %>

    <% if @booth.thumbnail_image.attached? %>
      <div class="mb-2">
        <%= image_tag @booth.thumbnail_image, style: "max-width: 240px; height: auto;" %>
      </div>

      <div class="form-check mb-2">
        <%= check_box_tag "booth[remove_thumbnail_image]", "1", false,
                          class: "form-check-input", id: "remove_thumbnail_image" %>
        <%= label_tag "remove_thumbnail_image", "現在のサムネを削除する", class: "form-check-label" %>
      </div>
    <% end %>

    <%= f.file_field :thumbnail_image, class: "form-control" %>
  </div>

  <div class="d-flex gap-2">
    <%= f.submit "更新する", class: "btn btn-primary" %>
    <%= link_to "戻る", cast_booth_path(@booth), class: "btn btn-outline-secondary" %>
  </div>
<% end %>
```

---

### 4.3 Admin show / edit 画面（最低限）

いま `admin/booths/show.html.erb` が無いので、**まずこれを作って導線を置く**のが早いです。

`app/views/admin/booths/show.html.erb`（新規）

```erb
<h1>ブース詳細</h1>

<p>booth: <strong>#<%= @booth.id %> <%= @booth.name %></strong></p>

<p>description:</p>
<pre class="bg-light p-2"><%= @booth.description.presence || "-" %></pre>

<p>thumbnail:</p>
<% if @booth.thumbnail_image.attached? %>
  <%= image_tag @booth.thumbnail_image, style: "max-width: 240px; height: auto;" %>
<% else %>
  <div class="text-muted">なし</div>
<% end %>

<%# 視聴導線（配信中のみ watch に行く） %>
<div class="mt-3 d-flex gap-2">
  <%= link_to "視聴", watch_admin_booth_path(@booth), class: "btn btn-primary btn-sm" %>

  <%# 既存の編集導線（権限ありのみ） %>
  <% policy = Authorization::BoothPolicy.new(current_user, @booth) %>
  <% if policy.update? %>
    <%= link_to "編集", edit_admin_booth_path(@booth), class: "btn btn-outline-secondary btn-sm" %>
  <% end %>
</div>
```

`Admin::BoothsController#show` も view を render するように変更（plain stubをやめる）：

```ruby
def show
end
```

`app/views/admin/booths/edit.html.erb`（新規）
※ cast edit とほぼ同じで、modelだけ `[:admin, @booth]`

```erb
<h1>ブース編集</h1>

<%= form_with model: [:admin, @booth] do |f| %>
  ...
<% end %>
```

---

## 5) テスト（更新成功をここに集約）

### 5.1 準備：アップロード用ファイルを置く

`test/fixtures/files/thumb.png` を追加（小さいpngでOK）

### 5.2 Integration test 追加

`test/integration/booth_edit_success_test.rb`（新規）

* cast（所属booth）で description 更新＋thumbnail attach が成功する
* store_admin（自店舗booth）で同様
* customer には public 側で「編集」文字列が出ない（軽く）

例（Store の必須カラムが name だけ前提。違うならそこだけ合わせてください）

```ruby
# frozen_string_literal: true

require "test_helper"

class BoothEditSuccessTest < ActionDispatch::IntegrationTest
  include ActionDispatch::TestProcess

  setup do
    @store1 = Store.create!(name: "store1")
    @booth1 = Booth.create!(store: @store1, name: "booth1", status: :offline)

    @cast = User.create!(email: "cast86@example.com", password: "password", role: :cast)
    BoothCast.create!(booth: @booth1, cast_user: @cast)

    @store_admin = User.create!(email: "admin86@example.com", password: "password", role: :store_admin)
    StoreMembership.create!(store: @store1, user: @store_admin, membership_role: :admin)

    @customer = User.create!(email: "cust86@example.com", password: "password", role: :customer)
  end

  test "cast can update description and attach thumbnail" do
    sign_in @cast, scope: :user

    file = fixture_file_upload(Rails.root.join("test/fixtures/files/thumb.png"), "image/png")

    patch cast_booth_path(@booth1), params: {
      booth: { description: "new desc", thumbnail_image: file }
    }

    assert_redirected_to cast_booth_path(@booth1)

    @booth1.reload
    assert_equal "new desc", @booth1.description
    assert @booth1.thumbnail_image.attached?
  end

  test "store_admin can update description and attach thumbnail" do
    sign_in @store_admin, scope: :user

    file = fixture_file_upload(Rails.root.join("test/fixtures/files/thumb.png"), "image/png")

    patch admin_booth_path(@booth1), params: {
      booth: { description: "admin desc", thumbnail_image: file }
    }

    assert_redirected_to admin_booth_path(@booth1)

    @booth1.reload
    assert_equal "admin desc", @booth1.description
    assert @booth1.thumbnail_image.attached?
  end

  test "customer does not see edit link on public booth show" do
    sign_in @customer, scope: :user

    get booth_path(@booth1)
    assert_response :success

    assert_not_includes response.body, "編集"
    assert_not_includes response.body, "ブース編集"
  end
end
```

> public `booths#show` がまだ stub でも「編集導線を入れてない」ならこのテストは通りやすいです。
> もし public show に “編集” という文字が別用途で出ていたら、文字列をより限定してください（例：`edit_cast_booth_path` が含まれない、など）。

### 5.3 #85 のテストをauthorized のときは “403じゃない” ことを確認するテストに直す

**変更前：**
```ruby
  test "store_admin can update only own store booth" do
    sign_in @store_admin, scope: :user

    patch admin_booth_path(@booth1), params: { booth: { name: "x" } }
    assert_response :not_implemented # update stubならこれでOK（403にならないのが重要）

    patch admin_booth_path(@booth2), params: { booth: { name: "x" } }
    assert_response :forbidden
  end

  test "system_admin can update any booth" do
    sign_in @system_admin, scope: :user

    patch admin_booth_path(@booth1), params: { booth: { name: "x" } }
    assert_response :not_implemented

    patch admin_booth_path(@booth2), params: { booth: { name: "x" } }
    assert_response :not_implemented
  end
```
**変更後：**
```ruby
  test "store_admin can update only own store booth" do
    sign_in @store_admin, scope: :user

    patch admin_booth_path(@booth1), params: { booth: { name: "x" } }
    assert_response :redirect
    assert_redirected_to admin_booth_path(@booth1)

    patch admin_booth_path(@booth2), params: { booth: { name: "x" } }
    assert_response :forbidden
  end

  test "system_admin can update any booth" do
    sign_in @system_admin, scope: :user

    patch admin_booth_path(@booth1), params: { booth: { name: "x" } }
    assert_response :redirect
    assert_redirected_to admin_booth_path(@booth1)

    patch admin_booth_path(@booth2), params: { booth: { name: "x" } }
    assert_response :redirect
    assert_redirected_to admin_booth_path(@booth2)
  end
```


---

## 6) 実装の落とし穴

* `image_tag @booth.thumbnail_image` は dev/test でも表示できる（ActiveStorage導入済み前提）
* purge はチェックボックスで明示的に行う（ファイル未選択と混同しない）
* `form_with` は file_fieldがあれば multipart になるので基本OK
* Turboの影響を避けたいなら `data: { turbo: false }` もあり（ただ基本は不要）

---

## 7) 実行コマンド（docker compose 経由）

```powershell
docker compose exec app bash -lc 'bin/rails test test/integration/booth_edit_success_test.rb'
```

Closes #86 
Relates to #82 