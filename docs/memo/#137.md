了解、Issue #137 は **「登録完了（ログイン）→ store_admin ダッシュボード（`admin_root_path`）」**で実装します。
以下、**実装プラン → 変更ファイル一覧 → コピペ用コード一式**の順で出します。

---

## 実装プラン（高レベル）

1. **DB**

* `stores.referral_code_id` を追加（FK）。
  ※ 既存 `stores` がある可能性があるので、まずは **NULL許容**で追加（Issueの「NULL不可推奨」に沿って、登録フローでは必須扱い・後でNOT NULL化できる状態にする）。

2. **Public ルーティング追加**

* `GET /stores/new_registration` → 登録フォーム
* `POST /stores/registrations` → 登録作成

3. **フォームオブジェクト + Service（トランザクション）**

* `Stores::RegistrationForm` で入力検証（referral_code / email重複など）
* `Stores::RegisterStoreAdmin`（仮名）で `Store + User(store_admin) + StoreMembership(admin)` をトランザクション生成

4. **登録成功時**

* `sign_in(user)` して `redirect_to admin_root_path`（notice 付き）

5. **登録失敗時**

* 422でフォーム再表示（エラーメッセージをフォーム内に表示）
* flash箱は layout にあるので、成功noticeはそこに乗る

---

## ブランチ名候補

* `feature/137-store-registration-store-admin`
* `feature/137-store-admin-first-signup`
* `feature/137-store-registration-with-referral-code`
* `feature/137-public-store-onboarding`

---

## 変更/追加ファイル一覧

### 変更

* `config/routes.rb`
* `app/models/store.rb`
* `app/models/referral_code.rb`
* `app/views/home/show.html.erb`

### 追加

* `app/controllers/stores/registrations_controller.rb`
* `app/forms/stores/registration_form.rb`
* `app/services/stores/register_store_admin.rb`
* `app/views/stores/registrations/new.html.erb`
* `db/migrate/XXXXXXXXXX_add_referral_code_id_to_stores.rb`
* （任意だけどおすすめ）`test/integration/stores_registration_test.rb`

---

## 実装コード（コピペ用）

### 1) ルーティング追加（`config/routes.rb`）

`root` の近く（Publicの前後どちらでもOK）に追記：

```rb
Rails.application.routes.draw do
  get "up" => "rails/health#show", as: :rails_health_check

  devise_for :users, skip: %i[registrations]

  root "home#show"

  # --- Public (store registration) ---
  namespace :stores do
    get  :new_registration, to: "registrations#new"
    post :registrations,    to: "registrations#create"
  end

  # --- Public (customer) ---
  resources :booths, only: %i[show]
  # ...（以下そのまま）
end
```

これで以下が使えます：

* `stores_new_registration_path`
* `stores_registrations_path`

---

### 2) マイグレーション（stores に referral_code_id 追加）

ユーザー好みのやり方で「空マイグレーション生成 → 全文貼り替え」でいきます。

**生成コマンド（Docker Compose 経由）**

```bash
docker compose run --rm app bin/rails g migration AddReferralCodeIdToStores
```

生成されたファイルを **全文これに置き換え**：

```rb
# frozen_string_literal: true

class AddReferralCodeIdToStores < ActiveRecord::Migration[8.1]
  def up
    unless column_exists?(:stores, :referral_code_id)
      add_reference :stores, :referral_code, foreign_key: false, index: false, null: true
    end

    unless index_exists?(:stores, :referral_code_id)
      add_index :stores, :referral_code_id
    end

    unless foreign_key_exists?(:stores, :referral_codes)
      add_foreign_key :stores, :referral_codes
    end
  end

  def down
    remove_foreign_key :stores, :referral_codes if foreign_key_exists?(:stores, :referral_codes)
    remove_index :stores, :referral_code_id if index_exists?(:stores, :referral_code_id)
    remove_reference :stores, :referral_code if column_exists?(:stores, :referral_code_id)
  end
end
```

**実行**

```bash
docker compose run --rm app bin/rails db:migrate
```

> NOTE: Issueには「NULL不可推奨」。既存Storeがあると `null: false` は即死しやすいので、まずは `null: true` で追加し、**登録フロー上は必須**にします。後でデータが揃ったら `null: false` へ締めるマイグレーションを追加できます。

---

### 3) モデル関連付け（`app/models/store.rb`）

```rb
# frozen_string_literal: true

class Store < ApplicationRecord
  belongs_to :referral_code, optional: true

  has_many :booths, dependent: :destroy
  has_many :store_memberships, dependent: :destroy
  has_many :drink_items, dependent: :destroy
  has_many :store_bans, dependent: :destroy
end
```

### 4) モデル関連付け（`app/models/referral_code.rb`）

```rb
# frozen_string_literal: true

class ReferralCode < ApplicationRecord
  has_many :stores, dependent: :nullify

  validates :code, presence: true, uniqueness: true

  def expired?
    expires_at.present? && expires_at < Time.current
  end

  # 店舗登録で「使用可能」かどうか（#137側で利用する想定）
  def usable?
    enabled? && !expired?
  end
end
```

---

### 5) Public コントローラ追加（`app/controllers/stores/registrations_controller.rb`）

```rb
# frozen_string_literal: true

module Stores
  class RegistrationsController < ApplicationController
    skip_before_action :authenticate_user!, raise: false

    def new
      @form = Stores::RegistrationForm.new(referral_code: params[:ref].to_s)
    end

    def create
      @form = Stores::RegistrationForm.new(registration_params)

      if @form.save
        sign_in(@form.user) # Devise
        redirect_to admin_root_path, notice: "店舗登録が完了しました"
      else
        render :new, status: :unprocessable_entity
      end
    end

    private

    def registration_params
      params.require(:store_registration).permit(
        :store_name,
        :email,
        :password,
        :password_confirmation,
        :referral_code
      )
    end
  end
end
```

---

### 6) フォームオブジェクト追加（`app/forms/stores/registration_form.rb`）

```rb
# frozen_string_literal: true

module Stores
  class RegistrationForm
    include ActiveModel::Model
    include ActiveModel::Attributes

    attribute :store_name, :string
    attribute :email, :string
    attribute :password, :string
    attribute :password_confirmation, :string
    attribute :referral_code, :string

    attr_reader :user, :store

    validates :store_name, presence: true
    validates :email, presence: true
    validates :password, presence: true, confirmation: true
    validates :password_confirmation, presence: true
    validates :referral_code, presence: true
    validate  :referral_code_must_be_usable
    validate  :email_must_be_unique

    def save
      return false unless valid?

      result = Stores::RegisterStoreAdmin.call!(
        store_name: store_name,
        email: email,
        password: password,
        referral_code: referral_code
      )

      @store = result.store
      @user  = result.user
      true
    rescue ActiveRecord::RecordInvalid => e
      errors.add(:base, e.record.errors.full_messages.join(", "))
      false
    end

    private

    def referral_code_must_be_usable
      rc = ReferralCode.find_by(code: referral_code)
      if rc.nil? || !rc.usable?
        errors.add(:referral_code, "が無効です")
      end
    end

    def email_must_be_unique
      return if email.blank?

      if User.exists?(email: email)
        errors.add(:email, "このemailアドレスはすでに使われています")
      end
    end
  end
end
```

---

### 7) サービス追加（`app/services/stores/register_store_admin.rb`）

```rb
# frozen_string_literal: true

module Stores
  class RegisterStoreAdmin
    Result = Struct.new(:store, :user, keyword_init: true)

    def self.call!(store_name:, email:, password:, referral_code:)
      new(store_name:, email:, password:, referral_code:).call!
    end

    def initialize(store_name:, email:, password:, referral_code:)
      @store_name = store_name
      @email = email
      @password = password
      @referral_code = referral_code
    end

    def call!
      rc = ReferralCode.find_by!(code: @referral_code)
      raise ActiveRecord::RecordInvalid.new(rc) unless rc.usable?

      store = nil
      user = nil

      ActiveRecord::Base.transaction do
        store = Store.create!(
          name: @store_name,
          referral_code: rc
        )

        user = User.create!(
          email: @email,
          password: @password,
          password_confirmation: @password,
          role: :store_admin
        )

        StoreMembership.create!(
          store: store,
          user: user,
          membership_role: :admin
        )
      end

      Result.new(store: store, user: user)
    end
  end
end
```

---

### 8) 登録フォーム view 追加（`app/views/stores/registrations/new.html.erb`）

Bootstrap前提の最小：

```erb
<h1 class="container mt-4">店舗として登録</h1>

<div class="container mt-3" style="max-width: 720px;">
  <% if @form.errors.any? %>
    <div class="alert alert-danger">
      <ul class="mb-0">
        <% @form.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="card">
    <div class="card-body">
      <%= form_with model: @form, url: stores_registrations_path, scope: :store_registration do |f| %>
        <div class="mb-3">
          <%= f.label :store_name, "店舗名", class: "form-label" %>
          <%= f.text_field :store_name, class: "form-control", required: true, autocomplete: "organization" %>
        </div>

        <div class="mb-3">
          <%= f.label :email, "メールアドレス", class: "form-label" %>
          <%= f.email_field :email, class: "form-control", required: true, autocomplete: "email" %>
        </div>

        <div class="mb-3">
          <%= f.label :password, "パスワード", class: "form-label" %>
          <%= f.password_field :password, class: "form-control", required: true, autocomplete: "new-password" %>
        </div>

        <div class="mb-3">
          <%= f.label :password_confirmation, "パスワード（確認）", class: "form-label" %>
          <%= f.password_field :password_confirmation, class: "form-control", required: true, autocomplete: "new-password" %>
        </div>

        <div class="mb-3">
          <%= f.label :referral_code, "紹介コード", class: "form-label" %>
          <%= f.text_field :referral_code, class: "form-control", required: true, autocomplete: "off" %>
          <div class="form-text">QR/DMに記載のコードを入力してください。</div>
        </div>

        <div class="d-flex gap-2">
          <%= f.submit "登録して管理画面へ", class: "btn btn-primary" %>
          <%= link_to "戻る", root_path, class: "btn btn-outline-secondary" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
```

---

### 9) home stub に導線追加（`app/views/home/show.html.erb`）

リンクを1本追加（ref付きの確認もしやすい）：

```erb
<ul>
  <li><%= link_to "Public booth show (id=7)", booth_path(7) %></li>
  <li><%= link_to "Cast booths", cast_booths_path %></li>
  <li><%= link_to "Store Admin Dashboard", admin_root_path %></li>
  <li><%= link_to "System Admin Dashboard", system_admin_root_path %></li>
  <li><%= link_to "店舗として登録（ref=TEST）", stores_new_registration_path(ref: "TEST") %></li>
</ul>
```

---

## （任意）最小の動作確認手順

1. system_admin でログイン → `/system_admin/referral_codes` から `code=TEST` を作成（enabled=true）
2. ログアウト
3. `/stores/new_registration?ref=TEST` へ
4. 店舗名・email・password を入力して登録
5. **admin dashboard（`/admin`）に遷移**できる（403にならない）
6. DBで `Store.referral_code_id` が入っていること、`User.role=store_admin`、`StoreMembership.admin` ができていることを確認

---

次にやるなら：このまま実装して動かして、もし `admin/dashboard#show` が空で寂しいなら「初回セットアップ（ブース作成へ）」のカードを置く、が自然です。

必要なら、このまま **PR文（目的/変更点/動作確認）とコミットメッセージ案**も出します。
