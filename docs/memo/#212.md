GO 了解。#212 の範囲で「最小差分」で進めつつ、**全ページで header/main/footer を統一**し、**フッター導線を確定仕様どおり**に揃えます（07設計準拠）。

---

## 実装方針（#212）

### 1) `application.html.erb` を header/main/footer に再構成

* **ヘッダー**：

  * 表示名 or email（`display_name_or_email` 既存利用）
  * role
  * current_store / current_booth（参照のみ。未選択は未選択表示）
  * wallet 残高（nil→0）
  * ログアウト（最小：ボタン）
* **メイン**：

  * `flash_inner` のDOMを維持（StoreBanGuard の turbo_stream target を壊さない）
  * `yield`
* **フッター**：

  * ホーム / お気に入り / 配信（cast以上のみ）/ 購入（POST）/ ダッシュボード（role別）

### 2) “container二重” を避ける

`main` 側で `container` を持つ方針にするので、**すでにページ側で container を持っている**ものは外します。

* `home/show`（ログイン時/未ログイン時）
* `favorites/*/index`

（booths/show や cast/admin/system_admin は現状 container が無いので、そのままでOK）

### 3) フッターの「購入（POST）」の扱い

* 07設計どおり「フッター購入は booth_id なし」
* `Wallet::PurchasesController` が `points` 必須なので、**固定で 1000pt** を送ります（最小）
* リダイレクトが外部ホストなので `data-turbo="false"` を付けます（booths/show と同じ）

---

## ブランチ名（再掲）

* `improve/212-layout-header-footer`
* `improve/212-unify-application-layout`
* `improve/212-global-layout-shell`

---

## 変更コード（貼り付け用）

### 1) `app/helpers/application_helper.rb`（追記・拡張）

```ruby
# frozen_string_literal: true

module ApplicationHelper
  def booth_status_badge(booth)
    return "" if booth.blank?

    live_like = booth.live? || booth.away?

    label = live_like ? "配信中" : "オフライン"
    klass = live_like ? "text-bg-danger" : "text-bg-secondary"

    content_tag(:span, label, class: "badge #{klass}")
  end

  def display_name_or_email(user)
    return "" if user.blank?

    user.display_name.presence || user.email
  end

  # --- Layout helpers (Issue #212) ---

  def role_badge(user)
    return "" if user.blank?

    label = user.role.to_s
    content_tag(:span, label, class: "badge text-bg-secondary")
  end

  def dashboard_path_for(user)
    return root_path if user.blank?

    if user.system_admin?
      system_admin_root_path
    elsif user.store_admin?
      admin_root_path
    elsif user.cast?
      cast_booths_path
    else
      root_path
    end
  end

  # 参照表示用：可能な範囲で current_store / current_booth を解決する（副作用なし）
  def layout_current_store
    return nil unless user_signed_in?

    # 管理画面では既存メソッドがあるのでそれを優先（整合ロジックも内包）
    return current_store if respond_to?(:current_store)

    store_id = session[:current_store_id]
    return nil if store_id.blank?

    store = Store.find_by(id: store_id)
    return nil if store.blank?

    # system_admin は参照のみOK
    return store if current_user.system_admin?

    # store_admin は自分の admin membership がある store のみ参照OK
    return store if current_user.at_least?(:store_admin) && current_user.admin_of_store?(store.id)

    nil
  end

  def layout_current_booth
    return nil unless user_signed_in?

    # cast配下では既存メソッドがあるのでそれを優先（操作可能チェックも内包）
    return current_booth if respond_to?(:current_booth)

    booth_id = session[:current_booth_id]
    return nil if booth_id.blank?

    booth = Booth.find_by(id: booth_id)
    return nil if booth.blank?

    # system_admin は参照のみOK
    return booth if current_user.system_admin?

    # store_admin は自分の store の booth を参照OK
    if current_user.at_least?(:store_admin) && current_user.admin_of_store?(booth.store_id)
      return booth
    end

    # cast は所属 booth のみ参照OK
    return booth if current_user.at_least?(:cast) && BoothCast.exists?(cast_user_id: current_user.id, booth_id: booth.id)

    nil
  end

  def layout_wallet_points
    return 0 unless user_signed_in?

    current_user.wallet&.available_points || 0
  end
end
```

---

### 2) `app/views/layouts/application.html.erb`（header/main/footer に再構成）

```erb
<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="App">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <% unless Rails.env.test? %>
      <%= stylesheet_link_tag "bootstrap-icons/font/bootstrap-icons", "data-turbo-track": "reload" %>
    <% end %>
    <%= javascript_importmap_tags %>
    <script
      src="https://web-broadcast.live-video.net/1.30.0/amazon-ivs-web-broadcast.js"
      data-turbo-track="reload"
    ></script>
  </head>

  <body class="d-flex flex-column min-vh-100">
    <%# --- Header (common) --- %>
    <header class="border-bottom bg-white">
      <div class="container py-2">
        <div class="d-flex justify-content-between align-items-center gap-3">
          <div class="d-flex align-items-center gap-2 flex-wrap">
            <div class="fw-semibold">
              <%= content_for(:title).presence || "App" %>
            </div>

            <% if user_signed_in? %>
              <span class="text-muted small">
                <%= display_name_or_email(current_user) %>
              </span>
              <%= role_badge(current_user) %>

              <% store = layout_current_store %>
              <span class="text-muted small">
                store:
                <% if store.present? %>
                  <strong><%= store.name.presence || "(no name)" %></strong>
                <% else %>
                  <span class="text-danger">未選択</span>
                <% end %>
              </span>

              <% booth = layout_current_booth %>
              <span class="text-muted small">
                booth:
                <% if booth.present? %>
                  <strong><%= booth.name.presence || "(no name)" %></strong>
                <% else %>
                  <span class="text-danger">未選択</span>
                <% end %>
              </span>

              <span class="text-muted small">
                points: <strong><%= layout_wallet_points %>pt</strong>
              </span>
            <% else %>
              <span class="text-muted small">未ログイン</span>
            <% end %>
          </div>

          <div class="d-flex align-items-center gap-2">
            <% if user_signed_in? %>
              <%= button_to "ログアウト",
                    destroy_user_session_path,
                    method: :delete,
                    class: "btn btn-sm btn-outline-secondary" %>
            <% else %>
              <%= link_to "ログイン", new_user_session_path, class: "btn btn-sm btn-primary" %>
            <% end %>
          </div>
        </div>
      </div>
    </header>

    <%# --- Main --- %>
    <main class="flex-grow-1 pb-5">
      <div class="container mt-3">
        <%# ★常時存在する flash の箱（StoreBanGuard の turbo_stream target） %>
        <div id="flash_inner">
          <% if notice.present? %>
            <div class="alert alert-success" role="alert"><%= notice %></div>
          <% end %>

          <% if alert.present? %>
            <div class="alert alert-danger" role="alert"><%= alert %></div>
          <% end %>
        </div>
      </div>

      <div class="container my-4">
        <%= yield %>
      </div>
    </main>

    <%# --- Footer (common top-level navigation) --- %>
    <footer class="border-top bg-white fixed-bottom">
      <div class="container py-2">
        <div class="d-flex justify-content-between align-items-center">
          <div class="d-flex gap-3">
            <%= link_to "ホーム", root_path, class: "btn btn-sm btn-outline-secondary" %>
            <%= link_to "お気に入り", favorites_booths_path, class: "btn btn-sm btn-outline-secondary" %>
          </div>

          <div>
            <% if user_signed_in? && current_user.at_least?(:cast) %>
              <%= link_to "配信", cast_booths_path, class: "btn btn-sm btn-primary" %>
            <% end %>
          </div>

          <div class="d-flex gap-2">
            <%= button_to "購入(1000pt)",
                  wallet_purchases_path,
                  method: :post,
                  params: { points: 1000 },
                  form: { data: { turbo: false } },
                  class: "btn btn-sm btn-outline-primary" %>

            <%= link_to "ダッシュボード", dashboard_path_for(current_user), class: "btn btn-sm btn-outline-secondary" %>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
```

---

### 3) `app/views/home/show.html.erb`（container二重回避 + 暫定導線撤去）

```erb
<% if user_signed_in? %>
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="h4 mb-0">ブース一覧</h1>
  </div>

  <%# --- Search (minimal / GET) --- %>
  <div class="card mb-3">
    <div class="card-body">
      <%= form_with url: root_path, method: :get, scope: nil do |f| %>
        <div class="row g-2 align-items-center">
          <div class="col-12 col-md-7">
            <%= f.text_field :q,
                  value: @q,
                  class: "form-control",
                  placeholder: "ブース名 / ストア名で検索（部分一致）",
                  autocomplete: "off" %>
          </div>

          <div class="col-12 col-md-auto">
            <div class="form-check">
              <%= f.check_box :online,
                    { class: "form-check-input", checked: @online_only, include_hidden: false },
                    "1" %>
              <%= f.label :online, "配信中のみ", class: "form-check-label" %>
            </div>
          </div>

          <div class="col-12 col-md-auto">
            <%= f.submit "検索", class: "btn btn-primary w-100" %>
          </div>

          <div class="col-12 col-md-auto">
            <%= link_to "クリア", root_path, class: "btn btn-outline-secondary w-100" %>
          </div>
        </div>
      <% end %>

      <% if @q.present? || @online_only %>
        <div class="mt-2 text-muted small">
          <% labels = [] %>
          <% labels << "“#{@q}”" if @q.present? %>
          <% labels << "配信中のみ" if @online_only %>
          <span><%= labels.join(" / ") %> の検索結果</span>
          <span class="ms-2">（<%= @booths.size %> 件）</span>
        </div>
      <% end %>
    </div>
  </div>

  <% if @booths.present? %>
    <div class="row row-cols-1 row-cols-md-3 g-3">
      <% @booths.each do |booth| %>
        <% primary_cast = booth.booth_casts.max_by { |bc| [bc.created_at, bc.id] }&.cast_user %>
        <% favorited = @favorite_booth_ids.include?(booth.id) %>

        <div class="col">
          <div class="card h-100">
            <%= link_to enter_booth_path(booth), class: "text-decoration-none text-reset" do %>
              <% if booth.thumbnail_image.attached? %>
                <%= image_tag booth.thumbnail_image,
                      class: "card-img-top",
                      style: "aspect-ratio: 16 / 9; object-fit: cover;" %>
              <% else %>
                <div class="bg-light d-flex align-items-center justify-content-center"
                     style="aspect-ratio: 16 / 9;">
                  <span class="text-muted small">no thumbnail</span>
                </div>
              <% end %>
            <% end %>

            <div class="card-body">
              <div class="d-flex justify-content-between align-items-start gap-2">
                <div class="fw-semibold">
                  <%= link_to booth.name, enter_booth_path(booth), class: "text-decoration-none text-reset" %>
                </div>
                <div><%= booth_status_badge(booth) %></div>
              </div>

              <div class="mt-2 text-muted small">
                <% if primary_cast.present? %>
                  cast: <%= display_name_or_email(primary_cast) %>
                <% else %>
                  cast: <span class="text-muted">（未設定）</span>
                <% end %>
              </div>

              <div class="text-muted small">
                store: <%= booth.store&.name %>
              </div>
            </div>

            <div class="card-footer bg-white border-0 pt-0">
              <%= render "favorites/booths/button_home", booth: booth, favorited: favorited %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="alert alert-secondary" role="alert">
      表示できるブースがありません。
    </div>
  <% end %>

  <hr class="my-4">

  <h2 class="h5 mb-3">店舗一覧</h2>

  <% if @stores.present? %>
    <div class="list-group mb-4">
      <% @stores.each do |store| %>
        <% favorited = @favorite_store_ids.include?(store.id) %>

        <div class="list-group-item">
          <div class="d-flex justify-content-between align-items-center gap-3">
            <div class="flex-grow-1">
              <%= link_to store.name, store_path(store), class: "text-decoration-none" %>
            </div>

            <%= render "favorites/stores/button_home", store: store, favorited: favorited %>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="alert alert-secondary" role="alert">
      表示できる店舗がありません。
    </div>
  <% end %>
<% else %>
  <div style="max-width: 720px;">
    <h1 class="mb-3">ようこそ</h1>
    <p class="text-muted">視聴するにはログイン、または新規アカウント作成を行ってください。</p>

    <div class="d-flex gap-2 mt-3">
      <%= link_to "ログイン", new_user_session_path, class: "btn btn-primary" %>
      <%= link_to "新規アカウント作成", sign_up_path, class: "btn btn-outline-primary" %>
    </div>
  </div>
<% end %>
```

---

### 4) `app/views/favorites/booths/index.html.erb`（container二重回避）

```erb
<div style="max-width: 960px;">
  <div class="d-flex justify-content-between align-items-start gap-3">
    <div>
      <h1 class="mb-1">お気に入りブース</h1>
      <p class="text-muted mb-0">新しい順</p>
    </div>

    <div class="d-flex gap-2">
      <%= link_to "店舗のお気に入り", favorites_stores_path, class: "btn btn-outline-secondary btn-sm" %>
    </div>
  </div>

  <hr class="my-4">

  <% if @favorite_booths.blank? %>
    <div class="alert alert-secondary" role="alert">
      お気に入りブースはありません。
    </div>
  <% else %>
    <div class="list-group">
      <% @favorite_booths.each do |favorite| %>
        <% booth = favorite.booth %>
        <div class="list-group-item">
          <div class="d-flex justify-content-between align-items-center gap-3">
            <div class="flex-grow-1">
              <div class="fw-semibold">
                <%= link_to booth.name, enter_booth_path(booth), class: "text-decoration-none" %>
              </div>
              <div class="text-muted small">
                store: <%= booth.store&.name %>
              </div>
            </div>

            <div class="text-end">
              <%= link_to "開く", enter_booth_path(booth), class: "btn btn-outline-primary btn-sm" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
```

---

### 5) `app/views/favorites/stores/index.html.erb`（container二重回避）

```erb
<div style="max-width: 960px;">
  <div class="d-flex justify-content-between align-items-start gap-3">
    <div>
      <h1 class="mb-1">お気に入り店舗</h1>
      <p class="text-muted mb-0">新しい順</p>
    </div>

    <div class="d-flex gap-2">
      <%= link_to "ブースのお気に入り", favorites_booths_path, class: "btn btn-outline-secondary btn-sm" %>
    </div>
  </div>

  <hr class="my-4">

  <% if @favorite_stores.blank? %>
    <div class="alert alert-secondary" role="alert">
      お気に入り店舗はありません。
    </div>
  <% else %>
    <div class="list-group">
      <% @favorite_stores.each do |favorite| %>
        <% store = favorite.store %>
        <div class="list-group-item">
          <div class="d-flex justify-content-between align-items-center gap-3">
            <div class="flex-grow-1">
              <div class="fw-semibold">
                <%= link_to store.name, store_path(store), class: "text-decoration-none" %>
              </div>
            </div>

            <div class="text-end">
              <%= link_to "開く", store_path(store), class: "btn btn-outline-primary btn-sm" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
```

---

## 実行コマンド（Windows + Docker compose 前提）

### ルーティングざっと確認

```bash
docker compose exec app bash -lc "bin/rails routes | sed -n '1,200p'"
```

### テスト

```bash
docker compose exec app bash -lc "bin/rails test"
```

---

## Done チェック（#212）

* 全ページでヘッダー/フッターが表示される（最小）
* 配信ボタンは `current_user.at_least?(:cast)` のみ表示
* ダッシュボードリンクが role 別に分岐する
* 既存の `flash_inner` が維持され、StoreBanGuard の turbo_stream が壊れない

---
---

失敗の原因はシンプルで、どちらも **テストが “古い文言（旧レイアウトの表示）” を前提にしている** だけです。

* `CurrentConsistencyAdminTest` は `"現在の店舗:"` を探している
  → 新レイアウトではヘッダーが `store:` 表記になっている（`store: Store 2` になってるのはログからも確認済み）
* `CustomerSignUpTest` は `"ログイン中: email"` を探している
  → それは home 側の暫定表示を削って、ヘッダーに寄せたので消えた（ログにも email はヘッダーに出ている）

**つまり、実装側が間違っているというより、テスト期待値を新UI仕様（#212）に合わせる修正**が必要です。
（#212 の Done は「ヘッダーに user/role/current/wallet を出す」なので、むしろテストもそこを見に行くのが筋）

---

## 修正方針（最小差分）

### A) CurrentConsistencyAdminTest

* `"現在の店舗:"` を探すのをやめて、**ヘッダーの `store:` 表示**を検証する
* 追加で、`booth:` も出ているので（ログ上 `Booth 2`）、テストの意図に沿って**booth優先がUIにも反映**されていることを確認できる

### B) CustomerSignUpTest

* `"ログイン中: #{email}"` をやめて、**ヘッダーに email が出ていること**を検証する

  * 具体的には `assert_includes @response.body, email`
  * もし role も確認したければ `customer` バッジ文字列も追加でチェック可能

---

## 変更コード（貼り付け用）

### 1) `test/integration/current_consistency_admin_test.rb`

該当箇所を以下に差し替え。

```ruby
# admin_root で current_store が解決されるタイミングで、store が booth2.store に補正される
get admin_root_path
assert_response :success

assert_equal store2.id, @request.session[:current_store_id], "booth優先で store_id が補正される"

# レイアウト統一後：ヘッダー表記に合わせて検証
assert_includes response.body, "store:"
assert_includes response.body, store2.name
assert_includes response.body, "booth:"
assert_includes response.body, booth2.name
```

（他の2テストは `store.name` を見ているだけなのでそのままでOK。もし `"現在の店舗:"` が他にもあれば同様に置換）

---

### 2) `test/integration/customer_sign_up_test.rb`

最後の1行を以下に差し替え。

```ruby
assert_includes @response.body, email
```

もし「roleも出ていること」まで見たければ、追加でこれもOKです（ただし UI文字列依存が増えるので好み）：

```ruby
assert_includes @response.body, "customer"
```

---

## 期待される結果

この2点を直せば、ログ上すでにヘッダーに email / store / booth が出ているので、**テストは通るはず**です。

---

必要なら、`assert_includes` をもう少し堅くして「ヘッダー内にあること」を `assert_select` で絞る形にもできますが、まずは上の最小差分が良いと思います。
