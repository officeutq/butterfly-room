了解、GOでいきます。#228 は **最小差分（実質1点修正）**で Done にできます。

## 実装方針（高レベル）

* 店舗選択（`POST /admin/current_store`）は「文脈確定」なので、**選択後は TOP（`root_path`）へ戻す**。
* Current の整合は既存のまま：

  * `session[:current_store_id]` をセット
  * `session[:current_booth_id]` をクリア
* admin ダッシュボードへの直接導線は「残す」：

  * `/admin/stores` の「管理者トップへ」（`admin_root_path`）はそのまま
  * layout フッターの「ダッシュボード」ボタンもそのまま

## タスク順（Done基準に沿って）

1. `Admin::CurrentStoresController#create` の成功時リダイレクトを `root_path` に変更
2. ローカルで動作確認

   * `/admin/stores` → 「この店舗を開く」→ TOPへ戻る
   * ヘッダーの `store:` が選択した店舗名に変わる（admin_stores_path へのリンクも維持）
   * `booth:` は未選択になる（current_booth クリア）
3. テスト（ある場合のみ）でリダイレクト期待値を更新 / 無ければ追加は任意（最小差分なら「既存があれば直す」）

---

## 変更内容（貼り付け用・コード）

**対象ファイル**: `app/controllers/admin/current_stores_controller.rb`

変更点は最後の1行のみです。

```ruby
# frozen_string_literal: true

module Admin
  class CurrentStoresController < Admin::BaseController
    def create
      store_id = params.require(:store_id)

      if current_user.system_admin?
        unless Store.exists?(id: store_id)
          session.delete(:current_store_id)
          redirect_to admin_stores_path, alert: "選択できない店舗です"
          return
        end
      else
        # session 改ざん対策：必ず membership チェック
        unless StoreMembership.exists?(
          user_id: current_user.id,
          store_id: store_id,
          membership_role: StoreMembership.membership_roles[:admin]
        )
          session.delete(:current_store_id)
          redirect_to admin_stores_path, alert: "選択できない店舗です"
          return
        end
      end

      session[:current_store_id] = store_id
      session.delete(:current_booth_id)

      # ★ #228: store選択=文脈確定 → TOP（root）へ統一
      redirect_to root_path, notice: "店舗を切り替えました"
    rescue ActionController::ParameterMissing
      redirect_to admin_stores_path, alert: "店舗を選択してください"
    end
  end
end
```

---

## ブランチ名（おすすめ）

* `improve/228-admin-stores-redirect-root`

## コミットメッセージ案

* `Improve admin current_store redirect to root (#228)`

（日本語寄りなら）

* `admin/stores: 店舗選択後の遷移をTOPへ統一 (#228)`

## PR本文（コピペ用）

**Title**

* `[Improve] admin/stores：店舗選択後の遷移をTOPへ統一（Currentセット→root） #228`

**変更点**

* `/admin/stores` の「この店舗を開く」押下後、`admin_root_path` ではなく `root_path`（TOP）へリダイレクトするよう変更
* `current_store` セットおよび `current_booth` クリアの挙動は従来どおり維持

**仕様**

* store選択＝文脈確定として、モード導線統一（#207）に合わせて TOP に戻す
* adminダッシュボードへの直接導線（「管理者トップへ」リンク、フッターの「ダッシュボード」）は維持

**動作確認**

* store_admin / system_admin の両方で `/admin/stores` から店舗選択 → TOPへ遷移すること
* ヘッダーの `store:` が選択した店舗に更新されること
* `booth:` が未選択にクリアされること
* 不正な店舗選択/未選択時は従来どおり `/admin/stores` に戻りエラー表示されること

**Closes**

* `Closes #228`

---

テストが期待しているリダイレクト先が **`admin_root_path`** のままなので、#228 の仕様変更（`root_path` へ）に合わせて **期待値を更新**すればOKです。失敗内容もその通りで、`/admin` を期待していたのに `/` へリダイレクトされた、になっています。

## 直すべき点（最小）

`test/integration/admin_stores_index_test.rb` の2箇所だけ：

* store_admin の selection flow
* system_admin の selection flow

どちらも

* `assert_redirected_to admin_root_path` → `assert_redirected_to root_path`

に変更します。

## 貼り付け用：修正後のファイル全文

```ruby
# frozen_string_literal: true

require "test_helper"

class AdminStoresIndexTest < ActionDispatch::IntegrationTest
  setup do
    @store1 = Store.create!(name: "store1")
    @store2 = Store.create!(name: "store2")

    @store_admin  = User.create!(email: "admin_idx@example.com", password: "password", role: :store_admin)
    @system_admin = User.create!(email: "sys_idx@example.com", password: "password", role: :system_admin)

    StoreMembership.create!(store: @store1, user: @store_admin, membership_role: :admin)
  end

  test "system_admin sees all stores on /admin/stores" do
    sign_in @system_admin, scope: :user

    get admin_stores_path
    assert_response :success

    assert_select "table tbody tr", count: 2
    assert_select "td", text: @store1.id.to_s
    assert_select "td", text: @store2.id.to_s
  end

  test "store_admin sees only admin membership stores on /admin/stores" do
    sign_in @store_admin, scope: :user

    get admin_stores_path
    assert_response :success

    assert_select "table tbody tr", count: 1
    assert_select "td", text: @store1.id.to_s
    assert_select "td", text: @store2.id.to_s, count: 0
  end

  test "can reach /admin/stores without current_store and selection flow works (store_admin)" do
    sign_in @store_admin, scope: :user

    # current_store 未選択でも /admin/stores に到達できる（選択画面なので）
    get admin_stores_path
    assert_response :success

    # 選択確定（membershipあり）
    post admin_current_store_path, params: { store_id: @store1.id }
    assert_response :redirect
    assert_redirected_to root_path

    follow_redirect!
    assert_response :success
  end

  test "can reach /admin/stores without current_store and selection flow works (system_admin)" do
    sign_in @system_admin, scope: :user

    get admin_stores_path
    assert_response :success

    # 選択確定（system_admin は membership 不要）
    post admin_current_store_path, params: { store_id: @store2.id }
    assert_response :redirect
    assert_redirected_to root_path

    follow_redirect!
    assert_response :success

    # 選択中ハイライト（table-active）が付くことを確認（session が反映されている）
    get admin_stores_path
    assert_response :success
    assert_select "tr.table-active td", text: @store2.id.to_s
  end
end
```

## 実行コマンド（docker compose 経由）

```bash
docker compose exec app bash -lc "bin/rails test test/integration/admin_stores_index_test.rb"
```

---

### コミットメッセージ案（テスト修正）

* `Fix admin store selection redirect expectation in tests (#228)`

### PR本文 追記（動作確認に1行足すなら）

* 「`admin_current_store` 選択後は `root_path` にリダイレクトすること（テストで担保）」

これでテストは通るはずです。
