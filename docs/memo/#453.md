了解です。
#453 は **土台だけ入れて、後続 #454 / #455 / #456 が参照できる状態にする** のが一番きれいです。

ただし 1 点だけ重要です。
Issue 本文どおりに `StreamSessionPolicy#view_token?` を `ViewerPolicy` へ委譲すると、**cast の viewer token 取得可否もこの時点で変わります。**
つまり、**#454 の本体は実質この変更で吸収されます。**

なので実装方針は次の2択ですが、今回は source of truth を優先して進めます。

* **採用方針**: `ViewerPolicy` を追加し、`StreamSessionPolicy#view_token?` を委譲する
* 影響: cast の viewer token 問題はこの時点で解消方向になる
* 結果: #454 は「重複確認」か「不要化」になる可能性あり

## 実装方針

1. `Authorization::ViewerPolicy` を新設
2. `viewer?` を単一の基準にする
3. `view_token? / ping_presence? / create_comment? / create_drink_order?` を ViewerPolicy に集約
4. `StreamSessionPolicy#view_token?` は ViewerPolicy に委譲
5. publisher 側は一切触らない

---

## 追加ファイル

### `app/services/authorization/viewer_policy.rb`

```ruby
# frozen_string_literal: true

module Authorization
  class ViewerPolicy < ApplicationPolicy
    def viewer?
      user.present?
    end

    def view_token?
      return false unless viewer?

      # BAN は customer のみ対象（現行仕様維持）
      checker = StoreBanChecker.new(store: record.store, user: user)
      !checker.banned?
    end

    def ping_presence?
      viewer?
    end

    def create_comment?
      viewer?
    end

    def create_drink_order?
      viewer?
    end
  end
end
```

---

## 修正ファイル

### `app/services/authorization/stream_session_policy.rb`

```ruby
# frozen_string_literal: true

module Authorization
  class StreamSessionPolicy < ApplicationPolicy
    def publish_token?
      return false unless user.present?

      booth = record.booth

      # 上位（store_admin/system_admin）は、所属 store の booth なら publisher token を許可（cast兼務）
      if user.at_least?(:store_admin)
        return true if user.system_admin?
        return user.admin_of_store?(booth.store_id)
      end

      # cast の基本条件
      return false unless BoothPolicy.new(user, booth).cast_operate?

      # フェーズ1: 担当cast（primary）が設定されている場合は、その1人だけ許可
      primary_cast_user_id = booth.primary_cast_user_id
      if primary_cast_user_id.present?
        return primary_cast_user_id == user.id
      end

      # 担当未設定の暫定措置：
      booth.cast_users.exists?(id: user.id) ||
        record.started_by_cast_user_id == user.id
    end

    def view_token?
      ViewerPolicy.new(user, record).view_token?
    end
  end
end
```

---

## この実装で満たせること

* `ViewerPolicy` が導入される
* viewer 判定の基準が `ViewerPolicy#viewer?` に置かれる
* `view_token?` の判定基準が authorization 層の単一点へ集約される
* 既存の publisher 判定は壊さない

## この Issue ではまだ触らないこと

* `PresencesController` で `ping_presence?` を使う実適用
* `CommentsController` で `create_comment?` を使う実適用
* `DrinkOrdersController` で `create_drink_order?` を使う実適用
* customer 専用データ構造（wallet / presence / drink_order）の見直し

これは後続の #455 / #456 でやる切り分けで大丈夫です。

必要なら次に、**#453 用のコミットメッセージと PR 文** もそのまま書きます。
