了解、#138 を **#47（キャスト招待）と完全同型**で実装します。
ただしご指定どおり、**承認後の遷移先は暫定で `dashboard_path`（`/dashboard`）**にします（`/admin` は触りません）。

以下、(1) 実装計画 → (2) 追加/修正ファイルのコピペ用コード（そのまま置き換え/新規作成）→ (3) 実行コマンド、の順です。

---

## 1) 実装計画（高レベル）

### 1. DB

* `store_admin_invitations` テーブル追加（#47と同等のindex構成）

  * `store_id, invited_by_user_id, token_digest, expires_at, used_at, accepted_by_user_id, timestamps`
  * `token_digest` は unique
  * `expires_at, used_at, store_id, store_id+created_at` に index

### 2. 招待モデル（public token）

* `StoreAdminInvitation`

  * token生成・digest化・find_by_token
  * `usable? / expired? / used? / status_label`

### 3. Service

* `StoreAdminInvitations::IssueInvitation`（発行・24h）
* `StoreAdminInvitations::AcceptInvitation`

  * **store_admin ロールのみ許可**
  * transaction + `invitation.lock!`
  * `StoreMembership.create!(membership_role: :admin)`（`RecordNotUnique` は成功扱い）
  * `used_at / accepted_by_user` 更新

### 4. Public controller（招待URLアクセス）

* `StoreAdminInvitationsController#show/#accept`

  * 未ログイン：`store_location_for` してログインへ（`invite_token` + `invite_kind=store_admin`）
  * store_admin以外：案内のみ
  * store_adminで既にadmin membershipあり：usableなら used_at を閉じる（#47同様）
  * accept成功時：

    * **`session[:current_store_id] = 招待store`**
    * **`session.delete(:current_booth_id)`**
    * `dashboard_path` へリダイレクト

### 5. Admin側（発行＋一覧）: 既存 `admin/casts#index` にセクション追加

* 既存の cast招待と同様に「noticeにURL表示」
* Phase1：無効化ボタン無し

### 6. store_admin 招待専用 新規作成（cast招待の同型）

* `StoreAdmins::RegistrationsController` + `StoreAdmins::RegistrationForm` + `StoreAdmins::RegisterStoreAdmin`

  * token必須
  * 作成後 `sign_in` → 招待ページへ戻す

### 7. Deviseログイン画面の案内出し分け

* 既存は cast招待専用文言なので、

  * `invite_kind=store_admin` のときは store_admin向けの案内＆「招待経由で store_admin 新規作成」リンクを出す

### 8. テスト

* `test/integration/store_admin_invitation_flow_test.rb`

  * guest → login redirect（`invite_kind=store_admin` 付き）→ 招待経由store_admin新規作成 → 招待ページに戻る
  * store_adminが承認（1回のみ）/ 2回目不可
  * 非store_adminは承認不可
  * 期限切れは承認不可
  * 承認後 session の `current_store_id` が招待storeになっていることを確認

---

## 2) ブランチ名候補

* `feature/138-store-admin-invitations`
* `feature/138-store-admin-invite-24h-onetime`
* `feature/138-admin-invite-store-admin`
* `improve/138-store-admin-invite-flow`
* `feature/138-store-admin-invite-dashboard-redirect`

---

## 3) コピペ用コード（新規作成/置き換え）

> **注意**: ここからはコードです（GO後なのでOK）。

---

### 3.1 migration（新規）

まず空マイグレーションを作成（あなたの流儀）：

```bash
docker compose run --rm app bin/rails g migration CreateStoreAdminInvitations
```

生成されたファイルを、以下で **全文置き換え**してください（ファイル名の timestamp は環境に合わせてください）:

`db/migrate/XXXXXXXXXXXXXX_create_store_admin_invitations.rb`

```ruby
# frozen_string_literal: true

class CreateStoreAdminInvitations < ActiveRecord::Migration[8.0]
  def change
    create_table :store_admin_invitations do |t|
      t.references :store, null: false, foreign_key: true
      t.references :invited_by_user, null: false, foreign_key: { to_table: :users }
      t.references :accepted_by_user, foreign_key: { to_table: :users }

      t.string :token_digest, null: false
      t.datetime :expires_at, null: false
      t.datetime :used_at

      t.timestamps
    end

    add_index :store_admin_invitations, :token_digest, unique: true
    add_index :store_admin_invitations, :expires_at
    add_index :store_admin_invitations, :used_at
    add_index :store_admin_invitations, %i[store_id created_at]
  end
end
```

---

### 3.2 Model（新規）

`app/models/store_admin_invitation.rb`

```ruby
# frozen_string_literal: true

require "digest"
require "securerandom"

class StoreAdminInvitation < ApplicationRecord
  belongs_to :store
  belongs_to :invited_by_user, class_name: "User"
  belongs_to :accepted_by_user, class_name: "User", optional: true

  validates :token_digest, presence: true, uniqueness: true
  validates :expires_at, presence: true

  scope :recent_first, -> { order(created_at: :desc, id: :desc) }

  def expired?
    expires_at < Time.current
  end

  def used?
    used_at.present?
  end

  def usable?
    !used? && !expired?
  end

  def status_label
    return "使用済み" if used?
    return "期限切れ" if expired?
    "有効"
  end

  # --- token digest helpers ---

  def self.generate_token
    SecureRandom.urlsafe_base64(32)
  end

  def self.digest_for(token)
    secret = Rails.application.secret_key_base
    Digest::SHA256.hexdigest("#{secret}--#{token}")
  end

  def self.find_by_token(token)
    find_by(token_digest: digest_for(token))
  end
end
```

---

### 3.3 Services（新規）

`app/services/store_admin_invitations/issue_invitation.rb`

```ruby
# frozen_string_literal: true

module StoreAdminInvitations
  class IssueInvitation
    Result = Struct.new(:invitation, :token, keyword_init: true)

    def self.call!(store:, invited_by_user:)
      new(store:, invited_by_user:).call!
    end

    def initialize(store:, invited_by_user:)
      @store = store
      @invited_by_user = invited_by_user
    end

    def call!
      token = StoreAdminInvitation.generate_token
      digest = StoreAdminInvitation.digest_for(token)

      invitation = nil

      ActiveRecord::Base.transaction do
        invitation = StoreAdminInvitation.create!(
          store: @store,
          invited_by_user: @invited_by_user,
          token_digest: digest,
          expires_at: 24.hours.from_now
        )
      end

      Result.new(invitation: invitation, token: token)
    end
  end
end
```

`app/services/store_admin_invitations/accept_invitation.rb`

```ruby
# frozen_string_literal: true

module StoreAdminInvitations
  class AcceptInvitation
    Result = Struct.new(:invitation, keyword_init: true)

    class NotUsable < StandardError; end
    class NotAuthorized < StandardError; end

    def self.call!(invitation:, actor:)
      new(invitation:, actor:).call!
    end

    def initialize(invitation:, actor:)
      @invitation = invitation
      @actor = actor
    end

    def call!
      raise NotAuthorized, "store_admin でログインしてください" unless @actor&.store_admin?

      ActiveRecord::Base.transaction do
        @invitation.lock!

        raise NotUsable, "この招待は使用できません（期限切れ/使用済み）" unless @invitation.usable?

        begin
          StoreMembership.create!(
            store: @invitation.store,
            user: @actor,
            membership_role: :admin
          )
        rescue ActiveRecord::RecordNotUnique
          # すでに所属済みでも、承認済みとして扱う（安全側・冪等）
        end

        @invitation.update!(
          used_at: Time.current,
          accepted_by_user: @actor
        )
      end

      Result.new(invitation: @invitation)
    end
  end
end
```

---

### 3.4 Public controller（新規）

`app/controllers/store_admin_invitations_controller.rb`

```ruby
# frozen_string_literal: true

class StoreAdminInvitationsController < ApplicationController
  before_action :set_invitation_by_token

  def show
    unless user_signed_in?
      store_location_for(:user, request.fullpath)
      redirect_to new_user_session_path(invite_token: params[:token], invite_kind: "store_admin"),
                  alert: "招待の承認には store_admin でログインしてください"
      return
    end

    # store_admin 以外は案内だけ表示（承認不可）
    unless current_user.store_admin?
      render :show, status: :ok
      return
    end

    # すでに admin membership を持つかチェック
    @already_member = StoreMembership.exists?(
      store_id: @invitation.store_id,
      user_id: current_user.id,
      membership_role: :admin
    )

    # 在籍済み かつ 招待がまだ usable なら closed 扱いにする
    if @already_member && @invitation.usable?
      ActiveRecord::Base.transaction do
        @invitation.lock!

        if @invitation.usable?
          @invitation.update!(
            used_at: Time.current,
            accepted_by_user: current_user
          )
        end
      end
    end

    render :show, status: :ok
  end

  def accept
    authenticate_user!

    unless current_user.store_admin?
      redirect_to store_admin_invitation_path(params[:token]), alert: "store_admin でログインして承認してください"
      return
    end

    StoreAdminInvitations::AcceptInvitation.call!(invitation: @invitation, actor: current_user)

    # ★current_store を招待対象に保証（重要）
    session[:current_store_id] = @invitation.store_id
    session.delete(:current_booth_id)

    redirect_to dashboard_path, notice: "store_admin 招待を承認しました（#{@invitation.store.name}）"
  rescue StoreAdminInvitations::AcceptInvitation::NotUsable => e
    redirect_to store_admin_invitation_path(params[:token]), alert: e.message
  rescue StoreAdminInvitations::AcceptInvitation::NotAuthorized => e
    redirect_to store_admin_invitation_path(params[:token]), alert: e.message
  end

  private

  def set_invitation_by_token
    @invitation = StoreAdminInvitation.find_by_token(params[:token].to_s)
    head :not_found if @invitation.blank?
  end
end
```

---

### 3.5 Public view（新規）

`app/views/store_admin_invitations/show.html.erb`

```erb
<div style="max-width: 720px;">
  <h1 class="h4 mb-3">store_admin 招待</h1>

  <div class="card mb-3">
    <div class="card-body">
      <div class="mb-2">
        <span class="text-muted">店舗：</span>
        <strong><%= @invitation.store.name %></strong>
      </div>

      <div class="mb-2">
        <span class="text-muted">期限：</span>
        <span><%= @invitation.expires_at.strftime("%Y-%m-%d %H:%M") %></span>
      </div>

      <div class="mb-0">
        <span class="text-muted">状態：</span>
        <span><%= @invitation.status_label %></span>
      </div>
    </div>
  </div>

  <% if !user_signed_in? %>
    <%# show では未ログイン時はログインへリダイレクト（基本到達しない想定） %>
    <div class="alert alert-secondary" role="alert">
      招待の承認には store_admin でログインしてください。
    </div>

  <% elsif !current_user.store_admin? %>
    <div class="alert alert-warning" role="alert">
      この招待は <strong>store_admin アカウント</strong>でのみ承認できます。<br>
      現在は store_admin 以外でログインしています。ログアウトして store_admin でログインし直してください。
    </div>

    <div class="d-flex gap-2 flex-wrap">
      <%= button_to "ログアウト",
            destroy_user_session_path,
            method: :delete,
            class: "btn btn-outline-secondary" %>

      <%= link_to "ログイン（store_admin）",
            new_user_session_path(invite_token: params[:token], invite_kind: "store_admin"),
            class: "btn btn-primary" %>

      <%= link_to "招待経由で store_admin 新規作成",
            store_admin_sign_up_path(token: params[:token]),
            class: "btn btn-outline-primary" %>
    </div>

  <% else %>
    <% if @already_member %>
      <div class="alert alert-success" role="alert">
        この店舗にはすでに store_admin として登録されています。
      </div>

      <div class="d-flex gap-2">
        <%= link_to "ダッシュボードへ", dashboard_path, class: "btn btn-outline-secondary" %>
      </div>

    <% elsif !@invitation.usable? %>
      <div class="alert alert-danger" role="alert">
        この招待は使用できません（期限切れ / 使用済み）。
      </div>

    <% else %>
      <div class="alert alert-info" role="alert">
        上記の店舗を管理する store_admin として参加します。よろしければ承認してください。
      </div>

      <div class="d-flex gap-2">
        <%= button_to "承認する",
              accept_store_admin_invitation_path(params[:token]),
              method: :post,
              class: "btn btn-primary",
              data: { turbo_confirm: "この店舗（#{@invitation.store.name}）の store_admin 招待を承認しますか？" } %>

        <%= link_to "キャンセル", dashboard_path, class: "btn btn-outline-secondary" %>
      </div>
    <% end %>
  <% end %>
</div>
```

---

### 3.6 store_admin 招待専用 登録（新規）

#### routes 用の controller

`app/controllers/store_admins/registrations_controller.rb`

```ruby
# frozen_string_literal: true

module StoreAdmins
  class RegistrationsController < ApplicationController
    skip_before_action :authenticate_user!, raise: false

    before_action :require_token!

    def new
      @form = StoreAdmins::RegistrationForm.new(email: params[:email].to_s)
    end

    def create
      @form = StoreAdmins::RegistrationForm.new(registration_params)

      if @form.save
        sign_in(@form.user) # Devise
        redirect_to store_admin_invitation_path(@token),
                    notice: "store_admin アカウントを作成しました。招待を承認してください。"
      else
        render :new, status: :unprocessable_entity
      end
    end

    private

    def require_token!
      @token = params[:token].to_s
      invitation = StoreAdminInvitation.find_by_token(@token)
      head :not_found if invitation.blank?
    end

    def registration_params
      params.require(:store_admin_registration).permit(
        :email,
        :password,
        :password_confirmation
      )
    end
  end
end
```

#### Form（castと同型）

`app/forms/store_admins/registration_form.rb`

```ruby
# frozen_string_literal: true

module StoreAdmins
  class RegistrationForm
    include ActiveModel::Model
    include ActiveModel::Attributes

    attribute :email, :string
    attribute :password, :string
    attribute :password_confirmation, :string

    attr_reader :user

    validates :email, presence: true
    validates :password, presence: true, confirmation: true
    validates :password_confirmation, presence: true
    validate  :email_must_be_unique

    def save
      return false unless valid?

      result = StoreAdmins::RegisterStoreAdmin.call!(
        email: email,
        password: password
      )

      @user = result.user
      true
    rescue ActiveRecord::RecordInvalid => e
      errors.add(:base, e.record.errors.full_messages.join(", "))
      false
    end

    private

    def email_must_be_unique
      return if email.blank?

      if User.exists?(email: email)
        errors.add(:email, "このemailアドレスはすでに使われています")
      end
    end
  end
end
```

#### Service（castの RegisterCast と同型）

`app/services/store_admins/register_store_admin.rb`

```ruby
# frozen_string_literal: true

module StoreAdmins
  class RegisterStoreAdmin
    Result = Struct.new(:user, keyword_init: true)

    def self.call!(email:, password:)
      new(email:, password:).call!
    end

    def initialize(email:, password:)
      @email = email
      @password = password
    end

    def call!
      user = nil

      ActiveRecord::Base.transaction do
        user = User.create!(
          email: @email,
          password: @password,
          password_confirmation: @password,
          role: :store_admin
        )
      end

      Result.new(user: user)
    end
  end
end
```

#### view（cast招待登録と同型）

`app/views/store_admins/registrations/new.html.erb`

```erb
<div style="max-width: 720px;">
  <h1 class="h4 mb-3">store_admin 新規登録（招待専用）</h1>

  <div class="alert alert-secondary" role="alert">
    招待の承認には store_admin アカウントが必要です。アカウントを作成してください。
  </div>

  <%= form_with model: @form,
        url: store_admin_sign_up_path(token: params[:token]),
        scope: :store_admin_registration do |f| %>

    <% if @form.errors.any? %>
      <div class="alert alert-danger" role="alert">
        <ul class="mb-0">
          <% @form.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="mb-3">
      <%= f.label :email, class: "form-label" %>
      <%= f.email_field :email, class: "form-control", autocomplete: "email" %>
    </div>

    <div class="mb-3">
      <%= f.label :password, class: "form-label" %>
      <%= f.password_field :password, class: "form-control", autocomplete: "new-password" %>
    </div>

    <div class="mb-3">
      <%= f.label :password_confirmation, class: "form-label" %>
      <%= f.password_field :password_confirmation, class: "form-control", autocomplete: "new-password" %>
    </div>

    <div class="d-flex gap-2">
      <%= f.submit "作成して続行", class: "btn btn-primary" %>
      <%= link_to "戻る（招待ページ）",
            store_admin_invitation_path(params[:token]),
            class: "btn btn-outline-secondary" %>
    </div>
  <% end %>
</div>
```

---

### 3.7 routes.rb（追記）

`config/routes.rb` の該当箇所に **追記**してください（位置は「Public (cast invitation)」の近くが分かりやすいです）。

```ruby
  # --- Public (store_admin invitation) ---
  get  "/store_admin_invitations/:token", to: "store_admin_invitations#show",   as: :store_admin_invitation
  post "/store_admin_invitations/:token/accept", to: "store_admin_invitations#accept", as: :accept_store_admin_invitation

  # --- StoreAdmin registration (invite only) ---
  get  "/store_admin/sign_up", to: "store_admins/registrations#new",    as: :store_admin_sign_up
  post "/store_admin/sign_up", to: "store_admins/registrations#create"
```

さらに `namespace :admin do ... resources :casts ...` に **collection action**を追加します（既存の `post :invite` の隣に足すのが自然）：

```ruby
    resources :casts, only: %i[index destroy] do
      collection do
        post :invite
        post :invite_store_admin
      end
    end
```

---

### 3.8 Admin::CastsController（追記）

`app/controllers/admin/casts_controller.rb` を以下に更新（全文置き換えが安全です）

```ruby
# frozen_string_literal: true

module Admin
  class CastsController < Admin::BaseController
    before_action :require_current_store!

    def index
      @cast_memberships =
        StoreMembership
          .includes(:user)
          .where(store_id: current_store.id, membership_role: :cast)
          .order(:id)

      @store_cast_invitations =
        StoreCastInvitation
          .includes(:invited_by_user, :accepted_by_user)
          .where(store_id: current_store.id)
          .recent_first

      @new_invitation = StoreCastInvitation.new

      @store_admin_invitations =
        StoreAdminInvitation
          .includes(:invited_by_user, :accepted_by_user)
          .where(store_id: current_store.id)
          .recent_first
    end

    # キャスト招待発行（note 任意）
    def invite
      note = params.require(:store_cast_invitation).permit(:note)[:note]

      result =
        StoreCastInvitations::IssueInvitation.call!(
          store: current_store,
          invited_by_user: current_user,
          note: note
        )

      token = result.token
      url = cast_invitation_url(token)

      redirect_to admin_casts_path,
                  notice: "招待を発行しました: #{url}"
    rescue ActionController::ParameterMissing
      redirect_to admin_casts_path, alert: "パラメータが不正です"
    rescue ActiveRecord::RecordInvalid => e
      redirect_to admin_casts_path, alert: e.record.errors.full_messages.join(", ")
    end

    # store_admin 招待発行（note なし）
    def invite_store_admin
      result =
        StoreAdminInvitations::IssueInvitation.call!(
          store: current_store,
          invited_by_user: current_user
        )

      token = result.token
      url = store_admin_invitation_url(token)

      redirect_to admin_casts_path,
                  notice: "招待を発行しました: #{url}"
    rescue ActiveRecord::RecordInvalid => e
      redirect_to admin_casts_path, alert: e.record.errors.full_messages.join(", ")
    end

    def destroy
      membership =
        StoreMembership
          .where(store_id: current_store.id, membership_role: :cast)
          .find(params[:id])

      membership.destroy!

      redirect_to admin_casts_path, notice: "キャスト登録を解除しました"
    rescue ActiveRecord::RecordNotFound
      head :not_found
    end
  end
end
```

---

### 3.9 admin/casts/index.html.erb（追記）

既存ファイルの末尾（キャスト招待一覧の下）に **store_admin招待セクション**を追加してください。

`app/views/admin/casts/index.html.erb` の最後に以下を追記：

```erb
<hr class="my-4">

<h2 class="h5 mt-4">store_admin 招待（ワンタイム / 24時間）</h2>

<div class="card mb-3">
  <div class="card-body">
    <p class="text-muted mb-3">
      招待URLを発行し、store_admin ロールのユーザーが承認した場合のみ、
      店舗管理者所属（store_memberships: admin）が作成されます（ロール付け替えは行いません）。
    </p>

    <%= button_to "招待URLを発行",
          invite_store_admin_admin_casts_path,
          method: :post,
          class: "btn btn-primary" %>

    <div class="mt-3 text-muted small">
      ※発行後のURLは「お知らせ（notice）」にも表示されます。必要に応じてコピペしてください。
    </div>
  </div>
</div>

<h3 class="h6 mt-4">招待一覧</h3>

<table class="table table-sm">
  <thead>
    <tr>
      <th>状態</th>
      <th>期限</th>
      <th>発行者</th>
      <th>承認者</th>
      <th>URL</th>
    </tr>
  </thead>

  <tbody>
    <% @store_admin_invitations.each do |inv| %>
      <tr>
        <td><%= inv.status_label %></td>
        <td><%= inv.expires_at.strftime("%Y-%m-%d %H:%M") %></td>
        <td><%= display_name_or_email(inv.invited_by_user) %></td>
        <td>
          <% if inv.accepted_by_user.present? %>
            <%= display_name_or_email(inv.accepted_by_user) %>
          <% else %>
            -
          <% end %>
        </td>
        <td class="text-nowrap">
          <%# token は保持していないので、ここでは直接URLを再生成できない（仕様通り：生tokenは発行時のみ） %>
          <span class="text-muted small">（発行時に控えてください）</span>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
```

---

### 3.10 devise ログイン画面（出し分け対応）

`app/views/devise/sessions/new.html.erb` を以下のように更新してください（既存の `invite_token` ブロックを置き換え）

```erb
<div style="max-width: 720px;">
  <h1 class="h4 mb-3">ログイン</h1>

  <% if params[:invite_token].present? %>
    <% invite_kind = params[:invite_kind].to_s.presence || "cast" %>

    <% if invite_kind == "store_admin" %>
      <div class="alert alert-info" role="alert">
        store_admin 招待を承認するには store_admin でログインしてください。<br>
        store_admin アカウントが無い場合は、招待経由で新規作成できます。
      </div>

      <div class="mb-3">
        <%= link_to "招待経由で store_admin 新規作成",
              store_admin_sign_up_path(token: params[:invite_token]),
              class: "btn btn-outline-primary" %>
      </div>
    <% else %>
      <div class="alert alert-info" role="alert">
        キャスト招待を承認するには cast でログインしてください。<br>
        cast アカウントが無い場合は、招待経由で新規作成できます。
      </div>

      <div class="mb-3">
        <%= link_to "招待経由で cast 新規作成",
              cast_sign_up_path(token: params[:invite_token]),
              class: "btn btn-outline-primary" %>
      </div>
    <% end %>
  <% end %>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="mb-3">
      <%= f.label :email, class: "form-label" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
    </div>

    <div class="mb-3">
      <%= f.label :password, class: "form-label" %>
      <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="form-check mb-3">
        <%= f.check_box :remember_me, class: "form-check-input" %>
        <%= f.label :remember_me, class: "form-check-label" %>
      </div>
    <% end %>

    <div class="d-flex gap-2">
      <%= f.submit "ログイン", class: "btn btn-primary" %>
      <%= link_to "ホーム", root_path, class: "btn btn-outline-secondary" %>
    </div>
  <% end %>
</div>
```

---

### 3.11 テスト（新規）

`test/integration/store_admin_invitation_flow_test.rb`

```ruby
# frozen_string_literal: true

require "test_helper"

class StoreAdminInvitationFlowTest < ActionDispatch::IntegrationTest
  test "guest visiting store_admin invitation is redirected to login and can sign up store_admin via invitation" do
    store = Store.create!(name: "Invite Store")
    inviter = User.create!(email: "inviter_admin@example.com", password: "password", role: :store_admin)
    StoreMembership.create!(store: store, user: inviter, membership_role: :admin)

    result = StoreAdminInvitations::IssueInvitation.call!(store: store, invited_by_user: inviter)
    token = result.token

    get store_admin_invitation_path(token)
    assert_response :redirect
    assert_match "/users/sign_in", response.location
    assert_match "invite_token=#{token}", response.location
    assert_match "invite_kind=store_admin", response.location

    # 招待経由 store_admin 新規登録へ
    get store_admin_sign_up_path(token: token)
    assert_response :ok

    post store_admin_sign_up_path(token: token), params: {
      store_admin_registration: {
        email: "new_store_admin@example.com",
        password: "password",
        password_confirmation: "password"
      }
    }
    assert_response :redirect
    follow_redirect!
    assert_response :ok
    assert_match "store_admin 招待", response.body
  end

  test "store_admin can accept invitation once and membership is created; second accept fails; current_store is set" do
    store = Store.create!(name: "Invite Store")
    inviter = User.create!(email: "inviter_admin2@example.com", password: "password", role: :store_admin)
    StoreMembership.create!(store: store, user: inviter, membership_role: :admin)

    store_admin_user = User.create!(email: "store_admin@example.com", password: "password", role: :store_admin)

    result = StoreAdminInvitations::IssueInvitation.call!(store: store, invited_by_user: inviter)
    token = result.token

    sign_in store_admin_user, scope: :user

    post accept_store_admin_invitation_path(token)
    assert_response :redirect
    follow_redirect!
    assert_response :ok

    assert StoreMembership.exists?(store: store, user: store_admin_user, membership_role: :admin)

    invitation = StoreAdminInvitation.find_by_token(token)
    assert invitation.used?
    assert_equal store_admin_user.id, invitation.accepted_by_user_id

    # current_store が招待対象にセットされている（重要）
    assert_equal store.id, @request.session[:current_store_id].to_i

    # 2回目は不可
    post accept_store_admin_invitation_path(token)
    assert_response :redirect
    follow_redirect!
    assert_response :ok
  end

  test "non-store_admin logged in cannot accept store_admin invitation" do
    store = Store.create!(name: "Invite Store")
    inviter = User.create!(email: "inviter_admin3@example.com", password: "password", role: :store_admin)
    StoreMembership.create!(store: store, user: inviter, membership_role: :admin)

    customer = User.create!(email: "customer_for_admin_invite@example.com", password: "password", role: :customer)

    result = StoreAdminInvitations::IssueInvitation.call!(store: store, invited_by_user: inviter)
    token = result.token

    sign_in customer, scope: :user

    get store_admin_invitation_path(token)
    assert_response :ok

    post accept_store_admin_invitation_path(token)
    assert_response :redirect
    follow_redirect!
    assert_response :ok

    assert_not StoreMembership.exists?(store: store, user: customer, membership_role: :admin)
  end

  test "expired store_admin invitation cannot be accepted" do
    store = Store.create!(name: "Invite Store")
    inviter = User.create!(email: "inviter_admin4@example.com", password: "password", role: :store_admin)
    StoreMembership.create!(store: store, user: inviter, membership_role: :admin)

    store_admin_user = User.create!(email: "store_admin2@example.com", password: "password", role: :store_admin)

    result = StoreAdminInvitations::IssueInvitation.call!(store: store, invited_by_user: inviter)
    token = result.token

    invitation = StoreAdminInvitation.find_by_token(token)
    invitation.update!(expires_at: 1.minute.ago)

    sign_in store_admin_user, scope: :user

    post accept_store_admin_invitation_path(token)
    assert_response :redirect
    follow_redirect!
    assert_response :ok

    assert_not StoreMembership.exists?(store: store, user: store_admin_user, membership_role: :admin)
  end
end
```

---

## 4) 実行コマンド（docker compose 前提）

### migrate

```bash
docker compose run --rm app bin/rails db:migrate
```

### テスト（まず招待フローだけ）

```bash
docker compose run --rm app bin/rails test test/integration/store_admin_invitation_flow_test.rb
```

### 全体

```bash
docker compose run --rm app bin/rails test
```

---

## 5) Doneチェック（Issue#138のDoneに対応）

* [x] store_admin が招待発行できる（admin/casts から発行）
* [x] 招待一覧（期限・使用済み）表示
* [x] 24時間失効
* [x] ワンタイム（used_at）
* [x] store_admin ロールのみ承認可能（service + controller）
* [x] StoreMembership(admin) 作成（RecordNotUniqueは成功扱い）
* [x] **承認後 current_store を招待対象にセット**し、暫定で `/dashboard` へ遷移
* [x] ロール付け替えなし（user.role は触らない）

---

必要なら、この実装に合わせた **コミットメッセージ案 / PR本文（変更点・仕様・動作確認）**もすぐ出せます。
