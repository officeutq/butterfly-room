了解。**チェックリスト違反（replaceで箱を消す）を踏まえて、#145 を改めて “正しい形” で GO**します。
今回は **「show側の箱 id は絶対に消さない」= `update` を使う** を強制します。

---

# 変更の要点（今回のGO版）

1. **`broadcast_stream_state` は `broadcast_update_to` にする**（これが本丸）

   * `#stream_state` の箱は show に固定され、**二度と消えない**
2. ドリンクメニューを `booths/_stream_state` に移し、**状態依存UIを集約**
3. StoreBanGuard の turbo_stream は **常時存在する `#flash_inner`** に向ける
4. pending_drink_orders の id 二重を解消（show側に箱を置かない）

---

# ✅ コピペ用コード（修正版）

## 1) `app/views/layouts/application.html.erb`（flashの箱を追加）

```erb
<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="App">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <% unless Rails.env.test? %>
      <%= stylesheet_link_tag "bootstrap-icons/font/bootstrap-icons", "data-turbo-track": "reload" %>
    <% end %>
    <%= javascript_importmap_tags %>
    <script
      src="https://web-broadcast.live-video.net/1.30.0/amazon-ivs-web-broadcast.js"
      data-turbo-track="reload"
    ></script>
  </head>

  <body>
    <%# ★常時存在する flash の箱（StoreBanGuard の turbo_stream target） %>
    <div class="container mt-3">
      <div id="flash_inner">
        <% if notice.present? %>
          <div class="alert alert-success" role="alert"><%= notice %></div>
        <% end %>

        <% if alert.present? %>
          <div class="alert alert-danger" role="alert"><%= alert %></div>
        <% end %>
      </div>
    </div>

    <%= yield %>
  </body>
</html>
```

---

## 2) `app/controllers/concerns/store_ban_guard.rb`（targetを flash_inner に固定）

```ruby
# frozen_string_literal: true

module StoreBanGuard
  extend ActiveSupport::Concern

  private

  def reject_banned_customer!(store:)
    return if store.blank?
    return unless user_signed_in?
    return unless current_user.customer?

    if Authorization::StoreBanChecker.new(store: store, user: current_user).banned?
      render_banned!
    end
  end

  def render_banned!
    respond_to do |format|
      format.json do
        render json: { error: "banned" }, status: :forbidden
      end

      format.turbo_stream do
        stream = turbo_stream.append(
          "flash_inner",
          <<~HTML
            <div class="alert alert-danger" role="alert">
              この店舗では操作できません（BANされています）。トップへ移動します。
            </div>
            <script>
              window.location.href = #{root_path.to_json};
            </script>
          HTML
        )

        render turbo_stream: stream, status: :ok
      end

      format.html do
        redirect_to root_path, status: :see_other
      end

      format.any { head :forbidden }
    end
  end
end
```

---

## 3) `app/views/booths/show.html.erb`（状態依存UIを stream_state に集約）

```erb
<% content_for :head do %>
  <meta name="turbo-cache-control" content="no-cache">
<% end %>

<h1>Public Booth#show (stub)</h1>

<p>booth_id: <%= @booth.id %></p>

<%# ★booth単位で常時購読（開始/終了/状態変化を自動反映） %>
<%= turbo_stream_from [@booth, :stream_state] %>

<%# ★箱（wrapper）は show 側に固定。idを絶対に消さない %>
<div id="stream_state">
  <%= render "booths/stream_state",
        booth: @booth,
        stream_session: @stream_session,
        comments: @comments,
        drink_items: @drink_items %>
</div>

<hr>

<h2>所持ポイント</h2>

<% if user_signed_in? %>
  <p>残高: <strong><%= @wallet&.available_points || 0 %>pt</strong></p>

  <div class="d-flex gap-2 mb-3">
    <%= button_to "1000pt 購入",
          wallet_purchases_path,
          method: :post,
          params: { points: 1000, booth_id: @booth.id },
          form: { data: { turbo: false } },
          class: "btn btn-sm btn-outline-primary" %>

    <%= button_to "3000pt 購入",
          wallet_purchases_path,
          method: :post,
          params: { points: 3000, booth_id: @booth.id },
          form: { data: { turbo: false } },
          class: "btn btn-sm btn-outline-primary" %>
  </div>
<% else %>
  <p class="text-muted">ログインすると購入できます</p>
<% end %>

<hr>

<div class="d-flex justify-content mb-3">
  <%= link_to "退室", root_path, class: "btn btn-outline-secondary" %>
</div>
```

---

## 4) `app/views/booths/_stream_state.html.erb`（ドリンクメニューをここへ）

```erb
<%# locals: booth:, stream_session:, comments:, drink_items: %>

<% live_like = booth.live? || booth.away? %>

<% if stream_session.present? && live_like %>
  <div
    data-controller="presence-poll"
    data-presence-poll-ping-url-value="<%= ping_stream_session_presence_path(stream_session) %>"
    data-presence-poll-summary-url-value="<%= presence_summary_stream_session_path(stream_session) %>"
    data-presence-poll-interval-ms-value="20000"
  >
    viewers: <span data-presence-poll-target="count">-</span>
  </div>

  <hr>

  <h2>ドリンクメニュー（enabledのみ）</h2>

  <% if (drink_items || []).any? %>
    <ul>
      <% (drink_items || []).each do |item| %>
        <li class="mb-2">
          <%= item.name %>（<%= item.price_points %>pt）

          <%= button_to "送信",
                stream_session_drink_orders_path(stream_session),
                method: :post,
                params: { drink_item_id: item.id },
                form: { data: { turbo: false } },
                class: "btn btn-sm btn-primary ms-2" %>
        </li>
      <% end %>
    </ul>
  <% else %>
    <p>現在、表示できるドリンクがありません</p>
  <% end %>

  <hr>

  <%= turbo_stream_from [stream_session, :pending_drink_orders] %>
  <%= render "stream_sessions/pending_drink_orders", stream_session: stream_session %>

  <hr>

  <%= turbo_stream_from [stream_session, :comments] %>

  <div id="comments">
    <%= render partial: "comments/comment", collection: (comments || []), as: :comment %>
  </div>

  <%= render "stream_sessions/comments/form", stream_session: stream_session %>

  <hr>

  <%= turbo_stream_from [stream_session, :viewer_stage] %>

  <div id="viewer_stage">
    <%= render "stream_sessions/ivs_viewer", stream_session: stream_session %>
  </div>

<% else %>
  <div class="text-muted">現在配信していません</div>

  <hr>

  <h2>ドリンクメニュー（enabledのみ）</h2>
  <p>配信中のみドリンク送信できます</p>

  <hr>

  <div class="text-muted">配信中のみ表示されます</div>

  <hr>

  <p>配信中のみコメントできます</p>

  <hr>

  <div id="viewer_stage"></div>
<% end %>
```

---

## 5) `app/notifiers/stream_session_notifier.rb`（ここが最重要：replace→update）

```ruby
# frozen_string_literal: true

class StreamSessionNotifier
  def self.broadcast_stream_state(booth:)
    booth = Booth.find(booth.id)
    stream_session = booth.current_stream_session

    Turbo::StreamsChannel.broadcast_update_to(
      [ booth, :stream_state ],
      target: "stream_state",
      partial: "booths/stream_state",
      locals: {
        booth: booth,
        stream_session: stream_session,
        comments: stream_session ? Comment.alive.where(stream_session: stream_session)
                                     .order(created_at: :desc).limit(50).reverse : [],
        drink_items: booth.store.drink_items.enabled_only.ordered
      }
    )
  end

  def self.broadcast_ended(stream_session)
    Turbo::StreamsChannel.broadcast_replace_to(
      [ stream_session, :pending_drink_orders ],
      target: "pending_drink_orders",
      partial: "stream_sessions/ended",
      locals: { stream_session: stream_session }
    )

    Turbo::StreamsChannel.broadcast_replace_to(
      [ stream_session, :cast_pending_drink_orders ],
      target: "cast_pending_drink_orders",
      partial: "cast/stream_sessions/ended",
      locals: { stream_session: stream_session }
    )

    Turbo::StreamsChannel.broadcast_replace_to(
      [ stream_session, :viewer_stage ],
      target: "viewer_stage",
      partial: "stream_sessions/viewer_ended",
      locals: { stream_session: stream_session }
    )

    Turbo::StreamsChannel.broadcast_replace_to(
      [ stream_session.booth, :stream_state ],
      target: "flash_inner",
      partial: "shared/flash_message",
      locals: { level: "secondary", message: "配信が終了しました。未消化ドリンクは返却されました。" }
    )
  end
end
```

---

## 6) `app/views/stream_sessions/_viewer_ended.html.erb`（target維持のためid付きで）

```erb
<div id="viewer_stage" class="alert alert-secondary mb-0" role="alert">
  配信が終了しました。
</div>
```

---

## ✅ ここまでやったら必ず通す検証（あなたが何十回も踏む箇所の再発防止）

配信開始 → 配信終了 のあと、console で必ず：

```js
document.querySelectorAll("#stream_state").length
```

* 常に **1** なら OK（箱が消えない＝updateが正しく効いている）

---

# 追加

## 1) flash 用 partial を作る

### `app/views/shared/_flash_message.html.erb`

```erb
<%# locals: level:, message: %>
<div class="alert alert-<%= level %>" role="alert">
  <%= message %>
</div>
```
