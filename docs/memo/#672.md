了解です。
#672 を、**既存プロフィール更新を壊さずに、電話番号登録だけを安全に追加する設計**でまとめて出します。

今回は次の方針です。

* `users.phone_number` と `users.phone_verified_at` を追加
* `phone_number` は **認証成功時だけ保存**
* 既存の `profile_params` には入れない
* プロフィール画面で

  * 電話番号入力 → OTP送信
  * OTP入力 → 認証成功で保存
* 他ユーザーが使っている認証済み番号は保存不可
* 既存プロフィール更新は従来どおり `PATCH /profile`

---

# 1. ブランチ名

```text
feature/672-user-phone-number
```

---

# 2. 空マイグレーション作成コマンド

まず空ファイルを作ってください。

```bash
docker compose run --rm app bin/rails generate migration AddPhoneNumberToUsers
```

作成されたファイル全文を、次で置き換えてください。

---

# 3. マイグレーション全文

```ruby
# frozen_string_literal: true

class AddPhoneNumberToUsers < ActiveRecord::Migration[8.1]
  def change
    add_column :users, :phone_number, :string
    add_column :users, :phone_verified_at, :datetime

    add_index :users, :phone_number, unique: true
  end
end
```

---

# 4. `app/models/user.rb` 全文置き換え

```ruby
class User < ApplicationRecord
  devise :database_authenticatable, :recoverable, :rememberable, :validatable

  enum :role, { customer: 0, cast: 1, store_admin: 2, system_admin: 3 }

  validates :bio, length: { maximum: 500 }, allow_nil: true
  validates :phone_number, uniqueness: true, allow_nil: true

  ROLE_LEVELS = {
    customer: 0,
    cast: 1,
    store_admin: 2,
    system_admin: 3
  }.freeze

  has_one_attached :avatar

  has_one :wallet, foreign_key: :customer_user_id, dependent: :destroy, inverse_of: :customer_user
  has_many :store_memberships, dependent: :destroy
  has_many :stores, through: :store_memberships
  has_many :booth_casts, foreign_key: :cast_user_id, dependent: :restrict_with_error
  has_many :cast_booths, through: :booth_casts, source: :booth
  has_many :favorite_booths, dependent: :destroy
  has_many :favorite_stores, dependent: :destroy

  # --- Soft delete ---
  def deleted?
    deleted_at.present?
  end

  # Devise: 停止ユーザーはログイン不可
  def active_for_authentication?
    super && !deleted?
  end

  def role_level
    ROLE_LEVELS.fetch(role.to_sym)
  rescue KeyError, NoMethodError
    -1
  end

  def at_least?(required_role)
    required_level = ROLE_LEVELS.fetch(required_role.to_sym)
    role_level >= required_level
  rescue KeyError
    false
  end

  def admin_of_store?(store_id)
    return false if store_id.blank?
    store_memberships.admin_only.exists?(store_id: store_id)
  end

  def phone_verified?
    phone_number.present? && phone_verified_at.present?
  end
end
```

---

# 5. `config/routes.rb` 差し替え箇所

`resource :profile, only: %i[edit update]` を次に置き換えてください。

```ruby
resource :profile, only: %i[edit update] do
  post :send_phone_otp
  post :verify_phone_otp
end
```

---

# 6. `app/controllers/profiles_controller.rb` 全文置き換え

```ruby
# frozen_string_literal: true

class ProfilesController < ApplicationController
  include RemovableImageAttachment
  include AttachmentPersistenceChecker

  before_action :authenticate_user!

  def edit
    @user = current_user
    @pending_phone_number = nil
  end

  def update
    @user = current_user

    success = @user.update(profile_params)

    if success
      unless ensure_attachment_persisted!(record: @user, attachment_name: :avatar)
        return respond_profile_update_error(@user.errors.full_messages)
      end

      purge_attachment_if_requested(
        record: @user,
        attachment_name: :avatar,
        remove_param_name: :remove_avatar
      )

      redirect_to root_path, notice: "プロフィールを更新しました"
    else
      respond_profile_update_error(@user.errors.full_messages)
    end
  end

  def send_phone_otp
    @user = current_user
    @pending_phone_number = phone_otp_params[:phone_number]

    PhoneVerifications::IssueOtpService.new(
      phone_number: @pending_phone_number,
      purpose: PhoneVerification::PURPOSE_VERIFY_PHONE,
      user: @user
    ).call!

    redirect_to edit_profile_path(phone_number: @pending_phone_number),
                notice: "認証コードをSMSで送信しました"
  rescue PhoneVerifications::PhoneNumberNormalizer::InvalidPhoneNumber
    redirect_to edit_profile_path, alert: "電話番号の形式が正しくありません"
  rescue PhoneVerifications::IssueOtpService::ResendRestricted
    redirect_to edit_profile_path(phone_number: @pending_phone_number),
                alert: "認証コードの再送は60秒後にお試しください"
  rescue ActiveRecord::RecordInvalid => e
    redirect_to edit_profile_path, alert: e.record.errors.full_messages.join(" / ")
  end

  def verify_phone_otp
    @user = current_user
    @pending_phone_number = verify_phone_otp_params[:phone_number]

    result = PhoneVerifications::VerifyOtpService.new(
      phone_number: @pending_phone_number,
      purpose: PhoneVerification::PURPOSE_VERIFY_PHONE,
      otp_code: verify_phone_otp_params[:otp_code]
    ).call!

    phone_number = result.phone_verification.phone_number

    if User.where.not(id: @user.id).exists?(phone_number: phone_number)
      return redirect_to edit_profile_path(phone_number: phone_number),
                         alert: "この電話番号はすでに他のユーザーに登録されています"
    end

    @user.update!(
      phone_number: phone_number,
      phone_verified_at: Time.current
    )

    redirect_to edit_profile_path, notice: "電話番号を認証して登録しました"
  rescue PhoneVerifications::PhoneNumberNormalizer::InvalidPhoneNumber
    redirect_to edit_profile_path, alert: "電話番号の形式が正しくありません"
  rescue PhoneVerifications::VerifyOtpService::NotFound,
         PhoneVerifications::VerifyOtpService::InvalidCode
    redirect_to edit_profile_path(phone_number: @pending_phone_number),
                alert: "認証コードが正しくありません"
  rescue PhoneVerifications::VerifyOtpService::Expired
    redirect_to edit_profile_path(phone_number: @pending_phone_number),
                alert: "認証コードの有効期限が切れています"
  rescue PhoneVerifications::VerifyOtpService::AttemptsExceeded
    redirect_to edit_profile_path(phone_number: @pending_phone_number),
                alert: "認証コードの試行回数が上限に達しました"
  rescue PhoneVerifications::VerifyOtpService::AlreadyCompleted
    redirect_to edit_profile_path(phone_number: @pending_phone_number),
                alert: "この認証コードはすでに使用されています"
  end

  private

  def profile_params
    params.require(:user).permit(:display_name, :bio, :avatar)
  end

  def phone_otp_params
    params.permit(:phone_number)
  end

  def verify_phone_otp_params
    params.permit(:phone_number, :otp_code)
  end

  def respond_profile_update_error(messages)
    message = messages.join(" / ")

    respond_to do |format|
      format.turbo_stream do
        flash.now[:alert] = message

        render turbo_stream: turbo_stream.update(
          "flash_inner",
          partial: "shared/flash_message",
          locals: { level: "danger", message: flash.now[:alert] }
        ), status: :unprocessable_entity
      end

      format.html do
        redirect_to edit_profile_path, alert: message
      end
    end
  end
end
```

---

# 7. `app/views/profiles/edit.html.erb` 全文置き換え

```erb
<% content_for :head do %>
  <%= render "shared/filepond_head" %>
<% end %>
<% content_for :title, "プロフィール編集" %>

<%= form_with model: @user, url: profile_path do |f| %>
  <div class="mb-3">
    <%= f.label :display_name, "表示名", class: "form-label" %>
    <%= f.text_field :display_name, class: "form-control" %>
    <div class="form-text">未入力の場合は email が表示されます（将来変更の可能性あり）</div>
  </div>

  <div class="mb-3">
    <%= f.label :bio, "自己紹介（bio）", class: "form-label" %>
    <%= f.text_area :bio, class: "form-control", rows: 6 %>
    <div class="form-text">最大500文字</div>
  </div>

  <div
    class="mb-3 image-upload-field"
    data-controller="image-upload"
    data-image-upload-width-value="1024"
    data-image-upload-height-value="1024"
    <% if @user.avatar.attached? && @user.avatar.blob&.persisted? %>
      data-image-upload-initial-url-value="<%= url_for(@user.avatar) %>"
    <% end %>>
    <%= f.label :avatar, "プロフィール画像", class: "form-label" %>

    <%= f.hidden_field :remove_avatar,
          value: "0",
          data: { "image-upload-target": "removeFlag" } %>

    <%= f.file_field :avatar,
          accept: ".jpg,.jpeg,.png,.webp,.heic,.heif",
          data: { "image-upload-target": "input" } %>
  </div>

  <div class="d-flex gap-2">
    <%= f.submit "更新する", class: "btn btn-primary" %>
    <%= link_to "戻る", dashboard_path, class: "btn btn-outline-secondary" %>
  </div>
<% end %>

<hr class="my-4">

<div class="card">
  <div class="card-body">
    <h2 class="h5 mb-3">電話番号認証</h2>

    <% if @user.phone_verified? %>
      <div class="alert alert-success mb-3">
        登録済み電話番号: <strong><%= @user.phone_number %></strong><br>
        認証日時: <%= l(@user.phone_verified_at) %>
      </div>
    <% else %>
      <div class="alert alert-secondary mb-3">
        電話番号はまだ認証されていません
      </div>
    <% end %>

    <%= form_with url: send_phone_otp_profile_path, method: :post, local: true, class: "mb-3" do %>
      <div class="mb-3">
        <%= label_tag :phone_number, "電話番号", class: "form-label" %>
        <%= telephone_field_tag :phone_number, params[:phone_number].presence || @user.phone_number, class: "form-control", placeholder: "09012345678" %>
        <div class="form-text">認証コードを送信する電話番号を入力してください</div>
      </div>

      <%= submit_tag "認証コードを送信", class: "btn btn-outline-primary" %>
    <% end %>

    <%= form_with url: verify_phone_otp_profile_path, method: :post, local: true do %>
      <div class="mb-3">
        <%= label_tag :phone_number, "認証対象の電話番号", class: "form-label" %>
        <%= telephone_field_tag :phone_number, params[:phone_number].presence || @user.phone_number, class: "form-control", placeholder: "09012345678" %>
      </div>

      <div class="mb-3">
        <%= label_tag :otp_code, "認証コード", class: "form-label" %>
        <%= text_field_tag :otp_code, nil, class: "form-control", inputmode: "numeric", maxlength: 6, placeholder: "6桁の認証コード" %>
      </div>

      <%= submit_tag "認証して電話番号を登録", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
```

---

# 8. `test/integration/profile_update_test.rb` 全文置き換え

```ruby
# frozen_string_literal: true

require "test_helper"

class ProfileUpdateTest < ActionDispatch::IntegrationTest
  test "profile update redirects to home with notice" do
    user = User.create!(
      email: "profile_update@example.com",
      password: "password",
      password_confirmation: "password",
      role: :customer
    )

    sign_in user, scope: :user

    patch profile_path, params: {
      user: {
        display_name: "更新後の表示名",
        bio: "更新後の自己紹介"
      }
    }

    assert_redirected_to root_path
    follow_redirect!
    assert_response :success
    assert_includes @response.body, "プロフィールを更新しました"

    user.reload
    assert_equal "更新後の表示名", user.display_name
    assert_equal "更新後の自己紹介", user.bio
  end

  test "profile update failure redirects to edit for html request" do
    user = User.create!(
      email: "profile_update_failure@example.com",
      password: "password",
      password_confirmation: "password",
      role: :customer,
      bio: "初期bio"
    )

    sign_in user, scope: :user

    patch profile_path, params: {
      user: {
        display_name: "更新後の表示名",
        bio: "a" * 501
      }
    }

    assert_redirected_to edit_profile_path

    follow_redirect!
    assert_response :success
    assert_includes @response.body, "プロフィール編集"

    user.reload
    assert_not_equal "更新後の表示名", user.display_name
    assert_equal "初期bio", user.bio
  end

  test "profile update failure returns unprocessable_entity for turbo_stream request" do
    user = User.create!(
      email: "profile_update_failure_turbo@example.com",
      password: "password",
      password_confirmation: "password",
      role: :customer,
      bio: "初期bio"
    )

    sign_in user, scope: :user

    patch profile_path,
          params: {
            user: {
              display_name: "更新後の表示名",
              bio: "a" * 501
            }
          },
          as: :turbo_stream

    assert_response :unprocessable_entity
    assert_includes @response.body, "turbo-stream"
    assert_includes @response.body, "flash_inner"

    user.reload
    assert_not_equal "更新後の表示名", user.display_name
    assert_equal "初期bio", user.bio
  end

  test "send phone otp issues verification without saving phone_number to user" do
    user = User.create!(
      email: "profile_phone_send@example.com",
      password: "password",
      password_confirmation: "password",
      role: :customer
    )

    sign_in user, scope: :user

    post send_phone_otp_profile_path, params: { phone_number: "09012345678" }

    assert_redirected_to edit_profile_path(phone_number: "09012345678")
    follow_redirect!
    assert_response :success
    assert_includes @response.body, "認証コードをSMSで送信しました"

    user.reload
    assert_nil user.phone_number
    assert_nil user.phone_verified_at

    verification = PhoneVerification.order(:id).last
    assert_equal user.id, verification.user_id
    assert_equal PhoneVerification::PURPOSE_VERIFY_PHONE, verification.purpose
    assert_equal "+819012345678", verification.phone_number
  end

  test "verify phone otp saves verified phone number to user" do
    user = User.create!(
      email: "profile_phone_verify@example.com",
      password: "password",
      password_confirmation: "password",
      role: :customer
    )

    sign_in user, scope: :user

    issue_result = PhoneVerifications::IssueOtpService.new(
      phone_number: "09012345678",
      purpose: PhoneVerification::PURPOSE_VERIFY_PHONE,
      user: user,
      sms_sender: fake_sms_sender
    ).call!

    post verify_phone_otp_profile_path, params: {
      phone_number: "09012345678",
      otp_code: issue_result.otp_code
    }

    assert_redirected_to edit_profile_path
    follow_redirect!
    assert_response :success
    assert_includes @response.body, "電話番号を認証して登録しました"

    user.reload
    assert_equal "+819012345678", user.phone_number
    assert user.phone_verified_at.present?
  end

  test "verify phone otp rejects phone number already used by another user" do
    existing_user = User.create!(
      email: "existing_phone_user@example.com",
      password: "password",
      password_confirmation: "password",
      role: :customer,
      phone_number: "+819012345678",
      phone_verified_at: Time.current
    )

    user = User.create!(
      email: "new_phone_user@example.com",
      password: "password",
      password_confirmation: "password",
      role: :customer
    )

    sign_in user, scope: :user

    issue_result = PhoneVerifications::IssueOtpService.new(
      phone_number: "09012345678",
      purpose: PhoneVerification::PURPOSE_VERIFY_PHONE,
      user: user,
      sms_sender: fake_sms_sender
    ).call!

    post verify_phone_otp_profile_path, params: {
      phone_number: "09012345678",
      otp_code: issue_result.otp_code
    }

    assert_redirected_to edit_profile_path(phone_number: "+819012345678")
    follow_redirect!
    assert_response :success
    assert_includes @response.body, "この電話番号はすでに他のユーザーに登録されています"

    user.reload
    assert_nil user.phone_number
    assert_nil user.phone_verified_at

    existing_user.reload
    assert_equal "+819012345678", existing_user.phone_number
  end

  private

  def fake_sms_sender
    Struct.new(:deliver!) do
      def deliver!(*); end
    end.new
  end
end
```

---

# 9. 実行コマンド

マイグレーション:

```bash
docker compose run --rm app bin/rails db:migrate
```

テスト:

```bash
docker compose run --rm app bin/rails test test/integration/profile_update_test.rb
```

必要なら関連もまとめて:

```bash
docker compose run --rm app bin/rails test test/models/user_test.rb test/models/phone_verification_test.rb test/services/phone_verifications/issue_otp_service_test.rb test/services/phone_verifications/verify_otp_service_test.rb test/integration/profile_update_test.rb
```

---

# 10. この実装で満たすこと

* 1ユーザー1電話番号
* 認証成功時のみ保存
* 他ユーザー利用中番号は保存不可
* 既存プロフィール更新を壊さない

---

# 11. 補足

今回、プロフィール画面では `form_with` を3本に分けています。

* 既存プロフィール更新
* OTP送信
* OTP検証

これにより、既存 `PATCH /profile` を壊さず、
電話番号だけ別フローで安全に扱えます。

---

このまま入れて、もしテストやルーティングで詰まったらエラー全文を貼ってください。

---
---

了解です。
では、**プロフィール編集から電話番号認証を切り離す** 形で、#672 の実装をコピペ可能な形で出します。

今回は次の構成にします。

* ダッシュボードに「電話番号認証」導線を追加
* `PhoneVerificationsController` を新設
* 画面を2つに分離

  * 電話番号入力ページ
  * 認証コード入力ページ
* 認証成功時のみ `users.phone_number` と `phone_verified_at` を保存
* 既存プロフィール編集から電話番号UIを削除
* 既存プロフィール更新機能はそのまま維持

---

# 1. `config/routes.rb` 全文置き換え

```ruby
# frozen_string_literal: true

Rails.application.routes.draw do
  get "up" => "rails/health#show", as: :rails_health_check

  devise_for :users, skip: %i[registrations]

  root "home#show"

  if Rails.env.development?
    namespace :dev do
      resource :banuba_verification, only: %i[show], controller: "banuba_verifications"
      resource :deepar_verification, only: %i[show], controller: "deepar_verifications"
      resource :filepond_verification, only: %i[show create], controller: "filepond_verifications"
    end
  end

  # --- Common dashboard (login required) ---
  get "/dashboard", to: "dashboard#show", as: :dashboard

  # --- Common profile (login required) ---
  resource :profile, only: %i[edit update]

  # --- Phone verification (login required) ---
  resource :phone_verification, only: [] do
    get :new
    post :create
    get :confirm
    post :verify
  end

  # --- Public user profiles (login required) ---
  resources :users, only: %i[show]

  # --- Public (customer registration) ---
  get  "/sign_up", to: "customers/registrations#new", as: :sign_up
  post "/sign_up", to: "customers/registrations#create"

  # --- Cast registration (invite only) ---
  get  "/cast/sign_up", to: "casts/registrations#new", as: :cast_sign_up
  post "/cast/sign_up", to: "casts/registrations#create"

  # --- StoreAdmin registration (invite only) ---
  get  "/store_admin/sign_up", to: "store_admins/registrations#new", as: :store_admin_sign_up
  post "/store_admin/sign_up", to: "store_admins/registrations#create"

  # --- Public (store registration) ---
  namespace :stores do
    get  :new_registration, to: "registrations#new"
    post :registrations, to: "registrations#create"
  end

  # --- Public (cast invitation) ---
  get  "/cast_invitations/:token", to: "cast_invitations#show", as: :cast_invitation
  post "/cast_invitations/:token/accept", to: "cast_invitations#accept", as: :accept_cast_invitation

  # --- Public (store_admin invitation) ---
  get  "/store_admin_invitations/:token", to: "store_admin_invitations#show", as: :store_admin_invitation
  post "/store_admin_invitations/:token/accept", to: "store_admin_invitations#accept", as: :accept_store_admin_invitation

  # --- Favorites (login required) ---
  namespace :favorites do
    resources :booths, only: %i[index]
    resources :stores, only: %i[index]
  end

  # --- Customer (login required) ---
  resources :stores, only: %i[show] do
    resource :favorite, only: %i[create destroy], controller: "favorites/stores"
  end

  # --- Public (customer) ---
  resources :booths, only: %i[show] do
    member do
      get :enter
      post :enter_as_cast
      get :viewer_drink_menu
    end

    resource :favorite, only: %i[create destroy], controller: "favorites/booths"
  end

  resources :stream_sessions, only: [] do
    resources :comments, only: %i[create], module: :stream_sessions do
      member do
        patch :hide
        patch :unhide
        post :report
      end
    end

    resources :drink_orders, only: %i[create], module: :stream_sessions
    resources :ivs_participant_tokens, only: %i[create], module: :stream_sessions

    resource :presence, only: [], module: :stream_sessions do
      post :ping
    end

    get :presence_summary, on: :member
  end

  namespace :wallet do
    resources :purchases, only: %i[new create]
  end

  get  "/checkout/return", to: "checkout#return"
  post "/webhooks/stripe", to: "webhooks/stripe#create"

  # --- Cast ---
  namespace :cast do
    resource :current_booth, only: %i[create]

    resources :booths, only: %i[index edit update] do
      collection do
        get :select_modal
      end

      get :live, on: :member
      patch :status, on: :member
      resources :stream_sessions, only: %i[create], module: :booths
    end

    resources :stream_sessions, only: %i[show] do
      post :finish, on: :member
      get  :pending_drink_orders, on: :member
      get  :meta_display, on: :member
      patch :metadata, on: :member
    end

    resources :drink_orders, only: [] do
      post :consume, on: :member
    end
  end

  # --- Store Admin ---
  namespace :admin do
    resources :stores, only: %i[index edit update] do
      collection do
        get :select_modal
      end
    end

    resource :payout_account, only: %i[edit update], controller: "store_payout_accounts"
    resource :current_store, only: %i[create], controller: "current_stores"

    resources :booths, only: %i[index new create] do
      member do
        patch :archive
        post :force_end
        post :assign_cast
      end
    end

    resources :drink_items, only: %i[index create update destroy]
    resources :store_bans, only: %i[index create destroy]
    resources :comment_reports, only: %i[index] do
      post :reject, on: :member
      post :ban, on: :member
    end

    resources :casts, only: %i[index destroy]
    resources :cast_invitations, only: %i[index create]
    resources :store_admin_invitations, only: %i[index create]

    get "/cast_metrics", to: "metrics#cast"

    resources :settlements, only: %i[index show]
  end

  # --- System Admin ---
  namespace :system_admin do
    resources :referral_codes, only: %i[index new create edit update]
    resources :users, only: %i[index new create edit update destroy]
    resources :effects, only: %i[index new create edit update]

    resources :settlements, only: %i[index show] do
      collection do
        get  "manual/new", to: "settlements#new_manual", as: :new_manual
        post "manual/preview", to: "settlements#preview_manual", as: :preview_manual
        post "manual", to: "settlements#create_manual", as: :create_manual

        post :export_csv
      end

      member do
        post :confirm
        post :mark_paid
      end
    end

    resources :settlement_exports, only: %i[index show create]
  end
end
```

---

# 2. 新規作成 `app/controllers/phone_verifications_controller.rb`

```ruby
# frozen_string_literal: true

class PhoneVerificationsController < ApplicationController
  before_action :authenticate_user!

  def new
    @user = current_user
    @phone_number = session[:pending_phone_verification_number].presence || @user.phone_number
  end

  def create
    @user = current_user
    phone_number = params[:phone_number].to_s

    result = PhoneVerifications::IssueOtpService.new(
      phone_number:,
      purpose: PhoneVerification::PURPOSE_VERIFY_PHONE,
      user: @user
    ).call!

    session[:pending_phone_verification_number] = result.phone_number

    redirect_to confirm_phone_verification_path, notice: "認証コードをSMSで送信しました"
  rescue PhoneVerifications::PhoneNumberNormalizer::InvalidPhoneNumber
    redirect_to new_phone_verification_path, alert: "電話番号の形式が正しくありません"
  rescue PhoneVerifications::IssueOtpService::ResendRestricted
    session[:pending_phone_verification_number] = phone_number
    redirect_to confirm_phone_verification_path, alert: "認証コードの再送は60秒後にお試しください"
  rescue ActiveRecord::RecordInvalid => e
    redirect_to new_phone_verification_path, alert: e.record.errors.full_messages.join(" / ")
  end

  def confirm
    @user = current_user
    @phone_number = session[:pending_phone_verification_number]

    if @phone_number.blank?
      redirect_to new_phone_verification_path, alert: "先に電話番号を入力してください"
    end
  end

  def verify
    @user = current_user
    phone_number = session[:pending_phone_verification_number].to_s
    otp_code = params[:otp_code].to_s

    if phone_number.blank?
      return redirect_to new_phone_verification_path, alert: "先に電話番号を入力してください"
    end

    result = PhoneVerifications::VerifyOtpService.new(
      phone_number:,
      purpose: PhoneVerification::PURPOSE_VERIFY_PHONE,
      otp_code:
    ).call!

    normalized_phone_number = result.phone_verification.phone_number

    if User.where.not(id: @user.id).exists?(phone_number: normalized_phone_number)
      return redirect_to new_phone_verification_path,
                         alert: "この電話番号はすでに他のユーザーに登録されています"
    end

    @user.update!(
      phone_number: normalized_phone_number,
      phone_verified_at: Time.current
    )

    session.delete(:pending_phone_verification_number)

    redirect_to dashboard_path, notice: "電話番号を認証して登録しました"
  rescue PhoneVerifications::VerifyOtpService::NotFound,
         PhoneVerifications::VerifyOtpService::InvalidCode
    redirect_to confirm_phone_verification_path, alert: "認証コードが正しくありません"
  rescue PhoneVerifications::VerifyOtpService::Expired
    redirect_to confirm_phone_verification_path, alert: "認証コードの有効期限が切れています"
  rescue PhoneVerifications::VerifyOtpService::AttemptsExceeded
    redirect_to confirm_phone_verification_path, alert: "認証コードの試行回数が上限に達しました"
  rescue PhoneVerifications::VerifyOtpService::AlreadyCompleted
    redirect_to confirm_phone_verification_path, alert: "この認証コードはすでに使用されています"
  end
end
```

---

# 3. `app/views/dashboard/show.html.erb` の差し替え箇所

このカードの直後あたりに追加してください。
`プロフィール編集` のすぐ下が自然です。

```erb
<% if current_user.at_least?(:customer) %>
  <div class="col-12 col-md-6 col-xl-4">
    <%= link_to new_phone_verification_path, class: "text-decoration-none" do %>
      <div class="card h-100 dashboard-role-card dashboard-role-card-customer" data-bs-theme="light">
        <div class="card-body">
          <h5 class="card-title mb-2">
            電話番号認証
            <% if current_user.phone_verified? %>
              <span class="badge text-bg-success ms-2">認証済み</span>
            <% end %>
          </h5>
          <p class="card-text mb-0">
            <% if current_user.phone_verified? %>
              認証済み電話番号の確認・変更を行います
            <% else %>
              電話番号を登録して認証します
            <% end %>
          </p>
        </div>
      </div>
    <% end %>
  </div>
<% end %>
```

---

# 4. `app/views/profiles/edit.html.erb` 全文置き換え

電話番号認証ブロックを削除して、元のプロフィール編集だけに戻します。

```erb
<% content_for :head do %>
  <%= render "shared/filepond_head" %>
<% end %>
<% content_for :title, "プロフィール編集" %>

<%= form_with model: @user, url: profile_path do |f| %>
  <div class="mb-3">
    <%= f.label :display_name, "表示名", class: "form-label" %>
    <%= f.text_field :display_name, class: "form-control" %>
    <div class="form-text">未入力の場合は email が表示されます（将来変更の可能性あり）</div>
  </div>

  <div class="mb-3">
    <%= f.label :bio, "自己紹介（bio）", class: "form-label" %>
    <%= f.text_area :bio, class: "form-control", rows: 6 %>
    <div class="form-text">最大500文字</div>
  </div>

  <div
    class="mb-3 image-upload-field"
    data-controller="image-upload"
    data-image-upload-width-value="1024"
    data-image-upload-height-value="1024"
    <% if @user.avatar.attached? && @user.avatar.blob&.persisted? %>
      data-image-upload-initial-url-value="<%= url_for(@user.avatar) %>"
    <% end %>>
    <%= f.label :avatar, "プロフィール画像", class: "form-label" %>

    <%= f.hidden_field :remove_avatar,
          value: "0",
          data: { "image-upload-target": "removeFlag" } %>

    <%= f.file_field :avatar,
          accept: ".jpg,.jpeg,.png,.webp,.heic,.heif",
          data: { "image-upload-target": "input" } %>
  </div>

  <div class="d-flex gap-2">
    <%= f.submit "更新する", class: "btn btn-primary" %>
    <%= link_to "戻る", dashboard_path, class: "btn btn-outline-secondary" %>
  </div>
<% end %>
```

---

# 5. 新規作成 `app/views/phone_verifications/new.html.erb`

```erb
<% content_for :title, "電話番号認証" %>

<div class="row justify-content-center">
  <div class="col-12 col-md-8 col-lg-6">
    <div class="card" data-bs-theme="light">
      <div class="card-body">
        <h1 class="h4 mb-3">電話番号認証</h1>

        <% if @user.phone_verified? %>
          <div class="alert alert-success">
            現在の認証済み電話番号: <strong><%= @user.phone_number %></strong><br>
            認証日時: <%= l(@user.phone_verified_at) %>
          </div>
          <p class="text-muted">
            新しい電話番号を入力して認証すると、登録済み番号を上書きできます。
          </p>
        <% else %>
          <div class="alert alert-secondary">
            電話番号はまだ認証されていません。
          </div>
        <% end %>

        <%= form_with url: phone_verification_path, method: :post do %>
          <div class="mb-3">
            <%= label_tag :phone_number, "電話番号", class: "form-label" %>
            <%= telephone_field_tag :phone_number, @phone_number, class: "form-control", placeholder: "09012345678" %>
            <div class="form-text">入力した電話番号宛に認証コードを送信します。</div>
          </div>

          <div class="d-flex gap-2">
            <%= submit_tag "認証コードを送信", class: "btn btn-primary" %>
            <%= link_to "戻る", dashboard_path, class: "btn btn-outline-secondary" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
```

---

# 6. 新規作成 `app/views/phone_verifications/confirm.html.erb`

```erb
<% content_for :title, "認証コード入力" %>

<div class="row justify-content-center">
  <div class="col-12 col-md-8 col-lg-6">
    <div class="card" data-bs-theme="light">
      <div class="card-body">
        <h1 class="h4 mb-3">認証コード入力</h1>

        <p class="mb-3">
          <strong><%= @phone_number %></strong> 宛に認証コードを送信しました。<br>
          SMSに記載された6桁の認証コードを入力してください。
        </p>

        <%= form_with url: verify_phone_verification_path, method: :post do %>
          <div class="mb-3">
            <%= label_tag :otp_code, "認証コード", class: "form-label" %>
            <%= text_field_tag :otp_code, nil, class: "form-control", inputmode: "numeric", maxlength: 6, placeholder: "6桁の認証コード" %>
          </div>

          <div class="d-flex gap-2 flex-wrap">
            <%= submit_tag "認証して登録", class: "btn btn-primary" %>
            <%= button_to "認証コードを再送", phone_verification_path, method: :post,
                  params: { phone_number: @phone_number },
                  class: "btn btn-outline-primary" %>
            <%= link_to "電話番号を入力し直す", new_phone_verification_path, class: "btn btn-outline-secondary" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
```

---

# 7. `test/integration/profile_update_test.rb` 全文置き換え

電話番号認証テストを分離するので、profile側は元の責務だけに戻します。

```ruby
# frozen_string_literal: true

require "test_helper"

class ProfileUpdateTest < ActionDispatch::IntegrationTest
  test "profile update redirects to home with notice" do
    user = User.create!(
      email: "profile_update@example.com",
      password: "password",
      password_confirmation: "password",
      role: :customer
    )

    sign_in user, scope: :user

    patch profile_path, params: {
      user: {
        display_name: "更新後の表示名",
        bio: "更新後の自己紹介"
      }
    }

    assert_redirected_to root_path
    follow_redirect!
    assert_response :success
    assert_includes @response.body, "プロフィールを更新しました"

    user.reload
    assert_equal "更新後の表示名", user.display_name
    assert_equal "更新後の自己紹介", user.bio
  end

  test "profile update failure redirects to edit for html request" do
    user = User.create!(
      email: "profile_update_failure@example.com",
      password: "password",
      password_confirmation: "password",
      role: :customer,
      bio: "初期bio"
    )

    sign_in user, scope: :user

    patch profile_path, params: {
      user: {
        display_name: "更新後の表示名",
        bio: "a" * 501
      }
    }

    assert_redirected_to edit_profile_path

    follow_redirect!
    assert_response :success
    assert_includes @response.body, "プロフィール編集"

    user.reload
    assert_not_equal "更新後の表示名", user.display_name
    assert_equal "初期bio", user.bio
  end

  test "profile update failure returns unprocessable_entity for turbo_stream request" do
    user = User.create!(
      email: "profile_update_failure_turbo@example.com",
      password: "password",
      password_confirmation: "password",
      role: :customer,
      bio: "初期bio"
    )

    sign_in user, scope: :user

    patch profile_path,
          params: {
            user: {
              display_name: "更新後の表示名",
              bio: "a" * 501
            }
          },
          as: :turbo_stream

    assert_response :unprocessable_entity
    assert_includes @response.body, "turbo-stream"
    assert_includes @response.body, "flash_inner"

    user.reload
    assert_not_equal "更新後の表示名", user.display_name
    assert_equal "初期bio", user.bio
  end
end
```

---

# 8. 新規作成 `test/integration/phone_verification_flow_test.rb`

```ruby
# frozen_string_literal: true

require "test_helper"

class PhoneVerificationFlowTest < ActionDispatch::IntegrationTest
  test "dashboard shows phone verification link" do
    user = User.create!(
      email: "dashboard_phone_link@example.com",
      password: "password",
      password_confirmation: "password",
      role: :customer
    )

    sign_in user, scope: :user

    get dashboard_path

    assert_response :success
    assert_includes @response.body, "電話番号認証"
  end

  test "send phone otp redirects to confirm without saving phone_number to user" do
    user = User.create!(
      email: "phone_send@example.com",
      password: "password",
      password_confirmation: "password",
      role: :customer
    )

    sign_in user, scope: :user

    post phone_verification_path, params: { phone_number: "09012345678" }

    assert_redirected_to confirm_phone_verification_path
    follow_redirect!
    assert_response :success
    assert_includes @response.body, "認証コード入力"

    user.reload
    assert_nil user.phone_number
    assert_nil user.phone_verified_at

    verification = PhoneVerification.order(:id).last
    assert_equal user.id, verification.user_id
    assert_equal "+819012345678", verification.phone_number
  end

  test "verify phone otp saves verified phone number to user" do
    user = User.create!(
      email: "phone_verify@example.com",
      password: "password",
      password_confirmation: "password",
      role: :customer
    )

    sign_in user, scope: :user

    issue_result = PhoneVerifications::IssueOtpService.new(
      phone_number: "09012345678",
      purpose: PhoneVerification::PURPOSE_VERIFY_PHONE,
      user: user,
      sms_sender: fake_sms_sender
    ).call!

    open_session do |sess|
      sess.post user_session_path, params: {
        user: { email: user.email, password: "password" }
      }
      sess.follow_redirect! if sess.response.redirect?
      sess.get new_phone_verification_path
      sess.request.session[:pending_phone_verification_number] = issue_result.phone_number

      sess.post verify_phone_verification_path, params: { otp_code: issue_result.otp_code }

      assert_equal dashboard_path, URI.parse(sess.response.redirect_url).path
    end

    user.reload
    assert_equal "+819012345678", user.phone_number
    assert user.phone_verified_at.present?
  end

  test "verify phone otp rejects phone number already used by another user" do
    User.create!(
      email: "existing_phone_user@example.com",
      password: "password",
      password_confirmation: "password",
      role: :customer,
      phone_number: "+819012345678",
      phone_verified_at: Time.current
    )

    user = User.create!(
      email: "new_phone_user@example.com",
      password: "password",
      password_confirmation: "password",
      role: :customer
    )

    sign_in user, scope: :user

    issue_result = PhoneVerifications::IssueOtpService.new(
      phone_number: "09012345678",
      purpose: PhoneVerification::PURPOSE_VERIFY_PHONE,
      user: user,
      sms_sender: fake_sms_sender
    ).call!

    open_session do |sess|
      sess.post user_session_path, params: {
        user: { email: user.email, password: "password" }
      }
      sess.follow_redirect! if sess.response.redirect?
      sess.get new_phone_verification_path
      sess.request.session[:pending_phone_verification_number] = issue_result.phone_number

      sess.post verify_phone_verification_path, params: { otp_code: issue_result.otp_code }

      assert_equal new_phone_verification_path, URI.parse(sess.response.redirect_url).path
      sess.follow_redirect!
      assert_includes sess.response.body, "この電話番号はすでに他のユーザーに登録されています"
    end

    user.reload
    assert_nil user.phone_number
    assert_nil user.phone_verified_at
  end

  private

  def fake_sms_sender
    Struct.new(:deliver!) do
      def deliver!(*); end
    end.new
  end
end
```

---

# 9. 実行コマンド

```bash
docker compose run --rm app bin/rails test test/integration/profile_update_test.rb test/integration/phone_verification_flow_test.rb
```

必要なら広めに:

```bash
docker compose run --rm app bin/rails test test/models/user_test.rb test/models/phone_verification_test.rb test/services/phone_verifications/issue_otp_service_test.rb test/services/phone_verifications/verify_otp_service_test.rb test/integration/profile_update_test.rb test/integration/phone_verification_flow_test.rb
```

---

# 10. この構成で改善される点

* プロフィール編集と電話番号認証が分離される
* 「電話番号欄」と「認証対象の電話番号欄」が混在しない
* 1画面1状態になる
* 将来の電話番号ログイン導線と揃えやすい
* 再送や期限切れの扱いも自然

---

# 11. 補足

今回は `pending_phone_verification_number` を **session** に持たせています。
#672 の範囲ならこれが一番シンプルです。

* 認証対象番号を User に先保存しない
* confirm画面で番号を確定表示できる
* 成功後に session を消せる

このまま入れて、もしテストや画面で詰まったらエラー全文か最新ファイルを貼ってください。
