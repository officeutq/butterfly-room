了解、GO なのでこのまま **実装プラン＋貼り付け用コード一式**を出します（差分形式ではなく「ファイル全文置き換え／部分置換」スタイルでまとめます）。

---

## 最終実装プラン（#270 / admin限定）

### 変更点（Tasks → Done に直結する順）

1. **Admin の直前ページ保存（session）**

   * `Admin::BaseController` に `before_action` を追加し、`/admin/...` の GET アクセス時に `session[:admin_return_to]` を保存
   * 除外：`/admin/stores` と `/admin/current_store`（選択画面やPOST周りで汚さない）

2. **/admin/stores が return_to / return_to_key を受け取れる**

   * `Admin::StoresController#index` で params を view に渡す
   * `admin/stores/index.html.erb` の「この店舗を開く」POSTに return_to / return_to_key を引き継ぐ

3. **店舗選択POST後のリダイレクト可変化**

   * `Admin::CurrentStoresController#create` の redirect 先を以下の優先順へ：

     1. `return_to_key`（allowlist）
     2. `return_to`（安全な `/admin/...` のみ）
     3. `session[:admin_return_to]`（安全な `/admin/...` のみ）
     4. フォールバック：`dashboard_path`（既存テストと現状UXに合わせる）

4. **ヘッダーの店舗選択リンク：admin画面では元ページへ戻る**

   * `application.html.erb` で、現在ページが `/admin/...` のときだけ `return_to: request.fullpath` を付与
   * `/admin/stores` 自身のときは付けない（無限ループ/ノイズ回避）

5. **「先に店舗を選択」導線：ダッシュボードから目的に戻す**

   * `dashboard/show` の「（先に店舗を選択）」リンクに、目的に応じて

     * `return_to_key`（例：精算・振込設定、店舗設定）
     * それ以外は `return_to`（例：/admin/booths など）
       を付ける

6. **Integration test 追加**

   * ヘッダー相当導線（return_to）
   * return_to_key 導線（allowlist）
   * 不正 return_to がフォールバックされる

---

## allowlist（return_to_key）最小セット（今回の実装範囲）

* `payout_account_edit` → `edit_admin_store_payout_account_path(store)`
* `store_edit` → `edit_admin_store_path(store)`

（必要になったら今後キー追加で拡張）

---

## 貼り付け用コード

### 1) `app/controllers/admin/base_controller.rb`（全文置き換え）

```ruby
module Admin
  class BaseController < ApplicationController
    before_action -> { require_at_least!(:store_admin) }
    after_action :store_admin_return_to

    private

    # admin領域の「直前ページ」をsessionに保存（Issue #270）
    #
    # - GET の HTML のみ対象（操作POST等は除外）
    # - 200 OK のときだけ保存（リダイレクト/エラーは「直前ページ」にしない）
    # - /admin/stores（選択画面）と /admin/current_store（選択POST）では保存しない
    # - 保存値は /admin/ で始まる相対パスのみ（安全側）
    def store_admin_return_to
      return unless request.get?
      return unless request.format.html?
      return unless response.status == 200
      return unless request.fullpath.start_with?("/admin/")

      # 選択画面 / 選択POST では保存しない
      return if request.path == "/admin/stores"
      return if request.path == "/admin/current_store"

      fullpath = request.fullpath.to_s

      # open redirect / 不正URL対策：/admin/ で始まり、// を含まないものだけ
      return unless fullpath.start_with?("/admin/")
      return if fullpath.start_with?("//")
      return if fullpath.include?("\n") || fullpath.include?("\r")
      return if fullpath.include?("\0")

      session[:admin_return_to] = fullpath
    end

    def current_store
      # --- Current整合（booth優先）---
      # session[:current_booth_id] が有効なら store は booth.store を優先し、
      # session[:current_store_id] の不一致は補正する。
      #
      # 無効 booth（record不存在 / 権限的に無効）は session[:current_booth_id] をクリアする。
      #
      # ※ 07_モード導線設計.md の Current整合ルールに準拠
      if session[:current_booth_id].present?
        booth = Booth.find_by(id: session[:current_booth_id])

        if booth.blank?
          # record不存在
          session.delete(:current_booth_id)
        else
          allowed =
            if current_user.system_admin?
              true
            else
              # store_admin: 自分が admin membership を持つ store の booth のみ有効
              admin_membership_exists_for_store?(booth.store_id)
            end

          if allowed
            # booth優先で store を確定し、session不一致は補正
            session[:current_store_id] = booth.store_id if session[:current_store_id].to_i != booth.store_id
            return booth.store
          else
            # 権限的に無効（脱退/改ざん等）
            session.delete(:current_booth_id)
          end
        end
      end

      # 1) session 優先
      if session[:current_store_id].present?
        store = Store.find_by(id: session[:current_store_id])

        if store.present?
          # system_admin は membership を持たない運用なので、存在する store ならOK
          return store if current_user.system_admin?

          # store_admin は admin membership がある store のみ有効
          return store if admin_membership_exists_for_store?(store.id)
        end

        # 改ざん/脱退/削除 への耐性：不正ならクリア
        session.delete(:current_store_id)
      end

      # 2) fallback: 従来通り「最初の admin membership」
      # ※ store_admin の既存挙動を維持（system_admin の暗黙デフォルトは廃止）
      membership =
        StoreMembership
          .includes(:store)
          .where(user_id: current_user.id, membership_role: :admin)
          .order(:id)
          .first

      return membership&.store unless current_user.system_admin?

      # system_admin: session store が無い / 無効な場合は未選択扱い（暗黙デフォルトは廃止）
      nil
    end

    helper_method :current_store

    def require_current_store!
      return if current_store.present?

      redirect_to admin_stores_path, alert: "操作対象の店舗を選択してください"
    end

    def admin_membership_exists_for_store?(store_id)
      StoreMembership.exists?(
        user_id: current_user.id,
        store_id: store_id,
        membership_role: StoreMembership.membership_roles[:admin]
      )
    end
  end
end
```

---

### 2) `app/controllers/admin/stores_controller.rb`（変更：indexで受け渡し）

`index` の末尾（`@current_store_id` の下）に2行追加します。全文ではなく **該当箇所だけ置換**でOKです。

```ruby
def index
  @stores =
    if current_user.system_admin?
      Store.order(:id)
    else
      Store
        .joins(:store_memberships)
        .where(store_memberships: { user_id: current_user.id, membership_role: StoreMembership.membership_roles[:admin] })
        .distinct
        .order(:id)
    end

  # 現在選択中があれば view で判定できるように
  @current_store_id = session[:current_store_id]

  # Issue #270: return_to / return_to_key を view へ渡す（POSTに引き継ぐ）
  @return_to = params[:return_to].presence
  @return_to_key = params[:return_to_key].presence
end
```

---

### 3) `app/views/admin/stores/index.html.erb`（変更：POSTへ引き継ぐ）

`button_to` を置き換えます。

```erb
<%= button_to "この店舗を開く",
      admin_current_store_path,
      method: :post,
      params: {
        store_id: store.id,
        return_to: @return_to,
        return_to_key: @return_to_key
      },
      class: "btn btn-primary btn-sm" %>
```

（これ以外の部分はそのまま）

---

### 4) `app/controllers/admin/current_stores_controller.rb`（全文置き換え）

```ruby
# frozen_string_literal: true

module Admin
  class CurrentStoresController < Admin::BaseController
    def create
      store_id = params.require(:store_id)

      if current_user.system_admin?
        unless Store.exists?(id: store_id)
          session.delete(:current_store_id)
          redirect_to admin_stores_path, alert: "選択できない店舗です"
          return
        end
      else
        # session 改ざん対策：必ず membership チェック
        unless StoreMembership.exists?(
          user_id: current_user.id,
          store_id: store_id,
          membership_role: StoreMembership.membership_roles[:admin]
        )
          session.delete(:current_store_id)
          redirect_to admin_stores_path, alert: "選択できない店舗です"
          return
        end
      end

      session[:current_store_id] = store_id
      session.delete(:current_booth_id)

      redirect_to resolve_redirect_path(store_id), notice: "店舗を切り替えました"
    rescue ActionController::ParameterMissing
      redirect_to admin_stores_path, alert: "店舗を選択してください"
    end

    private

    # Issue #270: 店舗選択後の遷移先決定
    #
    # 1) return_to_key（allowlist）
    # 2) return_to（/admin/ の安全な相対パスのみ）
    # 3) session[:admin_return_to]（同上）
    # 4) dashboard_path（既存UX/既存テストと整合）
    def resolve_redirect_path(store_id)
      store = Store.find_by(id: store_id)

      # 1) return_to_key
      key = params[:return_to_key].presence
      if key.present?
        path = resolve_return_to_key(key, store)
        return path if path.present?
      end

      # 2) return_to
      rt = safe_admin_return_to(params[:return_to])
      return rt if rt.present?

      # 3) session
      session_rt = safe_admin_return_to(session[:admin_return_to])
      return session_rt if session_rt.present?

      # 4) fallback
      dashboard_path
    end

    # allowlist方式（推奨）
    def resolve_return_to_key(key, store)
      return nil if store.blank?

      case key.to_s
      when "payout_account_edit"
        edit_admin_store_payout_account_path(store)
      when "store_edit"
        edit_admin_store_path(store)
      else
        nil
      end
    end

    # open redirect対策：/admin/ で始まる相対パスのみ許可
    def safe_admin_return_to(value)
      s = value.to_s
      return nil if s.blank?

      return nil unless s.start_with?("/admin/")
      return nil if s.start_with?("//")
      return nil if s.include?("\n") || s.include?("\r")
      return nil if s.include?("\0")

      s
    end
  end
end
```

---

### 5) `app/views/layouts/application.html.erb`（変更：ヘッダーの店舗選択リンクに return_to を付与）

該当箇所（`link_to admin_stores_path`）を、以下のように置き換えます。

元（抜粋）：

```erb
<%= link_to admin_stores_path,
      class: "link-secondary link-underline-opacity-0 link-underline-opacity-100-hover" do %>
```

置換後：

```erb
<% stores_link_params =
     if request.get? && request.fullpath.start_with?("/admin/") && request.path != "/admin/stores"
       { return_to: request.fullpath }
     else
       {}
     end %>

<%= link_to admin_stores_path(stores_link_params),
      class: "link-secondary link-underline-opacity-0 link-underline-opacity-100-hover" do %>
```

※ admin以外ではこのIssue対象外なので、return_to は付けません（あなたの指定どおり「admin限定」）。

---

### 6) `app/views/dashboard/show.html.erb`（変更：「先に店舗を選択」リンクに return_to / return_to_key を付与）

“store が無い場合”のリンクを、以下の方針で差し替えます（代表部分だけ、同様に他も置換）。

#### system_admin / store_admin 共通で差し替える考え方

* `店舗設定（先に店舗を選択）` → `return_to_key: "store_edit"`
* `精算・振込設定（先に店舗を選択）` → `return_to_key: "payout_account_edit"`
* それ以外の admin一覧系 → `return_to: admin_xxx_path`

具体例（system_admin 節の store無し側）：

**店舗設定（先に店舗を選択）**

```erb
<%= link_to "店舗設定（先に店舗を選択）",
      admin_stores_path(return_to_key: "store_edit"),
      class: "text-decoration-none" %>
```

**ブース一覧（先に店舗を選択）**

```erb
<%= link_to "ブース一覧（先に店舗を選択）",
      admin_stores_path(return_to: admin_booths_path),
      class: "text-decoration-none" %>
```

**キャスト管理（先に店舗を選択）**

```erb
<%= link_to "キャスト管理（先に店舗を選択）",
      admin_stores_path(return_to: admin_casts_path),
      class: "text-decoration-none" %>
```

**ブースにキャストを紐づける（先に店舗を選択）**

```erb
<%= link_to "ブースにキャストを紐づける（先に店舗を選択）",
      admin_stores_path(return_to: admin_booth_casts_path),
      class: "text-decoration-none" %>
```

**キャスト別数値一覧（先に店舗を選択）**

```erb
<%= link_to "キャスト別数値一覧（直近30日）（先に店舗を選択）",
      admin_stores_path(return_to: admin_cast_metrics_path),
      class: "text-decoration-none" %>
```

**ドリンクメニュー（先に店舗を選択）**

```erb
<%= link_to "ドリンクメニュー（先に店舗を選択）",
      admin_stores_path(return_to: admin_drink_items_path),
      class: "text-decoration-none" %>
```

**店舗BAN（先に店舗を選択）**

```erb
<%= link_to "店舗BAN（先に店舗を選択）",
      admin_stores_path(return_to: admin_store_bans_path),
      class: "text-decoration-none" %>
```

**精算・振込設定（先に店舗を選択）**

```erb
<%= link_to "精算・振込設定（先に店舗を選択）",
      admin_stores_path(return_to_key: "payout_account_edit"),
      class: "text-decoration-none" %>
```

**精算（先に店舗を選択）**

```erb
<%= link_to "精算（予定・履歴）（先に店舗を選択）",
      admin_stores_path(return_to: admin_settlements_path),
      class: "text-decoration-none" %>
```

store_admin 節にも同様に適用してください（文言は既存のまま、リンク先だけ置換）。

---

## 7) テスト追加

### `test/integration/admin_store_selection_return_test.rb`（新規作成）

```ruby
# frozen_string_literal: true

require "test_helper"

class AdminStoreSelectionReturnTest < ActionDispatch::IntegrationTest
  setup do
    @store = Store.create!(name: "store1")

    @system_admin = User.create!(email: "sys_rt@example.com", password: "password", role: :system_admin)

    @store_admin = User.create!(email: "admin_rt@example.com", password: "password", role: :store_admin)
    StoreMembership.create!(store: @store, user: @store_admin, membership_role: :admin)
  end

  test "return_to: selecting store redirects back to the admin page" do
    sign_in @store_admin, scope: :user

    # 例：/admin/booths に戻したい
    get admin_stores_path(return_to: admin_booths_path)
    assert_response :success

    post admin_current_store_path, params: { store_id: @store.id, return_to: admin_booths_path }
    assert_response :redirect
    assert_redirected_to admin_booths_path
  end

  test "return_to_key: payout_account_edit redirects to the store scoped page" do
    sign_in @system_admin, scope: :user

    get admin_stores_path(return_to_key: "payout_account_edit")
    assert_response :success

    post admin_current_store_path, params: { store_id: @store.id, return_to_key: "payout_account_edit" }
    assert_response :redirect
    assert_redirected_to edit_admin_store_payout_account_path(@store)
  end

  test "invalid return_to is rejected and falls back to dashboard" do
    sign_in @store_admin, scope: :user

    post admin_current_store_path, params: { store_id: @store.id, return_to: "//evil.example.com" }
    assert_response :redirect
    assert_redirected_to dashboard_path
  end
end
```

---

## 既存テストへの影響（今回の変更で壊れにくい理由）

* `AdminCurrentStoreRequiredForSystemAdminTest` の `post admin_current_store_path` は return_to も return_to_key も付けていない
  → 直前ページsessionもセットされない（POSTだけしている）
  → フォールバックの `dashboard_path` のままなので、既存期待に一致します。

---

## 動作確認手順（docker compose 前提）

* テスト：

  * `docker compose run --rm web bin/rails test`
  * または `docker compose run --rm web bin/rails test test/integration/admin_store_selection_return_test.rb`
* 手動確認（最短）：

  1. store_admin で `/admin/booths` を開く
  2. ヘッダーの store をクリック → 店舗一覧へ（URLに return_to が付く）
  3. 店舗選択 → `/admin/booths` に戻る
  4. system_admin で dashboard の「精算・振込設定（先に店舗を選択）」→ 店舗選択 → 該当storeの振込設定 edit に遷移

---

## コミットメッセージ案

* `Improve: Admin store selection redirects back via return_to/return_to_key (Issue #270)`

## PR本文たたき台（短め）

* 目的：admin の店舗選択後に元ページ/目的ページへ戻れるように改善（open redirect 対策込み）
* 変更点：

  * `/admin/stores` が return_to/return_to_key を受け取り POST に引き継ぐ
  * 店舗選択POSTのredirect先を可変化（allowlist + /admin限定）
  * admin領域の直前ページをsession保存
  * dashboard の「先に店舗を選択」導線を return_to(_key) 付きへ
  * integration test 追加
* 動作確認：テスト通過、手動でヘッダー導線・payout_account導線確認

---

必要なら、この実装に合わせて **Issue #270 の Done 条件（dashboard_path フォールバック版）**へ微修正案も出せます。

---
---

それは「今の実装だと仕様どおり」起きます。

いまのコードは **return_to を `/admin/` 配下しか許可しない**（`safe_admin_return_to`）ので、HOME（`/`）からヘッダーの store 選択を押しても、

* ヘッダー側：`return_to` を付けていない（adminページのときだけ付ける実装にしている）
* たとえ付けても：`/` は `/admin/` じゃないので弾かれる

→ 結果、選択後はフォールバック（dashboard か session の admin_return_to）になり、HOMEに戻りません。

---

## 直し方（おすすめ：安全は維持しつつ、HOME等も戻せるようにする）

### 方針

* `return_to` の許可範囲を **「相対パス（`/` 始まり）ならOK」**に広げる
  ※ open redirect は「`//`」「改行」「NULL」などを弾けば防げます
* ヘッダーの store 選択リンクは **admin以外でも `return_to: request.fullpath` を付ける**

これで「HOME → store選択 → HOMEに戻る」が成立します。

---

## 変更箇所（最小パッチ）

### 1) `app/views/layouts/application.html.erb`

ヘッダーの store リンク生成で、admin限定だった条件を外して **常に（ただし選択画面自身は除外）** return_to を付けます。

置換イメージ（あなたの既存コードに合わせて）：

* 今：`request.fullpath.start_with?("/admin/") ...` のときだけ return_to
* 変更：`GET かつ /admin/stores 以外`なら return_to

具体例：

```erb
<% stores_link_params =
     if request.get? && request.path != "/admin/stores"
       { return_to: request.fullpath }
     else
       {}
     end %>

<%= link_to admin_stores_path(stores_link_params),
      class: "link-secondary link-underline-opacity-0 link-underline-opacity-100-hover" do %>
  ...
<% end %>
```

（`/admin/current_store` はPOSTなのでここでは気にしなくてOK）

---

### 2) `app/controllers/admin/current_stores_controller.rb`

`safe_admin_return_to` を **`safe_return_to` に置き換え**、`/` 始まりの相対パスを許可します。ついでにループ防止で `/admin/stores` と `/admin/current_store` は拒否推奨です。

例（差し替え）：

```ruby
def safe_return_to(value)
  s = value.to_s
  return nil if s.blank?

  # 相対パスのみ許可（open redirect対策）
  return nil unless s.start_with?("/")
  return nil if s.start_with?("//")
  return nil if s.include?("\n") || s.include?("\r")
  return nil if s.include?("\0")

  # ループ/ノイズ防止：選択画面と選択POSTには戻さない
  return nil if s == "/admin/stores"
  return nil if s == "/admin/current_store"

  s
end
```

そして `resolve_redirect_path` 側で呼んでいる `safe_admin_return_to(...)` を `safe_return_to(...)` に変更します（params と session の両方）。

---

## これで直る動作

* HOME（`/`）で store 名をクリック → `/admin/stores?return_to=/` へ
* 店舗選択 → `return_to` が `/` として安全判定を通る → HOMEに戻る（store文脈は切替済み）

---

もし「やっぱり admin 以外は戻さず dashboard に戻したい」なら、いまの動作は正常なので、ヘッダーのリンク自体を adminページにだけ出す（または文言を変える）方向になります。
ただ、UX的には今回の“相対パス許可”が一番自然で、セキュリティも保てます。

---
---

了解。A（`/admin/stores` にいるときは、選択後は dashboard に戻す）が一番スッキリです。

やることは **「session[:admin_return_to] を使う前に、選択画面から来たPOSTなら session を無視する」**の1点だけ。

## 変更箇所

`app/controllers/admin/current_stores_controller.rb`

`resolve_redirect_path` の「③ session」を使う直前に、これを入れてください：

* **`request.referer` が `/admin/stores` なら session を使わず dashboard に落とす**

### コピペ用（該当メソッド差し替え）

※あなたのファイルにある `resolve_redirect_path` をこの形に合わせてください（他のロジックは維持）。

```ruby
def resolve_redirect_path(store_id)
  store = Store.find_by(id: store_id)

  # 1) return_to_key
  key = params[:return_to_key].presence
  if key.present?
    path = resolve_return_to_key(key, store)
    return path if path.present?
  end

  # 2) return_to
  rt = safe_return_to(params[:return_to])
  return rt if rt.present?

  # ★A: 選択画面（/admin/stores）からのPOSTは session 戻りを使わない
  if request.referer.to_s.start_with?(admin_stores_url)
    return dashboard_path
  end

  # 3) session（直前ページ）
  session_rt = safe_return_to(session[:admin_return_to])
  return session_rt if session_rt.present?

  # 4) fallback
  dashboard_path
end
```

### ポイント

* `admin_stores_url` を使うので host 付きで比較できます（`http://www.example.com/admin/stores?...` を含む referer でも吸収）。
* ここで `dashboard_path` に落とすので、「店舗選択画面でさらに選ぶ」時に “sessionっぽい挙動” は消えます。
* それ以外（店舗選択画面以外）では、従来通り `return_to` が最優先で効きます。

これで期待している UX になるはず。もし「`/admin/stores?return_to=...` から来た場合」も dashboard にしたい（=常に選択画面起点は dashboard）なら、このままでOKです。

