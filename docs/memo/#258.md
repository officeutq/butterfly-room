了解、GO なので **実装プラン確定＋コピペ用コード一括**で出します。
（方針：#257 と同じく **StoreLedgerEntry.occurred_at をSSOT**、carryoverは**表示のみ**、期間重複は **UI事前チェック＋DB EXCLUDE**、監査は **Rails.logger**。）

---

## 実装プラン（最小差分）

1. **routes** に system_admin/manual settlement 用の3ルートを追加

* GET `/system_admin/settlements/manual/new`（入力画面）
* POST `/system_admin/settlements/manual/preview`（プレビュー）
* POST `/system_admin/settlements/manual`（confirmed 作成）

2. **SystemAdmin::SettlementsController** 新規

* `new_manual`：空フォーム
* `preview_manual`：入力を検証し、集計してプレビュー表示
* `create_manual`：最終確定（Settlement作成）、重複や競合を親切メッセージで返す

3. **サービス2つ**（#257 の計算ルールと揃える）

* `Settlements::ManualPreviewService`：gross/share/fee + carryover（表示のみ）
* `Settlements::ManualCreateService`：Settlement(kind: manual, status: confirmed) を作る、監査ログ

4. **フォームオブジェクト**（コントローラ肥大化防止）

* `SystemAdmin::ManualSettlementForm`（ActiveModel）

  * JSTで parse
  * `period_from < period_to`
  * 期間重複チェック（既存区間を見せる）

5. **view**：`users/_form` の流儀でエラー表示（alert-danger + ul）

* preview結果を同じ画面に出し、confirmボタンで create に進む

6. **dashboard 露出**：system_admin セクションにだけリンク追加
   （共通footer等には出さない＝「通常導線に露出しない」を満たす）

7. **テスト**（integration）

* system_admin以外アクセス不可
* 不正期間を弾く
* 重複を弾く
* 金額計算が一致
* confirmed作成後に同期間で再作成不可

---

## ブランチ名（おすすめ）

* `feature/258-system-admin-manual-settlement`

---

# コピペ用コード

## 1) `config/routes.rb`（追記）

`namespace :system_admin do ... end` の中に追加してください（既存2resourcesの下でOK）。

```ruby
  # --- System Admin ---
  namespace :system_admin do
    resources :referral_codes, only: %i[index new create edit update]
    resources :users, only: %i[index new create edit update destroy]

    resources :settlements, only: [] do
      collection do
        get  "manual/new",     to: "settlements#new_manual",     as: :new_manual
        post "manual/preview", to: "settlements#preview_manual", as: :preview_manual
        post "manual",         to: "settlements#create_manual",  as: :create_manual
      end
    end
  end
```

---

## 2) `app/controllers/system_admin/settlements_controller.rb`（新規）

```ruby
# frozen_string_literal: true

module SystemAdmin
  class SettlementsController < SystemAdmin::BaseController
    def new_manual
      @form = ManualSettlementForm.new
      @preview = nil
    end

    def preview_manual
      @form = ManualSettlementForm.new(form_params)

      if @form.valid?
        @preview = Settlements::ManualPreviewService.new(
          store_id: @form.store_id,
          period_from: @form.period_from,
          period_to: @form.period_to
        ).call
        render :new_manual
      else
        @preview = nil
        render :new_manual, status: :unprocessable_entity
      end
    end

    def create_manual
      @form = ManualSettlementForm.new(form_params)

      unless @form.valid?
        @preview = nil
        render :new_manual, status: :unprocessable_entity
        return
      end

      result = Settlements::ManualCreateService.new(
        store_id: @form.store_id,
        period_from: @form.period_from,
        period_to: @form.period_to,
        actor_user: current_user
      ).call

      if result[:ok]
        redirect_to dashboard_path, notice: "マニュアル精算を作成しました（confirmed）"
        return
      end

      @preview = Settlements::ManualPreviewService.new(
        store_id: @form.store_id,
        period_from: @form.period_from,
        period_to: @form.period_to
      ).call

      @form.errors.add(:base, result[:message])
      render :new_manual, status: :unprocessable_entity
    end

    private

    def form_params
      params.require(:manual_settlement).permit(:store_id, :period_from, :period_to)
    end
  end
end
```

---

## 3) `app/forms/system_admin/manual_settlement_form.rb`（新規）

※ `app/forms` ディレクトリが無ければ作ってOKです（Railsはautoloadされます）。

```ruby
# frozen_string_literal: true

module SystemAdmin
  class ManualSettlementForm
    include ActiveModel::Model
    include ActiveModel::Attributes

    ZONE = "Asia/Tokyo"

    attribute :store_id, :integer
    attribute :period_from, :datetime
    attribute :period_to, :datetime

    validates :store_id, presence: true
    validates :period_from, presence: true
    validates :period_to, presence: true
    validate :period_range_validation
    validate :overlap_validation

    def period_from=(value)
      super(parse_time(value))
    end

    def period_to=(value)
      super(parse_time(value))
    end

    private

    def parse_time(value)
      return value if value.is_a?(Time) || value.is_a?(ActiveSupport::TimeWithZone)
      return nil if value.blank?

      Time.use_zone(ZONE) { Time.zone.parse(value.to_s) }
    end

    def period_range_validation
      return if period_from.blank? || period_to.blank?

      errors.add(:period_to, "は period_from より後にしてください") unless period_from < period_to
    end

    def overlap_validation
      return if store_id.blank? || period_from.blank? || period_to.blank?

      existing =
        Settlement
          .where(store_id: store_id)
          .where("tsrange(period_from, period_to) && tsrange(?, ?)", period_from, period_to)
          .order(period_from: :asc)
          .first

      return if existing.blank?

      from_str = existing.period_from.in_time_zone(ZONE).strftime("%Y-%m-%d %H:%M")
      to_str   = existing.period_to.in_time_zone(ZONE).strftime("%Y-%m-%d %H:%M")
      errors.add(:base, "この期間は既に精算済みです（#{from_str}..#{to_str}）")
    end
  end
end
```

---

## 4) `app/services/settlements/manual_preview_service.rb`（新規）

```ruby
# frozen_string_literal: true

module Settlements
  class ManualPreviewService
    ZONE = "Asia/Tokyo"
    SHARE_RATE = BigDecimal("0.7")

    def initialize(store_id:, period_from:, period_to:)
      @store_id = store_id
      @period_from = period_from
      @period_to = period_to
    end

    def call
      gross_yen =
        StoreLedgerEntry
          .where(store_id: @store_id, occurred_at: @period_from...@period_to)
          .sum(:points)
          .to_i

      store_share_yen = (BigDecimal(gross_yen) * SHARE_RATE).floor(0).to_i
      platform_fee_yen = gross_yen - store_share_yen

      carryover_yen =
        SettlementCarryover
          .where(store_id: @store_id)
          .sum(:amount_yen)
          .to_i

      {
        period_from: @period_from,
        period_to: @period_to,
        gross_yen: gross_yen,
        store_share_yen: store_share_yen,
        platform_fee_yen: platform_fee_yen,
        carryover_yen: carryover_yen
      }
    end
  end
end
```

---

## 5) `app/services/settlements/manual_create_service.rb`（新規）

```ruby
# frozen_string_literal: true

module Settlements
  class ManualCreateService
    ZONE = "Asia/Tokyo"

    def initialize(store_id:, period_from:, period_to:, actor_user:, logger: Rails.logger)
      @store_id = store_id
      @period_from = period_from
      @period_to = period_to
      @actor_user = actor_user
      @logger = logger
    end

    def call
      # 親切エラー（事前チェック）
      existing =
        Settlement
          .where(store_id: @store_id)
          .where("tsrange(period_from, period_to) && tsrange(?, ?)", @period_from, @period_to)
          .order(period_from: :asc)
          .first

      if existing.present?
        from_str = existing.period_from.in_time_zone(ZONE).strftime("%Y-%m-%d %H:%M")
        to_str   = existing.period_to.in_time_zone(ZONE).strftime("%Y-%m-%d %H:%M")
        return { ok: false, message: "この期間は既に精算済みです（#{from_str}..#{to_str}）" }
      end

      preview =
        ManualPreviewService.new(
          store_id: @store_id,
          period_from: @period_from,
          period_to: @period_to
        ).call

      settlement =
        Settlement.create!(
          store_id: @store_id,
          kind: :manual,
          status: :confirmed,
          confirmed_at: Time.use_zone(ZONE) { Time.zone.now },
          period_from: @period_from,
          period_to: @period_to,
          gross_yen: preview[:gross_yen],
          store_share_yen: preview[:store_share_yen],
          platform_fee_yen: preview[:platform_fee_yen]
        )

      @logger.info(
        "[ManualSettlement] created user_id=#{@actor_user.id} store_id=#{@store_id} " \
        "period=#{@period_from}..#{@period_to} gross=#{preview[:gross_yen]} " \
        "share=#{preview[:store_share_yen]} fee=#{preview[:platform_fee_yen]}"
      )

      { ok: true, settlement: settlement }
    rescue ActiveRecord::RecordNotUnique
      { ok: false, message: "この期間は既に精算済みです（同一期間の精算が存在します）" }
    rescue ActiveRecord::StatementInvalid => e
      # EXCLUDE制約 (PG::ExclusionViolation) など
      if e.cause&.class&.name.to_s.include?("ExclusionViolation")
        { ok: false, message: "この期間は既に精算済みです（期間が重複しています）" }
      else
        raise
      end
    end
  end
end
```

---

## 6) `app/views/system_admin/settlements/new_manual.html.erb`（新規）

Usersフォームの型に揃えています（alert-danger + ul）。

```erb
<h1>マニュアル精算（system_admin）</h1>

<div class="alert alert-secondary" role="alert">
  テスト期間のみ使用できます。JST固定、期間は <code>[from, to)</code>（from含む / to含まない）です。
</div>

<%= form_with scope: :manual_settlement,
      url: preview_manual_system_admin_settlements_path,
      method: :post,
      data: { turbo: false } do |f| %>
  <% if @form.errors.any? %>
    <div class="alert alert-danger">
      <ul class="mb-0">
        <% @form.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= f.label :store_id, "対象店舗", class: "form-label" %>
    <%= f.select :store_id,
          Store.order(id: :desc).map { |s| [s.name, s.id] },
          { include_blank: "選択してください" },
          class: "form-select",
          required: true %>
  </div>

  <div class="row g-3">
    <div class="col-md-6">
      <%= f.label :period_from, "period_from（JST）", class: "form-label" %>
      <%= f.datetime_field :period_from, class: "form-control", required: true %>
      <div class="form-text">from は含みます</div>
    </div>

    <div class="col-md-6">
      <%= f.label :period_to, "period_to（JST）", class: "form-label" %>
      <%= f.datetime_field :period_to, class: "form-control", required: true %>
      <div class="form-text">to は含みません</div>
    </div>
  </div>

  <div class="mt-3 d-flex gap-2">
    <%= f.submit "プレビュー", class: "btn btn-primary" %>
    <%= link_to "ダッシュボードへ戻る", dashboard_path, class: "btn btn-outline-secondary" %>
  </div>
<% end %>

<% if @preview.present? %>
  <hr class="my-4">

  <h2 class="h5 mb-3">プレビュー</h2>

  <table class="table table-sm">
    <tbody>
      <tr>
        <th style="width: 220px;">対象期間（JST）</th>
        <td>
          <%= @preview[:period_from].in_time_zone("Asia/Tokyo").strftime("%Y-%m-%d %H:%M") %>
          〜
          <%= @preview[:period_to].in_time_zone("Asia/Tokyo").strftime("%Y-%m-%d %H:%M") %>
          （toは含まない）
        </td>
      </tr>
      <tr>
        <th>gross_yen</th>
        <td><%= @preview[:gross_yen] %></td>
      </tr>
      <tr>
        <th>store_share_yen（gross×0.7 floor）</th>
        <td><%= @preview[:store_share_yen] %></td>
      </tr>
      <tr>
        <th>platform_fee_yen</th>
        <td><%= @preview[:platform_fee_yen] %></td>
      </tr>
      <tr>
        <th>carryover_yen（表示のみ）</th>
        <td><%= @preview[:carryover_yen] %></td>
      </tr>
    </tbody>
  </table>

  <div class="alert alert-warning" role="alert">
    この画面の「確定」は settlement 台帳を作成するだけです。支払や残高表示は扱いません。
  </div>

  <%= form_with scope: :manual_settlement,
        url: create_manual_system_admin_settlements_path,
        method: :post,
        data: { turbo: false } do |f| %>
    <%= f.hidden_field :store_id, value: @form.store_id %>
    <%= f.hidden_field :period_from, value: @form.period_from %>
    <%= f.hidden_field :period_to, value: @form.period_to %>

    <div class="d-flex gap-2">
      <%= f.submit "確定（confirmedで作成）", class: "btn btn-danger", data: { turbo_confirm: "この内容で精算を確定します。よろしいですか？" } %>
      <%= link_to "キャンセル", dashboard_path, class: "btn btn-outline-secondary" %>
    </div>
  <% end %>
<% end %>
```

> ルーティングヘルパを使いたい場合は、上の `url: "/system_admin/settlements/manual/preview"` 等を、生成された named route に置き換えます（命名が少し読みにくいので、まずは最小で固定パスにしています）。

---

## 7) `app/views/dashboard/show.html.erb`（追記）

system_admin セクションの list-group に 1行追加（露出はここだけ）。

```erb
      <li class="list-group-item">
        <%= link_to "マニュアル精算（テスト）", "/system_admin/settlements/manual/new", class: "text-decoration-none" %>
      </li>
```

（named route を使うなら後で差し替えOK。まずは最小差分。）

---

## 8) テスト `test/integration/system_admin_manual_settlements_test.rb`（新規）

```ruby
# frozen_string_literal: true

require "test_helper"

class SystemAdminManualSettlementsTest < ActionDispatch::IntegrationTest
  include ActiveSupport::Testing::TimeHelpers

  setup do
    @store = Store.create!(name: "Test Store")
    @system_admin = User.create!(email: "sa@example.com", password: "password", role: :system_admin)
    @store_admin  = User.create!(email: "sta@example.com", password: "password", role: :store_admin)
  end

  test "system_admin can access new" do
    sign_in @system_admin, scope: :user
    get "/system_admin/settlements/manual/new"
    assert_response :success
    assert_includes response.body, "マニュアル精算"
  end

  test "non system_admin cannot access" do
    sign_in @store_admin, scope: :user
    get "/system_admin/settlements/manual/new"
    assert_response :forbidden
  end

  test "period_from >= period_to is rejected" do
    sign_in @system_admin, scope: :user

    post "/system_admin/settlements/manual/preview", params: {
      manual_settlement: {
        store_id: @store.id,
        period_from: "2026-02-10 00:00",
        period_to: "2026-02-10 00:00"
      }
    }

    assert_response :unprocessable_entity
    assert_includes response.body, "period_from"
  end

  test "preview calculation matches rules (1pt=1yen, 0.7 floor)" do
    sign_in @system_admin, scope: :user

    travel_to Time.zone.parse("2026-02-01 00:00") do
      booth = Booth.create!(store: @store, name: "B", status: :offline)
      cast = User.create!(email: "cast@example.com", password: "password", role: :cast)
      ss = StreamSession.create!(store: @store, booth: booth, status: 0, started_at: Time.current, started_by_cast_user: cast)

      customer = User.create!(email: "c@example.com", password: "password", role: :customer)
      item = DrinkItem.create!(store: @store, name: "D", price_points: 100, position: 0, enabled: true)

      order1 = DrinkOrder.create!(store: @store, booth: booth, stream_session: ss, customer_user: customer, drink_item: item, status: :consumed)
      order2 = DrinkOrder.create!(store: @store, booth: booth, stream_session: ss, customer_user: customer, drink_item: item, status: :consumed)

      StoreLedgerEntry.create!(store: @store, stream_session: ss, drink_order: order1, points: 101, occurred_at: Time.zone.parse("2026-02-05 12:00"))
      StoreLedgerEntry.create!(store: @store, stream_session: ss, drink_order: order2, points: 102, occurred_at: Time.zone.parse("2026-02-05 13:00"))
    end

    post "/system_admin/settlements/manual/preview", params: {
      manual_settlement: {
        store_id: @store.id,
        period_from: "2026-02-01 00:00",
        period_to: "2026-03-01 00:00"
      }
    }

    assert_response :success
    gross = 203
    share = (BigDecimal(gross) * BigDecimal("0.7")).floor(0).to_i
    fee = gross - share

    assert_includes response.body, "gross_yen"
    assert_includes response.body, gross.to_s
    assert_includes response.body, share.to_s
    assert_includes response.body, fee.to_s
  end

  test "cannot create overlapping manual settlement" do
    sign_in @system_admin, scope: :user

    Settlement.create!(
      store: @store,
      kind: :manual,
      status: :confirmed,
      confirmed_at: Time.zone.parse("2026-02-01 00:00"),
      period_from: Time.zone.parse("2026-02-01 00:00"),
      period_to: Time.zone.parse("2026-03-01 00:00"),
      gross_yen: 0,
      store_share_yen: 0,
      platform_fee_yen: 0
    )

    post "/system_admin/settlements/manual/preview", params: {
      manual_settlement: {
        store_id: @store.id,
        period_from: "2026-02-10 00:00",
        period_to: "2026-02-20 00:00"
      }
    }

    assert_response :unprocessable_entity
    assert_includes response.body, "既に精算済み"
  end
end
```

> `sign_in` が未定義で落ちる場合：`test/test_helper.rb` に `include Devise::Test::IntegrationHelpers` が入ってない可能性があります。その場合は追加します（次の貼り付けで対応できます）。

---

## 実行コマンド（あなたの方針に合わせて docker compose 経由）

```bash
docker compose exec app bin/rails test
```

---

## 補足：DBマイグレーションは不要

* 期間重複の DB制約（EXCLUDE）も `period_from < period_to` も既にあるため、#258では追加不要です。
* 監査は要件上「ログでも可」なので、`created_by_user_id` 等は **今回は増やしません**（最小差分）。

---

必要なら次に、**コミットメッセージ案 / PR本文（変更点・仕様・動作確認）**も #258 向けに出します。
