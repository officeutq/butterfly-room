以下、Issue #178 を **“最小で正しい”**で完了させるための実装一式（計画 → ブランチ案 → コピペ用コード → テスト → コミット/PR文）です。

---

## (1) 実装方針（高レベル / コードなし）

* `GET /stores/:id` を追加（**customer以上ログイン必須**）
* Store詳細で表示する最小要素

  * store名
  * booth一覧（`archived_at` があるものは除外 = `Booth.active`）
  * boothごとの cast（`booth.cast_users`）
  * booth視聴への導線（`booth_path(booth)`）
  * （任意扱い）booth.status の簡易バッジ（offline/live/away/standby）
  * （将来 #176 用）お気に入りボタン領域の **wrapperだけ**用意（中身は stub）
* BAN は `StoreBanGuard` を流用し、Store詳細でも同様に弾く

---

## (2) ブランチ名候補（最終）

* `feature/178-store-detail-page`
* `feature/178-store-public-show`
* `feature/178-customer-store-show`

---

## (3) 変更ファイル一覧

* `config/routes.rb`（stores#show 追加）
* `app/controllers/stores_controller.rb`（新規）
* `app/views/stores/show.html.erb`（新規）
* `test/integration/store_show_test.rb`（新規）

---

## (4) コピペ用コード

### 4.1 `config/routes.rb`（追記）

`namespace :stores do ... end` の **直後**にこれを追加してください（順番重要：`/stores/new_registration` が show に吸われないようにするため）。

```ruby
  # --- Public (store registration) ---
  namespace :stores do
    get  :new_registration, to: "registrations#new"
    post :registrations,    to: "registrations#create"
  end

  # --- Customer (login required) ---
  resources :stores, only: %i[show]
```

> 既存の `# --- Public (customer) --- resources :booths` の前に置けばOKです。

---

### 4.2 `app/controllers/stores_controller.rb`（新規作成）

```ruby
# frozen_string_literal: true

class StoresController < ApplicationController
  include StoreBanGuard

  before_action -> { require_at_least!(:customer) }
  before_action :set_store, only: %i[show]
  before_action :reject_banned_customer_for_store!, only: %i[show]

  def show
    @booths =
      @store
        .booths
        .active
        .includes(:cast_users)
        .order(:id)
  end

  private

  def set_store
    @store = Store.find(params[:id])
  end

  def reject_banned_customer_for_store!
    reject_banned_customer!(store: @store)
  end
end
```

---

### 4.3 `app/views/stores/show.html.erb`（新規作成）

```erb
<div class="container mt-4" style="max-width: 960px;">
  <div class="d-flex justify-content-between align-items-start gap-3">
    <div>
      <h1 class="mb-1"><%= @store.name %></h1>
      <p class="text-muted mb-0">店舗詳細</p>
    </div>

    <%# #176 で Turbo 置換する前提の “置き場” だけ用意 %>
    <div id="store_favorite_button">
      <span class="badge text-bg-secondary">お気に入り（準備中）</span>
    </div>
  </div>

  <hr class="my-4">

  <h2 class="h5 mb-3">ブース一覧</h2>

  <% if @booths.blank? %>
    <div class="alert alert-warning" role="alert">
      公開中のブースがありません。
    </div>
  <% else %>
    <div class="list-group">
      <% @booths.each do |booth| %>
        <div class="list-group-item">
          <div class="d-flex justify-content-between align-items-center gap-3">
            <div class="flex-grow-1">
              <div class="d-flex align-items-center gap-2">
                <strong><%= booth.name %></strong>

                <%# 任意：状態バッジ（最小） %>
                <% if booth.live? %>
                  <span class="badge text-bg-danger">LIVE</span>
                <% elsif booth.standby? %>
                  <span class="badge text-bg-success">STANDBY</span>
                <% elsif booth.away? %>
                  <span class="badge text-bg-warning">AWAY</span>
                <% else %>
                  <span class="badge text-bg-secondary">OFFLINE</span>
                <% end %>
              </div>

              <% casts = booth.cast_users.to_a %>
              <% if casts.present? %>
                <div class="text-muted small mt-1">
                  cast:
                  <%= casts.map { |u| u.display_name.presence || u.email }.join(" / ") %>
                </div>
              <% else %>
                <div class="text-muted small mt-1">cast: (未設定)</div>
              <% end %>
            </div>

            <div class="text-end">
              <%= link_to "視聴ページへ", booth_path(booth), class: "btn btn-outline-primary btn-sm" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>

  <div class="mt-4">
    <%= link_to "戻る", root_path, class: "btn btn-outline-secondary" %>
  </div>
</div>
```

---

### 4.4 `test/integration/store_show_test.rb`（新規作成）

fixtures無しの流儀に合わせ、`create!` で組み立てます。

```ruby
# frozen_string_literal: true

require "test_helper"

class StoreShowTest < ActionDispatch::IntegrationTest
  test "guest is redirected to login" do
    store = Store.create!(name: "store")

    get store_path(store)
    assert_response :redirect
    assert_redirected_to new_user_session_path
  end

  test "customer can view store show and see active booths only" do
    store = Store.create!(name: "store")
    active_booth = Booth.create!(store: store, name: "active", status: :offline)
    Booth.create!(store: store, name: "archived", status: :offline, archived_at: Time.current)

    cast = User.create!(email: "cast@example.com", password: "password", role: :cast, display_name: "Cast A")
    BoothCast.create!(booth: active_booth, cast_user: cast)

    customer = User.create!(email: "customer@example.com", password: "password", role: :customer)
    sign_in customer

    get store_path(store)
    assert_response :success

    assert_includes @response.body, "store"
    assert_includes @response.body, "active"
    assert_includes @response.body, "Cast A"
    assert_includes @response.body, booth_path(active_booth)

    refute_includes @response.body, "archived"
  end
end
```

---

## (5) 動作確認コマンド（docker compose 経由）

```bash
docker compose exec app bash -lc "RAILS_ENV=test bin/rails test test/integration/store_show_test.rb"
docker compose exec app bash -lc "RAILS_ENV=test bin/rails test"
```

ブラウザ確認（ログイン後）：

* `GET /stores/:id` で store詳細表示
* boothリンクから `booths#show` に遷移できる
* archived booth が一覧に出ない

---

## (6) コミットメッセージ案

* `feat: add customer store show page (booths + casts, excludes archived)`

---

## (7) PR文（コピペ用）

**Title**
`[Feature] Store詳細ページ（customer向け：お気に入り対象の表示） #178`

**概要**
customer の回遊/お気に入り機能の前提として、Store詳細ページ（/stores/:id）を追加。
Store名・ブース一覧（archived除外）・cast 表示・視聴ページへの導線を最小構成で提供する。

**仕様変更 / 方針メモ**

* Issue本文にあった「public」は現状方針（未ログインはログイン系以外不可）に合わせ、**customer以上ログイン必須**として実装。

**実装内容**

* `GET /stores/:id` を追加（`StoresController#show`）

  * `require_at_least!(:customer)` でガード
  * `StoreBanGuard` を適用（BAN時は既存同様にリダイレクト）
* `app/views/stores/show.html.erb` を追加

  * store名
  * booth一覧（`Booth.active`）
  * boothごとの cast 表示（`booth.cast_users`）
  * booth視聴へのリンク（`booths#show`）
  * （任意）booth.status の簡易バッジ
  * お気に入りボタン領域の wrapper（#176 で置換予定）

**テスト**

* Integration test を追加

  * ゲストはログイン画面へリダイレクト
  * customer は store詳細を閲覧でき、archived booth は表示されない

**動作確認**

* `bin/rails test`
* ログイン後に `/stores/:id` 表示、boothへ遷移、archived除外を確認

---

このまま実装していけば #178 の Done（表示できる / booth遷移 / テスト通過）は満たせます。
もしこの後「お気に入りボタンを本当に置く（Turboで置換する）」に進めるなら、#176 側で `store_favorite_button` を `turbo_stream.replace` する形が一番事故りません。
