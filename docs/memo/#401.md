以下の方針で進めれば、そのまま Issue #401 のスコープに収まります。

## 実装プラン

1. `system_admin/referral_codes#index` の一覧をテーブルからカード一覧へ変更
2. 各カードで紹介URLを `stores_new_registration_url(ref: rc.code)` で表示
3. 既存 `clipboard_controller` を後方互換のまま拡張し、成功文言を個別指定可能にする
4. カード用の最小限のスタイルを `_layout.scss` に追加
5. 「ダッシュボードへ戻る」ボタンを削除
6. 既存の status badge / 編集 / 有効化・無効化導線は維持

---

## 変更対象ファイル

* `app/views/system_admin/referral_codes/index.html.erb`
* `app/javascript/controllers/clipboard_controller.js`
* `app/assets/stylesheets/components/_layout.scss`

---

## 1. app/views/system_admin/referral_codes/index.html.erb

```erb
<h1>紹介コード管理</h1>

<% if @referral_codes.any? %>
  <div class="referral-codes-list">
    <% @referral_codes.each do |rc| %>
      <% referral_url = stores_new_registration_url(ref: rc.code) %>

      <div class="card referral-code-card <%= "text-muted" if !rc.enabled? || rc.expired? %>">
        <div class="card-body">
          <div class="referral-code-row">
            <div class="referral-code-label">code</div>
            <div class="referral-code-value fw-semibold"><%= rc.code %></div>

            <div class="referral-code-inline-actions">
              <%= link_to "編集", edit_system_admin_referral_code_path(rc), class: "btn btn-outline-secondary btn-sm" %>

              <% if rc.enabled? %>
                <%= button_to "無効化",
                      system_admin_referral_code_path(rc),
                      method: :patch,
                      params: { referral_code: { enabled: false } },
                      class: "btn btn-outline-danger btn-sm",
                      data: { turbo_confirm: "この紹介コードを無効化します。よろしいですか？" } %>
              <% else %>
                <%= button_to "有効化",
                      system_admin_referral_code_path(rc),
                      method: :patch,
                      params: { referral_code: { enabled: true } },
                      class: "btn btn-outline-success btn-sm" %>
              <% end %>
            </div>
          </div>

          <div class="referral-code-row">
            <div class="referral-code-label">label</div>
            <div class="referral-code-value"><%= rc.label.presence || "-" %></div>
          </div>

          <div class="referral-code-row">
            <div class="referral-code-label">紹介URL</div>

            <div class="referral-code-value">
              <div class="referral-code-url-group">
                <code class="referral-code-url-inline"><%= referral_url %></code>

                <button
                  type="button"
                  class="btn btn-outline-secondary btn-sm referral-code-copy-btn"
                  data-controller="clipboard"
                  data-action="click->clipboard#copy"
                  data-clipboard-text="<%= referral_url %>"
                  data-clipboard-success-message="紹介URLをコピーしました"
                  data-clipboard-failure-message="URLのコピーに失敗しました"
                  aria-label="紹介URLをコピー"
                  title="紹介URLをコピー"
                >
                  <i class="bi bi-copy" aria-hidden="true"></i>
                </button>
              </div>
            </div>
          </div>

          <div class="referral-code-row">
            <div class="referral-code-label">status</div>
            <div class="referral-code-value">
              <div class="d-flex flex-wrap gap-2">
                <% if rc.expired? %>
                  <span class="badge text-bg-warning">expired</span>
                <% end %>

                <% if rc.enabled? %>
                  <span class="badge text-bg-success">enabled</span>
                <% else %>
                  <span class="badge text-bg-secondary">disabled</span>
                <% end %>

                <% unless rc.usable? %>
                  <span class="badge text-bg-danger">登録不可</span>
                <% end %>
              </div>
            </div>
          </div>

          <div class="referral-code-row">
            <div class="referral-code-label">expires_at</div>
            <div class="referral-code-value">
              <%= rc.expires_at.present? ? l(rc.expires_at) : "-" %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
<% else %>
  <div class="alert alert-secondary">
    紹介コードはまだ作成されていません。
  </div>
<% end %>

<div class="mt-3">
  <%= link_to "新規作成", new_system_admin_referral_code_path, class: "btn btn-primary" %>
</div>
```

---

## 2. app/javascript/controllers/clipboard_controller.js

```javascript
import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  async copy(event) {
    event.preventDefault()

    const text = this.element.dataset.clipboardText
    if (!text) return

    const successMessage = this.element.dataset.clipboardSuccessMessage || "コピーしました"
    const failureMessage = this.element.dataset.clipboardFailureMessage || "コピー失敗"

    this.element.disabled = true

    try {
      if (navigator.clipboard && navigator.clipboard.writeText) {
        await navigator.clipboard.writeText(text)
      } else {
        const ta = document.createElement("textarea")
        ta.value = text
        ta.setAttribute("readonly", "")
        ta.style.position = "absolute"
        ta.style.left = "-9999px"
        document.body.appendChild(ta)
        ta.select()
        document.execCommand("copy")
        document.body.removeChild(ta)
      }

      this.showFlash("success", successMessage)
    } catch (_e) {
      this.showFlash("danger", failureMessage)
    } finally {
      window.setTimeout(() => {
        this.element.disabled = false
      }, 300)
    }
  }

  showFlash(level, message) {
    const flashInner = document.getElementById("flash_inner")
    if (!flashInner) return

    const wrapper = document.createElement("div")
    wrapper.innerHTML = `
      <div class="alert alert-${level} alert-dismissible fade show" role="alert">
        ${this.escapeHtml(message)}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="閉じる"></button>
      </div>
    `.trim()

    const flashElement = wrapper.firstElementChild
    flashInner.replaceChildren(flashElement)
  }

  escapeHtml(value) {
    const div = document.createElement("div")
    div.textContent = value
    return div.innerHTML
  }
}
```

---

## 3. app/assets/stylesheets/components/_layout.scss

```scss
.header-logo {
  height: 48px;
  width: auto;
  display: block;
}

.app-header {
  display: flex;
}

.app-header-inner {
  margin: 0 auto;
}

.referral-codes-list {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(380px, 1fr));
  gap: 1rem;
  align-items: start;
}

.referral-code-card {
  height: 100%;
  background-color: #fff;
  color: #212529;
  border-color: rgba(0, 0, 0, 0.125);
}

.referral-code-card.text-muted {
  color: #6c757d !important;
}

.referral-code-row {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  padding: 0.5rem 0;
  border-bottom: 1px solid rgba(var(--bs-secondary-rgb), 0.2);
  min-width: 0;
}

.referral-code-row:last-child {
  border-bottom: 0;
  padding-bottom: 0;
}

.referral-code-label {
  width: 92px;
  min-width: 92px;
  color: var(--bs-secondary-color);
  font-size: 0.875rem;
}

.referral-code-card .referral-code-label {
  color: #6c757d;
}

.referral-code-value {
  min-width: 0;
  flex: 1 1 auto;
}

.referral-code-inline-actions {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  margin-left: auto;
  flex-wrap: wrap;
}

.referral-code-inline-actions .button_to {
  margin: 0;
}

.referral-code-url-group {
  display: flex;
  align-items: stretch;
  width: 100%;
  min-width: 0;
}

.referral-code-url-inline {
  display: block;
  flex: 1 1 auto;
  min-width: 0;
  margin: 0;
  padding: 0.5rem 0.75rem;
  border-radius: 0.375rem 0 0 0.375rem;
  background: rgba(var(--bs-secondary-rgb), 0.15);
  white-space: nowrap;
  overflow-x: auto;
}

.referral-code-card .referral-code-url-inline {
  background: #f8f9fa;
  color: #212529;
}

.referral-code-copy-btn {
  border-top-left-radius: 0;
  border-bottom-left-radius: 0;
  flex-shrink: 0;
}

@media (max-width: 991.98px) {
  .referral-codes-list {
    grid-template-columns: 1fr;
  }

  .referral-code-label {
    width: 84px;
    min-width: 84px;
  }
}

@media (max-width: 575.98px) {
  .referral-code-row {
    gap: 0.5rem;
  }

  .referral-code-label {
    width: 76px;
    min-width: 76px;
    font-size: 0.8125rem;
  }

  .referral-code-inline-actions {
    gap: 0.375rem;
  }

  .referral-code-inline-actions .btn,
  .referral-code-inline-actions .button_to .btn {
    padding: 0.25rem 0.5rem;
  }
}
```

---

## 補足

今回の実装では、URL は `stores_new_registration_url(ref: rc.code)` を使っています。
`_path` ではなく `_url` にしているので、一覧上にフルURLをそのまま表示・コピーできます。

この形なら Issue 本文の完了条件は一通り満たせます。
