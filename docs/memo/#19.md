## 0) 現状から見た実装方針（結論）

* **wallets**：`available_points/reserved_points` + CHECK + unique(customer_user_id) ✅
* **wallet_transactions**：`kind: hold` を記録できる ✅
* **drink_items**：`store_id/price_points/enabled` ✅
* **drink_orders**：`status: pending/consumed/refunded` + FIFO index ✅
* **store_bans**：store×customer のユニーク ✅
* **routes**：`StreamSessions::DrinkOrdersController#create` のルートが既にある ✅

なので、**実装は以下の3点が中心**：

1. `Wallets::HoldService`
2. `DrinkOrders::CreateService`（hold + pending作成 + wallet_transactions記録）
3. `StreamSessions::DrinkOrdersController#create`（402などのHTTP返却）
4. `DrinkOrderNotifier`（キャスト側 pending 一覧 replace）

---

## 1) 実装順（この順でやると安全）

1. **モデルに最低限の関連を足す（任意だけどおすすめ）**
2. `Wallets::HoldService` を作る（ロック・例外）
3. `DrinkOrders::CreateService` を作る（transactionで hold + drink_order + wallet_transaction）
4. `StreamSessions::DrinkOrdersController#create` を作る（例外→HTTP変換）
5. `DrinkOrderNotifier.replace_pending_list` を作る（Turbo Streams replace）
6. キャスト画面側に `id="cast_pending_drink_orders"` を用意し、部分テンプレを切る

---

## 2) 影響ファイル一覧（ほぼ確定）

### 追加

* `app/services/wallets/hold_service.rb`
* `app/services/drink_orders/create_service.rb`
* `app/controllers/stream_sessions/drink_orders_controller.rb`
* `app/notifiers/drink_order_notifier.rb`
* `app/views/cast/stream_sessions/_pending_drink_orders.html.erb`（replace用のpartial）
* （必要なら）`app/views/stream_sessions/drink_orders/create.turbo_stream.erb`（ただ今回は notifier で十分）
* `app/models/wallet.rb`

### 変更（任意だけど推奨）

* `app/models/wallet_transaction.rb`（関連追加）
* `app/models/drink_order.rb`（関連追加）
* キャスト側の show テンプレ（`id="cast_pending_drink_orders"` を置く）

---

## 3) コード：モデル（任意・おすすめ）

### `app/models/wallet.rb`（新規）

```ruby
class Wallet < ApplicationRecord
  belongs_to :customer_user, class_name: "User"
  has_many :wallet_transactions, dependent: :destroy

  validates :available_points, numericality: { only_integer: true, greater_than_or_equal_to: 0 }
  validates :reserved_points,  numericality: { only_integer: true, greater_than_or_equal_to: 0 }
end
```

### `app/models/wallet_transaction.rb`（追記）

```ruby
class WalletTransaction < ApplicationRecord
  belongs_to :wallet
  belongs_to :ref, polymorphic: true, optional: true

  enum :kind, { purchase: 0, hold: 1, release: 2, consume: 3, adjustment: 4 }
end
```

### `app/models/drink_order.rb`（追記）

```ruby
class DrinkOrder < ApplicationRecord
  belongs_to :store
  belongs_to :booth
  belongs_to :stream_session
  belongs_to :customer_user, class_name: "User"
  belongs_to :drink_item

  enum :status, { pending: 0, consumed: 1, refunded: 2 }
end
```

---

## 4) コード：Wallets::HoldService（必須）

`app/services/wallets/hold_service.rb`

```ruby
# frozen_string_literal: true

module Wallets
  class HoldService
    class InsufficientPoints < StandardError; end

    def initialize(wallet:, points:)
      @wallet = wallet
      @points = points.to_i
    end

    def call!
      raise ArgumentError, "points must be positive" unless @points.positive?

      @wallet.with_lock do
        if @wallet.available_points < @points
          raise InsufficientPoints
        end

        @wallet.available_points -= @points
        @wallet.reserved_points  += @points
        @wallet.save!
      end

      @wallet
    end
  end
end
```

**落とし穴**

* `with_lock` は必須（連打や並列でマイナスを作らない）

---

## 5) コード：DrinkOrders::CreateService（必須）

`app/services/drink_orders/create_service.rb`

```ruby
# frozen_string_literal: true

module DrinkOrders
  class CreateService
    class Forbidden < StandardError; end
    class Conflict < StandardError; end
    class InvalidItem < StandardError; end

    Result = Struct.new(:drink_order, :wallet, keyword_init: true)

    def initialize(stream_session:, customer_user:, drink_item:)
      @stream_session = stream_session
      @customer_user  = customer_user
      @drink_item     = drink_item
    end

    def call!
      booth = @stream_session.booth

      # booth 状態（live/awayだけOK）
      unless booth.status.in?(%w[live away])
        raise Conflict
      end

      # BAN
      if StoreBan.exists?(store_id: booth.store_id, customer_user_id: @customer_user.id)
        raise Forbidden
      end

      # item 妥当性
      unless @drink_item.store_id == booth.store_id && @drink_item.enabled? && @drink_item.price_points.to_i.positive?
        raise InvalidItem
      end

      wallet = Wallet.find_by!(customer_user_id: @customer_user.id)
      price  = @drink_item.price_points

      drink_order = nil

      ApplicationRecord.transaction do
        Wallets::HoldService.new(wallet: wallet, points: price).call!

        drink_order = DrinkOrder.create!(
          store_id: booth.store_id,
          booth_id: booth.id,
          stream_session_id: @stream_session.id,
          customer_user_id: @customer_user.id,
          drink_item_id: @drink_item.id,
          status: :pending
        )

        WalletTransaction.create!(
          wallet_id: wallet.id,
          kind: :hold,
          points: -price,
          ref: drink_order,
          occurred_at: Time.current
        )
      end

      # commit後に通知（ロールバック時の誤通知防止）
      DrinkOrderNotifier.replace_pending_list(drink_order)

      Result.new(drink_order: drink_order, wallet: wallet.reload)
    end
  end
end

```

**落とし穴**

* **通知は transaction 外**（超重要）
* `WalletTransaction.points` を「残高増減の符号」で統一するなら、hold は `-price` が自然（購入は `+`）

---

## 6) コード：StreamSessions::DrinkOrdersController（必須）

`app/controllers/stream_sessions/drink_orders_controller.rb`

```ruby
# frozen_string_literal: true

module StreamSessions
  class DrinkOrdersController < ApplicationController
    before_action :authenticate_user!
    before_action :require_customer!

    def create
      stream_session = StreamSession.find(params[:stream_session_id])
      drink_item = DrinkItem.find(params[:drink_item_id])

      result = DrinkOrders::CreateService.new(
        stream_session: stream_session,
        customer_user: current_user,
        drink_item: drink_item
      ).call!

      respond_to do |format|
        format.json do
          render json: {
            drink_order_id: result.drink_order.id,
            wallet: {
              available_points: result.wallet.available_points,
              reserved_points:  result.wallet.reserved_points
            }
          }, status: :created
        end
        format.html do
          redirect_back fallback_location: booth_path(stream_session.booth_id),
                        notice: "ドリンクを送信しました"
        end
      end
    rescue Wallets::HoldService::InsufficientPoints
      respond_to do |format|
        format.json { render json: { error: "insufficient_points" }, status: :payment_required } # 402
        format.html { redirect_back fallback_location: booth_path(stream_session.booth_id), alert: "ポイント不足です" }
      end
    rescue DrinkOrders::CreateService::Forbidden
      render json: { error: "forbidden" }, status: :forbidden
    rescue DrinkOrders::CreateService::Conflict
      render json: { error: "booth_not_live" }, status: :conflict
    rescue DrinkOrders::CreateService::InvalidItem
      render json: { error: "invalid_item" }, status: :unprocessable_entity
    end

    private

    def require_customer!
      head :forbidden unless current_user.customer?
    end
  end
end
```

**ポイント**

* 402 をちゃんと返す（残高不足）
* フロントは fetch/Stimulus で受けるのが楽（HotwireでもOK）

---

### 1) Cast::StreamSessionsController#pending_drink_orders を partial返却に変更
`app/controllers/cast/stream_sessions_controller.rb`
```ruby
module Cast
  class StreamSessionsController < Cast::BaseController

    ...

    def pending_drink_orders
      stream_session = StreamSession.find(params[:id])

      render partial: "cast/stream_sessions/pending_drink_orders",
             locals: { stream_session: stream_session }
    end
  end
end
```

---

## 7) コード：Notifier（pending一覧 replace）

今 `CommentNotifier.append` があるので、同じ流儀でいきます。

`app/notifiers/drink_order_notifier.rb`

```ruby
# frozen_string_literal: true

class DrinkOrderNotifier
  def self.replace_pending_list(drink_order)
    Turbo::StreamsChannel.broadcast_replace_to(
      [drink_order.stream_session, :pending_drink_orders],
      target: "cast_pending_drink_orders",
      partial: "cast/stream_sessions/pending_drink_orders",
      locals: { stream_session: drink_order.stream_session }
    )
  end
end
```

### キャスト側のビューに置くターゲット（例）

`cast/booths/show.html.erb` に「置き場所」を追加

```erb
<% if @stream_session.present? %>
  <%= turbo_stream_from [@stream_session, :pending_drink_orders] %>
<% end %>

<div id="cast-pending-drink-orders-container">
  <% if @stream_session.present? %>
    <%= render "cast/stream_sessions/pending_drink_orders", stream_session: @stream_session %>
  <% else %>
    <div class="text-muted">配信が開始されていません</div>
  <% end %>
</div>
```

### replace用 partial（最小）

`app/views/cast/stream_sessions/_pending_drink_orders.html.erb`

```erb
<% pending = DrinkOrder
  .includes(:drink_item)
  .where(stream_session_id: stream_session.id, status: :pending)
  .order(:created_at, :id) %>
<div id="cast_pending_drink_orders">
  <div class="vstack gap-2">
    <% if pending.empty? %>
      <div class="text-muted">未消化のドリンクはありません</div>
    <% else %>
      <% pending.each do |order| %>
        <div class="card">
          <div class="card-body py-2">
            <div class="fw-semibold"><%= order.drink_item.name %></div>
            <div class="text-muted small">
              <%= l(order.created_at, format: :short) rescue order.created_at %>
            </div>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
```

> ここは後で「consumeボタン」「customer名」「金額」など拡張すればOK。
> まずは “replaceされる” を最優先に最小で。

### customer 側 booths#show 最低限の「送信」UI
まず customer 側も購読を配列形式に統一

```erb
<%= turbo_stream_from [@stream_session, :comments] %>
```

`app/views/booths/show.html.erb` のドリンクメニュー部分を、送信ボタン付きにします。

```erb
<h2>ドリンクメニュー（enabledのみ）</h2>

<% if @stream_session.blank? %>
  <p>配信中のみドリンク送信できます</p>

<% elsif @drink_items.any? %>
  <ul>
    <% @drink_items.each do |item| %>
      <li class="mb-2">
        <%= item.name %>（<%= item.price_points %>pt）

        <%= button_to "送信",
              stream_session_drink_orders_path(@stream_session),
              method: :post,
              params: { drink_item_id: item.id },
              form: { data: { turbo: false } },
              class: "btn btn-sm btn-primary ms-2" %>
      </li>
    <% end %>
  </ul>

<% else %>
  <p>現在、表示できるドリンクがありません</p>
<% end %>
```

---

## 8) debug

`app\javascript\controllers\stream_debug_controller.js`

```js
import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  connect() {
    this.onBefore = (event) => {
      // turbo-stream-element を受け取る直前
      const el = event.target
      if (el && el.tagName === "TURBO-STREAM") {
        const action = el.getAttribute("action")
        const target = el.getAttribute("target")
        this.element.textContent = `last turbo-stream: action=${action} target=${target} at ${new Date().toLocaleTimeString()}`
      }
    }

    document.addEventListener("turbo:before-stream-render", this.onBefore)
  }

  disconnect() {
    document.removeEventListener("turbo:before-stream-render", this.onBefore)
  }
}
```

Closes #19 
Relates to #12 