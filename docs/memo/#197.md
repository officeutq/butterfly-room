了解、**実装に入ります**。今回は方針どおり、

* Turbo の `replace(target: ...)` が毎回当たるように
* **置換後HTMLにも必ず同じ target id が残る**
* そのために **画面別 wrapper partial（home / show）を分割**

で直します。
（最小差分優先で、controller 側の「show用＋home用の2つreplace」は現状維持します。片方は空振りしますが、Turbo的には問題なく、改修範囲が一番小さいです）

---

# 実装方針（高レベル）

## 直すポイント

* いま `replace(target: "booth_favorite_button")` で返している partial が `_button.html.erb`（= id を持たない）なので、**1回目の置換で id が消える**
* そこで、`replace` の返却を **wrapper partial** に変更し、partial の最外要素で **id を必ず復元**する

## 追加するwrapper partial（合計4つ）

* `favorites/booths/_button_home.html.erb`（`id="booth_#{booth.id}_favorite_button"`）
* `favorites/booths/_button_booth_show.html.erb`（`id="booth_favorite_button"`）
* `favorites/stores/_button_home.html.erb`（`id="store_#{store.id}_favorite_button"`）
* `favorites/stores/_button_store_show.html.erb`（`id="store_favorite_button"`）

---

# 変更ファイル一覧

* 追加

  * `app/views/favorites/booths/_button_home.html.erb`
  * `app/views/favorites/booths/_button_booth_show.html.erb`
  * `app/views/favorites/stores/_button_home.html.erb`
  * `app/views/favorites/stores/_button_store_show.html.erb`

* 変更

  * `app/controllers/favorites/booths_controller.rb`
  * `app/controllers/favorites/stores_controller.rb`
  * `app/views/home/show.html.erb`
  * `app/views/booths/show.html.erb`
  * `app/views/stores/show.html.erb`
  * `test/integration/favorites_toggle_test.rb`

---

# 変更内容（貼り付け用）

## 1) 追加: wrapper partial（booths）

### `app/views/favorites/booths/_button_home.html.erb`

```erb
<%# locals: booth:, favorited: %>
<div id="booth_<%= booth.id %>_favorite_button">
  <%= render "favorites/booths/button", booth: booth, favorited: favorited %>
</div>
```

### `app/views/favorites/booths/_button_booth_show.html.erb`

```erb
<%# locals: booth:, favorited: %>
<div id="booth_favorite_button" class="mb-3">
  <%= render "favorites/booths/button", booth: booth, favorited: favorited %>
</div>
```

---

## 2) 追加: wrapper partial（stores）

### `app/views/favorites/stores/_button_home.html.erb`

```erb
<%# locals: store:, favorited: %>
<div id="store_<%= store.id %>_favorite_button">
  <%= render "favorites/stores/button", store: store, favorited: favorited %>
</div>
```

### `app/views/favorites/stores/_button_store_show.html.erb`

```erb
<%# locals: store:, favorited: %>
<div id="store_favorite_button">
  <%= render "favorites/stores/button", store: store, favorited: favorited %>
</div>
```

---

## 3) 変更: controller（booths）

### `app/controllers/favorites/booths_controller.rb`

```ruby
# frozen_string_literal: true

module Favorites
  class BoothsController < ApplicationController
    before_action -> { require_at_least!(:customer) }
    before_action :set_booth, only: %i[create destroy]

    def index
      scope =
        current_user
          .favorite_booths
          .joins(:booth)
          .merge(Booth.active)
          .includes(booth: :store)
          .order(created_at: :desc, id: :desc)

      # Home と同じ「可能な範囲で予防」：customer のみ BAN store を除外
      if current_user.customer?
        banned_store_ids = StoreBan.where(customer_user_id: current_user.id).select(:store_id)
        scope = scope.where.not(booths: { store_id: banned_store_ids })
      end

      @favorite_booths = scope
    end

    def create
      current_user.favorite_booths.find_or_create_by!(booth: @booth)

      render_favorite_button(favorited: true)
    rescue ActiveRecord::RecordNotUnique
      # UNIQUE index で競合しても成功扱いに寄せる
      render_favorite_button(favorited: true)
    end

    def destroy
      current_user.favorite_booths.where(booth: @booth).destroy_all

      render_favorite_button(favorited: false)
    end

    private

    def set_booth
      @booth = Booth.active.find(params[:booth_id])
    end

    def render_favorite_button(favorited:)
      respond_to do |format|
        format.turbo_stream do
          streams = [
            turbo_stream.replace(
              "booth_favorite_button",
              partial: "favorites/booths/button_booth_show",
              formats: [:html],
              locals: { booth: @booth, favorited: favorited }
            ),
            turbo_stream.replace(
              "booth_#{@booth.id}_favorite_button",
              partial: "favorites/booths/button_home",
              formats: [:html],
              locals: { booth: @booth, favorited: favorited }
            )
          ]
          render turbo_stream: streams
        end

        format.html { redirect_back fallback_location: booth_path(@booth) }
      end
    end
  end
end
```

---

## 4) 変更: controller（stores）

### `app/controllers/favorites/stores_controller.rb`

```ruby
# frozen_string_literal: true

module Favorites
  class StoresController < ApplicationController
    before_action -> { require_at_least!(:customer) }
    before_action :set_store, only: %i[create destroy]

    def index
      scope =
        current_user
          .favorite_stores
          .joins(:store)
          .includes(:store)
          .order(created_at: :desc, id: :desc)

      # customer のみ BAN store を除外（Home と同じ思想）
      if current_user.customer?
        banned_store_ids = StoreBan.where(customer_user_id: current_user.id).select(:store_id)
        scope = scope.where.not(stores: { id: banned_store_ids })
      end

      @favorite_stores = scope
    end

    def create
      current_user.favorite_stores.find_or_create_by!(store: @store)

      render_favorite_button(favorited: true)
    rescue ActiveRecord::RecordNotUnique
      render_favorite_button(favorited: true)
    end

    def destroy
      current_user.favorite_stores.where(store: @store).destroy_all

      render_favorite_button(favorited: false)
    end

    private

    def set_store
      @store = Store.find(params[:store_id])
    end

    def render_favorite_button(favorited:)
      respond_to do |format|
        format.turbo_stream do
          streams = [
            turbo_stream.replace(
              "store_favorite_button",
              partial: "favorites/stores/button_store_show",
              formats: [:html],
              locals: { store: @store, favorited: favorited }
            ),
            turbo_stream.replace(
              "store_#{@store.id}_favorite_button",
              partial: "favorites/stores/button_home",
              formats: [:html],
              locals: { store: @store, favorited: favorited }
            )
          ]
          render turbo_stream: streams
        end

        format.html { redirect_back fallback_location: store_path(@store) }
      end
    end
  end
end
```

---

## 5) 変更: view（home/show）

### `app/views/home/show.html.erb`（差し替え箇所のみ：favorite部分）

**Boothカードの footer 内：**

```erb
<div class="card-footer bg-white border-0 pt-0">
  <%= render "favorites/booths/button_home", booth: booth, favorited: favorited %>
</div>
```

**店舗一覧の favorite 部分：**

```erb
<div class="d-flex justify-content-between align-items-center gap-3">
  <div class="flex-grow-1">
    <%= link_to store.name, store_path(store), class: "text-decoration-none" %>
  </div>

  <%= render "favorites/stores/button_home", store: store, favorited: favorited %>
</div>
```

（※今までの `<div id="..."> ... </div>` は wrapper partial 側で生成するので不要になります）

---

## 6) 変更: view（booths/show）

### `app/views/booths/show.html.erb`（favorite部分のみ）

```erb
<%# ★お気に入りボタン（stream_state とは分離して固定DOM） %>
<% if user_signed_in? %>
  <%= render "favorites/booths/button_booth_show", booth: @booth, favorited: @booth_favorited %>
<% end %>
```

---

## 7) 変更: view（stores/show）

### `app/views/stores/show.html.erb`（favorite部分のみ）

```erb
<%= render "favorites/stores/button_store_show", store: @store, favorited: @store_favorited %>
```

（元の `<div id="store_favorite_button"> ...` は wrapper partial 側で生成）

---

## 8) 変更: テスト（target id がレスポンスに残ることを保証）

### `test/integration/favorites_toggle_test.rb`

```ruby
# frozen_string_literal: true

require "test_helper"

class FavoritesToggleTest < ActionDispatch::IntegrationTest
  setup do
    @store = Store.create!(name: "store1")
    @booth = Booth.create!(store: @store, name: "booth1", status: :offline)
    @user  = User.create!(email: "customer@example.com", password: "password", role: :customer)
  end

  test "booth favorite create/destroy (turbo_stream)" do
    sign_in @user, scope: :user

    assert_difference("FavoriteBooth.count", +1) do
      post booth_favorite_path(@booth),
           headers: { "Accept" => "text/vnd.turbo-stream.html" }
    end
    assert_response :success
    assert_includes response.body, 'id="booth_favorite_button"'
    assert_includes response.body, %(id="booth_#{@booth.id}_favorite_button")

    assert_difference("FavoriteBooth.count", -1) do
      delete booth_favorite_path(@booth),
             headers: { "Accept" => "text/vnd.turbo-stream.html" }
    end
    assert_response :success
    assert_includes response.body, 'id="booth_favorite_button"'
    assert_includes response.body, %(id="booth_#{@booth.id}_favorite_button")
  end

  test "booth favorite is idempotent (double create does not break)" do
    sign_in @user, scope: :user

    post booth_favorite_path(@booth),
         headers: { "Accept" => "text/vnd.turbo-stream.html" }
    assert_response :success
    assert_includes response.body, 'id="booth_favorite_button"'
    assert_includes response.body, %(id="booth_#{@booth.id}_favorite_button")

    assert_no_difference("FavoriteBooth.count") do
      post booth_favorite_path(@booth),
           headers: { "Accept" => "text/vnd.turbo-stream.html" }
    end
    assert_response :success
    assert_includes response.body, 'id="booth_favorite_button"'
    assert_includes response.body, %(id="booth_#{@booth.id}_favorite_button")
  end

  test "store favorite create/destroy (turbo_stream)" do
    sign_in @user, scope: :user

    assert_difference("FavoriteStore.count", +1) do
      post store_favorite_path(@store),
           headers: { "Accept" => "text/vnd.turbo-stream.html" }
    end
    assert_response :success
    assert_includes response.body, 'id="store_favorite_button"'
    assert_includes response.body, %(id="store_#{@store.id}_favorite_button")

    assert_difference("FavoriteStore.count", -1) do
      delete store_favorite_path(@store),
             headers: { "Accept" => "text/vnd.turbo-stream.html" }
    end
    assert_response :success
    assert_includes response.body, 'id="store_favorite_button"'
    assert_includes response.body, %(id="store_#{@store.id}_favorite_button")
  end

  test "store favorite is idempotent (double create does not break)" do
    sign_in @user, scope: :user

    post store_favorite_path(@store),
         headers: { "Accept" => "text/vnd.turbo-stream.html" }
    assert_response :success
    assert_includes response.body, 'id="store_favorite_button"'
    assert_includes response.body, %(id="store_#{@store.id}_favorite_button")

    assert_no_difference("FavoriteStore.count") do
      post store_favorite_path(@store),
           headers: { "Accept" => "text/vnd.turbo-stream.html" }
    end
    assert_response :success
    assert_includes response.body, 'id="store_favorite_button"'
    assert_includes response.body, %(id="store_#{@store.id}_favorite_button")
  end
end
```

---

# 動作確認（手順）

* Home で booth のお気に入りを **2回以上連続**で押して、毎回切り替わる
* booth/show で **2回以上連続**で押して切り替わる
* store/show で **2回以上連続**で押して切り替わる
* Elements で確認：

  * 置換後も `div#booth_favorite_button` / `div#booth_123_favorite_button` が **残っている**
  * store も同様

---

# 実行コマンド（docker compose 経由）

```bash
docker compose exec app bash -lc "RAILS_ENV=test bin/rails test test/integration/favorites_toggle_test.rb"
docker compose exec app bash -lc "RAILS_ENV=test bin/rails test"
```

---

必要なら、このIssue向けの **コミットメッセージ案 / PR本文（変更点・仕様・動作確認）** も続けて作れます。
