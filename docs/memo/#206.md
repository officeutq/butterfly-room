了解、GOで進めます。
（#206 のスコープに合わせて **最小差分**・**Store.first 廃止**・**system_admin も未選択なら /admin/stores** を実現します）

# (1) High-level implementation understanding（最終）

* **変更の核は2点だけ**

  1. `Admin::BaseController#current_store` から **system_admin の暗黙デフォルト（Store.first）を削除**
     → `session[:current_store_id]` が無い/不正なら **nil（未選択）** へ
  2. `Admin::BaseController#require_current_store!` を **system_admin でも適用**
     → `admin_root_path` を含む admin主要画面は **未選択なら /admin/stores** へ誘導

* **例外は現状構造を維持**

  * `Admin::StoresController#index` と `Admin::CurrentStoresController#create` は **before_action :require_current_store! が付いていない**ので、そのまま例外になる（Issue仕様に一致）

* **store_admin の既存挙動は維持**

  * `current_store` の「最初の admin membership fallback」はそのまま（Issueは system_admin の暗黙デフォルト廃止が主眼）

---

# (2) Branch name candidates

* `improve/206-admin-current-store-system-admin`
* `improve/206-admin-require-current-store`
* `improve/206-admin-remove-store-first-default`
* `improve/206-admin-current-store-guard`

---

# (3) Implementation plan（順序付き）

1. `Admin::BaseController#current_store`

   * system_admin の `Store.first` fallback を削除（未選択は nil）
2. `Admin::BaseController#require_current_store!`

   * `system_admin` の早期returnを削除し、全adminユーザーに適用
3. テスト追加

   * system_admin が current_store 未選択で `admin_root_path` に行くと `/admin/stores` へ
   * system_admin が不正 `current_store_id` を持っている場合、クリアされ `/admin/stores` へ
   * store_admin が壊れてない（代表で admin_root_path 到達）

---

# (4) Paste-ready code（差分）

## 1) app/controllers/admin/base_controller.rb（更新）

```ruby
module Admin
  class BaseController < ApplicationController
    before_action -> { require_at_least!(:store_admin) }

    private

    def current_store
      # --- Current整合（booth優先）---
      # session[:current_booth_id] が有効なら store は booth.store を優先し、
      # session[:current_store_id] の不一致は補正する。
      #
      # 無効 booth（record不存在 / 権限的に無効）は session[:current_booth_id] をクリアする。
      #
      # ※ 07_モード導線設計.md の Current整合ルールに準拠
      if session[:current_booth_id].present?
        booth = Booth.find_by(id: session[:current_booth_id])

        if booth.blank?
          # record不存在
          session.delete(:current_booth_id)
        else
          allowed =
            if current_user.system_admin?
              true
            else
              # store_admin: 自分が admin membership を持つ store の booth のみ有効
              admin_membership_exists_for_store?(booth.store_id)
            end

          if allowed
            # booth優先で store を確定し、session不一致は補正
            session[:current_store_id] = booth.store_id if session[:current_store_id].to_i != booth.store_id
            return booth.store
          else
            # 権限的に無効（脱退/改ざん等）
            session.delete(:current_booth_id)
          end
        end
      end

      # 1) session 優先
      if session[:current_store_id].present?
        store = Store.find_by(id: session[:current_store_id])

        if store.present?
          # system_admin は membership を持たない運用なので、存在する store ならOK
          return store if current_user.system_admin?

          # store_admin は admin membership がある store のみ有効
          return store if admin_membership_exists_for_store?(store.id)
        end

        # 改ざん/脱退/削除 への耐性：不正ならクリア
        session.delete(:current_store_id)
      end

      # 2) fallback: 従来通り「最初の admin membership」
      # ※ store_admin の既存挙動を維持（system_admin の暗黙デフォルトは廃止）
      membership =
        StoreMembership
          .includes(:store)
          .where(user_id: current_user.id, membership_role: :admin)
          .order(:id)
          .first

      return membership&.store unless current_user.system_admin?

      # system_admin: session store が無い / 無効な場合は未選択扱い（暗黙デフォルトは廃止）
      nil
    end

    helper_method :current_store

    def require_current_store!
      return if current_store.present?

      redirect_to admin_stores_path, alert: "操作対象の店舗を選択してください"
    end

    def admin_membership_exists_for_store?(store_id)
      StoreMembership.exists?(
        user_id: current_user.id,
        store_id: store_id,
        membership_role: StoreMembership.membership_roles[:admin]
      )
    end
  end
end
```

---

## 2) test/integration/admin_current_store_required_for_system_admin_test.rb（新規）

```ruby
# frozen_string_literal: true

require "test_helper"

class AdminCurrentStoreRequiredForSystemAdminTest < ActionDispatch::IntegrationTest
  setup do
    @store = Store.create!(name: "store1")
    @system_admin = User.create!(email: "sys_guard@example.com", password: "password", role: :system_admin)

    @store_admin = User.create!(email: "admin_guard@example.com", password: "password", role: :store_admin)
    StoreMembership.create!(store: @store, user: @store_admin, membership_role: :admin)
  end

  test "system_admin without current_store is redirected to /admin/stores on admin root" do
    sign_in @system_admin, scope: :user

    get admin_root_path
    assert_response :redirect
    assert_redirected_to admin_stores_path

    follow_redirect!
    assert_response :success
  end

  test "system_admin with invalid current_store_id is corrected and redirected to /admin/stores" do
    sign_in @system_admin, scope: :user

    get admin_root_path, headers: { "rack.session" => { current_store_id: 999_999 } }
    assert_response :redirect
    assert_redirected_to admin_stores_path

    follow_redirect!
    assert_response :success

    # session がクリアされていること（選択中ハイライトが付かない）を確認
    assert_select "tr.table-active", count: 0
  end

  test "store_admin behavior is not broken (can access admin root without explicit selection)" do
    sign_in @store_admin, scope: :user

    get admin_root_path
    assert_response :success
  end
end
```

---

# (5) 動作確認（ローカル）

※Railsタスクは常に docker compose 経由で。

```bash
docker compose exec app bash -lc "bin/rails test"
```

任意（ブラウザ確認）：

* system_admin でログイン → `/admin` に直接アクセス → `/admin/stores` に飛ぶ
* `/admin/stores` で店舗選択 → root に戻る（#228 前提）

---

# (6) コミットメッセージ案

* `Improve admin: require explicit current_store for system_admin (#206)`
* `Improve admin current_store: remove Store.first fallback for system_admin (#206)`
* `Improve admin: redirect to store selection when current_store missing (#206)`

---

# (7) PR本文（コピペ用）

**Title**

* `[Improve] admin：system_admin も current_store を明示選択（未選択は /admin/stores へ・Store.first 廃止） #206`

**Body**

* 目的

  * admin 配下で「操作対象 store」を常に明確にし、system_admin でも暗黙のデフォルト（Store.first 等）に依存しない設計へ統一する（#206 / 親Epic #139）。
* 変更点

  * `Admin::BaseController#current_store` から system_admin の `Store.first` fallback を削除し、未選択/不正時は未選択扱い（nil）に変更。
  * `require_current_store!` を system_admin にも適用し、admin 主要画面は current_store 未選択/不正時に `/admin/stores` へ誘導。
  * 例外（`/admin/stores#index` と `current_store#create`）は既存構造のまま（before_action未設定のため到達可能）。
* 仕様メモ

  * system_admin: session の `current_store_id` が存在し、かつ store が存在する場合のみ current_store として有効。
  * store_admin: 従来通り「最初の admin membership fallback」を維持（最小差分）。
* テスト

  * system_admin：未選択で admin root → `/admin/stores` へリダイレクト
  * system_admin：不正 `current_store_id` → クリアされ `/admin/stores` へ
  * store_admin：admin root 到達（回帰チェック）
* 動作確認

  * `docker compose exec app bash -lc "bin/rails test"`
* Closes

  * `Closes #206`

---

原因はシンプルで、**#206 の仕様どおり「system_admin でも current_store 未選択なら admin 配下は /admin/stores へ誘導」になった**ため、既存テストが「未選択のまま admin の画面/操作ができる」前提で落ちています。

加えて、こちらが追加した `AdminCurrentStoreRequiredForSystemAdminTest` の「不正 session 注入」テストが、Railsの統合テスト環境だと不安定で `Hash` を踏んだ形です（`rack.session` 注入が期待どおりになっていない）。

以下の修正で全部通ります。**アプリ側のコードはそのまま**でOKです（= テストの期待値/準備を #206 に合わせる）。

---

## 1) `test/integration/booth_edit_authorization_test.rb` 修正

system_admin の admin 操作前に **current_store を選択**します（どの店舗でもOK。存在していれば良い）。

```ruby
# frozen_string_literal: true

require "test_helper"

class BoothEditAuthorizationTest < ActionDispatch::IntegrationTest
  setup do
    @store1 = Store.create!(name: "store1")
    @store2 = Store.create!(name: "store2")

    @booth1 = Booth.create!(store: @store1, name: "booth1", status: :offline)
    @booth2 = Booth.create!(store: @store2, name: "booth2", status: :offline)

    @customer     = User.create!(email: "customer@example.com", password: "password", role: :customer)
    @cast         = User.create!(email: "cast@example.com", password: "password", role: :cast)
    @store_admin  = User.create!(email: "admin@example.com", password: "password", role: :store_admin)
    @system_admin = User.create!(email: "sys@example.com", password: "password", role: :system_admin)

    BoothCast.create!(booth: @booth1, cast_user: @cast)
    StoreMembership.create!(store: @store1, user: @store_admin, membership_role: :admin)
  end

  test "customer cannot edit/update (403)" do
    sign_in @customer, scope: :user

    get edit_cast_booth_path(@booth1)
    assert_response :forbidden

    patch cast_booth_path(@booth1), params: { booth: { name: "x" } }
    assert_response :forbidden

    patch admin_booth_path(@booth1), params: { booth: { name: "x" } }
    assert_response :forbidden
  end

  test "cast can edit only own booth" do
    sign_in @cast, scope: :user

    get edit_cast_booth_path(@booth1)
    assert_response :success

    get edit_cast_booth_path(@booth2)
    assert_response :forbidden
  end

  test "store_admin can update only own store booth" do
    sign_in @store_admin, scope: :user

    patch admin_booth_path(@booth1), params: { booth: { name: "x" } }
    assert_response :redirect
    assert_redirected_to admin_booth_path(@booth1)

    patch admin_booth_path(@booth2), params: { booth: { name: "x" } }
    assert_response :not_found
  end

  test "system_admin can update any booth" do
    sign_in @system_admin, scope: :user

    # #206: admin配下は current_store 必須なので、先に選択する
    post admin_current_store_path, params: { store_id: @store1.id }
    assert_response :redirect
    assert_redirected_to root_path

    patch admin_booth_path(@booth1), params: { booth: { name: "x" } }
    assert_response :redirect
    assert_redirected_to admin_booth_path(@booth1)

    patch admin_booth_path(@booth2), params: { booth: { name: "x" } }
    assert_response :redirect
    assert_redirected_to admin_booth_path(@booth2)
  end
end
```

---

## 2) `test/integration/admin_booth_force_end_test.rb` 修正

同様に system_admin は force_end の前に current_store を選択。

```ruby
test "system_admin can force_end any booth" do
  sign_in @system_admin, scope: :user

  # #206: admin配下は current_store 必須なので、先に選択する
  post admin_current_store_path, params: { store_id: @store1.id }
  assert_response :redirect
  assert_redirected_to root_path

  post force_end_admin_booth_path(@booth2)
  assert_response :redirect
  assert_redirected_to admin_booth_path(@booth2)

  @booth2.reload
  @session2.reload

  assert @booth2.offline?
  assert_nil @booth2.current_stream_session_id
  assert_equal "ended", @session2.status
  assert @session2.ended_at.present?
end
```

（ファイル全体はそのままで、このテストブロックだけ差し替えればOK）

---

## 3) `test/integration/role_hierarchy_access_test.rb` 修正

ここは **仕様変更を反映した期待値**に直すのが自然です。

* system_admin は **admin_root は未選択だと 302 → /admin/stores**
* ただし **store選択後なら 200 success**

```ruby
test "system_admin can access cast/admin (not forbidden)" do
  sign_in @system_admin, scope: :user

  get cast_booths_path
  assert_response :success

  # #206: 未選択の admin_root は /admin/stores に誘導される
  get admin_root_path
  assert_response :redirect
  assert_redirected_to admin_stores_path

  # 選択後は admin_root に入れる
  post admin_current_store_path, params: { store_id: @store1.id }
  assert_response :redirect
  assert_redirected_to root_path

  get admin_root_path
  assert_response :success
end
```

---

## 4) `AdminCurrentStoreRequiredForSystemAdminTest` のエラー修正（おすすめ）

`rack.session` 注入はやめて、**「一度選択 → 対象storeを削除 → sessionが不正になる」**で再現します。
これがいちばん堅いです（仕様にも合う）。

`test/integration/admin_current_store_required_for_system_admin_test.rb` を **全文差し替え**してください：

```ruby
# frozen_string_literal: true

require "test_helper"

class AdminCurrentStoreRequiredForSystemAdminTest < ActionDispatch::IntegrationTest
  setup do
    @store = Store.create!(name: "store1")
    @system_admin = User.create!(email: "sys_guard@example.com", password: "password", role: :system_admin)

    @store_admin = User.create!(email: "admin_guard@example.com", password: "password", role: :store_admin)
    StoreMembership.create!(store: @store, user: @store_admin, membership_role: :admin)
  end

  test "system_admin without current_store is redirected to /admin/stores on admin root" do
    sign_in @system_admin, scope: :user

    get admin_root_path
    assert_response :redirect
    assert_redirected_to admin_stores_path

    follow_redirect!
    assert_response :success
  end

  test "system_admin with invalid current_store_id is corrected and redirected to /admin/stores" do
    sign_in @system_admin, scope: :user

    # いったん選択して session に入れる
    post admin_current_store_path, params: { store_id: @store.id }
    assert_response :redirect
    assert_redirected_to root_path

    # store を削除して session を不正化する
    @store.destroy!

    get admin_root_path
    assert_response :redirect
    assert_redirected_to admin_stores_path

    follow_redirect!
    assert_response :success
  end

  test "store_admin behavior is not broken (can access admin root without explicit selection)" do
    sign_in @store_admin, scope: :user

    get admin_root_path
    assert_response :success
  end
end
```

> これで「不正IDのとき session がクリアされ /admin/stores」という要件は満たせます（“不正”の作り方を安全にしただけ）。

---

## 実行コマンド

```bash
docker compose exec app bash -lc "bin/rails test"
```

---

もしこの修正後にまだ落ちる場合は、落ちたテスト名と failure を貼ってください。
（今回の3 failures + 1 error は上の修正で全部筋が通っています）

