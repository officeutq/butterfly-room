以下、Issue #47「店舗管理：キャスト招待（ワンタイム・24時間）」の **実装計画**と、**貼り付け可能なコード一式**です。
（※あなたの方針どおり、`admin/casts` の「候補castをselectで追加」は削除します）

---

# 1) 実装の全体像（作業順）

## 目的（Done条件に直結）

* 店舗管理者が招待発行できる
* 招待は 24h で失効
* ワンタイムで、1回のみ承認可能
* cast ロールでログイン中のユーザーのみ承認可能
* ロール付け替えはしない（＝招待承認では `User.role` を変更しない）

## 実装アーキテクチャ（既存の流儀に合わせる）

* 登録：`Customers/Stores` と同様に **Form + Service**（castも同様に追加）
* 招待承認：**Serviceでトランザクション**（Result struct + `call!`）
* token：URLに **生token**、DBには **digest**（SHA256 + secret_key_base）
* 未ログイン：招待URLアクセス時は **ログインへリダイレクト**し、ログイン後に招待URLへ戻す（stored_location）
* 招待経由のみ表示する “cast新規作成導線” は、Devise view を最小で1枚追加して対応（`devise/sessions/new`）

---

# 2) ブランチ名案（最終）

* `feature/47-store-cast-invitations`
* `feature/47-cast-invite-one-time-24h`
* `feature/47-admin-cast-invite`

---

# 3) 作業コマンド（docker compose 経由）

### 3-1. マイグレーション（空ファイル生成 → 全文置換）

```bash
docker compose exec app bash -lc "bin/rails g migration CreateStoreCastInvitations"
```

生成された migration を、下の **「db/migrate/..._create_store_cast_invitations.rb」** に全文置換。

### 3-2. テスト

```bash
docker compose exec app bash -lc "bin/rails test"
```

---

# 4) 変更ファイル一覧（貼り付け用：全文）

## 4-1. DB: store_cast_invitations

### db/migrate/XXXXXXXXXXXXXX_create_store_cast_invitations.rb

（ファイル名の先頭の timestamp は生成されたものを使ってください）

```ruby
# frozen_string_literal: true

class CreateStoreCastInvitations < ActiveRecord::Migration[8.1]
  def change
    create_table :store_cast_invitations do |t|
      t.references :store, null: false, foreign_key: true

      t.bigint :invited_by_user_id, null: false
      t.bigint :accepted_by_user_id

      t.string :token_digest, null: false
      t.datetime :expires_at, null: false
      t.datetime :used_at

      t.text :note

      t.timestamps
    end

    add_foreign_key :store_cast_invitations, :users, column: :invited_by_user_id
    add_foreign_key :store_cast_invitations, :users, column: :accepted_by_user_id

    add_index :store_cast_invitations, :token_digest, unique: true
    add_index :store_cast_invitations, :expires_at
    add_index :store_cast_invitations, :used_at
    add_index :store_cast_invitations, [:store_id, :created_at]
  end
end
```

---

## 4-2. Model: StoreCastInvitation

### app/models/store_cast_invitation.rb

```ruby
# frozen_string_literal: true

require "digest"
require "securerandom"

class StoreCastInvitation < ApplicationRecord
  belongs_to :store
  belongs_to :invited_by_user, class_name: "User"
  belongs_to :accepted_by_user, class_name: "User", optional: true

  validates :token_digest, presence: true, uniqueness: true
  validates :expires_at, presence: true

  scope :recent_first, -> { order(created_at: :desc, id: :desc) }

  def expired?
    expires_at < Time.current
  end

  def used?
    used_at.present?
  end

  def usable?
    !used? && !expired?
  end

  def status_label
    return "使用済み" if used?
    return "期限切れ" if expired?
    "有効"
  end

  # --- token digest helpers ---

  def self.generate_token
    SecureRandom.urlsafe_base64(32)
  end

  def self.digest_for(token)
    secret = Rails.application.secret_key_base
    Digest::SHA256.hexdigest("#{secret}--#{token}")
  end

  def self.find_by_token(token)
    find_by(token_digest: digest_for(token))
  end
end
```

---

## 4-3. Service: 招待発行 / 承認 / cast登録

### app/services/store_cast_invitations/issue_invitation.rb

```ruby
# frozen_string_literal: true

module StoreCastInvitations
  class IssueInvitation
    Result = Struct.new(:invitation, :token, keyword_init: true)

    def self.call!(store:, invited_by_user:, note: nil)
      new(store:, invited_by_user:, note:).call!
    end

    def initialize(store:, invited_by_user:, note:)
      @store = store
      @invited_by_user = invited_by_user
      @note = note
    end

    def call!
      token = StoreCastInvitation.generate_token
      digest = StoreCastInvitation.digest_for(token)

      invitation = nil

      ActiveRecord::Base.transaction do
        invitation = StoreCastInvitation.create!(
          store: @store,
          invited_by_user: @invited_by_user,
          token_digest: digest,
          expires_at: 24.hours.from_now,
          note: @note.presence
        )
      end

      Result.new(invitation: invitation, token: token)
    end
  end
end
```

### app/services/store_cast_invitations/accept_invitation.rb

```ruby
# frozen_string_literal: true

module StoreCastInvitations
  class AcceptInvitation
    Result = Struct.new(:invitation, keyword_init: true)

    class NotUsable < StandardError; end
    class NotAuthorized < StandardError; end

    def self.call!(invitation:, actor:)
      new(invitation:, actor:).call!
    end

    def initialize(invitation:, actor:)
      @invitation = invitation
      @actor = actor
    end

    def call!
      raise NotAuthorized, "cast でログインしてください" unless @actor&.cast?

      ActiveRecord::Base.transaction do
        @invitation.lock!

        raise NotUsable, "この招待は使用できません（期限切れ/使用済み）" unless @invitation.usable?

        begin
          StoreMembership.create!(
            store: @invitation.store,
            user: @actor,
            membership_role: :cast
          )
        rescue ActiveRecord::RecordNotUnique
          # すでに所属済みでも、承認済みとして扱う（安全側・冪等）
        end

        @invitation.update!(
          used_at: Time.current,
          accepted_by_user: @actor
        )
      end

      Result.new(invitation: @invitation)
    end
  end
end
```

### app/services/casts/register_cast.rb

```ruby
# frozen_string_literal: true

module Casts
  class RegisterCast
    Result = Struct.new(:user, keyword_init: true)

    def self.call!(email:, password:)
      new(email:, password:).call!
    end

    def initialize(email:, password:)
      @email = email
      @password = password
    end

    def call!
      user = nil

      ActiveRecord::Base.transaction do
        user = User.create!(
          email: @email,
          password: @password,
          password_confirmation: @password,
          role: :cast
        )
      end

      Result.new(user: user)
    end
  end
end
```

---

## 4-4. Form: cast 招待専用登録

### app/forms/casts/registration_form.rb

```ruby
# frozen_string_literal: true

module Casts
  class RegistrationForm
    include ActiveModel::Model
    include ActiveModel::Attributes

    attribute :email, :string
    attribute :password, :string
    attribute :password_confirmation, :string

    attr_reader :user

    validates :email, presence: true
    validates :password, presence: true, confirmation: true
    validates :password_confirmation, presence: true
    validate  :email_must_be_unique

    def save
      return false unless valid?

      result = Casts::RegisterCast.call!(
        email: email,
        password: password
      )

      @user = result.user
      true
    rescue ActiveRecord::RecordInvalid => e
      errors.add(:base, e.record.errors.full_messages.join(", "))
      false
    end

    private

    def email_must_be_unique
      return if email.blank?

      if User.exists?(email: email)
        errors.add(:email, "このemailアドレスはすでに使われています")
      end
    end
  end
end
```

---

## 4-5. Controller: Admin（admin/castsに統合）

### app/controllers/admin/casts_controller.rb（置換：全文）

```ruby
# frozen_string_literal: true

module Admin
  class CastsController < Admin::BaseController
    before_action :require_current_store!

    def index
      @cast_memberships =
        StoreMembership
          .includes(:user)
          .where(store_id: current_store.id, membership_role: :cast)
          .order(:id)

      @store_cast_invitations =
        StoreCastInvitation
          .includes(:invited_by_user, :accepted_by_user)
          .where(store_id: current_store.id)
          .recent_first

      @new_invitation = StoreCastInvitation.new
    end

    # 招待発行（note 任意）
    def invite
      note = params.require(:store_cast_invitation).permit(:note)[:note]

      result =
        StoreCastInvitations::IssueInvitation.call!(
          store: current_store,
          invited_by_user: current_user,
          note: note
        )

      token = result.token
      url = cast_invitation_url(token)

      redirect_to admin_casts_path,
                  notice: "招待を発行しました: #{url}"
    rescue ActionController::ParameterMissing
      redirect_to admin_casts_path, alert: "パラメータが不正です"
    rescue ActiveRecord::RecordInvalid => e
      redirect_to admin_casts_path, alert: e.record.errors.full_messages.join(", ")
    end

    def destroy
      membership =
        StoreMembership
          .where(store_id: current_store.id, membership_role: :cast)
          .find(params[:id])

      membership.destroy!

      redirect_to admin_casts_path, notice: "キャスト登録を解除しました"
    rescue ActiveRecord::RecordNotFound
      head :not_found
    end
  end
end
```

---

## 4-6. Controller: Public 招待ページ（承認ボタンあり）

### app/controllers/cast_invitations_controller.rb

```ruby
# frozen_string_literal: true

class CastInvitationsController < ApplicationController
  before_action :set_invitation_by_token

  def show
    unless user_signed_in?
      store_location_for(:user, request.fullpath)
      redirect_to new_user_session_path(invite_token: params[:token]), alert: "招待の承認には cast でログインしてください"
      return
    end

    # cast以外は案内だけ表示（承認不可）
    if !current_user.cast?
      render :show, status: :ok
      return
    end

    render :show, status: :ok
  end

  def accept
    authenticate_user!

    unless current_user.cast?
      redirect_to cast_invitation_path(params[:token]), alert: "cast でログインして承認してください"
      return
    end

    StoreCastInvitations::AcceptInvitation.call!(invitation: @invitation, actor: current_user)

    redirect_to root_path, notice: "キャスト招待を承認しました（#{@invitation.store.name}）"
  rescue StoreCastInvitations::AcceptInvitation::NotUsable => e
    redirect_to cast_invitation_path(params[:token]), alert: e.message
  rescue StoreCastInvitations::AcceptInvitation::NotAuthorized => e
    redirect_to cast_invitation_path(params[:token]), alert: e.message
  end

  private

  def set_invitation_by_token
    @invitation = StoreCastInvitation.find_by_token(params[:token].to_s)
    head :not_found if @invitation.blank?
  end
end
```

---

## 4-7. Controller: Cast 招待専用の新規登録

### app/controllers/casts/registrations_controller.rb

```ruby
# frozen_string_literal: true

module Casts
  class RegistrationsController < ApplicationController
    skip_before_action :authenticate_user!, raise: false

    before_action :require_token!

    def new
      @form = Casts::RegistrationForm.new(email: params[:email].to_s)
    end

    def create
      @form = Casts::RegistrationForm.new(registration_params)

      if @form.save
        sign_in(@form.user) # Devise
        redirect_to cast_invitation_path(@token), notice: "cast アカウントを作成しました。招待を承認してください。"
      else
        render :new, status: :unprocessable_entity
      end
    end

    private

    def require_token!
      @token = params[:token].to_s
      invitation = StoreCastInvitation.find_by_token(@token)
      head :not_found if invitation.blank?
    end

    def registration_params
      params.require(:cast_registration).permit(
        :email,
        :password,
        :password_confirmation
      )
    end
  end
end
```

---

## 4-8. Views

### app/views/admin/casts/index.html.erb（置換：全文）

```erb
<h1>キャスト管理</h1>

<h2 class="h5 mt-4">登録済みキャスト</h2>

<table class="table table-sm">
  <thead>
    <tr>
      <th>キャスト</th>
      <th>Email</th>
      <th class="text-end">操作</th>
    </tr>
  </thead>
  <tbody>
    <% @cast_memberships.each do |m| %>
      <tr>
        <td><%= m.user.display_name.presence || "-" %></td>
        <td><%= m.user.email %></td>
        <td class="text-end">
          <%= button_to "削除",
                        admin_cast_path(m),
                        method: :delete,
                        data: { turbo_confirm: "このキャスト登録を解除しますか？" },
                        class: "btn btn-sm btn-outline-danger" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<hr class="my-4">

<h2 class="h5 mt-4">キャスト招待（ワンタイム / 24時間）</h2>

<div class="card mb-3">
  <div class="card-body">
    <p class="text-muted mb-3">
      招待URLを発行し、cast ロールのユーザーが承認した場合のみ、キャスト所属（store_memberships: cast）が作成されます。
    </p>

    <%= form_with model: @new_invitation, url: invite_admin_casts_path, scope: :store_cast_invitation do |f| %>
      <div class="row g-2 align-items-end">
        <div class="col-12 col-md-8">
          <%= f.label :note, "メモ（任意）", class: "form-label" %>
          <%= f.text_area :note, class: "form-control", rows: 2, placeholder: "例）◯◯さん用 / SNSのアカウント名など" %>
        </div>
        <div class="col-12 col-md-auto">
          <%= f.submit "招待URLを発行", class: "btn btn-primary" %>
        </div>
      </div>
    <% end %>

    <div class="mt-3 text-muted small">
      ※発行後のURLは「お知らせ（notice）」にも表示されます。必要に応じてコピペしてください。
    </div>
  </div>
</div>

<h3 class="h6 mt-4">招待一覧</h3>

<table class="table table-sm">
  <thead>
    <tr>
      <th>状態</th>
      <th>期限</th>
      <th>メモ</th>
      <th>発行者</th>
      <th>承認者</th>
      <th>URL</th>
    </tr>
  </thead>

  <tbody>
    <% @store_cast_invitations.each do |inv| %>
      <tr>
        <td><%= inv.status_label %></td>
        <td><%= inv.expires_at.strftime("%Y-%m-%d %H:%M") %></td>
        <td><%= inv.note.presence || "-" %></td>
        <td><%= display_name_or_email(inv.invited_by_user) %></td>
        <td>
          <% if inv.accepted_by_user.present? %>
            <%= display_name_or_email(inv.accepted_by_user) %>
          <% else %>
            -
          <% end %>
        </td>
        <td class="text-nowrap">
          <%# token は保持していないので、ここでは直接URLを再生成できない（仕様通り：生tokenは発行時のみ） %>
          <span class="text-muted small">（発行時に控えてください）</span>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
```

> 重要：生tokenはDBに無いので、一覧でURLを再表示できません。
> 仕様（digest保存）に忠実です。
> 一覧に「URLコピー」をどうしても入れたい場合は「発行直後にだけ表示（ページ内に表示してコピー）」を追加できますが、後で再生成はできません。

---

### app/views/cast_invitations/show.html.erb（新規）

```erb
<div style="max-width: 720px;">
  <h1 class="h4 mb-3">キャスト招待</h1>

  <div class="card mb-3">
    <div class="card-body">
      <div class="mb-2">
        <span class="text-muted">店舗：</span>
        <strong><%= @invitation.store.name %></strong>
      </div>

      <% if @invitation.note.present? %>
        <div class="mb-2">
          <span class="text-muted">メモ：</span>
          <span><%= @invitation.note %></span>
        </div>
      <% end %>

      <div class="mb-2">
        <span class="text-muted">期限：</span>
        <span><%= @invitation.expires_at.strftime("%Y-%m-%d %H:%M") %></span>
      </div>

      <div class="mb-0">
        <span class="text-muted">状態：</span>
        <span><%= @invitation.status_label %></span>
      </div>
    </div>
  </div>

  <% if !user_signed_in? %>
    <%# showアクションでは未ログイン時はログインへリダイレクトされる設計（ここは基本到達しない想定） %>
    <div class="alert alert-secondary" role="alert">
      招待の承認には cast でログインしてください。
    </div>

  <% elsif !current_user.cast? %>
    <div class="alert alert-warning" role="alert">
      この招待は <strong>cast アカウント</strong>でのみ承認できます。<br>
      現在は cast 以外でログインしています。ログアウトして cast でログインし直してください。
    </div>

    <div class="d-flex gap-2">
      <%= button_to "ログアウト",
            destroy_user_session_path,
            method: :delete,
            class: "btn btn-outline-secondary" %>

      <%= link_to "ログイン（cast）", new_user_session_path(invite_token: params[:token]), class: "btn btn-primary" %>

      <%= link_to "招待経由で cast 新規作成",
            cast_sign_up_path(token: params[:token]),
            class: "btn btn-outline-primary" %>
    </div>

  <% else %>
    <% if !@invitation.usable? %>
      <div class="alert alert-danger" role="alert">
        この招待は使用できません（期限切れ / 使用済み）。
      </div>
    <% else %>
      <div class="alert alert-info" role="alert">
        上記の店舗にキャストとして参加します。よろしければ承認してください。
      </div>

      <div class="d-flex gap-2">
        <%= button_to "承認する",
              accept_cast_invitation_path(params[:token]),
              method: :post,
              class: "btn btn-primary",
              data: { turbo_confirm: "この店舗（#{@invitation.store.name}）のキャスト招待を承認しますか？" } %>

        <%= link_to "キャンセル", root_path, class: "btn btn-outline-secondary" %>
      </div>
    <% end %>
  <% end %>
</div>
```

---

### app/views/casts/registrations/new.html.erb（新規）

```erb
<div style="max-width: 720px;">
  <h1 class="h4 mb-3">cast 新規登録（招待専用）</h1>

  <div class="alert alert-secondary" role="alert">
    招待の承認には cast アカウントが必要です。アカウントを作成してください。
  </div>

  <%= form_with model: @form, url: cast_sign_up_path(token: params[:token]), scope: :cast_registration do |f| %>
    <% if @form.errors.any? %>
      <div class="alert alert-danger" role="alert">
        <ul class="mb-0">
          <% @form.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="mb-3">
      <%= f.label :email, class: "form-label" %>
      <%= f.email_field :email, class: "form-control", autocomplete: "email" %>
    </div>

    <div class="mb-3">
      <%= f.label :password, class: "form-label" %>
      <%= f.password_field :password, class: "form-control", autocomplete: "new-password" %>
    </div>

    <div class="mb-3">
      <%= f.label :password_confirmation, class: "form-label" %>
      <%= f.password_field :password_confirmation, class: "form-control", autocomplete: "new-password" %>
    </div>

    <div class="d-flex gap-2">
      <%= f.submit "作成して続行", class: "btn btn-primary" %>
      <%= link_to "戻る（招待ページ）", cast_invitation_path(params[:token]), class: "btn btn-outline-secondary" %>
    </div>
  <% end %>
</div>
```

---

## 4-9. Devise ログイン画面（招待経由時のみ導線表示）

### app/views/devise/sessions/new.html.erb（新規）

> これを追加することで、未ログインで招待URLへ来たときに
> 「cast 新規作成導線」を **ログイン画面にだけ**表示できます（要件どおり）。

```erb
<div style="max-width: 720px;">
  <h1 class="h4 mb-3">ログイン</h1>

  <% if params[:invite_token].present? %>
    <div class="alert alert-info" role="alert">
      キャスト招待を承認するには cast でログインしてください。<br>
      cast アカウントが無い場合は、招待経由で新規作成できます。
    </div>

    <div class="mb-3">
      <%= link_to "招待経由で cast 新規作成",
            cast_sign_up_path(token: params[:invite_token]),
            class: "btn btn-outline-primary" %>
    </div>
  <% end %>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="mb-3">
      <%= f.label :email, class: "form-label" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
    </div>

    <div class="mb-3">
      <%= f.label :password, class: "form-label" %>
      <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="form-check mb-3">
        <%= f.check_box :remember_me, class: "form-check-input" %>
        <%= f.label :remember_me, class: "form-check-label" %>
      </div>
    <% end %>

    <div class="d-flex gap-2">
      <%= f.submit "ログイン", class: "btn btn-primary" %>
      <%= link_to "ホーム", root_path, class: "btn btn-outline-secondary" %>
    </div>
  <% end %>
</div>
```

---

## 4-10. routes

### config/routes.rb（差分反映：該当部分を置換/追記）

```ruby
Rails.application.routes.draw do
  get "up" => "rails/health#show", as: :rails_health_check

  devise_for :users, skip: %i[registrations]

  root "home#show"

  # --- Common dashboard (login required) ---
  get "/dashboard", to: "dashboard#show", as: :dashboard

  # --- Public (customer registration) ---
  get  "/sign_up", to: "customers/registrations#new",    as: :sign_up
  post "/sign_up", to: "customers/registrations#create"

  # --- Cast registration (invite only) ---
  get  "/cast/sign_up", to: "casts/registrations#new",    as: :cast_sign_up
  post "/cast/sign_up", to: "casts/registrations#create"

  # --- Public (store registration) ---
  namespace :stores do
    get  :new_registration, to: "registrations#new"
    post :registrations,    to: "registrations#create"
  end

  # --- Public (cast invitation) ---
  get  "/cast_invitations/:token", to: "cast_invitations#show",   as: :cast_invitation
  post "/cast_invitations/:token/accept", to: "cast_invitations#accept", as: :accept_cast_invitation

  # --- Favorites (login required) ---
  namespace :favorites do
    resources :booths, only: %i[index]
    resources :stores, only: %i[index]
  end

  # --- Customer (login required) ---
  resources :stores, only: %i[show] do
    resource :favorite, only: %i[create destroy], controller: "favorites/stores"
  end

  # --- Public (customer) ---
  resources :booths, only: %i[show] do
    member do
      get :enter
      post :enter_as_cast
    end

    resource :favorite, only: %i[create destroy], controller: "favorites/booths"
  end

  resources :stream_sessions, only: [] do
    resources :comments, only: %i[create], module: :stream_sessions
    resources :drink_orders, only: %i[create], module: :stream_sessions
    resources :ivs_participant_tokens, only: %i[create], module: :stream_sessions

    resource :presence, only: [], module: :stream_sessions do
      post :ping
    end

    get :presence_summary, on: :member
  end

  namespace :wallet do
    resources :purchases, only: %i[create]
  end

  get  "/checkout/return", to: "checkout#return"
  post "/webhooks/stripe", to: "webhooks/stripe#create"

  # --- Cast ---
  namespace :cast do
    resource :current_booth, only: %i[create]

    resources :booths, only: %i[index show edit update] do
      get :live, on: :member
      patch :status, on: :member
      resources :stream_sessions, only: %i[create], module: :booths
    end

    resources :stream_sessions, only: [] do
      post :finish, on: :member
      get  :pending_drink_orders, on: :member
    end

    resources :drink_orders, only: [] do
      post :consume, on: :member
    end
  end

  # --- Store Admin ---
  namespace :admin do
    resources :stores, only: %i[index edit update]
    resource :current_store, only: %i[create], controller: "current_stores"

    resources :booths, only: %i[index show new edit create update] do
      member do
        get :watch
        patch :archive
        post :force_end
      end
    end

    resources :drink_items, only: %i[index create update destroy]
    resources :store_bans, only: %i[index create destroy]

    # ★ create（手動追加）をやめる：index + destroy のみ
    resources :casts, only: %i[index destroy] do
      collection do
        post :invite
      end
    end

    get "/cast_metrics", to: "metrics#cast"
  end

  # --- System Admin ---
  namespace :system_admin do
    resources :referral_codes, only: %i[index new create edit update]
    resources :users, only: %i[index new create edit update destroy]
  end
end
```

> `resource :current_store` は既に controller が `CurrentStoresController` なので `controller: "current_stores"` を明示しています（現状 routes との差異が出ないなら省略してOK）。

---

## 4-11. ダッシュボードに admin/casts 導線追加

### app/views/dashboard/show.html.erb（追記箇所だけ示すのではなく、**全文置換が安全**）

以下は **あなたの現行ファイル**に、store_admin/system_admin 両方へ「キャスト管理」リンクを追加した版です。

```erb
<div style="max-width: 960px;">
  <div class="d-flex justify-content-between align-items-start gap-3">
    <div>
      <h1 class="mb-1">ダッシュボード</h1>
      <p class="text-muted mb-0">ロール別リンク</p>
    </div>
  </div>

  <hr class="my-4">

  <% store = layout_current_store %>

  <% if current_user.system_admin? %>
    <h2 class="h5 mb-3">system_admin</h2>

    <ul class="list-group mb-4">
      <li class="list-group-item">
        <%= link_to "ユーザー管理", system_admin_users_path, class: "text-decoration-none" %>
      </li>
      <li class="list-group-item">
        <%= link_to "紹介コード管理", system_admin_referral_codes_path, class: "text-decoration-none" %>
      </li>

      <li class="list-group-item">
        <%= link_to "店舗一覧", admin_stores_path, class: "text-decoration-none" %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "店舗設定", edit_admin_store_path(store), class: "text-decoration-none" %>
        <% else %>
          <%= link_to "店舗設定（先に店舗を選択）", admin_stores_path, class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "ブース一覧", admin_booths_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "ブース一覧（先に店舗を選択）", admin_stores_path, class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "キャスト管理", admin_casts_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "キャスト管理（先に店舗を選択）", admin_stores_path, class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item text-muted">
        ユーザープロフィール編集（未実装）
      </li>
    </ul>

  <% elsif current_user.store_admin? %>
    <h2 class="h5 mb-3">store_admin</h2>

    <ul class="list-group mb-4">
      <li class="list-group-item">
        <%= link_to "店舗一覧", admin_stores_path, class: "text-decoration-none" %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "店舗設定", edit_admin_store_path(store), class: "text-decoration-none" %>
        <% else %>
          <%= link_to "店舗設定（先に店舗を選択）", admin_stores_path, class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "ブース一覧", admin_booths_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "ブース一覧（先に店舗を選択）", admin_stores_path, class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item">
        <% if store.present? %>
          <%= link_to "キャスト管理", admin_casts_path, class: "text-decoration-none" %>
        <% else %>
          <%= link_to "キャスト管理（先に店舗を選択）", admin_stores_path, class: "text-decoration-none" %>
        <% end %>
      </li>

      <li class="list-group-item text-muted">
        ユーザープロフィール編集（未実装）
      </li>
    </ul>

  <% elsif current_user.cast? %>
    <h2 class="h5 mb-3">cast</h2>

    <ul class="list-group mb-4">
      <li class="list-group-item">
        <%= link_to "ブース一覧", cast_booths_path, class: "text-decoration-none" %>
      </li>

      <li class="list-group-item text-muted">
        ユーザープロフィール編集（未実装）
      </li>
    </ul>

  <% else %>
    <h2 class="h5 mb-3">customer</h2>

    <ul class="list-group mb-4">
      <li class="list-group-item text-muted">
        ユーザープロフィール編集（未実装）
      </li>
    </ul>
  <% end %>
</div>
```

---

# 5) テスト（最低限の統合テスト）

> 既存の `fixtures :all` ですが、ここでは **テスト内で必要レコードをcreate**して完結させます。

### test/integration/store_cast_invitation_flow_test.rb（新規）

```ruby
# frozen_string_literal: true

require "test_helper"

class StoreCastInvitationFlowTest < ActionDispatch::IntegrationTest
  test "guest visiting invitation is redirected to login and can sign up cast via invitation" do
    store = Store.create!(name: "Invite Store")
    inviter = User.create!(email: "inviter@example.com", password: "password", role: :store_admin)
    StoreMembership.create!(store: store, user: inviter, membership_role: :admin)

    result = StoreCastInvitations::IssueInvitation.call!(store: store, invited_by_user: inviter, note: "note")
    token = result.token

    get cast_invitation_path(token)
    assert_response :redirect
    assert_match "/users/sign_in", response.location
    assert_match "invite_token=#{token}", response.location

    # 招待経由 cast 新規登録へ
    get cast_sign_up_path(token: token)
    assert_response :ok

    post cast_sign_up_path(token: token), params: {
      cast_registration: {
        email: "new_cast@example.com",
        password: "password",
        password_confirmation: "password"
      }
    }
    assert_response :redirect
    follow_redirect!
    assert_response :ok
    assert_match "キャスト招待", response.body
  end

  test "cast can accept invitation once and membership is created; second accept fails" do
    store = Store.create!(name: "Invite Store")
    inviter = User.create!(email: "inviter2@example.com", password: "password", role: :store_admin)
    StoreMembership.create!(store: store, user: inviter, membership_role: :admin)

    cast_user = User.create!(email: "cast@example.com", password: "password", role: :cast)

    result = StoreCastInvitations::IssueInvitation.call!(store: store, invited_by_user: inviter, note: nil)
    token = result.token

    sign_in cast_user

    post accept_cast_invitation_path(token)
    assert_response :redirect
    follow_redirect!
    assert_response :ok

    assert StoreMembership.exists?(store: store, user: cast_user, membership_role: :cast)

    invitation = StoreCastInvitation.find_by_token(token)
    assert invitation.used?
    assert_equal cast_user.id, invitation.accepted_by_user_id

    # 2回目は不可
    post accept_cast_invitation_path(token)
    assert_response :redirect
    follow_redirect!
    assert_response :ok
  end

  test "non-cast logged in cannot accept invitation" do
    store = Store.create!(name: "Invite Store")
    inviter = User.create!(email: "inviter3@example.com", password: "password", role: :store_admin)
    StoreMembership.create!(store: store, user: inviter, membership_role: :admin)

    customer = User.create!(email: "customer@example.com", password: "password", role: :customer)

    result = StoreCastInvitations::IssueInvitation.call!(store: store, invited_by_user: inviter, note: nil)
    token = result.token

    sign_in customer

    get cast_invitation_path(token)
    assert_response :ok

    post accept_cast_invitation_path(token)
    assert_response :redirect
    follow_redirect!
    assert_response :ok

    assert_not StoreMembership.exists?(store: store, user: customer, membership_role: :cast)
  end

  test "expired invitation cannot be accepted" do
    store = Store.create!(name: "Invite Store")
    inviter = User.create!(email: "inviter4@example.com", password: "password", role: :store_admin)
    StoreMembership.create!(store: store, user: inviter, membership_role: :admin)

    cast_user = User.create!(email: "cast2@example.com", password: "password", role: :cast)

    result = StoreCastInvitations::IssueInvitation.call!(store: store, invited_by_user: inviter, note: nil)
    token = result.token

    invitation = StoreCastInvitation.find_by_token(token)
    invitation.update!(expires_at: 1.minute.ago)

    sign_in cast_user

    post accept_cast_invitation_path(token)
    assert_response :redirect
    follow_redirect!
    assert_response :ok

    assert_not StoreMembership.exists?(store: store, user: cast_user, membership_role: :cast)
  end
end
```

---

# 6) 仕上げの注意点（重要）

## 招待一覧でURLコピーをどうするか

あなたの要求に「URLコピー表示」とありますが、今回 **digest保存**に忠実なので、

* **発行後（notice）に表示されたURLをコピペして控える**
  が基本動作です。

もし「一覧でもURLコピーが欲しい」を満たしたいなら、次のどちらかになります（どちらもセキュリティ/要件の観点で判断が必要）：

1. **発行直後のみ**、index画面内に「最後に発行したURL」を表示（sessionに一時保持）
2. DBに **tokenを平文保存**（要件と逆）

今回は要件優先で 1)も入れていません（最小差分）。必要なら追記できます。

---

# 7) 実装後の確認観点（手動）

* store_admin でログイン → 店舗選択 → `/admin/casts`

  * 招待発行できる（noticeにURL出る）
  * 招待一覧に note/期限/状態が出る
* 未ログインで招待URL → `/users/sign_in?invite_token=...` に飛ぶ

  * ログイン画面に「招待経由cast新規作成」ボタンが出る
* customerでログイン中に招待URL → 「castで承認して」表示
* castでログイン中に招待URL → 承認ボタンが出る

  * 承認後 `root_path` に戻り、store_membership(cast) が作られ、招待は使用済みになる
  * 2回目は不可

---

必要なら、次の追加もすぐ出せます：

* 「発行直後のURLを画面内に保持して“コピー用テキストボックス”で出す」（一覧にURLを出さない方針のまま、使い勝手を上げる）

まずはこのまま実装 → `docker compose exec app bash -lc "bin/rails db:migrate"` → `bin/rails test` で進めてください。

---
---
では、**そのままコピペでいける最小差分**を出します。
（既存構造は壊しません）

---

# ✅ 1️⃣ CastInvitationsController 修正

## `app/controllers/cast_invitations_controller.rb`

`show` を以下のように修正してください。

```ruby
def show
  unless user_signed_in?
    store_location_for(:user, request.fullpath)
    redirect_to new_user_session_path(invite_token: params[:token]), alert: "招待の承認には cast でログインしてください"
    return
  end

  # cast以外は案内だけ表示（承認不可）
  unless current_user.cast?
    render :show, status: :ok
    return
  end

  # --- 追加: すでに在籍しているかチェック ---
  @already_member = StoreMembership.exists?(
    store_id: @invitation.store_id,
    user_id: current_user.id,
    membership_role: :cast
  )

  # 在籍済み かつ 招待がまだ usable なら closed 扱いにする
  if @already_member && @invitation.usable?
    ActiveRecord::Base.transaction do
      @invitation.lock!

      if @invitation.usable?
        @invitation.update!(
          used_at: Time.current,
          accepted_by_user: current_user
        )
      end
    end
  end

  render :show, status: :ok
end
```

---

# ✅ 2️⃣ view 修正

## `app/views/cast_invitations/show.html.erb`

cast ログイン済みブロック内をこの順番に変更してください。

### 🔁 既存のこの部分：

```erb
  <% else %>
    <% if !@invitation.usable? %>
      ...
    <% else %>
      ...
    <% end %>
```

### 🔁 ↓ こう変更

```erb
  <% else %>
    <% if @already_member %>
      <div class="alert alert-success" role="alert">
        この店舗にはすでにキャストとして登録されています。
      </div>

      <div class="d-flex gap-2">
        <%= link_to "トップへ戻る", root_path, class: "btn btn-outline-secondary" %>
      </div>

    <% elsif !@invitation.usable? %>
      <div class="alert alert-danger" role="alert">
        この招待は使用できません（期限切れ / 使用済み）。
      </div>

    <% else %>
      <div class="alert alert-info" role="alert">
        上記の店舗にキャストとして参加します。よろしければ承認してください。
      </div>

      <div class="d-flex gap-2">
        <%= button_to "承認する",
              accept_cast_invitation_path(params[:token]),
              method: :post,
              class: "btn btn-primary",
              data: { turbo_confirm: "この店舗（#{@invitation.store.name}）のキャスト招待を承認しますか？" } %>

        <%= link_to "キャンセル", root_path, class: "btn btn-outline-secondary" %>
      </div>
    <% end %>
```

---

# 🎯 これで動作はこうなります

### ■ 既に在籍している cast がアクセス

* 承認ボタン出ない
* 「すでに登録されています」表示
* 招待は `used_at` 更新され closed 扱い

### ■ 未在籍 cast

* 承認ボタン表示
* POST → membership作成 + 招待 closed

### ■ 期限切れ / 使用済み

* エラー表示

---

# 🔒 安全性について

* transaction + lock で二重更新防止
* usable? を二重チェック（競合安全）
* AcceptInvitation 側も冪等なので二重POSTでも壊れない

設計的にもかなり綺麗です。

---

もし余裕があれば、次は：

* 招待一覧画面で status を「在籍済みにより自動クローズ」など表示分岐する
* store_admin 側の UI を整理

などもできます。

進めますか？
