以下の方針で差分です。
最新ファイル前提です。 

## 1. ブランチ作成

```bash
git switch -c feature/header-subnav-home-favorites
```

## 2. 変更対象

```text
app/views/layouts/application.html.erb
app/views/layouts/_header.html.erb
app/views/home/show.html.erb
app/views/home/_header_subnav.html.erb
app/views/favorites/_header_subnav.html.erb
app/views/favorites/booths/index.html.erb
app/views/favorites/stores/index.html.erb
app/views/favorites/users/index.html.erb
app/controllers/favorites/booths_controller.rb
app/controllers/favorites/stores_controller.rb
app/controllers/favorites/users_controller.rb
app/assets/stylesheets/components/_layout.scss
```

---

## 3. `app/views/layouts/application.html.erb`

`fixed_subnav_layout` 判定を削除して、通常ページは `_default_main` に戻します。

```erb
<% cast_live_layout = (controller_path == "cast/booths" && action_name == "live") %>
<% viewer_layout = (controller_path == "booths" && action_name == "show") %>

<body class="<%= [ "d-flex", "flex-column", "min-vh-100", ("cast-live-layout" if cast_live_layout), ("viewer-layout" if viewer_layout) ].compact.join(" ") %>">
  <%= render "layouts/header" %>

  <% if cast_live_layout %>
    <%= render "layouts/cast_live_main" %>
  <% elsif viewer_layout %>
    <%= render "layouts/viewer_main" %>
  <% else %>
    <%= render "layouts/default_main" %>
  <% end %>

  <%# --- Global Modal Frame (common) --- %>
  <turbo-frame
    id="modal"
    data-controller="modal"
    data-action="turbo:frame-load->modal#open">
  </turbo-frame>
</body>
```

---

## 4. `app/views/layouts/_header.html.erb`

一番下、`</header>` の直前に追加してください。

```erb
  <% header_subnav_layout =
       user_signed_in? &&
       (
         (controller_path == "home" && action_name == "show") ||
         controller_path.start_with?("favorites/")
       ) %>

  <% if header_subnav_layout %>
    <div class="border-top header-subnav">
      <div class="container py-2">
        <% if controller_path == "home" && action_name == "show" %>
          <%= render "home/header_subnav" %>
        <% elsif controller_path.start_with?("favorites/") %>
          <%= render "favorites/header_subnav" %>
        <% end %>
      </div>
    </div>
  <% end %>
</header>
```

---

## 5. 新規 `app/views/home/_header_subnav.html.erb`

```erb
<div class="d-flex flex-column gap-2">
  <div class="btn-group w-100" role="group" aria-label="home表示切り替え">
    <%= link_to "ブース",
          root_path(mode: "booths", q: @q.presence),
          class: ["btn", (@mode == "booths" ? "btn-secondary active" : "btn-outline-secondary")],
          "aria-current": (@mode == "booths" ? "page" : nil) %>

    <%= link_to "店舗",
          root_path(mode: "stores", q: @q.presence),
          class: ["btn", (@mode == "stores" ? "btn-secondary active" : "btn-outline-secondary")],
          "aria-current": (@mode == "stores" ? "page" : nil) %>

    <%= link_to "ユーザー",
          root_path(mode: "users", q: @q.presence),
          class: ["btn", (@mode == "users" ? "btn-secondary active" : "btn-outline-secondary")],
          "aria-current": (@mode == "users" ? "page" : nil) %>
  </div>

  <%= form_with url: root_path, method: :get, scope: nil do |f| %>
    <div class="input-group">
      <%= f.hidden_field :mode, value: @mode %>

      <%= f.text_field :q,
            value: @q,
            class: "form-control",
            placeholder: "キーワード",
            autocomplete: "off" %>

      <button class="btn btn-primary" type="submit" aria-label="検索">
        <i class="bi bi-search"></i>
      </button>
    </div>
  <% end %>
</div>
```

---

## 6. 新規 `app/views/favorites/_header_subnav.html.erb`

```erb
<div class="d-flex flex-column gap-2">
  <div class="btn-group w-100" role="group" aria-label="お気に入り表示切り替え">
    <%= link_to "ブース",
          favorites_booths_path(q: @q.presence),
          class: ["btn", (controller_path == "favorites/booths" ? "btn-secondary active" : "btn-outline-secondary")],
          "aria-current": (controller_path == "favorites/booths" ? "page" : nil) %>

    <%= link_to "店舗",
          favorites_stores_path(q: @q.presence),
          class: ["btn", (controller_path == "favorites/stores" ? "btn-secondary active" : "btn-outline-secondary")],
          "aria-current": (controller_path == "favorites/stores" ? "page" : nil) %>

    <%= link_to "ユーザー",
          favorites_users_path(q: @q.presence),
          class: ["btn", (controller_path == "favorites/users" ? "btn-secondary active" : "btn-outline-secondary")],
          "aria-current": (controller_path == "favorites/users" ? "page" : nil) %>
  </div>

  <%= form_with url: request.path, method: :get, scope: nil do |f| %>
    <div class="input-group">
      <%= f.text_field :q,
            value: @q,
            class: "form-control",
            placeholder: "お気に入りを検索",
            autocomplete: "off" %>

      <button class="btn btn-primary" type="submit" aria-label="検索">
        <i class="bi bi-search"></i>
      </button>
    </div>
  <% end %>
</div>
```

---

## 7. `app/views/home/show.html.erb`

冒頭の `content_for :fixed_subnav...` 全部削除。
つまり、ログイン時の最初はこうなればOKです。

```erb
<% if user_signed_in? %>
  <%= render "install_prompt" %>

  <%# --- Results (cards) --- %>
```

---

## 8. favorites 各 index から fixed_subnav 削除

3ファイル共通で、冒頭をこれだけにします。

```erb
<% content_for :title, "お気に入り" %>
```

削除するもの：

```erb
<% content_for :fixed_subnav_body_class, "page-with-fixed-subnav" %>

<% content_for :fixed_subnav do %>
  ...
<% end %>
```

---

## 9. `app/controllers/favorites/booths_controller.rb`

`index` の先頭に検索を追加します。

```ruby
def index
  @q = params[:q].to_s.strip

  scope =
    current_user
      .favorite_booths
      .joins(booth: :store)
      .merge(Booth.active)
      .includes(
        booth: [
          :store,
          :current_stream_session,
          { booth_casts: :cast_user },
          { thumbnail_image_attachment: :blob }
        ]
      )
      .order(created_at: :desc, id: :desc)

  if @q.present?
    q_like = "%#{ActiveRecord::Base.sanitize_sql_like(@q)}%"
    scope = scope.where("booths.name ILIKE :q OR booths.description ILIKE :q OR stores.name ILIKE :q", q: q_like)
  end
```

以降は既存のBAN除外からそのままでOKです。

---

## 10. `app/controllers/favorites/stores_controller.rb`

`index` の先頭をこうします。

```ruby
def index
  @q = params[:q].to_s.strip

  scope =
    current_user
      .favorite_stores
      .joins(:store)
      .includes(:store)
      .order(created_at: :desc, id: :desc)

  if @q.present?
    q_like = "%#{ActiveRecord::Base.sanitize_sql_like(@q)}%"
    scope =
      scope.where(
        "stores.name ILIKE :q OR stores.description ILIKE :q OR stores.area ILIKE :q OR stores.address ILIKE :q",
        q: q_like
      )
  end
```

以降は既存のBAN除外からそのままでOKです。

---

## 11. `app/controllers/favorites/users_controller.rb`

`index` を置き換え。

```ruby
def index
  @q = params[:q].to_s.strip

  scope =
    current_user
      .favorite_users
      .joins(:target_user)
      .includes(target_user: [ { avatar_attachment: :blob } ])
      .order(created_at: :desc, id: :desc)

  if @q.present?
    q_like = "%#{ActiveRecord::Base.sanitize_sql_like(@q)}%"
    scope = scope.where("users.display_name ILIKE :q OR users.bio ILIKE :q", q: q_like)
  end

  @favorite_users = scope
  @favorite_user_ids = @favorite_users.map(&:target_user_id).to_set
end
```

---

## 12. `app/assets/stylesheets/components/_layout.scss`

削除してOK。

```scss
.fixed-subnav {
  position: fixed;
  top: 62px;
  left: 0;
  right: 0;
  z-index: 1010;
  background: var(--bs-body-bg);
}

.fixed-subnav--with-form {
  .btn-group {
    width: 100%;
  }
}

.home-with-fixed-subnav {
  padding-top: 96px;
}

.page-with-fixed-subnav {
  padding-top: 56px;
}
```

この media 内も削除。

```scss
@media (max-width: 767.98px) {
  .home-with-fixed-subnav {
    padding-top: 92px;
  }

  .page-with-fixed-subnav {
    padding-top: 52px;
  }

  .fixed-subnav {
    top: 56px;
  }
}
```

追加は最小でこれ。

```scss
.header-subnav {
  background: var(--bs-body-bg);
}
```

---

## 確認コマンド

```bash
docker compose exec app bin/rails test
```

手動確認は最低これです。

```text
/
/
/?mode=stores
/?mode=users
/?mode=booths&q=テスト
/favorites/booths
/favorites/stores
/favorites/users
```

他ページで下段ナビが出なければOKです。
