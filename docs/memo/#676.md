では、**そのまま順に実装できる形**で、ファイルごとの修正内容をまとめます。
今回は **既存機能を壊さない最小差分** を優先しています。

---

# 1. マイグレーション作成

コマンド:

```bash
docker compose exec app bin/rails g migration AddOnboardingFieldsToStores
```

生成されたファイルを全文置き換え:

```ruby
# frozen_string_literal: true

class AddOnboardingFieldsToStores < ActiveRecord::Migration[8.1]
  def change
    add_column :stores, :onboarding_step, :integer
    add_column :stores, :onboarding_invite_copied_at, :datetime

    add_index :stores, :onboarding_step
    add_index :stores, :onboarding_invite_copied_at
  end
end
```

---

# 2. `app/models/store.rb`

全文ではなく、**追記・変更箇所**です。

## 2-1. 関連追加

`has_many` 群に追加:

```ruby
  has_many :store_cast_invitations, dependent: :destroy
  has_many :store_admin_invitations, dependent: :destroy
```

## 2-2. enum 追加

`business_type` enum の下あたりに追加:

```ruby
  enum :onboarding_step, {
    invite_cast: 0,
    create_invite: 1,
    setup_drinks: 2,
    completed: 3,
    skipped: 4
  }, prefix: true
```

## 2-3. メソッド追加

`payout_account_last4` の下あたりに追加:

```ruby
  def onboarding_active?
    onboarding_step.present? && !onboarding_completed? && !onboarding_skipped?
  end

  def advance_onboarding_to_create_invite!
    return unless onboarding_invite_cast?

    update!(onboarding_step: :create_invite)
  end

  def advance_onboarding_to_setup_drinks!
    return unless onboarding_create_invite?
    return if onboarding_invite_copied_at.blank?

    update!(onboarding_step: :setup_drinks)
  end

  def mark_onboarding_invite_copied!
    return unless onboarding_create_invite?

    update!(onboarding_invite_copied_at: Time.current)
    advance_onboarding_to_setup_drinks!
  end

  def complete_onboarding!
    return unless onboarding_setup_drinks?

    update!(onboarding_step: :completed)
  end

  def skip_onboarding!
    return unless onboarding_active?

    update!(onboarding_step: :skipped)
  end
```

---

# 3. `app/services/stores/register_store_admin.rb`

`Store.create!` の部分だけ変更:

```ruby
        store = Store.create!(
          name: @store_name,
          referral_code: rc,
          onboarding_step: :invite_cast
        )
```

---

# 4. `config/routes.rb`

`namespace :admin do` の中に追加:

```ruby
    resource :onboarding, only: [] do
      post :skip
      post :cast_invitation_copied
    end
```

追加位置は `resource :current_store` の下あたりで大丈夫です。

---

# 5. `app/controllers/admin/onboardings_controller.rb` を新規作成

```ruby
# frozen_string_literal: true

module Admin
  class OnboardingsController < Admin::BaseController
    before_action :require_current_store!

    def skip
      current_store.skip_onboarding!
      head :ok
    end

    def cast_invitation_copied
      if current_store.store_cast_invitations.exists?
        current_store.mark_onboarding_invite_copied!
      end

      head :ok
    end
  end
end
```

---

# 6. `app/controllers/admin/cast_invitations_controller.rb`

`index` の先頭で step を進めます。

修正版:

```ruby
# frozen_string_literal: true

module Admin
  class CastInvitationsController < Admin::BaseController
    before_action :require_current_store!

    def index
      current_store.advance_onboarding_to_create_invite!

      @store_cast_invitations =
        StoreCastInvitation
          .includes(:invited_by_user, :accepted_by_user)
          .where(store_id: current_store.id)
          .recent_first

      @new_invitation = StoreCastInvitation.new
    end

    def create
      note = params.require(:store_cast_invitation).permit(:note)[:note]

      result =
        StoreCastInvitations::IssueInvitation.call!(
          store: current_store,
          invited_by_user: current_user,
          note: note
        )

      token = result.token
      url = cast_invitation_url(token)

      result.invitation.update!(issued_url: url)

      redirect_to admin_cast_invitations_path,
                  notice: "招待を発行しました: #{url}"
    rescue ActionController::ParameterMissing
      redirect_to admin_cast_invitations_path, alert: "パラメータが不正です"
    rescue ActiveRecord::RecordInvalid => e
      redirect_to admin_cast_invitations_path, alert: e.record.errors.full_messages.join(", ")
    end
  end
end
```

---

# 7. `app/controllers/admin/drink_items_controller.rb`

成功時に onboarding 完了を入れます。

修正版:

```ruby
# frozen_string_literal: true

module Admin
  class DrinkItemsController < BaseController
    before_action :require_current_store!
    before_action :set_drink_item, only: %i[update destroy]

    def index
      @drink_item = current_store.drink_items.new(enabled: true, position: default_position)
      @drink_items = current_store.drink_items.ordered
      @editing_drink_item_id = params[:editing_id].to_i if params[:editing_id].present?
    end

    def create
      @drink_item = current_store.drink_items.new(drink_item_params)

      if @drink_item.save
        current_store.complete_onboarding!
        redirect_to admin_drink_items_path, notice: "作成しました"
      else
        @drink_items = current_store.drink_items.ordered
        @editing_drink_item_id = nil
        render :index, status: :unprocessable_entity
      end
    end

    def update
      if @drink_item.update(drink_item_params)
        current_store.complete_onboarding!
        redirect_to admin_drink_items_path, notice: "更新しました"
      else
        @drink_items = current_store.drink_items.ordered
        @editing_drink_item_id = @drink_item.id
        @drink_item = current_store.drink_items.new(enabled: true, position: default_position)
        render :index, status: :unprocessable_entity
      end
    end

    def destroy
      @drink_item.update!(enabled: false) # 論理削除
      redirect_to admin_drink_items_path, notice: "無効にしました"
    end

    private

    def set_drink_item
      @drink_item = current_store.drink_items.find(params[:id])
    end

    def drink_item_params
      params.require(:drink_item).permit(:name, :price_points, :position, :enabled, :icon_key)
    end

    def default_position
      last = current_store.drink_items.maximum(:position)
      last.present? ? last + 1 : 0
    end
  end
end
```

---

# 8. `app/views/dashboard/show.html.erb`

最初の `store` / `booth` 定義の直下に追加:

```erb
  <% if store&.onboarding_active? %>
    <div
      data-controller="onboarding"
      data-onboarding-step-value="<%= store.onboarding_step %>"
      data-onboarding-skip-url-value="<%= skip_admin_onboarding_path %>">
    </div>
  <% end %>
```

---

## 8-1. キャスト招待カードに data 属性追加

store が present の分岐をこう変更:

```erb
        <% if store.present? %>
          <%= link_to admin_cast_invitations_path,
                      class: "text-decoration-none",
                      data: { onboarding_target_element: "invite-cast-card" } do %>
            <div class="card h-100 dashboard-role-card dashboard-role-card-store-admin" data-bs-theme="light">
              <div class="card-body">
                <h5 class="card-title mb-2">キャスト招待</h5>
                <p class="card-text mb-0">キャスト招待URLの発行と確認を行います</p>
              </div>
            </div>
          <% end %>
```

---

## 8-2. ドリンクメニューカードに data 属性追加

store が present の分岐をこう変更:

```erb
        <% if store.present? %>
          <%= link_to admin_drink_items_path,
                      class: "text-decoration-none",
                      data: { onboarding_target_element: "setup-drinks-card" } do %>
            <div class="card h-100 dashboard-role-card dashboard-role-card-store-admin" data-bs-theme="light">
              <div class="card-body">
                <h5 class="card-title mb-2">ドリンクメニュー</h5>
                <p class="card-text mb-0">ドリンクメニューを管理します</p>
              </div>
            </div>
          <% end %>
```

---

# 9. `app/views/admin/cast_invitations/index.html.erb`

## 9-1. メモ欄に data 属性追加

```erb
<%= f.text_area :note,
      class: "form-control",
      rows: 2,
      placeholder: "例）◯◯さん用 / SNSのアカウント名など",
      data: { onboarding_target_element: "invite-note-field" } %>
```

## 9-2. 発行ボタンに data 属性追加

```erb
<%= f.submit "招待URLを発行",
      class: "btn btn-primary",
      data: { onboarding_target_element: "issue-invite-button" } %>
```

## 9-3. コピーボタンに data 属性追加

ボタンをこう変更:

```erb
<button
  type="button"
  class="btn btn-outline-secondary btn-sm referral-code-copy-btn"
  data-controller="clipboard"
  data-action="click->clipboard#copy"
  data-clipboard-text="<%= inv.issued_url %>"
  data-clipboard-success-message="招待URLをコピーしました"
  data-clipboard-failure-message="招待URLのコピーに失敗しました"
  data-clipboard-onboarding-copy-url-value="<%= cast_invitation_copied_admin_onboarding_path %>"
  data-onboarding-target-element="copy-invite-button"
  aria-label="招待URLをコピー"
  title="招待URLをコピー"
>
```

---

# 10. `app/views/admin/drink_items/_form.html.erb`

submit ボタンに data 属性を追加します。

```erb
<%= f.submit submit_label,
      class: "btn btn-sm btn-primary",
      data: {
        onboarding_target_element: (method.to_s == "post" ? "create-drink-submit" : "update-drink-submit")
      } %>
```

---

# 11. `app/javascript/controllers/clipboard_controller.js`

全文置き換えでどうぞ。

```javascript
import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  async copy(event) {
    event.preventDefault()

    const text = this.element.dataset.clipboardText
    if (!text) return

    const successMessage = this.element.dataset.clipboardSuccessMessage || "コピーしました"
    const failureMessage = this.element.dataset.clipboardFailureMessage || "コピー失敗"

    this.element.disabled = true

    try {
      if (navigator.clipboard && navigator.clipboard.writeText) {
        await navigator.clipboard.writeText(text)
      } else {
        const ta = document.createElement("textarea")
        ta.value = text
        ta.setAttribute("readonly", "")
        ta.style.position = "absolute"
        ta.style.left = "-9999px"
        document.body.appendChild(ta)
        ta.select()
        document.execCommand("copy")
        document.body.removeChild(ta)
      }

      await this.notifyOnboardingCopyIfNeeded()
      this.showFlash("success", successMessage)
    } catch (_e) {
      this.showFlash("danger", failureMessage)
    } finally {
      window.setTimeout(() => {
        this.element.disabled = false
      }, 300)
    }
  }

  async notifyOnboardingCopyIfNeeded() {
    const url = this.element.dataset.clipboardOnboardingCopyUrlValue
    if (!url) return

    const csrfToken = document.querySelector('meta[name="csrf-token"]')?.content
    if (!csrfToken) return

    await fetch(url, {
      method: "POST",
      headers: {
        "X-CSRF-Token": csrfToken,
        "Accept": "text/plain"
      },
      credentials: "same-origin"
    })
  }

  showFlash(level, message) {
    const flashInner = document.getElementById("flash_inner")
    if (!flashInner) return

    const wrapper = document.createElement("div")
    wrapper.innerHTML = `
      <div class="alert alert-${level} alert-dismissible fade show" role="alert">
        ${this.escapeHtml(message)}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="閉じる"></button>
      </div>
    `.trim()

    const flashElement = wrapper.firstElementChild
    flashInner.replaceChildren(flashElement)
  }

  escapeHtml(value) {
    const div = document.createElement("div")
    div.textContent = value
    return div.innerHTML
  }
}
```

---

# 12. `app/javascript/controllers/onboarding_controller.js` を新規作成

```javascript
import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  static values = {
    step: String,
    skipUrl: String
  }

  connect() {
    this.highlightedElement = null
    this.bubbleElement = null
    this.render()
  }

  disconnect() {
    this.removeHighlight()
    this.removeBubble()
  }

  render() {
    const config = this.stepConfig()
    if (!config) return

    const target = document.querySelector(`[data-onboarding-target-element="${config.target}"]`)
    if (!target) return

    this.applyHighlight(target)
    this.showBubble(target, config.message, config.showSkip !== false)
  }

  stepConfig() {
    switch (this.stepValue) {
      case "invite_cast":
        return {
          target: "invite-cast-card",
          message: "キャストを招待しましょう。このボタンを押してください。"
        }
      case "create_invite":
        if (document.querySelector('[data-onboarding-target-element="copy-invite-button"]')) {
          return {
            target: "copy-invite-button",
            message: "このボタンでURLをコピーできます。LINEなどで送信してください。"
          }
        }

        if (document.querySelector('[data-onboarding-target-element="issue-invite-button"]')) {
          return {
            target: "issue-invite-button",
            message: "招待URLを発行しましょう。"
          }
        }

        return {
          target: "invite-note-field",
          message: "招待を区別しやすいようにメモを入力できます（任意）。"
        }
      case "setup_drinks":
        if (document.querySelector('[data-onboarding-target-element="create-drink-submit"]')) {
          return {
            target: "create-drink-submit",
            message: "まずは1件、編集または追加してみましょう。"
          }
        }

        if (document.querySelector('[data-onboarding-target-element="update-drink-submit"]')) {
          return {
            target: "update-drink-submit",
            message: "まずは1件、編集または追加してみましょう。"
          }
        }

        return {
          target: "setup-drinks-card",
          message: "ドリンク設定を確認しましょう。最初から基本のドリンクが登録されています。"
        }
      default:
        return null
    }
  }

  applyHighlight(element) {
    this.removeHighlight()
    this.highlightedElement = element
    this.highlightedElement.classList.add("tutorial-highlight")
  }

  removeHighlight() {
    if (!this.highlightedElement) return
    this.highlightedElement.classList.remove("tutorial-highlight")
    this.highlightedElement = null
  }

  showBubble(target, message, showSkip) {
    this.removeBubble()

    const bubble = document.createElement("div")
    bubble.className = "tutorial-bubble"
    bubble.innerHTML = `
      <button type="button" class="tutorial-bubble-close" aria-label="閉じる">×</button>
      <div class="tutorial-bubble-body">${message}</div>
      ${showSkip ? '<button type="button" class="btn btn-sm btn-outline-secondary tutorial-skip-button">チュートリアルをスキップ</button>' : ""}
    `

    document.body.appendChild(bubble)
    this.bubbleElement = bubble

    const rect = target.getBoundingClientRect()
    bubble.style.top = `${window.scrollY + rect.bottom + 12}px`
    bubble.style.left = `${window.scrollX + Math.max(16, rect.left)}px`

    bubble.querySelector(".tutorial-bubble-close")?.addEventListener("click", () => {
      this.removeBubble()
    })

    bubble.querySelector(".tutorial-skip-button")?.addEventListener("click", () => {
      this.skip()
    })
  }

  removeBubble() {
    if (!this.bubbleElement) return
    this.bubbleElement.remove()
    this.bubbleElement = null
  }

  async skip() {
    if (!this.skipUrlValue) return

    const csrfToken = document.querySelector('meta[name="csrf-token"]')?.content
    if (!csrfToken) return

    await fetch(this.skipUrlValue, {
      method: "POST",
      headers: {
        "X-CSRF-Token": csrfToken,
        "Accept": "text/plain"
      },
      credentials: "same-origin"
    })

    this.removeHighlight()
    this.removeBubble()
  }
}
```

`index.js` は触らなくて大丈夫です。
今の eager load 方式で自動読込されます。

---

# 13. CSS 追加

`app/assets/stylesheets/components/_dashboard.scss` の末尾あたりに追加してください。

```scss
.tutorial-highlight {
  position: relative;
  z-index: 1040;
  box-shadow: 0 0 0 4px rgba(160, 120, 255, 0.5);
  border-radius: 8px;
}

.tutorial-bubble {
  position: absolute;
  z-index: 1050;
  max-width: 320px;
  padding: 12px;
  border-radius: 12px;
  background: #fff;
  color: #212529;
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.18);
}

.tutorial-bubble-close {
  border: 0;
  background: transparent;
  font-size: 18px;
  line-height: 1;
  float: right;
}

.tutorial-bubble-body {
  margin-bottom: 12px;
  white-space: normal;
}
```

---

# 14. 実装後にやること

順番はこれです。

```bash
docker compose exec app bin/rails db:migrate
docker compose exec app bin/rails test
```

---

# 15. 先に分かっている注意点

今回のままだと、Step2 の吹き出しは

* 招待画面に初めて来た時は「メモ欄」
* 発行後は「コピー」
* 画面再訪時にコピーボタンがあれば「コピー」

という挙動になります。
Issueの「段階的案内」に対しては、まず十分実用的です。

---

# 16. 最後に一つだけ

このまま実装は進められますが、**view 側で data 属性を付けた位置が想定と違うと吹き出し位置が少しズレる**ことはあります。
その場合は、あとで data 属性をカード本体やボタン本体に少し寄せれば直せます。

必要なら次で、**実装後に想定されるテスト項目** もそのまま書ける形で出します。
