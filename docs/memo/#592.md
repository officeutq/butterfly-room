了解です。
そのまま置き換えやすいように、**変更後の全文**をファイルごとにまとめて出します。
以下は、あなたが貼ってくれた最新ファイルを前提にした完成版です。

---

## 1. `app/views/layouts/_header.html.erb`

```erb
<%# --- Header (common) --- %>
<header id="app_header" class="border-bottom bg-body sticky-top">
  <div class="container-fluid app-header">
    <div class="d-flex justify-content-between align-items-center gap-3 app-header-inner">
      <%# 左領域 %>
      <div class="d-flex align-items-center gap-2">
        <div class="fw-semibold">
          <%= image_tag "logo.png", class: "header-logo", alt: (content_for(:title).presence || "App") %>
        </div>
      </div>

      <%# 右領域 %>
      <div class="d-flex align-items-center gap-2 header-right">
        <% if user_signed_in? %>
          <%= turbo_stream_from [current_user, :wallet] %>

          <%= link_to new_wallet_purchase_path(return_to: request.fullpath),
                data: { turbo_frame: "modal" },
                class: "header-wallet text-decoration-none d-inline-flex align-items-center gap-1",
                id: "wallet_balance" do %>
            <%= render "wallets/balance", wallet: current_user.wallet %>
          <% end %>

          <% unless current_user.customer? %>
            <span class="badge text-bg-secondary">
              <%= header_role_label(current_user) %>
            </span>
          <% end %>

          <div class="dropdown">
            <button
              class="btn btn-sm header-trigger d-flex align-items-center gap-2"
              type="button"
              data-bs-toggle="dropdown"
              aria-expanded="false">
              <%= user_avatar_badge(current_user, size: 24) %>

              <span class="d-none d-md-inline-block header-trigger-name text-truncate">
                <%= display_name_or_anonymous(current_user) %>
              </span>

              <span class="d-none d-lg-inline-block header-trigger-email text-truncate">
                <%= current_user.email %>
              </span>
            </button>

            <ul class="dropdown-menu dropdown-menu-end">
              <% if current_user.customer? %>
                <li class="d-md-none">
                  <span class="dropdown-item-text small">
                    <%= display_name_or_anonymous(current_user) %>
                  </span>
                </li>

                <li class="d-lg-none">
                  <span class="dropdown-item-text small">
                    <%= current_user.email %>
                  </span>
                </li>
              <% else %>
                <li class="d-md-none">
                  <span class="dropdown-item-text small">
                    <%= display_name_or_anonymous(current_user) %>
                  </span>
                </li>

                <li class="d-lg-none">
                  <span class="dropdown-item-text small">
                    <%= current_user.email %>
                  </span>
                </li>

                <% store = layout_current_store %>
                <li>
                  <span class="dropdown-item-text small">
                    store:
                    <% if current_user.at_least?(:store_admin) %>
                      <%= link_to select_modal_admin_stores_path(return_to: request.fullpath),
                            data: { turbo_frame: "modal" },
                            class: "link-secondary link-underline-opacity-0 link-underline-opacity-100-hover" do %>
                        <% if store.present? %>
                          <strong><%= store.name.presence || "(no name)" %></strong>
                        <% else %>
                          <span class="text-danger">未選択</span>
                        <% end %>
                      <% end %>
                    <% else %>
                      <% if store.present? %>
                        <strong><%= store.name.presence || "(no name)" %></strong>
                      <% else %>
                        <span class="text-danger">未選択</span>
                      <% end %>
                    <% end %>
                  </span>
                </li>

                <% booth = layout_current_booth %>
                <li>
                  <span class="dropdown-item-text small">
                    booth:
                    <%= link_to select_modal_cast_booths_path(return_to: request.fullpath),
                          data: { turbo_frame: "modal" },
                          class: "link-secondary link-underline-opacity-0 link-underline-opacity-100-hover" do %>
                      <% if booth.present? %>
                        <strong><%= booth.name.presence || "(no name)" %></strong>
                      <% else %>
                        <span class="text-danger">未選択</span>
                      <% end %>
                    <% end %>
                  </span>
                </li>
              <% end %>

              <li><hr class="dropdown-divider"></li>

              <li>
                <span class="dropdown-item-text small">
                  <%= current_build_label %>
                </span>
              </li>

              <li><hr class="dropdown-divider"></li>

              <li>
                <%= button_to destroy_user_session_path,
                      method: :delete,
                      class: "dropdown-item text-danger d-flex align-items-center gap-2" do %>
                  <i class="bi bi-box-arrow-right"></i>
                  <span>ログアウト</span>
                <% end %>
              </li>
            </ul>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</header>
```

---

## 2. `app/helpers/application_helper.rb`

```ruby
# frozen_string_literal: true

module ApplicationHelper
  def booth_status_badge(booth)
    return "" if booth.blank?

    live_like = booth.live? || booth.away?

    label = live_like ? "配信中" : "オフライン"
    klass = live_like ? "text-bg-danger" : "text-bg-secondary"

    content_tag(:span, label, class: "badge #{klass}")
  end

  def display_name_or_anonymous(user)
    return "" if user.blank?

    user.display_name.presence || "ななしさん"
  end

  def display_name_or_email(user)
    return "" if user.blank?

    user.display_name.presence || user.email
  end

  def user_avatar_badge(user, size: 24, klass: "")
    return "" if user.blank?

    if user.avatar.attached?
      image_tag(
        user.avatar,
        class: [ "rounded-circle border", klass ].join(" ").strip,
        style: "width: #{size}px; height: #{size}px; object-fit: cover;",
        alt: "avatar"
      )
    else
      label = display_name_or_anonymous(user).to_s
      initial = label.strip.presence ? label.strip[0] : "?"
      content_tag(
        :span,
        initial,
        class: [ "rounded-circle border text-muted d-inline-flex align-items-center justify-content-center", klass ].join(" ").strip,
        style: "width: #{size}px; height: #{size}px; font-size: #{(size * 0.45).round}px; line-height: 1;",
        aria: { label: "avatar" },
        title: label
      )
    end
  end

  # --- Layout helpers (Issue #212) ---

  def role_badge(user)
    return "" if user.blank?

    label = user.role.to_s
    content_tag(:span, label, class: "badge text-bg-secondary")
  end

  def header_role_label(user)
    return "" if user.blank?
    return "" if user.customer?

    case user.role.to_sym
    when :cast
      "CAST"
    when :store_admin
      "STORE"
    when :system_admin
      "ADMIN"
    else
      user.role.to_s.upcase
    end
  end

  def dashboard_path_for(_user)
    dashboard_path
  end

  # 参照表示用：可能な範囲で current_store / current_booth を解決する（副作用なし）
  def layout_current_store
    return nil unless user_signed_in?

    # admin配下では既存の整合ロジックを優先
    return current_store if respond_to?(:current_store)

    # ★booth優先（07設計どおり）
    booth = layout_current_booth
    return booth.store if booth.present?

    # fallback: session store（参照のみ）
    store_id = session[:current_store_id]
    return nil if store_id.blank?

    store = Store.find_by(id: store_id)
    return nil if store.blank?

    return store if current_user.system_admin?
    return store if current_user.at_least?(:store_admin) && current_user.admin_of_store?(store.id)

    nil
  end

  def layout_current_booth
    return nil unless user_signed_in?

    # cast配下では既存メソッドがあるのでそれを優先（操作可能チェックも内包）
    return current_booth if respond_to?(:current_booth)

    booth_id = session[:current_booth_id]
    return nil if booth_id.blank?

    booth = Booth.find_by(id: booth_id)
    return nil if booth.blank?

    # system_admin は参照のみOK
    return booth if current_user.system_admin?

    # store_admin は自分の store の booth を参照OK
    if current_user.at_least?(:store_admin) && current_user.admin_of_store?(booth.store_id)
      return booth
    end

    # cast は所属 booth のみ参照OK
    return booth if current_user.at_least?(:cast) && BoothCast.exists?(cast_user_id: current_user.id, booth_id: booth.id)

    nil
  end

  def layout_wallet_points
    return 0 unless user_signed_in?

    current_user.wallet&.available_points || 0
  end

  def enter_booth_switch_confirm_message(target_booth)
    return nil unless user_signed_in?
    return nil unless current_user.at_least?(:cast)
    return nil if target_booth.blank?

    current = layout_current_booth
    return nil if current.blank?
    return nil if current.id == target_booth.id
    return nil unless current.live? || current.away?

    "配信中（または離席中）のブースから切り替えます。よろしいですか？"
  end
end
```

---

## 3. `app/views/wallets/_balance.html.erb`

```erb
<%# locals: wallet: %>
<i class="bi bi-p-circle-fill text-warning"></i>
<strong><%= wallet&.available_points || 0 %>pt</strong>
```

---

## 4. `app/assets/stylesheets/application.scss`

```scss
@use "bootstrap/scss/bootstrap" as *;

:root {
  --live-stage-h: 0px;
  --viewer-stage-h: 0px;
  --app-header-h: 0px;
  --app-footer-h: 0px;
}

@import "components/layout";
@import "components/header";
@import "components/broadcast_layout";
@import "components/broadcast_meta";
@import "components/broadcast_drink";
@import "components/broadcast_comment";
@import "components/broadcast_ops";
@import "components/live";
@import "components/viewer";
@import "components/banuba_verification";
@import "components/drink_item";
@import "components/dashboard";
@import "components/booths_index";
@import "components/booths_card";
@import "components/stores_card";
```

---

## 5. 新規作成 `app/assets/stylesheets/components/_header.scss`

```scss
.app-header {
  padding-left: 1rem;
  padding-right: 1rem;
}

.app-header-inner {
  min-height: 64px;
  width: 100%;
}

.header-right {
  min-width: 0;
  white-space: nowrap;
  flex-wrap: nowrap;
}

.header-wallet {
  color: var(--bs-secondary-color);
  min-width: 0;
  white-space: nowrap;
}

.header-trigger {
  background: transparent;
  border: none;
  color: var(--bs-body-color);
  min-width: 0;
  max-width: 100%;
  white-space: nowrap;
}

.header-trigger:hover,
.header-trigger:focus,
.header-trigger:active,
.show > .header-trigger {
  background: rgba(0, 0, 0, 0.05);
  color: var(--bs-body-color);
  border: none;
  box-shadow: none;
}

.header-trigger-name,
.header-trigger-email {
  min-width: 0;
  vertical-align: middle;
}

.header-trigger-name {
  max-width: 120px;
}

.header-trigger-email {
  max-width: 180px;
  color: var(--bs-secondary-color);
}

.dropdown-menu .button_to {
  margin: 0;
}

@media (max-width: 767.98px) {
  .app-header {
    padding-left: 0.75rem;
    padding-right: 0.75rem;
  }

  .app-header-inner {
    min-height: 56px;
  }
}
```

---

## 6. `app/assets/stylesheets/components/_layout.scss`

これは**全文置き換え不要**です。
今回はそのままで大丈夫です。

---

## 作業手順

1. `app/views/layouts/_header.html.erb` を全文置き換え
2. `app/helpers/application_helper.rb` を全文置き換え
3. `app/views/wallets/_balance.html.erb` を全文置き換え
4. `app/assets/stylesheets/application.scss` を全文置き換え
5. `app/assets/stylesheets/components/_header.scss` を新規作成

---

## 補足

今回の版では、仕様どおり

* `container-fluid`
* 未ログイン時は右領域空白
* customer はロールバッジ非表示
* `CAST / STORE / ADMIN`
* `md` で名前まで、`lg` でメールまで表示
* dropdown内では名前・メールを必要な幅だけ補完

にしています。

このまま入れて、次は表示崩れやテスト結果を見ながら微調整する流れで大丈夫です。
