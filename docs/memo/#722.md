では、#722 を **そのまま実装できる差分イメージ**でまとめます。

---

## 1. `app/controllers/home_controller.rb`

`users` モードを追加します。
ついでに `@users` の初期化も入れておくと view 側が安定します。

```rb
# frozen_string_literal: true

class HomeController < ApplicationController
  def show
    @mode = params[:mode].to_s
    @mode = "booths" unless %w[booths stores users].include?(@mode)

    @q = params[:q].to_s.strip
    q_like =
      if @q.present?
        escaped = ActiveRecord::Base.sanitize_sql_like(@q)
        "%#{escaped}%"
      end

    @booths = Booth.none
    @stores = Store.none
    @users = User.none

    if @mode == "users"
      users = User.where(role: %i[cast store_admin])

      users = users.where("users.display_name ILIKE ?", q_like) if q_like.present?

      @users = users.order(id: :desc).limit(30)

      @favorite_booth_ids = Set.new
      @favorite_store_ids = Set.new
      return
    end

    if @mode == "stores"
      stores = Store.all

      stores = stores.where("stores.name ILIKE ?", q_like) if q_like.present?

      # --- online: MAX(started_at) ---
      online_started_at =
        Booth.active
            .where(status: %i[live away])
            .joins("INNER JOIN stream_sessions ss ON ss.id = booths.current_stream_session_id")
            .select("booths.store_id AS store_id, MAX(ss.started_at) AS max_started_at")
            .group("booths.store_id")

      # --- offline/standby: MAX(last_online_at) ---
      last_online =
        Booth.active
            .select("booths.store_id AS store_id, MAX(booths.last_online_at) AS max_last_online_at")
            .group("booths.store_id")

      stores =
        stores
          .joins("LEFT JOIN (#{online_started_at.to_sql}) online ON online.store_id = stores.id")
          .joins("LEFT JOIN (#{last_online.to_sql}) last_online ON last_online.store_id = stores.id")
          .order(
            Arel.sql("CASE WHEN online.max_started_at IS NOT NULL THEN 1 ELSE 0 END DESC"),
            Arel.sql("online.max_started_at DESC NULLS LAST"),
            Arel.sql("last_online.max_last_online_at DESC NULLS LAST"),
            id: :desc
          )
          .limit(30)

      if user_signed_in? && current_user.customer?
        banned_store_ids = StoreBan.where(customer_user_id: current_user.id).select(:store_id)
        stores = stores.where.not(id: banned_store_ids)
      end

      @stores = stores

      if user_signed_in?
        @favorite_store_ids =
          current_user.favorite_stores.where(store_id: @stores.select(:id)).pluck(:store_id).to_set
      else
        @favorite_store_ids = Set.new
      end

      @favorite_booth_ids = Set.new
      return
    end

    # --- mode=booths ---
    booths = Booth.active

    booths =
      booths.joins(<<~SQL)
        LEFT JOIN stream_sessions current_ss
          ON current_ss.id = booths.current_stream_session_id
      SQL

    booths = booths.where("booths.name ILIKE ?", q_like) if q_like.present?

    online_order =
      "CASE WHEN booths.status IN (#{Booth.statuses[:live]}, #{Booth.statuses[:away]}) THEN 1 ELSE 0 END"

    booths =
      booths
        .order(
          Arel.sql("#{online_order} DESC"),
          Arel.sql("
            CASE
              WHEN booths.status IN (#{Booth.statuses[:live]}, #{Booth.statuses[:away]})
              THEN current_ss.started_at
            END DESC NULLS LAST
          "),
          Arel.sql("booths.last_online_at DESC NULLS LAST"),
          id: :desc
        )
        .limit(60)

    # customer はBAN店舗を除外（現状踏襲）
    if user_signed_in? && current_user.customer?
      banned_store_ids = StoreBan.where(customer_user_id: current_user.id).select(:store_id)
      booths = booths.where.not(store_id: banned_store_ids)
    end

    @booths = booths

    # favorites（表示対象だけ）
    if user_signed_in?
      @favorite_booth_ids =
        current_user.favorite_booths.where(booth_id: @booths.select(:id)).pluck(:booth_id).to_set

      @favorite_store_ids =
        current_user.favorite_stores.where(store_id: @booths.select(:store_id)).pluck(:store_id).to_set
    else
      @favorite_booth_ids = Set.new
      @favorite_store_ids = Set.new
    end
  end
end
```

---

## 2. `app/views/home/show.html.erb`

`ユーザー` タブと結果表示分岐を追加します。

```erb
<% if user_signed_in? %>
  <%= render "install_prompt" %>

  <%# --- Search (Input group / GET) --- %>
  <div class="mb-3">
  <%= form_with url: root_path, method: :get, scope: nil, class: "d-flex flex-grow-1" do |f| %>
    <div class="input-group">
      <%= link_to "ブース",
            root_path(mode: "booths", q: @q.presence),
            class: ["btn", (@mode == "booths" ? "btn-secondary" : "btn-outline-secondary")] %>

      <%= link_to "店舗",
            root_path(mode: "stores", q: @q.presence),
            class: ["btn", (@mode == "stores" ? "btn-secondary" : "btn-outline-secondary")] %>

      <%= link_to "ユーザー",
            root_path(mode: "users", q: @q.presence),
            class: ["btn", (@mode == "users" ? "btn-secondary" : "btn-outline-secondary")] %>

      <%= f.hidden_field :mode, value: @mode %>

      <%= f.text_field :q,
            value: @q,
            class: "form-control",
            placeholder: "検索（部分一致）",
            autocomplete: "off" %>

      <button class="btn btn-primary" type="submit" aria-label="検索">
        <i class="bi bi-search"></i>
      </button>
    </div>
  </div>
  <% end %>

  <%# --- Results (cards) --- %>
  <% if @mode == "users" %>
    <% if @users.present? %>
      <div class="row row-cols-1 row-cols-md-3 g-3">
        <% @users.each do |user| %>
          <div class="col">
            <%= render "users/card", user: user %>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="alert alert-secondary" role="alert">
        表示できるユーザーがありません。
      </div>
    <% end %>
  <% elsif @mode == "stores" %>
    <% if @stores.present? %>
      <div class="row row-cols-1 row-cols-md-3 g-3">
        <% @stores.each do |store| %>
          <% favorited = @favorite_store_ids.include?(store.id) %>

          <div class="col">
            <%= render "stores/card", store: store, favorited: favorited %>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="alert alert-secondary" role="alert">
        表示できる店舗がありません。
      </div>
    <% end %>
  <% else %>
    <% if @booths.present? %>
      <div class="row row-cols-1 row-cols-md-3 g-3">
        <% @booths.each do |booth| %>
          <div class="col">
            <%= render "booths/card",
                  booth: booth,
                  booth_favorited: @favorite_booth_ids.include?(booth.id),
                  store_favorited: @favorite_store_ids.include?(booth.store_id) %>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="alert alert-secondary" role="alert">
        表示できるブースがありません。
      </div>
    <% end %>
  <% end %>
<% else %>
  <%= render "install_prompt" %>

  <div style="max-width: 720px;">
    <h1 class="mb-3">ようこそ</h1>
    <p class="text-muted">視聴するにはログイン、または新規アカウント作成を行ってください。</p>

    <div class="d-flex gap-2 mt-3">
      <%= link_to "ログイン", new_user_session_path, class: "btn btn-primary" %>
      <%= link_to "新規アカウント作成", sign_up_path, class: "btn btn-outline-primary" %>
    </div>
  </div>
<% end %>
```

---

## 3. 新規作成 `app/views/users/_card.html.erb`

まずは Bootstrap card ベースで十分です。
role 表示は `user_role_label(user)` を使います。

```erb
<%# locals: user: %>

<div class="card h-100 shadow-sm users-card" data-bs-theme="light">
  <div class="card-body d-flex flex-column">
    <div class="users-card-row">
      <div class="users-card-label">ユーザー名</div>

      <div class="users-card-value users-card-value-emphasis">
        <%= link_to display_name_or_anonymous(user),
              user_path(user),
              class: "text-decoration-none text-reset fw-semibold" %>
      </div>
    </div>

    <div class="users-card-row">
      <div class="users-card-label">ロール</div>

      <div class="users-card-value">
        <%= user_role_label(user) %>
      </div>
    </div>
  </div>
</div>
```

---

## 4. `test/integration/home_search_test.rb`

末尾にこの3本を追加してください。

```rb
test "mode=users: cast と store_admin のみ表示され、customer/system_admin は表示されない" do
  cast_user = create_user!(email: "cast_search@example.com", role: :cast)
  cast_user.update!(display_name: "Cast Search User")

  store_admin_user = create_user!(email: "store_admin_search@example.com", role: :store_admin)
  store_admin_user.update!(display_name: "Store Admin Search User")

  customer_user = create_user!(email: "customer_search@example.com", role: :customer)
  customer_user.update!(display_name: "Customer Search User")

  system_admin_user = create_user!(email: "system_admin_search@example.com", role: :system_admin)
  system_admin_user.update!(display_name: "System Admin Search User")

  login_as(customer_user, scope: :user)

  get root_path, params: { mode: "users" }
  assert_response :success

  body = @response.body

  assert_includes body, "value=\"users\""
  assert_includes body, ">ユーザー<"

  assert_includes body, "Cast Search User"
  assert_includes body, "Store Admin Search User"

  refute_includes body, "Customer Search User"
  refute_includes body, "System Admin Search User"
end

test "qあり + mode=users: display_name の部分一致で絞り込まれる" do
  cast_hit = create_user!(email: "users_hit@example.com", role: :cast)
  cast_hit.update!(display_name: "Rose User")

  cast_miss = create_user!(email: "users_miss@example.com", role: :cast)
  cast_miss.update!(display_name: "Tulip User")

  customer = create_user!(email: "users_search_customer@example.com", role: :customer)
  login_as(customer, scope: :user)

  get root_path, params: { mode: "users", q: "Ros" }
  assert_response :success

  body = @response.body

  assert_includes body, "Rose User"
  refute_includes body, "Tulip User"
end

test "mode=users: ユーザー名クリックで user詳細に遷移できるリンクが含まれる" do
  user = create_user!(email: "users_link@example.com", role: :cast)
  user.update!(display_name: "Link User")

  customer = create_user!(email: "users_link_customer@example.com", role: :customer)
  login_as(customer, scope: :user)

  get root_path, params: { mode: "users" }
  assert_response :success

  assert_includes @response.body, user_path(user)
end
```

---

## 5. テスト実行コマンド

このプロジェクト方針どおり `docker compose` 経由です。

```bash
docker compose exec app bin/rails test test/integration/home_search_test.rb
```

必要なら全体も。

```bash
docker compose exec app bin/rails test
```

---

## 6. 補足

`users-card` 系クラスは今は SCSS 未定義でも問題ありません。
完了条件は満たせますし、見た目調整はあとで分離できます。

もし最小限だけ整えたいなら、後で `_users_card.scss` を足して `application.scss` に import でもよいですが、#722 のスコープ上は不要です。

---

実装後にエラーやテスト失敗が出たら、そのまま最新ファイルか失敗ログを貼ってください。
