了解。#307 は「ロジック維持」を最優先に、**見た目だけを再配置**します（store/booth分岐・return_to・Turbo購読・ログアウト実装は現状のまま移植）。

---

## 実装方針（全体）

### 変更点の最小分割

1. `wallets/_balance` を **アイコン + `***pt` 表示**に変更（Turbo更新はそのまま）
2. `ApplicationHelper` に **アバター共通ヘルパ**を追加（fallbackは「No」→先頭1文字）
3. `application.html.erb` のヘッダーを **並び替え + 名前dropdown化**

   * dropdown 内に email / store / booth / logout を移植
   * フッターの「ポイント購入」「ログアウト」を削除（挙動は移植で維持）
4. ロゴ画像 `app/assets/images/logo.png` を追加し、ヘッダー左のアプリ名枠をロゴに置換
5. 必要なら `application.scss` にロゴ用の最小CSSを追加（なくても inline で完結可能）

---

## ブランチ名候補

* `improve/307-header-reorder`
* `improve/307-header-dropdown`
* `improve/307-header-wallet-icon`
* `improve/307-header-avatar-fallback`
* `improve/307-header-layout`

---

## 変更対象ファイル一覧

* `app/views/layouts/application.html.erb`
* `app/helpers/application_helper.rb`
* `app/views/wallets/_balance.html.erb`
* `app/assets/stylesheets/application.scss`（必要なら）
* `app/assets/images/logo.png`（新規追加）

---

## ペースト用コード（そのまま置換）

### 1) `app/views/wallets/_balance.html.erb`（全文置換）

```erb
<%# locals: wallet: %>
<i class="bi bi-p-circle-fill"></i>
<strong><%= wallet&.available_points || 0 %>pt</strong>
```

* `id="wallet_balance"` は layout 側に残すので Turbo更新は維持されます。

---

### 2) `app/helpers/application_helper.rb`（追記＋既存維持）

以下を **`display_name_or_email` の下あたり**に追記してください（既存メソッドは消さない）。

```rb
  def user_avatar_badge(user, size: 24, klass: "")
    return "" if user.blank?

    if user.avatar.attached?
      image_tag(
        user.avatar,
        class: ["rounded-circle border", klass].join(" ").strip,
        style: "width: #{size}px; height: #{size}px; object-fit: cover;",
        alt: "avatar"
      )
    else
      label = display_name_or_email(user).to_s
      initial = label.strip.presence ? label.strip[0] : "?"
      content_tag(
        :span,
        initial,
        class: ["rounded-circle border text-muted d-inline-flex align-items-center justify-content-center", klass].join(" ").strip,
        style: "width: #{size}px; height: #{size}px; font-size: #{(size * 0.45).round}px; line-height: 1;",
        aria: { label: "avatar" },
        title: label
      )
    end
  end
```

* fallback の先頭1文字は **display_name_or_email と同基準**です（#308で display_name_or_anonymous 化した後も、ここは呼び先の基準に追従できます）。
* 「ななしさん」は今回入れません（あなたの方針どおり #308）。

---

### 3) `app/assets/stylesheets/application.scss`（最小CSS追記・任意）

いまは bootstrap のみなので、ロゴサイズをCSSで寄せたいなら末尾に追記します（inline でもOK）。
追記する場合は以下を末尾へ：

```scss
.header-logo {
  height: 24px;
  width: auto;
  display: block;
}
```

---

### 4) `app/views/layouts/application.html.erb`（ヘッダー＋フッターを更新）

#### 4-1) ヘッダー部分：`<header ...>` ブロックを **全文置換**

`<%# --- Header (common) --- %>` から `</header>` までを、下記に置き換えてください。

```erb
    <%# --- Header (common) --- %>
    <header class="border-bottom bg-white">
      <div class="container py-2">
        <div class="d-flex justify-content-between align-items-center gap-3">
          <div class="d-flex align-items-center gap-2 flex-wrap">
            <%# 1) アプリ名（ロゴ） %>
            <div class="fw-semibold">
              <%= image_tag "logo.png", class: "header-logo", alt: (content_for(:title).presence || "App") %>
            </div>

            <% if user_signed_in? %>
              <%# 2) ポイント（アイコン + pt / Turbo更新維持） %>
              <%= turbo_stream_from [current_user, :wallet] %>
              <%= link_to new_wallet_purchase_path(return_to: request.fullpath),
                    data: { turbo_frame: "modal" },
                    class: "text-muted small text-decoration-none d-inline-flex align-items-center gap-1",
                    id: "wallet_balance" do %>
                <%= render "wallets/balance", wallet: current_user.wallet %>
              <% end %>

              <%# 3) アバター（fallback: 頭文字 / 共通ヘルパ） %>
              <%= user_avatar_badge(current_user, size: 24) %>

              <%# 4) 名前（dropdownトグル） %>
              <div class="dropdown">
                <button
                  class="btn btn-sm btn-link text-muted small p-0 text-decoration-none dropdown-toggle"
                  type="button"
                  data-bs-toggle="dropdown"
                  aria-expanded="false">
                  <%= display_name_or_email(current_user) %>
                </button>

                <ul class="dropdown-menu dropdown-menu-end">
                  <%# e-mail（常に表示） %>
                  <li>
                    <span class="dropdown-item-text text-muted small">
                      <%= current_user.email %>
                    </span>
                  </li>

                  <% unless current_user.customer? %>
                    <%# 店舗/ブース（現状ロジックを移植：リンク可否・return_to・未選択表示も維持） %>

                    <% store = layout_current_store %>
                    <li>
                      <span class="dropdown-item-text text-muted small">
                        store:
                        <% if current_user.at_least?(:store_admin) %>
                          <% stores_link_params =
                              if request.get? && request.path != "/admin/stores"
                                { return_to: request.fullpath }
                              else
                                {}
                              end %>

                          <%= link_to admin_stores_path(stores_link_params),
                                class: "link-secondary link-underline-opacity-0 link-underline-opacity-100-hover" do %>
                            <% if store.present? %>
                              <strong><%= store.name.presence || "(no name)" %></strong>
                            <% else %>
                              <span class="text-danger">未選択</span>
                            <% end %>
                          <% end %>
                        <% else %>
                          <%# cast: store は表示のみ（クリック不可） %>
                          <% if store.present? %>
                            <strong><%= store.name.presence || "(no name)" %></strong>
                          <% else %>
                            <span class="text-danger">未選択</span>
                          <% end %>
                        <% end %>
                      </span>
                    </li>

                    <% booth = layout_current_booth %>
                    <li>
                      <span class="dropdown-item-text text-muted small">
                        booth:
                        <% if current_user.cast? %>
                          <%= link_to cast_booths_path,
                                class: "link-secondary link-underline-opacity-0 link-underline-opacity-100-hover" do %>
                            <% if booth.present? %>
                              <strong><%= booth.name.presence || "(no name)" %></strong>
                            <% else %>
                              <span class="text-danger">未選択</span>
                            <% end %>
                          <% end %>
                        <% elsif current_user.at_least?(:store_admin) %>
                          <%= link_to admin_booths_path,
                                class: "link-secondary link-underline-opacity-0 link-underline-opacity-100-hover" do %>
                            <% if booth.present? %>
                              <strong><%= booth.name.presence || "(no name)" %></strong>
                            <% else %>
                              <span class="text-danger">未選択</span>
                            <% end %>
                          <% end %>
                        <% else %>
                          <%# 念のため（到達しない想定）：customer は外で除外済み %>
                          <% if booth.present? %>
                            <strong><%= booth.name.presence || "(no name)" %></strong>
                          <% else %>
                            <span class="text-danger">未選択</span>
                          <% end %>
                        <% end %>
                      </span>
                    </li>
                  <% end %>

                  <li><hr class="dropdown-divider"></li>

                  <%# ログアウト（フッターと同一実装を移植：confirm等も追加しない） %>
                  <li>
                    <%= button_to "ログアウト",
                          destroy_user_session_path,
                          method: :delete,
                          class: "dropdown-item" %>
                  </li>
                </ul>
              </div>

              <%# 5) ロールバッジ %>
              <%= role_badge(current_user) %>
            <% else %>
              <span class="text-muted small">未ログイン</span>
            <% end %>
          </div>

          <div class="d-flex align-items-center gap-2">
            <% unless user_signed_in? %>
              <%= link_to "ログイン", new_user_session_path, class: "btn btn-sm btn-primary" %>
            <% end %>
          </div>
        </div>
      </div>
    </header>
```

ポイント：

* 並びは指定通り **ロゴ → ポイント → アバター → 名前(dropdown) → バッジ**
* `turbo_stream_from` は維持
* `wallet_balance` は **id をリンクに付け替え**（更新ターゲットを維持しつつクリック導線に）
* store/booth の分岐と `stores_link_params` は **原文をそのまま移植**
* ログアウトもフッターの `button_to` を **そのまま移植**（confirm は追加しない）

#### 4-2) フッター部分：ポイント購入＋ログアウトを削除

`<footer ...>` 内の右側ボタン群から、以下2つを削除してください。

* `link_to "ポイント購入", new_wallet_purchase_path(...), data: { turbo_frame: "modal" }, ...`
* `button_to "ログアウト", destroy_user_session_path, method: :delete, ...`

削除後の右側はこう残る形です（参考：置換ではなく削除だけ）：

* `ダッシュボード` は残す

---

### 5) ロゴ画像の追加

* `app/assets/images/logo.png` を追加
  Propshaft なのでここに置けば `image_tag "logo.png"` で参照できます。

サイズは任意ですが、ヘッダーでは `height: 24px` 想定です。
画像が大きくても表示は縮小されます。

---

## デグレ影響範囲（Epic #285 ルールを本文に合わせて明記）

* 売上ロジックへの影響：なし
* 配信状態遷移への影響：なし
* current整合性への影響：なし
* Token API影響：なし

---

## 動作確認チェックリスト（手元での確認項目）

1. ログイン時ヘッダーの並びが指定通り
2. wallet が Turbo Stream で更新される（ポイント購入後など）
3. ポイント表示クリックでモーダルが開く（`data-turbo-frame="modal"`）
4. dropdown 内に email / store / booth / logout が表示される
5. store/booth のリンク可否が現状通り（castのstoreは非リンク等）
6. フッターから「ポイント購入」「ログアウト」が消えている
7. live画面でもヘッダーが崩れない（`live_layout_moved` でもヘッダーは共通なので確認）

---

## docker compose 経由のテスト/起動コマンド（いつものルール）

```bash
docker compose run --rm web bin/rails test
```

必要ならヘッダー確認は通常起動で。

---

## コミットメッセージ案（任意）

* `Improve header layout: reorder items, add dropdown, move wallet purchase`
* `Improve avatar fallback: show initial badge and extract helper`
* `UI: move logout into header dropdown`

---

このまま実装していけば Done 条件は全部満たせます。
もし「ロゴをテキストにフォールバックしたい（logo.png未配置でも動くように）」まで入れるなら、それもロジック変更なしで追加できますが、今回は最小に寄せてあります。
