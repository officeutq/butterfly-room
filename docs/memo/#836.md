OK、ここまで揃えば**実装いけます** 👍
そのままコピペで進められる差分を出します。

---

# ✅ 変更差分（コピペ用）

---

# ① routes削除

```diff
# config/routes.rb

namespace :admin do
  resources :booths, only: %i[index new create] do
    member do
      patch :archive
      post :force_end
-     post :assign_cast
    end
  end
```

---

# ② controller削除

```diff
# app/controllers/admin/booths_controller.rb

- def assign_cast
-   ...
- end
```

👉 **メソッドごと削除**

---

# ③ indexからcast_memberships削除

```diff
# app/controllers/admin/booths_controller.rb

def index
- @cast_memberships = ...
end
```

👉 indexで取得してるなら削除

---

# ④ View修正（ここがメイン）

```diff
# app/views/admin/booths/index.html.erb

- <h1 class="mb-4">ブース一覧</h1>
+ <h1 class="mb-4">ブース管理</h1>
```

---

## キャスト表示

```diff
- <div class="small text-muted mb-1">現在のキャスト</div>
+ <div class="small text-muted mb-1">所属キャスト</div>
```

---

```diff
<% if booth_cast.present? %>
-  <div class="d-flex align-items-center gap-2 flex-wrap">
-    <span><%= display_name_or_anonymous(booth_cast.cast_user) %></span>
-  </div>
+  <div class="d-flex align-items-center gap-2 flex-wrap">
+    <%= user_avatar_badge(booth_cast.cast_user, size: 24) %>
+    <span><%= display_name_or_anonymous(booth_cast.cast_user) %></span>
+  </div>
```

---

## 🔥 紐づけフォーム削除（重要）

これを**丸ごと削除**

```diff
- <% if !archived && booth_cast.blank? %>
-   <div class="mt-3">
-     <%= form_with url: assign_cast_admin_booth_path(booth), method: :post do %>
-       <%= hidden_field_tag :return_to, switch_return_to %>
-
-       <div class="d-flex gap-2 align-items-center flex-wrap">
-         <%= select_tag "booth_cast[cast_user_id]",
-               options_for_select(@cast_memberships.map { |m| [ display_name_or_anonymous(m.user), m.user.id ] }),
-               include_blank: "キャストを選択",
-               class: "form-select form-select-sm admin-booths-cast-select",
-               required: true %>
-
-         <%= submit_tag "紐づけ",
-               class: "btn btn-primary btn-sm",
-               data: { turbo_confirm: "このブースにキャストを紐づけます。紐づけ後は変更できません。よろしいですか？" } %>
-       </div>
-     <% end %>
-   </div>
- <% end %>
```

---

# 🎯 変更後の状態

こうなる👇

---

## 管理画面

* ブース管理（タイトル）
* 所属キャスト（アイコン付き）
* 紐づけUIなし

---

## 紐づけできる場所

* new（作成時）
* edit（未設定のみ）

---

## 削除されたもの

* assign_cast route
* assign_cast controller
* index紐づけUI
* cast_memberships（index）

---

# 👍 この変更の効果

* UIがめちゃくちゃスッキリ
* 操作ミス減る
* ドメインと一致（1ブース1キャスト）
  👉 

---

# ⚠️ 忘れがちな確認

これだけやっておいて👇

### テスト

```bash
docker compose exec app bin/rails test
```

---

### 手動確認

* ブース作成 → キャスト選べるか
* ブース一覧 → 紐づけUI消えてるか
* edit → 未設定なら紐づけできるか

---

# 🚀 次

終わったら👇

👉 コミットメッセージ & PR文出すので言って 👍
