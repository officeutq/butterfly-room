了解です。`_stores_card.scss` で進めます。

以下の4ファイルを修正・追加してください。

---

## 1. 新規 `app/views/stores/_card.html.erb`

```erb
<%# locals: store:, favorited: %>

<div class="card h-100 shadow-sm stores-card" data-bs-theme="light">
  <div class="stores-card-thumbnail-wrap">
    <%= link_to store_path(store), class: "d-block stores-card-thumbnail-link" do %>
      <% if store.thumbnail.attached? %>
        <%= image_tag store.thumbnail, class: "card-img-top stores-card-thumbnail" %>
      <% else %>
        <div class="stores-card-thumbnail stores-card-thumbnail-placeholder">
          <span class="text-muted small">no thumbnail</span>
        </div>
      <% end %>
    <% end %>
  </div>

  <div class="card-body d-flex flex-column">
    <div class="stores-card-row">
      <div class="stores-card-label">店舗名</div>

      <div class="stores-card-value stores-card-value-emphasis">
        <%= link_to store.name,
              store_path(store),
              class: "stores-card-name-link text-decoration-none text-reset" %>
      </div>

      <div class="stores-card-action">
        <%= render "favorites/stores/button_home", store: store, favorited: favorited %>
      </div>
    </div>

    <div class="stores-card-row">
      <div class="stores-card-label">地域</div>

      <div class="stores-card-value">
        <%= store.area.presence || "-" %>
      </div>

      <div class="stores-card-label">業態</div>

      <div class="stores-card-value">
        <%= Store::BUSINESS_TYPE_LABELS[store.business_type&.to_sym] || "-" %>
      </div>
    </div>

    <div class="stores-card-row stores-card-row-summary">
      <div class="stores-card-label">概要</div>

      <div class="stores-card-value stores-card-summary">
        <%= store.description.presence || "-" %>
      </div>
    </div>
  </div>
</div>
```

---

## 2. 修正 `app/views/home/show.html.erb`

```erb
<% if user_signed_in? %>
  <%# --- Search (Input group / GET) --- %>
  <div class="mb-3">
    <%= form_with url: root_path, method: :get, scope: nil do |f| %>
      <%= f.hidden_field :mode, value: @mode %>

      <div class="input-group">
        <div class="btn-group">
          <button
            class="btn btn-outline-secondary dropdown-toggle"
            type="button"
            data-bs-toggle="dropdown"
            aria-expanded="false">
            <%= @mode == "stores" ? "店舗" : "ブース" %>
          </button>

          <ul class="dropdown-menu">
            <li>
              <%= link_to "ブース",
                    root_path(mode: "booths", q: @q.presence),
                    class: "dropdown-item" %>
            </li>
            <li>
              <%= link_to "店舗",
                    root_path(mode: "stores", q: @q.presence),
                    class: "dropdown-item" %>
            </li>
          </ul>
        </div>

        <%= f.text_field :q,
              value: @q,
              class: "form-control",
              placeholder: "ブース名 / ストア名で検索（部分一致）",
              autocomplete: "off" %>

        <button class="btn btn-primary" type="submit" aria-label="検索">
          <i class="bi bi-search"></i>
        </button>
      </div>
    <% end %>
  </div>

  <%# --- Results (cards) --- %>
  <% if @mode == "stores" %>
    <% if @stores.present? %>
      <div class="row row-cols-1 row-cols-md-3 g-3">
        <% @stores.each do |store| %>
          <% favorited = @favorite_store_ids.include?(store.id) %>

          <div class="col">
            <%= render "stores/card", store: store, favorited: favorited %>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="alert alert-secondary" role="alert">
        表示できる店舗がありません。
      </div>
    <% end %>
  <% else %>
    <% if @booths.present? %>
      <div class="row row-cols-1 row-cols-md-3 g-3">
        <% @booths.each do |booth| %>
          <div class="col">
            <%= render "booths/card",
                  booth: booth,
                  booth_favorited: @favorite_booth_ids.include?(booth.id),
                  store_favorited: @favorite_store_ids.include?(booth.store_id) %>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="alert alert-secondary" role="alert">
        表示できるブースがありません。
      </div>
    <% end %>
  <% end %>
<% else %>
  <div style="max-width: 720px;">
    <h1 class="mb-3">ようこそ</h1>
    <p class="text-muted">視聴するにはログイン、または新規アカウント作成を行ってください。</p>

    <div class="d-flex gap-2 mt-3">
      <%= link_to "ログイン", new_user_session_path, class: "btn btn-primary" %>
      <%= link_to "新規アカウント作成", sign_up_path, class: "btn btn-outline-primary" %>
    </div>
  </div>
<% end %>
```

---

## 3. 修正 `app/views/favorites/stores/index.html.erb`

```erb
<div style="max-width: 960px;">
  <div class="d-flex justify-content-between align-items-start gap-3">
    <div>
      <h1 class="mb-1">お気に入り店舗</h1>
      <p class="text-muted mb-0">新しい順</p>
    </div>

    <div class="d-flex gap-2">
      <%= link_to "ブースのお気に入り", favorites_booths_path, class: "btn btn-outline-secondary btn-sm" %>
    </div>
  </div>

  <hr class="my-4">

  <% if @favorite_stores.blank? %>
    <div class="alert alert-secondary" role="alert">
      お気に入り店舗はありません。
    </div>
  <% else %>
    <div class="row row-cols-1 row-cols-md-3 g-3">
      <% @favorite_stores.each do |favorite| %>
        <% store = favorite.store %>

        <div class="col">
          <%= render "stores/card", store: store, favorited: true %>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
```

---

## 4. 新規 `app/assets/stylesheets/components/_stores_card.scss`

```scss
.stores-card {
  overflow: hidden;
}

.stores-card[data-bs-theme="light"] {
  color: var(--bs-body-color);
}

.stores-card-thumbnail-wrap {
  position: relative;
}

.stores-card-thumbnail-link {
  display: block;
}

.stores-card-thumbnail {
  display: block;
  aspect-ratio: 16 / 9;
  object-fit: cover;
}

.stores-card-thumbnail-placeholder {
  background: var(--bs-light);
  display: flex;
  align-items: center;
  justify-content: center;
}

.stores-card-row {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  min-width: 0;
}

.stores-card-row + .stores-card-row {
  margin-top: 0.5rem;
}

.stores-card-row-summary {
  align-items: flex-start;
}

.stores-card-label {
  flex: 0 0 auto;
  color: var(--bs-secondary-color);
  font-size: 0.875rem;
  line-height: 1.5;
  white-space: nowrap;
}

.stores-card-value {
  min-width: 0;
  flex: 0 1 auto;
  line-height: 1.5;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.stores-card-value-emphasis {
  font-weight: 600;
}

.stores-card-action {
  flex: 0 0 auto;
  margin-left: auto;
}

.stores-card-name-link {
  display: inline-block;
  max-width: 100%;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  vertical-align: middle;
}

.stores-card-summary {
  flex: 1 1 auto;
}
```

---

## 5. `app/assets/stylesheets/application.scss` に読み込み追加

もしまだ未追加なら、components の並びにこれを足してください。

```scss
@use "components/stores_card";
```

---

## 確認ポイント

* home の店舗一覧が共通 partial 化される
* favorites の list-group と「開く」ボタンが消える
* お気に入りボタンの Turbo Stream ID は既存のまま
* form の入れ子は発生しない
* 概要は1行省略になる
* サムネイルと店舗名の両方から `store_path(store)` に遷移できる

必要なら次に、そのまま貼れるテスト観点も整理します。
