了解です。
#633 用に、**そのままコピペできる形で全部**まとめます。

---

## ブランチ名

```bash
git checkout -b feature/633-filepond-commonize
```

---

## 今回やること

* `profile_avatar_controller.js` を共通名へ変更
* `_profile_avatar.scss` を共通名へ変更
* 既存画像を FilePond の初期プレビューに表示
* remove 仕様を固定
* プロフィールは `1024x1024`、ブース・店舗は `1920x1080`
* `mini_magick` 依存を撤去
* FilePond の script 読込を `content_for :head` に移動
* `AttachmentPersistenceChecker` を `models/concerns` に移動

---

# Step 1. 共通 head partial を追加

## 新規作成

`app/views/shared/_filepond_head.html.erb`

```erb
<script src="/vendor/filepond/filepond.min.js" defer></script>
<script src="/vendor/filepond/filepond-plugin-image-preview.min.js" defer></script>
<script src="/vendor/filepond/filepond-plugin-image-resize.min.js" defer></script>
<script src="/vendor/filepond/filepond-plugin-image-transform.min.js" defer></script>
```

---

# Step 2. 共通 Stimulus controller を追加

## 新規作成

`app/javascript/controllers/image_upload_controller.js`

```javascript
import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  static targets = ["input", "removeFlag"]
  static values = {
    initialUrl: String,
    width: Number,
    height: Number,
  }

  connect() {
    this.hadInitialFile = this.hasInitialUrlValue && this.initialUrlValue.length > 0
    this.initialize()
  }

  disconnect() {
    if (this.pond) this.pond.destroy()
  }

  initialize() {
    if (!window.FilePond) return

    this.registerPlugins()

    const files = this.hadInitialFile
      ? [
          {
            source: this.initialUrlValue,
            options: {
              type: "local",
            },
          },
        ]
      : []

    this.pond = window.FilePond.create(this.inputTarget, {
      storeAsFile: true,
      allowMultiple: false,
      allowImagePreview: true,
      allowImageResize: true,
      allowImageTransform: true,
      allowReorder: false,
      allowProcess: false,
      allowRevert: false,

      imageResizeTargetWidth: this.widthValue || 1024,
      imageResizeTargetHeight: this.heightValue || 1024,
      imageResizeMode: "contain",
      imageResizeUpscale: false,

      imageTransformOutputMimeType: "image/jpeg",
      imageTransformOutputQuality: 94,
      imageTransformCanvasBackgroundColor: "#ffffff",

      acceptedFileTypes: [
        "image/jpeg",
        "image/png",
        "image/webp",
        "image/heic",
        "image/heif",
      ],

      labelIdle: `
        <div class="image-upload-picker">
          <i class="bi bi-camera-fill image-upload-picker__icon" aria-hidden="true"></i>
        </div>
      `,

      files,
    })

    this.bindEvents()
  }

  bindEvents() {
    this.pond.on("addfile", () => {
      this.removeFlagTarget.value = "0"
    })

    this.pond.on("removefile", () => {
      const currentFilesCount = this.pond.getFiles().length

      if (currentFilesCount > 0) {
        this.removeFlagTarget.value = "0"
        return
      }

      this.removeFlagTarget.value = this.hadInitialFile ? "1" : "0"
    })
  }

  registerPlugins() {
    if (window.__filepondRegistered) return

    const plugins = [
      window.FilePondPluginImagePreview,
      window.FilePondPluginImageResize,
      window.FilePondPluginImageTransform,
    ].filter(Boolean)

    plugins.forEach((plugin) => {
      window.FilePond.registerPlugin(plugin)
    })

    window.__filepondRegistered = true
  }
}
```

---

# Step 3. 旧 controller は削除

## 削除

`app/javascript/controllers/profile_avatar_controller.js`

---

# Step 4. 共通 CSS を追加

## 新規作成

`app/assets/stylesheets/components/_image_upload.scss`

```scss
.image-upload-field {
  .filepond--root {
    margin-bottom: 0;
  }

  .filepond--drop-label {
    min-height: 180px;
  }

  .filepond--label-action {
    display: none;
  }

  .filepond--panel-root {
    background-color: #f8f9fa;
    border-radius: 16px;
  }

  .image-upload-picker {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 140px;
  }

  .image-upload-picker__icon {
    font-size: 2rem;
    color: #6c757d;
  }
}
```

---

# Step 5. 旧 CSS は削除

## 削除

`app/assets/stylesheets/components/_profile_avatar.scss`

---

# Step 6. application.scss を更新

## `app/assets/stylesheets/application.scss`

```scss
@use "bootstrap/scss/bootstrap" as *;

:root {
  --live-stage-h: 0px;
  --viewer-stage-h: 0px;
  --app-header-h: 0px;
  --app-footer-h: 64px;
}

@media (max-width: 575.98px) {
  :root {
    --app-footer-h: 60px;
  }
}

@import "components/layout";
@import "components/header";
@import "components/broadcast_layout";
@import "components/broadcast_meta";
@import "components/broadcast_drink";
@import "components/broadcast_comment";
@import "components/broadcast_ops";
@import "components/live";
@import "components/viewer";
@import "components/banuba_verification";
@import "components/drink_item";
@import "components/dashboard";
@import "components/booths_index";
@import "components/booths_card";
@import "components/stores_card";
@import "components/filepond_verification";
@import "components/image_upload";
```

---

# Step 7. profile edit を更新

## `app/views/profiles/edit.html.erb`

```erb
<% content_for :head do %>
  <%= render "shared/filepond_head" %>
<% end %>

<h1>プロフィール編集</h1>

<%= form_with model: @user, url: profile_path do |f| %>
  <% if @user.errors.any? %>
    <div class="alert alert-danger" role="alert">
      <ul class="mb-0">
        <% @user.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= f.label :display_name, "表示名", class: "form-label" %>
    <%= f.text_field :display_name, class: "form-control" %>
    <div class="form-text">未入力の場合は email が表示されます（将来変更の可能性あり）</div>
  </div>

  <div class="mb-3">
    <%= f.label :bio, "自己紹介（bio）", class: "form-label" %>
    <%= f.text_area :bio, class: "form-control", rows: 6 %>
    <div class="form-text">最大500文字</div>
  </div>

  <div
    class="mb-3 image-upload-field"
    data-controller="image-upload"
    data-image-upload-width-value="1024"
    data-image-upload-height-value="1024"
    <% if @user.avatar.attached? %>
      data-image-upload-initial-url-value="<%= url_for(@user.avatar) %>"
    <% end %>>
    <%= f.label :avatar, "プロフィール画像", class: "form-label" %>

    <%= f.hidden_field :remove_avatar,
          value: "0",
          data: { image_upload_target: "removeFlag" } %>

    <%= f.file_field :avatar,
          accept: ".jpg,.jpeg,.png,.webp,.heic,.heif",
          data: { image_upload_target: "input" } %>
  </div>

  <div class="d-flex gap-2">
    <%= f.submit "更新する", class: "btn btn-primary" %>
    <%= link_to "戻る", dashboard_path, class: "btn btn-outline-secondary" %>
  </div>
<% end %>
```

---

# Step 8. booth form を更新

## `app/views/booths/_form.html.erb`

```erb
<%# locals: f:, booth:, submit_label:, back_path: %>

<% if booth.errors.any? %>
  <div class="alert alert-danger">
    <ul class="mb-0">
      <% booth.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<div class="mb-3">
  <%= f.label :name, "ブース名", class: "form-label" %>
  <%= f.text_field :name, class: "form-control", required: true %>
</div>

<div class="mb-3">
  <%= f.label :description, "説明文", class: "form-label" %>
  <%= f.text_area :description, class: "form-control", rows: 6 %>
</div>

<div
  class="mb-3 image-upload-field"
  data-controller="image-upload"
  data-image-upload-width-value="1920"
  data-image-upload-height-value="1080"
  <% if booth.thumbnail_image.attached? %>
    data-image-upload-initial-url-value="<%= url_for(booth.thumbnail_image) %>"
  <% end %>>
  <%= f.label :thumbnail_image, "サムネ画像", class: "form-label" %>

  <%= f.hidden_field :remove_thumbnail_image,
        value: "0",
        data: { image_upload_target: "removeFlag" } %>

  <%= f.file_field :thumbnail_image,
        accept: ".jpg,.jpeg,.png,.webp,.heic,.heif",
        data: { image_upload_target: "input" } %>
</div>

<div class="d-flex gap-2">
  <%= f.submit submit_label, class: "btn btn-primary" %>
  <%= link_to "戻る", back_path, class: "btn btn-outline-secondary" %>
</div>
```

---

# Step 9. store form を更新

## `app/views/admin/stores/_form.html.erb`

```erb
<%# locals: f:, store:, submit_label:, back_path: %>

<% if store.errors.any? %>
  <div class="alert alert-danger" role="alert">
    <ul class="mb-0">
      <% store.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<div class="mb-3">
  <%= f.label :name, "店舗名（必須）", class: "form-label" %>
  <%= f.text_field :name, class: "form-control", required: true %>
</div>

<div class="mb-3">
  <%= f.label :description, "概要", class: "form-label" %>
  <%= f.text_area :description, class: "form-control", rows: 6 %>
  <div class="form-text">最大1000文字</div>
</div>

<div class="mb-3">
  <%= f.label :area, "地域", class: "form-label" %>
  <%= f.text_field :area, class: "form-control" %>
  <div class="form-text">最大50文字（例：渋谷 / 福岡市中央区 など）</div>
</div>

<div class="mb-3">
  <%= f.label :business_type, "業態", class: "form-label" %>
  <%= f.select :business_type,
        options_for_select(Store.business_type_select_options, store.business_type),
        { include_blank: true },
        class: "form-select" %>
</div>

<div
  class="mb-3 image-upload-field"
  data-controller="image-upload"
  data-image-upload-width-value="1920"
  data-image-upload-height-value="1080"
  <% if store.thumbnail.attached? %>
    data-image-upload-initial-url-value="<%= url_for(store.thumbnail) %>"
  <% end %>>
  <%= f.label :thumbnail, "サムネイル画像", class: "form-label" %>

  <%= f.hidden_field :remove_thumbnail,
        value: "0",
        data: { image_upload_target: "removeFlag" } %>

  <%= f.file_field :thumbnail,
        accept: ".jpg,.jpeg,.png,.webp,.heic,.heif",
        data: { image_upload_target: "input" } %>
</div>

<div class="d-flex gap-2">
  <%= f.submit submit_label, class: "btn btn-primary" %>
  <%= link_to "戻る", back_path, class: "btn btn-outline-secondary" %>
</div>
```

---

# Step 10. booth edit/new views を head 読込に変更

## `app/views/cast/booths/edit.html.erb`

```erb
<% content_for :head do %>
  <%= render "shared/filepond_head" %>
<% end %>

<h1>ブース編集</h1>

<%= form_with model: [:cast, @booth] do |f| %>
  <%= render "booths/form",
            f: f,
            booth: @booth,
            submit_label: "更新する",
            back_path: dashboard_path_for(current_user) %>
<% end %>
```

## `app/views/admin/booths/edit.html.erb`

```erb
<% content_for :head do %>
  <%= render "shared/filepond_head" %>
<% end %>

<h1>ブース編集</h1>

<%= form_with model: [:admin, @booth] do |f| %>
  <%= render "booths/form",
            f: f,
            booth: @booth,
            submit_label: "更新する",
            back_path: dashboard_path_for(current_user) %>
<% end %>
```

## `app/views/admin/booths/new.html.erb`

```erb
<% content_for :head do %>
  <%= render "shared/filepond_head" %>
<% end %>

<h1>ブース作成</h1>

<%= form_with model: [:admin, @booth] do |f| %>
  <%= render "booths/form",
            f: f,
            booth: @booth,
            submit_label: "作成する",
            back_path: dashboard_path_for(current_user) %>
<% end %>
```

---

# Step 11. store edit view を head 読込に変更

## `app/views/admin/stores/edit.html.erb`

```erb
<% content_for :head do %>
  <%= render "shared/filepond_head" %>
<% end %>

<h1>店舗設定</h1>

<%= form_with model: [:admin, @store] do |f| %>
  <%= render "form",
            f: f,
            store: @store,
            submit_label: "更新する",
            back_path: dashboard_path %>
<% end %>
```

---

# Step 12. AttachmentPersistenceChecker を models/concerns に移動

## 新規作成

`app/models/concerns/attachment_persistence_checker.rb`

```ruby
# frozen_string_literal: true

module AttachmentPersistenceChecker
  extend ActiveSupport::Concern

  private

  def ensure_attachment_persisted!(record:, attachment_name:)
    attachment = record.public_send(attachment_name)
    return true unless attachment.attached?

    # test環境ではスキップ
    return true if Rails.env.test?

    blob = attachment.blob

    unless blob.service.exist?(blob.key)
      Rails.logger.error(
        "[AttachmentPersistence] missing blob " \
        "record=#{record.class.name}##{record.id} " \
        "attachment=#{attachment_name} blob_id=#{blob.id} key=#{blob.key}"
      )

      attachment.purge
      record.errors.add(attachment_name, "の保存に失敗しました。再度アップロードしてください")

      return false
    end

    true
  end
end
```

## 削除

`app/controllers/concerns/attachment_persistence_checker.rb`

---

# Step 13. mini_magick 依存を削除

## `app/models/booth.rb`

```ruby
class Booth < ApplicationRecord
  belongs_to :store
  belongs_to :current_stream_session, class_name: "StreamSession", optional: true

  has_many :stream_sessions, dependent: :restrict_with_error
  has_many :booth_casts, dependent: :restrict_with_error
  has_many :cast_users, through: :booth_casts, source: :cast_user
  has_many :favorite_booths, dependent: :destroy
  has_one_attached :thumbnail_image

  enum :status, { offline: 0, live: 1, away: 2, standby: 3 }

  scope :active, -> { where(archived_at: nil) }
  scope :archived, -> { where.not(archived_at: nil) }

  def archived?
    archived_at.present?
  end

  def primary_cast_user_id
    booth_casts.order(created_at: :desc, id: :desc).pick(:cast_user_id)
  end

  def primary_cast_user
    if booth_casts.loaded?
      booth_casts.max_by { |booth_cast| [ booth_cast.created_at, booth_cast.id ] }&.cast_user
    else
      booth_casts.includes(:cast_user).order(created_at: :desc, id: :desc).first&.cast_user
    end
  end
end
```

## `app/models/store.rb`

```ruby
# frozen_string_literal: true

class Store < ApplicationRecord
  belongs_to :referral_code, optional: true

  has_many :booths, dependent: :destroy
  has_many :store_memberships, dependent: :destroy
  has_many :drink_items, dependent: :destroy
  has_many :store_bans, dependent: :destroy
  has_many :favorite_stores, dependent: :destroy
  has_many :store_payout_accounts, dependent: :restrict_with_error
  has_many :settlements, dependent: :restrict_with_error
  has_many :settlement_carryovers, dependent: :restrict_with_error
  has_one :active_payout_account, -> { active }, class_name: "StorePayoutAccount"
  has_one_attached :thumbnail

  enum :business_type, {
    cabaret: 0,
    girls_bar: 1,
    snack: 2,
    lounge: 3,
    concept_cafe: 4,
    other: 5
  }

  validates :name, presence: true
  validates :description, length: { maximum: 1000 }, allow_nil: true
  validates :area, length: { maximum: 50 }, allow_nil: true

  BUSINESS_TYPE_LABELS = {
    cabaret: "キャバクラ",
    girls_bar: "ガールズバー",
    snack: "スナック",
    lounge: "ラウンジ",
    concept_cafe: "コンカフェ",
    other: "その他"
  }.freeze

  def self.business_type_select_options
    business_types.keys.map { |k| [ BUSINESS_TYPE_LABELS[k.to_sym] || k.to_s, k ] }
  end

  def payout_account_configured?
    pa = active_payout_account
    return false if pa.blank?

    if pa.manual_bank?
      pa.bank_code.present? &&
        pa.branch_code.present? &&
        pa.account_type.present? &&
        pa.account_number.present? &&
        pa.account_holder_kana.present?
    elsif pa.stripe_connect?
      pa.stripe_account_id.present?
    else
      false
    end
  end

  def payout_account_unconfigured?
    !payout_account_configured?
  end

  def payout_account_last4
    pa = active_payout_account
    return nil if pa.blank?
    return nil if pa.account_number.blank?

    pa.account_number.to_s.last(4)
  end
end
```

## 削除

`app/models/concerns/normalized_image_attachment.rb`

---

# Step 14. controllers はそのままでOK

今回の controller 側は、`purge_attachment_if_requested` と `ensure_attachment_persisted!` の使い方はそのままで成立します。
`InvalidImageAttachment` を使っている箇所がもう無いとのことなので、追加修正は不要です。

---

# Step 15. 動作確認

```bash
docker compose up
```

確認画面:

* `profile/edit`
* `cast/booths/:id/edit`
* `admin/booths/:id/edit`
* `admin/booths/new`
* `admin/stores/:id/edit`

確認ポイント:

* 既存画像が FilePond 内に初期表示される
* 初期画像削除で remove が立つ
* 新規画像追加で remove が戻る
* プロフィールは `1024x1024`
* ブース・店舗は `1920x1080`
* head 経由で script が読み込まれる
* 保存できる
* 既存フォーム項目が壊れていない

---

# Step 16. テスト

```bash
docker compose run --rm app bin/rails test
```

---

## 補足

* `app/controllers/dev` 配下の FilePond 検証用コードは今回触りません
* `attachment_persistence_checker.rb` は controller concern ではなく、今回 `models/concerns` に移す方が責務的に自然です。現状は controller 側にあります。
* `NormalizedImageAttachment` は mini_magick 依存の concern なので、今回の共通化方針なら削除で問題ありません。現状は `Booth` / `Store` から参照されていました。
* `application.html.erb` には `yield :head` が既にあるので、`content_for :head` への移動はそのまま成立します。

実装後に引っかかったところがあれば、その差分だけ見て詰めましょう。

---
---

了解です。
では、**どのファイルのどこをどう直すか** を、いまの最新状態ベースで**貼り替え単位**で出します。

今回はもうすでに、

* 初期画像URLを view から渡している
* hidden の remove flag も入っている

ので、**実際に直す中心は `app/javascript/controllers/image_upload_controller.js` だけ**です。
view 側は、今貼ってくれた内容を見る限り、ほぼ入っています。

---

# まず結論

## 今回、主に修正するファイル

* `app/javascript/controllers/image_upload_controller.js`

## 基本そのままでよいファイル

* `app/views/profiles/edit.html.erb`
* `app/views/booths/_form.html.erb`
* `app/views/admin/stores/_form.html.erb`

これら3つは、すでに

* `data-image-upload-initial-url-value`
* hidden field
* file field target

が入っているので、**追加修正は基本不要**です。

---

# 修正内容

いまの `image_upload_controller.js` は、`files: [...]` で初期表示しようとしています。
これ自体は方向性として悪くないのですが、**remove の状態管理をより明確にするために、初期画像があるかどうかを内部状態で持つ**ように直した方が分かりやすいです。

---

# 1. `app/javascript/controllers/image_upload_controller.js` を全文置き換え

```javascript
import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  static targets = ["input", "removeFlag"]
  static values = {
    initialUrl: String,
    width: Number,
    height: Number,
  }

  connect() {
    this.hadInitialFile = this.hasInitialUrlValue && this.initialUrlValue.length > 0
    this.initializing = true

    requestAnimationFrame(() => {
      this.initialize()
    })
  }

  disconnect() {
    if (this.pond) this.pond.destroy()
  }

  initialize() {
    if (!window.FilePond) return
    if (!this.hasInputTarget) return

    this.registerPlugins()

    this.pond = window.FilePond.create(this.inputTarget, {
      storeAsFile: true,
      allowMultiple: false,
      allowImagePreview: true,
      allowImageResize: true,
      allowImageTransform: true,
      allowReorder: false,
      allowProcess: false,
      allowRevert: false,

      imageResizeTargetWidth: this.widthValue || 1024,
      imageResizeTargetHeight: this.heightValue || 1024,
      imageResizeMode: "contain",
      imageResizeUpscale: false,

      imageTransformOutputMimeType: "image/jpeg",
      imageTransformOutputQuality: 94,
      imageTransformCanvasBackgroundColor: "#ffffff",

      acceptedFileTypes: [
        "image/jpeg",
        "image/png",
        "image/webp",
        "image/heic",
        "image/heif",
      ],

      labelIdle: `
        <div class="image-upload-picker">
          <i class="bi bi-camera-fill image-upload-picker__icon" aria-hidden="true"></i>
        </div>
      `,
    })

    this.bindEvents()

    if (this.hadInitialFile) {
      this.pond.addFile(this.initialUrlValue, { type: "local" }).finally(() => {
        this.initializing = false
      })
    } else {
      this.initializing = false
    }
  }

  bindEvents() {
    this.pond.on("addfile", () => {
      if (this.initializing) return
      this.removeFlagTarget.value = "0"
    })

    this.pond.on("removefile", () => {
      if (this.initializing) return

      const currentFilesCount = this.pond.getFiles().length

      if (currentFilesCount > 0) {
        this.removeFlagTarget.value = "0"
        return
      }

      this.removeFlagTarget.value = this.hadInitialFile ? "1" : "0"
    })
  }

  registerPlugins() {
    if (window.__filepondRegistered) return

    const plugins = [
      window.FilePondPluginImagePreview,
      window.FilePondPluginImageResize,
      window.FilePondPluginImageTransform,
    ].filter(Boolean)

    plugins.forEach((plugin) => {
      window.FilePond.registerPlugin(plugin)
    })

    window.__filepondRegistered = true
  }
}
```

---

# 2. view 側は原則そのままでOK

今の3ファイルは、今回必要な属性がすでに入っています。

## `app/views/profiles/edit.html.erb`

* `data-image-upload-initial-url-value`
* `data: { image_upload_target: "removeFlag" }`
* `data: { image_upload_target: "input" }`

が入っているので、このままで大丈夫です。

## `app/views/booths/_form.html.erb`

同様にそのままでOKです。

## `app/views/admin/stores/_form.html.erb`

これもそのままでOKです。

---

# 3. 何が変わるか

この修正でこうなります。

## 既存画像がある場合

* 画面を開いた時点で FilePond 内に画像が出る
* そのまま何もせず保存すれば、remove は `0`
* × で消して保存すれば、remove は `1`

## 既存画像がない場合

* 最初は空
* 新しい画像を選ぶと remove は `0`
* 選んだ画像を消して空に戻すと remove は `0`

## 既存画像あり + 新しい画像を選ぶ

* remove は `0`
* 新しい画像で上書き

---

# 4. もし「既存画像が表示されない」場合に見るところ

この修正を入れても表示されないなら、次に見るのはこのどちらかです。

* FilePond が `local` file の URL をうまく読めていない
* `url_for(...)` の URL がその画面から見えていない

ただ、今の形ならまず一度はこれで試せます。

---

# 5. あなたの「どこをどう修正するかわからない」に対する最短回答

一言でいうとこうです。

* **修正するのは `app/javascript/controllers/image_upload_controller.js` だけ**
* **そのファイルを上の全文で丸ごと置き換える**
* view は今のままでよい

---

この置き換えをしたあと、
「既存画像は出るけど削除が反映されない」みたいな症状が出たら、その時点で controller のイベント部分だけ詰めれば大丈夫です。
