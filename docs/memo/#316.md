了解、GO ありがとう。#316 は「**live 画面だけ**」で

* `visualViewport.height - headerHeight - footerHeight` を計算して
* CSS 変数 `--live-stage-h` に反映
* `#live_stage` をその高さで固定（overflow hidden）
* さらに **live 画面中だけ** `html/body` のスクロールを止める

までを実装します（Stage 内スクロール窓は #320/#321 で）。

---

## 実装手順（高レベル）

1. **Stimulus controller を新規追加**
   `live_stage_controller.js` を追加して、Stage高さの計算と CSS 変数更新を担当

* `visualViewport.height` 優先、fallback `window.innerHeight`
* `#app_header` / `#app_footer` の `getBoundingClientRect().height`
* `resize` / `orientationchange` / `visualViewport.resize` / `visualViewport.scroll` で更新
* live 中だけ `html/body { overflow: hidden }` 相当にしてページスクロールを殺す（disconnect で戻す）

2. **live.html.erb に Stage ラッパを追加**

* `data-controller="ivs-publisher live-stage"` にして controller を併用
* `#live_stage` で main（本文）を包む
* `main` の `pb-5` は **不要**（Stage 高さが footer 分を引いているため）

3. **CSS を追加**（live 専用・非 live へ影響なし）

* `:root { --live-stage-h: 0px; }`
* `#live_stage { height: var(--live-stage-h); overflow: hidden; position: relative; }`

---

## ブランチ名（最終候補）

* `improve/316-live-stage-fixed-height`
* `improve/316-live-stage-viewport-layout`
* `improve/316-live-stage-visualviewport`

---

## 変更内容（コピペ用：差分）

### 1) 新規: `app/javascript/controllers/live_stage_controller.js`

```js
import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  connect() {
    // live中だけスクロールを殺す（non-live へ影響を残さない）
    this._prevHtmlOverflow = document.documentElement.style.overflow
    this._prevBodyOverflow = document.body.style.overflow
    document.documentElement.style.overflow = "hidden"
    document.body.style.overflow = "hidden"

    this._update = this._update.bind(this)

    // window events
    window.addEventListener("resize", this._update)
    window.addEventListener("orientationchange", this._update)

    // visualViewport events（iOS Safari 対策）
    this._vv = window.visualViewport || null
    if (this._vv) {
      this._vv.addEventListener("resize", this._update)
      // アドレスバー出入り等で height 変化が scroll で発生するケースがある
      this._vv.addEventListener("scroll", this._update)
    }

    // 初回計測（layout描画後に安定させる）
    requestAnimationFrame(() => this._update())
  }

  disconnect() {
    window.removeEventListener("resize", this._update)
    window.removeEventListener("orientationchange", this._update)

    if (this._vv) {
      this._vv.removeEventListener("resize", this._update)
      this._vv.removeEventListener("scroll", this._update)
    }

    // スクロール抑止を戻す（liveだけの副作用に閉じる）
    document.documentElement.style.overflow = this._prevHtmlOverflow
    document.body.style.overflow = this._prevBodyOverflow
  }

  _update() {
    const header = document.getElementById("app_header")
    const footer = document.getElementById("app_footer")

    const headerH = header ? header.getBoundingClientRect().height : 0
    const footerH = footer ? footer.getBoundingClientRect().height : 0

    const viewportH = this._vv?.height || window.innerHeight || 0

    // 端末・バー出入りのタイミングで小数や負数が出るので丸め＆下限
    const stageH = Math.max(0, Math.round(viewportH - headerH - footerH))

    document.documentElement.style.setProperty("--live-stage-h", `${stageH}px`)
  }
}
```

---

### 2) 変更: `app/views/cast/booths/live.html.erb`

```diff
 <div
-  data-controller="ivs-publisher"
+  data-controller="ivs-publisher live-stage"
   data-ivs-publisher-token-url-value="<%= stream_session_ivs_participant_tokens_path(@stream_session) %>"
   data-ivs-publisher-finish-url-value="<%= finish_cast_stream_session_path(@stream_session) %>"
   data-ivs-publisher-status-url-value="<%= status_cast_booth_path(@booth) %>"
   data-ivs-publisher-mirror-value="true"
   data-ivs-publisher-initial-mode-value="<%= @booth.away? ? "away" : "normal" %>"
   data-ivs-publisher-initial-booth-status-value="<%= @booth.status %>"
 >
-<main class="flex-grow-1 pb-5">
+<div id="live_stage">
+<main class="flex-grow-1">
   <div class="container mt-3">
     <div id="flash_inner">
       <% if notice.present? %>
         <div class="alert alert-success" role="alert"><%= notice %></div>
       <% end %>
       <% if alert.present? %>
         <div class="alert alert-danger" role="alert"><%= alert %></div>
       <% end %>
     </div>
   </div>

   <div class="container my-4">
     <h1>Cast Booth（Live）</h1>

     <p>booth: <strong>#<%= @booth.id %> <%= @booth.name %></strong></p>
     <p>status: <strong><%= @booth.status %></strong></p>
     <%= render "cast/booths/stream_meta",
                booth: @booth,
                stream_session: @stream_session %>

     <div class="card">
       <div class="card-header">
         配信（Real-Time Stage）
         <span class="ms-2 text-muted small" data-ivs-publisher-target="state">idle</span>

         <% if @booth.standby? %>
           <%= button_to "サマリーへ戻る",
                 finish_cast_stream_session_path(@stream_session),
                 method: :post,
                 class: "btn btn-outline-secondary",
                 data: { ivs_publisher_target: "summaryBtn" } %>
         <% end %>
       </div>

       <div class="card-body">
         <video class="w-100 rounded"
                style="aspect-ratio: 16 / 9; background: #000;"
                playsinline autoplay muted
                data-ivs-publisher-target="preview"></video>

         <canvas class="w-100 rounded d-none"
                 style="aspect-ratio: 16 / 9; background: #000;"
                 width="1280" height="720"
                 data-ivs-publisher-target="canvas"></canvas>

         <div class="mt-2 text-danger small"
              data-ivs-publisher-target="error"></div>

         <div class="mt-2 d-flex gap-2 flex-wrap">

           <!-- 離席 -->
           <%= form_with url: status_cast_booth_path(@booth, to: :away),
                         method: :patch,
                         data: { action: "turbo:submit-end->ivs-publisher#onBoothStatusPatched" } do %>
             <button type="submit"
               class="btn btn-warning btn-sm"
               data-ivs-publisher-target="awayBtn">
               離席
             </button>
           <% end %>

           <!-- 復帰 -->
           <%= form_with url: status_cast_booth_path(@booth, to: :live),
                         method: :patch,
                         data: { action: "turbo:submit-end->ivs-publisher#onBoothStatusPatched" } do %>
             <button type="submit"
               class="btn btn-success btn-sm"
               data-ivs-publisher-target="backBtn">
               復帰
             </button>
           <% end %>

           <!-- 離席中音声 -->
           <div class="form-check form-switch mt-3">
             <input class="form-check-input"
                    type="checkbox"
                    role="switch"
                    id="away-audio"
                    data-ivs-publisher-target="awayAudio"
                    data-action="change->ivs-publisher#toggleAwayAudio"
                    disabled>
             <label class="form-check-label" for="away-audio">
               離席中も音声を送る
             </label>
           </div>
         </div>
       </div>
     </div>
     <%= turbo_stream_from [@stream_session, :cast_pending_drink_orders] %>

     <div id="cast-pending-drink-orders-container">
       <%= render "cast/stream_sessions/pending_drink_orders", stream_session: @stream_session %>
     </div>

     <hr>

     <%= turbo_stream_from [@stream_session, :comments] %>

     <div id="cast_comment_section">
       <%= render "cast/booths/comment_section",
                  booth: @booth,
                  stream_session: @stream_session,
                  comments: @comments %>
     </div>
   </div>
 </main>
+</div>

 <footer id="app_footer" class="border-top bg-white fixed-bottom">
   <div class="container py-2">
     <div class="d-flex justify-content-between align-items-center">
```

> ポイント
>
> * `pb-5` を削除：Stage 高さが footer 分を差し引くので、padding でさらに押し下げると無駄に溢れます
> * main を `#live_stage` で包んで、Stage の高さ固定を効かせます

---

### 3) 変更: `app/assets/stylesheets/application.scss`

```diff
 @use "bootstrap/scss/bootstrap" as *;

+:root {
+  --live-stage-h: 0px;
+}
+
+#live_stage {
+  height: var(--live-stage-h);
+  overflow: hidden;
+  position: relative;
+}
+
 .header-logo {
   height: 24px;
   width: auto;
   display: block;
 }
```

---

## Done 条件のチェック観点（実装後セルフチェック）

* `cast/booths/live` で **ページ全体の縦スクロールが出ない**

  * iPhone Safari：アドレスバー出入りしても “ガクッ” と崩れない（`visualViewport` + scroll/resize 追従）
* 画面回転で `--live-stage-h` が更新され、Stage が header/footer を避けた高さに追従
* Stage 内が溢れても **Stage 側で隠れる**（overflow hidden）
* 他画面に影響がない（live controller の connect/disconnect で overflow を元に戻す）

---

## 参考: 実行確認コマンド（このプロジェクトの方針に合わせて）

CSS 反映確認（watch）:

```bash
docker compose run --rm web npm run watch:css
```

---

このまま実装してOKです。もし CI/ブラウザで「iOS Safari だけ 1〜2px だけスクロールできる」みたいな症状が出たら、次の一手（`stageH` に 1〜2px の余裕を引く、など）を #316 の範囲で追加できます。
