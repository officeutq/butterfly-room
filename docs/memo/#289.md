了解、**「standbyでも購読は維持」「`#comments` は空コンテナだけ置く（見せない）」「送信はサーバ側でも拒否」**で進めます。
以下、実装プラン → 変更コード（コピペ用）まで一気に出します。

---

## 実装プラン（#289）

### 1) UI（cast/booths/live）

* `turbo_stream_from [@stream_session, :comments]` は **常に描画**（standbyでも購読維持）
* `div#comments` も **常に描画**（空コンテナでOK）
* コメント一覧（partial render）と送信フォームは **`@booth.live? || @booth.away?` のときだけ描画**
* standby ではフォームも一覧も出さない（=表示/送信の導線を消す）

### 2) サーバ側ガード（CreateService）

* `@stream_session.booth.status` が **live/away 以外なら例外**（standby投稿を拒否）
* 返すステータスは **409 Conflict**（仕様案の「422 or 409」のうち、状態不整合なので 409 を採用）

### 3) テスト追加（integration）

* standby の live 画面：

  * `turbo-cable-stream-source`（購読）が含まれる
  * `div#comments` が存在する
  * `comment_form`（turbo-frame）が **存在しない**
* standby でコメントPOST（turbo_stream）：

  * `Comment.count` が増えない
  * レスポンスが **409**

---

## 変更コード（貼り替えでOK）

### 1) `app/services/stream_sessions/comments/create_service.rb`（全文置換）

```ruby
# frozen_string_literal: true

module StreamSessions
  module Comments
    class CreateService
      class BannedError < StandardError; end
      class RateLimitedError < StandardError; end
      class BoothNotLiveError < StandardError; end

      def initialize(stream_session:, user:, body:)
        @stream_session = stream_session
        @user = user
        @body = body.to_s.strip
      end

      def call
        raise ActiveRecord::RecordInvalid.new(Comment.new) if @body.blank?
        raise BoothNotLiveError unless booth_allows_comments?
        raise BannedError if banned?
        raise RateLimitedError if rate_limited?

        comment = Comment.create!(
          stream_session: @stream_session,
          booth_id: @stream_session.booth_id,
          user: @user,
          body: @body
        )

        CommentNotifier.append(comment)
        comment
      end

      private

      def booth_allows_comments?
        booth = @stream_session.booth
        booth.live? || booth.away?
      end

      def banned?
        return false unless @user.customer?
        StoreBan.exists?(store_id: @stream_session.store_id, customer_user_id: @user.id)
      end

      def rate_limited?
        Comment.where(user_id: @user.id, stream_session_id: @stream_session.id)
               .where("created_at >= ?", 1.second.ago)
               .exists?
      end
    end
  end
end
```

---

### 2) `app/controllers/stream_sessions/comments_controller.rb`（全文置換）

```ruby
# frozen_string_literal: true

module StreamSessions
  class CommentsController < ApplicationController
    include StoreBanGuard

    before_action :authenticate_user!
    before_action :set_stream_session
    before_action :reject_banned_customer_for_stream_session!

    def create
      StreamSessions::Comments::CreateService.new(
        stream_session: @stream_session,
        user: current_user,
        body: params.require(:comment).fetch(:body)
      ).call

      respond_to do |format|
        format.turbo_stream do
          render turbo_stream: turbo_stream.replace(
            "comment_form",
            partial: "stream_sessions/comments/form",
            formats: [ :html ],
            locals: { stream_session: @stream_session }
          )
        end
        format.html { redirect_back fallback_location: root_path }
      end
    rescue StreamSessions::Comments::CreateService::BoothNotLiveError
      render_comment_form_error("配信中（live/away）のときのみコメントできます", status: :conflict)
    rescue StreamSessions::Comments::CreateService::RateLimitedError
      render_comment_form_error("送信が速すぎます。少し待ってからもう一度送信してください", status: :unprocessable_entity)
    rescue ActionController::ParameterMissing, KeyError
      render_comment_form_error("入力が不正です", status: :unprocessable_entity)
    end

    private

    def set_stream_session
      @stream_session = StreamSession.find(params[:stream_session_id])
    end

    def reject_banned_customer_for_stream_session!
      reject_banned_customer!(store: @stream_session.store)
    end

    def render_comment_form_error(message, status:)
      respond_to do |format|
        format.turbo_stream do
          render(
            turbo_stream: turbo_stream.replace(
              "comment_form",
              partial: "stream_sessions/comments/form",
              formats: [ :html ],
              locals: { stream_session: @stream_session, error_message: message }
            ),
            status: status
          )
        end
        format.html { redirect_back fallback_location: root_path, alert: message }
      end
    end
  end
end
```

---

### 3) `app/views/cast/booths/live.html.erb`（コメント周りだけ置換）

このファイルは長いので、**`<%= turbo_stream_from [@stream_session, :comments] %>` から下のコメント領域**を、下記に差し替えてください（他はそのまま）。

```erb
<%= turbo_stream_from [@stream_session, :comments] %>

<div id="comments">
  <% if @booth.live? || @booth.away? %>
    <%= render partial: "comments/comment", collection: @comments, as: :comment %>
  <% end %>
</div>

<% if @booth.live? || @booth.away? %>
  <%= render "stream_sessions/comments/form", stream_session: @stream_session %>
<% end %>
```

ポイント：

* standby でも購読は張る
* standby でも `#comments` は存在（空）
* standby ではフォーム自体が描画されない（送信導線が無い）

---

### 4) `test/integration/cast/booths_two_screens_test.rb`（追記）

既存テストに、**次の2つの test を末尾に追加**してください。

```ruby
  test "standby: live subscribes comments but does not render comments UI" do
    StreamSessions::StartService.new(booth: @booth, actor: @cast).call
    assert @booth.reload.standby?

    get live_cast_booth_path(@booth)
    assert_response :success

    # 購読は維持される（turbo_stream_fromが生成するタグが含まれる）
    assert_includes response.body, "turbo-cable-stream-source"

    # append先の空コンテナは存在する
    assert_includes response.body, 'id="comments"'

    # フォーム（turbo-frame）は表示しない
    refute_includes response.body, 'id="comment_form"'
  end

  test "standby: cannot create comment (turbo_stream) and returns 409" do
    session = StreamSessions::StartService.new(booth: @booth, actor: @cast).call
    assert @booth.reload.standby?

    assert_no_difference "Comment.count" do
      post stream_session_comments_path(session),
           params: { comment: { body: "hello" } },
           headers: { "Accept" => "text/vnd.turbo-stream.html" }
    end

    assert_response :conflict
  end
```

---

## 動作確認（手元でやる順）

* standby で cast live を開く

  * コメント一覧/フォームが見えない
  * ただし購読は張られている（HTMLに turbo-cable-stream-source がある）
* standby 中に（仮にリクエストを作って）コメントPOST

  * 409
  * DBに保存されない
* live / away にすると従来通り表示・送信できる

---

必要なら、最後にコミットメッセージ案・PR本文もこの変更に合わせて作れます。

---
---
原因は確定です。

* `startBroadcast()` 内の `await this._patchBoothStatus("live")` は **fetch + Accept: application/json** で `Cast::BoothsController#status` を叩いています
* その結果、**HTML（ERB）の再評価は一切起きない**ので、standby 初期レンダリングで「フォーム無し」だった状態のまま固定されます
* 一方、視聴者コメントが反映されるのは `turbo_stream_from [@stream_session, :comments]` と `#comments` があるので append が効くから（これはOK）

なので、**booth.status が変わったタイミングで「コメントUI部分だけ」を Turbo Stream で差し替える**のが正解です。

---

# 解決策（最小で堅い）

## 方針

1. cast live 画面に「コメントUIの差し替え用コンテナ（例：`#cast_comment_section`）」を常設
2. `Cast::BoothsController#status` が **turbo_stream でも返せる**ようにして、そのコンテナを `turbo_stream.replace` する
3. `ivs_publisher_controller.js` の `_patchBoothStatus()` は `Accept: text/vnd.turbo-stream.html` で叩き、返ってきた turbo-stream を `Turbo.renderStreamMessage()` で適用する
   （※フォーム submit の away/live 切替は今まで通りでもOK。今回の「startBroadcast(fetch)」が主原因なので、そこを確実に直す）

これで standby→live の瞬間に **コメントフォームが出ます**。

---

# 変更点（コピペ実装）

## 1) `app/views/cast/booths/live.html.erb`（コメント周りを差し替え）

あなたが前回入れた「購読は維持しつつ、UIを条件表示」のブロックを、次に置き換えてください。

```erb
<%= turbo_stream_from [@stream_session, :comments] %>

<div id="cast_comment_section">
  <%= render "cast/booths/comment_section",
             booth: @booth,
             stream_session: @stream_session,
             comments: @comments %>
</div>
```

> ※購読は **このまま常に維持**。
> ※`cast_comment_section` だけを status 更新のたびに差し替えます。

---

## 2) 新規 partial：`app/views/cast/booths/_comment_section.html.erb`

新しくこのファイルを作ってください。

```erb
<%# standbyでも Turbo append の target が必要なので、#comments は常に置く %>
<div id="comments">
  <% if booth.live? || booth.away? %>
    <%= render partial: "comments/comment", collection: comments, as: :comment %>
  <% end %>
</div>

<% if booth.live? || booth.away? %>
  <%= render "stream_sessions/comments/form", stream_session: stream_session %>
<% end %>
```

* standby：`#comments` は空、フォーム無し
* live/away：一覧＋フォーム表示

---

## 3) `app/controllers/cast/booths_controller.rb`（status の turbo_stream を実装）

`def status` の `respond_to` を次のように調整してください（メソッド全文で置換してOK）。

```ruby
def status
  booth = StreamSessions::StatusService.new(
    booth: @booth,
    actor: current_user,
    to_status: params[:to]
  ).call

  StreamSessionNotifier.broadcast_stream_state(booth: booth)

  stream_session = booth.current_stream_session
  comments =
    if stream_session
      Comment.alive.where(stream_session: stream_session)
             .order(created_at: :desc).limit(50).reverse
    else
      []
    end

  respond_to do |format|
    format.html { redirect_to live_cast_booth_path(@booth), notice: "状態更新: #{params[:to]}" }

    format.turbo_stream do
      render turbo_stream: turbo_stream.replace(
        "cast_comment_section",
        partial: "cast/booths/comment_section",
        locals: { booth: booth, stream_session: stream_session, comments: comments }
      )
    end

    format.json { render json: { ok: true }, status: :ok }
    format.any { head :no_content }
  end
rescue => e
  respond_to do |format|
    format.html { redirect_to live_cast_booth_path(@booth), alert: e.message }
    format.turbo_stream { render plain: e.message, status: :unprocessable_entity }
    format.json { render json: { error: e.message }, status: :unprocessable_entity }
    format.any { render plain: e.message, status: :unprocessable_entity }
  end
end
```

ポイント：

* `format.turbo_stream` を追加
* `cast_comment_section` を差し替えるだけ（購読は触らない）

---

## 4) `app/javascript/controllers/ivs_publisher_controller.js`（_patchBoothStatus を Turbo Stream 対応）

`_patchBoothStatus(to)` を、下の実装に置き換えてください。

```js
async _patchBoothStatus(to) {
  console.log("[ivs-publisher] statusUrlValue=", this.statusUrlValue, "to=", to)
  if (!this.statusUrlValue) return

  const url = new URL(this.statusUrlValue, window.location.origin)
  url.searchParams.set("to", to)

  const resp = await fetch(url.toString(), {
    method: "PATCH",
    redirect: "manual",
    credentials: "same-origin",
    headers: {
      "Accept": "text/vnd.turbo-stream.html",
      "X-CSRF-Token": document.querySelector('meta[name="csrf-token"]')?.content
    },
  })

  if (resp.status >= 300 && resp.status < 400) {
    return
  }
  if (!resp.ok) throw new Error(`booth_status_failed(${resp.status})`)

  // Turbo Stream を手動適用
  const html = await resp.text()
  if (html && window.Turbo?.renderStreamMessage) {
    window.Turbo.renderStreamMessage(html)
  }
}
```

これで、standby→live になった瞬間に `cast_comment_section` が差し替わり、**フォームが出ます**。

---

# 追加テスト（任意だが堅くする）

現状のテストは「スタンバイ時にフォームが無い」までは見ていますが、
「スタンバイ→live にしたらフォームが出る」は未検証です。

integration でやるなら、

* standby で GET live（フォーム無し）
* PATCH status to=live を **Accept turbo-stream** で叩く
* 返ってきた turbo-stream 本文に `comment_form` が含まれることを assert
  くらいが最小です（ブラウザDOMまでは見ない）。

---

# 動作確認手順

1. standby 開始 → cast live 画面：フォーム無し（OK）
2. 「配信開始」押下（startBroadcast）

   * `_patchBoothStatus("live")` が turbo-stream を受け取り適用
   * **フォームが表示される（OK）**
3. viewer コメント → append で反映（OK）
4. 終了 → reload → standby 相当に戻る：フォーム消える（OK）

---

必要なら、この修正に合わせた **コミットメッセージ案 / PR本文**も出します。
