ALL FILES PROVIDED と判断します。では **GO 相当**として、ここからは最終アウトプット（実装計画＋貼り付けコード）に入ります。

---

## 実装方針（最小差分・A案確定）

### 変更点の核

* `StreamSessions::StartService#authorize!` を **役割判定だけ**から、以下の **所属/権限込み**へ変更する

  * **system_admin**: 常に許可
  * **store_admin以上**: `actor.admin_of_store?(booth.store_id)` が true のとき許可
  * **cast**: `BoothCast.exists?(cast_user_id: actor.id, booth_id: booth.id)` が true のとき許可
  * それ以外（未ログイン/不正）: 拒否
* 拒否時は `NotAuthorized` を raise し、controller の `alert: e.message` に載るので **メッセージをユーザー向けにする**

  * 文言は既存と揃えて **「選択できないブースです」** が無難（UI導線と同じ）

### なぜ BoothPolicy を触らないか

* `StreamSessionPolicy#publish_token?` が `BoothPolicy#cast_operate?` を “cast以上” として使っているため、ここに所属チェックを混ぜると影響が読みにくい
  → **StartServiceに閉じて修正**が最小差分・安全

---

## 回帰防止テスト方針（最小で堅い）

* `test/services/stream_sessions/start_service_test.rb` を新規作成（fixtures無し流儀に合わせて create! で組む）
* 追加するテストは最低2本：

  1. `cast` が **booth_casts 非所属** の booth で `StartService.call` → `NotAuthorized`（メッセージも確認）
  2. `cast` が **所属** している booth で開始できる（session作成 & boothが standby になる）

（任意だが安全確認として）

* store_admin の「自分の store の booth だけOK」を 1本入れても良い（ただし Issue 本文の最小要件ではないので、今回は入れなくてもOK）

---

## 変更対象ファイル

* ✅ `app/services/stream_sessions/start_service.rb`（認可修正）
* ✅ `test/services/stream_sessions/start_service_test.rb`（新規）

controller や policy は触らない（最小差分）

---

## 貼り付けコード（そのまま置き換え/新規作成）

### 1) `app/services/stream_sessions/start_service.rb`（全文置き換え）

```ruby
# frozen_string_literal: true

module StreamSessions
  class StartService
    class Error < StandardError; end
    class BoothNotOffline < Error; end
    class NotAuthorized < Error; end
    class BoothStageNotBound < Error; end
    class BoothArchived < Error; end

    def initialize(booth:, actor:)
      @booth = booth
      @actor = actor
    end

    def call
      authorize!

      StreamSession.transaction do
        booth = Booth.lock.find(@booth.id)

        raise BoothArchived, "booth is archived" if booth.archived?

        raise BoothNotOffline, "booth is #{booth.status}" unless booth.offline?

        raise BoothStageNotBound, "booth.ivs_stage_arn is blank" if booth.ivs_stage_arn.blank?

        # NOTE:
        # Stage は booth 固定。stream_session は booth.ivs_stage_arn をコピーして保持するだけ。
        # stream_session 起点で Stage を作る処理は廃止済み（旧 EnsureIvsStageService）。

        session = StreamSession.create!(
          booth: booth,
          store: booth.store,
          status: :live, # ※ここは現状維持でもOK（後で整理してもよい）
          started_at: Time.current,
          started_by_cast_user: @actor,
          ivs_stage_arn: booth.ivs_stage_arn # ★ここが #123
        )

        booth.update!(
          status: :standby,
          current_stream_session_id: session.id
        )

        session
      end
    end

    private

    def authorize!
      actor = @actor
      booth = @booth

      allowed =
        if actor.blank?
          false
        elsif actor.system_admin?
          true
        elsif actor.at_least?(:store_admin)
          actor.admin_of_store?(booth.store_id)
        else
          BoothCast.exists?(cast_user_id: actor.id, booth_id: booth.id)
        end

      raise NotAuthorized, "選択できないブースです" unless allowed
    end
  end
end
```

### 2) `test/services/stream_sessions/start_service_test.rb`（新規作成）

```ruby
# frozen_string_literal: true

require "test_helper"

class StreamSessions::StartServiceTest < ActiveSupport::TestCase
  test "cast cannot start stream when not belong to booth" do
    store = Store.create!(name: "Test Store")

    booth = Booth.create!(
      store: store,
      name: "Test Booth",
      status: :offline,
      ivs_stage_arn: "arn:aws:ivs:ap-northeast-1:123456789012:stage/abc"
    )

    cast = User.create!(email: "cast1@example.com", password: "password", role: :cast)
    other_cast = User.create!(email: "cast2@example.com", password: "password", role: :cast)

    # cast は booth に紐付けるが、other_cast は紐付けない
    BoothCast.create!(booth: booth, cast_user: cast)

    err = assert_raises(StreamSessions::StartService::NotAuthorized) do
      StreamSessions::StartService.new(booth: booth, actor: other_cast).call
    end
    assert_equal "選択できないブースです", err.message
  end

  test "cast can start stream when belong to booth" do
    store = Store.create!(name: "Test Store")

    booth = Booth.create!(
      store: store,
      name: "Test Booth",
      status: :offline,
      ivs_stage_arn: "arn:aws:ivs:ap-northeast-1:123456789012:stage/abc"
    )

    cast = User.create!(email: "cast@example.com", password: "password", role: :cast)
    BoothCast.create!(booth: booth, cast_user: cast)

    session = StreamSessions::StartService.new(booth: booth, actor: cast).call

    assert session.persisted?
    assert_equal booth.id, session.booth_id
    assert_equal store.id, session.store_id
    assert_equal cast.id, session.started_by_cast_user_id

    booth.reload
    assert booth.standby?
    assert_equal session.id, booth.current_stream_session_id
  end
end
```

---

## 既存テストとの整合（参考）

* `Cast::BoothsTwoScreensTest` が `StartService.new(...).call` を直接呼んでいて、そこでは `BoothCast` を作っているので、この変更で壊れません
* fixtures が無い運用なので、今回の service test も `create!` で揃えています（`FavoriteBoothTest` と同じ流儀）

---

## 動作確認チェック（Done 条件に対応）

* cast（非所属）で `POST /cast/booths/:id/stream_sessions` を叩く

  * `current_booth` が弾くケースもあるが、仮に到達しても **StartService が拒否**される
  * controller は `alert: e.message` → **「選択できないブースです」** が出る想定
* cast（所属）で開始でき、booth が standby になり live 画面へ遷移
* store_admin / system_admin の挙動は、`current_booth` と同じルールで StartService も許可するため整合

---

必要なら、このまま **コミットメッセージ案 / PR本文（変更点・仕様・動作確認）** も #134 向けに書けます。
