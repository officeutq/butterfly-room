# 選択の既存入口と共通処理の対応（#1274）

2026-09-16。#1256の調査時点の41項目は変更せず、現在の実装への対応を記録する。

[採用仕様](current_selection.md)に従いサーバー側の接続と、#1275のヘッダー/カードの固定表示・未保存確認・旧キャスト一覧撤去を実装し、mainへマージ済み。配信準備から他者配信中/閉鎖済みへの切替案内も#1276で実装した。ステージングへの反映・確認は[環境別記録](../ops/current_selection_staging_verification.md)を参照する。本番へは未反映。管理用店舗情報とブース管理統合は各担当Issueに残る。

| No. | 調査時点の動くタイミング | 現在の種類 | 接続先・維持理由 |
| --- | --- | --- | --- |
| 1 | メールまたはSMSでログインが完了した時 | ログイン共通判定 | ApplicationController#after_sign_in_path_for。メール/SMSとも本人Y・候補数・有効選択を再判定。 |
| 2 | ヘッダーからブース選択を開いた時 | 候補参照 | Cast::BoothsController#select_modal。ヘッダー指定はnormalize、必要操作はrequire_booth。準備を呼ばない。 |
| 3 | ブース選択モーダルで候補を選んで確定した時 | 選択確定 | Cast::CurrentBoothsController#create。再判定後、情報は保存だけ、明示的な配信画面復帰はPrepareSelectedBoothServiceの成功後に保存。他者配信中/閉鎖済みは情報へ。 |
| 4 | ヘッダーから店舗選択を開いた時 | 候補参照 | Admin::StoresController#select_modal。本人固定・唯一店舗を共通結果から参照し、同一店舗のブースを消さない。 |
| 5 | 店舗選択モーダルで候補を選んで確定した時 | 店舗変更確定 | Admin::CurrentStoresController#create。D01、同一店舗維持、不正要求拒否。 |
| 6 | ダッシュボードの「ブース情報」を押した時 | 必要操作 | 共通require_boothとURL照合。D01の唯一ブースが別店舗の場合もここで自動設定。 |
| 7 | ダッシュボードの店舗向け各カードを押した時 | 必要操作 | Admin::BaseControllerから共通require_storeへ。複数の最小ID補完を廃止。 |
| 8 | フッターの「配信」を押し、現在のブースへ進む時 | 準備入口 | BoothsController#enter。要求対象を認可・選択照合して既存EnterAsCastService。 |
| 9 | フッターの「配信」を押し、ブース選択へ進む時 | 候補参照→準備入口 | モーダルGETでは準備しない。確定した選択先のliveまたは選択POSTで準備する。閉鎖済みは準備せず情報で案内。 |
| 10 | フッターの「キャスト招待」を開いた時 | 選択を変えない参照 | Admin::CastInvitationsController#entry_storeは共通current_store。発行時にも対象を照合。 |
| 11 | 旧キャスト招待一覧のURLを開いた時 | 選択を変えない参照 | 同Controller#indexも共通current_store。招待一覧への転送を維持。 |
| 12 | キャスト向けブース一覧で「このブースへ切り替え」を押した時 | 入口廃止 | #1275で旧一覧・カードを撤去。旧GETは認可後ダッシュボードへ戻す。 |
| 13 | 店舗管理のブース管理で「このブースへ切り替え」を押した時 | 後続の画面撤去 | 選択POSTは共通化済み。旧管理一覧の切替ボタン・一覧GET撤去は#1293。 |
| 14 | 店舗一覧で店舗を選んで確定した時 | 店舗変更確定 | 店舗一覧の確定先も同じAdmin::CurrentStoresController#create。 |
| 15 | ブース情報の「編集」を押した時 | 対象固定の編集入口 | Cast::BoothsControllerで認可後に選択と照合。URLでは選択しない。 |
| 16 | ブース情報の「配信履歴」を押した時 | 対象固定の履歴参照 | Cast::Booths::StreamSessionsController#index。管理者の閉鎖済みを維持、キャストの直接閉鎖済み履歴は選択なしで維持。 |
| 17 | キャスト向けブース一覧・店舗管理のブース管理で「編集」を押した時 | 対象固定の編集入口 | 同じ編集Controllerで不一致を案内。キャスト一覧は撤去済み、管理一覧の撤去は#1293。 |
| 18 | ブースの編集URLを直接開いた時 | 対象固定の編集入口 | ①②の未選択補正を除き選択を変更しない。不正対象を先に認可。 |
| 19 | ブースの配信履歴URLを直接開いた時 | 対象固定の履歴参照 | 同じ履歴Controller。別ブースへの暗黙の切替なし。 |
| 20 | キャスト向けブース一覧で「配信」を押した時 | 入口廃止 | #1275で旧一覧を撤去。配信フッターなどの必要操作から共通選択・準備へ接続する。 |
| 21 | 店舗管理のブース管理で「配信/視聴」を押した時 | 対象固定の参照 | 選択外の公開ブースは視聴へ。選択中は視聴/配信の選択、続く配信入口で選択照合。非公開は配信不可。 |
| 22 | 共通ブースカードからブースを開いた時 | 対象固定の参照/準備入口 | 選択外は視聴へ進み管理選択を維持。選択中は担当キャストなら準備、管理者なら視聴/配信を選択。明示的な配信要求は別入口で照合。 |
| 23 | 視聴／配信の選択画面で「配信する」を押した時 | 準備入口 | BoothsController#enter_as_cast。未選択は必要操作モーダルへ、有効な別対象は409。 |
| 24 | ブースの配信画面URLを直接開いた時 | 準備入口 | Cast::BoothsController#live。対象照合後にPrepareSelectedBoothServiceが既存準備作成/再利用へ委譲し、閉鎖済み/他者配信中は情報へ。選択だけのGETと区別。 |
| 25 | 既に開いている配信画面を再表示した時 | 選択を変えない参照 | live再表示でURLのブースを再選択しない。次のアクセスで本人固定を再確認。 |
| 26 | 編集・配信履歴画面を再表示した時、別画面から戻された時 | 選択を変えない参照 | 情報・編集・履歴の対象照合とselection_return_pathで旧URLへの巻き戻りを防止。 |
| 27 | ブース編集画面で保存した時 | 対象固定の保存 | 古い対象への更新は409。JSON/Turboはフォームを置換せず、HTMLは入力を保持して再表示。 |
| 28 | 配信画面からブースの状態更新が送られた時 | 対象固定の状態変更 | statusはURLの認可と既存StatusService、Y/世代の契約を維持。選択を書き換えない。 |
| 29 | 配信リザルトを開き、配信画面へ戻された時 | 対象固定の参照 | Cast::StreamSessionsController#showは配信ID固定。未終了のliveへの復帰もそこで選択照合。 |
| 30 | 配信タイトルの保存後、配信画面へ戻された時 | 対象固定の保存 | metadataは配信ID固定のまま。配信の現在参照・準備中条件を維持し選択を書き換えない。 |
| 31 | 配信終了処理に失敗し、配信画面へ戻された時 | 対象固定の回復 | finishの既存Y/世代認可と終了/取消経路を維持。選択不一致を理由に終了を奪わない。 |
| 32 | キャスト招待の承認が成功した時 | 成功時だけのD02 | 承認Service成功後にinvitation_accepted。名前付き通知。invitation_booth_edit_idで初回B編集を固定。 |
| 33 | 店舗管理者招待の承認が成功した時 | 業務対象固定＋共通判定 | 店舗管理者招待の所属対象は招待店舗。成功後はnormalizeで既存の有効な選択を維持。 |
| 34 | 新規店舗登録が成功した時 | 新規利用者の共通判定 | Stores::RegistrationsController#create。新規利用者の唯一店舗を設定。初回設定の業務対象IDは維持。 |
| 35 | 店舗の代行登録が成功した時 | 業務対象固定＋共通判定 | Admin::StoresController#create。新店舗作成後も有効な選択を維持。新店舗編集ではヘッダーの選択を案内。 |
| 36 | キャスト画面で現在ブースを読み出した時 | 共通参照 | Cast::BaseControllerのparams/最小ID補完を削除。CurrentSelection#current_booth。 |
| 37 | 管理画面で現在店舗を読み出した時 | 共通参照 | Admin::BaseControllerの最小所属補完を削除。ヘッダー・Dashboardも同じ共通結果を使用。 |
| 38 | ブースに関する要求の存在・権限チェックで拒否された時 | 無効選択の整理 | 不許可処理からsessionの削除を除去。通常判定で元の選択自体が無効な場合だけ整理。 |
| 39 | 店舗切替要求の存在・権限チェックで拒否された時 | 不正要求の拒否 | 店舗選択は成功結果だけ保存。不正な要求で有効なブース・店舗を消さない。 |
| 40 | リンクにマウスを置くなどして、クリック前に先読み通信が送られた時 | 先読み抑止 | 準備/候補入口のprefetch・prerenderは204。情報・履歴URLも別対象への選択を行わない。 |
| 41 | 既存の配信準備POST `/cast/booths/:booth_id/stream_sessions` を受けた時 | 対象固定の準備入口 | Cast::Booths::StreamSessionsController#create。URL対象を認可・照合し、現在の別ブースへ読み替えない。 |

## 共通の接続箇所

- [CurrentSelection](../../app/controllers/concerns/current_selection.rb)でログインsessionを読み書きし、同じ結果をヘッダーと本文へ渡す。拒否結果は保存しない。
- [CurrentSelectionService](../../app/services/current_selection_service.rb)が所属・候補・本人配信をDBで再判定する。選択用のIVS通信は追加しない。
- 戻り先は許可した画面種別から新しい対象のURLへ組み立てる。公開詳細と特定リザルトは元の対象を維持する。
- ドリンク・口座・ブース作成・店舗責任者代行登録・管理者招待の店舗指定がないフォームにはselection_store_idを保持する。欠落は再読込を案内し、不正は拒否、不一致は409。URL/レコードに対象IDがある更新は、その対象の認可後に照合する。
- 招待の共有済み/取消は発行済み招待IDに対する処理であり、選択変更で別招待へ読み替えない。配信終了・取消も元の配信ID/Y/世代を維持する。
- 店舗初回設定のpending店舗IDと招待初回編集のブースIDは、現在選択を業務対象に代替しない。新規登録の基本導線を保持する。
- 問い合わせの店舗文脈補助、公開リストの絞り込みは管理選択を保存しない。認証セッション自体のログアウト/破棄も通常の手動切替と区別する。

## 確認方法

- preparation_selection_test：空き/他者の未配信準備/他者配信中・離席中/閉鎖済みへの切替、A準備の保持、直接URL/POST、API失敗、不整合、権限喪失、本人開始後の拒否。
- ivs_publisher_lifecycle_test.cjs：初回開始要求の取消完了待ち、成功確定との競合、取消結果不明、選択失敗時の準備保持、成功時のプレビュー破棄。

- current_selection_ui_test：3役割の候補0/1/複数、本人配信中/離席中、閉鎖済みのみ/混在、固定時のヘッダーと店舗カード非表示、旧一覧GET廃止、JSON選択応答。
- selection_switch_controller_test.cjs：未保存キャンセルでPOSTなし、続行、拒否/通信失敗で入力維持、画像編集状態、通常フォームの変更判定、招待モーダル継続。

- current_selection_entry_test：D01、対象不一致、古いHTML/JSON/Turboフォーム、不正要求、管理者の閉鎖済み、本人固定、候補GET、先読み、公開詳細/リザルト。
- cast/booth_navigation_test：キャスト・店舗管理者・システム管理者の情報/編集/履歴と閉鎖済み、未選択、有効選択保持。
- cast_invitation_flow_test / cast_invitation_broadcast_guard_test：成功通知、初回Bの固定、通常/SMSログイン復帰、配信中拒否、失敗時の維持。
- admin_store_selection_return_test / current_consistency_admin_test：同時保存、D01、同一店舗、無効選択、戻り先。
- publisher_preparation_test / publisher_full_flow_test：共通選択接続後もX準備/Y配信・再接続・終了を維持。外部IVSは模擬応答。
- 店舗登録・代行登録・ドリンク・口座・ブース作成の既存テストを更新し、フォームの元の対象IDと業務処理を確認する。

実端末の映像・音声、ステージング・本番への適用はこのローカル確認に含めない。環境横断の受入確認は#1277で区別して記録する。
