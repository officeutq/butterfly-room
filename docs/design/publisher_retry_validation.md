# 配信接続の再試行・終了結果の検証（#1336 / #1340）

2026-09-17。変更前はmain `1a9faaa`。以下はローカルのテストDBと代替AWS応答による検証であり、ステージング・本番への反映、実機や実IVSでの確認を意味しない。

## 実装の順序

| 子Issue | PR / 対象コミット | 内容 |
| --- | --- | --- |
| #1337 | #1341 / `e0809cb` | 切断上限、予定・回数・最終失敗、開始権、エラーログ |
| #1338 | #1342 / `593206c` | 開始確認の有限再試行、同じ要求の復旧・取消、最終失敗の記録 |
| #1339 | #1343 / `17fc82b` | 終了結果の確認、リザルト表示、強制終了通知、専用再確認ボタンの撤去 |
| #1340 | 本文書を追加したPR | 通信前の試行予約、応答保存障害・重複処理、対象を限定した運用復旧と横断検証 |

各PRは直前のPRを取り込み先にした連続した変更。2026-09-17に利用者の指示を受け、`5dadac4`を[ステージングへ反映](../ops/publisher_retry_staging_verification.md)した。2026-09-18に#1341→#1342→#1343→#1344の順でmainへマージし、mainは`be8da1c`。ステージングから実行コードの差分はなく、マージ後のCIも成功。本番は未反映。

## 自動テストで確認する境界

| 対象 | 確認内容 | 主なテスト |
| --- | --- | --- |
| 開始確認 | 初回・追加各回の成功、3回後の停止、同じ要求の状態確認、成功済みの取消禁止、古い画面・世代の遅延応答 | `ivs_publisher_lifecycle_test.cjs`、`publisher_confirmation_failure_test.rb`、`publisher_connections_test.rb` |
| 切断 | 初回＋3回、0.5秒・1秒・2秒、予定前の重複実行、上限後のジョブ・旧入口による再開禁止、旧4回以上の記録 | `disconnect_publisher_connection_service_test.rb` |
| DB・外部通信 | 外側transaction（DBの一連処理）取消時にAWSを呼ばない。通信前の予約を確定し、4回とも応答保存が失敗しても5回目を送らない | 同上、`publisher_connection_concurrency_test.rb` |
| 複数操作 | 重複ジョブ、2端末の復帰、開始と取消、終了と閉鎖・退会、同一人物の複数ブース、アカウント・リージョンの切断枠 | 同上、`publisher_reconnection_test.rb`、`publisher_ending_test.rb` |
| 通常終了 | 手元の映像・音声停止、同じ終了要求の有限再確認、終了結果不明での成功遷移禁止、終了・返却の重複防止 | `ivs_publisher_lifecycle_test.cjs`、`publisher_ending_test.rb` |
| 強制終了・リザルト | 同じセッションの送信だけ停止、別セッションに影響しない、切断待ちと最終失敗、遅い旧画面応答の無視、認可付きGETは切断しない | `publisher_result_test.cjs`、`publisher_ending_test.rb` |
| 実配信者と消化 | 実配信者を維持し、準備者と配信者の区別、本人だけの消化、終了時の未消化返却 | `publisher_full_flow_test.rb`、既存の配信・消化テスト |
| ログ | 最終失敗の要求UUID・人物・店舗・セッション、通常再実行での重複抑止、秘密情報の除外、一般ユーザーの閲覧拒否、保存障害の代替記録 | `disconnect_publisher_connection_service_test.rb`、`publisher_confirmation_failure_test.rb`、`error_subscriber_test.rb`、`system_admin_logs_test.rb` |
| 運用復旧 | 既定は表示のみ、環境・DB・保存済み対象の一致、失敗しても回数を戻さず、成功時だけ解放、終了・人物を再更新しない | `publisher_disconnect_runner_test.rb`、`disconnect_publisher_connection_service_test.rb` |

AWS SDK（AWSを呼ぶライブラリ）の切断クライアントは`retry_limit: 0, max_attempts: 1`、接続・読込の期限は各5秒。テスト用クライアントで設定が受理されることを確認した。実AWSの呼出回数・通信時間を実測した結果ではない。同じDBの切断枠は0.25秒単位で、別DBの環境や運用ツールとは共有しない。

## ローカル実行方法と結果

全体テストでは既存の旧仕様テストも含むため、ローカルの`.env`から配信制御の有効化を引き継がず、既定の無効状態に合わせる。今回の配信制御テストは個別に有効化し、AWSを代替応答へ差し替えて実行する。開発用DBで実行しない。

```powershell
docker compose exec -T -e RAILS_ENV=test -e DATABASE_URL=postgres://postgres:postgres@db:5432/butterfly_room_test -e ACTUAL_PUBLISHER_CONTROL_ENABLED=false -e PARALLEL_WORKERS=1 app bundle exec rails test
npm run test:js
docker compose exec -T app bundle exec rubocop
docker compose exec -T app bundle exec brakeman --no-pager
git diff --check
```

Rails全体は1,655件・14,626 assertions（検証）成功、失敗・エラー・スキップ0件。JavaScriptは200件成功。RuboCopは713ファイルで指摘なし。Brakemanは警告なし。`git diff --check`も成功した。

運用runnerの出力捕捉は並列実行時にも入れ子にしない。関連64件を2プロセスで実行し、464検証・失敗／エラー0件を確認した。

代替応答の試験データはテストDBだけを使い、実Stage（AWSの配信ルーム）や開発環境の配信接続に故障状態を作っていない。

## 隔離DBとブラウザーによる切断・復旧の通し確認

2026-09-18 07:33 JST、アプリコード`be8da1c`で実行。専用DB `butterfly_room_retry1340_8eaa9da4`、Windows上のChromium `149.0.7827.55`を使用。AWS応答は検証プロセス内だけで代替する。実際のRails認証・経路・Service・ジョブ・DB・リザルト・エラーログ画面を通すが、IVS SDKによる映像送信は行わない。

| 配信者・終了方法 | AWS代替応答 | 結果 |
| --- | --- | --- |
| キャスト・通常終了 | 初回失敗、再試行1回目成功 | 計2回で解放、エラーログなし、次回開始・終了成功 |
| 店舗管理者・通常終了 | 初回と再試行1回目失敗、2回目成功 | 計3回で解放、エラーログなし、次回開始・終了成功 |
| システム管理者・通常終了 | 再試行3回目で成功 | 計4回で解放、エラーログなし、次回開始・終了成功 |
| 店舗管理者・通常終了 | 初回＋3回とも失敗 | エラー表示・ログ1件、上限維持、対象1件の運用復旧後に再配信成功 |
| 店舗管理者・システム管理者による強制終了 | 初回＋3回とも失敗 | 終了・返却確定、エラー表示・ログ1件、上限維持、運用復旧後に再配信成功 |

- 再試行の実測間隔は1回目0.549〜0.662秒、2回目1.101〜1.176秒、3回目2.039〜2.144秒。ジョブは保存された予定時刻を過ぎてから実行し、DBの時刻・回数を書き換えていない。時刻・回数・対象IDは`tmp/retry1340-8eaa9da4/result.json`に記録した。
- リザルトの「再試行中」と赤い最終失敗表示、専用の再確認ボタンがないことを確認。PC幅1440と幅390の画面を保存・目視確認した。狭い幅の確認はスマートフォン実機確認として扱わない。
- 最終失敗後、別タブ・再読込・再ログイン・回収ジョブ・重複ジョブ・旧APIでも通常試行は4回のまま。次回準備を作るServiceは`publisher_disconnect_pending`で拒否した。
- システム管理者で実際のログ一覧から詳細へ移動し、例外クラス・要求UUID・実行者・店舗・セッションを照合。店舗管理者からの閲覧は拒否。ログに代替トークン文字列が保存されていないことも確認した。
- 既存の`script/recover_publisher_disconnect.rb`を同じ検証プロセス内で実行。既定の表示だけではAWS呼出を増やさず、保存済み環境・DB・Stage・参加者・要求を指定した適用だけが5回目の切断を行った。成功後に解放し、過去の最終失敗日時は保持した。
- 各ケースで未消化ドリンク2件・200ポイントを返却。復旧・次回配信後も元のセッション全列、実配信者、ドリンク、返却台帳、ウォレット、売上台帳が不変。全5ケースと次回分の10セッションを保持し、未終了セッション0・未解放接続0で終了した。
- 関連Railsテスト28件・267検証、JavaScript全200件が成功。開始確認の有限再試行・取消・終了結果不明・ログ保存障害・復旧指定の拒否は既存自動テストで照合した。

再現にはDocker Composeの`app`・`db`、Node.js、インストール済み依存とChromium、生成済みCSSを使用する。ホストの3014番ポートを空けて実行する。

```powershell
node script/verify_publisher_retry.cjs --run
```

毎回`butterfly_room_retry1340_<ランダム8桁>`のDBを新規作成し、空の専用DB・Rails test環境だけを許可する。通常の開発用・共通テスト用・ステージング・本番のDBは使用しない。HTTPはループバックの3014番だけへ公開し、障害設定・運用復旧の操作は子プロセスへの標準入力で行う。アプリに検証用HTTP経路・設定項目を追加していない。

結果JSON・画面は`tmp/retry1340-<同じ8桁>/`へ保存する。検証コンテナは停止・削除するが、専用DBの履歴は照合用に保持する。異常終了時も保存済み接続の復旧を試み、`cleanup.unreleased`と`cleanup.live`が0か確認する。実Stageを作成・切断しない。Solid Queueの実際の遅延やAWS側の呼出枠・応答時間、終了通知によるSDK停止はこの検証では測定していない。

## 開始確認・終了結果不明のブラウザー確認

2026-09-18 08:20 JST、アプリコードは同じ`be8da1c`のまま、専用DB `butterfly_room_retry1340_b8b00793`で実施。`script/verify_publisher_retry.cjs --run`から`script/publisher_retry_browser.cjs`を実行し、Chromium `149.0.7827.55`の実際の配信準備画面でボタンを操作した。

対象はキャスト・店舗管理者・システム管理者の3役割、画面幅1440／390の2種類。次の7ケースを各組合せで確認し、追加42ケースすべて成功。先の切断・復旧5ケースも再実行して成功した。

| ケース | ブラウザー・DBで照合した結果 |
| --- | --- |
| 開始確認が初回に成功 | 配信開始確定、実配信者の保存、エラーなし、通常終了でリザルト遷移 |
| 開始確認が再試行1回目に成功 | 0.5秒以上待ち、同じ要求UUID・世代・トークンで計2回。ログなし |
| 開始確認が再試行2回目に成功 | 0.5秒・1秒以上の待機、同じ要求で計3回。ログなし |
| 開始確認が再試行3回目に成功 | 0.5秒・1秒・2秒以上の待機、同じ要求で計4回。ログなし |
| 開始確認が初回＋3回とも失敗 | エラー表示・本人の要求に対応するログ1件、取消1回・保存済み接続の解放。実配信者・開始時刻は未設定。映像・音声停止、再確認ボタンなし。その後の通常の開始操作で成功し、ログは増えない |
| 終了要求がサーバーへ届かない | 同じ終了要求を計4回で停止。成功扱いで遷移せず、終了結果不明のエラー表示。映像・音声停止、開始ボタン無効。DBは配信中・未消化を維持し、検証後の管理終了で返却 |
| 終了・返却は確定したが応答が届かない | 同じ終了要求を計4回で停止し、同じ終了結果不明の表示。DBは終了済み・返却済みを維持。画面から終了処理を再呼出しても5回目を送らず、後処理でも二重返却しない |

開始の失敗応答はPlaywrightで対象の開始確認HTTPだけに503を返した。成功回・本人状態照会・最終失敗の記録・取消・終了は実際のRailsへ送る。終了結果不明は、要求送信前の遮断と、最初の要求をRailsへ送り終了を確定した後の応答遮断を分けた。SDKの配信通知と画面加工部分は検証用の代替実装、映像はcanvas、音声はChromiumの仮想デバイスを使用する。配信制御JS、再試行間隔、エラー表示、ボタン状態、認可・業務処理は差し替えていない。実IVS送信・Banubaの加工・スマートフォン実機の成功としては数えない。

結果は`tmp/retry1340-b8b00793/result.json`、エラー画面は同ディレクトリのPNGに保存。代表的なPC幅・幅390の画面を目視確認した。終了結果不明の各ケースでは未消化2件200ポイントを最終的に1回だけ返却し、実配信者を保持。検証履歴52セッションを保持し、未解放接続0・未終了セッション0で終了した。これにより、前回残していた開始確認の異常系・終了結果不明のブラウザー通し確認を完了とする。

## 実機・実AWSで残る確認

以下の全組合せの確認は未完了。2026-09-18、利用者からステージングでの配信開始・通常終了・強制終了が問題なく、操作に伴うエラーログもないと報告を受けた。その後の役割・端末の回答と、案内した3操作への回答を次のように記録する。

| 配信端末・役割 | 強制終了を操作する端末・役割 | 確認済みの操作 |
| --- | --- | --- |
| PC・店舗管理者 | PC・システム管理者 | 配信開始・通常終了・強制終了 |
| iPhone 15 Pro・キャスト | PC・システム管理者 | 配信開始・通常終了・再配信後の強制終了。案内した映像・音声とリザルト遷移を含む正常動作の報告 |
| スマートフォン・店舗管理者 | PC・システム管理者 | 同じ3操作（配信開始・通常終了・再配信後の強制終了）が正常だったとの報告 |

追加報告は「iPhone15pro:キャスト、PCシステム管理者で正常でした。」と「スマホ・店舗管理者でも正常にできました。」。店舗管理者のスマートフォン機種と、各ブラウザー名は今回未申告。[報告とログ照合の記録](../ops/publisher_retry_staging_verification.md#利用者の確認結果と残項目)を参照する。PCのキャスト、システム管理者自身によるPC／スマートフォンでの配信操作は未確認として残す。以前の#1277等の実機結果を今回変更部分の確認済みとして転用しない。

1. 準備から開始し、映像・音声が視聴側へ届く。通常終了で送信が止まり、リザルトへ移る。
2. 別ブラウザーから強制終了し、配信側への通知・送信停止・リザルト遷移、視聴側の終了を確認する。
3. 再接続・開始取消後に、正常な次の開始ができる。別タブ・再読込・再ログインで未確認接続の上限を戻せない。
4. 開始確認の各再試行成功・上限到達・取消後の再開始、終了結果そのものの不明表示は、上記の3役割×2画面幅のブラウザー通し確認で完了。切断の各再試行・上限後のリザルトも隔離検証で確認済み。実端末・実AWSの障害を発生させた検証ではない。
5. システム管理者のエラーログから[運用復旧手順](../ops/publisher_retry_recovery.md)を実行する流れは、上記隔離環境で確認済み。ステージングや本番の実接続を意図的に失敗させて復旧した結果ではない。

実施時は対象コミット・環境・役割・端末・ブラウザー・確認時刻・結果を追記する。実利用者の配信へ障害を注入しない。終了通知とAWS切断の両方が失敗する場合の即時停止は保証せず、新たな配信継続監視は追加していない。

#1340は上記の未実施項目が残るため、この文書追加だけでDoneにしない。
