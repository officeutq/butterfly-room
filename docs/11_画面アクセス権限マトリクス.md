# 画面アクセス権限マトリクス

## 1. 目的

現行実装における、role（権限種別）ごとの画面アクセス可否を一覧化する。

この文書は「どの role で、どの画面を表示できるか」を確認するための資料である。画面内の更新・削除などの操作には、表示権限に加えて対象データ、所属、配信状態などの条件が適用されるため、主要な条件を「条件・補足」列と「7. 画面内の主な操作条件」に記載する。

最終確認日: 2026-09-18（#1291〜#1293の管理統合を反映）

### 選択ルールの改修（#1271〜#1276）

2026-09-16に共通判定・サーバー入口・ヘッダー・準備切替をローカル実装済み。[入口対応表](design/current_selection_entries.md)と[確認記録](ops/current_selection_local_verification.md)を参照。選択改修の環境反映は[ステージング記録](ops/current_selection_staging_verification.md)、今回の管理統合の確認範囲は[横断検証記録](design/booth_management_validation.md)を参照。

下表の選択条件は[共通契約](design/current_selection.md)に従う。D01・D02の店舗変更・キャスト招待承認成功時の例外も接続済み。管理用店舗情報・ブース情報への統合も下表に反映する。

| 改修する経路 | 改修後の追加条件 | 担当 |
| --- | --- | --- |
| ブース・店舗の手動選択 | 候補0／1件、本人配信中・離席中は導線なし。サーバーでも固定を検証 | #1273〜#1275 |
| キャスト旧ブース一覧 | 共通モーダルへ集約。旧GETは認可後ダッシュボードへ | #1275・ローカル実装済み |
| 管理者の旧ブース管理一覧 | 旧GETは認証・役割確認後にダッシュボードへ。新規作成は独立カードから共通店舗選択を利用 | #1293 |
| 管理用情報・編集・履歴 | URL対象を認可し、選択と不一致なら変更せず案内。古い保存は409 | #1274・#1275 |
| 管理者の閉鎖済みブース | 候補への追加と選択、準備・開始・編集の拒否は接続済み。情報画面の共有非表示・履歴ボタン表示と認可は接続済み。キャストの閉鎖済み選択は不可 | #1273〜#1276・表示は#1292 |
| キャスト招待承認 | 本人配信中・離席中なら確認画面と承認直前で拒否。準備のみは同一視しない | #1279 |
| 店舗情報・各編集 | 管理用店舗情報へ編集入口・保存後の戻り先を集約。公開店舗詳細の編集リンク・専用復帰は廃止。情報と各編集で対象の認可・選択照合を適用 | #1251・#1252 |

公開閲覧の認可は広げない。公開詳細に残す編集リンクは本人プロフィールのみ。#1252で公開店舗詳細の編集リンク・専用復帰を廃止した。

## 2. 記号

| 記号 | 意味 |
| --- | --- |
| ○ | role だけで画面を表示できる。URL中の対象データが存在することなど、通常の前提は必要 |
| △ | role に加えて、所属、選択中の Store（店舗）/ booth（ブース）、招待 token（トークン）、公開状態などの条件が必要 |
| × | 画面を表示できない。実装によりログイン画面などへの redirect（転送）、`403 Forbidden`、`404 Not Found` のいずれかになる |

role は、原則として次の階層で下位 role の権限を包含する。

```text
system_admin > store_admin > cast > customer
```

ただし、退会、利用者向け問い合わせ、招待承認、営業支援会社向け代行登録など、特定 role だけを対象とする例外がある。

## 3. 全体概要

| 画面区分 | 未ログイン | customer（視聴者） | cast（配信者） | store_admin（店舗管理者） | system_admin（運営） |
| --- | :---: | :---: | :---: | :---: | :---: |
| 公開ページ | ○ | ○ | ○ | ○ | ○ |
| ログインユーザー共通画面 | × | ○ | ○ | ○ | ○ |
| booth詳細・視聴画面 | △ | ○ | ○ | ○ | ○ |
| お気に入り・ポイント画面 | × | ○ | ○ | ○ | ○ |
| cast 画面（`/cast/...`） | × | × | △ | △ | △ |
| Store 管理画面（`/admin/...`） | × | × | × | △ | △ |
| system_admin 画面（`/system_admin/...`） | × | × | × | × | ○ |

`△` の主な理由は、cast 画面では担当 booth、store_admin 画面では管理対象 Store、system_admin が Store 管理画面を使う場合は操作対象 Store の選択が必要になるためである。

## 4. 公開・アカウント関連画面

| 画面 | 主な path（パス） | 未ログイン | customer | cast | store_admin | system_admin | 条件・補足 |
| --- | --- | :---: | :---: | :---: | :---: | :---: | --- |
| ホーム・検索 | `/` | ○ | ○ | ○ | ○ | ○ | 未ログイン時は Store、ログイン時は booth が初期タブ |
| ログイン・新規登録案内 | `/welcome` | ○ | △ | △ | △ | △ | ログイン済みの場合は `/` へ redirect |
| ログイン | `/users/sign_in` | ○ | △ | △ | △ | △ | ログイン済みの場合の扱いは Devise（認証ライブラリ）の制御に従う |
| パスワード再設定申請 | `/users/password/new` | ○ | △ | △ | △ | △ | ログイン済みの場合の扱いは Devise の制御に従う |
| パスワード再設定 | `/users/password/edit` | △ | △ | △ | △ | △ | 有効な reset token（再設定用トークン）が必要 |
| SMS（ショートメッセージ）ログイン | `/phone_session` | ○ | ○ | ○ | ○ | ○ | 現行 Controller（リクエストを受ける層）にログイン済みユーザーを拒否する guard はない |
| SMS ログインコード確認 | `/phone_session/confirm` | △ | △ | △ | △ | △ | 電話番号登録、OTP（ワンタイム認証コード）、認証途中の session（セッション）が必要 |
| 店舗向け LP（ランディングページ） | `/stores/lp`、`/stores/lp_202607`、`/stores/lp_202609` | ○ | ○ | ○ | ○ | ○ | 公開ページ |
| 店舗向け FAQ（よくある質問） | `/stores/faq` | ○ | ○ | ○ | ○ | ○ | 公開ページ |
| 店舗向け問い合わせ | `/stores/contact` | ○ | × | × | × | × | 未ログイン専用。ログイン済みの場合は `/dashboard` へ redirect |
| 店舗向け問い合わせ完了 | `/stores/contact/thanks` | △ | × | × | × | × | 未ログイン専用。問い合わせ完了 session が必要 |
| 法務ページ | `/legal`、`/terms`、`/privacy_policy`、`/payment_services_act` | ○ | ○ | ○ | ○ | ○ | 公開ページ |
| 公開 Store 詳細 | `/stores/:id` | △ | △ | △ | △ | △ | `published` の Store のみ。BAN（利用禁止）対象の customer は対象 Store を表示不可 |
| booth 詳細・視聴 | `/booths/:id` | △ | △ | △ | △ | △ | `active` かつ公開 Store 配下の booth のみ。未ログインはlive / awayの映像とコメントを読み取り専用で閲覧でき、在室・視聴者数には含めない。BAN 対象の customer は表示不可 |
| booth・配信共有 | `/booths/:id/share`、`/booths/:id/share?stream=...` | △ | △ | △ | △ | △ | `active` かつ公開 Store 配下の booth のみ。認証状態やUser-Agentによらず同じ公開情報とOGPを返し、通常boothへJavaScript遷移する。`noindex` |
| ユーザープロフィール | `/users/:id` | △ | △ | △ | △ | △ | 下記「プロフィール対象別の閲覧条件」を適用する。運営にも他人の閲覧の特例はない。関連情報は公開 Store・active boothに限定 |
| customer 新規登録 | `/sign_up` | ○ | ○ | ○ | ○ | ○ | 現行 Controller にはログイン済みユーザーを拒否する guard（防御条件）がない。登録完了時は新規 customer でログインする |
| Store・store_admin 新規登録 | `/stores/new_registration` | ○ | ○ | ○ | ○ | ○ | 現行 Controller にはログイン済みユーザーを拒否する guard がない。アカウントと非公開Storeの作成後は新規 store_admin でログインする |
| 初回店舗設定・公開 | `/admin/stores/:store_id/registration_setup/edit` | × | × | × | △ | △ | 店舗登録直後のpending session、path・current Store・pendingのStore ID一致が必要。store_adminは対象Storeのadmin membershipも必要 |
| Store・store_admin 登録完了 | `/stores/registration/thanks` | × | × | × | △ | △ | 初回店舗設定・公開成功後のcompletion session、対象Storeとcurrent Storeの一致、管理権限が必要。completionは初回表示で消費する |
| cast 招待確認 | `/cast_invitations/:token` | △ | △ | △ | △ | △ | 有効な token が必要。招待承認は `cast` としてログインしている本人だけ |
| cast 招待経由の新規登録 | `/cast/sign_up?token=...` | △ | △ | △ | △ | △ | 有効な cast 招待 token が必要。登録完了時は新規 cast でログインする |
| store_admin 招待確認 | `/store_admin_invitations/:token` | △ | △ | △ | △ | △ | 有効な token が必要。招待承認は `store_admin` としてログインしている本人だけ |
| store_admin 招待経由の新規登録 | `/store_admin/sign_up?token=...` | △ | △ | △ | △ | △ | 有効な store_admin 招待 token が必要。登録完了時は新規 store_admin でログインする |


### プロフィール対象別の閲覧条件（#1213）

| プロフィールの対象 | 未ログイン | ログイン済みの他人 | 本人 |
| --- | :---: | :---: | :---: |
| 有効なキャスト | ○ | ○ | ○ |
| 通常の店舗管理者 | × | ○ | ○ |
| 営業支援会社の管理者 | × | × | ○ |
| 視聴者 | × | ○ | ○ |
| 運営 | × | × | ○ |
| 削除・退会済みユーザー | × | × | × |

- 削除・退会済みを本人例外より優先して除外する。閲覧条件外・不存在の詳細は404。退会済みのログイン自体は既存の認証層で拒否される。
- 通常店舗管理者の閲覧条件は店舗の公開状態・ブースの有無を問わない。
- 営業支援会社の管理者は、roleがstore_adminで、sales_support_company=trueの店舗へmembership_role=adminで所属する人。通常店舗との兼任も含む。cast所属だけでは該当しない。
- 「ログイン済みの他人」には運営も含む。プロフィール編集は本人のみとし、既存の運営用アカウント管理の機能・権限は変更しない。
- お気に入りユーザーの一覧・検索・登録も同じ閲覧条件を適用する。非表示になった登録は保持し、再び閲覧可能になれば表示する。閲覧不可でも自分の登録は解除でき、非公開プロフィール内容を応答しない。
- コメント名は未ログインでは全員リンクなし。ログイン済みは上表と本人例外を適用し、閲覧不可は文字だけ表示する。非表示コメントの既存制御を維持する。
- コメントからの導線制限は、未ログインによる有効なキャストの詳細URL・トップからのプロフィール閲覧を禁止しない。
- 未ログイン公開対象のキャスト以外のプロフィールにはnoindex,nofollowを設定する。

## 5. ログインユーザー共通・視聴者向け画面

| 画面 | 主な path | 未ログイン | customer | cast | store_admin | system_admin | 条件・補足 |
| --- | --- | :---: | :---: | :---: | :---: | :---: | --- |
| dashboard（ダッシュボード） | `/dashboard` | × | ○ | ○ | ○ | ○ | role、選択中の Store / booth、所属数に応じて表示カードが変わる |
| お知らせ一覧・詳細 | `/notifications`、`/notifications/:id` | × | ○ | ○ | ○ | ○ | 対象 role・ユーザーへ公開されたお知らせだけ表示 |
| プロフィール編集 | `/profile/edit` | × | ○ | ○ | ○ | ○ | 自分のプロフィールのみ。本人の詳細に編集リンクを表示し、通常保存後も本人の詳細へ戻る |
| メールアドレス変更 | `/email_change/edit` | × | ○ | ○ | ○ | ○ | プロフィール編集内のモーダル。自分のメールアドレスのみ、現在のパスワード必須 |
| 電話番号認証 | `/phone_verification`、`/phone_verification/confirm` | × | ○ | ○ | ○ | ○ | プロフィール編集内のモーダル。本人に発行されたコードと認証途中の session が必要 |
| 退会確認 | `/account_withdrawal` | × | ○ | ○ | ○ | × | system_admin は退会不可 |
| お気に入り一覧 | `/favorites/booths`、`/favorites/stores`、`/favorites/users` | × | ○ | ○ | ○ | ○ | 自分のお気に入りだけ表示。ユーザーは現在の閲覧者がプロフィールを閲覧できる対象のみ |
| ポイント購入 | `/wallet/purchases/new` | × | ○ | ○ | ○ | ○ | 現行 Controller はログイン済みの全 role を許可 |
| 利用者向け問い合わせ一覧・作成・詳細 | `/support/inquiries`、`/support/inquiries/new`、`/support/inquiries/:id` | × | ○ | ○ | ○ | × | `customer`、`cast`、`store_admin` の完全一致で許可。詳細は本人が作成した問い合わせのみ |

### 5.1 booth 入口の role 別動作

`/booths/:id/enter` は、role によって表示・遷移が変わる。

| role | 動作 |
| --- | --- |
| 未ログイン | `/welcome` へ redirect |
| customer | booth 詳細・視聴画面へ移動 |
| cast | 有効な選択と異なるカードは視聴へ。選択中の担当boothは準備／本人の配信へ復帰。未選択は共通の必要操作の選択条件に従う |
| store_admin | 有効な選択と異なるカードは視聴へ。選択中の管理対象では視聴／配信を選ぶ。管理対象外は視聴 |
| system_admin | 有効な選択と異なるカードは視聴へ。選択中では視聴／配信を選ぶ |

## 6. role 別管理画面

### 6.1 cast 画面

`/cast/...` は `cast` 以上の role が入口へアクセスできる。個別 booth へのアクセス範囲は次のとおり。

- cast: `BoothCast` で本人に紐づく booth
- store_admin: `StoreMembership` の `membership_role: admin` で管理する Store の booth
- system_admin: 全 booth

| 画面 | 主な path | 未ログイン | customer | cast | store_admin | system_admin | 条件・補足 |
| --- | --- | :---: | :---: | :---: | :---: | :---: | --- |
| 旧booth一覧 | `/cast/booths` | × | × | ○ | ○ | ○ | ダッシュボードへ戻す。選択画面は表示しない |
| booth選択モーダル | `/cast/booths/select_modal` | × | × | △ | △ | △ | 候補複数・本人未配信のみ。通常の変更はヘッダー、未選択の必要操作も同じモーダル。固定時は候補を表示しない |
| booth 情報 | `/cast/booths/:id` | × | × | △ | △ | △ | 対象権限と選択一致が必要。キャストの担当する閉鎖済みは例外として選択を変えず直接閲覧可能。管理者は閉鎖済みも選択可能。閉鎖済みの操作は履歴のみ、管理権限があれば切断状態も表示（#1291・#1292） |
| booth 編集 | `/cast/booths/:id/edit` | × | × | △ | △ | △ | `active` な操作可能 booth のみ。cast は名称・説明・画像を編集可能。初回キャスト紐づけは store_admin 以上のみ |
| 配信画面 | `/cast/booths/:id/live` | × | × | △ | △ | △ | `active` な操作可能 booth かつ現在の stream_session（配信セッション）が必要 |
| 配信履歴 | `/cast/booths/:booth_id/stream_sessions` | × | × | △ | △ | △ | URL対象の操作権限と選択一致を照合し、選択を上書きしない。キャストの閉鎖済み履歴は既存権限で直接閲覧し選択を維持 |
| 配信結果 | `/cast/stream_sessions/:id` | × | × | △ | △ | △ | 対象 booth の操作権限と、終了済み stream_session が必要 |

### 6.2 Store 管理画面

`/admin/...` は原則として `store_admin` 以上がアクセスできる。多くの画面で current_store（選択中店舗）が必要になる。

| 画面 | 主な path | 未ログイン | customer | cast | store_admin | system_admin | 条件・補足 |
| --- | --- | :---: | :---: | :---: | :---: | :---: | --- |
| Store 一覧・選択 | `/admin/stores`、`/admin/stores/select_modal` | × | × | × | ○ | ○ | store_admin は管理 Store のみ、system_admin は全 Store |
| 管理用の店舗情報 | `/admin/stores/:id` | × | × | × | △ | △ | URL対象の管理権限と選択一致が必要。公開／非公開とも表示可能。基本情報・全ドリンク・現在の伏字口座を表示。公開詳細には管理情報を出さない（#1251） |
| Store 情報編集 | `/admin/stores/:id/edit` | × | × | × | △ | △ | 両役割とも対象Storeと選択の一致が必要。store_admin は管理Storeのみ。通常の戻る・保存後は公開／非公開とも管理用店舗情報。公開詳細に編集リンクはない（#1252） |
| 営業支援会社による Store 代行登録 | `/admin/stores/new` | × | × | × | △ | × | `sales_support_company` の Store を管理する、完全一致の `store_admin` のみ |
| 旧booth一覧GET | `/admin/booths` | × | × | × | ○ | ○ | 店舗未選択でもダッシュボードへ戻す。一覧・選択モーダルを表示しない |
| booth 新規作成 | `/admin/booths/new`、POST `/admin/booths` | × | × | × | △ | △ | 管理可能なcurrent_storeが必要。既存ブースの選択は不要。作成対象店舗を表示し古いフォームの店舗不一致は409 |
| 所属キャスト・招待一覧（閲覧専用） | `/admin/casts`、`/admin/casts?tab=invitations` | × | × | × | △ | △ | current_store が必要 |
| キャスト招待モーダル | `/admin/cast_invitations/new`、発行・メモ更新・取消・共有完了 | × | × | × | △ | △ | 対象店舗の管理権限が必要。旧一覧GETは招待一覧タブへ転送 |
| store_admin 招待 | `/admin/store_admin_invitations` | × | × | × | △ | △ | current_store が必要 |
| 営業支援会社による store_admin 代行登録 | `/admin/store_admin_invitations/proxy_registration/new` | × | × | × | △ | × | 営業支援会社の管理権限を持ち、かつ current_store も管理する、完全一致の `store_admin` のみ |
| ドリンクメニュー管理 | `/admin/drink_items` | × | × | × | △ | △ | current_store が必要 |
| 売上一覧 | `/admin/sales` | × | × | × | △ | △ | current_store が必要 |
| 配信者別数値一覧 | `/admin/cast_metrics` | × | × | × | △ | △ | current_store が必要 |
| 通報一覧 | `/admin/comment_reports` | × | × | × | △ | △ | current_store が必要 |
| 振込先口座設定 | `/admin/payout_account/edit` | × | × | × | △ | △ | current_store が必要 |
| 精算予定・履歴・詳細 | `/admin/settlements`、`/admin/settlements/:id` | × | × | × | △ | △ | current_store が必要。店舗側には `confirmed`、`exported`、`paid` の精算だけを表示 |
| Store BAN 管理 | `/admin/store_bans` | × | × | × | × | △ | system_admin 専用。current_store が必要 |

system_admin が `/admin/...` へ直接アクセスした時に current_store が未選択の場合は、`/admin/stores` へ redirect して Store 選択を求める。

### 6.3 system_admin 画面

`/system_admin/...` は system_admin だけがアクセスできる。

| 画面 | 主な path | 未ログイン | customer | cast | store_admin | system_admin | 条件・補足 |
| --- | --- | :---: | :---: | :---: | :---: | :---: | --- |
| ユーザー管理 | `/system_admin/users`、`/system_admin/users/new`、`/system_admin/users/:id/edit` | × | × | × | × | ○ | 最後の system_admin 保護など、更新操作には追加制約あり |
| システムログ | `/system_admin/error_logs`、`/system_admin/change_logs`、各 `/:id` | × | × | × | × | ○ | 一覧・検索・詳細の参照のみ。アーカイブ済みも参照可能。編集・削除なし（#1257） |
| 紹介コード管理 | `/system_admin/referral_codes`、`/system_admin/referral_codes/new`、`/system_admin/referral_codes/:id/edit` | × | × | × | × | ○ |  |
| Effect 管理 | `/system_admin/effects`、`/system_admin/effects/new`、`/system_admin/effects/:id/edit` | × | × | × | × | ○ |  |
| お知らせ管理 | `/system_admin/notifications`、`/system_admin/notifications/new`、`/system_admin/notifications/:id/edit` | × | × | × | × | ○ |  |
| 店舗向け問い合わせ管理 | `/system_admin/store_contact_submissions`、`/system_admin/store_contact_submissions/:id` | × | × | × | × | ○ |  |
| LP 行動分析 | `/system_admin/lp_analytics`、`/system_admin/lp_analytics/visits/:public_id` | × | × | × | × | ○ |  |
| 利用者問い合わせ管理 | `/system_admin/support_inquiries`、`/system_admin/support_inquiries/:id` | × | × | × | × | ○ | 利用者向け `/support/inquiries` とは別画面 |
| 精算管理 | `/system_admin/settlements`、`/system_admin/settlements/:id` | × | × | × | × | ○ | 確定、支払済み変更、月次生成などの操作は状態条件あり |
| マニュアル精算 | `/system_admin/settlements/manual/new` | × | × | × | × | ○ | preview（プレビュー）後に作成 |
| 振込 CSV 管理 | `/system_admin/settlement_exports`、`/system_admin/settlement_exports/:id` | × | × | × | × | ○ |  |

## 7. 画面内の主な操作条件

### 実配信者による追加判定（#1280の目標仕様）

以下のロール・所属による基本権限に加え、配信中の本人操作は保存済みの実配信者と現在要求で判定する。準備作成者であることを配信者本人の権限へ読み替えない。未配信準備は権限者が再利用・終了でき、実配信者の未確定だけでは拒否しない。

配信中の通常終了・離席・復帰は本人、管理者強制終了は既存の管理権限と対象世代で判定する。独立した管理者のコメント権限を維持する。不明な人物に本人権限やプロフィールリンクを付与しない。閉鎖・退会済みの切断再試行は保存済みの切断意図に限定し、新しい配信を終了する入口にしない。詳細な認可表は [実配信者設計5節](design/actual_publisher.md) を参照。各実装の有効化前は現行コードの条件で動作する。

画面を表示できても、次の操作は追加条件を満たさない場合に実行できない。

| 操作 | 許可される role | 主な追加条件 |
| --- | --- | --- |
| お気に入り登録・解除 | ログイン済みの全 role | 対象の Store、booth、User が存在すること |
| 配信視聴 | 未ログイン、ログイン済みの全 role | `active` かつ公開 Store 配下のboothで、current stream_session一致かつlive / awayであること。未ログインは購読専用で在室・視聴者数の対象外。BAN対象customerは不可 |
| コメント閲覧 | 未ログイン、ログイン済みの全 role | 公開中の対象へアクセスできること。未ログインも読み取り専用でリアルタイム更新を受信する。BAN対象customerは不可 |
| コメント投稿・通報、ドリンク送信 | ログイン済みの全 role | 有効なstream_session等の条件を満たし、customerは対象StoreからBANされていないこと。未ログインは状態変更せずログイン要求モーダルを表示 |
| cast として booth に入る | cast、store_admin、system_admin | cast は担当 booth、store_admin は管理 Store の booth、system_admin は全 booth。ほかの booth で配信中、または他者が使用中の場合は開始不可 |
| publisher token（配信者用トークン）取得 | cast、store_admin、system_admin | cast は原則として booth の primary cast（主担当キャスト）。store_admin は管理 Store の booth。system_admin は全 booth |
| booth 編集 | cast、store_admin、system_admin | cast は担当 booth、store_admin は管理 Store の booth、system_admin は全 booth |
| booth 作成・アーカイブ・強制終了 | store_admin、system_admin | 管理可能な current_store と booth が必要。アーカイブ・終了は booth / stream_session の状態条件あり |
| Store 情報編集 | store_admin、system_admin | store_admin は管理 Store、system_admin は全 Store |
| 初回店舗設定の保存・公開 | store_admin、system_admin | 店舗登録直後のpending session、対象Store・current Storeの一致が必要。store_adminは対象Storeのadmin membershipも必要。公開状態はrequestから受け付けずサーバー側で有効化 |
| 店舗別管理操作 | store_admin、system_admin | current_store が必要。store_admin は admin membership（管理者所属）がある Store のみ |
| Store BAN 一覧・直接操作 | system_admin | current_store が必要。BAN 対象は customer |
| 招待承認 | cast または store_admin | 招待種別と完全一致する role でログインし、有効な未使用 token を使用すること |

## 8. 対象外

このマトリクスは利用者がブラウザで操作する主要画面を対象とする。次は対象外とする。

- POST / PATCH / DELETE だけを受け付ける、単独画面を持たない endpoint（エンドポイント）
- Stripe webhook（外部サービスからの通知受信口）
- LP 行動分析のイベント受信 endpoint
- Active Storage、Action Mailbox、Turbo が内部で使用する route
- health check（死活監視）、sitemap、XML、PDF / CSV download（ダウンロード）
- development 環境だけに存在する検証画面と LetterOpener

## 9. 実装上の根拠

画面や認可を変更した場合は、次の実コードとテストを確認してこの表も更新する。

- route 一覧: `config/routes.rb`
- role 階層: `app/models/user.rb`
- 全体の認証・role 判定: `app/controllers/application_controller.rb`
- cast 画面の入口・担当 booth 判定: `app/controllers/cast/base_controller.rb`
- Store 管理画面の入口・current_store 判定: `app/controllers/admin/base_controller.rb`
- system_admin 画面の入口: `app/controllers/system_admin/base_controller.rb`
- booth / stream_session / viewer の認可: `app/services/authorization/booth_policy.rb`、`stream_session_policy.rb`、`viewer_policy.rb`
- role 階層の結合テスト: `test/integration/role_hierarchy_access_test.rb`
- 未ログイン時の認証テスト: `test/integration/authentication_required_test.rb`

要件・基本方針は `02_要件定義書.md`、`03_基本設計_Phase1.md`、`04_Rails設計.md`、`07_モード導線設計.md` を参照する。
