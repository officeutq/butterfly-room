了解、ではそのまま実装GOで 👍

---

## 🌿 ブランチ作成

```bash
git checkout -b fix/viewer-meta-duplicate-count-698
```

---

## ✂️ 実装手順（今回やること）

### ① viewer側の重複countを削除

対象（例）👇

* `_ivs_viewer.html.erb`
* `viewer-meta-side`
* `viewer-meta-viewer-count`

```erb
<div class="viewer-meta-viewer-count">
  <i class="bi bi-people-fill"></i>
  <span data-presence-poll-target="count">-</span>
</div>
```

👉 **これを丸ごと削除**

---

### ② `stream_meta_display` に一本化されていること確認

```erb
<span data-presence-poll-target="count">
```

👉 **これが1箇所だけ存在する状態にする**

---

### ③ 見た目確認

チェックポイント👇

* [ ] viewerでcountが1つだけ表示される
* [ ] 数値が更新される（presence_poll動いてる）
* [ ] レイアウト崩れてない（右寄せズレてないか）
* [ ] standby / live 両方OK

---

### ④ 念のためgrep（Windows環境）

```bash
findstr /s /n "presence-poll-target=\"count\"" app\*.*
```

👉 1箇所だけになってればOK

---

## 🚀 次（完了後）

終わったら👇

* コミット
* PR作成（Closes #698）

まで一気にいけます

---

もし途中で

* count消えた
* 更新されない
* レイアウト崩れた

とか出たら、その時点で止めて貼ってください 👍
