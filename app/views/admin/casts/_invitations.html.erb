<% if @store_cast_invitations.empty? %>
  <p class="alert alert-info">表示できる招待はありません。「キャスト招待URLを発行」から招待できます。</p>
<% else %>
  <div class="referral-codes-list">
    <%= render partial: "invitation_card", collection: @store_cast_invitations, as: :invitation %>
  </div>
<% end %>
